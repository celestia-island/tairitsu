[package]
name = "tairitsu-macros"
version = "0.2.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "^2", features = ["full", "parsing"] }
quote = "^1"
proc-macro2 = "^1"
