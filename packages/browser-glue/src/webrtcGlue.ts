/**
 * webrtc glue — implements the `tairitsu-browser:webrtc` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-glue/../../packages/browser-worlds/wit/generated/webrtc.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for EventHandler */
export type EventHandler = (this: any, ev: any) => any;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for any values */
const _anyHandles = new Map<bigint, any>();
let _nextAny = 1n;

/** Handle table for event-handler values */
const _eventHandlerHandles = new Map<bigint, EventHandler>();
let _nextEventHandler = 1n;

/** Handle table for media-stream values */
const _mediaStreamHandles = new Map<bigint, MediaStream>();
let _nextMediaStream = 1n;

/** Handle table for media-stream-track values */
const _mediaStreamTrackHandles = new Map<bigint, MediaStreamTrack>();
let _nextMediaStreamTrack = 1n;

/** Handle table for promise-void values */
const _promiseVoidHandles = new Map<bigint, Promise<void>>();
let _nextPromiseVoid = 1n;

/** Handle table for rtc-dtls-transport values */
const _rtcDtlsTransportHandles = new Map<bigint, RTCDtlsTransport>();
let _nextRtcDtlsTransport = 1n;

/** Handle table for rtc-ice-candidate values */
const _rtcIceCandidateHandles = new Map<bigint, RTCIceCandidate>();
let _nextRtcIceCandidate = 1n;

/** Handle table for rtc-ice-transport values */
const _rtcIceTransportHandles = new Map<bigint, RTCIceTransport>();
let _nextRtcIceTransport = 1n;

/** Handle table for rtc-rtp-receiver values */
const _rtcRtpReceiverHandles = new Map<bigint, RTCRtpReceiver>();
let _nextRtcRtpReceiver = 1n;

/** Handle table for rtc-rtp-sender values */
const _rtcRtpSenderHandles = new Map<bigint, RTCRtpSender>();
let _nextRtcRtpSender = 1n;

/** Handle table for rtc-rtp-transceiver values */
const _rtcRtpTransceiverHandles = new Map<bigint, RTCRtpTransceiver>();
let _nextRtcRtpTransceiver = 1n;

/** Handle table for string values */
const _stringHandles = new Map<bigint, string>();
let _nextString = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a any value by handle. */
function lookupAny(handle: bigint): any {
  const obj = _anyHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`any handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional any value by handle. */
function lookupOptionAny(handle: bigint | undefined): any | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _anyHandles.get(handle) ?? null;
}

/** Lookup a event-handler value by handle. */
function lookupEventHandler(handle: bigint): EventHandler {
  const obj = _eventHandlerHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-handler handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-handler value by handle. */
function lookupOptionEventHandler(handle: bigint | undefined): EventHandler | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventHandlerHandles.get(handle) ?? null;
}

/** Lookup a media-stream value by handle. */
function lookupMediaStream(handle: bigint): MediaStream {
  const obj = _mediaStreamHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`media-stream handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional media-stream value by handle. */
function lookupOptionMediaStream(handle: bigint | undefined): MediaStream | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamHandles.get(handle) ?? null;
}

/** Lookup a media-stream-track value by handle. */
function lookupMediaStreamTrack(handle: bigint): MediaStreamTrack {
  const obj = _mediaStreamTrackHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`media-stream-track handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional media-stream-track value by handle. */
function lookupOptionMediaStreamTrack(handle: bigint | undefined): MediaStreamTrack | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamTrackHandles.get(handle) ?? null;
}

/** Lookup a promise-void value by handle. */
function lookupPromiseVoid(handle: bigint): Promise<void> {
  const obj = _promiseVoidHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`promise-void handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional promise-void value by handle. */
function lookupOptionPromiseVoid(handle: bigint | undefined): Promise<void> | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _promiseVoidHandles.get(handle) ?? null;
}

/** Lookup a rtc-dtls-transport value by handle. */
function lookupRtcDtlsTransport(handle: bigint): RTCDtlsTransport {
  const obj = _rtcDtlsTransportHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-dtls-transport handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-dtls-transport value by handle. */
function lookupOptionRtcDtlsTransport(handle: bigint | undefined): RTCDtlsTransport | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcDtlsTransportHandles.get(handle) ?? null;
}

/** Lookup a rtc-ice-candidate value by handle. */
function lookupRtcIceCandidate(handle: bigint): RTCIceCandidate {
  const obj = _rtcIceCandidateHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-ice-candidate handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-ice-candidate value by handle. */
function lookupOptionRtcIceCandidate(handle: bigint | undefined): RTCIceCandidate | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcIceCandidateHandles.get(handle) ?? null;
}

/** Lookup a rtc-ice-transport value by handle. */
function lookupRtcIceTransport(handle: bigint): RTCIceTransport {
  const obj = _rtcIceTransportHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-ice-transport handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-ice-transport value by handle. */
function lookupOptionRtcIceTransport(handle: bigint | undefined): RTCIceTransport | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcIceTransportHandles.get(handle) ?? null;
}

/** Lookup a rtc-rtp-receiver value by handle. */
function lookupRtcRtpReceiver(handle: bigint): RTCRtpReceiver {
  const obj = _rtcRtpReceiverHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-rtp-receiver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-rtp-receiver value by handle. */
function lookupOptionRtcRtpReceiver(handle: bigint | undefined): RTCRtpReceiver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpReceiverHandles.get(handle) ?? null;
}

/** Lookup a rtc-rtp-sender value by handle. */
function lookupRtcRtpSender(handle: bigint): RTCRtpSender {
  const obj = _rtcRtpSenderHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-rtp-sender handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-rtp-sender value by handle. */
function lookupOptionRtcRtpSender(handle: bigint | undefined): RTCRtpSender | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpSenderHandles.get(handle) ?? null;
}

/** Lookup a rtc-rtp-transceiver value by handle. */
function lookupRtcRtpTransceiver(handle: bigint): RTCRtpTransceiver {
  const obj = _rtcRtpTransceiverHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`rtc-rtp-transceiver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional rtc-rtp-transceiver value by handle. */
function lookupOptionRtcRtpTransceiver(handle: bigint | undefined): RTCRtpTransceiver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpTransceiverHandles.get(handle) ?? null;
}

/** Lookup a string value by handle. */
function lookupString(handle: bigint): string {
  const obj = _stringHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string value by handle. */
function lookupOptionString(handle: bigint | undefined): string | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-peer-connection
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcPeerConnectionHandle = bigint;

/** Handle table for RTCPeerConnection instances */
const _rtcPeerConnectionhandles = new Map<bigint, RTCPeerConnection>();
let _nextRTCPeerConnection = 1n;

/** Register a new RTCPeerConnection and return its handle. */
function registerRTCPeerConnection(obj: RTCPeerConnection): bigint {
  const handle = _nextRTCPeerConnection++;
  _rtcPeerConnectionhandles.set(handle, obj);
  return handle;
}
/** Lookup a RTCPeerConnection by handle, throwing if not found. */
function lookupRTCPeerConnection(handle: bigint): RTCPeerConnection {
  const obj = _rtcPeerConnectionhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCPeerConnection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCPeerConnection by handle. */
function lookupOptionRTCPeerConnection(handle: bigint | undefined): RTCPeerConnection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcPeerConnectionhandles.get(handle) ?? null;
}
/**
 * `create-offer()` operation.
 *
 * Async operation: returns request ID, poll with `pollCreateOffer()`
 */
export function createOffer(self: bigint, options: RTCOfferOptions | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.createOffer(options)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `createOffer()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollCreateOffer(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `create-answer()` operation.
 *
 * Async operation: returns request ID, poll with `pollCreateAnswer()`
 */
export function createAnswer(self: bigint, options: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.createAnswer(options)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `createAnswer()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollCreateAnswer(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `set-local-description()` operation.
 *
 * Async operation: returns request ID, poll with `pollSetLocalDescription()`
 */
export function setLocalDescription(self: bigint, description: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.setLocalDescription(description as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `setLocalDescription()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollSetLocalDescription(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-local-description()` operation.
 */
export function getLocalDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.localDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-current-local-description()` operation.
 */
export function getCurrentLocalDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.currentLocalDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-pending-local-description()` operation.
 */
export function getPendingLocalDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.pendingLocalDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-remote-description()` operation.
 *
 * Async operation: returns request ID, poll with `pollSetRemoteDescription()`
 */
export function setRemoteDescription(self: bigint, description: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.setRemoteDescription(description as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `setRemoteDescription()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollSetRemoteDescription(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-remote-description()` operation.
 */
export function getRemoteDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.remoteDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-current-remote-description()` operation.
 */
export function getCurrentRemoteDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.currentRemoteDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-pending-remote-description()` operation.
 */
export function getPendingRemoteDescription(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.pendingRemoteDescription;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `add-ice-candidate()` operation.
 *
 * Async operation: returns request ID, poll with `pollAddIceCandidate()`
 */
export function addIceCandidate(self: bigint, candidate: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.addIceCandidate(candidate as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `addIceCandidate()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollAddIceCandidate(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-signaling-state()` operation.
 */
export function getSignalingState(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.signalingState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-ice-gathering-state()` operation.
 */
export function getIceGatheringState(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.iceGatheringState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-ice-connection-state()` operation.
 */
export function getIceConnectionState(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.iceConnectionState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-connection-state()` operation.
 */
export function getConnectionState(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.connectionState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-can-trickle-ice-candidates()` operation.
 */
export function getCanTrickleIceCandidates(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const value = obj.canTrickleIceCandidates;
  return value != null ? (value ? 1n : 0n) : undefined;
}

/**
 * `restart-ice()` operation.
 *
 * Async operation: returns request ID, poll with `pollRestartIce()`
 */
export function restartIce(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = (obj as any).restartIce()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `restartIce()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollRestartIce(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `get-configuration()` operation.
 */
export function getConfiguration(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.getConfiguration();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-configuration()` operation.
 */
export function setConfiguration(self: bigint, configuration: bigint | undefined): void {
  const obj = lookupRTCPeerConnection(self);
  obj.setConfiguration(lookupOptionAny(configuration));
}

/**
 * `close()` operation.
 */
export function RtcPeerConnectionClose(self: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  obj.close();
}

/**
 * `get-onnegotiationneeded()` operation.
 */
export function getOnnegotiationneeded(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onnegotiationneeded;
}

/**
 * `set-onnegotiationneeded()` operation.
 */
export function setOnnegotiationneeded(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onnegotiationneeded = value as any;
}

/**
 * `get-onicecandidate()` operation.
 */
export function getOnicecandidate(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onicecandidate;
}

/**
 * `set-onicecandidate()` operation.
 */
export function setOnicecandidate(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onicecandidate = value as any;
}

/**
 * `get-onicecandidateerror()` operation.
 */
export function getOnicecandidateerror(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onicecandidateerror;
}

/**
 * `set-onicecandidateerror()` operation.
 */
export function setOnicecandidateerror(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onicecandidateerror = value;
}

/**
 * `get-onsignalingstatechange()` operation.
 */
export function getOnsignalingstatechange(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onsignalingstatechange;
}

/**
 * `set-onsignalingstatechange()` operation.
 */
export function setOnsignalingstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onsignalingstatechange = value as any;
}

/**
 * `get-oniceconnectionstatechange()` operation.
 */
export function getOniceconnectionstatechange(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).oniceconnectionstatechange;
}

/**
 * `set-oniceconnectionstatechange()` operation.
 */
export function setOniceconnectionstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).oniceconnectionstatechange = value as any;
}

/**
 * `get-onicegatheringstatechange()` operation.
 */
export function getOnicegatheringstatechange(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onicegatheringstatechange;
}

/**
 * `set-onicegatheringstatechange()` operation.
 */
export function setOnicegatheringstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onicegatheringstatechange = value as any;
}

/**
 * `get-onconnectionstatechange()` operation.
 */
export function getOnconnectionstatechange(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).onconnectionstatechange;
}

/**
 * `set-onconnectionstatechange()` operation.
 */
export function setOnconnectionstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).onconnectionstatechange = value as any;
}

/**
 * `generate-certificate()` operation.
 *
 * Async operation: returns request ID, poll with `pollGenerateCertificate()`
 */
export function generateCertificate(keygenAlgorithm: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const promise = RTCPeerConnection.generateCertificate(keygenAlgorithm as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `generateCertificate()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollGenerateCertificate(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-senders()` operation.
 */
export function getSenders(self: bigint): (bigint)[] {
  const obj = lookupRTCPeerConnection(self);
  const _arr = (obj as any).getSenders();
  const _handles: bigint[] = [];
  for (const item of _arr) {
    const handle = _nextRtcRtpSender++;
    _rtcRtpSenderHandles.set(handle, item);
    _handles.push(handle);
  }
  return _handles;
}

/**
 * `get-receivers()` operation.
 */
export function getReceivers(self: bigint): (bigint)[] {
  const obj = lookupRTCPeerConnection(self);
  const _arr = obj.getReceivers();
  const _handles: bigint[] = [];
  for (const item of _arr) {
    const handle = _nextRtcRtpReceiver++;
    _rtcRtpReceiverHandles.set(handle, item);
    _handles.push(handle);
  }
  return _handles;
}

/**
 * `get-transceivers()` operation.
 */
export function getTransceivers(self: bigint): (bigint)[] {
  const obj = lookupRTCPeerConnection(self);
  const _arr = obj.getTransceivers();
  const _handles: bigint[] = [];
  for (const item of _arr) {
    const handle = _nextRtcRtpTransceiver++;
    _rtcRtpTransceiverHandles.set(handle, item);
    _handles.push(handle);
  }
  return _handles;
}

/**
 * `add-track()` operation.
 */
export function addTrack(self: bigint, track: bigint, streams: (bigint)[]): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.addTrack(lookupOptionMediaStreamTrack(track) as any, ...Array.from(streams).map((h: bigint) => lookupMediaStream(h)));
  const handle = _nextRtcRtpSender++;
  _rtcRtpSenderHandles.set(handle, _callResult);
  return handle;
}

/**
 * `remove-track()` operation.
 */
export function removeTrack(self: bigint, sender: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  obj.removeTrack(lookupRtcRtpSender(sender));
}

/**
 * `add-transceiver()` operation.
 */
export function addTransceiver(self: bigint, trackOrKind: bigint, init: bigint | undefined): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.addTransceiver(trackOrKind as any, init as any);
  const handle = _nextRtcRtpTransceiver++;
  _rtcRtpTransceiverHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-ontrack()` operation.
 */
export function getOntrack(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).ontrack;
}

/**
 * `set-ontrack()` operation.
 */
export function setOntrack(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).ontrack = value as any;
}

/**
 * `get-sctp()` operation.
 */
export function getSctp(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.sctp;
  if (_callResult === null) return undefined;
  const handle = _nextRTCSctpTransport++;
  _rtcSctpTransporthandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-data-channel()` operation.
 */
export function createDataChannel(self: bigint, label: string, dataChannelDict: bigint | undefined): bigint {
  const obj = lookupRTCPeerConnection(self);
  const _callResult = obj.createDataChannel(label as any, dataChannelDict as any);
  const handle = _nextRTCDataChannel++;
  _rtcDataChannelhandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-ondatachannel()` operation.
 */
export function getOndatachannel(self: bigint): bigint {
  const obj = lookupRTCPeerConnection(self);
  return (obj as any).ondatachannel;
}

/**
 * `set-ondatachannel()` operation.
 */
export function setOndatachannel(self: bigint, value: bigint): void {
  const obj = lookupRTCPeerConnection(self);
  (obj as any).ondatachannel = value as any;
}

/**
 * `get-stats()` operation.
 *
 * Async operation: returns request ID, poll with `pollGetStats()`
 */
export function RtcPeerConnectionGetStats(self: bigint, selector: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCPeerConnection(self);
  const promise = obj.getStats(lookupOptionMediaStreamTrack(selector))
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `getStats()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollGetStats(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-session-description
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcSessionDescriptionHandle = bigint;

/** Handle table for RTCSessionDescription instances */
const _rtcSessionDescriptionhandles = new Map<bigint, RTCSessionDescription>();
let _nextRTCSessionDescription = 1n;

/** Lookup a RTCSessionDescription by handle, throwing if not found. */
function lookupRTCSessionDescription(handle: bigint): RTCSessionDescription {
  const obj = _rtcSessionDescriptionhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCSessionDescription handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCSessionDescription by handle. */
function lookupOptionRTCSessionDescription(handle: bigint | undefined): RTCSessionDescription | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcSessionDescriptionhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function RtcSessionDescriptionGetType(self: bigint): bigint {
  const obj = lookupRTCSessionDescription(self);
  const value = (obj as any).getType();
  switch (value) {
    case 'offer': return 0n;
    case 'pranswer': return 1n;
    case 'answer': return 2n;
    case 'rollback': return 3n;
    default: return 0n;
  }
}

/**
 * `get-sdp()` operation.
 */
export function getSdp(self: bigint): string {
  const obj = lookupRTCSessionDescription(self);
  return obj.sdp;
}

/**
 * `to-json()` operation.
 */
export function RtcSessionDescriptionToJson(self: bigint): bigint {
  const obj = lookupRTCSessionDescription(self);
  const _callResult = obj.toJSON();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-ice-candidate
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcIceCandidateHandle = bigint;

/** Handle table for RTCIceCandidate instances */
const _rtcIceCandidatehandles = new Map<bigint, RTCIceCandidate>();
let _nextRTCIceCandidate = 1n;

/** Lookup a RTCIceCandidate by handle, throwing if not found. */
function lookupRTCIceCandidate(handle: bigint): RTCIceCandidate {
  const obj = _rtcIceCandidatehandles.get(handle);
  if (!obj) {
    throw new Error(`RTCIceCandidate handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCIceCandidate by handle. */
function lookupOptionRTCIceCandidate(handle: bigint | undefined): RTCIceCandidate | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcIceCandidatehandles.get(handle) ?? null;
}
/**
 * `get-candidate()` operation.
 */
export function RtcIceCandidateGetCandidate(self: bigint): bigint {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.candidate;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-sdp-mid()` operation.
 */
export function getSdpMid(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.sdpMid;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-sdp-m-line-index()` operation.
 */
export function getSdpMLineIndex(self: bigint): number | undefined {
  const obj = lookupRTCIceCandidate(self);
  return obj.sdpMLineIndex ?? undefined;
}

/**
 * `get-foundation()` operation.
 */
export function getFoundation(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.foundation;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-component()` operation.
 */
export function RtcIceCandidateGetComponent(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.component;
  switch (value) {
    case 'rtp': return 0n;
    case 'rtcp': return 1n;
    default: return 0n;
  }
}

/**
 * `get-priority()` operation.
 */
export function getPriority(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.priority;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-address()` operation.
 */
export function RtcIceCandidateGetAddress(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.address;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-protocol()` operation.
 */
export function RtcIceCandidateGetProtocol(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.protocol;
  switch (value) {
    case 'udp': return 0n;
    case 'tcp': return 1n;
    default: return 0n;
  }
}

/**
 * `get-port()` operation.
 */
export function RtcIceCandidateGetPort(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.port;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-type()` operation.
 */
export function RtcIceCandidateGetType(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = (obj as any).getType();
  switch (value) {
    case 'host': return 0n;
    case 'srflx': return 1n;
    case 'prflx': return 2n;
    case 'relay': return 3n;
    default: return 0n;
  }
}

/**
 * `get-tcp-type()` operation.
 */
export function getTcpType(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.tcpType;
  switch (value) {
    case 'active': return 0n;
    case 'passive': return 1n;
    case 'so': return 2n;
    default: return 0n;
  }
}

/**
 * `get-related-address()` operation.
 */
export function getRelatedAddress(self: bigint): string | undefined {
  const obj = lookupRTCIceCandidate(self);
  return obj.relatedAddress ?? undefined;
}

/**
 * `get-related-port()` operation.
 */
export function getRelatedPort(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.relatedPort;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-username-fragment()` operation.
 */
export function getUsernameFragment(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = obj.usernameFragment;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-relay-protocol()` operation.
 */
export function getRelayProtocol(self: bigint): bigint | undefined {
  const obj = lookupRTCIceCandidate(self);
  const value = (obj as any).relayProtocol;
  switch (value) {
    case 'udp': return 0n;
    case 'tcp': return 1n;
    default: return 0n;
  }
}

/**
 * `get-url()` operation.
 */
export function RtcIceCandidateGetUrl(self: bigint): string | undefined {
  const obj = lookupRTCIceCandidate(self);
  return (obj as any).url ?? undefined;
}

/**
 * `to-json()` operation.
 */
export function RtcIceCandidateToJson(self: bigint): bigint {
  const obj = lookupRTCIceCandidate(self);
  const _callResult = obj.toJSON();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-peer-connection-ice-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcPeerConnectionIceEventHandle = bigint;

/** Handle table for RTCPeerConnectionIceEvent instances */
const _rtcPeerConnectionIceEventhandles = new Map<bigint, RTCPeerConnectionIceEvent>();
let _nextRTCPeerConnectionIceEvent = 1n;

/** Lookup a RTCPeerConnectionIceEvent by handle, throwing if not found. */
function lookupRTCPeerConnectionIceEvent(handle: bigint): RTCPeerConnectionIceEvent {
  const obj = _rtcPeerConnectionIceEventhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCPeerConnectionIceEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCPeerConnectionIceEvent by handle. */
function lookupOptionRTCPeerConnectionIceEvent(handle: bigint | undefined): RTCPeerConnectionIceEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcPeerConnectionIceEventhandles.get(handle) ?? null;
}
/**
 * `get-candidate()` operation.
 */
export function RtcPeerConnectionIceEventGetCandidate(self: bigint): bigint | undefined {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  const _callResult = obj.candidate;
  if (_callResult === null) return undefined;
  const handle = _nextRtcIceCandidate++;
  _rtcIceCandidateHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-url()` operation.
 */
export function RtcPeerConnectionIceEventGetUrl(self: bigint): string | undefined {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).url ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-peer-connection-ice-error-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcPeerConnectionIceErrorEventHandle = bigint;

/**
 * `get-address()` operation.
 */
export function RtcPeerConnectionIceErrorEventGetAddress(self: bigint): string | undefined {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).address ?? undefined;
}

/**
 * `get-port()` operation.
 */
export function RtcPeerConnectionIceErrorEventGetPort(self: bigint): number | undefined {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).port ?? undefined;
}

/**
 * `get-url()` operation.
 */
export function RtcPeerConnectionIceErrorEventGetUrl(self: bigint): string {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).url;
}

/**
 * `get-error-code()` operation.
 */
export function getErrorCode(self: bigint): number {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).errorCode;
}

/**
 * `get-error-text()` operation.
 */
export function getErrorText(self: bigint): string {
  const obj = lookupRTCPeerConnectionIceEvent(self);
  return (obj as any).errorText;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-certificate
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcCertificateHandle = bigint;

/** Handle table for RTCCertificate instances */
const _rtcCertificatehandles = new Map<bigint, RTCCertificate>();
let _nextRTCCertificate = 1n;

/** Lookup a RTCCertificate by handle, throwing if not found. */
function lookupRTCCertificate(handle: bigint): RTCCertificate {
  const obj = _rtcCertificatehandles.get(handle);
  if (!obj) {
    throw new Error(`RTCCertificate handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCCertificate by handle. */
function lookupOptionRTCCertificate(handle: bigint | undefined): RTCCertificate | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcCertificatehandles.get(handle) ?? null;
}
/**
 * `get-expires()` operation.
 */
export function getExpires(self: bigint): bigint {
  const obj = lookupRTCCertificate(self);
  return BigInt(obj.expires);
}

/**
 * `get-fingerprints()` operation.
 */
export function getFingerprints(self: bigint): bigint {
  const obj = lookupRTCCertificate(self);
  const _callResult = obj.getFingerprints();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-rtp-sender
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcRtpSenderHandle = bigint;

/** Handle table for RTCRtpSender instances */
const _rtcRtpSenderhandles = new Map<bigint, RTCRtpSender>();
let _nextRTCRtpSender = 1n;

/** Lookup a RTCRtpSender by handle, throwing if not found. */
function lookupRTCRtpSender(handle: bigint): RTCRtpSender {
  const obj = _rtcRtpSenderhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCRtpSender handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCRtpSender by handle. */
function lookupOptionRTCRtpSender(handle: bigint | undefined): RTCRtpSender | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpSenderhandles.get(handle) ?? null;
}
/**
 * `get-track()` operation.
 */
export function RtcRtpSenderGetTrack(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpSender(self);
  const _callResult = obj.track;
  if (_callResult === null) return undefined;
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-transport()` operation.
 */
export function RtcRtpSenderGetTransport(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpSender(self);
  const _callResult = obj.transport;
  if (_callResult === null) return undefined;
  const handle = _nextRtcDtlsTransport++;
  _rtcDtlsTransportHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-capabilities()` operation.
 */
export function RtcRtpSenderGetCapabilities(kind: string): bigint | undefined {
  return (RTCRtpSender as any).getCapabilities() ?? undefined;
}

/**
 * `set-parameters()` operation.
 */
export function setParameters(self: bigint, parameters: bigint, setParameterOptions: bigint | undefined): bigint {
  const obj = lookupRTCRtpSender(self);
  const _callResult = obj.setParameters(parameters as any, setParameterOptions);
  const handle = _nextPromiseVoid++;
  _promiseVoidHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-parameters()` operation.
 */
export function RtcRtpSenderGetParameters(self: bigint): bigint {
  const obj = lookupRTCRtpSender(self);
  const _callResult = obj.getParameters();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `replace-track()` operation.
 *
 * Async operation: returns request ID, poll with `pollReplaceTrack()`
 */
export function replaceTrack(self: bigint, withTrack: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupRTCRtpSender(self);
  const promise = obj.replaceTrack(lookupOptionMediaStreamTrack(withTrack))
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `replaceTrack()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollReplaceTrack(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `set-streams()` operation.
 */
export function setStreams(self: bigint, streams: (bigint)[]): void {
  const obj = lookupRTCRtpSender(self);
  (obj as any).streams = streams;
}

/**
 * `get-stats()` operation.
 */
export function RtcRtpSenderGetStats(self: bigint): bigint {
  const obj = lookupRTCRtpSender(self);
  return (obj as any).getStats();
}

/**
 * `get-dtmf()` operation.
 */
export function getDtmf(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpSender(self);
  const _callResult = obj.dtmf;
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-rtp-receiver
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcRtpReceiverHandle = bigint;

/** Handle table for RTCRtpReceiver instances */
const _rtcRtpReceiverhandles = new Map<bigint, RTCRtpReceiver>();
let _nextRTCRtpReceiver = 1n;

/** Lookup a RTCRtpReceiver by handle, throwing if not found. */
function lookupRTCRtpReceiver(handle: bigint): RTCRtpReceiver {
  const obj = _rtcRtpReceiverhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCRtpReceiver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCRtpReceiver by handle. */
function lookupOptionRTCRtpReceiver(handle: bigint | undefined): RTCRtpReceiver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpReceiverhandles.get(handle) ?? null;
}
/**
 * `get-track()` operation.
 */
export function RtcRtpReceiverGetTrack(self: bigint): bigint {
  const obj = lookupRTCRtpReceiver(self);
  const _callResult = obj.track;
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-transport()` operation.
 */
export function RtcRtpReceiverGetTransport(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpReceiver(self);
  const _callResult = obj.transport;
  if (_callResult === null) return undefined;
  const handle = _nextRtcDtlsTransport++;
  _rtcDtlsTransportHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-capabilities()` operation.
 */
export function RtcRtpReceiverGetCapabilities(kind: string): bigint | undefined {
  return (RTCRtpReceiver as any).getCapabilities() ?? undefined;
}

/**
 * `get-parameters()` operation.
 */
export function RtcRtpReceiverGetParameters(self: bigint): bigint {
  const obj = lookupRTCRtpReceiver(self);
  const _callResult = obj.getParameters();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-contributing-sources()` operation.
 */
export function getContributingSources(self: bigint): bigint {
  const obj = lookupRTCRtpReceiver(self);
  const _callResult = obj.getContributingSources();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-synchronization-sources()` operation.
 */
export function getSynchronizationSources(self: bigint): bigint {
  const obj = lookupRTCRtpReceiver(self);
  const _callResult = obj.getSynchronizationSources();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-stats()` operation.
 */
export function RtcRtpReceiverGetStats(self: bigint): bigint {
  const obj = lookupRTCRtpReceiver(self);
  return (obj as any).getStats();
}

/**
 * `get-jitter-buffer-target()` operation.
 */
export function getJitterBufferTarget(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpReceiver(self);
  const value = obj.jitterBufferTarget;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `set-jitter-buffer-target()` operation.
 */
export function setJitterBufferTarget(self: bigint, value: number | undefined): void {
  const obj = lookupRTCRtpReceiver(self);
  obj.jitterBufferTarget = value ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-rtp-transceiver
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcRtpTransceiverHandle = bigint;

/** Handle table for RTCRtpTransceiver instances */
const _rtcRtpTransceiverhandles = new Map<bigint, RTCRtpTransceiver>();
let _nextRTCRtpTransceiver = 1n;

/** Lookup a RTCRtpTransceiver by handle, throwing if not found. */
function lookupRTCRtpTransceiver(handle: bigint): RTCRtpTransceiver {
  const obj = _rtcRtpTransceiverhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCRtpTransceiver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCRtpTransceiver by handle. */
function lookupOptionRTCRtpTransceiver(handle: bigint | undefined): RTCRtpTransceiver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcRtpTransceiverhandles.get(handle) ?? null;
}
/**
 * `get-mid()` operation.
 */
export function getMid(self: bigint): string | undefined {
  const obj = lookupRTCRtpTransceiver(self);
  return obj.mid ?? undefined;
}

/**
 * `get-sender()` operation.
 */
export function getSender(self: bigint): bigint {
  const obj = lookupRTCRtpTransceiver(self);
  const _callResult = obj.sender;
  const handle = _nextRtcRtpSender++;
  _rtcRtpSenderHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-receiver()` operation.
 */
export function RtcRtpTransceiverGetReceiver(self: bigint): bigint {
  const obj = lookupRTCRtpTransceiver(self);
  const _callResult = obj.receiver;
  const handle = _nextRtcRtpReceiver++;
  _rtcRtpReceiverHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-direction()` operation.
 */
export function getDirection(self: bigint): bigint {
  const obj = lookupRTCRtpTransceiver(self);
  const value = obj.direction;
  switch (value) {
    case 'sendrecv': return 0n;
    case 'sendonly': return 1n;
    case 'recvonly': return 2n;
    case 'inactive': return 3n;
    case 'stopped': return 4n;
    default: return 0n;
  }
}

/**
 * `set-direction()` operation.
 */
export function setDirection(self: bigint, value: bigint): void {
  const obj = lookupRTCRtpTransceiver(self);
  const _enumInput = value;
  let enumValue: RTCRtpTransceiverDirection;
  if (_enumInput === 0n) { enumValue = 'sendrecv'; }
  if (_enumInput === 1n) { enumValue = 'sendonly'; }
  if (_enumInput === 2n) { enumValue = 'recvonly'; }
  if (_enumInput === 3n) { enumValue = 'inactive'; }
  if (_enumInput === 4n) { enumValue = 'stopped'; }
  else { enumValue = 'sendrecv'; }
  obj.direction = enumValue;
}

/**
 * `get-current-direction()` operation.
 */
export function getCurrentDirection(self: bigint): bigint | undefined {
  const obj = lookupRTCRtpTransceiver(self);
  const value = obj.currentDirection;
  switch (value) {
    case 'sendrecv': return 0n;
    case 'sendonly': return 1n;
    case 'recvonly': return 2n;
    case 'inactive': return 3n;
    case 'stopped': return 4n;
    default: return 0n;
  }
}

/**
 * `stop()` operation.
 */
export function stop(self: bigint): void {
  const obj = lookupRTCRtpTransceiver(self);
  obj.stop();
}

/**
 * `set-codec-preferences()` operation.
 */
export function setCodecPreferences(self: bigint, codecs: (bigint)[]): void {
  const obj = lookupRTCRtpTransceiver(self);
  (obj as any).setCodecPreferences(Array.from(codecs).map(Number));
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-dtls-transport
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcDtlsTransportHandle = bigint;

/** Handle table for RTCDtlsTransport instances */
const _rtcDtlsTransporthandles = new Map<bigint, RTCDtlsTransport>();
let _nextRTCDtlsTransport = 1n;

/** Lookup a RTCDtlsTransport by handle, throwing if not found. */
function lookupRTCDtlsTransport(handle: bigint): RTCDtlsTransport {
  const obj = _rtcDtlsTransporthandles.get(handle);
  if (!obj) {
    throw new Error(`RTCDtlsTransport handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCDtlsTransport by handle. */
function lookupOptionRTCDtlsTransport(handle: bigint | undefined): RTCDtlsTransport | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcDtlsTransporthandles.get(handle) ?? null;
}
/**
 * `get-ice-transport()` operation.
 */
export function getIceTransport(self: bigint): bigint {
  const obj = lookupRTCDtlsTransport(self);
  const _callResult = obj.iceTransport;
  const handle = _nextRtcIceTransport++;
  _rtcIceTransportHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-state()` operation.
 */
export function RtcDtlsTransportGetState(self: bigint): bigint {
  const obj = lookupRTCDtlsTransport(self);
  const value = obj.state;
  switch (value) {
    case 'new': return 0n;
    case 'connecting': return 1n;
    case 'connected': return 2n;
    case 'closed': return 3n;
    case 'failed': return 4n;
    default: return 0n;
  }
}

/**
 * `get-remote-certificates()` operation.
 */
export function getRemoteCertificates(self: bigint): bigint {
  const obj = lookupRTCDtlsTransport(self);
  const _callResult = obj.getRemoteCertificates();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-onstatechange()` operation.
 */
export function RtcDtlsTransportGetOnstatechange(self: bigint): bigint {
  const obj = lookupRTCDtlsTransport(self);
  return (obj as any).onstatechange;
}

/**
 * `set-onstatechange()` operation.
 */
export function RtcDtlsTransportSetOnstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCDtlsTransport(self);
  (obj as any).onstatechange = value as any;
}

/**
 * `get-onerror()` operation.
 */
export function RtcDtlsTransportGetOnerror(self: bigint): bigint {
  const obj = lookupRTCDtlsTransport(self);
  return (obj as any).onerror;
}

/**
 * `set-onerror()` operation.
 */
export function RtcDtlsTransportSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupRTCDtlsTransport(self);
  (obj as any).onerror = value as any;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-ice-transport
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcIceTransportHandle = bigint;

/** Handle table for RTCIceTransport instances */
const _rtcIceTransporthandles = new Map<bigint, RTCIceTransport>();
let _nextRTCIceTransport = 1n;

/** Lookup a RTCIceTransport by handle, throwing if not found. */
function lookupRTCIceTransport(handle: bigint): RTCIceTransport {
  const obj = _rtcIceTransporthandles.get(handle);
  if (!obj) {
    throw new Error(`RTCIceTransport handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCIceTransport by handle. */
function lookupOptionRTCIceTransport(handle: bigint | undefined): RTCIceTransport | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcIceTransporthandles.get(handle) ?? null;
}
/**
 * `get-role()` operation.
 */
export function getRole(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).role;
}

/**
 * `get-component()` operation.
 */
export function RtcIceTransportGetComponent(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  const _callResult = (obj as any).component;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-state()` operation.
 */
export function RtcIceTransportGetState(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  const value = obj.state;
  switch (value) {
    case 'new': return 0n;
    case 'checking': return 1n;
    case 'connected': return 2n;
    case 'completed': return 3n;
    case 'disconnected': return 4n;
    case 'closed': return 5n;
    case 'failed': return 6n;
    default: return 0n;
  }
}

/**
 * `get-gathering-state()` operation.
 */
export function getGatheringState(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  const value = obj.gatheringState;
  switch (value) {
    case 'new': return 0n;
    case 'gathering': return 1n;
    case 'complete': return 2n;
    default: return 0n;
  }
}

/**
 * `get-local-candidates()` operation.
 */
export function getLocalCandidates(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  const _callResult = (obj as any).localCandidates;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-remote-candidates()` operation.
 */
export function getRemoteCandidates(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  const _callResult = (obj as any).remoteCandidates;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-selected-candidate-pair()` operation.
 */
export function getSelectedCandidatePair(self: bigint): bigint | undefined {
  const obj = lookupRTCIceTransport(self);
  const _callResult = obj.getSelectedCandidatePair();
  if (_callResult === null) return undefined;
  const handle = _nextRTCIceCandidatePair++;
  _rtcIceCandidatePairhandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-local-parameters()` operation.
 */
export function getLocalParameters(self: bigint): bigint | undefined {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).localParameters ?? undefined;
}

/**
 * `get-remote-parameters()` operation.
 */
export function getRemoteParameters(self: bigint): bigint | undefined {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).remoteParameters ?? undefined;
}

/**
 * `get-onstatechange()` operation.
 */
export function RtcIceTransportGetOnstatechange(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).onstatechange;
}

/**
 * `set-onstatechange()` operation.
 */
export function RtcIceTransportSetOnstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCIceTransport(self);
  (obj as any).onstatechange = value as any;
}

/**
 * `get-ongatheringstatechange()` operation.
 */
export function getOngatheringstatechange(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).ongatheringstatechange;
}

/**
 * `set-ongatheringstatechange()` operation.
 */
export function setOngatheringstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCIceTransport(self);
  (obj as any).ongatheringstatechange = value as any;
}

/**
 * `get-onselectedcandidatepairchange()` operation.
 */
export function getOnselectedcandidatepairchange(self: bigint): bigint {
  const obj = lookupRTCIceTransport(self);
  return (obj as any).onselectedcandidatepairchange;
}

/**
 * `set-onselectedcandidatepairchange()` operation.
 */
export function setOnselectedcandidatepairchange(self: bigint, value: bigint): void {
  const obj = lookupRTCIceTransport(self);
  (obj as any).onselectedcandidatepairchange = value as any;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-ice-candidate-pair
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcIceCandidatePairHandle = bigint;

/** Handle table for RTCIceCandidatePair instances */
const _rtcIceCandidatePairhandles = new Map<bigint, RTCIceCandidatePair>();
let _nextRTCIceCandidatePair = 1n;

/** Lookup a RTCIceCandidatePair by handle, throwing if not found. */
function lookupRTCIceCandidatePair(handle: bigint): RTCIceCandidatePair {
  const obj = _rtcIceCandidatePairhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCIceCandidatePair handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCIceCandidatePair by handle. */
function lookupOptionRTCIceCandidatePair(handle: bigint | undefined): RTCIceCandidatePair | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcIceCandidatePairhandles.get(handle) ?? null;
}
/**
 * `get-local()` operation.
 */
export function getLocal(self: bigint): bigint {
  const obj = lookupRTCIceCandidatePair(self);
  const _callResult = obj.local;
  const handle = _nextRtcIceCandidate++;
  _rtcIceCandidateHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-remote()` operation.
 */
export function getRemote(self: bigint): bigint {
  const obj = lookupRTCIceCandidatePair(self);
  const _callResult = obj.remote;
  const handle = _nextRtcIceCandidate++;
  _rtcIceCandidateHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-track-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcTrackEventHandle = bigint;

/** Handle table for RTCTrackEvent instances */
const _rtcTrackEventhandles = new Map<bigint, RTCTrackEvent>();
let _nextRTCTrackEvent = 1n;

/** Lookup a RTCTrackEvent by handle, throwing if not found. */
function lookupRTCTrackEvent(handle: bigint): RTCTrackEvent {
  const obj = _rtcTrackEventhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCTrackEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCTrackEvent by handle. */
function lookupOptionRTCTrackEvent(handle: bigint | undefined): RTCTrackEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcTrackEventhandles.get(handle) ?? null;
}
/**
 * `get-receiver()` operation.
 */
export function RtcTrackEventGetReceiver(self: bigint): bigint {
  const obj = lookupRTCTrackEvent(self);
  const _callResult = obj.receiver;
  const handle = _nextRtcRtpReceiver++;
  _rtcRtpReceiverHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-track()` operation.
 */
export function RtcTrackEventGetTrack(self: bigint): bigint {
  const obj = lookupRTCTrackEvent(self);
  const _callResult = obj.track;
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-streams()` operation.
 */
export function getStreams(self: bigint): (bigint)[] {
  const obj = lookupRTCTrackEvent(self);
  const _arr = obj.streams;
  const _handles: bigint[] = [];
  for (const item of _arr) {
    const handle = _nextMediaStream++;
    _mediaStreamHandles.set(handle, item);
    _handles.push(handle);
  }
  return _handles;
}

/**
 * `get-transceiver()` operation.
 */
export function getTransceiver(self: bigint): bigint {
  const obj = lookupRTCTrackEvent(self);
  const _callResult = obj.transceiver;
  const handle = _nextRtcRtpTransceiver++;
  _rtcRtpTransceiverHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-sctp-transport
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcSctpTransportHandle = bigint;

/** Handle table for RTCSctpTransport instances */
const _rtcSctpTransporthandles = new Map<bigint, RTCSctpTransport>();
let _nextRTCSctpTransport = 1n;

/** Lookup a RTCSctpTransport by handle, throwing if not found. */
function lookupRTCSctpTransport(handle: bigint): RTCSctpTransport {
  const obj = _rtcSctpTransporthandles.get(handle);
  if (!obj) {
    throw new Error(`RTCSctpTransport handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCSctpTransport by handle. */
function lookupOptionRTCSctpTransport(handle: bigint | undefined): RTCSctpTransport | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcSctpTransporthandles.get(handle) ?? null;
}
/**
 * `get-transport()` operation.
 */
export function RtcSctpTransportGetTransport(self: bigint): bigint {
  const obj = lookupRTCSctpTransport(self);
  const _callResult = obj.transport;
  const handle = _nextRtcDtlsTransport++;
  _rtcDtlsTransportHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-state()` operation.
 */
export function RtcSctpTransportGetState(self: bigint): bigint {
  const obj = lookupRTCSctpTransport(self);
  const value = obj.state;
  switch (value) {
    case 'connecting': return 0n;
    case 'connected': return 1n;
    case 'closed': return 2n;
    default: return 0n;
  }
}

/**
 * `get-max-message-size()` operation.
 */
export function getMaxMessageSize(self: bigint): number | undefined {
  const obj = lookupRTCSctpTransport(self);
  return obj.maxMessageSize ?? undefined;
}

/**
 * `get-max-channels()` operation.
 */
export function getMaxChannels(self: bigint): number | undefined {
  const obj = lookupRTCSctpTransport(self);
  return obj.maxChannels ?? undefined;
}

/**
 * `get-onstatechange()` operation.
 */
export function RtcSctpTransportGetOnstatechange(self: bigint): bigint {
  const obj = lookupRTCSctpTransport(self);
  return (obj as any).onstatechange;
}

/**
 * `set-onstatechange()` operation.
 */
export function RtcSctpTransportSetOnstatechange(self: bigint, value: bigint): void {
  const obj = lookupRTCSctpTransport(self);
  (obj as any).onstatechange = value as any;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-data-channel
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcDataChannelHandle = bigint;

/** Handle table for RTCDataChannel instances */
const _rtcDataChannelhandles = new Map<bigint, RTCDataChannel>();
let _nextRTCDataChannel = 1n;

/** Lookup a RTCDataChannel by handle, throwing if not found. */
function lookupRTCDataChannel(handle: bigint): RTCDataChannel {
  const obj = _rtcDataChannelhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCDataChannel handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCDataChannel by handle. */
function lookupOptionRTCDataChannel(handle: bigint | undefined): RTCDataChannel | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcDataChannelhandles.get(handle) ?? null;
}
/**
 * `get-label()` operation.
 */
export function getLabel(self: bigint): string {
  const obj = lookupRTCDataChannel(self);
  return obj.label;
}

/**
 * `get-ordered()` operation.
 */
export function getOrdered(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return obj.ordered ? 1n : 0n;
}

/**
 * `get-max-packet-life-time()` operation.
 */
export function getMaxPacketLifeTime(self: bigint): bigint | undefined {
  const obj = lookupRTCDataChannel(self);
  const value = obj.maxPacketLifeTime;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-max-retransmits()` operation.
 */
export function getMaxRetransmits(self: bigint): bigint | undefined {
  const obj = lookupRTCDataChannel(self);
  const value = obj.maxRetransmits;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-protocol()` operation.
 */
export function RtcDataChannelGetProtocol(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  const value = obj.protocol;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-negotiated()` operation.
 */
export function getNegotiated(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return obj.negotiated ? 1n : 0n;
}

/**
 * `get-id()` operation.
 */
export function getId(self: bigint): bigint | undefined {
  const obj = lookupRTCDataChannel(self);
  const value = obj.id;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-ready-state()` operation.
 */
export function getReadyState(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  const value = obj.readyState;
  switch (value) {
    case 'connecting': return 0n;
    case 'open': return 1n;
    case 'closing': return 2n;
    case 'closed': return 3n;
    default: return 0n;
  }
}

/**
 * `get-buffered-amount()` operation.
 */
export function getBufferedAmount(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return BigInt(obj.bufferedAmount);
}

/**
 * `get-buffered-amount-low-threshold()` operation.
 */
export function getBufferedAmountLowThreshold(self: bigint): number {
  const obj = lookupRTCDataChannel(self);
  return obj.bufferedAmountLowThreshold;
}

/**
 * `set-buffered-amount-low-threshold()` operation.
 */
export function setBufferedAmountLowThreshold(self: bigint, value: number): void {
  const obj = lookupRTCDataChannel(self);
  obj.bufferedAmountLowThreshold = Number(value);
}

/**
 * `get-onopen()` operation.
 */
export function getOnopen(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onopen;
}

/**
 * `set-onopen()` operation.
 */
export function setOnopen(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onopen = value as any;
}

/**
 * `get-onbufferedamountlow()` operation.
 */
export function getOnbufferedamountlow(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onbufferedamountlow;
}

/**
 * `set-onbufferedamountlow()` operation.
 */
export function setOnbufferedamountlow(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onbufferedamountlow = value as any;
}

/**
 * `get-onerror()` operation.
 */
export function RtcDataChannelGetOnerror(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onerror;
}

/**
 * `set-onerror()` operation.
 */
export function RtcDataChannelSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onerror = value as any;
}

/**
 * `get-onclosing()` operation.
 */
export function getOnclosing(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onclosing;
}

/**
 * `set-onclosing()` operation.
 */
export function setOnclosing(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onclosing = value as any;
}

/**
 * `get-onclose()` operation.
 */
export function getOnclose(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onclose;
}

/**
 * `set-onclose()` operation.
 */
export function setOnclose(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onclose = value as any;
}

/**
 * `close()` operation.
 */
export function RtcDataChannelClose(self: bigint): void {
  const obj = lookupRTCDataChannel(self);
  obj.close();
}

/**
 * `get-onmessage()` operation.
 */
export function getOnmessage(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  return (obj as any).onmessage;
}

/**
 * `set-onmessage()` operation.
 */
export function setOnmessage(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  (obj as any).onmessage = value as any;
}

/**
 * `get-binary-type()` operation.
 */
export function getBinaryType(self: bigint): bigint {
  const obj = lookupRTCDataChannel(self);
  const value = obj.binaryType;
  switch (value) {
    case 'blob': return 0n;
    case 'arraybuffer': return 1n;
    default: return 0n;
  }
}

/**
 * `set-binary-type()` operation.
 */
export function setBinaryType(self: bigint, value: bigint): void {
  const obj = lookupRTCDataChannel(self);
  const _enumInput = value;
  let enumValue: RTCDataChannelBinaryType;
  if (_enumInput === 0n) { enumValue = 'blob'; }
  if (_enumInput === 1n) { enumValue = 'arraybuffer'; }
  else { enumValue = 'blob'; }
  obj.binaryType = enumValue;
}

/**
 * `send()` operation.
 */
export function send(self: bigint, data: string): void {
  const obj = lookupRTCDataChannel(self);
  obj.send(data as any);
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-data-channel-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcDataChannelEventHandle = bigint;

/** Handle table for RTCDataChannelEvent instances */
const _rtcDataChannelEventhandles = new Map<bigint, RTCDataChannelEvent>();
let _nextRTCDataChannelEvent = 1n;

/** Lookup a RTCDataChannelEvent by handle, throwing if not found. */
function lookupRTCDataChannelEvent(handle: bigint): RTCDataChannelEvent {
  const obj = _rtcDataChannelEventhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCDataChannelEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCDataChannelEvent by handle. */
function lookupOptionRTCDataChannelEvent(handle: bigint | undefined): RTCDataChannelEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcDataChannelEventhandles.get(handle) ?? null;
}
/**
 * `get-channel()` operation.
 */
export function getChannel(self: bigint): bigint {
  const obj = lookupRTCDataChannelEvent(self);
  const _callResult = obj.channel;
  const handle = _nextRTCDataChannel++;
  _rtcDataChannelhandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: rtcdtmf-sender
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcdtmfSenderHandle = bigint;

/** Handle table for RTCDTMFSender instances */
const _rtcdtmfSenderhandles = new Map<bigint, RTCDTMFSender>();
let _nextRTCDTMFSender = 1n;

/** Lookup a RTCDTMFSender by handle, throwing if not found. */
function lookupRTCDTMFSender(handle: bigint): RTCDTMFSender {
  const obj = _rtcdtmfSenderhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCDTMFSender handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCDTMFSender by handle. */
function lookupOptionRTCDTMFSender(handle: bigint | undefined): RTCDTMFSender | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcdtmfSenderhandles.get(handle) ?? null;
}
/**
 * `insert-dtmf()` operation.
 */
export function insertDtmf(self: bigint, tones: string, duration: number | undefined, interToneGap: number | undefined): void {
  const obj = lookupRTCDTMFSender(self);
  obj.insertDTMF(tones, duration, interToneGap);
}

/**
 * `get-ontonechange()` operation.
 */
export function getOntonechange(self: bigint): bigint {
  const obj = lookupRTCDTMFSender(self);
  const handler = obj.ontonechange;
  if (handler == null) return 0n;
  const handle = _nextEventHandler++;
  _eventHandlerHandles.set(handle, handler);
  return handle;
}

/**
 * `set-ontonechange()` operation.
 */
export function setOntonechange(self: bigint, value: bigint): void {
  const obj = lookupRTCDTMFSender(self);
  obj.ontonechange = value as any;
}

/**
 * `get-can-insert-dtmf()` operation.
 */
export function getCanInsertDtmf(self: bigint): boolean {
  const obj = lookupRTCDTMFSender(self);
  return (obj as any).getCanInsertDtmf();
}

/**
 * `get-tone-buffer()` operation.
 */
export function getToneBuffer(self: bigint): bigint {
  const obj = lookupRTCDTMFSender(self);
  const value = obj.toneBuffer;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

// ---------------------------------------------------------------------------
// WIT interface: rtcdtmf-tone-change-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcdtmfToneChangeEventHandle = bigint;

/** Handle table for RTCDTMFToneChangeEvent instances */
const _rtcdtmfToneChangeEventhandles = new Map<bigint, RTCDTMFToneChangeEvent>();
let _nextRTCDTMFToneChangeEvent = 1n;

/** Lookup a RTCDTMFToneChangeEvent by handle, throwing if not found. */
function lookupRTCDTMFToneChangeEvent(handle: bigint): RTCDTMFToneChangeEvent {
  const obj = _rtcdtmfToneChangeEventhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCDTMFToneChangeEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCDTMFToneChangeEvent by handle. */
function lookupOptionRTCDTMFToneChangeEvent(handle: bigint | undefined): RTCDTMFToneChangeEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcdtmfToneChangeEventhandles.get(handle) ?? null;
}
/**
 * `get-tone()` operation.
 */
export function getTone(self: bigint): string {
  const obj = lookupRTCDTMFToneChangeEvent(self);
  return obj.tone;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-error
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcErrorHandle = bigint;

/** Handle table for RTCError instances */
const _rtcErrorhandles = new Map<bigint, RTCError>();
let _nextRTCError = 1n;

/** Lookup a RTCError by handle, throwing if not found. */
function lookupRTCError(handle: bigint): RTCError {
  const obj = _rtcErrorhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCError handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCError by handle. */
function lookupOptionRTCError(handle: bigint | undefined): RTCError | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcErrorhandles.get(handle) ?? null;
}
/**
 * `get-error-detail()` operation.
 */
export function getErrorDetail(self: bigint): bigint {
  const obj = lookupRTCError(self);
  const value = obj.errorDetail;
  switch (value) {
    case 'data-channel-failure': return 0n;
    case 'dtls-failure': return 1n;
    case 'fingerprint-failure': return 2n;
    case 'sctp-failure': return 3n;
    default: return 0n;
  }
}

/**
 * `get-sdp-line-number()` operation.
 */
export function getSdpLineNumber(self: bigint): bigint | undefined {
  const obj = lookupRTCError(self);
  const value = obj.sdpLineNumber;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-sctp-cause-code()` operation.
 */
export function getSctpCauseCode(self: bigint): bigint | undefined {
  const obj = lookupRTCError(self);
  const value = obj.sctpCauseCode;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-received-alert()` operation.
 */
export function getReceivedAlert(self: bigint): bigint | undefined {
  const obj = lookupRTCError(self);
  const value = obj.receivedAlert;
  return value != null ? BigInt(value) : undefined;
}

/**
 * `get-sent-alert()` operation.
 */
export function getSentAlert(self: bigint): bigint | undefined {
  const obj = lookupRTCError(self);
  const value = obj.sentAlert;
  return value != null ? BigInt(value) : undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: rtc-error-event
// ---------------------------------------------------------------------------

/** Type alias */
export type RtcErrorEventHandle = bigint;

/** Handle table for RTCErrorEvent instances */
const _rtcErrorEventhandles = new Map<bigint, RTCErrorEvent>();
let _nextRTCErrorEvent = 1n;

/** Lookup a RTCErrorEvent by handle, throwing if not found. */
function lookupRTCErrorEvent(handle: bigint): RTCErrorEvent {
  const obj = _rtcErrorEventhandles.get(handle);
  if (!obj) {
    throw new Error(`RTCErrorEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional RTCErrorEvent by handle. */
function lookupOptionRTCErrorEvent(handle: bigint | undefined): RTCErrorEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rtcErrorEventhandles.get(handle) ?? null;
}
/**
 * `get-error()` operation.
 */
export function getError(self: bigint): bigint {
  const obj = lookupRTCErrorEvent(self);
  const _callResult = obj.error;
  const handle = _nextRTCError++;
  _rtcErrorhandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  createOffer,
  pollCreateOffer,
  createAnswer,
  pollCreateAnswer,
  setLocalDescription,
  pollSetLocalDescription,
  getLocalDescription,
  getCurrentLocalDescription,
  getPendingLocalDescription,
  setRemoteDescription,
  pollSetRemoteDescription,
  getRemoteDescription,
  getCurrentRemoteDescription,
  getPendingRemoteDescription,
  addIceCandidate,
  pollAddIceCandidate,
  getSignalingState,
  getIceGatheringState,
  getIceConnectionState,
  getConnectionState,
  getCanTrickleIceCandidates,
  restartIce,
  pollRestartIce,
  getConfiguration,
  setConfiguration,
  RtcPeerConnectionClose,
  getOnnegotiationneeded,
  setOnnegotiationneeded,
  getOnicecandidate,
  setOnicecandidate,
  getOnicecandidateerror,
  setOnicecandidateerror,
  getOnsignalingstatechange,
  setOnsignalingstatechange,
  getOniceconnectionstatechange,
  setOniceconnectionstatechange,
  getOnicegatheringstatechange,
  setOnicegatheringstatechange,
  getOnconnectionstatechange,
  setOnconnectionstatechange,
  generateCertificate,
  pollGenerateCertificate,
  getSenders,
  getReceivers,
  getTransceivers,
  addTrack,
  removeTrack,
  addTransceiver,
  getOntrack,
  setOntrack,
  getSctp,
  createDataChannel,
  getOndatachannel,
  setOndatachannel,
  RtcPeerConnectionGetStats,
  pollGetStats,
  RtcSessionDescriptionGetType,
  getSdp,
  RtcSessionDescriptionToJson,
  RtcIceCandidateGetCandidate,
  getSdpMid,
  getSdpMLineIndex,
  getFoundation,
  RtcIceCandidateGetComponent,
  getPriority,
  RtcIceCandidateGetAddress,
  RtcIceCandidateGetProtocol,
  RtcIceCandidateGetPort,
  RtcIceCandidateGetType,
  getTcpType,
  getRelatedAddress,
  getRelatedPort,
  getUsernameFragment,
  getRelayProtocol,
  RtcIceCandidateGetUrl,
  RtcIceCandidateToJson,
  RtcPeerConnectionIceEventGetCandidate,
  RtcPeerConnectionIceEventGetUrl,
  RtcPeerConnectionIceErrorEventGetAddress,
  RtcPeerConnectionIceErrorEventGetPort,
  RtcPeerConnectionIceErrorEventGetUrl,
  getErrorCode,
  getErrorText,
  getExpires,
  getFingerprints,
  RtcRtpSenderGetTrack,
  RtcRtpSenderGetTransport,
  RtcRtpSenderGetCapabilities,
  setParameters,
  RtcRtpSenderGetParameters,
  replaceTrack,
  pollReplaceTrack,
  setStreams,
  RtcRtpSenderGetStats,
  getDtmf,
  RtcRtpReceiverGetTrack,
  RtcRtpReceiverGetTransport,
  RtcRtpReceiverGetCapabilities,
  RtcRtpReceiverGetParameters,
  getContributingSources,
  getSynchronizationSources,
  RtcRtpReceiverGetStats,
  getJitterBufferTarget,
  setJitterBufferTarget,
  getMid,
  getSender,
  RtcRtpTransceiverGetReceiver,
  getDirection,
  setDirection,
  getCurrentDirection,
  stop,
  setCodecPreferences,
  getIceTransport,
  RtcDtlsTransportGetState,
  getRemoteCertificates,
  RtcDtlsTransportGetOnstatechange,
  RtcDtlsTransportSetOnstatechange,
  RtcDtlsTransportGetOnerror,
  RtcDtlsTransportSetOnerror,
  getRole,
  RtcIceTransportGetComponent,
  RtcIceTransportGetState,
  getGatheringState,
  getLocalCandidates,
  getRemoteCandidates,
  getSelectedCandidatePair,
  getLocalParameters,
  getRemoteParameters,
  RtcIceTransportGetOnstatechange,
  RtcIceTransportSetOnstatechange,
  getOngatheringstatechange,
  setOngatheringstatechange,
  getOnselectedcandidatepairchange,
  setOnselectedcandidatepairchange,
  getLocal,
  getRemote,
  RtcTrackEventGetReceiver,
  RtcTrackEventGetTrack,
  getStreams,
  getTransceiver,
  RtcSctpTransportGetTransport,
  RtcSctpTransportGetState,
  getMaxMessageSize,
  getMaxChannels,
  RtcSctpTransportGetOnstatechange,
  RtcSctpTransportSetOnstatechange,
  getLabel,
  getOrdered,
  getMaxPacketLifeTime,
  getMaxRetransmits,
  RtcDataChannelGetProtocol,
  getNegotiated,
  getId,
  getReadyState,
  getBufferedAmount,
  getBufferedAmountLowThreshold,
  setBufferedAmountLowThreshold,
  getOnopen,
  setOnopen,
  getOnbufferedamountlow,
  setOnbufferedamountlow,
  RtcDataChannelGetOnerror,
  RtcDataChannelSetOnerror,
  getOnclosing,
  setOnclosing,
  getOnclose,
  setOnclose,
  RtcDataChannelClose,
  getOnmessage,
  setOnmessage,
  getBinaryType,
  setBinaryType,
  send,
  getChannel,
  insertDtmf,
  getOntonechange,
  setOntonechange,
  getCanInsertDtmf,
  getToneBuffer,
  getTone,
  getErrorDetail,
  getSdpLineNumber,
  getSctpCauseCode,
  getReceivedAlert,
  getSentAlert,
  getError
};
