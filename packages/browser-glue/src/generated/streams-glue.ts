/**
 * streams glue — implements the `tairitsu-browser:streams` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-glue/../../packages/browser-worlds/wit/generated/streams.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// WIT interface: readable-stream
// ---------------------------------------------------------------------------

/** Type alias */
export type ReadableHandle = bigint;

/** Handle table for ReadableStream instances */
const _readableHandles = new Map<bigint, ReadableStream>();
let _nextReadable = 1n;

/** Get a ReadableStream by handle, throwing if not found. */
function getReadable(handle: bigint): ReadableStream {
  const obj = _readableHandles.get(handle);
  if (!obj) {
    throw new Error(`ReadableStream handle ${handle} not found`);
  }
  return obj;
}

/**
 * `get-reader()` operation.
 */
export function getReader(stream: bigint): bigint {
  const obj = getReadable(self);
  return obj.reader;
}

/**
 * `read-chunk()` operation.
 */
export function readChunk(reader: bigint): { ok: true; value: bigint } | { ok: false; error: string } {
  const obj = getReadable(self);
  return obj.readChunk(reader) ?? undefined;
}

/**
 * `cancel()` operation.
 *
 * Async operation: returns request ID, poll with `pollCancel()`
 */
export function cancel(stream: string | undefined, reason: string): string {
  const requestId = _nextAsyncHandle++;
  const obj = getReadable(self);
  const promise = obj.cancel(stream, reason)
    .then((result) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `cancel()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollCancel(requestId: bigint): { ok: true; value: string } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `is-locked()` operation.
 */
export function isLocked(stream: bigint): boolean {
  const obj = getReadable(self);
  return obj.isLocked(stream);
}

/**
 * `is-closed()` operation.
 */
export function isClosed(stream: bigint): boolean {
  const obj = getReadable(self);
  return obj.isClosed(stream);
}

// ---------------------------------------------------------------------------
// WIT interface: writable-stream
// ---------------------------------------------------------------------------

/** Type alias */
export type WritableHandle = bigint;

/** Handle table for WritableStream instances */
const _writableHandles = new Map<bigint, WritableStream>();
let _nextWritable = 1n;

/** Get a WritableStream by handle, throwing if not found. */
function getWritable(handle: bigint): WritableStream {
  const obj = _writableHandles.get(handle);
  if (!obj) {
    throw new Error(`WritableStream handle ${handle} not found`);
  }
  return obj;
}

/**
 * `get-writer()` operation.
 */
export function getWriter(stream: bigint): { ok: true; value: bigint } | { ok: false; error: string } {
  const obj = getWritable(self);
  return obj.writer;
}

/**
 * `write-chunk()` operation.
 */
export function writeChunk(writer: string, data: bigint | undefined): { ok: true; value: bigint } | { ok: false; error: string } {
  const obj = getWritable(self);
  return obj.writeChunk(writer, data);
}

/**
 * `poll-write()` operation.
 */
export function pollWrite(requestId: bigint): number | undefined {
  const obj = getWritable(self);
  return obj.pollWrite(requestId) ?? undefined;
}

/**
 * `close-writer()` operation.
 */
export function closeWriter(writer: bigint): { ok: true; value: bigint } | { ok: false; error: number } {
  const obj = getWritable(self);
  return obj.closeWriter(writer);
}

/**
 * `poll-close()` operation.
 */
export function pollClose(requestId: string): bigint | undefined {
  const obj = getWritable(self);
  return obj.pollClose(requestId) ?? undefined;
}

/**
 * `abort()` operation.
 *
 * Async operation: returns request ID, poll with `pollAbort()`
 */
export function abort(stream: bigint, reason: bigint | undefined): { ok: true } | { ok: false; error: string } {
  const requestId = _nextAsyncHandle++;
  const obj = getWritable(self);
  const promise = obj.abort(stream, reason)
    .then((result) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `abort()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollAbort(requestId: bigint): { ok: true; value: { ok: true } | { ok: false; error: string } } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `is-locked()` operation.
 */
export function isLocked(stream: bigint): bigint {
  const obj = getWritable(self);
  return obj.isLocked(stream);
}

// ---------------------------------------------------------------------------
// WIT interface: transform-stream
// ---------------------------------------------------------------------------

/** Type alias */
export type TransformHandle = bigint;

/** Handle table for TransformStream instances */
const _transformHandles = new Map<bigint, TransformStream>();
let _nextTransform = 1n;

/** Get a TransformStream by handle, throwing if not found. */
function getTransform(handle: bigint): TransformStream {
  const obj = _transformHandles.get(handle);
  if (!obj) {
    throw new Error(`TransformStream handle ${handle} not found`);
  }
  return obj;
}

/**
 * `get-readable()` operation.
 */
export function getReadable(stream: bigint): { ok: true; value: bigint } | { ok: false; error: bigint } {
  const obj = getTransform(self);
  return obj.readable;
}

/**
 * `get-writable()` operation.
 */
export function getWritable(stream: bigint): { ok: true; value: bigint } | { ok: false; error: string } {
  const obj = getTransform(self);
  return obj.writable;
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  getReader,
  readChunk,
  cancel,
  pollCancel,
  isLocked,
  isClosed,
  getWriter,
  writeChunk,
  pollWrite,
  closeWriter,
  pollClose,
  abort,
  pollAbort,
  isLocked,
  getReadable,
  getWritable
};
