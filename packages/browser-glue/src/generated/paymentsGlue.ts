/**
 * payments glue — implements the `tairitsu-browser:payments` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-worlds/wit/generated/payments.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for payment-address values */
const _paymentAddresshandles = new Map<bigint, PaymentAddress>();
let _nextPaymentAddress = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a payment-address value by handle. */
function lookupPaymentAddress(handle: bigint): PaymentAddress {
  const obj = _paymentAddresshandles.get(handle);
  if (obj === undefined) {
    throw new Error(`payment-address handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional payment-address value by handle. */
function lookupOptionPaymentAddress(handle: bigint | undefined): PaymentAddress | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _paymentAddresshandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: payment-request
// ---------------------------------------------------------------------------

/** Type alias */
export type PaymentRequestHandle = bigint;

/** Handle table for PaymentRequest instances */
const _paymentRequesthandles = new Map<bigint, PaymentRequest>();
let _nextPaymentRequest = 1n;

/** Lookup a PaymentRequest by handle, throwing if not found. */
function lookupPaymentRequest(handle: bigint): PaymentRequest {
  const obj = _paymentRequesthandles.get(handle);
  if (!obj) {
    throw new Error(`PaymentRequest handle ${handle} not found`);
  }
  return obj!;
}
/**
 * `show()` operation.
 *
 * Async operation: returns request ID, poll with `pollShow()`
 */
export function show(self: bigint, detailsPromise: string): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupPaymentRequest(self);
  const promise = obj.show(detailsPromise as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `show()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollShow(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `abort()` operation.
 *
 * Async operation: returns request ID, poll with `pollAbort()`
 */
export function abort(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupPaymentRequest(self);
  const promise = obj.abort()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `abort()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollAbort(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `can-make-payment()` operation.
 *
 * Async operation: returns request ID, poll with `pollCanMakePayment()`
 */
export function canMakePayment(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupPaymentRequest(self);
  const promise = obj.canMakePayment()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `canMakePayment()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollCanMakePayment(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-id()` operation.
 */
export function getId(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentRequest(self);
  return obj.id;
}

/**
 * `get-shipping-address()` operation.
 */
export function PaymentRequestGetShippingAddress(self: bigint): EventHandlerRecord | undefined {
  const obj = lookupPaymentRequest(self);
  return obj.shippingAddress ?? undefined;
}

/**
 * `get-shipping-option()` operation.
 */
export function PaymentRequestGetShippingOption(self: bigint): EventHandlerRecord | undefined {
  const obj = lookupPaymentRequest(self);
  return obj.shippingOption ?? undefined;
}

/**
 * `get-shipping-type()` operation.
 */
export function getShippingType(self: bigint): string {
  const obj = lookupPaymentRequest(self);
  return obj.shippingType ?? undefined;
}

/**
 * `get-onshippingaddresschange()` operation.
 */
export function getOnshippingaddresschange(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentRequest(self);
  return obj.onshippingaddresschange;
}

/**
 * `set-onshippingaddresschange()` operation.
 */
export function setOnshippingaddresschange(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupPaymentRequest(self);
  obj.onshippingaddresschange = value;
}

/**
 * `get-onshippingoptionchange()` operation.
 */
export function getOnshippingoptionchange(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentRequest(self);
  return obj.onshippingoptionchange;
}

/**
 * `set-onshippingoptionchange()` operation.
 */
export function setOnshippingoptionchange(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupPaymentRequest(self);
  obj.onshippingoptionchange = value;
}

/**
 * `get-onpaymentmethodchange()` operation.
 */
export function getOnpaymentmethodchange(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentRequest(self);
  return obj.onpaymentmethodchange;
}

/**
 * `set-onpaymentmethodchange()` operation.
 */
export function setOnpaymentmethodchange(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupPaymentRequest(self);
  obj.onpaymentmethodchange = value;
}

// ---------------------------------------------------------------------------
// WIT interface: payment-response
// ---------------------------------------------------------------------------

/** Type alias */
export type PaymentResponseHandle = bigint;

/** Handle table for PaymentResponse instances */
const _paymentResponsehandles = new Map<bigint, PaymentResponse>();
let _nextPaymentResponse = 1n;

/** Lookup a PaymentResponse by handle, throwing if not found. */
function lookupPaymentResponse(handle: bigint): PaymentResponse {
  const obj = _paymentResponsehandles.get(handle);
  if (!obj) {
    throw new Error(`PaymentResponse handle ${handle} not found`);
  }
  return obj!;
}
/**
 * `to-json()` operation.
 */
export function toJson(self: bigint): string {
  const obj = lookupPaymentResponse(self);
  return obj.toJSON();
}

/**
 * `get-request-id()` operation.
 */
export function getRequestId(self: bigint): bigint {
  const obj = lookupPaymentResponse(self);
  return obj.requestId;
}

/**
 * `get-method-name()` operation.
 */
export function PaymentResponseGetMethodName(self: bigint): string {
  const obj = lookupPaymentResponse(self);
  return obj.methodName;
}

/**
 * `get-details()` operation.
 */
export function getDetails(self: bigint): number {
  const obj = lookupPaymentResponse(self);
  return obj.details;
}

/**
 * `get-shipping-address()` operation.
 */
export function PaymentResponseGetShippingAddress(self: bigint): bigint | undefined {
  const obj = lookupPaymentResponse(self);
  const _callResult = obj.shippingAddress;
  if (_callResult === null) return undefined;
  const handle = _nextPaymentAddress++;
  _paymentAddresshandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-shipping-option()` operation.
 */
export function PaymentResponseGetShippingOption(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentResponse(self);
  return obj.shippingOption ?? undefined;
}

/**
 * `get-payer-name()` operation.
 */
export function getPayerName(self: bigint): string | undefined {
  const obj = lookupPaymentResponse(self);
  return obj.payerName ?? undefined;
}

/**
 * `get-payer-email()` operation.
 */
export function getPayerEmail(self: bigint): string | undefined {
  const obj = lookupPaymentResponse(self);
  return obj.payerEmail ?? undefined;
}

/**
 * `get-payer-phone()` operation.
 */
export function getPayerPhone(self: bigint): number {
  const obj = lookupPaymentResponse(self);
  return obj.payerPhone ?? undefined;
}

/**
 * `complete()` operation.
 *
 * Async operation: returns request ID, poll with `pollComplete()`
 */
export function complete(self: bigint, result: bigint | undefined, details: number | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupPaymentResponse(self);
  const promise = obj.complete(result as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `complete()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollComplete(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `retry()` operation.
 *
 * Async operation: returns request ID, poll with `pollRetry()`
 */
export function retry(self: bigint, errorFields: PaymentValidationErrors | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupPaymentResponse(self);
  const promise = obj.retry(errorFields)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `retry()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollRetry(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-onpayerdetailchange()` operation.
 */
export function getOnpayerdetailchange(self: bigint): EventHandlerRecord {
  const obj = lookupPaymentResponse(self);
  return obj.onpayerdetailchange;
}

/**
 * `set-onpayerdetailchange()` operation.
 */
export function setOnpayerdetailchange(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupPaymentResponse(self);
  obj.onpayerdetailchange = value;
}

// ---------------------------------------------------------------------------
// WIT interface: payment-method-change-event
// ---------------------------------------------------------------------------

/** Type alias */
export type PaymentMethodChangeEventHandle = bigint;

/** Handle table for PaymentMethodChangeEvent instances */
const _paymentMethodChangeEventhandles = new Map<bigint, PaymentMethodChangeEvent>();
let _nextPaymentMethodChangeEvent = 1n;

/** Lookup a PaymentMethodChangeEvent by handle, throwing if not found. */
function lookupPaymentMethodChangeEvent(handle: bigint): PaymentMethodChangeEvent {
  const obj = _paymentMethodChangeEventhandles.get(handle);
  if (!obj) {
    throw new Error(`PaymentMethodChangeEvent handle ${handle} not found`);
  }
  return obj!;
}
/**
 * `get-method-name()` operation.
 */
export function PaymentMethodChangeEventGetMethodName(self: bigint): string {
  const obj = lookupPaymentMethodChangeEvent(self);
  return obj.methodName;
}

/**
 * `get-method-details()` operation.
 */
export function getMethodDetails(self: bigint): bigint | undefined {
  const obj = lookupPaymentMethodChangeEvent(self);
  return obj.methodDetails ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: payment-request-update-event
// ---------------------------------------------------------------------------

/** Type alias */
export type PaymentRequestUpdateEventHandle = bigint;

/** Handle table for PaymentRequestUpdateEvent instances */
const _paymentRequestUpdateEventhandles = new Map<bigint, PaymentRequestUpdateEvent>();
let _nextPaymentRequestUpdateEvent = 1n;

/** Lookup a PaymentRequestUpdateEvent by handle, throwing if not found. */
function lookupPaymentRequestUpdateEvent(handle: bigint): PaymentRequestUpdateEvent {
  const obj = _paymentRequestUpdateEventhandles.get(handle);
  if (!obj) {
    throw new Error(`PaymentRequestUpdateEvent handle ${handle} not found`);
  }
  return obj!;
}
/**
 * `update-with()` operation.
 */
export function updateWith(self: bigint, detailsPromise: bigint): void {
  const obj = lookupPaymentRequestUpdateEvent(self);
  obj.updateWith(detailsPromise as any);
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  show,
  pollShow,
  abort,
  pollAbort,
  canMakePayment,
  pollCanMakePayment,
  getId,
  PaymentRequestGetShippingAddress,
  PaymentRequestGetShippingOption,
  getShippingType,
  getOnshippingaddresschange,
  setOnshippingaddresschange,
  getOnshippingoptionchange,
  setOnshippingoptionchange,
  getOnpaymentmethodchange,
  setOnpaymentmethodchange,
  toJson,
  getRequestId,
  PaymentResponseGetMethodName,
  getDetails,
  PaymentResponseGetShippingAddress,
  PaymentResponseGetShippingOption,
  getPayerName,
  getPayerEmail,
  getPayerPhone,
  complete,
  pollComplete,
  retry,
  pollRetry,
  getOnpayerdetailchange,
  setOnpayerdetailchange,
  PaymentMethodChangeEventGetMethodName,
  getMethodDetails,
  updateWith
};
