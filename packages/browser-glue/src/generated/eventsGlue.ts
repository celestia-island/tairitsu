/**
 * events glue — implements the `tairitsu-browser:events` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-worlds/wit/generated/events.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for data-transfer values */
const _dataTransferHandles = new Map<bigint, DataTransfer>();
let _nextDataTransfer = 1n;

/** Handle table for event-target values */
const _eventTargetHandles = new Map<bigint, EventTarget>();
let _nextEventTarget = 1n;

/** Handle table for pointer-event-list values */
const _pointerEventListHandles = new Map<bigint, PointerEvent[]>();
let _nextPointerEventList = 1n;

/** Handle table for promise-any values */
const _promiseAnyHandles = new Map<bigint, Promise<any>>();
let _nextPromiseAny = 1n;

/** Handle table for promise-string values */
const _promiseStringHandles = new Map<bigint, Promise<string>>();
let _nextPromiseString = 1n;

/** Handle table for string-list values */
const _stringListHandles = new Map<bigint, string[]>();
let _nextStringList = 1n;

/** Handle table for touch values */
const _touchHandles = new Map<bigint, Touch>();
let _nextTouch = 1n;

/** Handle table for touch-list values */
const _touchListHandles = new Map<bigint, TouchList>();
let _nextTouchList = 1n;

/** Handle table for window values */
const _windowHandles = new Map<bigint, Window>();
let _nextWindow = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a data-transfer value by handle. */
function lookupDataTransfer(handle: bigint): DataTransfer {
  const obj = _dataTransferHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`data-transfer handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional data-transfer value by handle. */
function lookupOptionDataTransfer(handle: bigint | undefined): DataTransfer | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _dataTransferHandles.get(handle) ?? null;
}

/** Lookup a event-target value by handle. */
function lookupEventTarget(handle: bigint): EventTarget {
  const obj = _eventTargetHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-target handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-target value by handle. */
function lookupOptionEventTarget(handle: bigint | undefined): EventTarget | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventTargetHandles.get(handle) ?? null;
}

/** Lookup a pointer-event-list value by handle. */
function lookupPointerEventList(handle: bigint): PointerEvent[] {
  const obj = _pointerEventListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`pointer-event-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional pointer-event-list value by handle. */
function lookupOptionPointerEventList(handle: bigint | undefined): PointerEvent[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _pointerEventListHandles.get(handle) ?? null;
}

/** Lookup a promise-any value by handle. */
function lookupPromiseAny(handle: bigint): Promise<any> {
  const obj = _promiseAnyHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`promise-any handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional promise-any value by handle. */
function lookupOptionPromiseAny(handle: bigint | undefined): Promise<any> | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _promiseAnyHandles.get(handle) ?? null;
}

/** Lookup a promise-string value by handle. */
function lookupPromiseString(handle: bigint): Promise<string> {
  const obj = _promiseStringHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`promise-string handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional promise-string value by handle. */
function lookupOptionPromiseString(handle: bigint | undefined): Promise<string> | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _promiseStringHandles.get(handle) ?? null;
}

/** Lookup a string-list value by handle. */
function lookupStringList(handle: bigint): string[] {
  const obj = _stringListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string-list value by handle. */
function lookupOptionStringList(handle: bigint | undefined): string[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringListHandles.get(handle) ?? null;
}

/** Lookup a touch value by handle. */
function lookupTouch(handle: bigint): Touch {
  const obj = _touchHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`touch handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional touch value by handle. */
function lookupOptionTouch(handle: bigint | undefined): Touch | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _touchHandles.get(handle) ?? null;
}

/** Lookup a touch-list value by handle. */
function lookupTouchList(handle: bigint): TouchList {
  const obj = _touchListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`touch-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional touch-list value by handle. */
function lookupOptionTouchList(handle: bigint | undefined): TouchList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _touchListHandles.get(handle) ?? null;
}

/** Lookup a window value by handle. */
function lookupWindow(handle: bigint): Window {
  const obj = _windowHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`window handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional window value by handle. */
function lookupOptionWindow(handle: bigint | undefined): Window | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _windowHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: clipboard-event
// ---------------------------------------------------------------------------

/** Type alias */
export type ClipboardEventHandle = bigint;

/** Handle table for ClipboardEvent instances */
const _clipboardEventhandles = new Map<bigint, ClipboardEvent>();
let _nextClipboardEvent = 1n;

/** Lookup a ClipboardEvent by handle, throwing if not found. */
function lookupClipboardEvent(handle: bigint): ClipboardEvent {
  const obj = _clipboardEventhandles.get(handle);
  if (!obj) {
    throw new Error(`ClipboardEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ClipboardEvent by handle. */
function lookupOptionClipboardEvent(handle: bigint | undefined): ClipboardEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _clipboardEventhandles.get(handle) ?? null;
}
/**
 * `get-clipboard-data()` operation.
 */
export function getClipboardData(self: bigint): bigint | undefined {
  const obj = lookupClipboardEvent(self);
  const _callResult = obj.clipboardData;
  if (_callResult === null) return undefined;
  const handle = _nextDataTransfer++;
  _dataTransferHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: clipboard-change-event
// ---------------------------------------------------------------------------

/** Type alias */
export type ClipboardChangeEventHandle = bigint;

/**
 * `get-types()` operation.
 */
export function ClipboardChangeEventGetTypes(self: bigint): string {
  const obj = lookupClipboardEvent(self);
  return obj.type;
}

/**
 * `get-change-id()` operation.
 */
export function getChangeId(self: bigint): bigint {
  const obj = lookupClipboardEvent(self);
  return (obj as any).changeId;
}

// ---------------------------------------------------------------------------
// WIT interface: clipboard-item
// ---------------------------------------------------------------------------

/** Type alias */
export type ClipboardItemHandle = bigint;

/** Handle table for ClipboardItem instances */
const _clipboardItemhandles = new Map<bigint, ClipboardItem>();
let _nextClipboardItem = 1n;

/** Lookup a ClipboardItem by handle, throwing if not found. */
function lookupClipboardItem(handle: bigint): ClipboardItem {
  const obj = _clipboardItemhandles.get(handle);
  if (!obj) {
    throw new Error(`ClipboardItem handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ClipboardItem by handle. */
function lookupOptionClipboardItem(handle: bigint | undefined): ClipboardItem | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _clipboardItemhandles.get(handle) ?? null;
}
/**
 * `get-presentation-style()` operation.
 */
export function getPresentationStyle(self: bigint): bigint {
  const obj = lookupClipboardItem(self);
  const value = obj.presentationStyle;
  switch (value) {
    case 'unspecified': return 0n;
    case 'inline': return 1n;
    case 'attachment': return 2n;
    default: return 0n;
  }
}

/**
 * `get-types()` operation.
 */
export function ClipboardItemGetTypes(self: bigint): bigint {
  const obj = lookupClipboardItem(self);
  const _callResult = [...obj.types];
  const handle = _nextStringList++;
  _stringListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-type()` operation.
 */
export function getType(self: bigint, type: string): bigint {
  const obj = lookupClipboardItem(self);
  const _callResult = obj.getType(type as any);
  const handle = _nextPromiseAny++;
  _promiseAnyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `supports()` operation.
 */
export function supports(type: string): bigint {
  return ClipboardItem.supports(type as any) ? 1n : 0n;
}

// ---------------------------------------------------------------------------
// WIT interface: clipboard
// ---------------------------------------------------------------------------

/** Type alias */
export type ClipboardHandle = bigint;

/** Handle table for Clipboard instances */
const _clipboardHandles = new Map<bigint, Clipboard>();
let _nextClipboard = 1n;

/** Lookup a Clipboard by handle, throwing if not found. */
function lookupClipboard(handle: bigint): Clipboard {
  const obj = _clipboardHandles.get(handle);
  if (!obj) {
    throw new Error(`Clipboard handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional Clipboard by handle. */
function lookupOptionClipboard(handle: bigint | undefined): Clipboard | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _clipboardHandles.get(handle) ?? null;
}
/**
 * `read()` operation.
 *
 * Async operation: returns request ID, poll with `pollRead()`
 */
export function read(self: bigint, formats: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupClipboard(self);
  const promise = obj.read()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `read()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollRead(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `read-text()` operation.
 *
 * Async operation: returns request ID, poll with `pollReadText()`
 */
export function readText(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupClipboard(self);
  const promise = obj.readText()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `readText()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollReadText(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `write()` operation.
 *
 * Async operation: returns request ID, poll with `pollWrite()`
 */
export function write(self: bigint, data: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupClipboard(self);
  const promise = obj.write(data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `write()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollWrite(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `write-text()` operation.
 *
 * Async operation: returns request ID, poll with `pollWriteText()`
 */
export function writeText(self: bigint, data: string): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupClipboard(self);
  const promise = obj.writeText(data)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `writeText()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollWriteText(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: pointer-event
// ---------------------------------------------------------------------------

/** Type alias */
export type PointerEventHandle = bigint;

/** Handle table for PointerEvent instances */
const _pointerEventhandles = new Map<bigint, PointerEvent>();
let _nextPointerEvent = 1n;

/** Lookup a PointerEvent by handle, throwing if not found. */
function lookupPointerEvent(handle: bigint): PointerEvent {
  const obj = _pointerEventhandles.get(handle);
  if (!obj) {
    throw new Error(`PointerEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional PointerEvent by handle. */
function lookupOptionPointerEvent(handle: bigint | undefined): PointerEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _pointerEventhandles.get(handle) ?? null;
}
/**
 * `get-pointer-id()` operation.
 */
export function getPointerId(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.pointerId);
}

/**
 * `get-width()` operation.
 */
export function getWidth(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.width);
}

/**
 * `get-height()` operation.
 */
export function getHeight(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.height);
}

/**
 * `get-pressure()` operation.
 */
export function getPressure(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.pressure);
}

/**
 * `get-tangential-pressure()` operation.
 */
export function getTangentialPressure(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.tangentialPressure);
}

/**
 * `get-tilt-x()` operation.
 */
export function getTiltX(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.tiltX);
}

/**
 * `get-tilt-y()` operation.
 */
export function getTiltY(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.tiltY);
}

/**
 * `get-twist()` operation.
 */
export function getTwist(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.twist);
}

/**
 * `get-altitude-angle()` operation.
 */
export function PointerEventGetAltitudeAngle(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.altitudeAngle);
}

/**
 * `get-azimuth-angle()` operation.
 */
export function PointerEventGetAzimuthAngle(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return BigInt(obj.azimuthAngle);
}

/**
 * `get-pointer-type()` operation.
 */
export function getPointerType(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  const value = obj.pointerType;
  switch (value) {
    case 'mouse': return 0n;
    case 'pen': return 1n;
    case 'touch': return 2n;
    default: return 0n;
  }
}

/**
 * `get-is-primary()` operation.
 */
export function getIsPrimary(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  return obj.isPrimary ? 1n : 0n;
}

/**
 * `get-persistent-device-id()` operation.
 */
export function getPersistentDeviceId(self: bigint): number {
  const obj = lookupPointerEvent(self);
  return (obj as any).persistentDeviceId;
}

/**
 * `get-coalesced-events()` operation.
 */
export function getCoalescedEvents(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  const _callResult = obj.getCoalescedEvents();
  const handle = _nextPointerEventList++;
  _pointerEventListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-predicted-events()` operation.
 */
export function getPredictedEvents(self: bigint): bigint {
  const obj = lookupPointerEvent(self);
  const _callResult = obj.getPredictedEvents();
  const handle = _nextPointerEventList++;
  _pointerEventListHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: wheel-event
// ---------------------------------------------------------------------------

/** Type alias */
export type WheelEventHandle = bigint;

/** Handle table for WheelEvent instances */
const _wheelEventhandles = new Map<bigint, WheelEvent>();
let _nextWheelEvent = 1n;

/** Lookup a WheelEvent by handle, throwing if not found. */
function lookupWheelEvent(handle: bigint): WheelEvent {
  const obj = _wheelEventhandles.get(handle);
  if (!obj) {
    throw new Error(`WheelEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional WheelEvent by handle. */
function lookupOptionWheelEvent(handle: bigint | undefined): WheelEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _wheelEventhandles.get(handle) ?? null;
}
/**
 * `get-delta-x()` operation.
 */
export function getDeltaX(self: bigint): number {
  const obj = lookupWheelEvent(self);
  return obj.deltaX;
}

/**
 * `get-delta-y()` operation.
 */
export function getDeltaY(self: bigint): number {
  const obj = lookupWheelEvent(self);
  return obj.deltaY;
}

/**
 * `get-delta-z()` operation.
 */
export function getDeltaZ(self: bigint): number {
  const obj = lookupWheelEvent(self);
  return obj.deltaZ;
}

/**
 * `get-delta-mode()` operation.
 */
export function getDeltaMode(self: bigint): bigint {
  const obj = lookupWheelEvent(self);
  return BigInt(obj.deltaMode);
}

// ---------------------------------------------------------------------------
// WIT interface: touch
// ---------------------------------------------------------------------------

/** Type alias */
export type TouchHandle = bigint;

/**
 * `get-identifier()` operation.
 */
export function getIdentifier(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.identifier);
}

/**
 * `get-target()` operation.
 */
export function getTarget(self: bigint): bigint {
  const obj = lookupTouch(self);
  const _callResult = obj.target;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-screen-x()` operation.
 */
export function getScreenX(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.screenX);
}

/**
 * `get-screen-y()` operation.
 */
export function getScreenY(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.screenY);
}

/**
 * `get-client-x()` operation.
 */
export function getClientX(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.clientX);
}

/**
 * `get-client-y()` operation.
 */
export function getClientY(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.clientY);
}

/**
 * `get-page-x()` operation.
 */
export function getPageX(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.pageX);
}

/**
 * `get-page-y()` operation.
 */
export function getPageY(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.pageY);
}

/**
 * `get-radius-x()` operation.
 */
export function getRadiusX(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.radiusX);
}

/**
 * `get-radius-y()` operation.
 */
export function getRadiusY(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.radiusY);
}

/**
 * `get-rotation-angle()` operation.
 */
export function getRotationAngle(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.rotationAngle);
}

/**
 * `get-force()` operation.
 */
export function getForce(self: bigint): bigint {
  const obj = lookupTouch(self);
  return BigInt(obj.force);
}

/**
 * `get-altitude-angle()` operation.
 */
export function TouchGetAltitudeAngle(self: bigint): number {
  const obj = lookupTouch(self);
  return (obj as any).altitudeAngle;
}

/**
 * `get-azimuth-angle()` operation.
 */
export function TouchGetAzimuthAngle(self: bigint): boolean {
  const obj = lookupTouch(self);
  return (obj as any).azimuthAngle;
}

/**
 * `get-touch-type()` operation.
 */
export function getTouchType(self: bigint): boolean {
  const obj = lookupTouch(self);
  return (obj as any).touchType;
}

// ---------------------------------------------------------------------------
// WIT interface: touch-list
// ---------------------------------------------------------------------------

/** Type alias */
export type TouchListHandle = bigint;

/**
 * `get-length()` operation.
 */
export function getLength(self: bigint): number {
  const obj = lookupTouchList(self);
  return obj.length;
}

/**
 * `item()` operation.
 */
export function item(self: bigint, index: number): bigint | undefined {
  const obj = lookupTouchList(self);
  const _callResult = obj.item(Number(index));
  if (_callResult === null) return undefined;
  const handle = _nextTouch++;
  _touchHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: touch-event
// ---------------------------------------------------------------------------

/** Type alias */
export type TouchEventHandle = bigint;

/** Handle table for TouchEvent instances */
const _touchEventhandles = new Map<bigint, TouchEvent>();
let _nextTouchEvent = 1n;

/** Lookup a TouchEvent by handle, throwing if not found. */
function lookupTouchEvent(handle: bigint): TouchEvent {
  const obj = _touchEventhandles.get(handle);
  if (!obj) {
    throw new Error(`TouchEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional TouchEvent by handle. */
function lookupOptionTouchEvent(handle: bigint | undefined): TouchEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _touchEventhandles.get(handle) ?? null;
}
/**
 * `get-touches()` operation.
 */
export function getTouches(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  const _callResult = obj.touches;
  const handle = _nextTouchList++;
  _touchListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-target-touches()` operation.
 */
export function getTargetTouches(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  const _callResult = obj.targetTouches;
  const handle = _nextTouchList++;
  _touchListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-changed-touches()` operation.
 */
export function getChangedTouches(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  const _callResult = obj.changedTouches;
  const handle = _nextTouchList++;
  _touchListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-alt-key()` operation.
 */
export function TouchEventGetAltKey(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  return obj.altKey ? 1n : 0n;
}

/**
 * `get-meta-key()` operation.
 */
export function TouchEventGetMetaKey(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  return obj.metaKey ? 1n : 0n;
}

/**
 * `get-ctrl-key()` operation.
 */
export function TouchEventGetCtrlKey(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  return obj.ctrlKey ? 1n : 0n;
}

/**
 * `get-shift-key()` operation.
 */
export function TouchEventGetShiftKey(self: bigint): bigint {
  const obj = lookupTouchEvent(self);
  return obj.shiftKey ? 1n : 0n;
}

/**
 * `get-modifier-state()` operation.
 */
export function TouchEventGetModifierState(self: bigint, keyArg: string): boolean {
  const obj = lookupTouchEvent(self);
  return (obj as any).getModifierState(keyArg);
}

// ---------------------------------------------------------------------------
// WIT interface: ui-event
// ---------------------------------------------------------------------------

/** Type alias */
export type UiEventHandle = bigint;

/** Handle table for UIEvent instances */
const _uiEventhandles = new Map<bigint, UIEvent>();
let _nextUIEvent = 1n;

/** Lookup a UIEvent by handle, throwing if not found. */
function lookupUIEvent(handle: bigint): UIEvent {
  const obj = _uiEventhandles.get(handle);
  if (!obj) {
    throw new Error(`UIEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional UIEvent by handle. */
function lookupOptionUIEvent(handle: bigint | undefined): UIEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _uiEventhandles.get(handle) ?? null;
}
/**
 * `get-view()` operation.
 */
export function getView(self: bigint): bigint | undefined {
  const obj = lookupUIEvent(self);
  const _callResult = obj.view;
  if (_callResult === null) return undefined;
  const handle = _nextWindow++;
  _windowHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-detail()` operation.
 */
export function getDetail(self: bigint): bigint {
  const obj = lookupUIEvent(self);
  return BigInt(obj.detail);
}

/**
 * `init-ui-event()` operation.
 */
export function initUiEvent(self: bigint, typeArg: string, bubblesArg: bigint, cancelableArg: boolean | undefined, viewArg: bigint | undefined, detailArg: number | undefined): void {
  const obj = lookupUIEvent(self);
  obj.initUIEvent(typeArg as any, bubblesArg !== undefined ? Boolean(bubblesArg) : undefined, cancelableArg !== undefined ? Boolean(cancelableArg) : undefined, lookupOptionWindow(viewArg), Number(detailArg));
}

/**
 * `get-which()` operation.
 */
export function getWhich(self: bigint): bigint {
  const obj = lookupUIEvent(self);
  return BigInt(obj.which);
}

// ---------------------------------------------------------------------------
// WIT interface: focus-event
// ---------------------------------------------------------------------------

/** Type alias */
export type FocusEventHandle = bigint;

/** Handle table for FocusEvent instances */
const _focusEventhandles = new Map<bigint, FocusEvent>();
let _nextFocusEvent = 1n;

/** Lookup a FocusEvent by handle, throwing if not found. */
function lookupFocusEvent(handle: bigint): FocusEvent {
  const obj = _focusEventhandles.get(handle);
  if (!obj) {
    throw new Error(`FocusEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional FocusEvent by handle. */
function lookupOptionFocusEvent(handle: bigint | undefined): FocusEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _focusEventhandles.get(handle) ?? null;
}
/**
 * `get-related-target()` operation.
 */
export function getRelatedTarget(self: bigint): bigint | undefined {
  const obj = lookupFocusEvent(self);
  const _callResult = obj.relatedTarget;
  if (_callResult === null) return undefined;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: input-event
// ---------------------------------------------------------------------------

/** Type alias */
export type InputEventHandle = bigint;

/** Handle table for InputEvent instances */
const _inputEventhandles = new Map<bigint, InputEvent>();
let _nextInputEvent = 1n;

/** Lookup a InputEvent by handle, throwing if not found. */
function lookupInputEvent(handle: bigint): InputEvent {
  const obj = _inputEventhandles.get(handle);
  if (!obj) {
    throw new Error(`InputEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional InputEvent by handle. */
function lookupOptionInputEvent(handle: bigint | undefined): InputEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _inputEventhandles.get(handle) ?? null;
}
/**
 * `get-data()` operation.
 */
export function InputEventGetData(self: bigint): string | undefined {
  const obj = lookupInputEvent(self);
  return obj.data ?? undefined;
}

/**
 * `get-is-composing()` operation.
 */
export function InputEventGetIsComposing(self: bigint): bigint {
  const obj = lookupInputEvent(self);
  return obj.isComposing ? 1n : 0n;
}

/**
 * `get-input-type()` operation.
 */
export function getInputType(self: bigint): bigint {
  const obj = lookupInputEvent(self);
  const value = obj.inputType;
  switch (value) {
    case 'insertText': return 0n;
    case 'insertReplacementText': return 1n;
    case 'insertLineBreak': return 2n;
    case 'insertParagraph': return 3n;
    case 'insertFromPaste': return 4n;
    case 'deleteContentBackward': return 5n;
    case 'deleteContentForward': return 6n;
    case 'deleteByCut': return 7n;
    case 'historyUndo': return 8n;
    case 'historyRedo': return 9n;
    default: return 0n;
  }
}

// ---------------------------------------------------------------------------
// WIT interface: keyboard-event
// ---------------------------------------------------------------------------

/** Type alias */
export type KeyboardEventHandle = bigint;

/** Handle table for KeyboardEvent instances */
const _keyboardEventhandles = new Map<bigint, KeyboardEvent>();
let _nextKeyboardEvent = 1n;

/** Lookup a KeyboardEvent by handle, throwing if not found. */
function lookupKeyboardEvent(handle: bigint): KeyboardEvent {
  const obj = _keyboardEventhandles.get(handle);
  if (!obj) {
    throw new Error(`KeyboardEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional KeyboardEvent by handle. */
function lookupOptionKeyboardEvent(handle: bigint | undefined): KeyboardEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _keyboardEventhandles.get(handle) ?? null;
}
/**
 * `get-key()` operation.
 */
export function getKey(self: bigint): bigint {
  const obj = lookupKeyboardEvent(self);
  const value = obj.key;
  switch ((value as any)) {
    default: return 0n;
  }
}

/**
 * `get-code()` operation.
 */
export function getCode(self: bigint): string {
  const obj = lookupKeyboardEvent(self);
  return obj.code;
}

/**
 * `get-location()` operation.
 */
export function getLocation(self: bigint): number {
  const obj = lookupKeyboardEvent(self);
  return obj.location;
}

/**
 * `get-ctrl-key()` operation.
 */
export function KeyboardEventGetCtrlKey(self: bigint): bigint {
  const obj = lookupKeyboardEvent(self);
  return obj.ctrlKey ? 1n : 0n;
}

/**
 * `get-shift-key()` operation.
 */
export function KeyboardEventGetShiftKey(self: bigint): boolean {
  const obj = lookupKeyboardEvent(self);
  return obj.shiftKey;
}

/**
 * `get-alt-key()` operation.
 */
export function KeyboardEventGetAltKey(self: bigint): bigint {
  const obj = lookupKeyboardEvent(self);
  return obj.altKey ? 1n : 0n;
}

/**
 * `get-meta-key()` operation.
 */
export function KeyboardEventGetMetaKey(self: bigint): boolean {
  const obj = lookupKeyboardEvent(self);
  return obj.metaKey;
}

/**
 * `get-repeat()` operation.
 */
export function getRepeat(self: bigint): bigint {
  const obj = lookupKeyboardEvent(self);
  return obj.repeat ? 1n : 0n;
}

/**
 * `get-is-composing()` operation.
 */
export function KeyboardEventGetIsComposing(self: bigint): bigint {
  const obj = lookupKeyboardEvent(self);
  return obj.isComposing ? 1n : 0n;
}

/**
 * `get-modifier-state()` operation.
 */
export function KeyboardEventGetModifierState(self: bigint, keyArg: string): boolean {
  const obj = lookupKeyboardEvent(self);
  return obj.getModifierState(keyArg);
}

/**
 * `init-keyboard-event()` operation.
 */
export function initKeyboardEvent(self: bigint, typeArg: string, bubblesArg: number | undefined, cancelableArg: string, viewArg: bigint | undefined, keyArg: string | undefined, locationArg: string | undefined, ctrlKey: boolean | undefined, altKey: boolean | undefined, shiftKey: boolean | undefined, metaKey: string | undefined): void {
  const obj = lookupKeyboardEvent(self);
  obj.initKeyboardEvent(typeArg as any, bubblesArg !== undefined ? Boolean(bubblesArg) : undefined, cancelableArg !== undefined ? Boolean(cancelableArg) : undefined, lookupOptionWindow(viewArg), keyArg as any, locationArg, Boolean(ctrlKey), Boolean(altKey), Boolean(shiftKey), metaKey !== undefined ? Boolean(metaKey) : undefined);
}

/**
 * `get-char-code()` operation.
 */
export function getCharCode(self: bigint): number {
  const obj = lookupKeyboardEvent(self);
  return obj.charCode;
}

/**
 * `get-key-code()` operation.
 */
export function getKeyCode(self: bigint): number {
  const obj = lookupKeyboardEvent(self);
  return obj.keyCode;
}

// ---------------------------------------------------------------------------
// WIT interface: composition-event
// ---------------------------------------------------------------------------

/** Type alias */
export type CompositionEventHandle = bigint;

/** Handle table for CompositionEvent instances */
const _compositionEventhandles = new Map<bigint, CompositionEvent>();
let _nextCompositionEvent = 1n;

/** Lookup a CompositionEvent by handle, throwing if not found. */
function lookupCompositionEvent(handle: bigint): CompositionEvent {
  const obj = _compositionEventhandles.get(handle);
  if (!obj) {
    throw new Error(`CompositionEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional CompositionEvent by handle. */
function lookupOptionCompositionEvent(handle: bigint | undefined): CompositionEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _compositionEventhandles.get(handle) ?? null;
}
/**
 * `get-data()` operation.
 */
export function CompositionEventGetData(self: bigint): bigint {
  const obj = lookupCompositionEvent(self);
  const value = obj.data;
  switch ((value as any)) {
    default: return 0n;
  }
}

/**
 * `init-composition-event()` operation.
 */
export function initCompositionEvent(self: bigint, typeArg: string, bubblesArg: boolean | undefined, cancelableArg: boolean | undefined, viewArg: bigint | undefined, dataArg: string | undefined): void {
  const obj = lookupCompositionEvent(self);
  obj.initCompositionEvent(typeArg as any, bubblesArg, cancelableArg, lookupOptionWindow(viewArg), dataArg as any);
}

// ---------------------------------------------------------------------------
// WIT interface: text-event
// ---------------------------------------------------------------------------

/** Type alias */
export type TextEventHandle = bigint;

/** Handle table for TextEvent instances */
const _textEventhandles = new Map<bigint, TextEvent>();
let _nextTextEvent = 1n;

/** Lookup a TextEvent by handle, throwing if not found. */
function lookupTextEvent(handle: bigint): TextEvent {
  const obj = _textEventhandles.get(handle);
  if (!obj) {
    throw new Error(`TextEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional TextEvent by handle. */
function lookupOptionTextEvent(handle: bigint | undefined): TextEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _textEventhandles.get(handle) ?? null;
}
/**
 * `get-data()` operation.
 */
export function TextEventGetData(self: bigint): string {
  const obj = lookupTextEvent(self);
  return obj.data;
}

/**
 * `init-text-event()` operation.
 */
export function initTextEvent(self: bigint, type: string, bubbles: boolean | undefined, cancelable: boolean | undefined, view: boolean | undefined, data: string | undefined): void {
  const obj = lookupTextEvent(self);
  obj.initTextEvent(type, Boolean(bubbles), Boolean(cancelable), lookupOptionWindow(view), data);
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  getClipboardData,
  ClipboardChangeEventGetTypes,
  getChangeId,
  getPresentationStyle,
  ClipboardItemGetTypes,
  getType,
  supports,
  read,
  pollRead,
  readText,
  pollReadText,
  write,
  pollWrite,
  writeText,
  pollWriteText,
  getPointerId,
  getWidth,
  getHeight,
  getPressure,
  getTangentialPressure,
  getTiltX,
  getTiltY,
  getTwist,
  PointerEventGetAltitudeAngle,
  PointerEventGetAzimuthAngle,
  getPointerType,
  getIsPrimary,
  getPersistentDeviceId,
  getCoalescedEvents,
  getPredictedEvents,
  getDeltaX,
  getDeltaY,
  getDeltaZ,
  getDeltaMode,
  getIdentifier,
  getTarget,
  getScreenX,
  getScreenY,
  getClientX,
  getClientY,
  getPageX,
  getPageY,
  getRadiusX,
  getRadiusY,
  getRotationAngle,
  getForce,
  TouchGetAltitudeAngle,
  TouchGetAzimuthAngle,
  getTouchType,
  getLength,
  item,
  getTouches,
  getTargetTouches,
  getChangedTouches,
  TouchEventGetAltKey,
  TouchEventGetMetaKey,
  TouchEventGetCtrlKey,
  TouchEventGetShiftKey,
  TouchEventGetModifierState,
  getView,
  getDetail,
  initUiEvent,
  getWhich,
  getRelatedTarget,
  InputEventGetData,
  InputEventGetIsComposing,
  getInputType,
  getKey,
  getCode,
  getLocation,
  KeyboardEventGetCtrlKey,
  KeyboardEventGetShiftKey,
  KeyboardEventGetAltKey,
  KeyboardEventGetMetaKey,
  getRepeat,
  KeyboardEventGetIsComposing,
  KeyboardEventGetModifierState,
  initKeyboardEvent,
  getCharCode,
  getKeyCode,
  CompositionEventGetData,
  initCompositionEvent,
  TextEventGetData,
  initTextEvent
};
