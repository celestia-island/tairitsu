/**
 * dom glue — implements the `tairitsu-browser:dom` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-worlds/wit/generated/dom.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for abort-signal values */
const _abortSignalHandles = new Map<bigint, AbortSignal>();
let _nextAbortSignal = 1n;

/** Handle table for attr values */
const _attrHandles = new Map<bigint, Attr>();
let _nextAttr = 1n;

/** Handle table for document values */
const _documentHandles = new Map<bigint, Document>();
let _nextDocument = 1n;

/** Handle table for document-fragment values */
const _documentFragmentHandles = new Map<bigint, DocumentFragment>();
let _nextDocumentFragment = 1n;

/** Handle table for document-type values */
const _documentTypeHandles = new Map<bigint, DocumentType>();
let _nextDocumentType = 1n;

/** Handle table for element values */
const _elementHandles = new Map<bigint, Element>();
let _nextElement = 1n;

/** Handle table for event values */
const _eventHandles = new Map<bigint, Event>();
let _nextEvent = 1n;

/** Handle table for event-listener values */
const _eventListenerHandles = new Map<bigint, EventListener>();
let _nextEventListener = 1n;

/** Handle table for event-target values */
const _eventTargetHandles = new Map<bigint, EventTarget>();
let _nextEventTarget = 1n;

/** Handle table for event-target-list values */
const _eventTargetListHandles = new Map<bigint, EventTarget[]>();
let _nextEventTargetList = 1n;

/** Handle table for html-collection values */
const _htmlCollectionHandles = new Map<bigint, HTMLCollection>();
let _nextHtmlCollection = 1n;

/** Handle table for html-slot-element values */
const _htmlSlotElementHandles = new Map<bigint, HTMLSlotElement>();
let _nextHtmlSlotElement = 1n;

/** Handle table for mutation-record-list values */
const _mutationRecordListHandles = new Map<bigint, MutationRecord[]>();
let _nextMutationRecordList = 1n;

/** Handle table for node values */
const _nodeHandles = new Map<bigint, Node>();
let _nextNode = 1n;

/** Handle table for node-filter values */
const _nodeFilterHandles = new Map<bigint, NodeFilter>();
let _nextNodeFilter = 1n;

/** Handle table for node-list values */
const _nodeListHandles = new Map<bigint, NodeList>();
let _nextNodeList = 1n;

/** Handle table for number values */
const _numberHandles = new Map<bigint, number>();
let _nextNumber = 1n;

/** Handle table for string values */
const _stringHandles = new Map<bigint, string>();
let _nextString = 1n;

/** Handle table for text values */
const _textHandles = new Map<bigint, Text>();
let _nextText = 1n;

/** Handle table for xpath-expression values */
const _xpathExpressionHandles = new Map<bigint, XPathExpression>();
let _nextXpathExpression = 1n;

/** Handle table for xpath-ns-resolver values */
const _xpathNsResolverHandles = new Map<bigint, XPathNSResolver>();
let _nextXpathNsResolver = 1n;

/** Handle table for xpath-result values */
const _xpathResultHandles = new Map<bigint, XPathResult>();
let _nextXpathResult = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a abort-signal value by handle. */
function lookupAbortSignal(handle: bigint): AbortSignal {
  const obj = _abortSignalHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`abort-signal handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional abort-signal value by handle. */
function lookupOptionAbortSignal(handle: bigint | undefined): AbortSignal | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _abortSignalHandles.get(handle) ?? null;
}

/** Lookup a attr value by handle. */
function lookupAttr(handle: bigint): Attr {
  const obj = _attrHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`attr handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional attr value by handle. */
function lookupOptionAttr(handle: bigint | undefined): Attr | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _attrHandles.get(handle) ?? null;
}

/** Lookup a document value by handle. */
function lookupDocument(handle: bigint): Document {
  const obj = _documentHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document value by handle. */
function lookupOptionDocument(handle: bigint | undefined): Document | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentHandles.get(handle) ?? null;
}

/** Lookup a document-fragment value by handle. */
function lookupDocumentFragment(handle: bigint): DocumentFragment {
  const obj = _documentFragmentHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document-fragment handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document-fragment value by handle. */
function lookupOptionDocumentFragment(handle: bigint | undefined): DocumentFragment | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentFragmentHandles.get(handle) ?? null;
}

/** Lookup a document-type value by handle. */
function lookupDocumentType(handle: bigint): DocumentType {
  const obj = _documentTypeHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document-type handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document-type value by handle. */
function lookupOptionDocumentType(handle: bigint | undefined): DocumentType | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentTypeHandles.get(handle) ?? null;
}

/** Lookup a element value by handle. */
function lookupElement(handle: bigint): Element {
  const obj = _elementHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`element handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional element value by handle. */
function lookupOptionElement(handle: bigint | undefined): Element | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _elementHandles.get(handle) ?? null;
}

/** Lookup a event value by handle. */
function lookupEvent(handle: bigint): Event {
  const obj = _eventHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event value by handle. */
function lookupOptionEvent(handle: bigint | undefined): Event | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventHandles.get(handle) ?? null;
}

/** Lookup a event-listener value by handle. */
function lookupEventListener(handle: bigint): EventListener {
  const obj = _eventListenerHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-listener handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-listener value by handle. */
function lookupOptionEventListener(handle: bigint | undefined): EventListener | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventListenerHandles.get(handle) ?? null;
}

/** Lookup a event-target value by handle. */
function lookupEventTarget(handle: bigint): EventTarget {
  const obj = _eventTargetHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-target handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-target value by handle. */
function lookupOptionEventTarget(handle: bigint | undefined): EventTarget | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventTargetHandles.get(handle) ?? null;
}

/** Lookup a event-target-list value by handle. */
function lookupEventTargetList(handle: bigint): EventTarget[] {
  const obj = _eventTargetListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-target-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-target-list value by handle. */
function lookupOptionEventTargetList(handle: bigint | undefined): EventTarget[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventTargetListHandles.get(handle) ?? null;
}

/** Lookup a html-collection value by handle. */
function lookupHtmlCollection(handle: bigint): HTMLCollection {
  const obj = _htmlCollectionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`html-collection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional html-collection value by handle. */
function lookupOptionHtmlCollection(handle: bigint | undefined): HTMLCollection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlCollectionHandles.get(handle) ?? null;
}

/** Lookup a html-slot-element value by handle. */
function lookupHtmlSlotElement(handle: bigint): HTMLSlotElement {
  const obj = _htmlSlotElementHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`html-slot-element handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional html-slot-element value by handle. */
function lookupOptionHtmlSlotElement(handle: bigint | undefined): HTMLSlotElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlSlotElementHandles.get(handle) ?? null;
}

/** Lookup a mutation-record-list value by handle. */
function lookupMutationRecordList(handle: bigint): MutationRecord[] {
  const obj = _mutationRecordListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`mutation-record-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional mutation-record-list value by handle. */
function lookupOptionMutationRecordList(handle: bigint | undefined): MutationRecord[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mutationRecordListHandles.get(handle) ?? null;
}

/** Lookup a node value by handle. */
function lookupNode(handle: bigint): Node {
  const obj = _nodeHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node value by handle. */
function lookupOptionNode(handle: bigint | undefined): Node | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeHandles.get(handle) ?? null;
}

/** Lookup a node-filter value by handle. */
function lookupNodeFilter(handle: bigint): NodeFilter {
  const obj = _nodeFilterHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node-filter handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node-filter value by handle. */
function lookupOptionNodeFilter(handle: bigint | undefined): NodeFilter | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeFilterHandles.get(handle) ?? null;
}

/** Lookup a node-list value by handle. */
function lookupNodeList(handle: bigint): NodeList {
  const obj = _nodeListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node-list value by handle. */
function lookupOptionNodeList(handle: bigint | undefined): NodeList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeListHandles.get(handle) ?? null;
}

/** Lookup a number value by handle. */
function lookupNumber(handle: bigint): number {
  const obj = _numberHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`number handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional number value by handle. */
function lookupOptionNumber(handle: bigint | undefined): number | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _numberHandles.get(handle) ?? null;
}

/** Lookup a string value by handle. */
function lookupString(handle: bigint): string {
  const obj = _stringHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string value by handle. */
function lookupOptionString(handle: bigint | undefined): string | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringHandles.get(handle) ?? null;
}

/** Lookup a text value by handle. */
function lookupText(handle: bigint): Text {
  const obj = _textHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`text handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional text value by handle. */
function lookupOptionText(handle: bigint | undefined): Text | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _textHandles.get(handle) ?? null;
}

/** Lookup a xpath-expression value by handle. */
function lookupXpathExpression(handle: bigint): XPathExpression {
  const obj = _xpathExpressionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`xpath-expression handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional xpath-expression value by handle. */
function lookupOptionXpathExpression(handle: bigint | undefined): XPathExpression | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xpathExpressionHandles.get(handle) ?? null;
}

/** Lookup a xpath-ns-resolver value by handle. */
function lookupXpathNsResolver(handle: bigint): XPathNSResolver {
  const obj = _xpathNsResolverHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`xpath-ns-resolver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional xpath-ns-resolver value by handle. */
function lookupOptionXpathNsResolver(handle: bigint | undefined): XPathNSResolver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xpathNsResolverHandles.get(handle) ?? null;
}

/** Lookup a xpath-result value by handle. */
function lookupXpathResult(handle: bigint): XPathResult {
  const obj = _xpathResultHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`xpath-result handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional xpath-result value by handle. */
function lookupOptionXpathResult(handle: bigint | undefined): XPathResult | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xpathResultHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: event
// ---------------------------------------------------------------------------

/** Type alias */
export type EventHandle = bigint;

/**
 * `get-type()` operation.
 */
export function EventGetType(self: bigint): string {
  const obj = lookupEvent(self);
  return (obj as any).getType();
}

/**
 * `get-target()` operation.
 */
export function EventGetTarget(self: bigint): bigint | undefined {
  const obj = lookupEvent(self);
  const _callResult = obj.target;
  if (_callResult === null) return undefined;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-src-element()` operation.
 */
export function getSrcElement(self: bigint): bigint | undefined {
  const obj = lookupEvent(self);
  const _callResult = obj.srcElement;
  if (_callResult === null) return undefined;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-current-target()` operation.
 */
export function getCurrentTarget(self: bigint): bigint | undefined {
  const obj = lookupEvent(self);
  const _callResult = obj.currentTarget;
  if (_callResult === null) return undefined;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

/**
 * `composed-path()` operation.
 */
export function composedPath(self: bigint): bigint {
  const obj = lookupEvent(self);
  const _callResult = obj.composedPath();
  const handle = _nextEventTargetList++;
  _eventTargetListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-event-phase()` operation.
 */
export function getEventPhase(self: bigint): bigint {
  const obj = lookupEvent(self);
  return BigInt(obj.eventPhase);
}

/**
 * `stop-propagation()` operation.
 */
export function stopPropagation(self: bigint): void {
  const obj = lookupEvent(self);
  obj.stopPropagation();
}

/**
 * `get-cancel-bubble()` operation.
 */
export function getCancelBubble(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.cancelBubble ? 1n : 0n;
}

/**
 * `set-cancel-bubble()` operation.
 */
export function setCancelBubble(self: bigint, value: boolean): void {
  const obj = lookupEvent(self);
  obj.cancelBubble = value;
}

/**
 * `stop-immediate-propagation()` operation.
 */
export function stopImmediatePropagation(self: bigint): void {
  const obj = lookupEvent(self);
  obj.stopImmediatePropagation();
}

/**
 * `get-bubbles()` operation.
 */
export function getBubbles(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.bubbles ? 1n : 0n;
}

/**
 * `get-cancelable()` operation.
 */
export function getCancelable(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.cancelable ? 1n : 0n;
}

/**
 * `get-return-value()` operation.
 */
export function getReturnValue(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.returnValue ? 1n : 0n;
}

/**
 * `set-return-value()` operation.
 */
export function setReturnValue(self: bigint, value: boolean): void {
  const obj = lookupEvent(self);
  obj.returnValue = value;
}

/**
 * `prevent-default()` operation.
 */
export function preventDefault(self: bigint): void {
  const obj = lookupEvent(self);
  obj.preventDefault();
}

/**
 * `get-default-prevented()` operation.
 */
export function getDefaultPrevented(self: bigint): boolean {
  const obj = lookupEvent(self);
  return obj.defaultPrevented;
}

/**
 * `get-composed()` operation.
 */
export function getComposed(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.composed ? 1n : 0n;
}

/**
 * `get-is-trusted()` operation.
 */
export function getIsTrusted(self: bigint): bigint {
  const obj = lookupEvent(self);
  return obj.isTrusted ? 1n : 0n;
}

/**
 * `get-time-stamp()` operation.
 */
export function getTimeStamp(self: bigint): bigint {
  const obj = lookupEvent(self);
  return BigInt(obj.timeStamp);
}

/**
 * `init-event()` operation.
 */
export function initEvent(self: bigint, type: string, bubbles: boolean | undefined, cancelable: boolean | undefined): void {
  const obj = lookupEvent(self);
  obj.initEvent(type as any, bubbles !== undefined ? Boolean(bubbles) : undefined, cancelable !== undefined ? Boolean(cancelable) : undefined);
}

// ---------------------------------------------------------------------------
// WIT interface: custom-event
// ---------------------------------------------------------------------------

/** Type alias */
export type CustomEventHandle = bigint;

/** Handle table for CustomEvent instances */
const _customEventhandles = new Map<bigint, CustomEvent>();
let _nextCustomEvent = 1n;

/** Lookup a CustomEvent by handle, throwing if not found. */
function lookupCustomEvent(handle: bigint): CustomEvent {
  const obj = _customEventhandles.get(handle);
  if (!obj) {
    throw new Error(`CustomEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional CustomEvent by handle. */
function lookupOptionCustomEvent(handle: bigint | undefined): CustomEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _customEventhandles.get(handle) ?? null;
}
/**
 * `get-detail()` operation.
 */
export function getDetail(self: bigint): string {
  const obj = lookupCustomEvent(self);
  return obj.detail;
}

/**
 * `init-custom-event()` operation.
 */
export function initCustomEvent(self: bigint, type: string, bubbles: boolean | undefined, cancelable: boolean | undefined, detail: string | undefined): void {
  const obj = lookupCustomEvent(self);
  obj.initCustomEvent(type, bubbles, cancelable, detail);
}

// ---------------------------------------------------------------------------
// WIT interface: event-target
// ---------------------------------------------------------------------------

/** Type alias */
export type EventTargetHandle = bigint;

/**
 * `add-event-listener()` operation.
 */
export function addEventListener(self: bigint, type: string, callback: bigint | undefined, options: bigint | undefined): void {
  const obj = lookupEventTarget(self);
  obj.addEventListener(type, callback !== undefined ? lookupEventListener(callback) : null, options as any);
}

/**
 * `remove-event-listener()` operation.
 */
export function removeEventListener(self: bigint, type: string, callback: bigint | undefined, options: bigint | undefined): void {
  const obj = lookupEventTarget(self);
  obj.removeEventListener(type, callback !== undefined ? lookupEventListener(callback) : null, options as any);
}

/**
 * `dispatch-event()` operation.
 */
export function dispatchEvent(self: bigint, event: bigint): boolean {
  const obj = lookupEventTarget(self);
  return (obj as any).dispatchEvent(lookupEvent(event));
}

// ---------------------------------------------------------------------------
// WIT interface: event-listener
// ---------------------------------------------------------------------------

/** Type alias */
export type EventListenerHandle = bigint;

/**
 * `handle-event()` operation.
 */
export function handleEvent(self: bigint, event: bigint): void {
  const obj = lookupEventListener(self);
  (obj as any).handleEvent(event);
}

// ---------------------------------------------------------------------------
// WIT interface: abort-controller
// ---------------------------------------------------------------------------

/** Type alias */
export type AbortControllerHandle = bigint;

/** Handle table for AbortController instances */
const _abortControllerhandles = new Map<bigint, AbortController>();
let _nextAbortController = 1n;

/** Lookup a AbortController by handle, throwing if not found. */
function lookupAbortController(handle: bigint): AbortController {
  const obj = _abortControllerhandles.get(handle);
  if (!obj) {
    throw new Error(`AbortController handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AbortController by handle. */
function lookupOptionAbortController(handle: bigint | undefined): AbortController | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _abortControllerhandles.get(handle) ?? null;
}
/**
 * `get-signal()` operation.
 */
export function getSignal(self: bigint): bigint {
  const obj = lookupAbortController(self);
  const _callResult = obj.signal;
  const handle = _nextAbortSignal++;
  _abortSignalHandles.set(handle, _callResult);
  return handle;
}

/**
 * `abort()` operation.
 */
export function AbortControllerAbort(self: bigint, reason: string | undefined): void {
  const obj = lookupAbortController(self);
  obj.abort(reason as any);
}

// ---------------------------------------------------------------------------
// WIT interface: abort-signal
// ---------------------------------------------------------------------------

/** Type alias */
export type AbortSignalHandle = bigint;

/**
 * `abort()` operation.
 */
export function AbortSignalAbort(reason: string | undefined): bigint {
  const _callResult = AbortSignal.abort(reason);
  const handle = _nextAbortSignal++;
  _abortSignalHandles.set(handle, _callResult);
  return handle;
}

/**
 * `timeout()` operation.
 */
export function timeout(milliseconds: bigint): bigint {
  const _callResult = AbortSignal.timeout(Number(milliseconds));
  const handle = _nextAbortSignal++;
  _abortSignalHandles.set(handle, _callResult);
  return handle;
}

/**
 * `any()` operation.
 */
export function any(signals: (bigint)[]): bigint {
  const _callResult = AbortSignal.any(Array.from(signals).map((h: bigint) => lookupAbortSignal(h)));
  const handle = _nextAbortSignal++;
  _abortSignalHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-aborted()` operation.
 */
export function getAborted(self: bigint): bigint {
  const obj = lookupAbortSignal(self);
  return obj.aborted ? 1n : 0n;
}

/**
 * `get-reason()` operation.
 */
export function getReason(self: bigint): string {
  const obj = lookupAbortSignal(self);
  return obj.reason;
}

/**
 * `throw-if-aborted()` operation.
 */
export function throwIfAborted(self: bigint): void {
  const obj = lookupAbortSignal(self);
  obj.throwIfAborted();
}

/**
 * `get-onabort()` operation.
 */
export function getOnabort(self: bigint): EventHandlerRecord {
  const obj = lookupAbortSignal(self);
  return obj.onabort;
}

/**
 * `set-onabort()` operation.
 */
export function setOnabort(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupAbortSignal(self);
  obj.onabort = value;
}

// ---------------------------------------------------------------------------
// WIT interface: non-element-parent-node
// ---------------------------------------------------------------------------

/** Type alias */
export type NonElementParentNodeHandle = bigint;

/** Handle table for NonElementParentNode instances */
const _nonElementParentNodehandles = new Map<bigint, NonElementParentNode>();
let _nextNonElementParentNode = 1n;

/** Lookup a NonElementParentNode by handle, throwing if not found. */
function lookupNonElementParentNode(handle: bigint): NonElementParentNode {
  const obj = _nonElementParentNodehandles.get(handle);
  if (!obj) {
    throw new Error(`NonElementParentNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional NonElementParentNode by handle. */
function lookupOptionNonElementParentNode(handle: bigint | undefined): NonElementParentNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nonElementParentNodehandles.get(handle) ?? null;
}
/**
 * `get-element-by-id()` operation.
 */
export function getElementById(self: bigint, elementId: string): bigint | undefined {
  const obj = lookupNonElementParentNode(self);
  const _callResult = obj.getElementById(elementId);
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: parent-node
// ---------------------------------------------------------------------------

/** Type alias */
export type ParentNodeHandle = bigint;

/** Handle table for ParentNode instances */
const _parentNodehandles = new Map<bigint, ParentNode>();
let _nextParentNode = 1n;

/** Lookup a ParentNode by handle, throwing if not found. */
function lookupParentNode(handle: bigint): ParentNode {
  const obj = _parentNodehandles.get(handle);
  if (!obj) {
    throw new Error(`ParentNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ParentNode by handle. */
function lookupOptionParentNode(handle: bigint | undefined): ParentNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _parentNodehandles.get(handle) ?? null;
}
/**
 * `get-children()` operation.
 */
export function getChildren(self: bigint): bigint {
  const obj = lookupParentNode(self);
  const _callResult = obj.children;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-first-element-child()` operation.
 */
export function getFirstElementChild(self: bigint): bigint | undefined {
  const obj = lookupParentNode(self);
  const _callResult = obj.firstElementChild;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-last-element-child()` operation.
 */
export function getLastElementChild(self: bigint): bigint | undefined {
  const obj = lookupParentNode(self);
  const _callResult = obj.lastElementChild;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-child-element-count()` operation.
 */
export function getChildElementCount(self: bigint): bigint {
  const obj = lookupParentNode(self);
  const _callResult = obj.childElementCount;
  const handle = _nextNumber++;
  _numberHandles.set(handle, _callResult);
  return handle;
}

/**
 * `prepend()` operation.
 */
export function prepend(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupParentNode(self);
  obj.prepend(nodes as any);
}

/**
 * `append()` operation.
 */
export function append(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupParentNode(self);
  obj.append(nodes as any);
}

/**
 * `replace-children()` operation.
 */
export function replaceChildren(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupParentNode(self);
  obj.replaceChildren(nodes as any);
}

/**
 * `move-before()` operation.
 */
export function moveBefore(self: bigint, node: bigint, child: bigint | undefined): void {
  const obj = lookupParentNode(self);
  (obj as any).moveBefore(node, child);
}

/**
 * `query-selector()` operation.
 */
export function querySelector(self: bigint, selectors: string): bigint | undefined {
  const obj = lookupParentNode(self);
  const _callResult = obj.querySelector(selectors);
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-selector-all()` operation.
 */
export function querySelectorAll(self: bigint, selectors: string): bigint {
  const obj = lookupParentNode(self);
  const _callResult = obj.querySelectorAll(selectors);
  const handle = _nextNodeList++;
  _nodeListHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: non-document-type-child-node
// ---------------------------------------------------------------------------

/** Type alias */
export type NonDocumentTypeChildNodeHandle = bigint;

/** Handle table for NonDocumentTypeChildNode instances */
const _nonDocumentTypeChildNodehandles = new Map<bigint, NonDocumentTypeChildNode>();
let _nextNonDocumentTypeChildNode = 1n;

/** Lookup a NonDocumentTypeChildNode by handle, throwing if not found. */
function lookupNonDocumentTypeChildNode(handle: bigint): NonDocumentTypeChildNode {
  const obj = _nonDocumentTypeChildNodehandles.get(handle);
  if (!obj) {
    throw new Error(`NonDocumentTypeChildNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional NonDocumentTypeChildNode by handle. */
function lookupOptionNonDocumentTypeChildNode(handle: bigint | undefined): NonDocumentTypeChildNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nonDocumentTypeChildNodehandles.get(handle) ?? null;
}
/**
 * `get-previous-element-sibling()` operation.
 */
export function getPreviousElementSibling(self: bigint): bigint | undefined {
  const obj = lookupNonDocumentTypeChildNode(self);
  const _callResult = obj.previousElementSibling;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-next-element-sibling()` operation.
 */
export function getNextElementSibling(self: bigint): bigint | undefined {
  const obj = lookupNonDocumentTypeChildNode(self);
  const _callResult = obj.nextElementSibling;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: child-node
// ---------------------------------------------------------------------------

/** Type alias */
export type ChildNodeHandle = bigint;

/** Handle table for ChildNode instances */
const _childNodehandles = new Map<bigint, ChildNode>();
let _nextChildNode = 1n;

/** Lookup a ChildNode by handle, throwing if not found. */
function lookupChildNode(handle: bigint): ChildNode {
  const obj = _childNodehandles.get(handle);
  if (!obj) {
    throw new Error(`ChildNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ChildNode by handle. */
function lookupOptionChildNode(handle: bigint | undefined): ChildNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _childNodehandles.get(handle) ?? null;
}
/**
 * `before()` operation.
 */
export function before(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupChildNode(self);
  obj.before(nodes as any);
}

/**
 * `after()` operation.
 */
export function after(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupChildNode(self);
  obj.after(nodes as any);
}

/**
 * `replace-with()` operation.
 */
export function replaceWith(self: bigint, nodes: (bigint)[]): void {
  const obj = lookupChildNode(self);
  obj.replaceWith(nodes as any);
}

/**
 * `remove()` operation.
 */
export function ChildNodeRemove(self: bigint): void {
  const obj = lookupChildNode(self);
  obj.remove();
}

// ---------------------------------------------------------------------------
// WIT interface: slottable
// ---------------------------------------------------------------------------

/** Type alias */
export type SlottableHandle = bigint;

/** Handle table for Slottable instances */
const _slottableHandles = new Map<bigint, Slottable>();
let _nextSlottable = 1n;

/** Lookup a Slottable by handle, throwing if not found. */
function lookupSlottable(handle: bigint): Slottable {
  const obj = _slottableHandles.get(handle);
  if (!obj) {
    throw new Error(`Slottable handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional Slottable by handle. */
function lookupOptionSlottable(handle: bigint | undefined): Slottable | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _slottableHandles.get(handle) ?? null;
}
/**
 * `get-assigned-slot()` operation.
 */
export function getAssignedSlot(self: bigint): bigint | undefined {
  const obj = lookupSlottable(self);
  const _callResult = obj.assignedSlot;
  if (_callResult === null) return undefined;
  const handle = _nextHtmlSlotElement++;
  _htmlSlotElementHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: node-list
// ---------------------------------------------------------------------------

/** Type alias */
export type NodeListHandle = bigint;

/**
 * `item()` operation.
 */
export function NodeListItem(self: bigint, index: number): bigint | undefined {
  const obj = lookupNodeList(self);
  const _callResult = obj.item(index);
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-length()` operation.
 */
export function NodeListGetLength(self: bigint): number {
  const obj = lookupNodeList(self);
  return obj.length;
}

// ---------------------------------------------------------------------------
// WIT interface: html-collection
// ---------------------------------------------------------------------------

/** Type alias */
export type HtmlCollectionHandle = bigint;

/** Handle table for HTMLCollection instances */
const _htmlCollectionhandles = new Map<bigint, HTMLCollection>();
let _nextHTMLCollection = 1n;

/** Lookup a HTMLCollection by handle, throwing if not found. */
function lookupHTMLCollection(handle: bigint): HTMLCollection {
  const obj = _htmlCollectionhandles.get(handle);
  if (!obj) {
    throw new Error(`HTMLCollection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional HTMLCollection by handle. */
function lookupOptionHTMLCollection(handle: bigint | undefined): HTMLCollection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlCollectionhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function HtmlCollectionGetLength(self: bigint): number {
  const obj = lookupHTMLCollection(self);
  return obj.length;
}

/**
 * `item()` operation.
 */
export function HtmlCollectionItem(self: bigint, index: number): bigint | undefined {
  const obj = lookupHTMLCollection(self);
  const _callResult = obj.item(index);
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `named-item()` operation.
 */
export function namedItem(self: bigint, name: string): bigint | undefined {
  const obj = lookupHTMLCollection(self);
  const _callResult = obj.namedItem(name);
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: mutation-observer
// ---------------------------------------------------------------------------

/** Type alias */
export type MutationObserverHandle = bigint;

/** Handle table for MutationObserver instances */
const _mutationObserverhandles = new Map<bigint, MutationObserver>();
let _nextMutationObserver = 1n;

/** Lookup a MutationObserver by handle, throwing if not found. */
function lookupMutationObserver(handle: bigint): MutationObserver {
  const obj = _mutationObserverhandles.get(handle);
  if (!obj) {
    throw new Error(`MutationObserver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MutationObserver by handle. */
function lookupOptionMutationObserver(handle: bigint | undefined): MutationObserver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mutationObserverhandles.get(handle) ?? null;
}
/**
 * `observe()` operation.
 */
export function observe(self: bigint, target: bigint, options: bigint | undefined): void {
  const obj = lookupMutationObserver(self);
  obj.observe(lookupNode(target), options as any);
}

/**
 * `disconnect()` operation.
 */
export function disconnect(self: bigint): void {
  const obj = lookupMutationObserver(self);
  obj.disconnect();
}

/**
 * `take-records()` operation.
 */
export function takeRecords(self: bigint): bigint {
  const obj = lookupMutationObserver(self);
  const _callResult = obj.takeRecords();
  const handle = _nextMutationRecordList++;
  _mutationRecordListHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: mutation-record
// ---------------------------------------------------------------------------

/** Type alias */
export type MutationRecordHandle = bigint;

/** Handle table for MutationRecord instances */
const _mutationRecordhandles = new Map<bigint, MutationRecord>();
let _nextMutationRecord = 1n;

/** Lookup a MutationRecord by handle, throwing if not found. */
function lookupMutationRecord(handle: bigint): MutationRecord {
  const obj = _mutationRecordhandles.get(handle);
  if (!obj) {
    throw new Error(`MutationRecord handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MutationRecord by handle. */
function lookupOptionMutationRecord(handle: bigint | undefined): MutationRecord | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mutationRecordhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function MutationRecordGetType(self: bigint): string {
  const obj = lookupMutationRecord(self);
  return (obj as any).getType();
}

/**
 * `get-target()` operation.
 */
export function MutationRecordGetTarget(self: bigint): bigint {
  const obj = lookupMutationRecord(self);
  const _callResult = obj.target;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-added-nodes()` operation.
 */
export function getAddedNodes(self: bigint): bigint {
  const obj = lookupMutationRecord(self);
  const _callResult = obj.addedNodes;
  const handle = _nextNodeList++;
  _nodeListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-removed-nodes()` operation.
 */
export function getRemovedNodes(self: bigint): bigint {
  const obj = lookupMutationRecord(self);
  const _callResult = obj.removedNodes;
  const handle = _nextNodeList++;
  _nodeListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-previous-sibling()` operation.
 */
export function MutationRecordGetPreviousSibling(self: bigint): bigint | undefined {
  const obj = lookupMutationRecord(self);
  const _callResult = obj.previousSibling;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-next-sibling()` operation.
 */
export function MutationRecordGetNextSibling(self: bigint): bigint | undefined {
  const obj = lookupMutationRecord(self);
  const _callResult = obj.nextSibling;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-attribute-name()` operation.
 */
export function getAttributeName(self: bigint): string | undefined {
  const obj = lookupMutationRecord(self);
  return obj.attributeName ?? undefined;
}

/**
 * `get-attribute-namespace()` operation.
 */
export function getAttributeNamespace(self: bigint): string | undefined {
  const obj = lookupMutationRecord(self);
  return obj.attributeNamespace ?? undefined;
}

/**
 * `get-old-value()` operation.
 */
export function getOldValue(self: bigint): string | undefined {
  const obj = lookupMutationRecord(self);
  return obj.oldValue ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: node
// ---------------------------------------------------------------------------

/** Type alias */
export type NodeHandle = bigint;

/**
 * `get-node-type()` operation.
 */
export function getNodeType(self: bigint): number {
  const obj = lookupNode(self);
  return obj.nodeType;
}

/**
 * `get-node-name()` operation.
 */
export function getNodeName(self: bigint): string {
  const obj = lookupNode(self);
  return obj.nodeName;
}

/**
 * `get-base-uri()` operation.
 */
export function getBaseUri(self: bigint): string {
  const obj = lookupNode(self);
  return obj.baseURI;
}

/**
 * `get-is-connected()` operation.
 */
export function getIsConnected(self: bigint): boolean {
  const obj = lookupNode(self);
  return obj.isConnected;
}

/**
 * `get-owner-document()` operation.
 */
export function getOwnerDocument(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.ownerDocument;
  if (_callResult === null) return undefined;
  const handle = _nextDocument++;
  _documentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-root-node()` operation.
 */
export function getRootNode(self: bigint, options: bigint | undefined): bigint {
  const obj = lookupNode(self);
  const _callResult = (obj as any).getRootNode(options);
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-parent-node()` operation.
 */
export function getParentNode(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.parentNode;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-parent-element()` operation.
 */
export function getParentElement(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.parentElement;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `has-child-nodes()` operation.
 */
export function hasChildNodes(self: bigint): boolean {
  const obj = lookupNode(self);
  return obj.hasChildNodes();
}

/**
 * `get-child-nodes()` operation.
 */
export function getChildNodes(self: bigint): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.childNodes;
  const handle = _nextNodeList++;
  _nodeListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-first-child()` operation.
 */
export function getFirstChild(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.firstChild;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-last-child()` operation.
 */
export function getLastChild(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.lastChild;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-previous-sibling()` operation.
 */
export function NodeGetPreviousSibling(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.previousSibling;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-next-sibling()` operation.
 */
export function NodeGetNextSibling(self: bigint): bigint | undefined {
  const obj = lookupNode(self);
  const _callResult = obj.nextSibling;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-node-value()` operation.
 */
export function getNodeValue(self: bigint): string | undefined {
  const obj = lookupNode(self);
  return obj.nodeValue ?? undefined;
}

/**
 * `set-node-value()` operation.
 */
export function setNodeValue(self: bigint, value: string | undefined): void {
  const obj = lookupNode(self);
  obj.nodeValue = value ?? null;
}

/**
 * `get-text-content()` operation.
 */
export function getTextContent(self: bigint): string | undefined {
  const obj = lookupNode(self);
  return obj.textContent ?? undefined;
}

/**
 * `set-text-content()` operation.
 */
export function setTextContent(self: bigint, value: string | undefined): void {
  const obj = lookupNode(self);
  obj.textContent = value ?? null;
}

/**
 * `normalize()` operation.
 */
export function normalize(self: bigint): void {
  const obj = lookupNode(self);
  obj.normalize();
}

/**
 * `clone-node()` operation.
 */
export function cloneNode(self: bigint, subtree: boolean | undefined): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.cloneNode(subtree);
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `is-equal-node()` operation.
 */
export function isEqualNode(self: bigint, otherNode: bigint | undefined): boolean {
  const obj = lookupNode(self);
  return obj.isEqualNode(lookupOptionNode(otherNode));
}

/**
 * `is-same-node()` operation.
 */
export function isSameNode(self: bigint, otherNode: bigint | undefined): boolean {
  const obj = lookupNode(self);
  return obj.isSameNode(lookupOptionNode(otherNode));
}

/**
 * `compare-document-position()` operation.
 */
export function compareDocumentPosition(self: bigint, other: bigint): number {
  const obj = lookupNode(self);
  return obj.compareDocumentPosition(lookupNode(other));
}

/**
 * `contains()` operation.
 */
export function NodeContains(self: bigint, other: bigint | undefined): boolean {
  const obj = lookupNode(self);
  return obj.contains(lookupOptionNode(other));
}

/**
 * `lookup-prefix()` operation.
 */
export function lookupPrefix(self: bigint, namespace: string | undefined): string | undefined {
  const obj = lookupNode(self);
  return obj.lookupPrefix(namespace as any) ?? undefined;
}

/**
 * `lookup-namespace-uri()` operation.
 */
export function NodeLookupNamespaceUri(self: bigint, prefix: string | undefined): string | undefined {
  const obj = lookupNode(self);
  return obj.lookupNamespaceURI(prefix as any) ?? undefined;
}

/**
 * `is-default-namespace()` operation.
 */
export function isDefaultNamespace(self: bigint, namespace: string | undefined): boolean {
  const obj = lookupNode(self);
  return obj.isDefaultNamespace(namespace as any);
}

/**
 * `insert-before()` operation.
 */
export function insertBefore(self: bigint, node: bigint, child: bigint | undefined): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.insertBefore(lookupNode(node), lookupOptionNode(child));
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `append-child()` operation.
 */
export function appendChild(self: bigint, node: bigint): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.appendChild(lookupNode(node));
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `replace-child()` operation.
 */
export function replaceChild(self: bigint, node: bigint, child: bigint): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.replaceChild(lookupNode(node), lookupNode(child));
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `remove-child()` operation.
 */
export function removeChild(self: bigint, child: bigint): bigint {
  const obj = lookupNode(self);
  const _callResult = obj.removeChild(lookupNode(child));
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: dom-implementation
// ---------------------------------------------------------------------------

/** Type alias */
export type DomImplementationHandle = bigint;

/** Handle table for DOMImplementation instances */
const _domImplementationhandles = new Map<bigint, DOMImplementation>();
let _nextDOMImplementation = 1n;

/** Register a new DOMImplementation and return its handle. */
function registerDOMImplementation(obj: DOMImplementation): bigint {
  const handle = _nextDOMImplementation++;
  _domImplementationhandles.set(handle, obj);
  return handle;
}
/** Lookup a DOMImplementation by handle, throwing if not found. */
function lookupDOMImplementation(handle: bigint): DOMImplementation {
  const obj = _domImplementationhandles.get(handle);
  if (!obj) {
    throw new Error(`DOMImplementation handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional DOMImplementation by handle. */
function lookupOptionDOMImplementation(handle: bigint | undefined): DOMImplementation | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _domImplementationhandles.get(handle) ?? null;
}
/**
 * `create-document-type()` operation.
 */
export function createDocumentType(self: bigint, name: string, publicId: string, systemId: string): bigint {
  const obj = lookupDOMImplementation(self);
  const _callResult = obj.createDocumentType(name, publicId as any, systemId as any);
  const handle = _nextDocumentType++;
  _documentTypeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-document()` operation.
 */
export function createDocument(self: bigint, namespace: string | undefined, qualifiedName: string, doctype: bigint | undefined): bigint {
  const obj = lookupDOMImplementation(self);
  const _callResult = obj.createDocument(namespace as any, qualifiedName as any, lookupOptionDocumentType(doctype));
  const handle = _nextDocument++;
  _documentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-html-document()` operation.
 */
export function createHtmlDocument(self: bigint, title: string | undefined): bigint {
  const obj = lookupDOMImplementation(self);
  const _callResult = obj.createHTMLDocument(title as any);
  const handle = _nextDocument++;
  _documentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `has-feature()` operation.
 */
export function hasFeature(self: bigint): boolean {
  const obj = lookupDOMImplementation(self);
  return obj.hasFeature();
}

// ---------------------------------------------------------------------------
// WIT interface: document-type
// ---------------------------------------------------------------------------

/** Type alias */
export type DocumentTypeHandle = bigint;

/**
 * `get-name()` operation.
 */
export function DocumentTypeGetName(self: bigint): bigint {
  const obj = lookupDocumentType(self);
  const _callResult = obj.name;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-public-id()` operation.
 */
export function getPublicId(self: bigint): bigint {
  const obj = lookupDocumentType(self);
  const _callResult = obj.publicId;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-system-id()` operation.
 */
export function getSystemId(self: bigint): bigint {
  const obj = lookupDocumentType(self);
  const _callResult = obj.systemId;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: shadow-root
// ---------------------------------------------------------------------------

/** Type alias */
export type ShadowRootHandle = bigint;

/** Handle table for ShadowRoot instances */
const _shadowRoothandles = new Map<bigint, ShadowRoot>();
let _nextShadowRoot = 1n;

/** Lookup a ShadowRoot by handle, throwing if not found. */
function lookupShadowRoot(handle: bigint): ShadowRoot {
  const obj = _shadowRoothandles.get(handle);
  if (!obj) {
    throw new Error(`ShadowRoot handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ShadowRoot by handle. */
function lookupOptionShadowRoot(handle: bigint | undefined): ShadowRoot | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _shadowRoothandles.get(handle) ?? null;
}
/**
 * `get-mode()` operation.
 */
export function getMode(self: bigint): bigint {
  const obj = lookupShadowRoot(self);
  const value = obj.mode;
  switch (value) {
    case 'open': return 0n;
    case 'closed': return 1n;
    default: return 0n;
  }
}

/**
 * `get-delegates-focus()` operation.
 */
export function getDelegatesFocus(self: bigint): boolean {
  const obj = lookupShadowRoot(self);
  return obj.delegatesFocus;
}

/**
 * `get-slot-assignment()` operation.
 */
export function getSlotAssignment(self: bigint): bigint {
  const obj = lookupShadowRoot(self);
  const value = obj.slotAssignment;
  switch (value) {
    case 'manual': return 0n;
    case 'named': return 1n;
    default: return 0n;
  }
}

/**
 * `get-clonable()` operation.
 */
export function getClonable(self: bigint): boolean {
  const obj = lookupShadowRoot(self);
  return obj.clonable;
}

/**
 * `get-serializable()` operation.
 */
export function getSerializable(self: bigint): boolean {
  const obj = lookupShadowRoot(self);
  return obj.serializable;
}

/**
 * `get-host()` operation.
 */
export function getHost(self: bigint): bigint {
  const obj = lookupShadowRoot(self);
  const _callResult = obj.host;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-onslotchange()` operation.
 */
export function getOnslotchange(self: bigint): EventHandlerRecord {
  const obj = lookupShadowRoot(self);
  return obj.onslotchange;
}

/**
 * `set-onslotchange()` operation.
 */
export function setOnslotchange(self: bigint, value: EventHandlerRecord): void {
  const obj = lookupShadowRoot(self);
  obj.onslotchange = value;
}

/**
 * `set-html-unsafe()` operation.
 */
export function setHtmlUnsafe(self: bigint, html: bigint): void {
  const obj = lookupShadowRoot(self);
  obj.setHTMLUnsafe = html as any;
}

/**
 * `get-html()` operation.
 */
export function getHtml(self: bigint, options: bigint | undefined): string {
  const obj = lookupShadowRoot(self);
  return (obj as any).getHtml(options);
}

/**
 * `get-inner-html()` operation.
 */
export function getInnerHtml(self: bigint): bigint {
  const obj = lookupShadowRoot(self);
  const value = obj.innerHTML;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `set-inner-html()` operation.
 */
export function setInnerHtml(self: bigint, value: bigint): void {
  const obj = lookupShadowRoot(self);
  const _enumInput = value;
  let enumValue: HTMLString | '';
  if (_enumInput === 0n) { enumValue = ''; }
  else { enumValue = ''; }
  obj.innerHTML = enumValue as any;
}

// ---------------------------------------------------------------------------
// WIT interface: named-node-map
// ---------------------------------------------------------------------------

/** Type alias */
export type NamedNodeMapHandle = bigint;

/** Handle table for NamedNodeMap instances */
const _namedNodeMaphandles = new Map<bigint, NamedNodeMap>();
let _nextNamedNodeMap = 1n;

/** Lookup a NamedNodeMap by handle, throwing if not found. */
function lookupNamedNodeMap(handle: bigint): NamedNodeMap {
  const obj = _namedNodeMaphandles.get(handle);
  if (!obj) {
    throw new Error(`NamedNodeMap handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional NamedNodeMap by handle. */
function lookupOptionNamedNodeMap(handle: bigint | undefined): NamedNodeMap | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _namedNodeMaphandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function NamedNodeMapGetLength(self: bigint): number {
  const obj = lookupNamedNodeMap(self);
  return obj.length;
}

/**
 * `item()` operation.
 */
export function NamedNodeMapItem(self: bigint, index: number): bigint | undefined {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.item(index);
  if (_callResult === null) return undefined;
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-named-item()` operation.
 */
export function getNamedItem(self: bigint, qualifiedName: string): bigint | undefined {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.getNamedItem(qualifiedName as any);
  if (_callResult === null) return undefined;
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-named-item-ns()` operation.
 */
export function getNamedItemNs(self: bigint, namespace: string | undefined, localName: string): bigint | undefined {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.getNamedItemNS(namespace as any, localName as any);
  if (_callResult === null) return undefined;
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-named-item()` operation.
 */
export function setNamedItem(self: bigint, attr: bigint): bigint | undefined {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.setNamedItem(lookupAttr(attr));
  if (_callResult === null) return undefined;
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-named-item-ns()` operation.
 */
export function setNamedItemNs(self: bigint, attr: bigint): bigint | undefined {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.setNamedItemNS(lookupAttr(attr));
  if (_callResult === null) return undefined;
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `remove-named-item()` operation.
 */
export function removeNamedItem(self: bigint, qualifiedName: string): bigint {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.removeNamedItem(qualifiedName as any);
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `remove-named-item-ns()` operation.
 */
export function removeNamedItemNs(self: bigint, namespace: string | undefined, localName: string): bigint {
  const obj = lookupNamedNodeMap(self);
  const _callResult = obj.removeNamedItemNS(namespace as any, localName as any);
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: attr
// ---------------------------------------------------------------------------

/** Type alias */
export type AttrHandle = bigint;

/**
 * `get-namespace-uri()` operation.
 */
export function getNamespaceUri(self: bigint): string | undefined {
  const obj = lookupAttr(self);
  return obj.namespaceURI ?? undefined;
}

/**
 * `get-prefix()` operation.
 */
export function getPrefix(self: bigint): string | undefined {
  const obj = lookupAttr(self);
  return obj.prefix ?? undefined;
}

/**
 * `get-local-name()` operation.
 */
export function getLocalName(self: bigint): string {
  const obj = lookupAttr(self);
  return obj.localName;
}

/**
 * `get-name()` operation.
 */
export function AttrGetName(self: bigint): string {
  const obj = lookupAttr(self);
  return obj.name;
}

/**
 * `get-value()` operation.
 */
export function AttrGetValue(self: bigint): string {
  const obj = lookupAttr(self);
  return obj.value;
}

/**
 * `set-value()` operation.
 */
export function AttrSetValue(self: bigint, value: string): void {
  const obj = lookupAttr(self);
  obj.value = value;
}

/**
 * `get-owner-element()` operation.
 */
export function getOwnerElement(self: bigint): bigint | undefined {
  const obj = lookupAttr(self);
  const _callResult = obj.ownerElement;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-specified()` operation.
 */
export function getSpecified(self: bigint): boolean {
  const obj = lookupAttr(self);
  return obj.specified;
}

// ---------------------------------------------------------------------------
// WIT interface: character-data
// ---------------------------------------------------------------------------

/** Type alias */
export type CharacterDataHandle = bigint;

/** Handle table for CharacterData instances */
const _characterDatahandles = new Map<bigint, CharacterData>();
let _nextCharacterData = 1n;

/** Lookup a CharacterData by handle, throwing if not found. */
function lookupCharacterData(handle: bigint): CharacterData {
  const obj = _characterDatahandles.get(handle);
  if (!obj) {
    throw new Error(`CharacterData handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional CharacterData by handle. */
function lookupOptionCharacterData(handle: bigint | undefined): CharacterData | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _characterDatahandles.get(handle) ?? null;
}
/**
 * `get-data()` operation.
 */
export function getData(self: bigint): string {
  const obj = lookupCharacterData(self);
  return obj.data;
}

/**
 * `set-data()` operation.
 */
export function setData(self: bigint, value: string): void {
  const obj = lookupCharacterData(self);
  obj.data = value;
}

/**
 * `get-length()` operation.
 */
export function CharacterDataGetLength(self: bigint): number {
  const obj = lookupCharacterData(self);
  return obj.length;
}

/**
 * `substring-data()` operation.
 */
export function substringData(self: bigint, offset: number, count: number): string {
  const obj = lookupCharacterData(self);
  return obj.substringData(Number(offset), Number(count));
}

/**
 * `append-data()` operation.
 */
export function appendData(self: bigint, data: string): void {
  const obj = lookupCharacterData(self);
  obj.appendData(data as any);
}

/**
 * `insert-data()` operation.
 */
export function insertData(self: bigint, offset: number, data: string): void {
  const obj = lookupCharacterData(self);
  obj.insertData(Number(offset), data as any);
}

/**
 * `delete-data()` operation.
 */
export function deleteData(self: bigint, offset: number, count: number): void {
  const obj = lookupCharacterData(self);
  obj.deleteData(Number(offset), Number(count));
}

/**
 * `replace-data()` operation.
 */
export function replaceData(self: bigint, offset: number, count: number, data: string): void {
  const obj = lookupCharacterData(self);
  obj.replaceData(Number(offset), Number(count), data as any);
}

// ---------------------------------------------------------------------------
// WIT interface: text
// ---------------------------------------------------------------------------

/** Type alias */
export type TextHandle = bigint;

/**
 * `split-text()` operation.
 */
export function splitText(self: bigint, offset: number): bigint {
  const obj = lookupText(self);
  const _callResult = obj.splitText(offset);
  const handle = _nextText++;
  _textHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-whole-text()` operation.
 */
export function getWholeText(self: bigint): string {
  const obj = lookupText(self);
  return obj.wholeText;
}

// ---------------------------------------------------------------------------
// WIT interface: processing-instruction
// ---------------------------------------------------------------------------

/** Type alias */
export type ProcessingInstructionHandle = bigint;

/** Handle table for ProcessingInstruction instances */
const _processingInstructionhandles = new Map<bigint, ProcessingInstruction>();
let _nextProcessingInstruction = 1n;

/** Lookup a ProcessingInstruction by handle, throwing if not found. */
function lookupProcessingInstruction(handle: bigint): ProcessingInstruction {
  const obj = _processingInstructionhandles.get(handle);
  if (!obj) {
    throw new Error(`ProcessingInstruction handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ProcessingInstruction by handle. */
function lookupOptionProcessingInstruction(handle: bigint | undefined): ProcessingInstruction | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _processingInstructionhandles.get(handle) ?? null;
}
/**
 * `get-target()` operation.
 */
export function ProcessingInstructionGetTarget(self: bigint): string {
  const obj = lookupProcessingInstruction(self);
  return obj.target;
}

// ---------------------------------------------------------------------------
// WIT interface: abstract-range
// ---------------------------------------------------------------------------

/** Type alias */
export type AbstractRangeHandle = bigint;

/** Handle table for AbstractRange instances */
const _abstractRangehandles = new Map<bigint, AbstractRange>();
let _nextAbstractRange = 1n;

/** Lookup a AbstractRange by handle, throwing if not found. */
function lookupAbstractRange(handle: bigint): AbstractRange {
  const obj = _abstractRangehandles.get(handle);
  if (!obj) {
    throw new Error(`AbstractRange handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AbstractRange by handle. */
function lookupOptionAbstractRange(handle: bigint | undefined): AbstractRange | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _abstractRangehandles.get(handle) ?? null;
}
/**
 * `get-start-container()` operation.
 */
export function getStartContainer(self: bigint): bigint {
  const obj = lookupAbstractRange(self);
  const _callResult = obj.startContainer;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-start-offset()` operation.
 */
export function getStartOffset(self: bigint): bigint {
  const obj = lookupAbstractRange(self);
  return BigInt(obj.startOffset);
}

/**
 * `get-end-container()` operation.
 */
export function getEndContainer(self: bigint): bigint {
  const obj = lookupAbstractRange(self);
  const _callResult = obj.endContainer;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-end-offset()` operation.
 */
export function getEndOffset(self: bigint): bigint {
  const obj = lookupAbstractRange(self);
  return BigInt(obj.endOffset);
}

/**
 * `get-collapsed()` operation.
 */
export function getCollapsed(self: bigint): boolean {
  const obj = lookupAbstractRange(self);
  return obj.collapsed;
}

// ---------------------------------------------------------------------------
// WIT interface: node-iterator
// ---------------------------------------------------------------------------

/** Type alias */
export type NodeIteratorHandle = bigint;

/** Handle table for NodeIterator instances */
const _nodeIteratorhandles = new Map<bigint, NodeIterator>();
let _nextNodeIterator = 1n;

/** Lookup a NodeIterator by handle, throwing if not found. */
function lookupNodeIterator(handle: bigint): NodeIterator {
  const obj = _nodeIteratorhandles.get(handle);
  if (!obj) {
    throw new Error(`NodeIterator handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional NodeIterator by handle. */
function lookupOptionNodeIterator(handle: bigint | undefined): NodeIterator | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeIteratorhandles.get(handle) ?? null;
}
/**
 * `get-root()` operation.
 */
export function NodeIteratorGetRoot(self: bigint): bigint {
  const obj = lookupNodeIterator(self);
  const _callResult = obj.root;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-reference-node()` operation.
 */
export function getReferenceNode(self: bigint): bigint {
  const obj = lookupNodeIterator(self);
  const _callResult = obj.referenceNode;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-pointer-before-reference-node()` operation.
 */
export function getPointerBeforeReferenceNode(self: bigint): bigint {
  const obj = lookupNodeIterator(self);
  return obj.pointerBeforeReferenceNode ? 1n : 0n;
}

/**
 * `get-what-to-show()` operation.
 */
export function NodeIteratorGetWhatToShow(self: bigint): number {
  const obj = lookupNodeIterator(self);
  return obj.whatToShow;
}

/**
 * `get-filter()` operation.
 */
export function NodeIteratorGetFilter(self: bigint): bigint | undefined {
  const obj = lookupNodeIterator(self);
  const _callResult = obj.filter;
  if (_callResult === null) return undefined;
  const handle = _nextNodeFilter++;
  _nodeFilterHandles.set(handle, _callResult);
  return handle;
}

/**
 * `next-node()` operation.
 */
export function NodeIteratorNextNode(self: bigint): bigint | undefined {
  const obj = lookupNodeIterator(self);
  const _callResult = obj.nextNode();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `previous-node()` operation.
 */
export function NodeIteratorPreviousNode(self: bigint): bigint | undefined {
  const obj = lookupNodeIterator(self);
  const _callResult = obj.previousNode();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `detach()` operation.
 */
export function detach(self: bigint): void {
  const obj = lookupNodeIterator(self);
  obj.detach();
}

// ---------------------------------------------------------------------------
// WIT interface: tree-walker
// ---------------------------------------------------------------------------

/** Type alias */
export type TreeWalkerHandle = bigint;

/** Handle table for TreeWalker instances */
const _treeWalkerhandles = new Map<bigint, TreeWalker>();
let _nextTreeWalker = 1n;

/** Lookup a TreeWalker by handle, throwing if not found. */
function lookupTreeWalker(handle: bigint): TreeWalker {
  const obj = _treeWalkerhandles.get(handle);
  if (!obj) {
    throw new Error(`TreeWalker handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional TreeWalker by handle. */
function lookupOptionTreeWalker(handle: bigint | undefined): TreeWalker | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _treeWalkerhandles.get(handle) ?? null;
}
/**
 * `get-root()` operation.
 */
export function TreeWalkerGetRoot(self: bigint): bigint {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.root;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-what-to-show()` operation.
 */
export function TreeWalkerGetWhatToShow(self: bigint): number {
  const obj = lookupTreeWalker(self);
  return obj.whatToShow;
}

/**
 * `get-filter()` operation.
 */
export function TreeWalkerGetFilter(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.filter;
  if (_callResult === null) return undefined;
  const handle = _nextNodeFilter++;
  _nodeFilterHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-current-node()` operation.
 */
export function getCurrentNode(self: bigint): bigint {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.currentNode;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-current-node()` operation.
 */
export function setCurrentNode(self: bigint, value: bigint): void {
  const obj = lookupTreeWalker(self);
  obj.currentNode = lookupNode(value);
}

/**
 * `parent-node()` operation.
 */
export function parentNode(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.parentNode();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `first-child()` operation.
 */
export function firstChild(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.firstChild();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `last-child()` operation.
 */
export function lastChild(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.lastChild();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `previous-sibling()` operation.
 */
export function previousSibling(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.previousSibling();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `next-sibling()` operation.
 */
export function nextSibling(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.nextSibling();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `previous-node()` operation.
 */
export function TreeWalkerPreviousNode(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.previousNode();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `next-node()` operation.
 */
export function TreeWalkerNextNode(self: bigint): bigint | undefined {
  const obj = lookupTreeWalker(self);
  const _callResult = obj.nextNode();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: node-filter
// ---------------------------------------------------------------------------

/** Type alias */
export type NodeFilterHandle = bigint;

/**
 * `accept-node()` operation.
 */
export function acceptNode(self: bigint, node: bigint): number {
  const obj = lookupNodeFilter(self);
  return (obj as any).acceptNode(node);
}

// ---------------------------------------------------------------------------
// WIT interface: dom-token-list
// ---------------------------------------------------------------------------

/** Type alias */
export type DomTokenListHandle = bigint;

/** Handle table for DOMTokenList instances */
const _domTokenListhandles = new Map<bigint, DOMTokenList>();
let _nextDOMTokenList = 1n;

/** Lookup a DOMTokenList by handle, throwing if not found. */
function lookupDOMTokenList(handle: bigint): DOMTokenList {
  const obj = _domTokenListhandles.get(handle);
  if (!obj) {
    throw new Error(`DOMTokenList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional DOMTokenList by handle. */
function lookupOptionDOMTokenList(handle: bigint | undefined): DOMTokenList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _domTokenListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function DomTokenListGetLength(self: bigint): bigint {
  const obj = lookupDOMTokenList(self);
  return BigInt(obj.length);
}

/**
 * `item()` operation.
 */
export function DomTokenListItem(self: bigint, index: number): bigint | undefined {
  const obj = lookupDOMTokenList(self);
  const _callResult = obj.item(index);
  if (_callResult === null) return undefined;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `contains()` operation.
 */
export function DomTokenListContains(self: bigint, token: string): bigint {
  const obj = lookupDOMTokenList(self);
  return obj.contains(token) ? 1n : 0n;
}

/**
 * `add()` operation.
 */
export function add(self: bigint, tokens: (string)[]): void {
  const obj = lookupDOMTokenList(self);
  obj.add(tokens as any);
}

/**
 * `remove()` operation.
 */
export function DomTokenListRemove(self: bigint, tokens: (string)[]): void {
  const obj = lookupDOMTokenList(self);
  obj.remove(tokens as any);
}

/**
 * `toggle()` operation.
 */
export function toggle(self: bigint, token: string, force: boolean | undefined): bigint {
  const obj = lookupDOMTokenList(self);
  return obj.toggle(token as any, force) ? 1n : 0n;
}

/**
 * `replace()` operation.
 */
export function replace(self: bigint, token: string, newToken: string): EventHandlerRecord {
  const obj = lookupDOMTokenList(self);
  return obj.replace(token as any, newToken as any);
}

/**
 * `supports()` operation.
 */
export function supports(self: bigint, token: string): bigint {
  const obj = lookupDOMTokenList(self);
  return obj.supports(token as any) ? 1n : 0n;
}

/**
 * `get-value()` operation.
 */
export function DomTokenListGetValue(self: bigint): bigint {
  const obj = lookupDOMTokenList(self);
  const value = obj.value;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `set-value()` operation.
 */
export function DomTokenListSetValue(self: bigint, value: bigint): void {
  const obj = lookupDOMTokenList(self);
  const _enumInput = value;
  let enumValue: DOMTokenListValue | '';
  if (_enumInput === 0n) { enumValue = ''; }
  else { enumValue = ''; }
  obj.value = enumValue as any;
}

// ---------------------------------------------------------------------------
// WIT interface: x-path-result
// ---------------------------------------------------------------------------

/** Type alias */
export type XPathResultHandle = bigint;

/** Handle table for XPathResult instances */
const _xPathResulthandles = new Map<bigint, XPathResult>();
let _nextXPathResult = 1n;

/** Lookup a XPathResult by handle, throwing if not found. */
function lookupXPathResult(handle: bigint): XPathResult {
  const obj = _xPathResulthandles.get(handle);
  if (!obj) {
    throw new Error(`XPathResult handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional XPathResult by handle. */
function lookupOptionXPathResult(handle: bigint | undefined): XPathResult | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xPathResulthandles.get(handle) ?? null;
}
/**
 * `get-result-type()` operation.
 */
export function getResultType(self: bigint): EventHandlerRecord {
  const obj = lookupXPathResult(self);
  return obj.resultType;
}

/**
 * `get-number-value()` operation.
 */
export function getNumberValue(self: bigint): number {
  const obj = lookupXPathResult(self);
  return obj.numberValue;
}

/**
 * `get-string-value()` operation.
 */
export function getStringValue(self: bigint): string {
  const obj = lookupXPathResult(self);
  return obj.stringValue;
}

/**
 * `get-boolean-value()` operation.
 */
export function getBooleanValue(self: bigint): boolean {
  const obj = lookupXPathResult(self);
  return obj.booleanValue;
}

/**
 * `get-single-node-value()` operation.
 */
export function getSingleNodeValue(self: bigint): bigint | undefined {
  const obj = lookupXPathResult(self);
  const _callResult = obj.singleNodeValue;
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-invalid-iterator-state()` operation.
 */
export function getInvalidIteratorState(self: bigint): boolean {
  const obj = lookupXPathResult(self);
  return obj.invalidIteratorState;
}

/**
 * `get-snapshot-length()` operation.
 */
export function getSnapshotLength(self: bigint): number {
  const obj = lookupXPathResult(self);
  return obj.snapshotLength;
}

/**
 * `iterate-next()` operation.
 */
export function iterateNext(self: bigint): bigint | undefined {
  const obj = lookupXPathResult(self);
  const _callResult = obj.iterateNext();
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `snapshot-item()` operation.
 */
export function snapshotItem(self: bigint, index: number): bigint | undefined {
  const obj = lookupXPathResult(self);
  const _callResult = obj.snapshotItem(index);
  if (_callResult === null) return undefined;
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: x-path-expression
// ---------------------------------------------------------------------------

/** Type alias */
export type XPathExpressionHandle = bigint;

/** Handle table for XPathExpression instances */
const _xPathExpressionhandles = new Map<bigint, XPathExpression>();
let _nextXPathExpression = 1n;

/** Lookup a XPathExpression by handle, throwing if not found. */
function lookupXPathExpression(handle: bigint): XPathExpression {
  const obj = _xPathExpressionhandles.get(handle);
  if (!obj) {
    throw new Error(`XPathExpression handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional XPathExpression by handle. */
function lookupOptionXPathExpression(handle: bigint | undefined): XPathExpression | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xPathExpressionhandles.get(handle) ?? null;
}
/**
 * `evaluate()` operation.
 */
export function XPathExpressionEvaluate(self: bigint, contextNode: bigint, type: number | undefined, result: bigint | undefined): bigint {
  const obj = lookupXPathExpression(self);
  const _callResult = obj.evaluate(lookupNode(contextNode), type, lookupOptionXpathResult(result));
  const handle = _nextXpathResult++;
  _xpathResultHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: x-path-ns-resolver
// ---------------------------------------------------------------------------

/** Type alias */
export type XPathNsResolverHandle = bigint;

/** Handle table for XPathNSResolver instances */
const _xPathNsResolverhandles = new Map<bigint, XPathNSResolver>();
let _nextXPathNSResolver = 1n;

/** Lookup a XPathNSResolver by handle, throwing if not found. */
function lookupXPathNSResolver(handle: bigint): XPathNSResolver {
  const obj = _xPathNsResolverhandles.get(handle);
  if (!obj) {
    throw new Error(`XPathNSResolver handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional XPathNSResolver by handle. */
function lookupOptionXPathNSResolver(handle: bigint | undefined): XPathNSResolver | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xPathNsResolverhandles.get(handle) ?? null;
}
/**
 * `lookup-namespace-uri()` operation.
 */
export function XPathNsResolverLookupNamespaceUri(self: bigint, prefix: string | undefined): string | undefined {
  const obj = lookupXPathNSResolver(self);
  return (obj as any).lookupNamespaceURI(prefix) ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: x-path-evaluator-base
// ---------------------------------------------------------------------------

/** Type alias */
export type XPathEvaluatorBaseHandle = bigint;

/** Handle table for XPathEvaluatorBase instances */
const _xPathEvaluatorBasehandles = new Map<bigint, XPathEvaluatorBase>();
let _nextXPathEvaluatorBase = 1n;

/** Register a new XPathEvaluatorBase and return its handle. */
function registerXPathEvaluatorBase(obj: XPathEvaluatorBase): bigint {
  const handle = _nextXPathEvaluatorBase++;
  _xPathEvaluatorBasehandles.set(handle, obj);
  return handle;
}
/** Lookup a XPathEvaluatorBase by handle, throwing if not found. */
function lookupXPathEvaluatorBase(handle: bigint): XPathEvaluatorBase {
  const obj = _xPathEvaluatorBasehandles.get(handle);
  if (!obj) {
    throw new Error(`XPathEvaluatorBase handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional XPathEvaluatorBase by handle. */
function lookupOptionXPathEvaluatorBase(handle: bigint | undefined): XPathEvaluatorBase | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xPathEvaluatorBasehandles.get(handle) ?? null;
}
/**
 * `create-expression()` operation.
 */
export function createExpression(self: bigint, expression: string, resolver: bigint | undefined): bigint {
  const obj = lookupXPathEvaluatorBase(self);
  const _callResult = obj.createExpression(expression, resolver as any);
  const handle = _nextXpathExpression++;
  _xpathExpressionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-ns-resolver()` operation.
 */
export function createNsResolver(self: bigint, nodeResolver: bigint): bigint {
  const obj = lookupXPathEvaluatorBase(self);
  const _callResult = obj.createNSResolver(lookupNode(nodeResolver));
  const handle = _nextXpathNsResolver++;
  _xpathNsResolverHandles.set(handle, _callResult);
  return handle;
}

/**
 * `evaluate()` operation.
 */
export function XPathEvaluatorBaseEvaluate(self: bigint, expression: string, contextNode: bigint, resolver: bigint | undefined, type: number | undefined, result: bigint | undefined): bigint {
  const obj = lookupXPathEvaluatorBase(self);
  const _callResult = obj.evaluate(expression, lookupNode(contextNode), resolver as any, type, lookupOptionXpathResult(result));
  const handle = _nextXpathResult++;
  _xpathResultHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: xslt-processor
// ---------------------------------------------------------------------------

/** Type alias */
export type XsltProcessorHandle = bigint;

/** Handle table for XSLTProcessor instances */
const _xsltProcessorhandles = new Map<bigint, XSLTProcessor>();
let _nextXSLTProcessor = 1n;

/** Lookup a XSLTProcessor by handle, throwing if not found. */
function lookupXSLTProcessor(handle: bigint): XSLTProcessor {
  const obj = _xsltProcessorhandles.get(handle);
  if (!obj) {
    throw new Error(`XSLTProcessor handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional XSLTProcessor by handle. */
function lookupOptionXSLTProcessor(handle: bigint | undefined): XSLTProcessor | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _xsltProcessorhandles.get(handle) ?? null;
}
/**
 * `import-stylesheet()` operation.
 */
export function importStylesheet(self: bigint, style: bigint): void {
  const obj = lookupXSLTProcessor(self);
  obj.importStylesheet(lookupNode(style));
}

/**
 * `transform-to-fragment()` operation.
 */
export function transformToFragment(self: bigint, source: bigint, output: bigint): bigint {
  const obj = lookupXSLTProcessor(self);
  const _callResult = obj.transformToFragment(lookupNode(source), lookupDocument(output));
  const handle = _nextDocumentFragment++;
  _documentFragmentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `transform-to-document()` operation.
 */
export function transformToDocument(self: bigint, source: bigint): bigint {
  const obj = lookupXSLTProcessor(self);
  const _callResult = obj.transformToDocument(lookupNode(source));
  const handle = _nextDocument++;
  _documentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-parameter()` operation.
 */
export function setParameter(self: bigint, namespaceUri: string, localName: string, value: string): void {
  const obj = lookupXSLTProcessor(self);
  obj.setParameter(namespaceUri, localName, value);
}

/**
 * `get-parameter()` operation.
 */
export function getParameter(self: bigint, namespaceUri: string, localName: string): string {
  const obj = lookupXSLTProcessor(self);
  return obj.getParameter(namespaceUri, localName as any);
}

/**
 * `remove-parameter()` operation.
 */
export function removeParameter(self: bigint, namespaceUri: string, localName: string): void {
  const obj = lookupXSLTProcessor(self);
  obj.removeParameter(namespaceUri, localName);
}

/**
 * `clear-parameters()` operation.
 */
export function clearParameters(self: bigint): void {
  const obj = lookupXSLTProcessor(self);
  obj.clearParameters();
}

/**
 * `reset()` operation.
 */
export function reset(self: bigint): void {
  const obj = lookupXSLTProcessor(self);
  obj.reset();
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  EventGetType,
  EventGetTarget,
  getSrcElement,
  getCurrentTarget,
  composedPath,
  getEventPhase,
  stopPropagation,
  getCancelBubble,
  setCancelBubble,
  stopImmediatePropagation,
  getBubbles,
  getCancelable,
  getReturnValue,
  setReturnValue,
  preventDefault,
  getDefaultPrevented,
  getComposed,
  getIsTrusted,
  getTimeStamp,
  initEvent,
  getDetail,
  initCustomEvent,
  addEventListener,
  removeEventListener,
  dispatchEvent,
  handleEvent,
  getSignal,
  AbortControllerAbort,
  AbortSignalAbort,
  timeout,
  any,
  getAborted,
  getReason,
  throwIfAborted,
  getOnabort,
  setOnabort,
  getElementById,
  getChildren,
  getFirstElementChild,
  getLastElementChild,
  getChildElementCount,
  prepend,
  append,
  replaceChildren,
  moveBefore,
  querySelector,
  querySelectorAll,
  getPreviousElementSibling,
  getNextElementSibling,
  before,
  after,
  replaceWith,
  ChildNodeRemove,
  getAssignedSlot,
  NodeListItem,
  NodeListGetLength,
  HtmlCollectionGetLength,
  HtmlCollectionItem,
  namedItem,
  observe,
  disconnect,
  takeRecords,
  MutationRecordGetType,
  MutationRecordGetTarget,
  getAddedNodes,
  getRemovedNodes,
  MutationRecordGetPreviousSibling,
  MutationRecordGetNextSibling,
  getAttributeName,
  getAttributeNamespace,
  getOldValue,
  getNodeType,
  getNodeName,
  getBaseUri,
  getIsConnected,
  getOwnerDocument,
  getRootNode,
  getParentNode,
  getParentElement,
  hasChildNodes,
  getChildNodes,
  getFirstChild,
  getLastChild,
  NodeGetPreviousSibling,
  NodeGetNextSibling,
  getNodeValue,
  setNodeValue,
  getTextContent,
  setTextContent,
  normalize,
  cloneNode,
  isEqualNode,
  isSameNode,
  compareDocumentPosition,
  NodeContains,
  lookupPrefix,
  NodeLookupNamespaceUri,
  isDefaultNamespace,
  insertBefore,
  appendChild,
  replaceChild,
  removeChild,
  createDocumentType,
  createDocument,
  createHtmlDocument,
  hasFeature,
  DocumentTypeGetName,
  getPublicId,
  getSystemId,
  getMode,
  getDelegatesFocus,
  getSlotAssignment,
  getClonable,
  getSerializable,
  getHost,
  getOnslotchange,
  setOnslotchange,
  setHtmlUnsafe,
  getHtml,
  getInnerHtml,
  setInnerHtml,
  NamedNodeMapGetLength,
  NamedNodeMapItem,
  getNamedItem,
  getNamedItemNs,
  setNamedItem,
  setNamedItemNs,
  removeNamedItem,
  removeNamedItemNs,
  getNamespaceUri,
  getPrefix,
  getLocalName,
  AttrGetName,
  AttrGetValue,
  AttrSetValue,
  getOwnerElement,
  getSpecified,
  getData,
  setData,
  CharacterDataGetLength,
  substringData,
  appendData,
  insertData,
  deleteData,
  replaceData,
  splitText,
  getWholeText,
  ProcessingInstructionGetTarget,
  getStartContainer,
  getStartOffset,
  getEndContainer,
  getEndOffset,
  getCollapsed,
  NodeIteratorGetRoot,
  getReferenceNode,
  getPointerBeforeReferenceNode,
  NodeIteratorGetWhatToShow,
  NodeIteratorGetFilter,
  NodeIteratorNextNode,
  NodeIteratorPreviousNode,
  detach,
  TreeWalkerGetRoot,
  TreeWalkerGetWhatToShow,
  TreeWalkerGetFilter,
  getCurrentNode,
  setCurrentNode,
  parentNode,
  firstChild,
  lastChild,
  previousSibling,
  nextSibling,
  TreeWalkerPreviousNode,
  TreeWalkerNextNode,
  acceptNode,
  DomTokenListGetLength,
  DomTokenListItem,
  DomTokenListContains,
  add,
  DomTokenListRemove,
  toggle,
  replace,
  supports,
  DomTokenListGetValue,
  DomTokenListSetValue,
  getResultType,
  getNumberValue,
  getStringValue,
  getBooleanValue,
  getSingleNodeValue,
  getInvalidIteratorState,
  getSnapshotLength,
  iterateNext,
  snapshotItem,
  XPathExpressionEvaluate,
  XPathNsResolverLookupNamespaceUri,
  createExpression,
  createNsResolver,
  XPathEvaluatorBaseEvaluate,
  importStylesheet,
  transformToFragment,
  transformToDocument,
  setParameter,
  getParameter,
  removeParameter,
  clearParameters,
  reset
};
