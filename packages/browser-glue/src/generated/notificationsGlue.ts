/**
 * notifications glue — implements the `tairitsu-browser:notifications` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-worlds/wit/generated/notifications.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for GeometryUtils */
export type GeometryUtils = unknown;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = unknown;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = unknown;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = unknown;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = unknown;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = unknown;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = unknown;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = unknown;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = unknown;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = unknown;

/** Type definition for Navigation */
export type Navigation = unknown;

/** Type definition for NavigationTransition */
export type NavigationTransition = unknown;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = unknown;

/** Type definition for NavigationDestination */
export type NavigationDestination = unknown;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// WIT interface: notification
// ---------------------------------------------------------------------------

/** Type alias */
export type NotificationHandle = bigint;

/** Handle table for Notification instances */
const _notificationHandles = new Map<bigint, Notification>();
let _nextNotification = 1n;

/** Get a Notification by handle, throwing if not found. */
function getNotification(handle: bigint): Notification {
  const obj = _notificationHandles.get(handle);
  if (!obj) {
    throw new Error(`Notification handle ${handle} not found`);
  }
  return obj;
}
/**
 * `get-permission()` operation.
 */
export function getPermission(): bigint {
  return Notification.permission;
}

/**
 * `request-permission()` operation.
 *
 * Async operation: returns request ID, poll with `pollRequestPermission()`
 */
export function requestPermission(deprecatedCallback: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const promise = Notification.requestPermission(deprecatedCallback)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `requestPermission()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollRequestPermission(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-max-actions()` operation.
 */
export function getMaxActions(): number {
  return (Notification as any).maxActions;
}

/**
 * `get-onclick()` operation.
 */
export function getOnclick(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return obj.onclick;
}

/**
 * `set-onclick()` operation.
 */
export function setOnclick(self: bigint, value: number | undefined): void {
  const obj = getNotification(self);
  obj.onclick = value;
}

/**
 * `get-onshow()` operation.
 */
export function getOnshow(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return obj.onshow;
}

/**
 * `set-onshow()` operation.
 */
export function setOnshow(self: bigint, value: EventHandlerRecord): void {
  const obj = getNotification(self);
  obj.onshow = value;
}

/**
 * `get-onerror()` operation.
 */
export function getOnerror(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return obj.onerror;
}

/**
 * `set-onerror()` operation.
 */
export function setOnerror(self: bigint, value: EventHandlerRecord): void {
  const obj = getNotification(self);
  obj.onerror = value;
}

/**
 * `get-onclose()` operation.
 */
export function getOnclose(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return obj.onclose;
}

/**
 * `set-onclose()` operation.
 */
export function setOnclose(self: bigint, value: number): void {
  const obj = getNotification(self);
  obj.onclose = value;
}

/**
 * `get-title()` operation.
 */
export function getTitle(self: bigint): string {
  const obj = getNotification(self);
  return obj.title;
}

/**
 * `get-dir()` operation.
 */
export function getDir(self: bigint): bigint {
  const obj = getNotification(self);
  return obj.dir;
}

/**
 * `get-lang()` operation.
 */
export function getLang(self: bigint): number {
  const obj = getNotification(self);
  return obj.lang;
}

/**
 * `get-body()` operation.
 */
export function getBody(self: bigint): boolean {
  const obj = getNotification(self);
  return obj.body;
}

/**
 * `get-navigate()` operation.
 */
export function getNavigate(self: bigint): string {
  const obj = getNotification(self);
  return (obj as any).navigate;
}

/**
 * `get-tag()` operation.
 */
export function getTag(self: bigint): string {
  const obj = getNotification(self);
  return obj.tag;
}

/**
 * `get-image()` operation.
 */
export function getImage(self: bigint): number {
  const obj = getNotification(self);
  return (obj as any).image;
}

/**
 * `get-icon()` operation.
 */
export function getIcon(self: bigint): string {
  const obj = getNotification(self);
  return obj.icon;
}

/**
 * `get-badge()` operation.
 */
export function getBadge(self: bigint): number {
  const obj = getNotification(self);
  return obj.badge;
}

/**
 * `get-vibrate()` operation.
 */
export function getVibrate(self: bigint): number {
  const obj = getNotification(self);
  return (obj as any).vibrate;
}

/**
 * `get-timestamp()` operation.
 */
export function getTimestamp(self: bigint): boolean {
  const obj = getNotification(self);
  return (obj as any).timestamp;
}

/**
 * `get-renotify()` operation.
 */
export function getRenotify(self: bigint): number {
  const obj = getNotification(self);
  return (obj as any).renotify;
}

/**
 * `get-silent()` operation.
 */
export function getSilent(self: bigint): number {
  const obj = getNotification(self);
  return obj.silent ?? undefined;
}

/**
 * `get-require-interaction()` operation.
 */
export function getRequireInteraction(self: bigint): bigint {
  const obj = getNotification(self);
  return obj.requireInteraction ? 1n : 0n;
}

/**
 * `get-data()` operation.
 */
export function getData(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return obj.data;
}

/**
 * `get-actions()` operation.
 */
export function getActions(self: bigint): EventHandlerRecord {
  const obj = getNotification(self);
  return (obj as any).actions;
}

/**
 * `close()` operation.
 */
export function close(self: bigint): void {
  const obj = getNotification(self);
  obj.close();
}

// ---------------------------------------------------------------------------
// WIT interface: service-worker-registration
// ---------------------------------------------------------------------------

/** Type alias */
export type ServiceWorkerRegistrationHandle = bigint;

/** Handle table for ServiceWorkerRegistration instances */
const _serviceWorkerRegistrationhandles = new Map<bigint, ServiceWorkerRegistration>();
let _nextServiceWorkerRegistration = 1n;

/** Get a ServiceWorkerRegistration by handle, throwing if not found. */
function getServiceWorkerRegistration(handle: bigint): ServiceWorkerRegistration {
  const obj = _serviceWorkerRegistrationhandles.get(handle);
  if (!obj) {
    throw new Error(`ServiceWorkerRegistration handle ${handle} not found`);
  }
  return obj;
}
/**
 * `show-notification()` operation.
 *
 * Async operation: returns request ID, poll with `pollShowNotification()`
 */
export function showNotification(self: bigint, title: string, options: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = getServiceWorkerRegistration(self);
  const promise = obj.showNotification(title, options)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `showNotification()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollShowNotification(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-notifications()` operation.
 *
 * Async operation: returns request ID, poll with `pollGetNotifications()`
 */
export function getNotifications(self: bigint, filter: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = getServiceWorkerRegistration(self);
  const promise = obj.getNotifications(filter)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `getNotifications()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollGetNotifications(requestId: bigint): { ok: true; value: string } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: string } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-installing()` operation.
 */
export function getInstalling(self: bigint): bigint | undefined {
  const obj = getServiceWorkerRegistration(self);
  return obj.installing ?? undefined;
}

/**
 * `get-waiting()` operation.
 */
export function getWaiting(self: bigint): number {
  const obj = getServiceWorkerRegistration(self);
  return obj.waiting ?? undefined;
}

/**
 * `get-active()` operation.
 */
export function getActive(self: bigint): bigint | undefined {
  const obj = getServiceWorkerRegistration(self);
  return obj.active ?? undefined;
}

/**
 * `get-navigation-preload()` operation.
 */
export function getNavigationPreload(self: bigint): bigint {
  const obj = getServiceWorkerRegistration(self);
  return obj.navigationPreload;
}

/**
 * `get-scope()` operation.
 */
export function getScope(self: bigint): bigint {
  const obj = getServiceWorkerRegistration(self);
  return obj.scope;
}

/**
 * `get-update-via-cache()` operation.
 */
export function getUpdateViaCache(self: bigint): bigint {
  const obj = getServiceWorkerRegistration(self);
  return obj.updateViaCache;
}

/**
 * `update()` operation.
 *
 * Async operation: returns request ID, poll with `pollUpdate()`
 */
export function update(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = getServiceWorkerRegistration(self);
  const promise = obj.update()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `update()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollUpdate(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `unregister()` operation.
 *
 * Async operation: returns request ID, poll with `pollUnregister()`
 */
export function unregister(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = getServiceWorkerRegistration(self);
  const promise = obj.unregister()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `unregister()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollUnregister(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-onupdatefound()` operation.
 */
export function getOnupdatefound(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerRegistration(self);
  return obj.onupdatefound;
}

/**
 * `set-onupdatefound()` operation.
 */
export function setOnupdatefound(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerRegistration(self);
  obj.onupdatefound = value;
}

// ---------------------------------------------------------------------------
// WIT interface: notification-event
// ---------------------------------------------------------------------------

/** Type alias */
export type NotificationEventHandle = bigint;

/** Handle table for NotificationEvent instances */
const _notificationEventhandles = new Map<bigint, NotificationEvent>();
let _nextNotificationEvent = 1n;

/** Get a NotificationEvent by handle, throwing if not found. */
function getNotificationEvent(handle: bigint): NotificationEvent {
  const obj = _notificationEventhandles.get(handle);
  if (!obj) {
    throw new Error(`NotificationEvent handle ${handle} not found`);
  }
  return obj;
}
/**
 * `get-notification()` operation.
 */
export function NotificationEventGetNotification(self: bigint): bigint {
  const obj = getNotificationEvent(self);
  return (obj as any).notification;
}

/**
 * `get-action()` operation.
 */
export function getAction(self: bigint): string {
  const obj = getNotificationEvent(self);
  return (obj as any).action;
}

// ---------------------------------------------------------------------------
// WIT interface: service-worker-global-scope
// ---------------------------------------------------------------------------

/** Type alias */
export type ServiceWorkerGlobalScopeHandle = bigint;

/** Handle table for ServiceWorkerGlobalScope instances */
const _serviceWorkerGlobalScopehandles = new Map<bigint, ServiceWorkerGlobalScope>();
let _nextServiceWorkerGlobalScope = 1n;

/** Get a ServiceWorkerGlobalScope by handle, throwing if not found. */
function getServiceWorkerGlobalScope(handle: bigint): ServiceWorkerGlobalScope {
  const obj = _serviceWorkerGlobalScopehandles.get(handle);
  if (!obj) {
    throw new Error(`ServiceWorkerGlobalScope handle ${handle} not found`);
  }
  return obj;
}
/**
 * `get-onnotificationclick()` operation.
 */
export function getOnnotificationclick(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onnotificationclick;
}

/**
 * `set-onnotificationclick()` operation.
 */
export function setOnnotificationclick(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onnotificationclick = value;
}

/**
 * `get-onnotificationclose()` operation.
 */
export function getOnnotificationclose(self: bigint): boolean {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onnotificationclose;
}

/**
 * `set-onnotificationclose()` operation.
 */
export function setOnnotificationclose(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onnotificationclose = value;
}

/**
 * `get-clients()` operation.
 */
export function getClients(self: bigint): boolean {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.clients;
}

/**
 * `get-registration()` operation.
 */
export function getRegistration(self: bigint): boolean {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.registration;
}

/**
 * `get-service-worker()` operation.
 */
export function getServiceWorker(self: bigint): bigint {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.serviceWorker;
}

/**
 * `skip-waiting()` operation.
 */
export function skipWaiting(self: bigint): bigint {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.skipWaiting();
}

/**
 * `get-oninstall()` operation.
 */
export function getOninstall(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.oninstall;
}

/**
 * `set-oninstall()` operation.
 */
export function setOninstall(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.oninstall = value;
}

/**
 * `get-onactivate()` operation.
 */
export function getOnactivate(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onactivate;
}

/**
 * `set-onactivate()` operation.
 */
export function setOnactivate(self: bigint, value: boolean): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onactivate = value;
}

/**
 * `get-onfetch()` operation.
 */
export function getOnfetch(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onfetch;
}

/**
 * `set-onfetch()` operation.
 */
export function setOnfetch(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onfetch = value;
}

/**
 * `get-onmessage()` operation.
 */
export function getOnmessage(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onmessage;
}

/**
 * `set-onmessage()` operation.
 */
export function setOnmessage(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onmessage = value;
}

/**
 * `get-onmessageerror()` operation.
 */
export function getOnmessageerror(self: bigint): EventHandlerRecord {
  const obj = getServiceWorkerGlobalScope(self);
  return obj.onmessageerror;
}

/**
 * `set-onmessageerror()` operation.
 */
export function setOnmessageerror(self: bigint, value: EventHandlerRecord): void {
  const obj = getServiceWorkerGlobalScope(self);
  obj.onmessageerror = value;
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  getPermission,
  requestPermission,
  pollRequestPermission,
  getMaxActions,
  getOnclick,
  setOnclick,
  getOnshow,
  setOnshow,
  getOnerror,
  setOnerror,
  getOnclose,
  setOnclose,
  getTitle,
  getDir,
  getLang,
  getBody,
  getNavigate,
  getTag,
  getImage,
  getIcon,
  getBadge,
  getVibrate,
  getTimestamp,
  getRenotify,
  getSilent,
  getRequireInteraction,
  getData,
  getActions,
  close,
  showNotification,
  pollShowNotification,
  getNotifications,
  pollGetNotifications,
  getInstalling,
  getWaiting,
  getActive,
  getNavigationPreload,
  getScope,
  getUpdateViaCache,
  update,
  pollUpdate,
  unregister,
  pollUnregister,
  getOnupdatefound,
  setOnupdatefound,
  NotificationEventGetNotification,
  getAction,
  getOnnotificationclick,
  setOnnotificationclick,
  getOnnotificationclose,
  setOnnotificationclose,
  getClients,
  getRegistration,
  getServiceWorker,
  skipWaiting,
  getOninstall,
  setOninstall,
  getOnactivate,
  setOnactivate,
  getOnfetch,
  setOnfetch,
  getOnmessage,
  setOnmessage,
  getOnmessageerror,
  setOnmessageerror
};
