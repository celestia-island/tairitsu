/**
 * @tairitsu/browser-glue
 *
 * Re-exports all browser WIT glue implementations from the glue/ subdirectory.
 * The glue/ directory is populated by: just glue-gen (scripts/generate_browser_glue.py)
 */
export * from "./glue/index";
