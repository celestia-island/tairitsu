/**
 * media glue — implements the `tairitsu-browser:media` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-glue/../../packages/browser-worlds/wit/generated/media.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for EventHandler */
export type EventHandler = (this: any, ev: any) => any;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for any values */
const _anyHandles = new Map<bigint, any>();
let _nextAny = 1n;

/** Handle table for blob values */
const _blobHandles = new Map<bigint, Blob>();
let _nextBlob = 1n;

/** Handle table for media-image values */
const _mediaImageHandles = new Map<bigint, MediaImage>();
let _nextMediaImage = 1n;

/** Handle table for media-stream values */
const _mediaStreamHandles = new Map<bigint, MediaStream>();
let _nextMediaStream = 1n;

/** Handle table for media-stream-track values */
const _mediaStreamTrackHandles = new Map<bigint, MediaStreamTrack>();
let _nextMediaStreamTrack = 1n;

/** Handle table for promise-any values */
const _promiseAnyHandles = new Map<bigint, Promise<any>>();
let _nextPromiseAny = 1n;

/** Handle table for promise-void values */
const _promiseVoidHandles = new Map<bigint, Promise<void>>();
let _nextPromiseVoid = 1n;

/** Handle table for speech-synthesis-utterance values */
const _speechSynthesisUtteranceHandles = new Map<bigint, SpeechSynthesisUtterance>();
let _nextSpeechSynthesisUtterance = 1n;

/** Handle table for speech-synthesis-voice values */
const _speechSynthesisVoiceHandles = new Map<bigint, SpeechSynthesisVoice>();
let _nextSpeechSynthesisVoice = 1n;

/** Handle table for speech-synthesis-voice-list values */
const _speechSynthesisVoiceListhandles = new Map<bigint, SpeechSynthesisVoice[]>();
let _nextSpeechSynthesisVoiceList = 1n;

/** Handle table for string values */
const _stringHandles = new Map<bigint, string>();
let _nextString = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a any value by handle. */
function lookupAny(handle: bigint): any {
  const obj = _anyHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`any handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional any value by handle. */
function lookupOptionAny(handle: bigint | undefined): any | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _anyHandles.get(handle) ?? null;
}

/** Lookup a blob value by handle. */
function lookupBlob(handle: bigint): Blob {
  const obj = _blobHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`blob handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional blob value by handle. */
function lookupOptionBlob(handle: bigint | undefined): Blob | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _blobHandles.get(handle) ?? null;
}

/** Lookup a media-image value by handle. */
function lookupMediaImage(handle: bigint): MediaImage {
  const obj = _mediaImageHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`media-image handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional media-image value by handle. */
function lookupOptionMediaImage(handle: bigint | undefined): MediaImage | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaImageHandles.get(handle) ?? null;
}

/** Lookup a media-stream value by handle. */
function lookupMediaStream(handle: bigint): MediaStream {
  const obj = _mediaStreamHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`media-stream handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional media-stream value by handle. */
function lookupOptionMediaStream(handle: bigint | undefined): MediaStream | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamHandles.get(handle) ?? null;
}

/** Lookup a media-stream-track value by handle. */
function lookupMediaStreamTrack(handle: bigint): MediaStreamTrack {
  const obj = _mediaStreamTrackHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`media-stream-track handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional media-stream-track value by handle. */
function lookupOptionMediaStreamTrack(handle: bigint | undefined): MediaStreamTrack | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamTrackHandles.get(handle) ?? null;
}

/** Lookup a promise-any value by handle. */
function lookupPromiseAny(handle: bigint): Promise<any> {
  const obj = _promiseAnyHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`promise-any handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional promise-any value by handle. */
function lookupOptionPromiseAny(handle: bigint | undefined): Promise<any> | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _promiseAnyHandles.get(handle) ?? null;
}

/** Lookup a promise-void value by handle. */
function lookupPromiseVoid(handle: bigint): Promise<void> {
  const obj = _promiseVoidHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`promise-void handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional promise-void value by handle. */
function lookupOptionPromiseVoid(handle: bigint | undefined): Promise<void> | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _promiseVoidHandles.get(handle) ?? null;
}

/** Lookup a speech-synthesis-utterance value by handle. */
function lookupSpeechSynthesisUtterance(handle: bigint): SpeechSynthesisUtterance {
  const obj = _speechSynthesisUtteranceHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`speech-synthesis-utterance handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional speech-synthesis-utterance value by handle. */
function lookupOptionSpeechSynthesisUtterance(handle: bigint | undefined): SpeechSynthesisUtterance | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesisUtteranceHandles.get(handle) ?? null;
}

/** Lookup a speech-synthesis-voice value by handle. */
function lookupSpeechSynthesisVoice(handle: bigint): SpeechSynthesisVoice {
  const obj = _speechSynthesisVoiceHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`speech-synthesis-voice handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional speech-synthesis-voice value by handle. */
function lookupOptionSpeechSynthesisVoice(handle: bigint | undefined): SpeechSynthesisVoice | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesisVoiceHandles.get(handle) ?? null;
}

/** Lookup a speech-synthesis-voice-list value by handle. */
function lookupSpeechSynthesisVoiceList(handle: bigint): SpeechSynthesisVoice[] {
  const obj = _speechSynthesisVoiceListhandles.get(handle);
  if (obj === undefined) {
    throw new Error(`speech-synthesis-voice-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional speech-synthesis-voice-list value by handle. */
function lookupOptionSpeechSynthesisVoiceList(handle: bigint | undefined): SpeechSynthesisVoice[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesisVoiceListhandles.get(handle) ?? null;
}

/** Lookup a string value by handle. */
function lookupString(handle: bigint): string {
  const obj = _stringHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string value by handle. */
function lookupOptionString(handle: bigint | undefined): string | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: media-capabilities
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaCapabilitiesHandle = bigint;

/** Handle table for MediaCapabilities instances */
const _mediaCapabilitieshandles = new Map<bigint, MediaCapabilities>();
let _nextMediaCapabilities = 1n;

/** Lookup a MediaCapabilities by handle, throwing if not found. */
function lookupMediaCapabilities(handle: bigint): MediaCapabilities {
  const obj = _mediaCapabilitieshandles.get(handle);
  if (!obj) {
    throw new Error(`MediaCapabilities handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaCapabilities by handle. */
function lookupOptionMediaCapabilities(handle: bigint | undefined): MediaCapabilities | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaCapabilitieshandles.get(handle) ?? null;
}
/**
 * `decoding-info()` operation.
 *
 * Async operation: returns request ID, poll with `pollDecodingInfo()`
 */
export function decodingInfo(self: bigint, configuration: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaCapabilities(self);
  const promise = obj.decodingInfo(configuration as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `decodingInfo()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollDecodingInfo(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `encoding-info()` operation.
 *
 * Async operation: returns request ID, poll with `pollEncodingInfo()`
 */
export function encodingInfo(self: bigint, configuration: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaCapabilities(self);
  const promise = obj.encodingInfo(configuration as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `encodingInfo()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollEncodingInfo(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: media-stream
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaStreamHandle = bigint;

/**
 * `get-id()` operation.
 */
export function MediaStreamGetId(self: bigint): string {
  const obj = lookupMediaStream(self);
  return obj.id;
}

/**
 * `get-audio-tracks()` operation.
 */
export function getAudioTracks(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  const _callResult = obj.getAudioTracks();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-video-tracks()` operation.
 */
export function getVideoTracks(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  const _callResult = obj.getVideoTracks();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-tracks()` operation.
 */
export function getTracks(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  const _callResult = (obj as any).tracks;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-track-by-id()` operation.
 */
export function getTrackById(self: bigint, trackId: string): bigint | undefined {
  const obj = lookupMediaStream(self);
  const _callResult = obj.getTrackById(trackId as any);
  if (_callResult === null) return undefined;
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

/**
 * `add-track()` operation.
 */
export function addTrack(self: bigint, track: bigint): void {
  const obj = lookupMediaStream(self);
  obj.addTrack(lookupMediaStreamTrack(track));
}

/**
 * `remove-track()` operation.
 */
export function removeTrack(self: bigint, track: bigint): void {
  const obj = lookupMediaStream(self);
  obj.removeTrack(lookupMediaStreamTrack(track));
}

/**
 * `clone()` operation.
 */
export function MediaStreamClone(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  const _callResult = obj.clone();
  const handle = _nextMediaStream++;
  _mediaStreamHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-active()` operation.
 */
export function getActive(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  return obj.active ? 1n : 0n;
}

/**
 * `get-onaddtrack()` operation.
 */
export function getOnaddtrack(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  return (obj as any).onaddtrack;
}

/**
 * `set-onaddtrack()` operation.
 */
export function setOnaddtrack(self: bigint, value: bigint): void {
  const obj = lookupMediaStream(self);
  (obj as any).onaddtrack = value as any;
}

/**
 * `get-onremovetrack()` operation.
 */
export function getOnremovetrack(self: bigint): bigint {
  const obj = lookupMediaStream(self);
  return (obj as any).onremovetrack;
}

/**
 * `set-onremovetrack()` operation.
 */
export function setOnremovetrack(self: bigint, value: bigint): void {
  const obj = lookupMediaStream(self);
  (obj as any).onremovetrack = value as any;
}

// ---------------------------------------------------------------------------
// WIT interface: media-stream-track
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaStreamTrackHandle = bigint;

/**
 * `get-kind()` operation.
 */
export function MediaStreamTrackGetKind(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const value = obj.kind;
  switch ((value as any)) {
    default: return 0n;
  }
}

/**
 * `get-id()` operation.
 */
export function MediaStreamTrackGetId(self: bigint): string {
  const obj = lookupMediaStreamTrack(self);
  return obj.id;
}

/**
 * `get-label()` operation.
 */
export function MediaStreamTrackGetLabel(self: bigint): string {
  const obj = lookupMediaStreamTrack(self);
  return obj.label;
}

/**
 * `get-enabled()` operation.
 */
export function getEnabled(self: bigint): boolean {
  const obj = lookupMediaStreamTrack(self);
  return obj.enabled;
}

/**
 * `set-enabled()` operation.
 */
export function setEnabled(self: bigint, value: boolean): void {
  const obj = lookupMediaStreamTrack(self);
  obj.enabled = value;
}

/**
 * `get-muted()` operation.
 */
export function getMuted(self: bigint): boolean {
  const obj = lookupMediaStreamTrack(self);
  return obj.muted;
}

/**
 * `get-onmute()` operation.
 */
export function getOnmute(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  return (obj as any).onmute;
}

/**
 * `set-onmute()` operation.
 */
export function setOnmute(self: bigint, value: bigint): void {
  const obj = lookupMediaStreamTrack(self);
  (obj as any).onmute = value;
}

/**
 * `get-onunmute()` operation.
 */
export function getOnunmute(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  return (obj as any).onunmute;
}

/**
 * `set-onunmute()` operation.
 */
export function setOnunmute(self: bigint, value: bigint): void {
  const obj = lookupMediaStreamTrack(self);
  (obj as any).onunmute = value;
}

/**
 * `get-ready-state()` operation.
 */
export function getReadyState(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const value = obj.readyState;
  switch (value) {
    case 'live': return 0n;
    case 'ended': return 1n;
    default: return 0n;
  }
}

/**
 * `get-onended()` operation.
 */
export function MediaStreamTrackGetOnended(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  return (obj as any).onended;
}

/**
 * `set-onended()` operation.
 */
export function MediaStreamTrackSetOnended(self: bigint, value: bigint): void {
  const obj = lookupMediaStreamTrack(self);
  (obj as any).onended = value;
}

/**
 * `clone()` operation.
 */
export function MediaStreamTrackClone(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const _callResult = obj.clone();
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

/**
 * `stop()` operation.
 */
export function MediaStreamTrackStop(self: bigint): void {
  const obj = lookupMediaStreamTrack(self);
  obj.stop();
}

/**
 * `get-capabilities()` operation.
 */
export function MediaStreamTrackGetCapabilities(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const _callResult = obj.getCapabilities();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-constraints()` operation.
 */
export function getConstraints(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const _callResult = obj.getConstraints();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-settings()` operation.
 */
export function getSettings(self: bigint): bigint {
  const obj = lookupMediaStreamTrack(self);
  const _callResult = (obj as any).settings;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `apply-constraints()` operation.
 *
 * Async operation: returns request ID, poll with `pollApplyConstraints()`
 */
export function applyConstraints(self: bigint, constraints: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaStreamTrack(self);
  const promise = obj.applyConstraints(constraints as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `applyConstraints()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollApplyConstraints(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: media-stream-track-event
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaStreamTrackEventHandle = bigint;

/** Handle table for MediaStreamTrackEvent instances */
const _mediaStreamTrackEventhandles = new Map<bigint, MediaStreamTrackEvent>();
let _nextMediaStreamTrackEvent = 1n;

/** Lookup a MediaStreamTrackEvent by handle, throwing if not found. */
function lookupMediaStreamTrackEvent(handle: bigint): MediaStreamTrackEvent {
  const obj = _mediaStreamTrackEventhandles.get(handle);
  if (!obj) {
    throw new Error(`MediaStreamTrackEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaStreamTrackEvent by handle. */
function lookupOptionMediaStreamTrackEvent(handle: bigint | undefined): MediaStreamTrackEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamTrackEventhandles.get(handle) ?? null;
}
/**
 * `get-track()` operation.
 */
export function getTrack(self: bigint): bigint {
  const obj = lookupMediaStreamTrackEvent(self);
  const _callResult = obj.track;
  const handle = _nextMediaStreamTrack++;
  _mediaStreamTrackHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: overconstrained-error
// ---------------------------------------------------------------------------

/** Type alias */
export type OverconstrainedErrorHandle = bigint;

/** Handle table for OverconstrainedError instances */
const _overconstrainedErrorhandles = new Map<bigint, OverconstrainedError>();
let _nextOverconstrainedError = 1n;

/** Lookup a OverconstrainedError by handle, throwing if not found. */
function lookupOverconstrainedError(handle: bigint): OverconstrainedError {
  const obj = _overconstrainedErrorhandles.get(handle);
  if (!obj) {
    throw new Error(`OverconstrainedError handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional OverconstrainedError by handle. */
function lookupOptionOverconstrainedError(handle: bigint | undefined): OverconstrainedError | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _overconstrainedErrorhandles.get(handle) ?? null;
}
/**
 * `get-constraint()` operation.
 */
export function getConstraint(self: bigint): string {
  const obj = lookupOverconstrainedError(self);
  return obj.constraint;
}

// ---------------------------------------------------------------------------
// WIT interface: media-devices
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaDevicesHandle = bigint;

/** Handle table for MediaDevices instances */
const _mediaDeviceshandles = new Map<bigint, MediaDevices>();
let _nextMediaDevices = 1n;

/** Lookup a MediaDevices by handle, throwing if not found. */
function lookupMediaDevices(handle: bigint): MediaDevices {
  const obj = _mediaDeviceshandles.get(handle);
  if (!obj) {
    throw new Error(`MediaDevices handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaDevices by handle. */
function lookupOptionMediaDevices(handle: bigint | undefined): MediaDevices | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaDeviceshandles.get(handle) ?? null;
}
/**
 * `get-ondevicechange()` operation.
 */
export function getOndevicechange(self: bigint): bigint {
  const obj = lookupMediaDevices(self);
  return (obj as any).ondevicechange;
}

/**
 * `set-ondevicechange()` operation.
 */
export function setOndevicechange(self: bigint, value: bigint): void {
  const obj = lookupMediaDevices(self);
  (obj as any).ondevicechange = value as any;
}

/**
 * `enumerate-devices()` operation.
 *
 * Async operation: returns request ID, poll with `pollEnumerateDevices()`
 */
export function enumerateDevices(self: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaDevices(self);
  const promise = obj.enumerateDevices()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `enumerateDevices()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollEnumerateDevices(requestId: bigint): { ok: true } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result ?? undefined;
}

/**
 * `get-supported-constraints()` operation.
 */
export function getSupportedConstraints(self: bigint): bigint {
  const obj = lookupMediaDevices(self);
  const _callResult = obj.getSupportedConstraints();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-user-media()` operation.
 *
 * Async operation: returns request ID, poll with `pollGetUserMedia()`
 */
export function getUserMedia(self: bigint, constraints: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaDevices(self);
  const promise = obj.getUserMedia(constraints as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `getUserMedia()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollGetUserMedia(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-display-media()` operation.
 *
 * Async operation: returns request ID, poll with `pollGetDisplayMedia()`
 */
export function getDisplayMedia(self: bigint, options: bigint | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupMediaDevices(self);
  const promise = obj.getDisplayMedia(options as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `getDisplayMedia()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollGetDisplayMedia(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: media-device-info
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaDeviceInfoHandle = bigint;

/** Handle table for MediaDeviceInfo instances */
const _mediaDeviceInfohandles = new Map<bigint, MediaDeviceInfo>();
let _nextMediaDeviceInfo = 1n;

/** Lookup a MediaDeviceInfo by handle, throwing if not found. */
function lookupMediaDeviceInfo(handle: bigint): MediaDeviceInfo {
  const obj = _mediaDeviceInfohandles.get(handle);
  if (!obj) {
    throw new Error(`MediaDeviceInfo handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaDeviceInfo by handle. */
function lookupOptionMediaDeviceInfo(handle: bigint | undefined): MediaDeviceInfo | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaDeviceInfohandles.get(handle) ?? null;
}
/**
 * `get-device-id()` operation.
 */
export function getDeviceId(self: bigint): string {
  const obj = lookupMediaDeviceInfo(self);
  return obj.deviceId;
}

/**
 * `get-kind()` operation.
 */
export function MediaDeviceInfoGetKind(self: bigint): bigint {
  const obj = lookupMediaDeviceInfo(self);
  const value = obj.kind;
  switch (value) {
    case 'audioinput': return 0n;
    case 'audiooutput': return 1n;
    case 'videoinput': return 2n;
    default: return 0n;
  }
}

/**
 * `get-label()` operation.
 */
export function MediaDeviceInfoGetLabel(self: bigint): string {
  const obj = lookupMediaDeviceInfo(self);
  return obj.label;
}

/**
 * `get-group-id()` operation.
 */
export function getGroupId(self: bigint): string {
  const obj = lookupMediaDeviceInfo(self);
  return obj.groupId;
}

/**
 * `to-json()` operation.
 */
export function MediaDeviceInfoToJson(self: bigint): bigint {
  const obj = lookupMediaDeviceInfo(self);
  return obj.toJSON();
}

// ---------------------------------------------------------------------------
// WIT interface: input-device-info
// ---------------------------------------------------------------------------

/** Type alias */
export type InputDeviceInfoHandle = bigint;

/** Handle table for InputDeviceInfo instances */
const _inputDeviceInfohandles = new Map<bigint, InputDeviceInfo>();
let _nextInputDeviceInfo = 1n;

/** Lookup a InputDeviceInfo by handle, throwing if not found. */
function lookupInputDeviceInfo(handle: bigint): InputDeviceInfo {
  const obj = _inputDeviceInfohandles.get(handle);
  if (!obj) {
    throw new Error(`InputDeviceInfo handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional InputDeviceInfo by handle. */
function lookupOptionInputDeviceInfo(handle: bigint | undefined): InputDeviceInfo | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _inputDeviceInfohandles.get(handle) ?? null;
}
/**
 * `get-capabilities()` operation.
 */
export function InputDeviceInfoGetCapabilities(self: bigint): bigint {
  const obj = lookupInputDeviceInfo(self);
  const _callResult = obj.getCapabilities();
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: device-change-event
// ---------------------------------------------------------------------------

/** Type alias */
export type DeviceChangeEventHandle = bigint;

/** Handle table for DeviceChangeEvent instances */
const _deviceChangeEventhandles = new Map<bigint, DeviceChangeEvent>();
let _nextDeviceChangeEvent = 1n;

/** Lookup a DeviceChangeEvent by handle, throwing if not found. */
function lookupDeviceChangeEvent(handle: bigint): DeviceChangeEvent {
  const obj = _deviceChangeEventhandles.get(handle);
  if (!obj) {
    throw new Error(`DeviceChangeEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional DeviceChangeEvent by handle. */
function lookupOptionDeviceChangeEvent(handle: bigint | undefined): DeviceChangeEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _deviceChangeEventhandles.get(handle) ?? null;
}
/**
 * `get-devices()` operation.
 */
export function getDevices(self: bigint): (bigint)[] {
  const obj = lookupDeviceChangeEvent(self);
  return (obj as any).devices;
}

/**
 * `get-user-inserted-devices()` operation.
 */
export function getUserInsertedDevices(self: bigint): (bigint)[] {
  const obj = lookupDeviceChangeEvent(self);
  return (obj as any).userInsertedDevices;
}

// ---------------------------------------------------------------------------
// WIT interface: media-session
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaSessionHandle = bigint;

/** Handle table for MediaSession instances */
const _mediaSessionhandles = new Map<bigint, MediaSession>();
let _nextMediaSession = 1n;

/** Lookup a MediaSession by handle, throwing if not found. */
function lookupMediaSession(handle: bigint): MediaSession {
  const obj = _mediaSessionhandles.get(handle);
  if (!obj) {
    throw new Error(`MediaSession handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaSession by handle. */
function lookupOptionMediaSession(handle: bigint | undefined): MediaSession | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaSessionhandles.get(handle) ?? null;
}
/**
 * `get-metadata()` operation.
 */
export function getMetadata(self: bigint): bigint | undefined {
  const obj = lookupMediaSession(self);
  const _callResult = (obj as any).getMetadata();
  if (_callResult === null) return undefined;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-metadata()` operation.
 */
export function setMetadata(self: bigint, value: bigint | undefined): void {
  const obj = lookupMediaSession(self);
  (obj as any).metadata = value;
}

/**
 * `get-playback-state()` operation.
 */
export function getPlaybackState(self: bigint): bigint {
  const obj = lookupMediaSession(self);
  const _callResult = obj.playbackState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-playback-state()` operation.
 */
export function setPlaybackState(self: bigint, value: bigint): void {
  const obj = lookupMediaSession(self);
  obj.playbackState = value as any;
}

/**
 * `set-action-handler()` operation.
 */
export function setActionHandler(self: bigint, action: bigint, handler: bigint | undefined): void {
  const obj = lookupMediaSession(self);
  obj.setActionHandler(action as any, handler as any);
}

/**
 * `set-position-state()` operation.
 */
export function setPositionState(self: bigint, state: bigint | undefined): void {
  const obj = lookupMediaSession(self);
  obj.setPositionState(state as any);
}

/**
 * `set-microphone-active()` operation.
 */
export function setMicrophoneActive(self: bigint, active: boolean): bigint {
  const obj = lookupMediaSession(self);
  const _callResult = obj.setMicrophoneActive(Boolean(active));
  const handle = _nextPromiseVoid++;
  _promiseVoidHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-camera-active()` operation.
 */
export function setCameraActive(self: bigint, active: boolean): bigint {
  const obj = lookupMediaSession(self);
  const _callResult = obj.setCameraActive(active);
  const handle = _nextPromiseVoid++;
  _promiseVoidHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-screenshare-active()` operation.
 */
export function setScreenshareActive(self: bigint, active: boolean): bigint {
  const obj = lookupMediaSession(self);
  return (obj as any).setScreenshareActive(active);
}

// ---------------------------------------------------------------------------
// WIT interface: media-metadata
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaMetadataHandle = bigint;

/** Handle table for MediaMetadata instances */
const _mediaMetadatahandles = new Map<bigint, MediaMetadata>();
let _nextMediaMetadata = 1n;

/** Lookup a MediaMetadata by handle, throwing if not found. */
function lookupMediaMetadata(handle: bigint): MediaMetadata {
  const obj = _mediaMetadatahandles.get(handle);
  if (!obj) {
    throw new Error(`MediaMetadata handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaMetadata by handle. */
function lookupOptionMediaMetadata(handle: bigint | undefined): MediaMetadata | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaMetadatahandles.get(handle) ?? null;
}
/**
 * `get-title()` operation.
 */
export function MediaMetadataGetTitle(self: bigint): string {
  const obj = lookupMediaMetadata(self);
  return obj.title;
}

/**
 * `set-title()` operation.
 */
export function setTitle(self: bigint, value: string): void {
  const obj = lookupMediaMetadata(self);
  obj.title = value;
}

/**
 * `get-artist()` operation.
 */
export function getArtist(self: bigint): string {
  const obj = lookupMediaMetadata(self);
  return obj.artist;
}

/**
 * `set-artist()` operation.
 */
export function setArtist(self: bigint, value: string): void {
  const obj = lookupMediaMetadata(self);
  obj.artist = value;
}

/**
 * `get-album()` operation.
 */
export function getAlbum(self: bigint): string {
  const obj = lookupMediaMetadata(self);
  return obj.album;
}

/**
 * `set-album()` operation.
 */
export function setAlbum(self: bigint, value: string): void {
  const obj = lookupMediaMetadata(self);
  obj.album = value as any;
}

/**
 * `get-artwork()` operation.
 */
export function MediaMetadataGetArtwork(self: bigint): (bigint)[] {
  const obj = lookupMediaMetadata(self);
  const _arr = obj.artwork;
  const _handles: bigint[] = [];
  for (const item of _arr) {
    const handle = _nextMediaImage++;
    _mediaImageHandles.set(handle, item);
    _handles.push(handle);
  }
  return _handles;
}

/**
 * `set-artwork()` operation.
 */
export function setArtwork(self: bigint, value: (bigint)[]): void {
  const obj = lookupMediaMetadata(self);
  obj.artwork = Array.from(value).map((h: bigint) => lookupMediaImage(h));
}

/**
 * `get-chapter-info()` operation.
 */
export function getChapterInfo(self: bigint): (bigint)[] {
  const obj = lookupMediaMetadata(self);
  return (obj as any).chapterInfo;
}

// ---------------------------------------------------------------------------
// WIT interface: chapter-information
// ---------------------------------------------------------------------------

/** Type alias */
export type ChapterInformationHandle = bigint;

/** Handle table for ChapterInformation instances */
const _chapterInformationhandles = new Map<bigint, ChapterInformation>();
let _nextChapterInformation = 1n;

/** Lookup a ChapterInformation by handle, throwing if not found. */
function lookupChapterInformation(handle: bigint): ChapterInformation {
  const obj = _chapterInformationhandles.get(handle);
  if (!obj) {
    throw new Error(`ChapterInformation handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ChapterInformation by handle. */
function lookupOptionChapterInformation(handle: bigint | undefined): ChapterInformation | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _chapterInformationhandles.get(handle) ?? null;
}
/**
 * `get-title()` operation.
 */
export function ChapterInformationGetTitle(self: bigint): string {
  const obj = lookupChapterInformation(self);
  return obj.title;
}

/**
 * `get-start-time()` operation.
 */
export function getStartTime(self: bigint): number {
  const obj = lookupChapterInformation(self);
  return obj.startTime;
}

/**
 * `get-artwork()` operation.
 */
export function ChapterInformationGetArtwork(self: bigint): (bigint)[] {
  const obj = lookupChapterInformation(self);
  return obj.artwork;
}

// ---------------------------------------------------------------------------
// WIT interface: media-recorder
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaRecorderHandle = bigint;

/** Handle table for MediaRecorder instances */
const _mediaRecorderhandles = new Map<bigint, MediaRecorder>();
let _nextMediaRecorder = 1n;

/** Lookup a MediaRecorder by handle, throwing if not found. */
function lookupMediaRecorder(handle: bigint): MediaRecorder {
  const obj = _mediaRecorderhandles.get(handle);
  if (!obj) {
    throw new Error(`MediaRecorder handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaRecorder by handle. */
function lookupOptionMediaRecorder(handle: bigint | undefined): MediaRecorder | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaRecorderhandles.get(handle) ?? null;
}
/**
 * `get-stream()` operation.
 */
export function MediaRecorderGetStream(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  const _callResult = obj.stream;
  const handle = _nextMediaStream++;
  _mediaStreamHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-mime-type()` operation.
 */
export function getMimeType(self: bigint): string {
  const obj = lookupMediaRecorder(self);
  return obj.mimeType;
}

/**
 * `get-state()` operation.
 */
export function MediaRecorderGetState(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  const value = obj.state;
  switch (value) {
    case 'inactive': return 0n;
    case 'recording': return 1n;
    case 'paused': return 2n;
    default: return 0n;
  }
}

/**
 * `get-onstart()` operation.
 */
export function MediaRecorderGetOnstart(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).onstart;
}

/**
 * `set-onstart()` operation.
 */
export function MediaRecorderSetOnstart(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).onstart = value as any;
}

/**
 * `get-onstop()` operation.
 */
export function getOnstop(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).onstop;
}

/**
 * `set-onstop()` operation.
 */
export function setOnstop(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).onstop = value as any;
}

/**
 * `get-ondataavailable()` operation.
 */
export function getOndataavailable(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).ondataavailable;
}

/**
 * `set-ondataavailable()` operation.
 */
export function setOndataavailable(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).ondataavailable = value as any;
}

/**
 * `get-onpause()` operation.
 */
export function MediaRecorderGetOnpause(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).onpause;
}

/**
 * `set-onpause()` operation.
 */
export function MediaRecorderSetOnpause(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).onpause = value as any;
}

/**
 * `get-onresume()` operation.
 */
export function MediaRecorderGetOnresume(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).onresume;
}

/**
 * `set-onresume()` operation.
 */
export function MediaRecorderSetOnresume(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).onresume = value as any;
}

/**
 * `get-onerror()` operation.
 */
export function MediaRecorderGetOnerror(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).onerror;
}

/**
 * `set-onerror()` operation.
 */
export function MediaRecorderSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).onerror = value as any;
}

/**
 * `get-video-bits-per-second()` operation.
 */
export function getVideoBitsPerSecond(self: bigint): number {
  const obj = lookupMediaRecorder(self);
  return obj.videoBitsPerSecond;
}

/**
 * `get-audio-bits-per-second()` operation.
 */
export function getAudioBitsPerSecond(self: bigint): number {
  const obj = lookupMediaRecorder(self);
  return obj.audioBitsPerSecond;
}

/**
 * `get-audio-bitrate-mode()` operation.
 */
export function getAudioBitrateMode(self: bigint): bigint {
  const obj = lookupMediaRecorder(self);
  return (obj as any).audioBitrateMode;
}

/**
 * `start()` operation.
 */
export function MediaRecorderStart(self: bigint, timeslice: number | undefined): void {
  const obj = lookupMediaRecorder(self);
  (obj as any).start(Number(timeslice));
}

/**
 * `stop()` operation.
 */
export function MediaRecorderStop(self: bigint): void {
  const obj = lookupMediaRecorder(self);
  obj.stop();
}

/**
 * `pause()` operation.
 */
export function MediaRecorderPause(self: bigint): void {
  const obj = lookupMediaRecorder(self);
  obj.pause();
}

/**
 * `resume()` operation.
 */
export function MediaRecorderResume(self: bigint): void {
  const obj = lookupMediaRecorder(self);
  obj.resume();
}

/**
 * `request-data()` operation.
 */
export function requestData(self: bigint): void {
  const obj = lookupMediaRecorder(self);
  obj.requestData();
}

/**
 * `is-type-supported()` operation.
 */
export function isTypeSupported(type: string): bigint {
  return MediaRecorder.isTypeSupported(type) ? 1n : 0n;
}

// ---------------------------------------------------------------------------
// WIT interface: blob-event
// ---------------------------------------------------------------------------

/** Type alias */
export type BlobEventHandle = bigint;

/** Handle table for BlobEvent instances */
const _blobEventhandles = new Map<bigint, BlobEvent>();
let _nextBlobEvent = 1n;

/** Lookup a BlobEvent by handle, throwing if not found. */
function lookupBlobEvent(handle: bigint): BlobEvent {
  const obj = _blobEventhandles.get(handle);
  if (!obj) {
    throw new Error(`BlobEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional BlobEvent by handle. */
function lookupOptionBlobEvent(handle: bigint | undefined): BlobEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _blobEventhandles.get(handle) ?? null;
}
/**
 * `get-data()` operation.
 */
export function getData(self: bigint): bigint {
  const obj = lookupBlobEvent(self);
  const _callResult = obj.data;
  const handle = _nextBlob++;
  _blobHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-timecode()` operation.
 */
export function getTimecode(self: bigint): bigint {
  const obj = lookupBlobEvent(self);
  return BigInt(obj.timecode);
}

// ---------------------------------------------------------------------------
// WIT interface: capture-controller
// ---------------------------------------------------------------------------

/** Type alias */
export type CaptureControllerHandle = bigint;

/** Handle table for CaptureController instances */
const _captureControllerhandles = new Map<bigint, CaptureController>();
let _nextCaptureController = 1n;

/** Lookup a CaptureController by handle, throwing if not found. */
function lookupCaptureController(handle: bigint): CaptureController {
  const obj = _captureControllerhandles.get(handle);
  if (!obj) {
    throw new Error(`CaptureController handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional CaptureController by handle. */
function lookupOptionCaptureController(handle: bigint | undefined): CaptureController | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _captureControllerhandles.get(handle) ?? null;
}
/**
 * `set-focus-behavior()` operation.
 */
export function setFocusBehavior(self: bigint, focusBehavior: bigint): void {
  const obj = lookupCaptureController(self);
  obj.focusBehavior = focusBehavior;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionHandle = bigint;

/** Handle table for SpeechRecognition instances */
const _speechRecognitionhandles = new Map<bigint, SpeechRecognition>();
let _nextSpeechRecognition = 1n;

/** Lookup a SpeechRecognition by handle, throwing if not found. */
function lookupSpeechRecognition(handle: bigint): SpeechRecognition {
  const obj = _speechRecognitionhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognition handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognition by handle. */
function lookupOptionSpeechRecognition(handle: bigint | undefined): SpeechRecognition | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionhandles.get(handle) ?? null;
}
/**
 * `get-grammars()` operation.
 */
export function getGrammars(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.grammars;
}

/**
 * `set-grammars()` operation.
 */
export function setGrammars(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.grammars = value;
}

/**
 * `get-lang()` operation.
 */
export function SpeechRecognitionGetLang(self: bigint): string {
  const obj = lookupSpeechRecognition(self);
  return obj.lang;
}

/**
 * `set-lang()` operation.
 */
export function SpeechRecognitionSetLang(self: bigint, value: string): void {
  const obj = lookupSpeechRecognition(self);
  obj.lang = value;
}

/**
 * `get-continuous()` operation.
 */
export function getContinuous(self: bigint): boolean {
  const obj = lookupSpeechRecognition(self);
  return obj.continuous;
}

/**
 * `set-continuous()` operation.
 */
export function setContinuous(self: bigint, value: boolean): void {
  const obj = lookupSpeechRecognition(self);
  obj.continuous = value;
}

/**
 * `get-interim-results()` operation.
 */
export function getInterimResults(self: bigint): boolean {
  const obj = lookupSpeechRecognition(self);
  return obj.interimResults;
}

/**
 * `set-interim-results()` operation.
 */
export function setInterimResults(self: bigint, value: boolean): void {
  const obj = lookupSpeechRecognition(self);
  obj.interimResults = value;
}

/**
 * `get-max-alternatives()` operation.
 */
export function getMaxAlternatives(self: bigint): number {
  const obj = lookupSpeechRecognition(self);
  return obj.maxAlternatives;
}

/**
 * `set-max-alternatives()` operation.
 */
export function setMaxAlternatives(self: bigint, value: number): void {
  const obj = lookupSpeechRecognition(self);
  obj.maxAlternatives = value;
}

/**
 * `get-process-locally()` operation.
 */
export function getProcessLocally(self: bigint): boolean {
  const obj = lookupSpeechRecognition(self);
  return obj.processLocally;
}

/**
 * `set-process-locally()` operation.
 */
export function setProcessLocally(self: bigint, value: boolean): void {
  const obj = lookupSpeechRecognition(self);
  obj.processLocally = value;
}

/**
 * `get-phrases()` operation.
 */
export function getPhrases(self: bigint): (bigint)[] {
  const obj = lookupSpeechRecognition(self);
  return obj.phrases;
}

/**
 * `set-phrases()` operation.
 */
export function setPhrases(self: bigint, value: (bigint)[]): void {
  const obj = lookupSpeechRecognition(self);
  obj.phrases = value;
}

/**
 * `start()` operation.
 */
export function SpeechRecognitionStart(self: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.start();
}

/**
 * `stop()` operation.
 */
export function SpeechRecognitionStop(self: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.stop();
}

/**
 * `abort()` operation.
 */
export function abort(self: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.abort();
}

/**
 * `available()` operation.
 */
export function available(options: bigint): bigint {
  return (globalThis as any).SpeechRecognition.available(options);
}

/**
 * `install()` operation.
 */
export function install(options: bigint): bigint {
  return (globalThis as any).SpeechRecognition.install(options);
}

/**
 * `get-onaudiostart()` operation.
 */
export function getOnaudiostart(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onaudiostart;
}

/**
 * `set-onaudiostart()` operation.
 */
export function setOnaudiostart(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onaudiostart = value;
}

/**
 * `get-onsoundstart()` operation.
 */
export function getOnsoundstart(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onsoundstart;
}

/**
 * `set-onsoundstart()` operation.
 */
export function setOnsoundstart(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onsoundstart = value;
}

/**
 * `get-onspeechstart()` operation.
 */
export function getOnspeechstart(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onspeechstart;
}

/**
 * `set-onspeechstart()` operation.
 */
export function setOnspeechstart(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onspeechstart = value;
}

/**
 * `get-onspeechend()` operation.
 */
export function getOnspeechend(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onspeechend;
}

/**
 * `set-onspeechend()` operation.
 */
export function setOnspeechend(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onspeechend = value;
}

/**
 * `get-onsoundend()` operation.
 */
export function getOnsoundend(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onsoundend;
}

/**
 * `set-onsoundend()` operation.
 */
export function setOnsoundend(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onsoundend = value;
}

/**
 * `get-onaudioend()` operation.
 */
export function getOnaudioend(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onaudioend;
}

/**
 * `set-onaudioend()` operation.
 */
export function setOnaudioend(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onaudioend = value;
}

/**
 * `get-onresult()` operation.
 */
export function getOnresult(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onresult;
}

/**
 * `set-onresult()` operation.
 */
export function setOnresult(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onresult = value;
}

/**
 * `get-onnomatch()` operation.
 */
export function getOnnomatch(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onnomatch;
}

/**
 * `set-onnomatch()` operation.
 */
export function setOnnomatch(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onnomatch = value;
}

/**
 * `get-onerror()` operation.
 */
export function SpeechRecognitionGetOnerror(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onerror;
}

/**
 * `set-onerror()` operation.
 */
export function SpeechRecognitionSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onerror = value;
}

/**
 * `get-onstart()` operation.
 */
export function SpeechRecognitionGetOnstart(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onstart;
}

/**
 * `set-onstart()` operation.
 */
export function SpeechRecognitionSetOnstart(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onstart = value;
}

/**
 * `get-onend()` operation.
 */
export function SpeechRecognitionGetOnend(self: bigint): bigint {
  const obj = lookupSpeechRecognition(self);
  return obj.onend;
}

/**
 * `set-onend()` operation.
 */
export function SpeechRecognitionSetOnend(self: bigint, value: bigint): void {
  const obj = lookupSpeechRecognition(self);
  obj.onend = value;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-error-event
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionErrorEventHandle = bigint;

/** Handle table for SpeechRecognitionErrorEvent instances */
const _speechRecognitionErrorEventhandles = new Map<bigint, SpeechRecognitionErrorEvent>();
let _nextSpeechRecognitionErrorEvent = 1n;

/** Lookup a SpeechRecognitionErrorEvent by handle, throwing if not found. */
function lookupSpeechRecognitionErrorEvent(handle: bigint): SpeechRecognitionErrorEvent {
  const obj = _speechRecognitionErrorEventhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionErrorEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionErrorEvent by handle. */
function lookupOptionSpeechRecognitionErrorEvent(handle: bigint | undefined): SpeechRecognitionErrorEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionErrorEventhandles.get(handle) ?? null;
}
/**
 * `get-error()` operation.
 */
export function SpeechRecognitionErrorEventGetError(self: bigint): bigint {
  const obj = lookupSpeechRecognitionErrorEvent(self);
  return (obj as any).error;
}

/**
 * `get-message()` operation.
 */
export function getMessage(self: bigint): string {
  const obj = lookupSpeechRecognitionErrorEvent(self);
  return (obj as any).message;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-alternative
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionAlternativeHandle = bigint;

/** Handle table for SpeechRecognitionAlternative instances */
const _speechRecognitionAlternativehandles = new Map<bigint, SpeechRecognitionAlternative>();
let _nextSpeechRecognitionAlternative = 1n;

/** Lookup a SpeechRecognitionAlternative by handle, throwing if not found. */
function lookupSpeechRecognitionAlternative(handle: bigint): SpeechRecognitionAlternative {
  const obj = _speechRecognitionAlternativehandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionAlternative handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionAlternative by handle. */
function lookupOptionSpeechRecognitionAlternative(handle: bigint | undefined): SpeechRecognitionAlternative | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionAlternativehandles.get(handle) ?? null;
}
/**
 * `get-transcript()` operation.
 */
export function getTranscript(self: bigint): bigint {
  const obj = lookupSpeechRecognitionAlternative(self);
  const value = obj.transcript;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-confidence()` operation.
 */
export function getConfidence(self: bigint): bigint {
  const obj = lookupSpeechRecognitionAlternative(self);
  return BigInt(obj.confidence);
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-result
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionResultHandle = bigint;

/** Handle table for SpeechRecognitionResult instances */
const _speechRecognitionResulthandles = new Map<bigint, SpeechRecognitionResult>();
let _nextSpeechRecognitionResult = 1n;

/** Lookup a SpeechRecognitionResult by handle, throwing if not found. */
function lookupSpeechRecognitionResult(handle: bigint): SpeechRecognitionResult {
  const obj = _speechRecognitionResulthandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionResult handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionResult by handle. */
function lookupOptionSpeechRecognitionResult(handle: bigint | undefined): SpeechRecognitionResult | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionResulthandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SpeechRecognitionResultGetLength(self: bigint): bigint {
  const obj = lookupSpeechRecognitionResult(self);
  return BigInt(obj.length);
}

/**
 * `item()` operation.
 */
export function SpeechRecognitionResultItem(self: bigint, index: number): bigint {
  const obj = lookupSpeechRecognitionResult(self);
  const _callResult = obj.item(Number(index));
  const handle = _nextSpeechRecognitionAlternative++;
  _speechRecognitionAlternativehandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-is-final()` operation.
 */
export function getIsFinal(self: bigint): bigint {
  const obj = lookupSpeechRecognitionResult(self);
  return obj.isFinal ? 1n : 0n;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-result-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionResultListHandle = bigint;

/** Handle table for SpeechRecognitionResultList instances */
const _speechRecognitionResultListhandles = new Map<bigint, SpeechRecognitionResultList>();
let _nextSpeechRecognitionResultList = 1n;

/** Lookup a SpeechRecognitionResultList by handle, throwing if not found. */
function lookupSpeechRecognitionResultList(handle: bigint): SpeechRecognitionResultList {
  const obj = _speechRecognitionResultListhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionResultList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionResultList by handle. */
function lookupOptionSpeechRecognitionResultList(handle: bigint | undefined): SpeechRecognitionResultList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionResultListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SpeechRecognitionResultListGetLength(self: bigint): bigint {
  const obj = lookupSpeechRecognitionResultList(self);
  return BigInt(obj.length);
}

/**
 * `item()` operation.
 */
export function SpeechRecognitionResultListItem(self: bigint, index: number): bigint {
  const obj = lookupSpeechRecognitionResultList(self);
  const _callResult = obj.item(Number(index));
  const handle = _nextSpeechRecognitionResult++;
  _speechRecognitionResulthandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-event
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionEventHandle = bigint;

/** Handle table for SpeechRecognitionEvent instances */
const _speechRecognitionEventhandles = new Map<bigint, SpeechRecognitionEvent>();
let _nextSpeechRecognitionEvent = 1n;

/** Lookup a SpeechRecognitionEvent by handle, throwing if not found. */
function lookupSpeechRecognitionEvent(handle: bigint): SpeechRecognitionEvent {
  const obj = _speechRecognitionEventhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionEvent by handle. */
function lookupOptionSpeechRecognitionEvent(handle: bigint | undefined): SpeechRecognitionEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionEventhandles.get(handle) ?? null;
}
/**
 * `get-result-index()` operation.
 */
export function getResultIndex(self: bigint): number {
  const obj = lookupSpeechRecognitionEvent(self);
  return (obj as any).resultIndex;
}

/**
 * `get-results()` operation.
 */
export function getResults(self: bigint): bigint {
  const obj = lookupSpeechRecognitionEvent(self);
  return (obj as any).results;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-grammar
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechGrammarHandle = bigint;

/** Handle table for SpeechGrammar instances */
const _speechGrammarhandles = new Map<bigint, SpeechGrammar>();
let _nextSpeechGrammar = 1n;

/** Lookup a SpeechGrammar by handle, throwing if not found. */
function lookupSpeechGrammar(handle: bigint): SpeechGrammar {
  const obj = _speechGrammarhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechGrammar handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechGrammar by handle. */
function lookupOptionSpeechGrammar(handle: bigint | undefined): SpeechGrammar | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechGrammarhandles.get(handle) ?? null;
}
/**
 * `get-src()` operation.
 */
export function getSrc(self: bigint): string {
  const obj = lookupSpeechGrammar(self);
  return obj.src;
}

/**
 * `set-src()` operation.
 */
export function setSrc(self: bigint, value: string): void {
  const obj = lookupSpeechGrammar(self);
  obj.src = value;
}

/**
 * `get-weight()` operation.
 */
export function getWeight(self: bigint): number {
  const obj = lookupSpeechGrammar(self);
  return obj.weight;
}

/**
 * `set-weight()` operation.
 */
export function setWeight(self: bigint, value: number): void {
  const obj = lookupSpeechGrammar(self);
  obj.weight = value;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-grammar-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechGrammarListHandle = bigint;

/** Handle table for SpeechGrammarList instances */
const _speechGrammarListhandles = new Map<bigint, SpeechGrammarList>();
let _nextSpeechGrammarList = 1n;

/** Lookup a SpeechGrammarList by handle, throwing if not found. */
function lookupSpeechGrammarList(handle: bigint): SpeechGrammarList {
  const obj = _speechGrammarListhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechGrammarList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechGrammarList by handle. */
function lookupOptionSpeechGrammarList(handle: bigint | undefined): SpeechGrammarList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechGrammarListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SpeechGrammarListGetLength(self: bigint): number {
  const obj = lookupSpeechGrammarList(self);
  return obj.length;
}

/**
 * `item()` operation.
 */
export function SpeechGrammarListItem(self: bigint, index: number): bigint {
  const obj = lookupSpeechGrammarList(self);
  return obj.item(index);
}

/**
 * `add-from-uri()` operation.
 */
export function addFromUri(self: bigint, src: string, weight: number | undefined): void {
  const obj = lookupSpeechGrammarList(self);
  obj.addFromUri(src, weight);
}

/**
 * `add-from-string()` operation.
 */
export function addFromString(self: bigint, string: string, weight: number | undefined): void {
  const obj = lookupSpeechGrammarList(self);
  obj.addFromString(string, weight);
}

// ---------------------------------------------------------------------------
// WIT interface: speech-recognition-phrase
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechRecognitionPhraseHandle = bigint;

/** Handle table for SpeechRecognitionPhrase instances */
const _speechRecognitionPhrasehandles = new Map<bigint, SpeechRecognitionPhrase>();
let _nextSpeechRecognitionPhrase = 1n;

/** Lookup a SpeechRecognitionPhrase by handle, throwing if not found. */
function lookupSpeechRecognitionPhrase(handle: bigint): SpeechRecognitionPhrase {
  const obj = _speechRecognitionPhrasehandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechRecognitionPhrase handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechRecognitionPhrase by handle. */
function lookupOptionSpeechRecognitionPhrase(handle: bigint | undefined): SpeechRecognitionPhrase | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechRecognitionPhrasehandles.get(handle) ?? null;
}
/**
 * `get-phrase()` operation.
 */
export function getPhrase(self: bigint): string {
  const obj = lookupSpeechRecognitionPhrase(self);
  return obj.phrase;
}

/**
 * `get-boost()` operation.
 */
export function getBoost(self: bigint): number {
  const obj = lookupSpeechRecognitionPhrase(self);
  return obj.boost;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-synthesis
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechSynthesisHandle = bigint;

/** Handle table for SpeechSynthesis instances */
const _speechSynthesishandles = new Map<bigint, SpeechSynthesis>();
let _nextSpeechSynthesis = 1n;

/** Lookup a SpeechSynthesis by handle, throwing if not found. */
function lookupSpeechSynthesis(handle: bigint): SpeechSynthesis {
  const obj = _speechSynthesishandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechSynthesis handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechSynthesis by handle. */
function lookupOptionSpeechSynthesis(handle: bigint | undefined): SpeechSynthesis | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesishandles.get(handle) ?? null;
}
/**
 * `get-pending()` operation.
 */
export function getPending(self: bigint): bigint {
  const obj = lookupSpeechSynthesis(self);
  return obj.pending ? 1n : 0n;
}

/**
 * `get-speaking()` operation.
 */
export function getSpeaking(self: bigint): boolean {
  const obj = lookupSpeechSynthesis(self);
  return obj.speaking;
}

/**
 * `get-paused()` operation.
 */
export function getPaused(self: bigint): boolean {
  const obj = lookupSpeechSynthesis(self);
  return obj.paused;
}

/**
 * `get-onvoiceschanged()` operation.
 */
export function getOnvoiceschanged(self: bigint): bigint {
  const obj = lookupSpeechSynthesis(self);
  return (obj as any).onvoiceschanged;
}

/**
 * `set-onvoiceschanged()` operation.
 */
export function setOnvoiceschanged(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesis(self);
  (obj as any).onvoiceschanged = value as any;
}

/**
 * `speak()` operation.
 */
export function speak(self: bigint, utterance: bigint): void {
  const obj = lookupSpeechSynthesis(self);
  obj.speak(lookupSpeechSynthesisUtterance(utterance));
}

/**
 * `cancel()` operation.
 */
export function cancel(self: bigint): void {
  const obj = lookupSpeechSynthesis(self);
  obj.cancel();
}

/**
 * `pause()` operation.
 */
export function SpeechSynthesisPause(self: bigint): void {
  const obj = lookupSpeechSynthesis(self);
  obj.pause();
}

/**
 * `resume()` operation.
 */
export function SpeechSynthesisResume(self: bigint): void {
  const obj = lookupSpeechSynthesis(self);
  obj.resume();
}

/**
 * `get-voices()` operation.
 */
export function getVoices(self: bigint): bigint {
  const obj = lookupSpeechSynthesis(self);
  const _callResult = (obj as any).getVoices();
  const handle = _nextSpeechSynthesisVoiceList++;
  _speechSynthesisVoiceListhandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-synthesis-utterance
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechSynthesisUtteranceHandle = bigint;

/**
 * `get-text()` operation.
 */
export function getText(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  const value = obj.text;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `set-text()` operation.
 */
export function setText(self: bigint, value: string): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.text = value as any;
}

/**
 * `get-lang()` operation.
 */
export function SpeechSynthesisUtteranceGetLang(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  const value = obj.lang;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `set-lang()` operation.
 */
export function SpeechSynthesisUtteranceSetLang(self: bigint, value: string): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.lang = value as any;
}

/**
 * `get-voice()` operation.
 */
export function getVoice(self: bigint): bigint | undefined {
  const obj = lookupSpeechSynthesisUtterance(self);
  const _callResult = obj.voice;
  if (_callResult === null) return undefined;
  const handle = _nextSpeechSynthesisVoice++;
  _speechSynthesisVoiceHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-voice()` operation.
 */
export function setVoice(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.voice = lookupSpeechSynthesisVoice(value);
}

/**
 * `get-volume()` operation.
 */
export function getVolume(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return BigInt(obj.volume);
}

/**
 * `set-volume()` operation.
 */
export function setVolume(self: bigint, value: number): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.volume = value;
}

/**
 * `get-rate()` operation.
 */
export function getRate(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return BigInt(obj.rate);
}

/**
 * `set-rate()` operation.
 */
export function setRate(self: bigint, value: number): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.rate = Number(value);
}

/**
 * `get-pitch()` operation.
 */
export function getPitch(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return BigInt(obj.pitch);
}

/**
 * `set-pitch()` operation.
 */
export function setPitch(self: bigint, value: number): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  obj.pitch = value;
}

/**
 * `get-onstart()` operation.
 */
export function SpeechSynthesisUtteranceGetOnstart(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onstart;
}

/**
 * `set-onstart()` operation.
 */
export function SpeechSynthesisUtteranceSetOnstart(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onstart = value as any;
}

/**
 * `get-onend()` operation.
 */
export function SpeechSynthesisUtteranceGetOnend(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onend;
}

/**
 * `set-onend()` operation.
 */
export function SpeechSynthesisUtteranceSetOnend(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onend = value as any;
}

/**
 * `get-onerror()` operation.
 */
export function SpeechSynthesisUtteranceGetOnerror(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onerror;
}

/**
 * `set-onerror()` operation.
 */
export function SpeechSynthesisUtteranceSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onerror = value as any;
}

/**
 * `get-onpause()` operation.
 */
export function SpeechSynthesisUtteranceGetOnpause(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onpause;
}

/**
 * `set-onpause()` operation.
 */
export function SpeechSynthesisUtteranceSetOnpause(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onpause = value as any;
}

/**
 * `get-onresume()` operation.
 */
export function SpeechSynthesisUtteranceGetOnresume(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onresume;
}

/**
 * `set-onresume()` operation.
 */
export function SpeechSynthesisUtteranceSetOnresume(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onresume = value as any;
}

/**
 * `get-onmark()` operation.
 */
export function getOnmark(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onmark;
}

/**
 * `set-onmark()` operation.
 */
export function setOnmark(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onmark = value as any;
}

/**
 * `get-onboundary()` operation.
 */
export function getOnboundary(self: bigint): bigint {
  const obj = lookupSpeechSynthesisUtterance(self);
  return (obj as any).onboundary;
}

/**
 * `set-onboundary()` operation.
 */
export function setOnboundary(self: bigint, value: bigint): void {
  const obj = lookupSpeechSynthesisUtterance(self);
  (obj as any).onboundary = value as any;
}

// ---------------------------------------------------------------------------
// WIT interface: speech-synthesis-event
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechSynthesisEventHandle = bigint;

/** Handle table for SpeechSynthesisEvent instances */
const _speechSynthesisEventhandles = new Map<bigint, SpeechSynthesisEvent>();
let _nextSpeechSynthesisEvent = 1n;

/** Lookup a SpeechSynthesisEvent by handle, throwing if not found. */
function lookupSpeechSynthesisEvent(handle: bigint): SpeechSynthesisEvent {
  const obj = _speechSynthesisEventhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechSynthesisEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechSynthesisEvent by handle. */
function lookupOptionSpeechSynthesisEvent(handle: bigint | undefined): SpeechSynthesisEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesisEventhandles.get(handle) ?? null;
}
/**
 * `get-utterance()` operation.
 */
export function getUtterance(self: bigint): bigint {
  const obj = lookupSpeechSynthesisEvent(self);
  const _callResult = obj.utterance;
  const handle = _nextSpeechSynthesisUtterance++;
  _speechSynthesisUtteranceHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-char-index()` operation.
 */
export function getCharIndex(self: bigint): number {
  const obj = lookupSpeechSynthesisEvent(self);
  return obj.charIndex;
}

/**
 * `get-char-length()` operation.
 */
export function getCharLength(self: bigint): number {
  const obj = lookupSpeechSynthesisEvent(self);
  return obj.charLength;
}

/**
 * `get-elapsed-time()` operation.
 */
export function getElapsedTime(self: bigint): bigint {
  const obj = lookupSpeechSynthesisEvent(self);
  return obj.elapsedTime ? 1n : 0n;
}

/**
 * `get-name()` operation.
 */
export function SpeechSynthesisEventGetName(self: bigint): bigint {
  const obj = lookupSpeechSynthesisEvent(self);
  const value = obj.name;
  switch ((value as any)) {
    default: return 0n;
  }
}

// ---------------------------------------------------------------------------
// WIT interface: speech-synthesis-error-event
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechSynthesisErrorEventHandle = bigint;

/** Handle table for SpeechSynthesisErrorEvent instances */
const _speechSynthesisErrorEventhandles = new Map<bigint, SpeechSynthesisErrorEvent>();
let _nextSpeechSynthesisErrorEvent = 1n;

/** Lookup a SpeechSynthesisErrorEvent by handle, throwing if not found. */
function lookupSpeechSynthesisErrorEvent(handle: bigint): SpeechSynthesisErrorEvent {
  const obj = _speechSynthesisErrorEventhandles.get(handle);
  if (!obj) {
    throw new Error(`SpeechSynthesisErrorEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SpeechSynthesisErrorEvent by handle. */
function lookupOptionSpeechSynthesisErrorEvent(handle: bigint | undefined): SpeechSynthesisErrorEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _speechSynthesisErrorEventhandles.get(handle) ?? null;
}
/**
 * `get-error()` operation.
 */
export function SpeechSynthesisErrorEventGetError(self: bigint): bigint {
  const obj = lookupSpeechSynthesisErrorEvent(self);
  const value = obj.error;
  switch (value) {
    case 'canceled': return 0n;
    case 'interrupted': return 1n;
    case 'audio-busy': return 2n;
    case 'audio-hardware': return 3n;
    case 'network': return 4n;
    case 'synthesis-unavailable': return 5n;
    case 'synthesis-failed': return 6n;
    case 'language-unavailable': return 7n;
    case 'voice-unavailable': return 8n;
    case 'text-too-long': return 9n;
    case 'invalid-argument': return 10n;
    default: return 0n;
  }
}

// ---------------------------------------------------------------------------
// WIT interface: speech-synthesis-voice
// ---------------------------------------------------------------------------

/** Type alias */
export type SpeechSynthesisVoiceHandle = bigint;

/**
 * `get-voice-uri()` operation.
 */
export function getVoiceUri(self: bigint): string {
  const obj = lookupSpeechSynthesisVoice(self);
  return obj.voiceURI;
}

/**
 * `get-name()` operation.
 */
export function SpeechSynthesisVoiceGetName(self: bigint): bigint {
  const obj = lookupSpeechSynthesisVoice(self);
  const value = obj.name;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-lang()` operation.
 */
export function SpeechSynthesisVoiceGetLang(self: bigint): bigint {
  const obj = lookupSpeechSynthesisVoice(self);
  const value = obj.lang;
  switch ((value as any)) {
    case '': return 0n;
    default: return 0n;
  }
}

/**
 * `get-local-service()` operation.
 */
export function getLocalService(self: bigint): bigint {
  const obj = lookupSpeechSynthesisVoice(self);
  return obj.localService ? 1n : 0n;
}

/**
 * `get-default()` operation.
 */
export function getDefault(self: bigint): bigint {
  const obj = lookupSpeechSynthesisVoice(self);
  return obj.default ? 1n : 0n;
}

// ---------------------------------------------------------------------------
// WIT interface: base-audio-context
// ---------------------------------------------------------------------------

/** Type alias */
export type BaseAudioContextHandle = bigint;

/** Handle table for BaseAudioContext instances */
const _baseAudioContexthandles = new Map<bigint, BaseAudioContext>();
let _nextBaseAudioContext = 1n;

/** Register a new BaseAudioContext and return its handle. */
function registerBaseAudioContext(obj: BaseAudioContext): bigint {
  const handle = _nextBaseAudioContext++;
  _baseAudioContexthandles.set(handle, obj);
  return handle;
}
/** Lookup a BaseAudioContext by handle, throwing if not found. */
function lookupBaseAudioContext(handle: bigint): BaseAudioContext {
  const obj = _baseAudioContexthandles.get(handle);
  if (!obj) {
    throw new Error(`BaseAudioContext handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional BaseAudioContext by handle. */
function lookupOptionBaseAudioContext(handle: bigint | undefined): BaseAudioContext | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _baseAudioContexthandles.get(handle) ?? null;
}
/**
 * `get-destination()` operation.
 */
export function getDestination(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.destination;
}

/**
 * `get-sample-rate()` operation.
 */
export function BaseAudioContextGetSampleRate(self: bigint): number {
  const obj = lookupBaseAudioContext(self);
  return obj.sampleRate;
}

/**
 * `get-current-time()` operation.
 */
export function BaseAudioContextGetCurrentTime(self: bigint): number {
  const obj = lookupBaseAudioContext(self);
  return obj.currentTime;
}

/**
 * `get-listener()` operation.
 */
export function getListener(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.listener;
}

/**
 * `get-state()` operation.
 */
export function BaseAudioContextGetState(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.state;
}

/**
 * `get-render-quantum-size()` operation.
 */
export function BaseAudioContextGetRenderQuantumSize(self: bigint): number {
  const obj = lookupBaseAudioContext(self);
  return obj.renderQuantumSize;
}

/**
 * `get-audio-worklet()` operation.
 */
export function getAudioWorklet(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.audioWorklet;
}

/**
 * `get-onstatechange()` operation.
 */
export function getOnstatechange(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.onstatechange;
}

/**
 * `set-onstatechange()` operation.
 */
export function setOnstatechange(self: bigint, value: bigint): void {
  const obj = lookupBaseAudioContext(self);
  obj.onstatechange = value;
}

/**
 * `create-analyser()` operation.
 */
export function createAnalyser(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createAnalyser();
}

/**
 * `create-biquad-filter()` operation.
 */
export function createBiquadFilter(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createBiquadFilter();
}

/**
 * `create-buffer()` operation.
 */
export function createBuffer(self: bigint, numberOfChannels: number, length: number, sampleRate: number): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createBuffer(numberOfChannels, length, sampleRate);
}

/**
 * `create-buffer-source()` operation.
 */
export function createBufferSource(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createBufferSource();
}

/**
 * `create-channel-merger()` operation.
 */
export function createChannelMerger(self: bigint, numberOfInputs: number | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createChannelMerger(numberOfInputs);
}

/**
 * `create-channel-splitter()` operation.
 */
export function createChannelSplitter(self: bigint, numberOfOutputs: number | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createChannelSplitter(numberOfOutputs);
}

/**
 * `create-constant-source()` operation.
 */
export function createConstantSource(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createConstantSource();
}

/**
 * `create-convolver()` operation.
 */
export function createConvolver(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createConvolver();
}

/**
 * `create-delay()` operation.
 */
export function createDelay(self: bigint, maxDelayTime: number | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createDelay(maxDelayTime);
}

/**
 * `create-dynamics-compressor()` operation.
 */
export function createDynamicsCompressor(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createDynamicsCompressor();
}

/**
 * `create-gain()` operation.
 */
export function createGain(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createGain();
}

/**
 * `create-iir-filter()` operation.
 */
export function createIirFilter(self: bigint, feedforward: (number)[], feedback: (number)[]): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createIirFilter(feedforward, feedback);
}

/**
 * `create-oscillator()` operation.
 */
export function createOscillator(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createOscillator();
}

/**
 * `create-panner()` operation.
 */
export function createPanner(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createPanner();
}

/**
 * `create-periodic-wave()` operation.
 */
export function createPeriodicWave(self: bigint, real: (number)[], imag: (number)[], constraints: bigint | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createPeriodicWave(real, imag, constraints);
}

/**
 * `create-script-processor()` operation.
 */
export function createScriptProcessor(self: bigint, bufferSize: number | undefined, numberOfInputChannels: number | undefined, numberOfOutputChannels: number | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createScriptProcessor(bufferSize, numberOfInputChannels, numberOfOutputChannels);
}

/**
 * `create-stereo-panner()` operation.
 */
export function createStereoPanner(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createStereoPanner();
}

/**
 * `create-wave-shaper()` operation.
 */
export function createWaveShaper(self: bigint): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.createWaveShaper();
}

/**
 * `decode-audio-data()` operation.
 */
export function decodeAudioData(self: bigint, audioData: Uint8Array, successCallback: bigint | undefined, errorCallback: bigint | undefined): bigint {
  const obj = lookupBaseAudioContext(self);
  return obj.decodeAudioData(audioData, successCallback, errorCallback);
}

// ---------------------------------------------------------------------------
// WIT interface: audio-context
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioContextHandle = bigint;

/** Handle table for AudioContext instances */
const _audioContexthandles = new Map<bigint, AudioContext>();
let _nextAudioContext = 1n;

/** Register a new AudioContext and return its handle. */
function registerAudioContext(obj: AudioContext): bigint {
  const handle = _nextAudioContext++;
  _audioContexthandles.set(handle, obj);
  return handle;
}
/** Lookup a AudioContext by handle, throwing if not found. */
function lookupAudioContext(handle: bigint): AudioContext {
  const obj = _audioContexthandles.get(handle);
  if (!obj) {
    throw new Error(`AudioContext handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioContext by handle. */
function lookupOptionAudioContext(handle: bigint | undefined): AudioContext | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioContexthandles.get(handle) ?? null;
}
/**
 * `get-base-latency()` operation.
 */
export function getBaseLatency(self: bigint): number {
  const obj = lookupAudioContext(self);
  return obj.baseLatency;
}

/**
 * `get-output-latency()` operation.
 */
export function getOutputLatency(self: bigint): number {
  const obj = lookupAudioContext(self);
  return obj.outputLatency;
}

/**
 * `get-sink-id()` operation.
 */
export function getSinkId(self: bigint): string {
  const obj = lookupAudioContext(self);
  return obj.sinkId;
}

/**
 * `get-onsinkchange()` operation.
 */
export function getOnsinkchange(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.onsinkchange;
}

/**
 * `set-onsinkchange()` operation.
 */
export function setOnsinkchange(self: bigint, value: bigint): void {
  const obj = lookupAudioContext(self);
  obj.onsinkchange = value;
}

/**
 * `get-onerror()` operation.
 */
export function AudioContextGetOnerror(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.onerror;
}

/**
 * `set-onerror()` operation.
 */
export function AudioContextSetOnerror(self: bigint, value: bigint): void {
  const obj = lookupAudioContext(self);
  obj.onerror = value;
}

/**
 * `get-playback-stats()` operation.
 */
export function getPlaybackStats(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.playbackStats;
}

/**
 * `get-output-timestamp()` operation.
 */
export function getOutputTimestamp(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.outputTimestamp;
}

/**
 * `resume()` operation.
 */
export function AudioContextResume(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.resume();
}

/**
 * `suspend()` operation.
 */
export function AudioContextSuspend(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.suspend();
}

/**
 * `close()` operation.
 */
export function close(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.close();
}

/**
 * `set-sink-id()` operation.
 */
export function setSinkId(self: bigint, sinkId: string): bigint {
  const obj = lookupAudioContext(self);
  obj.sinkId = sinkId;
}

/**
 * `create-media-element-source()` operation.
 */
export function createMediaElementSource(self: bigint, mediaElement: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.createMediaElementSource(mediaElement);
}

/**
 * `create-media-stream-source()` operation.
 */
export function createMediaStreamSource(self: bigint, mediaStream: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.createMediaStreamSource(mediaStream);
}

/**
 * `create-media-stream-track-source()` operation.
 */
export function createMediaStreamTrackSource(self: bigint, mediaStreamTrack: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.createMediaStreamTrackSource(mediaStreamTrack);
}

/**
 * `create-media-stream-destination()` operation.
 */
export function createMediaStreamDestination(self: bigint): bigint {
  const obj = lookupAudioContext(self);
  return obj.createMediaStreamDestination();
}

// ---------------------------------------------------------------------------
// WIT interface: audio-sink-info
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioSinkInfoHandle = bigint;

/** Handle table for AudioSinkInfo instances */
const _audioSinkInfohandles = new Map<bigint, AudioSinkInfo>();
let _nextAudioSinkInfo = 1n;

/** Lookup a AudioSinkInfo by handle, throwing if not found. */
function lookupAudioSinkInfo(handle: bigint): AudioSinkInfo {
  const obj = _audioSinkInfohandles.get(handle);
  if (!obj) {
    throw new Error(`AudioSinkInfo handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioSinkInfo by handle. */
function lookupOptionAudioSinkInfo(handle: bigint | undefined): AudioSinkInfo | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioSinkInfohandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function AudioSinkInfoGetType(self: bigint): bigint {
  const obj = lookupAudioSinkInfo(self);
  return obj.getType();
}

// ---------------------------------------------------------------------------
// WIT interface: offline-audio-context
// ---------------------------------------------------------------------------

/** Type alias */
export type OfflineAudioContextHandle = bigint;

/** Handle table for OfflineAudioContext instances */
const _offlineAudioContexthandles = new Map<bigint, OfflineAudioContext>();
let _nextOfflineAudioContext = 1n;

/** Lookup a OfflineAudioContext by handle, throwing if not found. */
function lookupOfflineAudioContext(handle: bigint): OfflineAudioContext {
  const obj = _offlineAudioContexthandles.get(handle);
  if (!obj) {
    throw new Error(`OfflineAudioContext handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional OfflineAudioContext by handle. */
function lookupOptionOfflineAudioContext(handle: bigint | undefined): OfflineAudioContext | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _offlineAudioContexthandles.get(handle) ?? null;
}
/**
 * `start-rendering()` operation.
 */
export function startRendering(self: bigint): bigint {
  const obj = lookupOfflineAudioContext(self);
  return obj.startRendering();
}

/**
 * `resume()` operation.
 */
export function OfflineAudioContextResume(self: bigint): bigint {
  const obj = lookupOfflineAudioContext(self);
  return obj.resume();
}

/**
 * `suspend()` operation.
 */
export function OfflineAudioContextSuspend(self: bigint, suspendTime: number): bigint {
  const obj = lookupOfflineAudioContext(self);
  return obj.suspend(suspendTime);
}

/**
 * `get-length()` operation.
 */
export function OfflineAudioContextGetLength(self: bigint): number {
  const obj = lookupOfflineAudioContext(self);
  return obj.length;
}

/**
 * `get-oncomplete()` operation.
 */
export function getOncomplete(self: bigint): bigint {
  const obj = lookupOfflineAudioContext(self);
  return obj.oncomplete;
}

/**
 * `set-oncomplete()` operation.
 */
export function setOncomplete(self: bigint, value: bigint): void {
  const obj = lookupOfflineAudioContext(self);
  obj.oncomplete = value;
}

// ---------------------------------------------------------------------------
// WIT interface: offline-audio-completion-event
// ---------------------------------------------------------------------------

/** Type alias */
export type OfflineAudioCompletionEventHandle = bigint;

/** Handle table for OfflineAudioCompletionEvent instances */
const _offlineAudioCompletionEventhandles = new Map<bigint, OfflineAudioCompletionEvent>();
let _nextOfflineAudioCompletionEvent = 1n;

/** Lookup a OfflineAudioCompletionEvent by handle, throwing if not found. */
function lookupOfflineAudioCompletionEvent(handle: bigint): OfflineAudioCompletionEvent {
  const obj = _offlineAudioCompletionEventhandles.get(handle);
  if (!obj) {
    throw new Error(`OfflineAudioCompletionEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional OfflineAudioCompletionEvent by handle. */
function lookupOptionOfflineAudioCompletionEvent(handle: bigint | undefined): OfflineAudioCompletionEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _offlineAudioCompletionEventhandles.get(handle) ?? null;
}
/**
 * `get-rendered-buffer()` operation.
 */
export function getRenderedBuffer(self: bigint): bigint {
  const obj = lookupOfflineAudioCompletionEvent(self);
  return obj.renderedBuffer;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-buffer
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioBufferHandle = bigint;

/** Handle table for AudioBuffer instances */
const _audioBufferhandles = new Map<bigint, AudioBuffer>();
let _nextAudioBuffer = 1n;

/** Lookup a AudioBuffer by handle, throwing if not found. */
function lookupAudioBuffer(handle: bigint): AudioBuffer {
  const obj = _audioBufferhandles.get(handle);
  if (!obj) {
    throw new Error(`AudioBuffer handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioBuffer by handle. */
function lookupOptionAudioBuffer(handle: bigint | undefined): AudioBuffer | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioBufferhandles.get(handle) ?? null;
}
/**
 * `get-sample-rate()` operation.
 */
export function AudioBufferGetSampleRate(self: bigint): number {
  const obj = lookupAudioBuffer(self);
  return obj.sampleRate;
}

/**
 * `get-length()` operation.
 */
export function AudioBufferGetLength(self: bigint): number {
  const obj = lookupAudioBuffer(self);
  return obj.length;
}

/**
 * `get-duration()` operation.
 */
export function getDuration(self: bigint): number {
  const obj = lookupAudioBuffer(self);
  return obj.duration;
}

/**
 * `get-number-of-channels()` operation.
 */
export function getNumberOfChannels(self: bigint): number {
  const obj = lookupAudioBuffer(self);
  return obj.numberOfChannels;
}

/**
 * `get-channel-data()` operation.
 */
export function getChannelData(self: bigint, channel: number): (number)[] {
  const obj = lookupAudioBuffer(self);
  return obj.channelData;
}

/**
 * `copy-from-channel()` operation.
 */
export function copyFromChannel(self: bigint, destination: (number)[], channelNumber: number, bufferOffset: number | undefined): void {
  const obj = lookupAudioBuffer(self);
  obj.copyFromChannel(destination, channelNumber, bufferOffset);
}

/**
 * `copy-to-channel()` operation.
 */
export function copyToChannel(self: bigint, source: (number)[], channelNumber: number, bufferOffset: number | undefined): void {
  const obj = lookupAudioBuffer(self);
  obj.copyToChannel(source, channelNumber, bufferOffset);
}

// ---------------------------------------------------------------------------
// WIT interface: audio-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioNodeHandle = bigint;

/** Handle table for AudioNode instances */
const _audioNodehandles = new Map<bigint, AudioNode>();
let _nextAudioNode = 1n;

/** Lookup a AudioNode by handle, throwing if not found. */
function lookupAudioNode(handle: bigint): AudioNode {
  const obj = _audioNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioNode by handle. */
function lookupOptionAudioNode(handle: bigint | undefined): AudioNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioNodehandles.get(handle) ?? null;
}
/**
 * `connect()` operation.
 */
export function connect(self: bigint, destinationNode: bigint, output: number | undefined, input: number | undefined): bigint {
  const obj = lookupAudioNode(self);
  return obj.connect(destinationNode, output, input);
}

/**
 * `disconnect()` operation.
 */
export function disconnect(self: bigint): void {
  const obj = lookupAudioNode(self);
  obj.disconnect();
}

/**
 * `get-context()` operation.
 */
export function getContext(self: bigint): bigint {
  const obj = lookupAudioNode(self);
  return obj.context;
}

/**
 * `get-number-of-inputs()` operation.
 */
export function getNumberOfInputs(self: bigint): number {
  const obj = lookupAudioNode(self);
  return obj.numberOfInputs;
}

/**
 * `get-number-of-outputs()` operation.
 */
export function getNumberOfOutputs(self: bigint): number {
  const obj = lookupAudioNode(self);
  return obj.numberOfOutputs;
}

/**
 * `get-channel-count()` operation.
 */
export function getChannelCount(self: bigint): number {
  const obj = lookupAudioNode(self);
  return obj.channelCount;
}

/**
 * `set-channel-count()` operation.
 */
export function setChannelCount(self: bigint, value: number): void {
  const obj = lookupAudioNode(self);
  obj.channelCount = value;
}

/**
 * `get-channel-count-mode()` operation.
 */
export function getChannelCountMode(self: bigint): bigint {
  const obj = lookupAudioNode(self);
  return obj.channelCountMode;
}

/**
 * `set-channel-count-mode()` operation.
 */
export function setChannelCountMode(self: bigint, value: bigint): void {
  const obj = lookupAudioNode(self);
  obj.channelCountMode = value;
}

/**
 * `get-channel-interpretation()` operation.
 */
export function getChannelInterpretation(self: bigint): bigint {
  const obj = lookupAudioNode(self);
  return obj.channelInterpretation;
}

/**
 * `set-channel-interpretation()` operation.
 */
export function setChannelInterpretation(self: bigint, value: bigint): void {
  const obj = lookupAudioNode(self);
  obj.channelInterpretation = value;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-param
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioParamHandle = bigint;

/** Handle table for AudioParam instances */
const _audioParamhandles = new Map<bigint, AudioParam>();
let _nextAudioParam = 1n;

/** Lookup a AudioParam by handle, throwing if not found. */
function lookupAudioParam(handle: bigint): AudioParam {
  const obj = _audioParamhandles.get(handle);
  if (!obj) {
    throw new Error(`AudioParam handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioParam by handle. */
function lookupOptionAudioParam(handle: bigint | undefined): AudioParam | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioParamhandles.get(handle) ?? null;
}
/**
 * `get-value()` operation.
 */
export function getValue(self: bigint): number {
  const obj = lookupAudioParam(self);
  return obj.value;
}

/**
 * `set-value()` operation.
 */
export function setValue(self: bigint, value: number): void {
  const obj = lookupAudioParam(self);
  obj.value = value;
}

/**
 * `get-automation-rate()` operation.
 */
export function getAutomationRate(self: bigint): bigint {
  const obj = lookupAudioParam(self);
  return obj.automationRate;
}

/**
 * `set-automation-rate()` operation.
 */
export function setAutomationRate(self: bigint, value: bigint): void {
  const obj = lookupAudioParam(self);
  obj.automationRate = value;
}

/**
 * `get-default-value()` operation.
 */
export function getDefaultValue(self: bigint): number {
  const obj = lookupAudioParam(self);
  return obj.defaultValue;
}

/**
 * `get-min-value()` operation.
 */
export function getMinValue(self: bigint): number {
  const obj = lookupAudioParam(self);
  return obj.minValue;
}

/**
 * `get-max-value()` operation.
 */
export function getMaxValue(self: bigint): number {
  const obj = lookupAudioParam(self);
  return obj.maxValue;
}

/**
 * `set-value-at-time()` operation.
 */
export function setValueAtTime(self: bigint, value: number, startTime: number): bigint {
  const obj = lookupAudioParam(self);
  obj.valueAtTime = startTime;
}

/**
 * `linear-ramp-to-value-at-time()` operation.
 */
export function linearRampToValueAtTime(self: bigint, value: number, endTime: number): bigint {
  const obj = lookupAudioParam(self);
  return obj.linearRampToValueAtTime(value, endTime);
}

/**
 * `exponential-ramp-to-value-at-time()` operation.
 */
export function exponentialRampToValueAtTime(self: bigint, value: number, endTime: number): bigint {
  const obj = lookupAudioParam(self);
  return obj.exponentialRampToValueAtTime(value, endTime);
}

/**
 * `set-target-at-time()` operation.
 */
export function setTargetAtTime(self: bigint, target: number, startTime: number, timeConstant: number): bigint {
  const obj = lookupAudioParam(self);
  obj.targetAtTime = timeConstant;
}

/**
 * `set-value-curve-at-time()` operation.
 */
export function setValueCurveAtTime(self: bigint, values: (number)[], startTime: number, duration: number): bigint {
  const obj = lookupAudioParam(self);
  obj.valueCurveAtTime = duration;
}

/**
 * `cancel-scheduled-values()` operation.
 */
export function cancelScheduledValues(self: bigint, cancelTime: number): bigint {
  const obj = lookupAudioParam(self);
  return obj.cancelScheduledValues(cancelTime);
}

/**
 * `cancel-and-hold-at-time()` operation.
 */
export function cancelAndHoldAtTime(self: bigint, cancelTime: number): bigint {
  const obj = lookupAudioParam(self);
  return obj.cancelAndHoldAtTime(cancelTime);
}

// ---------------------------------------------------------------------------
// WIT interface: audio-scheduled-source-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioScheduledSourceNodeHandle = bigint;

/** Handle table for AudioScheduledSourceNode instances */
const _audioScheduledSourceNodehandles = new Map<bigint, AudioScheduledSourceNode>();
let _nextAudioScheduledSourceNode = 1n;

/** Lookup a AudioScheduledSourceNode by handle, throwing if not found. */
function lookupAudioScheduledSourceNode(handle: bigint): AudioScheduledSourceNode {
  const obj = _audioScheduledSourceNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioScheduledSourceNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioScheduledSourceNode by handle. */
function lookupOptionAudioScheduledSourceNode(handle: bigint | undefined): AudioScheduledSourceNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioScheduledSourceNodehandles.get(handle) ?? null;
}
/**
 * `get-onended()` operation.
 */
export function AudioScheduledSourceNodeGetOnended(self: bigint): bigint {
  const obj = lookupAudioScheduledSourceNode(self);
  return obj.onended;
}

/**
 * `set-onended()` operation.
 */
export function AudioScheduledSourceNodeSetOnended(self: bigint, value: bigint): void {
  const obj = lookupAudioScheduledSourceNode(self);
  obj.onended = value;
}

/**
 * `start()` operation.
 */
export function AudioScheduledSourceNodeStart(self: bigint, when: number | undefined): void {
  const obj = lookupAudioScheduledSourceNode(self);
  obj.start(when);
}

/**
 * `stop()` operation.
 */
export function AudioScheduledSourceNodeStop(self: bigint, when: number | undefined): void {
  const obj = lookupAudioScheduledSourceNode(self);
  obj.stop(when);
}

// ---------------------------------------------------------------------------
// WIT interface: analyser-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AnalyserNodeHandle = bigint;

/** Handle table for AnalyserNode instances */
const _analyserNodehandles = new Map<bigint, AnalyserNode>();
let _nextAnalyserNode = 1n;

/** Lookup a AnalyserNode by handle, throwing if not found. */
function lookupAnalyserNode(handle: bigint): AnalyserNode {
  const obj = _analyserNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AnalyserNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AnalyserNode by handle. */
function lookupOptionAnalyserNode(handle: bigint | undefined): AnalyserNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _analyserNodehandles.get(handle) ?? null;
}
/**
 * `get-float-frequency-data()` operation.
 */
export function getFloatFrequencyData(self: bigint, array: (number)[]): void {
  const obj = lookupAnalyserNode(self);
  return obj.floatFrequencyData;
}

/**
 * `get-byte-frequency-data()` operation.
 */
export function getByteFrequencyData(self: bigint, array: Uint8Array): void {
  const obj = lookupAnalyserNode(self);
  return obj.byteFrequencyData;
}

/**
 * `get-float-time-domain-data()` operation.
 */
export function getFloatTimeDomainData(self: bigint, array: (number)[]): void {
  const obj = lookupAnalyserNode(self);
  return obj.floatTimeDomainData;
}

/**
 * `get-byte-time-domain-data()` operation.
 */
export function getByteTimeDomainData(self: bigint, array: Uint8Array): void {
  const obj = lookupAnalyserNode(self);
  return obj.byteTimeDomainData;
}

/**
 * `get-fft-size()` operation.
 */
export function getFftSize(self: bigint): number {
  const obj = lookupAnalyserNode(self);
  return obj.fftSize;
}

/**
 * `set-fft-size()` operation.
 */
export function setFftSize(self: bigint, value: number): void {
  const obj = lookupAnalyserNode(self);
  obj.fftSize = value;
}

/**
 * `get-frequency-bin-count()` operation.
 */
export function getFrequencyBinCount(self: bigint): number {
  const obj = lookupAnalyserNode(self);
  return obj.frequencyBinCount;
}

/**
 * `get-min-decibels()` operation.
 */
export function getMinDecibels(self: bigint): number {
  const obj = lookupAnalyserNode(self);
  return obj.minDecibels;
}

/**
 * `set-min-decibels()` operation.
 */
export function setMinDecibels(self: bigint, value: number): void {
  const obj = lookupAnalyserNode(self);
  obj.minDecibels = value;
}

/**
 * `get-max-decibels()` operation.
 */
export function getMaxDecibels(self: bigint): number {
  const obj = lookupAnalyserNode(self);
  return obj.maxDecibels;
}

/**
 * `set-max-decibels()` operation.
 */
export function setMaxDecibels(self: bigint, value: number): void {
  const obj = lookupAnalyserNode(self);
  obj.maxDecibels = value;
}

/**
 * `get-smoothing-time-constant()` operation.
 */
export function getSmoothingTimeConstant(self: bigint): number {
  const obj = lookupAnalyserNode(self);
  return obj.smoothingTimeConstant;
}

/**
 * `set-smoothing-time-constant()` operation.
 */
export function setSmoothingTimeConstant(self: bigint, value: number): void {
  const obj = lookupAnalyserNode(self);
  obj.smoothingTimeConstant = value;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-buffer-source-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioBufferSourceNodeHandle = bigint;

/** Handle table for AudioBufferSourceNode instances */
const _audioBufferSourceNodehandles = new Map<bigint, AudioBufferSourceNode>();
let _nextAudioBufferSourceNode = 1n;

/** Lookup a AudioBufferSourceNode by handle, throwing if not found. */
function lookupAudioBufferSourceNode(handle: bigint): AudioBufferSourceNode {
  const obj = _audioBufferSourceNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioBufferSourceNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioBufferSourceNode by handle. */
function lookupOptionAudioBufferSourceNode(handle: bigint | undefined): AudioBufferSourceNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioBufferSourceNodehandles.get(handle) ?? null;
}
/**
 * `get-buffer()` operation.
 */
export function AudioBufferSourceNodeGetBuffer(self: bigint): bigint | undefined {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.buffer ?? undefined;
}

/**
 * `set-buffer()` operation.
 */
export function AudioBufferSourceNodeSetBuffer(self: bigint, value: bigint | undefined): void {
  const obj = lookupAudioBufferSourceNode(self);
  obj.buffer = value;
}

/**
 * `get-playback-rate()` operation.
 */
export function getPlaybackRate(self: bigint): bigint {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.playbackRate;
}

/**
 * `get-detune()` operation.
 */
export function AudioBufferSourceNodeGetDetune(self: bigint): bigint {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.detune;
}

/**
 * `get-loop()` operation.
 */
export function getLoop(self: bigint): boolean {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.loop;
}

/**
 * `set-loop()` operation.
 */
export function setLoop(self: bigint, value: boolean): void {
  const obj = lookupAudioBufferSourceNode(self);
  obj.loop = value;
}

/**
 * `get-loop-start()` operation.
 */
export function getLoopStart(self: bigint): number {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.loopStart;
}

/**
 * `set-loop-start()` operation.
 */
export function setLoopStart(self: bigint, value: number): void {
  const obj = lookupAudioBufferSourceNode(self);
  obj.loopStart = value;
}

/**
 * `get-loop-end()` operation.
 */
export function getLoopEnd(self: bigint): number {
  const obj = lookupAudioBufferSourceNode(self);
  return obj.loopEnd;
}

/**
 * `set-loop-end()` operation.
 */
export function setLoopEnd(self: bigint, value: number): void {
  const obj = lookupAudioBufferSourceNode(self);
  obj.loopEnd = value;
}

/**
 * `start()` operation.
 */
export function AudioBufferSourceNodeStart(self: bigint, when: number | undefined, offset: number | undefined, duration: number | undefined): void {
  const obj = lookupAudioBufferSourceNode(self);
  obj.start(when, offset, duration);
}

// ---------------------------------------------------------------------------
// WIT interface: audio-destination-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioDestinationNodeHandle = bigint;

/** Handle table for AudioDestinationNode instances */
const _audioDestinationNodehandles = new Map<bigint, AudioDestinationNode>();
let _nextAudioDestinationNode = 1n;

/** Lookup a AudioDestinationNode by handle, throwing if not found. */
function lookupAudioDestinationNode(handle: bigint): AudioDestinationNode {
  const obj = _audioDestinationNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioDestinationNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioDestinationNode by handle. */
function lookupOptionAudioDestinationNode(handle: bigint | undefined): AudioDestinationNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioDestinationNodehandles.get(handle) ?? null;
}
/**
 * `get-max-channel-count()` operation.
 */
export function getMaxChannelCount(self: bigint): number {
  const obj = lookupAudioDestinationNode(self);
  return obj.maxChannelCount;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-listener
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioListenerHandle = bigint;

/** Handle table for AudioListener instances */
const _audioListenerhandles = new Map<bigint, AudioListener>();
let _nextAudioListener = 1n;

/** Lookup a AudioListener by handle, throwing if not found. */
function lookupAudioListener(handle: bigint): AudioListener {
  const obj = _audioListenerhandles.get(handle);
  if (!obj) {
    throw new Error(`AudioListener handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioListener by handle. */
function lookupOptionAudioListener(handle: bigint | undefined): AudioListener | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioListenerhandles.get(handle) ?? null;
}
/**
 * `get-position-x()` operation.
 */
export function AudioListenerGetPositionX(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.positionX;
}

/**
 * `get-position-y()` operation.
 */
export function AudioListenerGetPositionY(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.positionY;
}

/**
 * `get-position-z()` operation.
 */
export function AudioListenerGetPositionZ(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.positionZ;
}

/**
 * `get-forward-x()` operation.
 */
export function getForwardX(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.forwardX;
}

/**
 * `get-forward-y()` operation.
 */
export function getForwardY(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.forwardY;
}

/**
 * `get-forward-z()` operation.
 */
export function getForwardZ(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.forwardZ;
}

/**
 * `get-up-x()` operation.
 */
export function getUpX(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.upX;
}

/**
 * `get-up-y()` operation.
 */
export function getUpY(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.upY;
}

/**
 * `get-up-z()` operation.
 */
export function getUpZ(self: bigint): bigint {
  const obj = lookupAudioListener(self);
  return obj.upZ;
}

/**
 * `set-position()` operation.
 */
export function AudioListenerSetPosition(self: bigint, x: number, y: number, z: number): void {
  const obj = lookupAudioListener(self);
  obj.position = z;
}

/**
 * `set-orientation()` operation.
 */
export function AudioListenerSetOrientation(self: bigint, x: number, y: number, z: number, xUp: number, yUp: number, zUp: number): void {
  const obj = lookupAudioListener(self);
  obj.orientation = zUp;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-processing-event
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioProcessingEventHandle = bigint;

/** Handle table for AudioProcessingEvent instances */
const _audioProcessingEventhandles = new Map<bigint, AudioProcessingEvent>();
let _nextAudioProcessingEvent = 1n;

/** Lookup a AudioProcessingEvent by handle, throwing if not found. */
function lookupAudioProcessingEvent(handle: bigint): AudioProcessingEvent {
  const obj = _audioProcessingEventhandles.get(handle);
  if (!obj) {
    throw new Error(`AudioProcessingEvent handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioProcessingEvent by handle. */
function lookupOptionAudioProcessingEvent(handle: bigint | undefined): AudioProcessingEvent | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioProcessingEventhandles.get(handle) ?? null;
}
/**
 * `get-playback-time()` operation.
 */
export function getPlaybackTime(self: bigint): number {
  const obj = lookupAudioProcessingEvent(self);
  return obj.playbackTime;
}

/**
 * `get-input-buffer()` operation.
 */
export function getInputBuffer(self: bigint): bigint {
  const obj = lookupAudioProcessingEvent(self);
  return obj.inputBuffer;
}

/**
 * `get-output-buffer()` operation.
 */
export function getOutputBuffer(self: bigint): bigint {
  const obj = lookupAudioProcessingEvent(self);
  return obj.outputBuffer;
}

// ---------------------------------------------------------------------------
// WIT interface: biquad-filter-node
// ---------------------------------------------------------------------------

/** Type alias */
export type BiquadFilterNodeHandle = bigint;

/** Handle table for BiquadFilterNode instances */
const _biquadFilterNodehandles = new Map<bigint, BiquadFilterNode>();
let _nextBiquadFilterNode = 1n;

/** Lookup a BiquadFilterNode by handle, throwing if not found. */
function lookupBiquadFilterNode(handle: bigint): BiquadFilterNode {
  const obj = _biquadFilterNodehandles.get(handle);
  if (!obj) {
    throw new Error(`BiquadFilterNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional BiquadFilterNode by handle. */
function lookupOptionBiquadFilterNode(handle: bigint | undefined): BiquadFilterNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _biquadFilterNodehandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function BiquadFilterNodeGetType(self: bigint): bigint {
  const obj = lookupBiquadFilterNode(self);
  return obj.getType();
}

/**
 * `set-type()` operation.
 */
export function BiquadFilterNodeSetType(self: bigint, value: bigint): void {
  const obj = lookupBiquadFilterNode(self);
  obj.type = value;
}

/**
 * `get-frequency()` operation.
 */
export function BiquadFilterNodeGetFrequency(self: bigint): bigint {
  const obj = lookupBiquadFilterNode(self);
  return obj.frequency;
}

/**
 * `get-detune()` operation.
 */
export function BiquadFilterNodeGetDetune(self: bigint): bigint {
  const obj = lookupBiquadFilterNode(self);
  return obj.detune;
}

/**
 * `get-q()` operation.
 */
export function getQ(self: bigint): bigint {
  const obj = lookupBiquadFilterNode(self);
  return obj.q;
}

/**
 * `get-gain()` operation.
 */
export function BiquadFilterNodeGetGain(self: bigint): bigint {
  const obj = lookupBiquadFilterNode(self);
  return obj.gain;
}

/**
 * `get-frequency-response()` operation.
 */
export function BiquadFilterNodeGetFrequencyResponse(self: bigint, frequencyHz: (number)[], magResponse: (number)[], phaseResponse: (number)[]): void {
  const obj = lookupBiquadFilterNode(self);
  return obj.frequencyResponse;
}

// ---------------------------------------------------------------------------
// WIT interface: constant-source-node
// ---------------------------------------------------------------------------

/** Type alias */
export type ConstantSourceNodeHandle = bigint;

/** Handle table for ConstantSourceNode instances */
const _constantSourceNodehandles = new Map<bigint, ConstantSourceNode>();
let _nextConstantSourceNode = 1n;

/** Lookup a ConstantSourceNode by handle, throwing if not found. */
function lookupConstantSourceNode(handle: bigint): ConstantSourceNode {
  const obj = _constantSourceNodehandles.get(handle);
  if (!obj) {
    throw new Error(`ConstantSourceNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ConstantSourceNode by handle. */
function lookupOptionConstantSourceNode(handle: bigint | undefined): ConstantSourceNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _constantSourceNodehandles.get(handle) ?? null;
}
/**
 * `get-offset()` operation.
 */
export function getOffset(self: bigint): bigint {
  const obj = lookupConstantSourceNode(self);
  return obj.offset;
}

// ---------------------------------------------------------------------------
// WIT interface: convolver-node
// ---------------------------------------------------------------------------

/** Type alias */
export type ConvolverNodeHandle = bigint;

/** Handle table for ConvolverNode instances */
const _convolverNodehandles = new Map<bigint, ConvolverNode>();
let _nextConvolverNode = 1n;

/** Lookup a ConvolverNode by handle, throwing if not found. */
function lookupConvolverNode(handle: bigint): ConvolverNode {
  const obj = _convolverNodehandles.get(handle);
  if (!obj) {
    throw new Error(`ConvolverNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ConvolverNode by handle. */
function lookupOptionConvolverNode(handle: bigint | undefined): ConvolverNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _convolverNodehandles.get(handle) ?? null;
}
/**
 * `get-buffer()` operation.
 */
export function ConvolverNodeGetBuffer(self: bigint): bigint | undefined {
  const obj = lookupConvolverNode(self);
  return obj.buffer ?? undefined;
}

/**
 * `set-buffer()` operation.
 */
export function ConvolverNodeSetBuffer(self: bigint, value: bigint | undefined): void {
  const obj = lookupConvolverNode(self);
  obj.buffer = value;
}

/**
 * `get-normalize()` operation.
 */
export function getNormalize(self: bigint): boolean {
  const obj = lookupConvolverNode(self);
  return obj.normalize;
}

/**
 * `set-normalize()` operation.
 */
export function setNormalize(self: bigint, value: boolean): void {
  const obj = lookupConvolverNode(self);
  obj.normalize = value;
}

// ---------------------------------------------------------------------------
// WIT interface: delay-node
// ---------------------------------------------------------------------------

/** Type alias */
export type DelayNodeHandle = bigint;

/** Handle table for DelayNode instances */
const _delayNodehandles = new Map<bigint, DelayNode>();
let _nextDelayNode = 1n;

/** Lookup a DelayNode by handle, throwing if not found. */
function lookupDelayNode(handle: bigint): DelayNode {
  const obj = _delayNodehandles.get(handle);
  if (!obj) {
    throw new Error(`DelayNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional DelayNode by handle. */
function lookupOptionDelayNode(handle: bigint | undefined): DelayNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _delayNodehandles.get(handle) ?? null;
}
/**
 * `get-delay-time()` operation.
 */
export function getDelayTime(self: bigint): bigint {
  const obj = lookupDelayNode(self);
  return obj.delayTime;
}

// ---------------------------------------------------------------------------
// WIT interface: dynamics-compressor-node
// ---------------------------------------------------------------------------

/** Type alias */
export type DynamicsCompressorNodeHandle = bigint;

/** Handle table for DynamicsCompressorNode instances */
const _dynamicsCompressorNodehandles = new Map<bigint, DynamicsCompressorNode>();
let _nextDynamicsCompressorNode = 1n;

/** Lookup a DynamicsCompressorNode by handle, throwing if not found. */
function lookupDynamicsCompressorNode(handle: bigint): DynamicsCompressorNode {
  const obj = _dynamicsCompressorNodehandles.get(handle);
  if (!obj) {
    throw new Error(`DynamicsCompressorNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional DynamicsCompressorNode by handle. */
function lookupOptionDynamicsCompressorNode(handle: bigint | undefined): DynamicsCompressorNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _dynamicsCompressorNodehandles.get(handle) ?? null;
}
/**
 * `get-threshold()` operation.
 */
export function getThreshold(self: bigint): bigint {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.threshold;
}

/**
 * `get-knee()` operation.
 */
export function getKnee(self: bigint): bigint {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.knee;
}

/**
 * `get-ratio()` operation.
 */
export function getRatio(self: bigint): bigint {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.ratio;
}

/**
 * `get-reduction()` operation.
 */
export function getReduction(self: bigint): number {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.reduction;
}

/**
 * `get-attack()` operation.
 */
export function getAttack(self: bigint): bigint {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.attack;
}

/**
 * `get-release()` operation.
 */
export function getRelease(self: bigint): bigint {
  const obj = lookupDynamicsCompressorNode(self);
  return obj.release;
}

// ---------------------------------------------------------------------------
// WIT interface: gain-node
// ---------------------------------------------------------------------------

/** Type alias */
export type GainNodeHandle = bigint;

/** Handle table for GainNode instances */
const _gainNodehandles = new Map<bigint, GainNode>();
let _nextGainNode = 1n;

/** Lookup a GainNode by handle, throwing if not found. */
function lookupGainNode(handle: bigint): GainNode {
  const obj = _gainNodehandles.get(handle);
  if (!obj) {
    throw new Error(`GainNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional GainNode by handle. */
function lookupOptionGainNode(handle: bigint | undefined): GainNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _gainNodehandles.get(handle) ?? null;
}
/**
 * `get-gain()` operation.
 */
export function GainNodeGetGain(self: bigint): bigint {
  const obj = lookupGainNode(self);
  return obj.gain;
}

// ---------------------------------------------------------------------------
// WIT interface: iir-filter-node
// ---------------------------------------------------------------------------

/** Type alias */
export type IirFilterNodeHandle = bigint;

/** Handle table for IirFilterNode instances */
const _iirFilterNodehandles = new Map<bigint, IirFilterNode>();
let _nextIirFilterNode = 1n;

/** Lookup a IirFilterNode by handle, throwing if not found. */
function lookupIirFilterNode(handle: bigint): IirFilterNode {
  const obj = _iirFilterNodehandles.get(handle);
  if (!obj) {
    throw new Error(`IirFilterNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional IirFilterNode by handle. */
function lookupOptionIirFilterNode(handle: bigint | undefined): IirFilterNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _iirFilterNodehandles.get(handle) ?? null;
}
/**
 * `get-frequency-response()` operation.
 */
export function IirFilterNodeGetFrequencyResponse(self: bigint, frequencyHz: (number)[], magResponse: (number)[], phaseResponse: (number)[]): void {
  const obj = lookupIirFilterNode(self);
  return obj.frequencyResponse;
}

// ---------------------------------------------------------------------------
// WIT interface: media-element-audio-source-node
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaElementAudioSourceNodeHandle = bigint;

/** Handle table for MediaElementAudioSourceNode instances */
const _mediaElementAudioSourceNodehandles = new Map<bigint, MediaElementAudioSourceNode>();
let _nextMediaElementAudioSourceNode = 1n;

/** Lookup a MediaElementAudioSourceNode by handle, throwing if not found. */
function lookupMediaElementAudioSourceNode(handle: bigint): MediaElementAudioSourceNode {
  const obj = _mediaElementAudioSourceNodehandles.get(handle);
  if (!obj) {
    throw new Error(`MediaElementAudioSourceNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaElementAudioSourceNode by handle. */
function lookupOptionMediaElementAudioSourceNode(handle: bigint | undefined): MediaElementAudioSourceNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaElementAudioSourceNodehandles.get(handle) ?? null;
}
/**
 * `get-media-element()` operation.
 */
export function getMediaElement(self: bigint): bigint {
  const obj = lookupMediaElementAudioSourceNode(self);
  return obj.mediaElement;
}

// ---------------------------------------------------------------------------
// WIT interface: media-stream-audio-destination-node
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaStreamAudioDestinationNodeHandle = bigint;

/** Handle table for MediaStreamAudioDestinationNode instances */
const _mediaStreamAudioDestinationNodehandles = new Map<bigint, MediaStreamAudioDestinationNode>();
let _nextMediaStreamAudioDestinationNode = 1n;

/** Lookup a MediaStreamAudioDestinationNode by handle, throwing if not found. */
function lookupMediaStreamAudioDestinationNode(handle: bigint): MediaStreamAudioDestinationNode {
  const obj = _mediaStreamAudioDestinationNodehandles.get(handle);
  if (!obj) {
    throw new Error(`MediaStreamAudioDestinationNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaStreamAudioDestinationNode by handle. */
function lookupOptionMediaStreamAudioDestinationNode(handle: bigint | undefined): MediaStreamAudioDestinationNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamAudioDestinationNodehandles.get(handle) ?? null;
}
/**
 * `get-stream()` operation.
 */
export function MediaStreamAudioDestinationNodeGetStream(self: bigint): bigint {
  const obj = lookupMediaStreamAudioDestinationNode(self);
  return obj.stream;
}

// ---------------------------------------------------------------------------
// WIT interface: media-stream-audio-source-node
// ---------------------------------------------------------------------------

/** Type alias */
export type MediaStreamAudioSourceNodeHandle = bigint;

/** Handle table for MediaStreamAudioSourceNode instances */
const _mediaStreamAudioSourceNodehandles = new Map<bigint, MediaStreamAudioSourceNode>();
let _nextMediaStreamAudioSourceNode = 1n;

/** Lookup a MediaStreamAudioSourceNode by handle, throwing if not found. */
function lookupMediaStreamAudioSourceNode(handle: bigint): MediaStreamAudioSourceNode {
  const obj = _mediaStreamAudioSourceNodehandles.get(handle);
  if (!obj) {
    throw new Error(`MediaStreamAudioSourceNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional MediaStreamAudioSourceNode by handle. */
function lookupOptionMediaStreamAudioSourceNode(handle: bigint | undefined): MediaStreamAudioSourceNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _mediaStreamAudioSourceNodehandles.get(handle) ?? null;
}
/**
 * `get-media-stream()` operation.
 */
export function getMediaStream(self: bigint): bigint {
  const obj = lookupMediaStreamAudioSourceNode(self);
  return obj.mediaStream;
}

// ---------------------------------------------------------------------------
// WIT interface: oscillator-node
// ---------------------------------------------------------------------------

/** Type alias */
export type OscillatorNodeHandle = bigint;

/** Handle table for OscillatorNode instances */
const _oscillatorNodehandles = new Map<bigint, OscillatorNode>();
let _nextOscillatorNode = 1n;

/** Lookup a OscillatorNode by handle, throwing if not found. */
function lookupOscillatorNode(handle: bigint): OscillatorNode {
  const obj = _oscillatorNodehandles.get(handle);
  if (!obj) {
    throw new Error(`OscillatorNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional OscillatorNode by handle. */
function lookupOptionOscillatorNode(handle: bigint | undefined): OscillatorNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _oscillatorNodehandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function OscillatorNodeGetType(self: bigint): bigint {
  const obj = lookupOscillatorNode(self);
  return obj.getType();
}

/**
 * `set-type()` operation.
 */
export function OscillatorNodeSetType(self: bigint, value: bigint): void {
  const obj = lookupOscillatorNode(self);
  obj.type = value;
}

/**
 * `get-frequency()` operation.
 */
export function OscillatorNodeGetFrequency(self: bigint): bigint {
  const obj = lookupOscillatorNode(self);
  return obj.frequency;
}

/**
 * `get-detune()` operation.
 */
export function OscillatorNodeGetDetune(self: bigint): bigint {
  const obj = lookupOscillatorNode(self);
  return obj.detune;
}

/**
 * `set-periodic-wave()` operation.
 */
export function setPeriodicWave(self: bigint, periodicWave: bigint): void {
  const obj = lookupOscillatorNode(self);
  obj.periodicWave = periodicWave;
}

// ---------------------------------------------------------------------------
// WIT interface: panner-node
// ---------------------------------------------------------------------------

/** Type alias */
export type PannerNodeHandle = bigint;

/** Handle table for PannerNode instances */
const _pannerNodehandles = new Map<bigint, PannerNode>();
let _nextPannerNode = 1n;

/** Lookup a PannerNode by handle, throwing if not found. */
function lookupPannerNode(handle: bigint): PannerNode {
  const obj = _pannerNodehandles.get(handle);
  if (!obj) {
    throw new Error(`PannerNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional PannerNode by handle. */
function lookupOptionPannerNode(handle: bigint | undefined): PannerNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _pannerNodehandles.get(handle) ?? null;
}
/**
 * `get-panning-model()` operation.
 */
export function getPanningModel(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.panningModel;
}

/**
 * `set-panning-model()` operation.
 */
export function setPanningModel(self: bigint, value: bigint): void {
  const obj = lookupPannerNode(self);
  obj.panningModel = value;
}

/**
 * `get-position-x()` operation.
 */
export function PannerNodeGetPositionX(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.positionX;
}

/**
 * `get-position-y()` operation.
 */
export function PannerNodeGetPositionY(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.positionY;
}

/**
 * `get-position-z()` operation.
 */
export function PannerNodeGetPositionZ(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.positionZ;
}

/**
 * `get-orientation-x()` operation.
 */
export function getOrientationX(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.orientationX;
}

/**
 * `get-orientation-y()` operation.
 */
export function getOrientationY(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.orientationY;
}

/**
 * `get-orientation-z()` operation.
 */
export function getOrientationZ(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.orientationZ;
}

/**
 * `get-distance-model()` operation.
 */
export function getDistanceModel(self: bigint): bigint {
  const obj = lookupPannerNode(self);
  return obj.distanceModel;
}

/**
 * `set-distance-model()` operation.
 */
export function setDistanceModel(self: bigint, value: bigint): void {
  const obj = lookupPannerNode(self);
  obj.distanceModel = value;
}

/**
 * `get-ref-distance()` operation.
 */
export function getRefDistance(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.refDistance;
}

/**
 * `set-ref-distance()` operation.
 */
export function setRefDistance(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.refDistance = value;
}

/**
 * `get-max-distance()` operation.
 */
export function getMaxDistance(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.maxDistance;
}

/**
 * `set-max-distance()` operation.
 */
export function setMaxDistance(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.maxDistance = value;
}

/**
 * `get-rolloff-factor()` operation.
 */
export function getRolloffFactor(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.rolloffFactor;
}

/**
 * `set-rolloff-factor()` operation.
 */
export function setRolloffFactor(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.rolloffFactor = value;
}

/**
 * `get-cone-inner-angle()` operation.
 */
export function getConeInnerAngle(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.coneInnerAngle;
}

/**
 * `set-cone-inner-angle()` operation.
 */
export function setConeInnerAngle(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.coneInnerAngle = value;
}

/**
 * `get-cone-outer-angle()` operation.
 */
export function getConeOuterAngle(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.coneOuterAngle;
}

/**
 * `set-cone-outer-angle()` operation.
 */
export function setConeOuterAngle(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.coneOuterAngle = value;
}

/**
 * `get-cone-outer-gain()` operation.
 */
export function getConeOuterGain(self: bigint): number {
  const obj = lookupPannerNode(self);
  return obj.coneOuterGain;
}

/**
 * `set-cone-outer-gain()` operation.
 */
export function setConeOuterGain(self: bigint, value: number): void {
  const obj = lookupPannerNode(self);
  obj.coneOuterGain = value;
}

/**
 * `set-position()` operation.
 */
export function PannerNodeSetPosition(self: bigint, x: number, y: number, z: number): void {
  const obj = lookupPannerNode(self);
  obj.position = z;
}

/**
 * `set-orientation()` operation.
 */
export function PannerNodeSetOrientation(self: bigint, x: number, y: number, z: number): void {
  const obj = lookupPannerNode(self);
  obj.orientation = z;
}

// ---------------------------------------------------------------------------
// WIT interface: script-processor-node
// ---------------------------------------------------------------------------

/** Type alias */
export type ScriptProcessorNodeHandle = bigint;

/** Handle table for ScriptProcessorNode instances */
const _scriptProcessorNodehandles = new Map<bigint, ScriptProcessorNode>();
let _nextScriptProcessorNode = 1n;

/** Lookup a ScriptProcessorNode by handle, throwing if not found. */
function lookupScriptProcessorNode(handle: bigint): ScriptProcessorNode {
  const obj = _scriptProcessorNodehandles.get(handle);
  if (!obj) {
    throw new Error(`ScriptProcessorNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ScriptProcessorNode by handle. */
function lookupOptionScriptProcessorNode(handle: bigint | undefined): ScriptProcessorNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _scriptProcessorNodehandles.get(handle) ?? null;
}
/**
 * `get-onaudioprocess()` operation.
 */
export function getOnaudioprocess(self: bigint): bigint {
  const obj = lookupScriptProcessorNode(self);
  return obj.onaudioprocess;
}

/**
 * `set-onaudioprocess()` operation.
 */
export function setOnaudioprocess(self: bigint, value: bigint): void {
  const obj = lookupScriptProcessorNode(self);
  obj.onaudioprocess = value;
}

/**
 * `get-buffer-size()` operation.
 */
export function getBufferSize(self: bigint): number {
  const obj = lookupScriptProcessorNode(self);
  return obj.bufferSize;
}

// ---------------------------------------------------------------------------
// WIT interface: stereo-panner-node
// ---------------------------------------------------------------------------

/** Type alias */
export type StereoPannerNodeHandle = bigint;

/** Handle table for StereoPannerNode instances */
const _stereoPannerNodehandles = new Map<bigint, StereoPannerNode>();
let _nextStereoPannerNode = 1n;

/** Lookup a StereoPannerNode by handle, throwing if not found. */
function lookupStereoPannerNode(handle: bigint): StereoPannerNode {
  const obj = _stereoPannerNodehandles.get(handle);
  if (!obj) {
    throw new Error(`StereoPannerNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional StereoPannerNode by handle. */
function lookupOptionStereoPannerNode(handle: bigint | undefined): StereoPannerNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stereoPannerNodehandles.get(handle) ?? null;
}
/**
 * `get-pan()` operation.
 */
export function getPan(self: bigint): bigint {
  const obj = lookupStereoPannerNode(self);
  return obj.pan;
}

// ---------------------------------------------------------------------------
// WIT interface: wave-shaper-node
// ---------------------------------------------------------------------------

/** Type alias */
export type WaveShaperNodeHandle = bigint;

/** Handle table for WaveShaperNode instances */
const _waveShaperNodehandles = new Map<bigint, WaveShaperNode>();
let _nextWaveShaperNode = 1n;

/** Lookup a WaveShaperNode by handle, throwing if not found. */
function lookupWaveShaperNode(handle: bigint): WaveShaperNode {
  const obj = _waveShaperNodehandles.get(handle);
  if (!obj) {
    throw new Error(`WaveShaperNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional WaveShaperNode by handle. */
function lookupOptionWaveShaperNode(handle: bigint | undefined): WaveShaperNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _waveShaperNodehandles.get(handle) ?? null;
}
/**
 * `get-curve()` operation.
 */
export function getCurve(self: bigint): (number)[] | undefined {
  const obj = lookupWaveShaperNode(self);
  return obj.curve ?? undefined;
}

/**
 * `set-curve()` operation.
 */
export function setCurve(self: bigint, value: (number)[] | undefined): void {
  const obj = lookupWaveShaperNode(self);
  obj.curve = value;
}

/**
 * `get-oversample()` operation.
 */
export function getOversample(self: bigint): bigint {
  const obj = lookupWaveShaperNode(self);
  return obj.oversample;
}

/**
 * `set-oversample()` operation.
 */
export function setOversample(self: bigint, value: bigint): void {
  const obj = lookupWaveShaperNode(self);
  obj.oversample = value;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-worklet
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioWorkletHandle = bigint;

/** Handle table for AudioWorklet instances */
const _audioWorklethandles = new Map<bigint, AudioWorklet>();
let _nextAudioWorklet = 1n;

/** Lookup a AudioWorklet by handle, throwing if not found. */
function lookupAudioWorklet(handle: bigint): AudioWorklet {
  const obj = _audioWorklethandles.get(handle);
  if (!obj) {
    throw new Error(`AudioWorklet handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioWorklet by handle. */
function lookupOptionAudioWorklet(handle: bigint | undefined): AudioWorklet | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioWorklethandles.get(handle) ?? null;
}
/**
 * `get-port()` operation.
 */
export function AudioWorkletGetPort(self: bigint): bigint {
  const obj = lookupAudioWorklet(self);
  return obj.port;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-worklet-global-scope
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioWorkletGlobalScopeHandle = bigint;

/** Handle table for AudioWorkletGlobalScope instances */
const _audioWorkletGlobalScopehandles = new Map<bigint, AudioWorkletGlobalScope>();
let _nextAudioWorkletGlobalScope = 1n;

/** Lookup a AudioWorkletGlobalScope by handle, throwing if not found. */
function lookupAudioWorkletGlobalScope(handle: bigint): AudioWorkletGlobalScope {
  const obj = _audioWorkletGlobalScopehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioWorkletGlobalScope handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioWorkletGlobalScope by handle. */
function lookupOptionAudioWorkletGlobalScope(handle: bigint | undefined): AudioWorkletGlobalScope | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioWorkletGlobalScopehandles.get(handle) ?? null;
}
/**
 * `register-processor()` operation.
 */
export function registerProcessor(self: bigint, name: string, processorCtor: bigint): void {
  const obj = lookupAudioWorkletGlobalScope(self);
  obj.registerProcessor(name, processorCtor);
}

/**
 * `get-current-frame()` operation.
 */
export function getCurrentFrame(self: bigint): bigint {
  const obj = lookupAudioWorkletGlobalScope(self);
  return obj.currentFrame;
}

/**
 * `get-current-time()` operation.
 */
export function AudioWorkletGlobalScopeGetCurrentTime(self: bigint): number {
  const obj = lookupAudioWorkletGlobalScope(self);
  return obj.currentTime;
}

/**
 * `get-sample-rate()` operation.
 */
export function AudioWorkletGlobalScopeGetSampleRate(self: bigint): number {
  const obj = lookupAudioWorkletGlobalScope(self);
  return obj.sampleRate;
}

/**
 * `get-render-quantum-size()` operation.
 */
export function AudioWorkletGlobalScopeGetRenderQuantumSize(self: bigint): number {
  const obj = lookupAudioWorkletGlobalScope(self);
  return obj.renderQuantumSize;
}

/**
 * `get-port()` operation.
 */
export function AudioWorkletGlobalScopeGetPort(self: bigint): bigint {
  const obj = lookupAudioWorkletGlobalScope(self);
  return obj.port;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-worklet-node
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioWorkletNodeHandle = bigint;

/** Handle table for AudioWorkletNode instances */
const _audioWorkletNodehandles = new Map<bigint, AudioWorkletNode>();
let _nextAudioWorkletNode = 1n;

/** Lookup a AudioWorkletNode by handle, throwing if not found. */
function lookupAudioWorkletNode(handle: bigint): AudioWorkletNode {
  const obj = _audioWorkletNodehandles.get(handle);
  if (!obj) {
    throw new Error(`AudioWorkletNode handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioWorkletNode by handle. */
function lookupOptionAudioWorkletNode(handle: bigint | undefined): AudioWorkletNode | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioWorkletNodehandles.get(handle) ?? null;
}
/**
 * `get-parameters()` operation.
 */
export function getParameters(self: bigint): bigint {
  const obj = lookupAudioWorkletNode(self);
  return obj.getParameters();
}

/**
 * `get-port()` operation.
 */
export function AudioWorkletNodeGetPort(self: bigint): bigint {
  const obj = lookupAudioWorkletNode(self);
  return obj.port;
}

/**
 * `get-onprocessorerror()` operation.
 */
export function getOnprocessorerror(self: bigint): bigint {
  const obj = lookupAudioWorkletNode(self);
  return obj.onprocessorerror;
}

/**
 * `set-onprocessorerror()` operation.
 */
export function setOnprocessorerror(self: bigint, value: bigint): void {
  const obj = lookupAudioWorkletNode(self);
  obj.onprocessorerror = value;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-worklet-processor
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioWorkletProcessorHandle = bigint;

/** Handle table for AudioWorkletProcessor instances */
const _audioWorkletProcessorhandles = new Map<bigint, AudioWorkletProcessor>();
let _nextAudioWorkletProcessor = 1n;

/** Lookup a AudioWorkletProcessor by handle, throwing if not found. */
function lookupAudioWorkletProcessor(handle: bigint): AudioWorkletProcessor {
  const obj = _audioWorkletProcessorhandles.get(handle);
  if (!obj) {
    throw new Error(`AudioWorkletProcessor handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioWorkletProcessor by handle. */
function lookupOptionAudioWorkletProcessor(handle: bigint | undefined): AudioWorkletProcessor | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioWorkletProcessorhandles.get(handle) ?? null;
}
/**
 * `get-port()` operation.
 */
export function AudioWorkletProcessorGetPort(self: bigint): bigint {
  const obj = lookupAudioWorkletProcessor(self);
  return obj.port;
}

// ---------------------------------------------------------------------------
// WIT interface: audio-playback-stats
// ---------------------------------------------------------------------------

/** Type alias */
export type AudioPlaybackStatsHandle = bigint;

/** Handle table for AudioPlaybackStats instances */
const _audioPlaybackStatshandles = new Map<bigint, AudioPlaybackStats>();
let _nextAudioPlaybackStats = 1n;

/** Lookup a AudioPlaybackStats by handle, throwing if not found. */
function lookupAudioPlaybackStats(handle: bigint): AudioPlaybackStats {
  const obj = _audioPlaybackStatshandles.get(handle);
  if (!obj) {
    throw new Error(`AudioPlaybackStats handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional AudioPlaybackStats by handle. */
function lookupOptionAudioPlaybackStats(handle: bigint | undefined): AudioPlaybackStats | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _audioPlaybackStatshandles.get(handle) ?? null;
}
/**
 * `get-underrun-duration()` operation.
 */
export function getUnderrunDuration(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.underrunDuration;
}

/**
 * `get-underrun-events()` operation.
 */
export function getUnderrunEvents(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.underrunEvents;
}

/**
 * `get-total-duration()` operation.
 */
export function getTotalDuration(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.totalDuration;
}

/**
 * `get-average-latency()` operation.
 */
export function getAverageLatency(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.averageLatency;
}

/**
 * `get-minimum-latency()` operation.
 */
export function getMinimumLatency(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.minimumLatency;
}

/**
 * `get-maximum-latency()` operation.
 */
export function getMaximumLatency(self: bigint): number {
  const obj = lookupAudioPlaybackStats(self);
  return obj.maximumLatency;
}

/**
 * `reset-latency()` operation.
 */
export function resetLatency(self: bigint): void {
  const obj = lookupAudioPlaybackStats(self);
  obj.resetLatency();
}

/**
 * `to-json()` operation.
 */
export function AudioPlaybackStatsToJson(self: bigint): bigint {
  const obj = lookupAudioPlaybackStats(self);
  return obj.toJSON();
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  decodingInfo,
  pollDecodingInfo,
  encodingInfo,
  pollEncodingInfo,
  MediaStreamGetId,
  getAudioTracks,
  getVideoTracks,
  getTracks,
  getTrackById,
  addTrack,
  removeTrack,
  MediaStreamClone,
  getActive,
  getOnaddtrack,
  setOnaddtrack,
  getOnremovetrack,
  setOnremovetrack,
  MediaStreamTrackGetKind,
  MediaStreamTrackGetId,
  MediaStreamTrackGetLabel,
  getEnabled,
  setEnabled,
  getMuted,
  getOnmute,
  setOnmute,
  getOnunmute,
  setOnunmute,
  getReadyState,
  MediaStreamTrackGetOnended,
  MediaStreamTrackSetOnended,
  MediaStreamTrackClone,
  MediaStreamTrackStop,
  MediaStreamTrackGetCapabilities,
  getConstraints,
  getSettings,
  applyConstraints,
  pollApplyConstraints,
  getTrack,
  getConstraint,
  getOndevicechange,
  setOndevicechange,
  enumerateDevices,
  pollEnumerateDevices,
  getSupportedConstraints,
  getUserMedia,
  pollGetUserMedia,
  getDisplayMedia,
  pollGetDisplayMedia,
  getDeviceId,
  MediaDeviceInfoGetKind,
  MediaDeviceInfoGetLabel,
  getGroupId,
  MediaDeviceInfoToJson,
  InputDeviceInfoGetCapabilities,
  getDevices,
  getUserInsertedDevices,
  getMetadata,
  setMetadata,
  getPlaybackState,
  setPlaybackState,
  setActionHandler,
  setPositionState,
  setMicrophoneActive,
  setCameraActive,
  setScreenshareActive,
  MediaMetadataGetTitle,
  setTitle,
  getArtist,
  setArtist,
  getAlbum,
  setAlbum,
  MediaMetadataGetArtwork,
  setArtwork,
  getChapterInfo,
  ChapterInformationGetTitle,
  getStartTime,
  ChapterInformationGetArtwork,
  MediaRecorderGetStream,
  getMimeType,
  MediaRecorderGetState,
  MediaRecorderGetOnstart,
  MediaRecorderSetOnstart,
  getOnstop,
  setOnstop,
  getOndataavailable,
  setOndataavailable,
  MediaRecorderGetOnpause,
  MediaRecorderSetOnpause,
  MediaRecorderGetOnresume,
  MediaRecorderSetOnresume,
  MediaRecorderGetOnerror,
  MediaRecorderSetOnerror,
  getVideoBitsPerSecond,
  getAudioBitsPerSecond,
  getAudioBitrateMode,
  MediaRecorderStart,
  MediaRecorderStop,
  MediaRecorderPause,
  MediaRecorderResume,
  requestData,
  isTypeSupported,
  getData,
  getTimecode,
  setFocusBehavior,
  getGrammars,
  setGrammars,
  SpeechRecognitionGetLang,
  SpeechRecognitionSetLang,
  getContinuous,
  setContinuous,
  getInterimResults,
  setInterimResults,
  getMaxAlternatives,
  setMaxAlternatives,
  getProcessLocally,
  setProcessLocally,
  getPhrases,
  setPhrases,
  SpeechRecognitionStart,
  SpeechRecognitionStop,
  abort,
  available,
  install,
  getOnaudiostart,
  setOnaudiostart,
  getOnsoundstart,
  setOnsoundstart,
  getOnspeechstart,
  setOnspeechstart,
  getOnspeechend,
  setOnspeechend,
  getOnsoundend,
  setOnsoundend,
  getOnaudioend,
  setOnaudioend,
  getOnresult,
  setOnresult,
  getOnnomatch,
  setOnnomatch,
  SpeechRecognitionGetOnerror,
  SpeechRecognitionSetOnerror,
  SpeechRecognitionGetOnstart,
  SpeechRecognitionSetOnstart,
  SpeechRecognitionGetOnend,
  SpeechRecognitionSetOnend,
  SpeechRecognitionErrorEventGetError,
  getMessage,
  getTranscript,
  getConfidence,
  SpeechRecognitionResultGetLength,
  SpeechRecognitionResultItem,
  getIsFinal,
  SpeechRecognitionResultListGetLength,
  SpeechRecognitionResultListItem,
  getResultIndex,
  getResults,
  getSrc,
  setSrc,
  getWeight,
  setWeight,
  SpeechGrammarListGetLength,
  SpeechGrammarListItem,
  addFromUri,
  addFromString,
  getPhrase,
  getBoost,
  getPending,
  getSpeaking,
  getPaused,
  getOnvoiceschanged,
  setOnvoiceschanged,
  speak,
  cancel,
  SpeechSynthesisPause,
  SpeechSynthesisResume,
  getVoices,
  getText,
  setText,
  SpeechSynthesisUtteranceGetLang,
  SpeechSynthesisUtteranceSetLang,
  getVoice,
  setVoice,
  getVolume,
  setVolume,
  getRate,
  setRate,
  getPitch,
  setPitch,
  SpeechSynthesisUtteranceGetOnstart,
  SpeechSynthesisUtteranceSetOnstart,
  SpeechSynthesisUtteranceGetOnend,
  SpeechSynthesisUtteranceSetOnend,
  SpeechSynthesisUtteranceGetOnerror,
  SpeechSynthesisUtteranceSetOnerror,
  SpeechSynthesisUtteranceGetOnpause,
  SpeechSynthesisUtteranceSetOnpause,
  SpeechSynthesisUtteranceGetOnresume,
  SpeechSynthesisUtteranceSetOnresume,
  getOnmark,
  setOnmark,
  getOnboundary,
  setOnboundary,
  getUtterance,
  getCharIndex,
  getCharLength,
  getElapsedTime,
  SpeechSynthesisEventGetName,
  SpeechSynthesisErrorEventGetError,
  getVoiceUri,
  SpeechSynthesisVoiceGetName,
  SpeechSynthesisVoiceGetLang,
  getLocalService,
  getDefault,
  getDestination,
  BaseAudioContextGetSampleRate,
  BaseAudioContextGetCurrentTime,
  getListener,
  BaseAudioContextGetState,
  BaseAudioContextGetRenderQuantumSize,
  getAudioWorklet,
  getOnstatechange,
  setOnstatechange,
  createAnalyser,
  createBiquadFilter,
  createBuffer,
  createBufferSource,
  createChannelMerger,
  createChannelSplitter,
  createConstantSource,
  createConvolver,
  createDelay,
  createDynamicsCompressor,
  createGain,
  createIirFilter,
  createOscillator,
  createPanner,
  createPeriodicWave,
  createScriptProcessor,
  createStereoPanner,
  createWaveShaper,
  decodeAudioData,
  getBaseLatency,
  getOutputLatency,
  getSinkId,
  getOnsinkchange,
  setOnsinkchange,
  AudioContextGetOnerror,
  AudioContextSetOnerror,
  getPlaybackStats,
  getOutputTimestamp,
  AudioContextResume,
  AudioContextSuspend,
  close,
  setSinkId,
  createMediaElementSource,
  createMediaStreamSource,
  createMediaStreamTrackSource,
  createMediaStreamDestination,
  AudioSinkInfoGetType,
  startRendering,
  OfflineAudioContextResume,
  OfflineAudioContextSuspend,
  OfflineAudioContextGetLength,
  getOncomplete,
  setOncomplete,
  getRenderedBuffer,
  AudioBufferGetSampleRate,
  AudioBufferGetLength,
  getDuration,
  getNumberOfChannels,
  getChannelData,
  copyFromChannel,
  copyToChannel,
  connect,
  disconnect,
  getContext,
  getNumberOfInputs,
  getNumberOfOutputs,
  getChannelCount,
  setChannelCount,
  getChannelCountMode,
  setChannelCountMode,
  getChannelInterpretation,
  setChannelInterpretation,
  getValue,
  setValue,
  getAutomationRate,
  setAutomationRate,
  getDefaultValue,
  getMinValue,
  getMaxValue,
  setValueAtTime,
  linearRampToValueAtTime,
  exponentialRampToValueAtTime,
  setTargetAtTime,
  setValueCurveAtTime,
  cancelScheduledValues,
  cancelAndHoldAtTime,
  AudioScheduledSourceNodeGetOnended,
  AudioScheduledSourceNodeSetOnended,
  AudioScheduledSourceNodeStart,
  AudioScheduledSourceNodeStop,
  getFloatFrequencyData,
  getByteFrequencyData,
  getFloatTimeDomainData,
  getByteTimeDomainData,
  getFftSize,
  setFftSize,
  getFrequencyBinCount,
  getMinDecibels,
  setMinDecibels,
  getMaxDecibels,
  setMaxDecibels,
  getSmoothingTimeConstant,
  setSmoothingTimeConstant,
  AudioBufferSourceNodeGetBuffer,
  AudioBufferSourceNodeSetBuffer,
  getPlaybackRate,
  AudioBufferSourceNodeGetDetune,
  getLoop,
  setLoop,
  getLoopStart,
  setLoopStart,
  getLoopEnd,
  setLoopEnd,
  AudioBufferSourceNodeStart,
  getMaxChannelCount,
  AudioListenerGetPositionX,
  AudioListenerGetPositionY,
  AudioListenerGetPositionZ,
  getForwardX,
  getForwardY,
  getForwardZ,
  getUpX,
  getUpY,
  getUpZ,
  AudioListenerSetPosition,
  AudioListenerSetOrientation,
  getPlaybackTime,
  getInputBuffer,
  getOutputBuffer,
  BiquadFilterNodeGetType,
  BiquadFilterNodeSetType,
  BiquadFilterNodeGetFrequency,
  BiquadFilterNodeGetDetune,
  getQ,
  BiquadFilterNodeGetGain,
  BiquadFilterNodeGetFrequencyResponse,
  getOffset,
  ConvolverNodeGetBuffer,
  ConvolverNodeSetBuffer,
  getNormalize,
  setNormalize,
  getDelayTime,
  getThreshold,
  getKnee,
  getRatio,
  getReduction,
  getAttack,
  getRelease,
  GainNodeGetGain,
  IirFilterNodeGetFrequencyResponse,
  getMediaElement,
  MediaStreamAudioDestinationNodeGetStream,
  getMediaStream,
  OscillatorNodeGetType,
  OscillatorNodeSetType,
  OscillatorNodeGetFrequency,
  OscillatorNodeGetDetune,
  setPeriodicWave,
  getPanningModel,
  setPanningModel,
  PannerNodeGetPositionX,
  PannerNodeGetPositionY,
  PannerNodeGetPositionZ,
  getOrientationX,
  getOrientationY,
  getOrientationZ,
  getDistanceModel,
  setDistanceModel,
  getRefDistance,
  setRefDistance,
  getMaxDistance,
  setMaxDistance,
  getRolloffFactor,
  setRolloffFactor,
  getConeInnerAngle,
  setConeInnerAngle,
  getConeOuterAngle,
  setConeOuterAngle,
  getConeOuterGain,
  setConeOuterGain,
  PannerNodeSetPosition,
  PannerNodeSetOrientation,
  getOnaudioprocess,
  setOnaudioprocess,
  getBufferSize,
  getPan,
  getCurve,
  setCurve,
  getOversample,
  setOversample,
  AudioWorkletGetPort,
  registerProcessor,
  getCurrentFrame,
  AudioWorkletGlobalScopeGetCurrentTime,
  AudioWorkletGlobalScopeGetSampleRate,
  AudioWorkletGlobalScopeGetRenderQuantumSize,
  AudioWorkletGlobalScopeGetPort,
  getParameters,
  AudioWorkletNodeGetPort,
  getOnprocessorerror,
  setOnprocessorerror,
  AudioWorkletProcessorGetPort,
  getUnderrunDuration,
  getUnderrunEvents,
  getTotalDuration,
  getAverageLatency,
  getMinimumLatency,
  getMaximumLatency,
  resetLatency,
  AudioPlaybackStatsToJson
};
