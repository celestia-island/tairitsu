/**
 * crypto glue — implements the `tairitsu-browser:crypto` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-worlds/wit/generated/crypto.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for EventHandler */
export type EventHandler = (this: any, ev: any) => any;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for any values */
const _anyHandles = new Map<bigint, any>();
let _nextAny = 1n;

/** Handle table for string-list values */
const _stringListHandles = new Map<bigint, string[]>();
let _nextStringList = 1n;

/** Handle table for uint8-array values */
const _uint8ArrayHandles = new Map<bigint, Uint8Array>();
let _nextUint8Array = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a any value by handle. */
function lookupAny(handle: bigint): any {
  const obj = _anyHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`any handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional any value by handle. */
function lookupOptionAny(handle: bigint | undefined): any | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _anyHandles.get(handle) ?? null;
}

/** Lookup a string-list value by handle. */
function lookupStringList(handle: bigint): string[] {
  const obj = _stringListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string-list value by handle. */
function lookupOptionStringList(handle: bigint | undefined): string[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringListHandles.get(handle) ?? null;
}

/** Lookup a uint8-array value by handle. */
function lookupUint8Array(handle: bigint): Uint8Array {
  const obj = _uint8ArrayHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`uint8-array handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional uint8-array value by handle. */
function lookupOptionUint8Array(handle: bigint | undefined): Uint8Array | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _uint8ArrayHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: crypto
// ---------------------------------------------------------------------------

/** Type alias */
export type CryptoHandle = bigint;

/** Handle for global singleton Crypto (fixed to 0n). */
const _Crypto_HANDLE = 0n;

/** Get the global Crypto object. */
function getGlobalCrypto(): Crypto {
  return window.crypto;
}

/**
 * `get-subtle()` operation.
 */
export function getSubtle(): bigint {
  const _callResult = window.crypto.subtle;
  const handle = _nextSubtleCrypto++;
  _subtleCryptohandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-random-values()` operation.
 */
export function getRandomValues(array: bigint): bigint {
  const _callResult = window.crypto.getRandomValues(lookupUint8Array(array));
  const handle = _nextUint8Array++;
  _uint8ArrayHandles.set(handle, _callResult);
  return handle;
}

/**
 * `random-uuid()` operation.
 */
export function randomUuid(): string {
  return window.crypto.randomUUID();
}

// ---------------------------------------------------------------------------
// WIT interface: crypto-key
// ---------------------------------------------------------------------------

/** Type alias */
export type CryptoKeyHandle = bigint;

/** Handle table for CryptoKey instances */
const _cryptoKeyhandles = new Map<bigint, CryptoKey>();
let _nextCryptoKey = 1n;

/** Lookup a CryptoKey by handle, throwing if not found. */
function lookupCryptoKey(handle: bigint): CryptoKey {
  const obj = _cryptoKeyhandles.get(handle);
  if (!obj) {
    throw new Error(`CryptoKey handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional CryptoKey by handle. */
function lookupOptionCryptoKey(handle: bigint | undefined): CryptoKey | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _cryptoKeyhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function getType(self: bigint): bigint {
  const obj = lookupCryptoKey(self);
  return (obj as any).getType();
}

/**
 * `get-extractable()` operation.
 */
export function getExtractable(self: bigint): bigint {
  const obj = lookupCryptoKey(self);
  return obj.extractable ? 1n : 0n;
}

/**
 * `get-algorithm()` operation.
 */
export function getAlgorithm(self: bigint): bigint {
  const obj = lookupCryptoKey(self);
  const _callResult = obj.algorithm;
  const handle = _nextAny++;
  _anyHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-usages()` operation.
 */
export function getUsages(self: bigint): bigint {
  const obj = lookupCryptoKey(self);
  const _callResult = obj.usages;
  const handle = _nextStringList++;
  _stringListHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: subtle-crypto
// ---------------------------------------------------------------------------

/** Type alias */
export type SubtleCryptoHandle = bigint;

/** Handle table for SubtleCrypto instances */
const _subtleCryptohandles = new Map<bigint, SubtleCrypto>();
let _nextSubtleCrypto = 1n;

/** Lookup a SubtleCrypto by handle, throwing if not found. */
function lookupSubtleCrypto(handle: bigint): SubtleCrypto {
  const obj = _subtleCryptohandles.get(handle);
  if (!obj) {
    throw new Error(`SubtleCrypto handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SubtleCrypto by handle. */
function lookupOptionSubtleCrypto(handle: bigint | undefined): SubtleCrypto | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _subtleCryptohandles.get(handle) ?? null;
}
/**
 * `encrypt()` operation.
 *
 * Async operation: returns request ID, poll with `pollEncrypt()`
 */
export function encrypt(self: bigint, algorithm: bigint, key: bigint, data: Uint8Array): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.encrypt(algorithm as any, lookupCryptoKey(key), data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `encrypt()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollEncrypt(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `decrypt()` operation.
 *
 * Async operation: returns request ID, poll with `pollDecrypt()`
 */
export function decrypt(self: bigint, algorithm: bigint, key: bigint, data: Uint8Array): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.decrypt(algorithm as any, lookupCryptoKey(key), data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `decrypt()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollDecrypt(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `sign()` operation.
 *
 * Async operation: returns request ID, poll with `pollSign()`
 */
export function sign(self: bigint, algorithm: bigint, key: bigint, data: Uint8Array): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.sign(algorithm as any, lookupCryptoKey(key), data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `sign()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollSign(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `verify()` operation.
 *
 * Async operation: returns request ID, poll with `pollVerify()`
 */
export function verify(self: bigint, algorithm: bigint, key: bigint, signature: Uint8Array, data: Uint8Array): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.verify(algorithm as any, lookupCryptoKey(key), signature as any, data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `verify()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollVerify(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `digest()` operation.
 *
 * Async operation: returns request ID, poll with `pollDigest()`
 */
export function digest(self: bigint, algorithm: bigint, data: Uint8Array): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.digest(algorithm as any, data as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `digest()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollDigest(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `derive-key()` operation.
 *
 * Async operation: returns request ID, poll with `pollDeriveKey()`
 */
export function deriveKey(self: bigint, algorithm: bigint, baseKey: bigint, derivedKeyType: bigint, extractable: boolean, keyUsages: (bigint)[]): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = (obj as any).deriveKey(algorithm as any, lookupOptionCryptoKey(baseKey), derivedKeyType as any, extractable, keyUsages)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `deriveKey()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollDeriveKey(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `derive-bits()` operation.
 *
 * Async operation: returns request ID, poll with `pollDeriveBits()`
 */
export function deriveBits(self: bigint, algorithm: bigint, baseKey: bigint, length: number | undefined): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.deriveBits(algorithm as any, lookupCryptoKey(baseKey), Number(length))
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `deriveBits()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollDeriveBits(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `import-key()` operation.
 *
 * Async operation: returns request ID, poll with `pollImportKey()`
 */
export function importKey(self: bigint, format: bigint, keyData: Uint8Array, algorithm: bigint, extractable: boolean, keyUsages: (bigint)[]): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = (obj as any).importKey(format as any, keyData as any, algorithm as any, extractable, keyUsages)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `importKey()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollImportKey(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `wrap-key()` operation.
 *
 * Async operation: returns request ID, poll with `pollWrapKey()`
 */
export function wrapKey(self: bigint, format: bigint, key: bigint, wrappingKey: bigint, wrapAlgorithm: bigint): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = obj.wrapKey(format as any, lookupCryptoKey(key), lookupCryptoKey(wrappingKey), wrapAlgorithm as any)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `wrapKey()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollWrapKey(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `unwrap-key()` operation.
 *
 * Async operation: returns request ID, poll with `pollUnwrapKey()`
 */
export function unwrapKey(self: bigint, format: bigint, wrappedKey: Uint8Array, unwrappingKey: bigint, unwrapAlgorithm: bigint, unwrappedKeyAlgorithm: bigint, extractable: boolean, keyUsages: (bigint)[]): bigint {
  const requestId = _nextAsyncHandle++;
  const obj = lookupSubtleCrypto(self);
  const promise = (obj as any).unwrapKey(format as any, wrappedKey as any, lookupCryptoKey(unwrappingKey), unwrapAlgorithm, unwrappedKeyAlgorithm, extractable, keyUsages)
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `unwrapKey()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollUnwrapKey(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  getSubtle,
  getRandomValues,
  randomUuid,
  getType,
  getExtractable,
  getAlgorithm,
  getUsages,
  encrypt,
  pollEncrypt,
  decrypt,
  pollDecrypt,
  sign,
  pollSign,
  verify,
  pollVerify,
  digest,
  pollDigest,
  deriveKey,
  pollDeriveKey,
  deriveBits,
  pollDeriveBits,
  importKey,
  pollImportKey,
  wrapKey,
  pollWrapKey,
  unwrapKey,
  pollUnwrapKey
};
