/// Unified browser world — combines all auto-generated domain interfaces.
///
/// Generated by: scripts/generate_browser_wit.py
/// W3C/WHATWG webref: https://github.com/w3c/webref (MIT)
///
/// All browser objects are represented as opaque u64 handles.
/// Regenerate with: just wit-gen
///
/// Status: auto-generated (Phase A) — review before use in production
package tairitsu-browser:full@0.2.0;

/// Event callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when DOM events fire.
interface event-callbacks {
    /// Opaque listener identifier returned by add-event-listener.
    type listener-id = u64;
    /// Opaque event handle for prevent-default/stop-propagation.
    type event-handle = u64;

    /// Mouse event data structure.
    record mouse-event-data {
        client-x: f64,
        client-y: f64,
        offset-x: f64,
        offset-y: f64,
        button: u8,
        buttons: u8,
        ctrl-key: bool,
        shift-key: bool,
        alt-key: bool,
        meta-key: bool,
    }

    /// Keyboard event data structure.
    record keyboard-event-data {
        key: string,
        code: string,
        key-code: u32,
        ctrl-key: bool,
        shift-key: bool,
        alt-key: bool,
        meta-key: bool,
        repeat: bool,
    }

    /// Focus event data structure.
    record focus-event-data {
        related-target: option<u64>,
    }

    /// Input event data structure.
    record input-event-data {
        data: option<string>,
        input-type: string,
    }

    /// Called when a mouse event fires on a registered listener.
    on-mouse-event: func(listener-id: listener-id, event: event-handle, data: mouse-event-data);
    /// Called when a keyboard event fires on a registered listener.
    on-keyboard-event: func(listener-id: listener-id, event: event-handle, data: keyboard-event-data);
    /// Called when a focus event fires on a registered listener.
    on-focus-event: func(listener-id: listener-id, event: event-handle, data: focus-event-data);
    /// Called when an input event fires on a registered listener.
    on-input-event: func(listener-id: listener-id, event: event-handle, data: input-event-data);
    /// Called for any other event type.
    on-generic-event: func(listener-id: listener-id, event: event-handle, event-type: string);
}

/// Timer callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when timers expire.
interface timer-callbacks {
    /// Callback invoked when a timeout expires.
    on-timeout: func(callback-id: u64);
}

/// Animation callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when animation frames are ready.
interface animation-callbacks {
    /// Callback invoked when an animation frame is ready.
    /// The timestamp is the number of milliseconds since the time origin.
    on-frame: func(callback-id: u64, timestamp: f64);
}

/// Resize observer callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when observed elements are resized.
interface resize-observer-callbacks {
    use types.{dom-rect};

    /// Resize observer entry data passed to the callback.
    record resize-entry {
        /// The element being resized.
        target: u64,
        /// The content rectangle of the target element.
        content-rect: dom-rect,
    }

    /// Callback invoked when observed elements are resized.
    on-resize: func(callback-id: u64, entries: list<resize-entry>);
}

/// Mutation observer callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when DOM mutations occur.
interface mutation-observer-callbacks {
    /// Mutation record data passed to the callback.
    record mutation-entry {
        /// The type of mutation ("attributes", "characterData", or "childList").
        mutation-type: string,
        /// The target node for the mutation.
        target: u64,
        /// The added nodes (handle to NodeList).
        added-nodes: option<u64>,
        /// The removed nodes (handle to NodeList).
        removed-nodes: option<u64>,
        /// The previous sibling node.
        previous-sibling: option<u64>,
        /// The next sibling node.
        next-sibling: option<u64>,
        /// The attribute name (for attribute mutations).
        attribute-name: option<string>,
        /// The attribute namespace (for attribute mutations).
        attribute-namespace: option<string>,
        /// The old value (for attribute or characterData mutations).
        old-value: option<string>,
    }

    /// Callback invoked when DOM mutations occur.
    on-mutate: func(callback-id: u64, entries: list<mutation-entry>);
}

/// MediaQueryList callback interface - implemented by the component, called by host.
///
/// The host (browser-glue) calls these functions when media query state changes.
interface media-query-list-callbacks {
    /// Callback invoked when a media query's matches state changes.
    on-change: func(callback-id: u64, matches: bool);
}

/// Scroll callback interface - implemented by the component, called by host.
interface scroll-callbacks {
    /// Callback invoked when a scroll event occurs.
    on-scroll-event: func(callback-id: u64, scroll-x: f64, scroll-y: f64);
}

/// Window resize callback interface - implemented by the component, called by host.
interface window-resize-callbacks {
    /// Callback invoked when the window is resized.
    on-window-resize: func(callback-id: u64, width: s32, height: s32);
}

/// Platform helpers interface - helper functions for platform-specific operations.
///
/// These are convenience functions that wrap complex browser APIs.
interface platform-helpers {
    use types.{dom-rect};

    /// Get the inner width of the window.
    inner-width: func() -> s32;

    /// Get the inner height of the window.
    inner-height: func() -> s32;

    /// Set a timeout with the given callback ID.
    set-timeout: func(callback-id: u64, ms: s32) -> s32;

    /// Clear a timeout.
    clear-timeout: func(id: s32);

    /// Request an animation frame with the given callback ID.
    request-animation-frame: func(callback-id: u64) -> u32;

    /// Cancel an animation frame.
    cancel-animation-frame: func(id: u32);

    /// Get the bounding client rectangle of an element.
    get-bounding-client-rect: func(element: u64) -> dom-rect;

    /// Create a resize observer with the given callback ID.
    create-resize-observer: func(callback-id: u64) -> u64;

    /// Observe element resizes.
    observe-resize: func(observer: u64, element: u64);

    /// Unobserve element resizes.
    unobserve-resize: func(observer: u64, element: u64);

    /// Disconnect a resize observer.
    disconnect-resize: func(observer: u64);

    /// Create a mutation observer with the given callback ID.
    create-mutation-observer: func(callback-id: u64) -> u64;

    /// Observe DOM mutations.
    observe-mutations: func(observer: u64, target: u64, options: option<u64>);

    /// Disconnect a mutation observer.
    disconnect-mutation: func(observer: u64);

    /// Get an element by its ID.
    get-element-by-id: func(id: string) -> option<u64>;

    /// Query the first element matching a CSS selector.
    query-selector: func(selector: string) -> option<u64>;

    /// Query all elements matching a CSS selector.
    query-selector-all: func(selector: string) -> list<u64>;

    /// Get the element at the given viewport coordinates.
    element-from-point: func(x: s32, y: s32) -> option<u64>;

    /// Get the closest ancestor matching a CSS selector.
    element-closest: func(element: u64, selector: string) -> option<u64>;

    /// Get the current scroll Y position of the window.
    get-scroll-y: func() -> f64;

    /// Scroll the window to the given position with behavior.
    scroll-to: func(top: f64, behavior: string);

    /// Register a callback for scroll events.
    on-scroll: func(callback-id: u64);

    /// Register a callback for window resize events.
    on-resize-callback: func(callback-id: u64);

    /// Copy text to the clipboard.
    copy-to-clipboard: func(text: string) -> bool;

    /// Read text from the clipboard.
    read-clipboard: func() -> option<string>;

    /// Check if the user prefers dark mode.
    prefers-dark-mode: func() -> bool;

    /// Get the bounding rect of an element by its ID.
    get-element-rect-by-id: func(id: string) -> option<dom-rect>;

    /// Get the bounding rect of the first descendant matching a class within an element.
    get-bounding-rect-by-class: func(class-name: string, element: u64) -> option<dom-rect>;

    /// Request fullscreen mode for an element.
    request-fullscreen: func(element: u64);
}

/// Component lifecycle interface - implemented by the component.
interface lifecycle {
    /// Called by the host after the component is instantiated.
    /// This is the entry point for the component's main logic.
    start: func() -> result<_, string>;
}

/// Console interface for logging.
interface console {
    /// Log a message to the console.
    log: func(message: string);
    /// Log a warning to the console.
    warn: func(message: string);
    /// Log an error to the console.
    error: func(message: string);
}

/// Event target interface for adding/removing event listeners.
interface event-target {
    /// Add an event listener to a target node.
    /// Returns a listener-id that can be used to remove the listener.
    add-event-listener: func(target: u64, event-type: string, use-capture: bool) -> result<u64, string>;
    /// Remove a previously registered event listener.
    remove-event-listener: func(target: u64, listener-id: u64) -> result<_, string>;
    /// Prevent the default action for this event.
    prevent-default: func(event: u64);
    /// Stop the event from propagating further.
    stop-propagation: func(event: u64);
}

/// Style interface - convenience wrapper for CSS operations.
/// This is a simplified interface for the most common style operations.
interface style {
    /// Set a CSS property on an element.
    set-style-property: func(element: u64, property: string, value: string) -> result<_, string>;
    /// Get a CSS property value from an element.
    get-style-property: func(element: u64, property: string) -> option<string>;
    /// Remove a CSS property from an element.
    remove-style-property: func(element: u64, property: string) -> result<_, string>;
}

interface credential {
    /// Opaque handle to a host-side `Credential` instance.
    type credential-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: credential-handle) -> string;

    /// `type` attribute — getter.
    get-type: func(self: credential-handle) -> string;

    /// `isConditionalMediationAvailable()` operation.
    is-conditional-mediation-available: func() -> u64;
}

interface credential-user-data {
    /// Opaque handle to a host-side `CredentialUserData` instance.
    type credential-user-data-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: credential-user-data-handle) -> string;

    /// `iconURL` attribute — getter.
    get-icon-url: func(self: credential-user-data-handle) -> string;
}

interface credentials-container {
    /// Opaque handle to a host-side `CredentialsContainer` instance.
    type credentials-container-handle = u64;

    /// `get()` operation.
    get: func(self: credentials-container-handle, options: option<u64>) -> u64;

    /// `store()` operation.
    store: func(self: credentials-container-handle, credential: u64) -> u64;

    /// `create()` operation.
    create: func(self: credentials-container-handle, options: option<u64>) -> u64;

    /// `preventSilentAccess()` operation.
    prevent-silent-access: func(self: credentials-container-handle) -> u64;
}

interface password-credential {
    /// Opaque handle to a host-side `PasswordCredential` instance.
    type password-credential-handle = u64;

    /// `password` attribute — getter.
    get-password: func(self: password-credential-handle) -> string;
}

interface federated-credential {
    /// Opaque handle to a host-side `FederatedCredential` instance.
    type federated-credential-handle = u64;

    /// `provider` attribute — getter.
    get-provider: func(self: federated-credential-handle) -> string;

    /// `protocol` attribute — getter.
    get-protocol: func(self: federated-credential-handle) -> option<string>;
}

interface public-key-credential {
    /// Opaque handle to a host-side `PublicKeyCredential` instance.
    type public-key-credential-handle = u64;

    /// `rawId` attribute — getter.
    get-raw-id: func(self: public-key-credential-handle) -> list<u8>;

    /// `response` attribute — getter.
    get-response: func(self: public-key-credential-handle) -> u64;

    /// `authenticatorAttachment` attribute — getter.
    get-authenticator-attachment: func(self: public-key-credential-handle) -> option<string>;

    /// `getClientExtensionResults()` operation.
    get-client-extension-results: func(self: public-key-credential-handle) -> u64;

    /// `isConditionalMediationAvailable()` operation.
    is-conditional-mediation-available: func() -> u64;

    /// `toJSON()` operation.
    to-json: func(self: public-key-credential-handle) -> u64;

    /// `isUserVerifyingPlatformAuthenticatorAvailable()` operation.
    is-user-verifying-platform-authenticator-available: func() -> u64;

    /// `getClientCapabilities()` operation.
    get-client-capabilities: func() -> u64;

    /// `parseCreationOptionsFromJSON()` operation.
    parse-creation-options-from-json: func(options: u64) -> u64;

    /// `parseRequestOptionsFromJSON()` operation.
    parse-request-options-from-json: func(options: u64) -> u64;

    /// `signalUnknownCredential()` operation.
    signal-unknown-credential: func(options: u64) -> u64;

    /// `signalAllAcceptedCredentials()` operation.
    signal-all-accepted-credentials: func(options: u64) -> u64;

    /// `signalCurrentUserDetails()` operation.
    signal-current-user-details: func(options: u64) -> u64;
}

interface authenticator-response {
    /// Opaque handle to a host-side `AuthenticatorResponse` instance.
    type authenticator-response-handle = u64;

    /// `clientDataJSON` attribute — getter.
    get-client-data-json: func(self: authenticator-response-handle) -> list<u8>;
}

interface authenticator-attestation-response {
    /// Opaque handle to a host-side `AuthenticatorAttestationResponse` instance.
    type authenticator-attestation-response-handle = u64;

    /// `attestationObject` attribute — getter.
    get-attestation-object: func(self: authenticator-attestation-response-handle) -> list<u8>;

    /// `getTransports()` operation.
    get-transports: func(self: authenticator-attestation-response-handle) -> list<string>;

    /// `getAuthenticatorData()` operation.
    get-authenticator-data: func(self: authenticator-attestation-response-handle) -> list<u8>;

    /// `getPublicKey()` operation.
    get-public-key: func(self: authenticator-attestation-response-handle) -> option<list<u8>>;

    /// `getPublicKeyAlgorithm()` operation.
    get-public-key-algorithm: func(self: authenticator-attestation-response-handle) -> u64;
}

interface authenticator-assertion-response {
    /// Opaque handle to a host-side `AuthenticatorAssertionResponse` instance.
    type authenticator-assertion-response-handle = u64;

    /// `authenticatorData` attribute — getter.
    get-authenticator-data: func(self: authenticator-assertion-response-handle) -> list<u8>;

    /// `signature` attribute — getter.
    get-signature: func(self: authenticator-assertion-response-handle) -> list<u8>;

    /// `userHandle` attribute — getter.
    get-user-handle: func(self: authenticator-assertion-response-handle) -> option<list<u8>>;
}

interface types {

    /// DOMRect values - x, y, width, height

    record dom-rect {

        x: f64,

        y: f64,

        width: f64,

        height: f64,

    }

}

interface audio-decoder {
    /// Opaque handle to a host-side `AudioDecoder` instance.
    type audio-decoder-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: audio-decoder-handle) -> u64;

    /// `decodeQueueSize` attribute — getter.
    get-decode-queue-size: func(self: audio-decoder-handle) -> u32;

    /// `ondequeue` attribute — getter.
    get-ondequeue: func(self: audio-decoder-handle) -> u64;
    /// `ondequeue` attribute — setter.
    set-ondequeue: func(self: audio-decoder-handle, value: u64);

    /// `configure()` operation.
    configure: func(self: audio-decoder-handle, config: u64);

    /// `decode()` operation.
    decode: func(self: audio-decoder-handle, chunk: u64);

    /// `flush()` operation.
    flush: func(self: audio-decoder-handle) -> u64;

    /// `reset()` operation.
    reset: func(self: audio-decoder-handle);

    /// `close()` operation.
    close: func(self: audio-decoder-handle);

    /// `isConfigSupported()` operation.
    is-config-supported: func(config: u64) -> u64;
}

interface video-decoder {
    /// Opaque handle to a host-side `VideoDecoder` instance.
    type video-decoder-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: video-decoder-handle) -> u64;

    /// `decodeQueueSize` attribute — getter.
    get-decode-queue-size: func(self: video-decoder-handle) -> u32;

    /// `ondequeue` attribute — getter.
    get-ondequeue: func(self: video-decoder-handle) -> u64;
    /// `ondequeue` attribute — setter.
    set-ondequeue: func(self: video-decoder-handle, value: u64);

    /// `configure()` operation.
    configure: func(self: video-decoder-handle, config: u64);

    /// `decode()` operation.
    decode: func(self: video-decoder-handle, chunk: u64);

    /// `flush()` operation.
    flush: func(self: video-decoder-handle) -> u64;

    /// `reset()` operation.
    reset: func(self: video-decoder-handle);

    /// `close()` operation.
    close: func(self: video-decoder-handle);

    /// `isConfigSupported()` operation.
    is-config-supported: func(config: u64) -> u64;
}

interface audio-encoder {
    /// Opaque handle to a host-side `AudioEncoder` instance.
    type audio-encoder-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: audio-encoder-handle) -> u64;

    /// `encodeQueueSize` attribute — getter.
    get-encode-queue-size: func(self: audio-encoder-handle) -> u32;

    /// `ondequeue` attribute — getter.
    get-ondequeue: func(self: audio-encoder-handle) -> u64;
    /// `ondequeue` attribute — setter.
    set-ondequeue: func(self: audio-encoder-handle, value: u64);

    /// `configure()` operation.
    configure: func(self: audio-encoder-handle, config: u64);

    /// `encode()` operation.
    encode: func(self: audio-encoder-handle, data: u64);

    /// `flush()` operation.
    flush: func(self: audio-encoder-handle) -> u64;

    /// `reset()` operation.
    reset: func(self: audio-encoder-handle);

    /// `close()` operation.
    close: func(self: audio-encoder-handle);

    /// `isConfigSupported()` operation.
    is-config-supported: func(config: u64) -> u64;
}

interface video-encoder {
    /// Opaque handle to a host-side `VideoEncoder` instance.
    type video-encoder-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: video-encoder-handle) -> u64;

    /// `encodeQueueSize` attribute — getter.
    get-encode-queue-size: func(self: video-encoder-handle) -> u32;

    /// `ondequeue` attribute — getter.
    get-ondequeue: func(self: video-encoder-handle) -> u64;
    /// `ondequeue` attribute — setter.
    set-ondequeue: func(self: video-encoder-handle, value: u64);

    /// `configure()` operation.
    configure: func(self: video-encoder-handle, config: u64);

    /// `encode()` operation.
    encode: func(self: video-encoder-handle, frame: u64, options: option<u64>);

    /// `flush()` operation.
    flush: func(self: video-encoder-handle) -> u64;

    /// `reset()` operation.
    reset: func(self: video-encoder-handle);

    /// `close()` operation.
    close: func(self: video-encoder-handle);

    /// `isConfigSupported()` operation.
    is-config-supported: func(config: u64) -> u64;
}

interface encoded-audio-chunk {
    /// Opaque handle to a host-side `EncodedAudioChunk` instance.
    type encoded-audio-chunk-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: encoded-audio-chunk-handle) -> u64;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: encoded-audio-chunk-handle) -> s64;

    /// `duration` attribute — getter.
    get-duration: func(self: encoded-audio-chunk-handle) -> option<u64>;

    /// `byteLength` attribute — getter.
    get-byte-length: func(self: encoded-audio-chunk-handle) -> u32;

    /// `copyTo()` operation.
    copy-to: func(self: encoded-audio-chunk-handle, destination: u64);
}

interface encoded-video-chunk {
    /// Opaque handle to a host-side `EncodedVideoChunk` instance.
    type encoded-video-chunk-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: encoded-video-chunk-handle) -> u64;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: encoded-video-chunk-handle) -> s64;

    /// `duration` attribute — getter.
    get-duration: func(self: encoded-video-chunk-handle) -> option<u64>;

    /// `byteLength` attribute — getter.
    get-byte-length: func(self: encoded-video-chunk-handle) -> u32;

    /// `copyTo()` operation.
    copy-to: func(self: encoded-video-chunk-handle, destination: u64);
}

interface audio-data {
    /// Opaque handle to a host-side `AudioData` instance.
    type audio-data-handle = u64;

    /// `format` attribute — getter.
    get-format: func(self: audio-data-handle) -> option<u64>;

    /// `sampleRate` attribute — getter.
    get-sample-rate: func(self: audio-data-handle) -> f32;

    /// `numberOfFrames` attribute — getter.
    get-number-of-frames: func(self: audio-data-handle) -> u32;

    /// `numberOfChannels` attribute — getter.
    get-number-of-channels: func(self: audio-data-handle) -> u32;

    /// `duration` attribute — getter.
    get-duration: func(self: audio-data-handle) -> u64;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: audio-data-handle) -> s64;

    /// `allocationSize()` operation.
    allocation-size: func(self: audio-data-handle, options: u64) -> u32;

    /// `copyTo()` operation.
    copy-to: func(self: audio-data-handle, destination: u64, options: u64);

    /// `clone()` operation.
    clone: func(self: audio-data-handle) -> u64;

    /// `close()` operation.
    close: func(self: audio-data-handle);
}

interface video-frame {
    /// Opaque handle to a host-side `VideoFrame` instance.

    use types.{dom-rect};

    type video-frame-handle = u64;

    /// `format` attribute — getter.
    get-format: func(self: video-frame-handle) -> option<u64>;

    /// `codedWidth` attribute — getter.
    get-coded-width: func(self: video-frame-handle) -> u32;

    /// `codedHeight` attribute — getter.
    get-coded-height: func(self: video-frame-handle) -> u32;

    /// `codedRect` attribute — getter.
    get-coded-rect: func(self: video-frame-handle) -> option<dom-rect>;

    /// `visibleRect` attribute — getter.
    get-visible-rect: func(self: video-frame-handle) -> option<dom-rect>;

    /// `rotation` attribute — getter.
    get-rotation: func(self: video-frame-handle) -> f64;

    /// `flip` attribute — getter.
    get-flip: func(self: video-frame-handle) -> bool;

    /// `displayWidth` attribute — getter.
    get-display-width: func(self: video-frame-handle) -> u32;

    /// `displayHeight` attribute — getter.
    get-display-height: func(self: video-frame-handle) -> u32;

    /// `duration` attribute — getter.
    get-duration: func(self: video-frame-handle) -> option<u64>;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: video-frame-handle) -> s64;

    /// `colorSpace` attribute — getter.
    get-color-space: func(self: video-frame-handle) -> u64;

    /// `metadata()` operation.
    metadata: func(self: video-frame-handle) -> u64;

    /// `allocationSize()` operation.
    allocation-size: func(self: video-frame-handle, options: option<u64>) -> u32;

    /// `copyTo()` operation.
    copy-to: func(self: video-frame-handle, destination: u64, options: option<u64>) -> u64;

    /// `clone()` operation.
    clone: func(self: video-frame-handle) -> u64;

    /// `close()` operation.
    close: func(self: video-frame-handle);
}

interface video-color-space {
    /// Opaque handle to a host-side `VideoColorSpace` instance.
    type video-color-space-handle = u64;

    /// `primaries` attribute — getter.
    get-primaries: func(self: video-color-space-handle) -> option<u64>;

    /// `transfer` attribute — getter.
    get-transfer: func(self: video-color-space-handle) -> option<u64>;

    /// `matrix` attribute — getter.
    get-matrix: func(self: video-color-space-handle) -> option<u64>;

    /// `fullRange` attribute — getter.
    get-full-range: func(self: video-color-space-handle) -> option<bool>;

    /// `toJSON()` operation.
    to-json: func(self: video-color-space-handle) -> u64;
}

interface image-decoder {
    /// Opaque handle to a host-side `ImageDecoder` instance.
    type image-decoder-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: image-decoder-handle) -> string;

    /// `complete` attribute — getter.
    get-complete: func(self: image-decoder-handle) -> bool;

    /// `completed` attribute — getter.
    get-completed: func(self: image-decoder-handle) -> u64;

    /// `tracks` attribute — getter.
    get-tracks: func(self: image-decoder-handle) -> u64;

    /// `decode()` operation.
    decode: func(self: image-decoder-handle, options: option<u64>) -> u64;

    /// `reset()` operation.
    reset: func(self: image-decoder-handle);

    /// `close()` operation.
    close: func(self: image-decoder-handle);

    /// `isTypeSupported()` operation.
    is-type-supported: func(%type: string) -> u64;
}

interface image-track-list {
    /// Opaque handle to a host-side `ImageTrackList` instance.
    type image-track-list-handle = u64;

    /// `ImageTrack()` operation.
    image-track: func(self: image-track-list-handle, index: u32);

    /// `ready` attribute — getter.
    get-ready: func(self: image-track-list-handle) -> u64;

    /// `length` attribute — getter.
    get-length: func(self: image-track-list-handle) -> u32;

    /// `selectedIndex` attribute — getter.
    get-selected-index: func(self: image-track-list-handle) -> s32;

    /// `selectedTrack` attribute — getter.
    get-selected-track: func(self: image-track-list-handle) -> option<u64>;
}

interface image-track {
    /// Opaque handle to a host-side `ImageTrack` instance.
    type image-track-handle = u64;

    /// `animated` attribute — getter.
    get-animated: func(self: image-track-handle) -> bool;

    /// `frameCount` attribute — getter.
    get-frame-count: func(self: image-track-handle) -> u32;

    /// `repetitionCount` attribute — getter.
    get-repetition-count: func(self: image-track-handle) -> f32;

    /// `selected` attribute — getter.
    get-selected: func(self: image-track-handle) -> bool;
    /// `selected` attribute — setter.
    set-selected: func(self: image-track-handle, value: bool);
}

interface web-gl-object {
    /// Opaque handle to a host-side `WebGLObject` instance.
    type web-gl-object-handle = u64;

    /// `label` attribute — getter.
    get-label: func(self: web-gl-object-handle) -> string;
    /// `label` attribute — setter.
    set-label: func(self: web-gl-object-handle, value: string);
}

interface web-gl-active-info {
    /// Opaque handle to a host-side `WebGLActiveInfo` instance.
    type web-gl-active-info-handle = u64;

    /// `size` attribute — getter.
    get-size: func(self: web-gl-active-info-handle) -> u64;

    /// `type` attribute — getter.
    get-type: func(self: web-gl-active-info-handle) -> u64;

    /// `name` attribute — getter.
    get-name: func(self: web-gl-active-info-handle) -> string;
}

interface web-gl-shader-precision-format {
    /// Opaque handle to a host-side `WebGLShaderPrecisionFormat` instance.
    type web-gl-shader-precision-format-handle = u64;

    /// `rangeMin` attribute — getter.
    get-range-min: func(self: web-gl-shader-precision-format-handle) -> u64;

    /// `rangeMax` attribute — getter.
    get-range-max: func(self: web-gl-shader-precision-format-handle) -> u64;

    /// `precision` attribute — getter.
    get-precision: func(self: web-gl-shader-precision-format-handle) -> u64;
}

interface web-gl-rendering-context-base {
    /// Opaque handle to a host-side `WebGLRenderingContextBase` instance.
    type web-gl-rendering-context-base-handle = u64;

    /// `canvas` attribute — getter.
    get-canvas: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `drawingBufferWidth` attribute — getter.
    get-drawing-buffer-width: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `drawingBufferHeight` attribute — getter.
    get-drawing-buffer-height: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `drawingBufferFormat` attribute — getter.
    get-drawing-buffer-format: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `drawingBufferColorSpace` attribute — getter.
    get-drawing-buffer-color-space: func(self: web-gl-rendering-context-base-handle) -> u64;
    /// `drawingBufferColorSpace` attribute — setter.
    set-drawing-buffer-color-space: func(self: web-gl-rendering-context-base-handle, value: u64);

    /// `unpackColorSpace` attribute — getter.
    get-unpack-color-space: func(self: web-gl-rendering-context-base-handle) -> u64;
    /// `unpackColorSpace` attribute — setter.
    set-unpack-color-space: func(self: web-gl-rendering-context-base-handle, value: u64);

    /// `getContextAttributes()` operation.
    get-context-attributes: func(self: web-gl-rendering-context-base-handle) -> option<u64>;

    /// `isContextLost()` operation.
    is-context-lost: func(self: web-gl-rendering-context-base-handle) -> bool;

    /// `getSupportedExtensions()` operation.
    get-supported-extensions: func(self: web-gl-rendering-context-base-handle) -> option<list<string>>;

    /// `getExtension()` operation.
    get-extension: func(self: web-gl-rendering-context-base-handle, name: string) -> option<u64>;

    /// `drawingBufferStorage()` operation.
    drawing-buffer-storage: func(self: web-gl-rendering-context-base-handle, sized-format: u64, width: u32, height: u32);

    /// `activeTexture()` operation.
    active-texture: func(self: web-gl-rendering-context-base-handle, texture: u64);

    /// `attachShader()` operation.
    attach-shader: func(self: web-gl-rendering-context-base-handle, program: u64, shader: u64);

    /// `bindAttribLocation()` operation.
    bind-attrib-location: func(self: web-gl-rendering-context-base-handle, program: u64, index: u64, name: string);

    /// `bindBuffer()` operation.
    bind-buffer: func(self: web-gl-rendering-context-base-handle, target: u64, buffer: option<u64>);

    /// `bindFramebuffer()` operation.
    bind-framebuffer: func(self: web-gl-rendering-context-base-handle, target: u64, framebuffer: option<u64>);

    /// `bindRenderbuffer()` operation.
    bind-renderbuffer: func(self: web-gl-rendering-context-base-handle, target: u64, renderbuffer: option<u64>);

    /// `bindTexture()` operation.
    bind-texture: func(self: web-gl-rendering-context-base-handle, target: u64, texture: option<u64>);

    /// `blendColor()` operation.
    blend-color: func(self: web-gl-rendering-context-base-handle, red: u64, green: u64, blue: u64, alpha: u64);

    /// `blendEquation()` operation.
    blend-equation: func(self: web-gl-rendering-context-base-handle, mode: u64);

    /// `blendEquationSeparate()` operation.
    blend-equation-separate: func(self: web-gl-rendering-context-base-handle, mode-rgb: u64, mode-alpha: u64);

    /// `blendFunc()` operation.
    blend-func: func(self: web-gl-rendering-context-base-handle, sfactor: u64, dfactor: u64);

    /// `blendFuncSeparate()` operation.
    blend-func-separate: func(self: web-gl-rendering-context-base-handle, src-rgb: u64, dst-rgb: u64, src-alpha: u64, dst-alpha: u64);

    /// `checkFramebufferStatus()` operation.
    check-framebuffer-status: func(self: web-gl-rendering-context-base-handle, target: u64) -> u64;

    /// `clear()` operation.
    clear: func(self: web-gl-rendering-context-base-handle, mask: u64);

    /// `clearColor()` operation.
    clear-color: func(self: web-gl-rendering-context-base-handle, red: u64, green: u64, blue: u64, alpha: u64);

    /// `clearDepth()` operation.
    clear-depth: func(self: web-gl-rendering-context-base-handle, depth: u64);

    /// `clearStencil()` operation.
    clear-stencil: func(self: web-gl-rendering-context-base-handle, s: u64);

    /// `colorMask()` operation.
    color-mask: func(self: web-gl-rendering-context-base-handle, red: u64, green: u64, blue: u64, alpha: u64);

    /// `compileShader()` operation.
    compile-shader: func(self: web-gl-rendering-context-base-handle, shader: u64);

    /// `copyTexImage2D()` operation.
    copy-tex-image2-d: func(self: web-gl-rendering-context-base-handle, target: u64, level: u64, internalformat: u64, x: u64, y: u64, width: u64, height: u64, border: u64);

    /// `copyTexSubImage2D()` operation.
    copy-tex-sub-image2-d: func(self: web-gl-rendering-context-base-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, x: u64, y: u64, width: u64, height: u64);

    /// `createBuffer()` operation.
    create-buffer: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `createFramebuffer()` operation.
    create-framebuffer: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `createProgram()` operation.
    create-program: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `createRenderbuffer()` operation.
    create-renderbuffer: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `createShader()` operation.
    create-shader: func(self: web-gl-rendering-context-base-handle, %type: u64) -> option<u64>;

    /// `createTexture()` operation.
    create-texture: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `cullFace()` operation.
    cull-face: func(self: web-gl-rendering-context-base-handle, mode: u64);

    /// `deleteBuffer()` operation.
    delete-buffer: func(self: web-gl-rendering-context-base-handle, buffer: option<u64>);

    /// `deleteFramebuffer()` operation.
    delete-framebuffer: func(self: web-gl-rendering-context-base-handle, framebuffer: option<u64>);

    /// `deleteProgram()` operation.
    delete-program: func(self: web-gl-rendering-context-base-handle, program: option<u64>);

    /// `deleteRenderbuffer()` operation.
    delete-renderbuffer: func(self: web-gl-rendering-context-base-handle, renderbuffer: option<u64>);

    /// `deleteShader()` operation.
    delete-shader: func(self: web-gl-rendering-context-base-handle, shader: option<u64>);

    /// `deleteTexture()` operation.
    delete-texture: func(self: web-gl-rendering-context-base-handle, texture: option<u64>);

    /// `depthFunc()` operation.
    depth-func: func(self: web-gl-rendering-context-base-handle, %func: u64);

    /// `depthMask()` operation.
    depth-mask: func(self: web-gl-rendering-context-base-handle, flag: u64);

    /// `depthRange()` operation.
    depth-range: func(self: web-gl-rendering-context-base-handle, z-near: u64, z-far: u64);

    /// `detachShader()` operation.
    detach-shader: func(self: web-gl-rendering-context-base-handle, program: u64, shader: u64);

    /// `disable()` operation.
    disable: func(self: web-gl-rendering-context-base-handle, cap: u64);

    /// `disableVertexAttribArray()` operation.
    disable-vertex-attrib-array: func(self: web-gl-rendering-context-base-handle, index: u64);

    /// `drawArrays()` operation.
    draw-arrays: func(self: web-gl-rendering-context-base-handle, mode: u64, first: u64, count: u64);

    /// `drawElements()` operation.
    draw-elements: func(self: web-gl-rendering-context-base-handle, mode: u64, count: u64, %type: u64, offset: u64);

    /// `enable()` operation.
    enable: func(self: web-gl-rendering-context-base-handle, cap: u64);

    /// `enableVertexAttribArray()` operation.
    enable-vertex-attrib-array: func(self: web-gl-rendering-context-base-handle, index: u64);

    /// `finish()` operation.
    finish: func(self: web-gl-rendering-context-base-handle);

    /// `flush()` operation.
    flush: func(self: web-gl-rendering-context-base-handle);

    /// `framebufferRenderbuffer()` operation.
    framebuffer-renderbuffer: func(self: web-gl-rendering-context-base-handle, target: u64, attachment: u64, renderbuffertarget: u64, renderbuffer: option<u64>);

    /// `framebufferTexture2D()` operation.
    framebuffer-texture2-d: func(self: web-gl-rendering-context-base-handle, target: u64, attachment: u64, textarget: u64, texture: option<u64>, level: u64);

    /// `frontFace()` operation.
    front-face: func(self: web-gl-rendering-context-base-handle, mode: u64);

    /// `generateMipmap()` operation.
    generate-mipmap: func(self: web-gl-rendering-context-base-handle, target: u64);

    /// `getActiveAttrib()` operation.
    get-active-attrib: func(self: web-gl-rendering-context-base-handle, program: u64, index: u64) -> option<u64>;

    /// `getActiveUniform()` operation.
    get-active-uniform: func(self: web-gl-rendering-context-base-handle, program: u64, index: u64) -> option<u64>;

    /// `getAttachedShaders()` operation.
    get-attached-shaders: func(self: web-gl-rendering-context-base-handle, program: u64) -> option<list<u64>>;

    /// `getAttribLocation()` operation.
    get-attrib-location: func(self: web-gl-rendering-context-base-handle, program: u64, name: string) -> u64;

    /// `getBufferParameter()` operation.
    get-buffer-parameter: func(self: web-gl-rendering-context-base-handle, target: u64, pname: u64) -> string;

    /// `getParameter()` operation.
    get-parameter: func(self: web-gl-rendering-context-base-handle, pname: u64) -> string;

    /// `getError()` operation.
    get-error: func(self: web-gl-rendering-context-base-handle) -> u64;

    /// `getFramebufferAttachmentParameter()` operation.
    get-framebuffer-attachment-parameter: func(self: web-gl-rendering-context-base-handle, target: u64, attachment: u64, pname: u64) -> string;

    /// `getProgramParameter()` operation.
    get-program-parameter: func(self: web-gl-rendering-context-base-handle, program: u64, pname: u64) -> string;

    /// `getProgramInfoLog()` operation.
    get-program-info-log: func(self: web-gl-rendering-context-base-handle, program: u64) -> option<string>;

    /// `getRenderbufferParameter()` operation.
    get-renderbuffer-parameter: func(self: web-gl-rendering-context-base-handle, target: u64, pname: u64) -> string;

    /// `getShaderParameter()` operation.
    get-shader-parameter: func(self: web-gl-rendering-context-base-handle, shader: u64, pname: u64) -> string;

    /// `getShaderPrecisionFormat()` operation.
    get-shader-precision-format: func(self: web-gl-rendering-context-base-handle, shadertype: u64, precisiontype: u64) -> option<u64>;

    /// `getShaderInfoLog()` operation.
    get-shader-info-log: func(self: web-gl-rendering-context-base-handle, shader: u64) -> option<string>;

    /// `getShaderSource()` operation.
    get-shader-source: func(self: web-gl-rendering-context-base-handle, shader: u64) -> option<string>;

    /// `getTexParameter()` operation.
    get-tex-parameter: func(self: web-gl-rendering-context-base-handle, target: u64, pname: u64) -> string;

    /// `getUniform()` operation.
    get-uniform: func(self: web-gl-rendering-context-base-handle, program: u64, location: u64) -> string;

    /// `getUniformLocation()` operation.
    get-uniform-location: func(self: web-gl-rendering-context-base-handle, program: u64, name: string) -> option<u64>;

    /// `getVertexAttrib()` operation.
    get-vertex-attrib: func(self: web-gl-rendering-context-base-handle, index: u64, pname: u64) -> string;

    /// `getVertexAttribOffset()` operation.
    get-vertex-attrib-offset: func(self: web-gl-rendering-context-base-handle, index: u64, pname: u64) -> u64;

    /// `hint()` operation.
    hint: func(self: web-gl-rendering-context-base-handle, target: u64, mode: u64);

    /// `isBuffer()` operation.
    is-buffer: func(self: web-gl-rendering-context-base-handle, buffer: option<u64>) -> u64;

    /// `isEnabled()` operation.
    is-enabled: func(self: web-gl-rendering-context-base-handle, cap: u64) -> u64;

    /// `isFramebuffer()` operation.
    is-framebuffer: func(self: web-gl-rendering-context-base-handle, framebuffer: option<u64>) -> u64;

    /// `isProgram()` operation.
    is-program: func(self: web-gl-rendering-context-base-handle, program: option<u64>) -> u64;

    /// `isRenderbuffer()` operation.
    is-renderbuffer: func(self: web-gl-rendering-context-base-handle, renderbuffer: option<u64>) -> u64;

    /// `isShader()` operation.
    is-shader: func(self: web-gl-rendering-context-base-handle, shader: option<u64>) -> u64;

    /// `isTexture()` operation.
    is-texture: func(self: web-gl-rendering-context-base-handle, texture: option<u64>) -> u64;

    /// `lineWidth()` operation.
    line-width: func(self: web-gl-rendering-context-base-handle, width: u64);

    /// `linkProgram()` operation.
    link-program: func(self: web-gl-rendering-context-base-handle, program: u64);

    /// `pixelStorei()` operation.
    pixel-storei: func(self: web-gl-rendering-context-base-handle, pname: u64, param: u64);

    /// `polygonOffset()` operation.
    polygon-offset: func(self: web-gl-rendering-context-base-handle, factor: u64, units: u64);

    /// `renderbufferStorage()` operation.
    renderbuffer-storage: func(self: web-gl-rendering-context-base-handle, target: u64, internalformat: u64, width: u64, height: u64);

    /// `sampleCoverage()` operation.
    sample-coverage: func(self: web-gl-rendering-context-base-handle, value: u64, invert: u64);

    /// `scissor()` operation.
    scissor: func(self: web-gl-rendering-context-base-handle, x: u64, y: u64, width: u64, height: u64);

    /// `shaderSource()` operation.
    shader-source: func(self: web-gl-rendering-context-base-handle, shader: u64, source: string);

    /// `stencilFunc()` operation.
    stencil-func: func(self: web-gl-rendering-context-base-handle, %func: u64, ref: u64, mask: u64);

    /// `stencilFuncSeparate()` operation.
    stencil-func-separate: func(self: web-gl-rendering-context-base-handle, face: u64, %func: u64, ref: u64, mask: u64);

    /// `stencilMask()` operation.
    stencil-mask: func(self: web-gl-rendering-context-base-handle, mask: u64);

    /// `stencilMaskSeparate()` operation.
    stencil-mask-separate: func(self: web-gl-rendering-context-base-handle, face: u64, mask: u64);

    /// `stencilOp()` operation.
    stencil-op: func(self: web-gl-rendering-context-base-handle, fail: u64, zfail: u64, zpass: u64);

    /// `stencilOpSeparate()` operation.
    stencil-op-separate: func(self: web-gl-rendering-context-base-handle, face: u64, fail: u64, zfail: u64, zpass: u64);

    /// `texParameterf()` operation.
    tex-parameterf: func(self: web-gl-rendering-context-base-handle, target: u64, pname: u64, param: u64);

    /// `texParameteri()` operation.
    tex-parameteri: func(self: web-gl-rendering-context-base-handle, target: u64, pname: u64, param: u64);

    /// `uniform1f()` operation.
    uniform1f: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64);

    /// `uniform2f()` operation.
    uniform2f: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64);

    /// `uniform3f()` operation.
    uniform3f: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64, z: u64);

    /// `uniform4f()` operation.
    uniform4f: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64, z: u64, w: u64);

    /// `uniform1i()` operation.
    uniform1i: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64);

    /// `uniform2i()` operation.
    uniform2i: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64);

    /// `uniform3i()` operation.
    uniform3i: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64, z: u64);

    /// `uniform4i()` operation.
    uniform4i: func(self: web-gl-rendering-context-base-handle, location: option<u64>, x: u64, y: u64, z: u64, w: u64);

    /// `useProgram()` operation.
    use-program: func(self: web-gl-rendering-context-base-handle, program: option<u64>);

    /// `validateProgram()` operation.
    validate-program: func(self: web-gl-rendering-context-base-handle, program: u64);

    /// `vertexAttrib1f()` operation.
    vertex-attrib1f: func(self: web-gl-rendering-context-base-handle, index: u64, x: u64);

    /// `vertexAttrib2f()` operation.
    vertex-attrib2f: func(self: web-gl-rendering-context-base-handle, index: u64, x: u64, y: u64);

    /// `vertexAttrib3f()` operation.
    vertex-attrib3f: func(self: web-gl-rendering-context-base-handle, index: u64, x: u64, y: u64, z: u64);

    /// `vertexAttrib4f()` operation.
    vertex-attrib4f: func(self: web-gl-rendering-context-base-handle, index: u64, x: u64, y: u64, z: u64, w: u64);

    /// `vertexAttrib1fv()` operation.
    vertex-attrib1fv: func(self: web-gl-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttrib2fv()` operation.
    vertex-attrib2fv: func(self: web-gl-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttrib3fv()` operation.
    vertex-attrib3fv: func(self: web-gl-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttrib4fv()` operation.
    vertex-attrib4fv: func(self: web-gl-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttribPointer()` operation.
    vertex-attrib-pointer: func(self: web-gl-rendering-context-base-handle, index: u64, size: u64, %type: u64, normalized: u64, stride: u64, offset: u64);

    /// `viewport()` operation.
    viewport: func(self: web-gl-rendering-context-base-handle, x: u64, y: u64, width: u64, height: u64);
}

interface web-gl-rendering-context-overloads {
    /// Opaque handle to a host-side `WebGLRenderingContextOverloads` instance.
    type web-gl-rendering-context-overloads-handle = u64;

    /// `bufferData()` operation.
    buffer-data: func(self: web-gl-rendering-context-overloads-handle, target: u64, size: u64, usage: u64);

    /// `bufferSubData()` operation.
    buffer-sub-data: func(self: web-gl-rendering-context-overloads-handle, target: u64, offset: u64, data: u64);

    /// `compressedTexImage2D()` operation.
    compressed-tex-image2-d: func(self: web-gl-rendering-context-overloads-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, border: u64, data: list<u8>);

    /// `compressedTexSubImage2D()` operation.
    compressed-tex-sub-image2-d: func(self: web-gl-rendering-context-overloads-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, width: u64, height: u64, format: u64, data: list<u8>);

    /// `readPixels()` operation.
    read-pixels: func(self: web-gl-rendering-context-overloads-handle, x: u64, y: u64, width: u64, height: u64, format: u64, %type: u64, pixels: option<list<u8>>);

    /// `texImage2D()` operation.
    tex-image2-d: func(self: web-gl-rendering-context-overloads-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, border: u64, format: u64, %type: u64, pixels: option<list<u8>>);

    /// `texSubImage2D()` operation.
    tex-sub-image2-d: func(self: web-gl-rendering-context-overloads-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, width: u64, height: u64, format: u64, %type: u64, pixels: option<list<u8>>);

    /// `uniform1fv()` operation.
    uniform1fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform2fv()` operation.
    uniform2fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform3fv()` operation.
    uniform3fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform4fv()` operation.
    uniform4fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform1iv()` operation.
    uniform1iv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform2iv()` operation.
    uniform2iv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform3iv()` operation.
    uniform3iv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniform4iv()` operation.
    uniform4iv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, v: u64);

    /// `uniformMatrix2fv()` operation.
    uniform-matrix2fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, transpose: u64, value: u64);

    /// `uniformMatrix3fv()` operation.
    uniform-matrix3fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, transpose: u64, value: u64);

    /// `uniformMatrix4fv()` operation.
    uniform-matrix4fv: func(self: web-gl-rendering-context-overloads-handle, location: option<u64>, transpose: u64, value: u64);
}

interface web-gl-context-event {
    /// Opaque handle to a host-side `WebGLContextEvent` instance.
    type web-gl-context-event-handle = u64;

    /// `statusMessage` attribute — getter.
    get-status-message: func(self: web-gl-context-event-handle) -> string;
}

interface web-gl2-rendering-context-base {
    /// Opaque handle to a host-side `WebGL2RenderingContextBase` instance.
    type web-gl2-rendering-context-base-handle = u64;

    /// `copyBufferSubData()` operation.
    copy-buffer-sub-data: func(self: web-gl2-rendering-context-base-handle, read-target: u64, write-target: u64, read-offset: u64, write-offset: u64, size: u64);

    /// `getBufferSubData()` operation.
    get-buffer-sub-data: func(self: web-gl2-rendering-context-base-handle, target: u64, src-byte-offset: u64, dst-buffer: list<u8>, dst-offset: option<u64>, length: option<u64>);

    /// `blitFramebuffer()` operation.
    blit-framebuffer: func(self: web-gl2-rendering-context-base-handle, src-x0: u64, src-y0: u64, src-x1: u64, src-y1: u64, dst-x0: u64, dst-y0: u64, dst-x1: u64, dst-y1: u64, mask: u64, filter: u64);

    /// `framebufferTextureLayer()` operation.
    framebuffer-texture-layer: func(self: web-gl2-rendering-context-base-handle, target: u64, attachment: u64, texture: option<u64>, level: u64, layer: u64);

    /// `invalidateFramebuffer()` operation.
    invalidate-framebuffer: func(self: web-gl2-rendering-context-base-handle, target: u64, attachments: list<u64>);

    /// `invalidateSubFramebuffer()` operation.
    invalidate-sub-framebuffer: func(self: web-gl2-rendering-context-base-handle, target: u64, attachments: list<u64>, x: u64, y: u64, width: u64, height: u64);

    /// `readBuffer()` operation.
    read-buffer: func(self: web-gl2-rendering-context-base-handle, src: u64);

    /// `getInternalformatParameter()` operation.
    get-internalformat-parameter: func(self: web-gl2-rendering-context-base-handle, target: u64, internalformat: u64, pname: u64) -> string;

    /// `renderbufferStorageMultisample()` operation.
    renderbuffer-storage-multisample: func(self: web-gl2-rendering-context-base-handle, target: u64, samples: u64, internalformat: u64, width: u64, height: u64);

    /// `texStorage2D()` operation.
    tex-storage2-d: func(self: web-gl2-rendering-context-base-handle, target: u64, levels: u64, internalformat: u64, width: u64, height: u64);

    /// `texStorage3D()` operation.
    tex-storage3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, levels: u64, internalformat: u64, width: u64, height: u64, depth: u64);

    /// `texImage3D()` operation.
    tex-image3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, depth: u64, border: u64, format: u64, %type: u64, pbo-offset: u64);

    /// `texSubImage3D()` operation.
    tex-sub-image3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, zoffset: u64, width: u64, height: u64, depth: u64, format: u64, %type: u64, pbo-offset: u64);

    /// `copyTexSubImage3D()` operation.
    copy-tex-sub-image3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, zoffset: u64, x: u64, y: u64, width: u64, height: u64);

    /// `compressedTexImage3D()` operation.
    compressed-tex-image3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, depth: u64, border: u64, image-size: u64, offset: u64);

    /// `compressedTexSubImage3D()` operation.
    compressed-tex-sub-image3-d: func(self: web-gl2-rendering-context-base-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, zoffset: u64, width: u64, height: u64, depth: u64, format: u64, image-size: u64, offset: u64);

    /// `getFragDataLocation()` operation.
    get-frag-data-location: func(self: web-gl2-rendering-context-base-handle, program: u64, name: string) -> u64;

    /// `uniform1ui()` operation.
    uniform1ui: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, v0: u64);

    /// `uniform2ui()` operation.
    uniform2ui: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, v0: u64, v1: u64);

    /// `uniform3ui()` operation.
    uniform3ui: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, v0: u64, v1: u64, v2: u64);

    /// `uniform4ui()` operation.
    uniform4ui: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, v0: u64, v1: u64, v2: u64, v3: u64);

    /// `uniform1uiv()` operation.
    uniform1uiv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform2uiv()` operation.
    uniform2uiv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform3uiv()` operation.
    uniform3uiv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform4uiv()` operation.
    uniform4uiv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix3x2fv()` operation.
    uniform-matrix3x2fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix4x2fv()` operation.
    uniform-matrix4x2fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix2x3fv()` operation.
    uniform-matrix2x3fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix4x3fv()` operation.
    uniform-matrix4x3fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix2x4fv()` operation.
    uniform-matrix2x4fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix3x4fv()` operation.
    uniform-matrix3x4fv: func(self: web-gl2-rendering-context-base-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `vertexAttribI4i()` operation.
    vertex-attrib-i4i: func(self: web-gl2-rendering-context-base-handle, index: u64, x: u64, y: u64, z: u64, w: u64);

    /// `vertexAttribI4iv()` operation.
    vertex-attrib-i4iv: func(self: web-gl2-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttribI4ui()` operation.
    vertex-attrib-i4ui: func(self: web-gl2-rendering-context-base-handle, index: u64, x: u64, y: u64, z: u64, w: u64);

    /// `vertexAttribI4uiv()` operation.
    vertex-attrib-i4uiv: func(self: web-gl2-rendering-context-base-handle, index: u64, values: u64);

    /// `vertexAttribIPointer()` operation.
    vertex-attrib-i-pointer: func(self: web-gl2-rendering-context-base-handle, index: u64, size: u64, %type: u64, stride: u64, offset: u64);

    /// `vertexAttribDivisor()` operation.
    vertex-attrib-divisor: func(self: web-gl2-rendering-context-base-handle, index: u64, divisor: u64);

    /// `drawArraysInstanced()` operation.
    draw-arrays-instanced: func(self: web-gl2-rendering-context-base-handle, mode: u64, first: u64, count: u64, instance-count: u64);

    /// `drawElementsInstanced()` operation.
    draw-elements-instanced: func(self: web-gl2-rendering-context-base-handle, mode: u64, count: u64, %type: u64, offset: u64, instance-count: u64);

    /// `drawRangeElements()` operation.
    draw-range-elements: func(self: web-gl2-rendering-context-base-handle, mode: u64, start: u64, end: u64, count: u64, %type: u64, offset: u64);

    /// `drawBuffers()` operation.
    draw-buffers: func(self: web-gl2-rendering-context-base-handle, buffers: list<u64>);

    /// `clearBufferfv()` operation.
    clear-bufferfv: func(self: web-gl2-rendering-context-base-handle, buffer: u64, drawbuffer: u64, values: u64, src-offset: option<u64>);

    /// `clearBufferiv()` operation.
    clear-bufferiv: func(self: web-gl2-rendering-context-base-handle, buffer: u64, drawbuffer: u64, values: u64, src-offset: option<u64>);

    /// `clearBufferuiv()` operation.
    clear-bufferuiv: func(self: web-gl2-rendering-context-base-handle, buffer: u64, drawbuffer: u64, values: u64, src-offset: option<u64>);

    /// `clearBufferfi()` operation.
    clear-bufferfi: func(self: web-gl2-rendering-context-base-handle, buffer: u64, drawbuffer: u64, depth: u64, stencil: u64);

    /// `createQuery()` operation.
    create-query: func(self: web-gl2-rendering-context-base-handle) -> u64;

    /// `deleteQuery()` operation.
    delete-query: func(self: web-gl2-rendering-context-base-handle, query: option<u64>);

    /// `isQuery()` operation.
    is-query: func(self: web-gl2-rendering-context-base-handle, query: option<u64>) -> u64;

    /// `beginQuery()` operation.
    begin-query: func(self: web-gl2-rendering-context-base-handle, target: u64, query: u64);

    /// `endQuery()` operation.
    end-query: func(self: web-gl2-rendering-context-base-handle, target: u64);

    /// `getQuery()` operation.
    get-query: func(self: web-gl2-rendering-context-base-handle, target: u64, pname: u64) -> option<u64>;

    /// `getQueryParameter()` operation.
    get-query-parameter: func(self: web-gl2-rendering-context-base-handle, query: u64, pname: u64) -> string;

    /// `createSampler()` operation.
    create-sampler: func(self: web-gl2-rendering-context-base-handle) -> u64;

    /// `deleteSampler()` operation.
    delete-sampler: func(self: web-gl2-rendering-context-base-handle, sampler: option<u64>);

    /// `isSampler()` operation.
    is-sampler: func(self: web-gl2-rendering-context-base-handle, sampler: option<u64>) -> u64;

    /// `bindSampler()` operation.
    bind-sampler: func(self: web-gl2-rendering-context-base-handle, unit: u64, sampler: option<u64>);

    /// `samplerParameteri()` operation.
    sampler-parameteri: func(self: web-gl2-rendering-context-base-handle, sampler: u64, pname: u64, param: u64);

    /// `samplerParameterf()` operation.
    sampler-parameterf: func(self: web-gl2-rendering-context-base-handle, sampler: u64, pname: u64, param: u64);

    /// `getSamplerParameter()` operation.
    get-sampler-parameter: func(self: web-gl2-rendering-context-base-handle, sampler: u64, pname: u64) -> string;

    /// `fenceSync()` operation.
    fence-sync: func(self: web-gl2-rendering-context-base-handle, condition: u64, %flags: u64) -> option<u64>;

    /// `isSync()` operation.
    is-sync: func(self: web-gl2-rendering-context-base-handle, sync: option<u64>) -> u64;

    /// `deleteSync()` operation.
    delete-sync: func(self: web-gl2-rendering-context-base-handle, sync: option<u64>);

    /// `clientWaitSync()` operation.
    client-wait-sync: func(self: web-gl2-rendering-context-base-handle, sync: u64, %flags: u64, timeout: u64) -> u64;

    /// `waitSync()` operation.
    wait-sync: func(self: web-gl2-rendering-context-base-handle, sync: u64, %flags: u64, timeout: u64);

    /// `getSyncParameter()` operation.
    get-sync-parameter: func(self: web-gl2-rendering-context-base-handle, sync: u64, pname: u64) -> string;

    /// `createTransformFeedback()` operation.
    create-transform-feedback: func(self: web-gl2-rendering-context-base-handle) -> u64;

    /// `deleteTransformFeedback()` operation.
    delete-transform-feedback: func(self: web-gl2-rendering-context-base-handle, tf: option<u64>);

    /// `isTransformFeedback()` operation.
    is-transform-feedback: func(self: web-gl2-rendering-context-base-handle, tf: option<u64>) -> u64;

    /// `bindTransformFeedback()` operation.
    bind-transform-feedback: func(self: web-gl2-rendering-context-base-handle, target: u64, tf: option<u64>);

    /// `beginTransformFeedback()` operation.
    begin-transform-feedback: func(self: web-gl2-rendering-context-base-handle, primitive-mode: u64);

    /// `endTransformFeedback()` operation.
    end-transform-feedback: func(self: web-gl2-rendering-context-base-handle);

    /// `transformFeedbackVaryings()` operation.
    transform-feedback-varyings: func(self: web-gl2-rendering-context-base-handle, program: u64, varyings: list<string>, buffer-mode: u64);

    /// `getTransformFeedbackVarying()` operation.
    get-transform-feedback-varying: func(self: web-gl2-rendering-context-base-handle, program: u64, index: u64) -> option<u64>;

    /// `pauseTransformFeedback()` operation.
    pause-transform-feedback: func(self: web-gl2-rendering-context-base-handle);

    /// `resumeTransformFeedback()` operation.
    resume-transform-feedback: func(self: web-gl2-rendering-context-base-handle);

    /// `bindBufferBase()` operation.
    bind-buffer-base: func(self: web-gl2-rendering-context-base-handle, target: u64, index: u64, buffer: option<u64>);

    /// `bindBufferRange()` operation.
    bind-buffer-range: func(self: web-gl2-rendering-context-base-handle, target: u64, index: u64, buffer: option<u64>, offset: u64, size: u64);

    /// `getIndexedParameter()` operation.
    get-indexed-parameter: func(self: web-gl2-rendering-context-base-handle, target: u64, index: u64) -> string;

    /// `getUniformIndices()` operation.
    get-uniform-indices: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-names: list<string>) -> option<list<u64>>;

    /// `getActiveUniforms()` operation.
    get-active-uniforms: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-indices: list<u64>, pname: u64) -> string;

    /// `getUniformBlockIndex()` operation.
    get-uniform-block-index: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-block-name: string) -> u64;

    /// `getActiveUniformBlockParameter()` operation.
    get-active-uniform-block-parameter: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-block-index: u64, pname: u64) -> string;

    /// `getActiveUniformBlockName()` operation.
    get-active-uniform-block-name: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-block-index: u64) -> option<string>;

    /// `uniformBlockBinding()` operation.
    uniform-block-binding: func(self: web-gl2-rendering-context-base-handle, program: u64, uniform-block-index: u64, uniform-block-binding: u64);

    /// `createVertexArray()` operation.
    create-vertex-array: func(self: web-gl2-rendering-context-base-handle) -> u64;

    /// `deleteVertexArray()` operation.
    delete-vertex-array: func(self: web-gl2-rendering-context-base-handle, vertex-array: option<u64>);

    /// `isVertexArray()` operation.
    is-vertex-array: func(self: web-gl2-rendering-context-base-handle, vertex-array: option<u64>) -> u64;

    /// `bindVertexArray()` operation.
    bind-vertex-array: func(self: web-gl2-rendering-context-base-handle, array: option<u64>);
}

interface web-gl2-rendering-context-overloads {
    /// Opaque handle to a host-side `WebGL2RenderingContextOverloads` instance.
    type web-gl2-rendering-context-overloads-handle = u64;

    /// `bufferData()` operation.
    buffer-data: func(self: web-gl2-rendering-context-overloads-handle, target: u64, size: u64, usage: u64);

    /// `bufferSubData()` operation.
    buffer-sub-data: func(self: web-gl2-rendering-context-overloads-handle, target: u64, dst-byte-offset: u64, src-data: u64);

    /// `texImage2D()` operation.
    tex-image2-d: func(self: web-gl2-rendering-context-overloads-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, border: u64, format: u64, %type: u64, pixels: option<list<u8>>);

    /// `texSubImage2D()` operation.
    tex-sub-image2-d: func(self: web-gl2-rendering-context-overloads-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, width: u64, height: u64, format: u64, %type: u64, pixels: option<list<u8>>);

    /// `compressedTexImage2D()` operation.
    compressed-tex-image2-d: func(self: web-gl2-rendering-context-overloads-handle, target: u64, level: u64, internalformat: u64, width: u64, height: u64, border: u64, image-size: u64, offset: u64);

    /// `compressedTexSubImage2D()` operation.
    compressed-tex-sub-image2-d: func(self: web-gl2-rendering-context-overloads-handle, target: u64, level: u64, xoffset: u64, yoffset: u64, width: u64, height: u64, format: u64, image-size: u64, offset: u64);

    /// `uniform1fv()` operation.
    uniform1fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform2fv()` operation.
    uniform2fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform3fv()` operation.
    uniform3fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform4fv()` operation.
    uniform4fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform1iv()` operation.
    uniform1iv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform2iv()` operation.
    uniform2iv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform3iv()` operation.
    uniform3iv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniform4iv()` operation.
    uniform4iv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix2fv()` operation.
    uniform-matrix2fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix3fv()` operation.
    uniform-matrix3fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `uniformMatrix4fv()` operation.
    uniform-matrix4fv: func(self: web-gl2-rendering-context-overloads-handle, location: option<u64>, transpose: u64, data: u64, src-offset: option<u64>, src-length: option<u64>);

    /// `readPixels()` operation.
    read-pixels: func(self: web-gl2-rendering-context-overloads-handle, x: u64, y: u64, width: u64, height: u64, format: u64, %type: u64, dst-data: option<list<u8>>);
}

interface crypto {
    /// Opaque handle to a host-side `Crypto` instance.
    type crypto-handle = u64;

    /// `subtle` attribute — getter.
    get-subtle: func() -> u64;

    /// `getRandomValues()` operation.
    get-random-values: func(array: list<u8>) -> list<u8>;

    /// `randomUUID()` operation.
    random-uuid: func() -> string;
}

interface crypto-key {
    /// Opaque handle to a host-side `CryptoKey` instance.
    type crypto-key-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: crypto-key-handle) -> u64;

    /// `extractable` attribute — getter.
    get-extractable: func(self: crypto-key-handle) -> bool;

    /// `algorithm` attribute — getter.
    get-algorithm: func(self: crypto-key-handle) -> u64;

    /// `usages` attribute — getter.
    get-usages: func(self: crypto-key-handle) -> u64;
}

interface subtle-crypto {
    /// Opaque handle to a host-side `SubtleCrypto` instance.
    type subtle-crypto-handle = u64;

    /// `encrypt()` operation.
    encrypt: func(self: subtle-crypto-handle, algorithm: u64, key: u64, data: list<u8>) -> u64;

    /// `decrypt()` operation.
    decrypt: func(self: subtle-crypto-handle, algorithm: u64, key: u64, data: list<u8>) -> u64;

    /// `sign()` operation.
    sign: func(self: subtle-crypto-handle, algorithm: u64, key: u64, data: list<u8>) -> u64;

    /// `verify()` operation.
    verify: func(self: subtle-crypto-handle, algorithm: u64, key: u64, signature: list<u8>, data: list<u8>) -> u64;

    /// `digest()` operation.
    digest: func(self: subtle-crypto-handle, algorithm: u64, data: list<u8>) -> u64;

    /// `deriveKey()` operation.
    derive-key: func(self: subtle-crypto-handle, algorithm: u64, base-key: u64, derived-key-type: u64, extractable: bool, key-usages: list<u64>) -> u64;

    /// `deriveBits()` operation.
    derive-bits: func(self: subtle-crypto-handle, algorithm: u64, base-key: u64, length: option<u32>) -> u64;

    /// `importKey()` operation.
    import-key: func(self: subtle-crypto-handle, format: u64, key-data: list<u8>, algorithm: u64, extractable: bool, key-usages: list<u64>) -> u64;

    /// `wrapKey()` operation.
    wrap-key: func(self: subtle-crypto-handle, format: u64, key: u64, wrapping-key: u64, wrap-algorithm: u64) -> u64;

    /// `unwrapKey()` operation.
    unwrap-key: func(self: subtle-crypto-handle, format: u64, wrapped-key: list<u8>, unwrapping-key: u64, unwrap-algorithm: u64, unwrapped-key-algorithm: u64, extractable: bool, key-usages: list<u64>) -> u64;
}

interface animation-event {
    /// Opaque handle to a host-side `AnimationEvent` instance.
    type animation-event-handle = u64;

    /// `animationName` attribute — getter.
    get-animation-name: func(self: animation-event-handle) -> string;

    /// `elapsedTime` attribute — getter.
    get-elapsed-time: func(self: animation-event-handle) -> f64;

    /// `pseudoElement` attribute — getter.
    get-pseudo-element: func(self: animation-event-handle) -> string;
}

interface css-rule {
    /// Opaque handle to a host-side `CSSRule` instance.
    type css-rule-handle = u64;

    /// `cssText` attribute — getter.
    get-css-text: func(self: css-rule-handle) -> string;
    /// `cssText` attribute — setter.
    set-css-text: func(self: css-rule-handle, value: string);

    /// `parentRule` attribute — getter.
    get-parent-rule: func(self: css-rule-handle) -> option<u64>;

    /// `parentStyleSheet` attribute — getter.
    get-parent-style-sheet: func(self: css-rule-handle) -> option<u64>;

    /// `type` attribute — getter.
    get-type: func(self: css-rule-handle) -> u16;
}

interface css-keyframe-rule {
    /// Opaque handle to a host-side `CSSKeyframeRule` instance.
    type css-keyframe-rule-handle = u64;

    /// `keyText` attribute — getter.
    get-key-text: func(self: css-keyframe-rule-handle) -> string;
    /// `keyText` attribute — setter.
    set-key-text: func(self: css-keyframe-rule-handle, value: string);

    /// `style` attribute — getter.
    get-style: func(self: css-keyframe-rule-handle) -> u64;
}

interface css-keyframes-rule {
    /// Opaque handle to a host-side `CSSKeyframesRule` instance.
    type css-keyframes-rule-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: css-keyframes-rule-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: css-keyframes-rule-handle, value: string);

    /// `cssRules` attribute — getter.
    get-css-rules: func(self: css-keyframes-rule-handle) -> u64;

    /// `length` attribute — getter.
    get-length: func(self: css-keyframes-rule-handle) -> u32;

    /// `CSSKeyframeRule()` operation.
    css-keyframe-rule: func(self: css-keyframes-rule-handle, index: u32);

    /// `appendRule()` operation.
    append-rule: func(self: css-keyframes-rule-handle, rule: string);

    /// `deleteRule()` operation.
    delete-rule: func(self: css-keyframes-rule-handle, select: string);

    /// `findRule()` operation.
    find-rule: func(self: css-keyframes-rule-handle, select: string) -> option<u64>;
}

interface global-event-handlers {
    /// Opaque handle to a host-side `GlobalEventHandlers` instance.
    type global-event-handlers-handle = u64;

    /// `onanimationstart` attribute — getter.
    get-onanimationstart: func(self: global-event-handlers-handle) -> u64;
    /// `onanimationstart` attribute — setter.
    set-onanimationstart: func(self: global-event-handlers-handle, value: u64);

    /// `onanimationiteration` attribute — getter.
    get-onanimationiteration: func(self: global-event-handlers-handle) -> u64;
    /// `onanimationiteration` attribute — setter.
    set-onanimationiteration: func(self: global-event-handlers-handle, value: u64);

    /// `onanimationend` attribute — getter.
    get-onanimationend: func(self: global-event-handlers-handle) -> u64;
    /// `onanimationend` attribute — setter.
    set-onanimationend: func(self: global-event-handlers-handle, value: u64);

    /// `onanimationcancel` attribute — getter.
    get-onanimationcancel: func(self: global-event-handlers-handle) -> u64;
    /// `onanimationcancel` attribute — setter.
    set-onanimationcancel: func(self: global-event-handlers-handle, value: u64);

    /// `ontransitionrun` attribute — getter.
    get-ontransitionrun: func(self: global-event-handlers-handle) -> u64;
    /// `ontransitionrun` attribute — setter.
    set-ontransitionrun: func(self: global-event-handlers-handle, value: u64);

    /// `ontransitionstart` attribute — getter.
    get-ontransitionstart: func(self: global-event-handlers-handle) -> u64;
    /// `ontransitionstart` attribute — setter.
    set-ontransitionstart: func(self: global-event-handlers-handle, value: u64);

    /// `ontransitionend` attribute — getter.
    get-ontransitionend: func(self: global-event-handlers-handle) -> u64;
    /// `ontransitionend` attribute — setter.
    set-ontransitionend: func(self: global-event-handlers-handle, value: u64);

    /// `ontransitioncancel` attribute — getter.
    get-ontransitioncancel: func(self: global-event-handlers-handle) -> u64;
    /// `ontransitioncancel` attribute — setter.
    set-ontransitioncancel: func(self: global-event-handlers-handle, value: u64);

    /// `onabort` attribute — getter.
    get-onabort: func(self: global-event-handlers-handle) -> u64;
    /// `onabort` attribute — setter.
    set-onabort: func(self: global-event-handlers-handle, value: u64);

    /// `onauxclick` attribute — getter.
    get-onauxclick: func(self: global-event-handlers-handle) -> u64;
    /// `onauxclick` attribute — setter.
    set-onauxclick: func(self: global-event-handlers-handle, value: u64);

    /// `onbeforeinput` attribute — getter.
    get-onbeforeinput: func(self: global-event-handlers-handle) -> u64;
    /// `onbeforeinput` attribute — setter.
    set-onbeforeinput: func(self: global-event-handlers-handle, value: u64);

    /// `onbeforematch` attribute — getter.
    get-onbeforematch: func(self: global-event-handlers-handle) -> u64;
    /// `onbeforematch` attribute — setter.
    set-onbeforematch: func(self: global-event-handlers-handle, value: u64);

    /// `onbeforetoggle` attribute — getter.
    get-onbeforetoggle: func(self: global-event-handlers-handle) -> u64;
    /// `onbeforetoggle` attribute — setter.
    set-onbeforetoggle: func(self: global-event-handlers-handle, value: u64);

    /// `onblur` attribute — getter.
    get-onblur: func(self: global-event-handlers-handle) -> u64;
    /// `onblur` attribute — setter.
    set-onblur: func(self: global-event-handlers-handle, value: u64);

    /// `oncancel` attribute — getter.
    get-oncancel: func(self: global-event-handlers-handle) -> u64;
    /// `oncancel` attribute — setter.
    set-oncancel: func(self: global-event-handlers-handle, value: u64);

    /// `oncanplay` attribute — getter.
    get-oncanplay: func(self: global-event-handlers-handle) -> u64;
    /// `oncanplay` attribute — setter.
    set-oncanplay: func(self: global-event-handlers-handle, value: u64);

    /// `oncanplaythrough` attribute — getter.
    get-oncanplaythrough: func(self: global-event-handlers-handle) -> u64;
    /// `oncanplaythrough` attribute — setter.
    set-oncanplaythrough: func(self: global-event-handlers-handle, value: u64);

    /// `onchange` attribute — getter.
    get-onchange: func(self: global-event-handlers-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: global-event-handlers-handle, value: u64);

    /// `onclick` attribute — getter.
    get-onclick: func(self: global-event-handlers-handle) -> u64;
    /// `onclick` attribute — setter.
    set-onclick: func(self: global-event-handlers-handle, value: u64);

    /// `onclose` attribute — getter.
    get-onclose: func(self: global-event-handlers-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: global-event-handlers-handle, value: u64);

    /// `oncommand` attribute — getter.
    get-oncommand: func(self: global-event-handlers-handle) -> u64;
    /// `oncommand` attribute — setter.
    set-oncommand: func(self: global-event-handlers-handle, value: u64);

    /// `oncontextlost` attribute — getter.
    get-oncontextlost: func(self: global-event-handlers-handle) -> u64;
    /// `oncontextlost` attribute — setter.
    set-oncontextlost: func(self: global-event-handlers-handle, value: u64);

    /// `oncontextmenu` attribute — getter.
    get-oncontextmenu: func(self: global-event-handlers-handle) -> u64;
    /// `oncontextmenu` attribute — setter.
    set-oncontextmenu: func(self: global-event-handlers-handle, value: u64);

    /// `oncontextrestored` attribute — getter.
    get-oncontextrestored: func(self: global-event-handlers-handle) -> u64;
    /// `oncontextrestored` attribute — setter.
    set-oncontextrestored: func(self: global-event-handlers-handle, value: u64);

    /// `oncopy` attribute — getter.
    get-oncopy: func(self: global-event-handlers-handle) -> u64;
    /// `oncopy` attribute — setter.
    set-oncopy: func(self: global-event-handlers-handle, value: u64);

    /// `oncuechange` attribute — getter.
    get-oncuechange: func(self: global-event-handlers-handle) -> u64;
    /// `oncuechange` attribute — setter.
    set-oncuechange: func(self: global-event-handlers-handle, value: u64);

    /// `oncut` attribute — getter.
    get-oncut: func(self: global-event-handlers-handle) -> u64;
    /// `oncut` attribute — setter.
    set-oncut: func(self: global-event-handlers-handle, value: u64);

    /// `ondblclick` attribute — getter.
    get-ondblclick: func(self: global-event-handlers-handle) -> u64;
    /// `ondblclick` attribute — setter.
    set-ondblclick: func(self: global-event-handlers-handle, value: u64);

    /// `ondrag` attribute — getter.
    get-ondrag: func(self: global-event-handlers-handle) -> u64;
    /// `ondrag` attribute — setter.
    set-ondrag: func(self: global-event-handlers-handle, value: u64);

    /// `ondragend` attribute — getter.
    get-ondragend: func(self: global-event-handlers-handle) -> u64;
    /// `ondragend` attribute — setter.
    set-ondragend: func(self: global-event-handlers-handle, value: u64);

    /// `ondragenter` attribute — getter.
    get-ondragenter: func(self: global-event-handlers-handle) -> u64;
    /// `ondragenter` attribute — setter.
    set-ondragenter: func(self: global-event-handlers-handle, value: u64);

    /// `ondragleave` attribute — getter.
    get-ondragleave: func(self: global-event-handlers-handle) -> u64;
    /// `ondragleave` attribute — setter.
    set-ondragleave: func(self: global-event-handlers-handle, value: u64);

    /// `ondragover` attribute — getter.
    get-ondragover: func(self: global-event-handlers-handle) -> u64;
    /// `ondragover` attribute — setter.
    set-ondragover: func(self: global-event-handlers-handle, value: u64);

    /// `ondragstart` attribute — getter.
    get-ondragstart: func(self: global-event-handlers-handle) -> u64;
    /// `ondragstart` attribute — setter.
    set-ondragstart: func(self: global-event-handlers-handle, value: u64);

    /// `ondrop` attribute — getter.
    get-ondrop: func(self: global-event-handlers-handle) -> u64;
    /// `ondrop` attribute — setter.
    set-ondrop: func(self: global-event-handlers-handle, value: u64);

    /// `ondurationchange` attribute — getter.
    get-ondurationchange: func(self: global-event-handlers-handle) -> u64;
    /// `ondurationchange` attribute — setter.
    set-ondurationchange: func(self: global-event-handlers-handle, value: u64);

    /// `onemptied` attribute — getter.
    get-onemptied: func(self: global-event-handlers-handle) -> u64;
    /// `onemptied` attribute — setter.
    set-onemptied: func(self: global-event-handlers-handle, value: u64);

    /// `onended` attribute — getter.
    get-onended: func(self: global-event-handlers-handle) -> u64;
    /// `onended` attribute — setter.
    set-onended: func(self: global-event-handlers-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: global-event-handlers-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: global-event-handlers-handle, value: u64);

    /// `onfocus` attribute — getter.
    get-onfocus: func(self: global-event-handlers-handle) -> u64;
    /// `onfocus` attribute — setter.
    set-onfocus: func(self: global-event-handlers-handle, value: u64);

    /// `onformdata` attribute — getter.
    get-onformdata: func(self: global-event-handlers-handle) -> u64;
    /// `onformdata` attribute — setter.
    set-onformdata: func(self: global-event-handlers-handle, value: u64);

    /// `oninput` attribute — getter.
    get-oninput: func(self: global-event-handlers-handle) -> u64;
    /// `oninput` attribute — setter.
    set-oninput: func(self: global-event-handlers-handle, value: u64);

    /// `oninvalid` attribute — getter.
    get-oninvalid: func(self: global-event-handlers-handle) -> u64;
    /// `oninvalid` attribute — setter.
    set-oninvalid: func(self: global-event-handlers-handle, value: u64);

    /// `onkeydown` attribute — getter.
    get-onkeydown: func(self: global-event-handlers-handle) -> u64;
    /// `onkeydown` attribute — setter.
    set-onkeydown: func(self: global-event-handlers-handle, value: u64);

    /// `onkeypress` attribute — getter.
    get-onkeypress: func(self: global-event-handlers-handle) -> u64;
    /// `onkeypress` attribute — setter.
    set-onkeypress: func(self: global-event-handlers-handle, value: u64);

    /// `onkeyup` attribute — getter.
    get-onkeyup: func(self: global-event-handlers-handle) -> u64;
    /// `onkeyup` attribute — setter.
    set-onkeyup: func(self: global-event-handlers-handle, value: u64);

    /// `onload` attribute — getter.
    get-onload: func(self: global-event-handlers-handle) -> u64;
    /// `onload` attribute — setter.
    set-onload: func(self: global-event-handlers-handle, value: u64);

    /// `onloadeddata` attribute — getter.
    get-onloadeddata: func(self: global-event-handlers-handle) -> u64;
    /// `onloadeddata` attribute — setter.
    set-onloadeddata: func(self: global-event-handlers-handle, value: u64);

    /// `onloadedmetadata` attribute — getter.
    get-onloadedmetadata: func(self: global-event-handlers-handle) -> u64;
    /// `onloadedmetadata` attribute — setter.
    set-onloadedmetadata: func(self: global-event-handlers-handle, value: u64);

    /// `onloadstart` attribute — getter.
    get-onloadstart: func(self: global-event-handlers-handle) -> u64;
    /// `onloadstart` attribute — setter.
    set-onloadstart: func(self: global-event-handlers-handle, value: u64);

    /// `onmousedown` attribute — getter.
    get-onmousedown: func(self: global-event-handlers-handle) -> u64;
    /// `onmousedown` attribute — setter.
    set-onmousedown: func(self: global-event-handlers-handle, value: u64);

    /// `onmouseenter` attribute — getter.
    get-onmouseenter: func(self: global-event-handlers-handle) -> u64;
    /// `onmouseenter` attribute — setter.
    set-onmouseenter: func(self: global-event-handlers-handle, value: u64);

    /// `onmouseleave` attribute — getter.
    get-onmouseleave: func(self: global-event-handlers-handle) -> u64;
    /// `onmouseleave` attribute — setter.
    set-onmouseleave: func(self: global-event-handlers-handle, value: u64);

    /// `onmousemove` attribute — getter.
    get-onmousemove: func(self: global-event-handlers-handle) -> u64;
    /// `onmousemove` attribute — setter.
    set-onmousemove: func(self: global-event-handlers-handle, value: u64);

    /// `onmouseout` attribute — getter.
    get-onmouseout: func(self: global-event-handlers-handle) -> u64;
    /// `onmouseout` attribute — setter.
    set-onmouseout: func(self: global-event-handlers-handle, value: u64);

    /// `onmouseover` attribute — getter.
    get-onmouseover: func(self: global-event-handlers-handle) -> u64;
    /// `onmouseover` attribute — setter.
    set-onmouseover: func(self: global-event-handlers-handle, value: u64);

    /// `onmouseup` attribute — getter.
    get-onmouseup: func(self: global-event-handlers-handle) -> u64;
    /// `onmouseup` attribute — setter.
    set-onmouseup: func(self: global-event-handlers-handle, value: u64);

    /// `onpaste` attribute — getter.
    get-onpaste: func(self: global-event-handlers-handle) -> u64;
    /// `onpaste` attribute — setter.
    set-onpaste: func(self: global-event-handlers-handle, value: u64);

    /// `onpause` attribute — getter.
    get-onpause: func(self: global-event-handlers-handle) -> u64;
    /// `onpause` attribute — setter.
    set-onpause: func(self: global-event-handlers-handle, value: u64);

    /// `onplay` attribute — getter.
    get-onplay: func(self: global-event-handlers-handle) -> u64;
    /// `onplay` attribute — setter.
    set-onplay: func(self: global-event-handlers-handle, value: u64);

    /// `onplaying` attribute — getter.
    get-onplaying: func(self: global-event-handlers-handle) -> u64;
    /// `onplaying` attribute — setter.
    set-onplaying: func(self: global-event-handlers-handle, value: u64);

    /// `onprogress` attribute — getter.
    get-onprogress: func(self: global-event-handlers-handle) -> u64;
    /// `onprogress` attribute — setter.
    set-onprogress: func(self: global-event-handlers-handle, value: u64);

    /// `onratechange` attribute — getter.
    get-onratechange: func(self: global-event-handlers-handle) -> u64;
    /// `onratechange` attribute — setter.
    set-onratechange: func(self: global-event-handlers-handle, value: u64);

    /// `onreset` attribute — getter.
    get-onreset: func(self: global-event-handlers-handle) -> u64;
    /// `onreset` attribute — setter.
    set-onreset: func(self: global-event-handlers-handle, value: u64);

    /// `onresize` attribute — getter.
    get-onresize: func(self: global-event-handlers-handle) -> u64;
    /// `onresize` attribute — setter.
    set-onresize: func(self: global-event-handlers-handle, value: u64);

    /// `onscroll` attribute — getter.
    get-onscroll: func(self: global-event-handlers-handle) -> u64;
    /// `onscroll` attribute — setter.
    set-onscroll: func(self: global-event-handlers-handle, value: u64);

    /// `onscrollend` attribute — getter.
    get-onscrollend: func(self: global-event-handlers-handle) -> u64;
    /// `onscrollend` attribute — setter.
    set-onscrollend: func(self: global-event-handlers-handle, value: u64);

    /// `onsecuritypolicyviolation` attribute — getter.
    get-onsecuritypolicyviolation: func(self: global-event-handlers-handle) -> u64;
    /// `onsecuritypolicyviolation` attribute — setter.
    set-onsecuritypolicyviolation: func(self: global-event-handlers-handle, value: u64);

    /// `onseeked` attribute — getter.
    get-onseeked: func(self: global-event-handlers-handle) -> u64;
    /// `onseeked` attribute — setter.
    set-onseeked: func(self: global-event-handlers-handle, value: u64);

    /// `onseeking` attribute — getter.
    get-onseeking: func(self: global-event-handlers-handle) -> u64;
    /// `onseeking` attribute — setter.
    set-onseeking: func(self: global-event-handlers-handle, value: u64);

    /// `onselect` attribute — getter.
    get-onselect: func(self: global-event-handlers-handle) -> u64;
    /// `onselect` attribute — setter.
    set-onselect: func(self: global-event-handlers-handle, value: u64);

    /// `onslotchange` attribute — getter.
    get-onslotchange: func(self: global-event-handlers-handle) -> u64;
    /// `onslotchange` attribute — setter.
    set-onslotchange: func(self: global-event-handlers-handle, value: u64);

    /// `onstalled` attribute — getter.
    get-onstalled: func(self: global-event-handlers-handle) -> u64;
    /// `onstalled` attribute — setter.
    set-onstalled: func(self: global-event-handlers-handle, value: u64);

    /// `onsubmit` attribute — getter.
    get-onsubmit: func(self: global-event-handlers-handle) -> u64;
    /// `onsubmit` attribute — setter.
    set-onsubmit: func(self: global-event-handlers-handle, value: u64);

    /// `onsuspend` attribute — getter.
    get-onsuspend: func(self: global-event-handlers-handle) -> u64;
    /// `onsuspend` attribute — setter.
    set-onsuspend: func(self: global-event-handlers-handle, value: u64);

    /// `ontimeupdate` attribute — getter.
    get-ontimeupdate: func(self: global-event-handlers-handle) -> u64;
    /// `ontimeupdate` attribute — setter.
    set-ontimeupdate: func(self: global-event-handlers-handle, value: u64);

    /// `ontoggle` attribute — getter.
    get-ontoggle: func(self: global-event-handlers-handle) -> u64;
    /// `ontoggle` attribute — setter.
    set-ontoggle: func(self: global-event-handlers-handle, value: u64);

    /// `onvolumechange` attribute — getter.
    get-onvolumechange: func(self: global-event-handlers-handle) -> u64;
    /// `onvolumechange` attribute — setter.
    set-onvolumechange: func(self: global-event-handlers-handle, value: u64);

    /// `onwaiting` attribute — getter.
    get-onwaiting: func(self: global-event-handlers-handle) -> u64;
    /// `onwaiting` attribute — setter.
    set-onwaiting: func(self: global-event-handlers-handle, value: u64);

    /// `onwebkitanimationend` attribute — getter.
    get-onwebkitanimationend: func(self: global-event-handlers-handle) -> u64;
    /// `onwebkitanimationend` attribute — setter.
    set-onwebkitanimationend: func(self: global-event-handlers-handle, value: u64);

    /// `onwebkitanimationiteration` attribute — getter.
    get-onwebkitanimationiteration: func(self: global-event-handlers-handle) -> u64;
    /// `onwebkitanimationiteration` attribute — setter.
    set-onwebkitanimationiteration: func(self: global-event-handlers-handle, value: u64);

    /// `onwebkitanimationstart` attribute — getter.
    get-onwebkitanimationstart: func(self: global-event-handlers-handle) -> u64;
    /// `onwebkitanimationstart` attribute — setter.
    set-onwebkitanimationstart: func(self: global-event-handlers-handle, value: u64);

    /// `onwebkittransitionend` attribute — getter.
    get-onwebkittransitionend: func(self: global-event-handlers-handle) -> u64;
    /// `onwebkittransitionend` attribute — setter.
    set-onwebkittransitionend: func(self: global-event-handlers-handle, value: u64);

    /// `onwheel` attribute — getter.
    get-onwheel: func(self: global-event-handlers-handle) -> u64;
    /// `onwheel` attribute — setter.
    set-onwheel: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerover` attribute — getter.
    get-onpointerover: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerover` attribute — setter.
    set-onpointerover: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerenter` attribute — getter.
    get-onpointerenter: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerenter` attribute — setter.
    set-onpointerenter: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerdown` attribute — getter.
    get-onpointerdown: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerdown` attribute — setter.
    set-onpointerdown: func(self: global-event-handlers-handle, value: u64);

    /// `onpointermove` attribute — getter.
    get-onpointermove: func(self: global-event-handlers-handle) -> u64;
    /// `onpointermove` attribute — setter.
    set-onpointermove: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerrawupdate` attribute — getter.
    get-onpointerrawupdate: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerrawupdate` attribute — setter.
    set-onpointerrawupdate: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerup` attribute — getter.
    get-onpointerup: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerup` attribute — setter.
    set-onpointerup: func(self: global-event-handlers-handle, value: u64);

    /// `onpointercancel` attribute — getter.
    get-onpointercancel: func(self: global-event-handlers-handle) -> u64;
    /// `onpointercancel` attribute — setter.
    set-onpointercancel: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerout` attribute — getter.
    get-onpointerout: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerout` attribute — setter.
    set-onpointerout: func(self: global-event-handlers-handle, value: u64);

    /// `onpointerleave` attribute — getter.
    get-onpointerleave: func(self: global-event-handlers-handle) -> u64;
    /// `onpointerleave` attribute — setter.
    set-onpointerleave: func(self: global-event-handlers-handle, value: u64);

    /// `ongotpointercapture` attribute — getter.
    get-ongotpointercapture: func(self: global-event-handlers-handle) -> u64;
    /// `ongotpointercapture` attribute — setter.
    set-ongotpointercapture: func(self: global-event-handlers-handle, value: u64);

    /// `onlostpointercapture` attribute — getter.
    get-onlostpointercapture: func(self: global-event-handlers-handle) -> u64;
    /// `onlostpointercapture` attribute — setter.
    set-onlostpointercapture: func(self: global-event-handlers-handle, value: u64);

    /// `onselectstart` attribute — getter.
    get-onselectstart: func(self: global-event-handlers-handle) -> u64;
    /// `onselectstart` attribute — setter.
    set-onselectstart: func(self: global-event-handlers-handle, value: u64);

    /// `onselectionchange` attribute — getter.
    get-onselectionchange: func(self: global-event-handlers-handle) -> u64;
    /// `onselectionchange` attribute — setter.
    set-onselectionchange: func(self: global-event-handlers-handle, value: u64);

    /// `ontouchstart` attribute — getter.
    get-ontouchstart: func(self: global-event-handlers-handle) -> u64;
    /// `ontouchstart` attribute — setter.
    set-ontouchstart: func(self: global-event-handlers-handle, value: u64);

    /// `ontouchend` attribute — getter.
    get-ontouchend: func(self: global-event-handlers-handle) -> u64;
    /// `ontouchend` attribute — setter.
    set-ontouchend: func(self: global-event-handlers-handle, value: u64);

    /// `ontouchmove` attribute — getter.
    get-ontouchmove: func(self: global-event-handlers-handle) -> u64;
    /// `ontouchmove` attribute — setter.
    set-ontouchmove: func(self: global-event-handlers-handle, value: u64);

    /// `ontouchcancel` attribute — getter.
    get-ontouchcancel: func(self: global-event-handlers-handle) -> u64;
    /// `ontouchcancel` attribute — setter.
    set-ontouchcancel: func(self: global-event-handlers-handle, value: u64);
}

interface css-condition-rule {
    /// Opaque handle to a host-side `CSSConditionRule` instance.
    type css-condition-rule-handle = u64;

    /// `conditionText` attribute — getter.
    get-condition-text: func(self: css-condition-rule-handle) -> string;
}

interface css-media-rule {
    /// Opaque handle to a host-side `CSSMediaRule` instance.
    type css-media-rule-handle = u64;

    /// `media` attribute — getter.
    get-media: func(self: css-media-rule-handle) -> u64;

    /// `matches` attribute — getter.
    get-matches: func(self: css-media-rule-handle) -> bool;
}

interface css-supports-rule {
    /// Opaque handle to a host-side `CSSSupportsRule` instance.
    type css-supports-rule-handle = u64;

    /// `matches` attribute — getter.
    get-matches: func(self: css-supports-rule-handle) -> bool;
}

interface css-font-face-descriptors {
    /// Opaque handle to a host-side `CSSFontFaceDescriptors` instance.
    type css-font-face-descriptors-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: css-font-face-descriptors-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontFamily` attribute — getter.
    get-font-family: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontFamily` attribute — setter.
    set-font-family: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontStyle` attribute — getter.
    get-font-style: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontStyle` attribute — setter.
    set-font-style: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontWeight` attribute — getter.
    get-font-weight: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontWeight` attribute — setter.
    set-font-weight: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontStretch` attribute — getter.
    get-font-stretch: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontStretch` attribute — setter.
    set-font-stretch: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontWidth` attribute — getter.
    get-font-width: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontWidth` attribute — setter.
    set-font-width: func(self: css-font-face-descriptors-handle, value: string);

    /// `unicodeRange` attribute — getter.
    get-unicode-range: func(self: css-font-face-descriptors-handle) -> string;
    /// `unicodeRange` attribute — setter.
    set-unicode-range: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontFeatureSettings` attribute — getter.
    get-font-feature-settings: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontFeatureSettings` attribute — setter.
    set-font-feature-settings: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontVariationSettings` attribute — getter.
    get-font-variation-settings: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontVariationSettings` attribute — setter.
    set-font-variation-settings: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontNamedInstance` attribute — getter.
    get-font-named-instance: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontNamedInstance` attribute — setter.
    set-font-named-instance: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontDisplay` attribute — getter.
    get-font-display: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontDisplay` attribute — setter.
    set-font-display: func(self: css-font-face-descriptors-handle, value: string);

    /// `fontLanguageOverride` attribute — getter.
    get-font-language-override: func(self: css-font-face-descriptors-handle) -> string;
    /// `fontLanguageOverride` attribute — setter.
    set-font-language-override: func(self: css-font-face-descriptors-handle, value: string);

    /// `ascentOverride` attribute — getter.
    get-ascent-override: func(self: css-font-face-descriptors-handle) -> string;
    /// `ascentOverride` attribute — setter.
    set-ascent-override: func(self: css-font-face-descriptors-handle, value: string);

    /// `descentOverride` attribute — getter.
    get-descent-override: func(self: css-font-face-descriptors-handle) -> string;
    /// `descentOverride` attribute — setter.
    set-descent-override: func(self: css-font-face-descriptors-handle, value: string);

    /// `lineGapOverride` attribute — getter.
    get-line-gap-override: func(self: css-font-face-descriptors-handle) -> string;
    /// `lineGapOverride` attribute — setter.
    set-line-gap-override: func(self: css-font-face-descriptors-handle, value: string);
}

interface css-font-face-rule {
    /// Opaque handle to a host-side `CSSFontFaceRule` instance.
    type css-font-face-rule-handle = u64;

    /// `style` attribute — getter.
    get-style: func(self: css-font-face-rule-handle) -> u64;
}

interface css-font-feature-values-rule {
    /// Opaque handle to a host-side `CSSFontFeatureValuesRule` instance.
    type css-font-feature-values-rule-handle = u64;

    /// `fontFamily` attribute — getter.
    get-font-family: func(self: css-font-feature-values-rule-handle) -> string;
    /// `fontFamily` attribute — setter.
    set-font-family: func(self: css-font-feature-values-rule-handle, value: string);

    /// `annotation` attribute — getter.
    get-annotation: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `ornaments` attribute — getter.
    get-ornaments: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `stylistic` attribute — getter.
    get-stylistic: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `swash` attribute — getter.
    get-swash: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `characterVariant` attribute — getter.
    get-character-variant: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `styleset` attribute — getter.
    get-styleset: func(self: css-font-feature-values-rule-handle) -> u64;

    /// `historicalForms` attribute — getter.
    get-historical-forms: func(self: css-font-feature-values-rule-handle) -> u64;
}

interface css-font-feature-values-map {
    /// Opaque handle to a host-side `CSSFontFeatureValuesMap` instance.
    type css-font-feature-values-map-handle = u64;

    /// `set()` operation.
    set: func(self: css-font-feature-values-map-handle, feature-value-name: string, values: f64);
}

interface css-font-palette-values-rule {
    /// Opaque handle to a host-side `CSSFontPaletteValuesRule` instance.
    type css-font-palette-values-rule-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: css-font-palette-values-rule-handle) -> string;

    /// `fontFamily` attribute — getter.
    get-font-family: func(self: css-font-palette-values-rule-handle) -> string;

    /// `basePalette` attribute — getter.
    get-base-palette: func(self: css-font-palette-values-rule-handle) -> string;

    /// `overrideColors` attribute — getter.
    get-override-colors: func(self: css-font-palette-values-rule-handle) -> string;
}

interface transition-event {
    /// Opaque handle to a host-side `TransitionEvent` instance.
    type transition-event-handle = u64;

    /// `propertyName` attribute — getter.
    get-property-name: func(self: transition-event-handle) -> string;

    /// `elapsedTime` attribute — getter.
    get-elapsed-time: func(self: transition-event-handle) -> f64;

    /// `pseudoElement` attribute — getter.
    get-pseudo-element: func(self: transition-event-handle) -> string;
}

interface window {
    /// Opaque handle to a host-side `Window` instance.
    type window-handle = u64;

    /// `matchMedia()` operation.
    match-media: func(query: string) -> u64;

    /// `screen` attribute — getter.
    get-screen: func() -> u64;

    /// `visualViewport` attribute — getter.
    get-visual-viewport: func() -> option<u64>;

    /// `moveTo()` operation.
    move-to: func(x: s32, y: s32);

    /// `moveBy()` operation.
    move-by: func(x: s32, y: s32);

    /// `resizeTo()` operation.
    resize-to: func(width: s32, height: s32);

    /// `resizeBy()` operation.
    resize-by: func(x: s32, y: s32);

    /// `innerWidth` attribute — getter.
    get-inner-width: func() -> s32;

    /// `innerHeight` attribute — getter.
    get-inner-height: func() -> s32;

    /// `scrollX` attribute — getter.
    get-scroll-x: func() -> f64;

    /// `pageXOffset` attribute — getter.
    get-page-x-offset: func() -> f64;

    /// `scrollY` attribute — getter.
    get-scroll-y: func() -> f64;

    /// `pageYOffset` attribute — getter.
    get-page-y-offset: func() -> f64;

    /// `scroll()` operation.
    scroll: func(options: option<u64>) -> u64;

    /// `scrollTo()` operation.
    scroll-to: func(options: option<u64>) -> u64;

    /// `scrollBy()` operation.
    scroll-by: func(options: option<u64>) -> u64;

    /// `screenX` attribute — getter.
    get-screen-x: func() -> s32;

    /// `screenLeft` attribute — getter.
    get-screen-left: func() -> s32;

    /// `screenY` attribute — getter.
    get-screen-y: func() -> s32;

    /// `screenTop` attribute — getter.
    get-screen-top: func() -> s32;

    /// `outerWidth` attribute — getter.
    get-outer-width: func() -> s32;

    /// `outerHeight` attribute — getter.
    get-outer-height: func() -> s32;

    /// `devicePixelRatio` attribute — getter.
    get-device-pixel-ratio: func() -> f64;

    /// `getComputedStyle()` operation.
    get-computed-style: func(elt: u64, pseudo-elt: option<string>) -> u64;

    /// `event` attribute — getter.
    get-event: func() -> u64;

    /// `fetchLater()` operation.
    fetch-later: func(input: u64, init: option<u64>) -> u64;

    /// `window` attribute — getter.
    get-window: func() -> u64;

    /// `self` attribute — getter.
    get-self: func() -> u64;

    /// `document` attribute — getter.
    get-document: func() -> u64;

    /// `name` attribute — getter.
    get-name: func() -> string;
    /// `name` attribute — setter.
    set-name: func(value: string);

    /// `location` attribute — getter.
    get-location: func() -> u64;

    /// `history` attribute — getter.
    get-history: func() -> u64;

    /// `navigation` attribute — getter.
    get-navigation: func() -> u64;

    /// `customElements` attribute — getter.
    get-custom-elements: func() -> u64;

    /// `locationbar` attribute — getter.
    get-locationbar: func() -> u64;

    /// `menubar` attribute — getter.
    get-menubar: func() -> u64;

    /// `personalbar` attribute — getter.
    get-personalbar: func() -> u64;

    /// `scrollbars` attribute — getter.
    get-scrollbars: func() -> u64;

    /// `statusbar` attribute — getter.
    get-statusbar: func() -> u64;

    /// `toolbar` attribute — getter.
    get-toolbar: func() -> u64;

    /// `status` attribute — getter.
    get-status: func() -> string;
    /// `status` attribute — setter.
    set-status: func(value: string);

    /// `close()` operation.
    close: func();

    /// `closed` attribute — getter.
    get-closed: func() -> bool;

    /// `stop()` operation.
    stop: func();

    /// `focus()` operation.
    focus: func();

    /// `blur()` operation.
    blur: func();

    /// `frames` attribute — getter.
    get-frames: func() -> u64;

    /// `length` attribute — getter.
    get-length: func() -> u32;

    /// `top` attribute — getter.
    get-top: func() -> option<u64>;

    /// `opener` attribute — getter.
    get-opener: func() -> string;
    /// `opener` attribute — setter.
    set-opener: func(value: string);

    /// `parent` attribute — getter.
    get-parent: func() -> option<u64>;

    /// `frameElement` attribute — getter.
    get-frame-element: func() -> option<u64>;

    /// `open()` operation.
    open: func(url: option<string>, target: option<string>, features: option<string>) -> option<u64>;

    /// `object()` operation.
    object: func(name: string);

    /// `navigator` attribute — getter.
    get-navigator: func() -> u64;

    /// `clientInformation` attribute — getter.
    get-client-information: func() -> u64;

    /// `originAgentCluster` attribute — getter.
    get-origin-agent-cluster: func() -> bool;

    /// `alert()` operation.
    alert: func();

    /// `confirm()` operation.
    confirm: func(message: option<string>) -> bool;

    /// `prompt()` operation.
    prompt: func(message: option<string>, default: option<string>) -> option<string>;

    /// `print()` operation.
    print: func();

    /// `postMessage()` operation.
    post-message: func(message: string, target-origin: string, transfer: option<list<u64>>);

    /// `captureEvents()` operation.
    capture-events: func();

    /// `releaseEvents()` operation.
    release-events: func();

    /// `external` attribute — getter.
    get-external: func() -> u64;

    /// `getSelection()` operation.
    get-selection: func() -> option<u64>;

    /// `speechSynthesis` attribute — getter.
    get-speech-synthesis: func() -> u64;
}

interface media-query-list {
    /// Opaque handle to a host-side `MediaQueryList` instance.
    type media-query-list-handle = u64;

    /// `media` attribute — getter.
    get-media: func(self: media-query-list-handle) -> string;

    /// `matches` attribute — getter.
    get-matches: func(self: media-query-list-handle) -> bool;

    /// `addListener()` operation.
    add-listener: func(self: media-query-list-handle, callback: option<u64>);

    /// `removeListener()` operation.
    remove-listener: func(self: media-query-list-handle, callback: option<u64>);

    /// `onchange` attribute — getter.
    get-onchange: func(self: media-query-list-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: media-query-list-handle, value: u64);
}

interface media-query-list-event {
    /// Opaque handle to a host-side `MediaQueryListEvent` instance.
    type media-query-list-event-handle = u64;

    /// `media` attribute — getter.
    get-media: func(self: media-query-list-event-handle) -> string;

    /// `matches` attribute — getter.
    get-matches: func(self: media-query-list-event-handle) -> bool;
}

interface screen {
    /// Opaque handle to a host-side `Screen` instance.
    type screen-handle = u64;

    /// `availWidth` attribute — getter.
    get-avail-width: func() -> s32;

    /// `availHeight` attribute — getter.
    get-avail-height: func() -> s32;

    /// `width` attribute — getter.
    get-width: func() -> s32;

    /// `height` attribute — getter.
    get-height: func() -> s32;

    /// `colorDepth` attribute — getter.
    get-color-depth: func() -> u32;

    /// `pixelDepth` attribute — getter.
    get-pixel-depth: func() -> u32;

    /// `orientation` attribute — getter.
    get-orientation: func() -> u64;
}

interface caret-position {
    /// Opaque handle to a host-side `CaretPosition` instance.

    use types.{dom-rect};

    type caret-position-handle = u64;

    /// `offsetNode` attribute — getter.
    get-offset-node: func(self: caret-position-handle) -> u64;

    /// `offset` attribute — getter.
    get-offset: func(self: caret-position-handle) -> u32;

    /// `getClientRect()` operation.
    get-client-rect: func(self: caret-position-handle) -> option<dom-rect>;
}

interface element {
    /// Opaque handle to a host-side `Element` instance.

    use types.{dom-rect};

    type element-handle = u64;

    /// `getClientRects()` operation.
    get-client-rects: func(self: element-handle) -> u64;

    /// `getBoundingClientRect()` operation.
    get-bounding-client-rect: func(self: element-handle) -> dom-rect;

    /// `checkVisibility()` operation.
    check-visibility: func(self: element-handle, options: option<u64>) -> bool;

    /// `scrollIntoView()` operation.
    scroll-into-view: func(self: element-handle, arg: option<bool>) -> u64;

    /// `scroll()` operation.
    scroll: func(self: element-handle, options: option<u64>) -> u64;

    /// `scrollTo()` operation.
    scroll-to: func(self: element-handle, options: option<u64>) -> u64;

    /// `scrollBy()` operation.
    scroll-by: func(self: element-handle, options: option<u64>) -> u64;

    /// `scrollTop` attribute — getter.
    get-scroll-top: func(self: element-handle) -> f64;
    /// `scrollTop` attribute — setter.
    set-scroll-top: func(self: element-handle, value: f64);

    /// `scrollLeft` attribute — getter.
    get-scroll-left: func(self: element-handle) -> f64;
    /// `scrollLeft` attribute — setter.
    set-scroll-left: func(self: element-handle, value: f64);

    /// `scrollWidth` attribute — getter.
    get-scroll-width: func(self: element-handle) -> s32;

    /// `scrollHeight` attribute — getter.
    get-scroll-height: func(self: element-handle) -> s32;

    /// `clientTop` attribute — getter.
    get-client-top: func(self: element-handle) -> s32;

    /// `clientLeft` attribute — getter.
    get-client-left: func(self: element-handle) -> s32;

    /// `clientWidth` attribute — getter.
    get-client-width: func(self: element-handle) -> s32;

    /// `clientHeight` attribute — getter.
    get-client-height: func(self: element-handle) -> s32;

    /// `currentCSSZoom` attribute — getter.
    get-current-css-zoom: func(self: element-handle) -> f64;

    /// `namespaceURI` attribute — getter.
    get-namespace-uri: func(self: element-handle) -> option<string>;

    /// `prefix` attribute — getter.
    get-prefix: func(self: element-handle) -> option<string>;

    /// `localName` attribute — getter.
    get-local-name: func(self: element-handle) -> string;

    /// `tagName` attribute — getter.
    get-tag-name: func(self: element-handle) -> string;

    /// `id` attribute — getter.
    get-id: func(self: element-handle) -> string;
    /// `id` attribute — setter.
    set-id: func(self: element-handle, value: string);

    /// `className` attribute — getter.
    get-class-name: func(self: element-handle) -> string;
    /// `className` attribute — setter.
    set-class-name: func(self: element-handle, value: string);

    /// `classList` attribute — getter.
    get-class-list: func(self: element-handle) -> u64;

    /// `slot` attribute — getter.
    get-slot: func(self: element-handle) -> string;
    /// `slot` attribute — setter.
    set-slot: func(self: element-handle, value: string);

    /// `hasAttributes()` operation.
    has-attributes: func(self: element-handle) -> bool;

    /// `attributes` attribute — getter.
    get-attributes: func(self: element-handle) -> u64;

    /// `getAttributeNames()` operation.
    get-attribute-names: func(self: element-handle) -> list<string>;

    /// `getAttribute()` operation.
    get-attribute: func(self: element-handle, qualified-name: string) -> option<string>;

    /// `getAttributeNS()` operation.
    get-attribute-ns: func(self: element-handle, namespace: option<string>, local-name: string) -> option<string>;

    /// `setAttribute()` operation.
    set-attribute: func(self: element-handle, qualified-name: string, value: string);

    /// `setAttributeNS()` operation.
    set-attribute-ns: func(self: element-handle, namespace: option<string>, qualified-name: string, value: string);

    /// `removeAttribute()` operation.
    remove-attribute: func(self: element-handle, qualified-name: string);

    /// `removeAttributeNS()` operation.
    remove-attribute-ns: func(self: element-handle, namespace: option<string>, local-name: string);

    /// `toggleAttribute()` operation.
    toggle-attribute: func(self: element-handle, qualified-name: string, force: option<bool>) -> bool;

    /// `hasAttribute()` operation.
    has-attribute: func(self: element-handle, qualified-name: string) -> bool;

    /// `hasAttributeNS()` operation.
    has-attribute-ns: func(self: element-handle, namespace: option<string>, local-name: string) -> bool;

    /// `getAttributeNode()` operation.
    get-attribute-node: func(self: element-handle, qualified-name: string) -> option<u64>;

    /// `getAttributeNodeNS()` operation.
    get-attribute-node-ns: func(self: element-handle, namespace: option<string>, local-name: string) -> option<u64>;

    /// `setAttributeNode()` operation.
    set-attribute-node: func(self: element-handle, attr: u64) -> option<u64>;

    /// `setAttributeNodeNS()` operation.
    set-attribute-node-ns: func(self: element-handle, attr: u64) -> option<u64>;

    /// `removeAttributeNode()` operation.
    remove-attribute-node: func(self: element-handle, attr: u64) -> u64;

    /// `attachShadow()` operation.
    attach-shadow: func(self: element-handle, init: u64) -> u64;

    /// `shadowRoot` attribute — getter.
    get-shadow-root: func(self: element-handle) -> option<u64>;

    /// `customElementRegistry` attribute — getter.
    get-custom-element-registry: func(self: element-handle) -> option<u64>;

    /// `closest()` operation.
    closest: func(self: element-handle, selectors: string) -> option<u64>;

    /// `matches()` operation.
    matches: func(self: element-handle, selectors: string) -> bool;

    /// `webkitMatchesSelector()` operation.
    webkit-matches-selector: func(self: element-handle, selectors: string) -> bool;

    /// `getElementsByTagName()` operation.
    get-elements-by-tag-name: func(self: element-handle, qualified-name: string) -> u64;

    /// `getElementsByTagNameNS()` operation.
    get-elements-by-tag-name-ns: func(self: element-handle, namespace: option<string>, local-name: string) -> u64;

    /// `getElementsByClassName()` operation.
    get-elements-by-class-name: func(self: element-handle, class-names: string) -> u64;

    /// `insertAdjacentElement()` operation.
    insert-adjacent-element: func(self: element-handle, %where: string, element: u64) -> option<u64>;

    /// `insertAdjacentText()` operation.
    insert-adjacent-text: func(self: element-handle, %where: string, data: string);

    /// `requestFullscreen()` operation.
    request-fullscreen: func(self: element-handle, options: option<u64>) -> u64;

    /// `onfullscreenchange` attribute — getter.
    get-onfullscreenchange: func(self: element-handle) -> u64;
    /// `onfullscreenchange` attribute — setter.
    set-onfullscreenchange: func(self: element-handle, value: u64);

    /// `onfullscreenerror` attribute — getter.
    get-onfullscreenerror: func(self: element-handle) -> u64;
    /// `onfullscreenerror` attribute — setter.
    set-onfullscreenerror: func(self: element-handle, value: u64);

    /// `setHTMLUnsafe()` operation.
    set-html-unsafe: func(self: element-handle, html: string);

    /// `getHTML()` operation.
    get-html: func(self: element-handle, options: option<u64>) -> string;

    /// `innerHTML` attribute — getter.
    get-inner-html: func(self: element-handle) -> string;
    /// `innerHTML` attribute — setter.
    set-inner-html: func(self: element-handle, value: string);

    /// `outerHTML` attribute — getter.
    get-outer-html: func(self: element-handle) -> string;
    /// `outerHTML` attribute — setter.
    set-outer-html: func(self: element-handle, value: string);

    /// `insertAdjacentHTML()` operation.
    insert-adjacent-html: func(self: element-handle, position: string, %string: string);

    /// `setPointerCapture()` operation.
    set-pointer-capture: func(self: element-handle, pointer-id: s32);

    /// `releasePointerCapture()` operation.
    release-pointer-capture: func(self: element-handle, pointer-id: s32);

    /// `hasPointerCapture()` operation.
    has-pointer-capture: func(self: element-handle, pointer-id: s32) -> bool;
}

interface html-element {
    /// Opaque handle to a host-side `HTMLElement` instance.
    type html-element-handle = u64;

    /// `scrollParent` attribute — getter.
    get-scroll-parent: func(self: html-element-handle) -> option<u64>;

    /// `offsetParent` attribute — getter.
    get-offset-parent: func(self: html-element-handle) -> option<u64>;

    /// `offsetTop` attribute — getter.
    get-offset-top: func(self: html-element-handle) -> s32;

    /// `offsetLeft` attribute — getter.
    get-offset-left: func(self: html-element-handle) -> s32;

    /// `offsetWidth` attribute — getter.
    get-offset-width: func(self: html-element-handle) -> s32;

    /// `offsetHeight` attribute — getter.
    get-offset-height: func(self: html-element-handle) -> s32;

    /// `title` attribute — getter.
    get-title: func(self: html-element-handle) -> string;
    /// `title` attribute — setter.
    set-title: func(self: html-element-handle, value: string);

    /// `lang` attribute — getter.
    get-lang: func(self: html-element-handle) -> string;
    /// `lang` attribute — setter.
    set-lang: func(self: html-element-handle, value: string);

    /// `translate` attribute — getter.
    get-translate: func(self: html-element-handle) -> bool;
    /// `translate` attribute — setter.
    set-translate: func(self: html-element-handle, value: bool);

    /// `dir` attribute — getter.
    get-dir: func(self: html-element-handle) -> string;
    /// `dir` attribute — setter.
    set-dir: func(self: html-element-handle, value: string);

    /// `hidden` attribute — getter.
    get-hidden: func(self: html-element-handle) -> option<bool>;
    /// `hidden` attribute — setter.
    set-hidden: func(self: html-element-handle, value: option<bool>);

    /// `inert` attribute — getter.
    get-inert: func(self: html-element-handle) -> bool;
    /// `inert` attribute — setter.
    set-inert: func(self: html-element-handle, value: bool);

    /// `click()` operation.
    click: func(self: html-element-handle);

    /// `accessKey` attribute — getter.
    get-access-key: func(self: html-element-handle) -> string;
    /// `accessKey` attribute — setter.
    set-access-key: func(self: html-element-handle, value: string);

    /// `accessKeyLabel` attribute — getter.
    get-access-key-label: func(self: html-element-handle) -> string;

    /// `draggable` attribute — getter.
    get-draggable: func(self: html-element-handle) -> bool;
    /// `draggable` attribute — setter.
    set-draggable: func(self: html-element-handle, value: bool);

    /// `spellcheck` attribute — getter.
    get-spellcheck: func(self: html-element-handle) -> bool;
    /// `spellcheck` attribute — setter.
    set-spellcheck: func(self: html-element-handle, value: bool);

    /// `writingSuggestions` attribute — getter.
    get-writing-suggestions: func(self: html-element-handle) -> string;
    /// `writingSuggestions` attribute — setter.
    set-writing-suggestions: func(self: html-element-handle, value: string);

    /// `autocapitalize` attribute — getter.
    get-autocapitalize: func(self: html-element-handle) -> string;
    /// `autocapitalize` attribute — setter.
    set-autocapitalize: func(self: html-element-handle, value: string);

    /// `autocorrect` attribute — getter.
    get-autocorrect: func(self: html-element-handle) -> bool;
    /// `autocorrect` attribute — setter.
    set-autocorrect: func(self: html-element-handle, value: bool);

    /// `innerText` attribute — getter.
    get-inner-text: func(self: html-element-handle) -> string;
    /// `innerText` attribute — setter.
    set-inner-text: func(self: html-element-handle, value: string);

    /// `outerText` attribute — getter.
    get-outer-text: func(self: html-element-handle) -> string;
    /// `outerText` attribute — setter.
    set-outer-text: func(self: html-element-handle, value: string);

    /// `attachInternals()` operation.
    attach-internals: func(self: html-element-handle) -> u64;

    /// `showPopover()` operation.
    show-popover: func(self: html-element-handle, options: option<u64>);

    /// `hidePopover()` operation.
    hide-popover: func(self: html-element-handle);

    /// `togglePopover()` operation.
    toggle-popover: func(self: html-element-handle, options: option<bool>) -> bool;

    /// `popover` attribute — getter.
    get-popover: func(self: html-element-handle) -> option<string>;
    /// `popover` attribute — setter.
    set-popover: func(self: html-element-handle, value: option<string>);

    /// `headingOffset` attribute — getter.
    get-heading-offset: func(self: html-element-handle) -> u32;
    /// `headingOffset` attribute — setter.
    set-heading-offset: func(self: html-element-handle, value: u32);

    /// `headingReset` attribute — getter.
    get-heading-reset: func(self: html-element-handle) -> bool;
    /// `headingReset` attribute — setter.
    set-heading-reset: func(self: html-element-handle, value: bool);
}

interface html-image-element {
    /// Opaque handle to a host-side `HTMLImageElement` instance.
    type html-image-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: html-image-element-handle) -> s32;

    /// `y` attribute — getter.
    get-y: func(self: html-image-element-handle) -> s32;

    /// `alt` attribute — getter.
    get-alt: func(self: html-image-element-handle) -> string;
    /// `alt` attribute — setter.
    set-alt: func(self: html-image-element-handle, value: string);

    /// `src` attribute — getter.
    get-src: func(self: html-image-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-image-element-handle, value: string);

    /// `srcset` attribute — getter.
    get-srcset: func(self: html-image-element-handle) -> string;
    /// `srcset` attribute — setter.
    set-srcset: func(self: html-image-element-handle, value: string);

    /// `sizes` attribute — getter.
    get-sizes: func(self: html-image-element-handle) -> string;
    /// `sizes` attribute — setter.
    set-sizes: func(self: html-image-element-handle, value: string);

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: html-image-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: html-image-element-handle, value: option<string>);

    /// `useMap` attribute — getter.
    get-use-map: func(self: html-image-element-handle) -> string;
    /// `useMap` attribute — setter.
    set-use-map: func(self: html-image-element-handle, value: string);

    /// `isMap` attribute — getter.
    get-is-map: func(self: html-image-element-handle) -> bool;
    /// `isMap` attribute — setter.
    set-is-map: func(self: html-image-element-handle, value: bool);

    /// `width` attribute — getter.
    get-width: func(self: html-image-element-handle) -> u32;
    /// `width` attribute — setter.
    set-width: func(self: html-image-element-handle, value: u32);

    /// `height` attribute — getter.
    get-height: func(self: html-image-element-handle) -> u32;
    /// `height` attribute — setter.
    set-height: func(self: html-image-element-handle, value: u32);

    /// `naturalWidth` attribute — getter.
    get-natural-width: func(self: html-image-element-handle) -> u32;

    /// `naturalHeight` attribute — getter.
    get-natural-height: func(self: html-image-element-handle) -> u32;

    /// `complete` attribute — getter.
    get-complete: func(self: html-image-element-handle) -> bool;

    /// `currentSrc` attribute — getter.
    get-current-src: func(self: html-image-element-handle) -> string;

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: html-image-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: html-image-element-handle, value: string);

    /// `decoding` attribute — getter.
    get-decoding: func(self: html-image-element-handle) -> string;
    /// `decoding` attribute — setter.
    set-decoding: func(self: html-image-element-handle, value: string);

    /// `loading` attribute — getter.
    get-loading: func(self: html-image-element-handle) -> string;
    /// `loading` attribute — setter.
    set-loading: func(self: html-image-element-handle, value: string);

    /// `fetchPriority` attribute — getter.
    get-fetch-priority: func(self: html-image-element-handle) -> string;
    /// `fetchPriority` attribute — setter.
    set-fetch-priority: func(self: html-image-element-handle, value: string);

    /// `decode()` operation.
    decode: func(self: html-image-element-handle) -> u64;

    /// `name` attribute — getter.
    get-name: func(self: html-image-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-image-element-handle, value: string);

    /// `lowsrc` attribute — getter.
    get-lowsrc: func(self: html-image-element-handle) -> string;
    /// `lowsrc` attribute — setter.
    set-lowsrc: func(self: html-image-element-handle, value: string);

    /// `align` attribute — getter.
    get-align: func(self: html-image-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-image-element-handle, value: string);

    /// `hspace` attribute — getter.
    get-hspace: func(self: html-image-element-handle) -> u32;
    /// `hspace` attribute — setter.
    set-hspace: func(self: html-image-element-handle, value: u32);

    /// `vspace` attribute — getter.
    get-vspace: func(self: html-image-element-handle) -> u32;
    /// `vspace` attribute — setter.
    set-vspace: func(self: html-image-element-handle, value: u32);

    /// `longDesc` attribute — getter.
    get-long-desc: func(self: html-image-element-handle) -> string;
    /// `longDesc` attribute — setter.
    set-long-desc: func(self: html-image-element-handle, value: string);

    /// `border` attribute — getter.
    get-border: func(self: html-image-element-handle) -> string;
    /// `border` attribute — setter.
    set-border: func(self: html-image-element-handle, value: string);
}

interface range {
    /// Opaque handle to a host-side `Range` instance.

    use types.{dom-rect};

    type range-handle = u64;

    /// `getClientRects()` operation.
    get-client-rects: func(self: range-handle) -> u64;

    /// `getBoundingClientRect()` operation.
    get-bounding-client-rect: func(self: range-handle) -> dom-rect;

    /// `commonAncestorContainer` attribute — getter.
    get-common-ancestor-container: func(self: range-handle) -> u64;

    /// `setStart()` operation.
    set-start: func(self: range-handle, node: u64, offset: u32);

    /// `setEnd()` operation.
    set-end: func(self: range-handle, node: u64, offset: u32);

    /// `setStartBefore()` operation.
    set-start-before: func(self: range-handle, node: u64);

    /// `setStartAfter()` operation.
    set-start-after: func(self: range-handle, node: u64);

    /// `setEndBefore()` operation.
    set-end-before: func(self: range-handle, node: u64);

    /// `setEndAfter()` operation.
    set-end-after: func(self: range-handle, node: u64);

    /// `collapse()` operation.
    collapse: func(self: range-handle, to-start: option<bool>);

    /// `selectNode()` operation.
    select-node: func(self: range-handle, node: u64);

    /// `selectNodeContents()` operation.
    select-node-contents: func(self: range-handle, node: u64);

    /// `compareBoundaryPoints()` operation.
    compare-boundary-points: func(self: range-handle, how: u16, source-range: u64) -> s16;

    /// `deleteContents()` operation.
    delete-contents: func(self: range-handle);

    /// `extractContents()` operation.
    extract-contents: func(self: range-handle) -> u64;

    /// `cloneContents()` operation.
    clone-contents: func(self: range-handle) -> u64;

    /// `insertNode()` operation.
    insert-node: func(self: range-handle, node: u64);

    /// `surroundContents()` operation.
    surround-contents: func(self: range-handle, new-parent: u64);

    /// `cloneRange()` operation.
    clone-range: func(self: range-handle) -> u64;

    /// `detach()` operation.
    detach: func(self: range-handle);

    /// `isPointInRange()` operation.
    is-point-in-range: func(self: range-handle, node: u64, offset: u32) -> bool;

    /// `comparePoint()` operation.
    compare-point: func(self: range-handle, node: u64, offset: u32) -> s16;

    /// `intersectsNode()` operation.
    intersects-node: func(self: range-handle, node: u64) -> bool;

    /// `createContextualFragment()` operation.
    create-contextual-fragment: func(self: range-handle, %string: string) -> u64;
}

interface mouse-event {
    /// Opaque handle to a host-side `MouseEvent` instance.
    type mouse-event-handle = u64;

    /// `screenX` attribute — getter.
    get-screen-x: func(self: mouse-event-handle) -> f64;

    /// `screenY` attribute — getter.
    get-screen-y: func(self: mouse-event-handle) -> f64;

    /// `pageX` attribute — getter.
    get-page-x: func(self: mouse-event-handle) -> f64;

    /// `pageY` attribute — getter.
    get-page-y: func(self: mouse-event-handle) -> f64;

    /// `clientX` attribute — getter.
    get-client-x: func(self: mouse-event-handle) -> f64;

    /// `clientY` attribute — getter.
    get-client-y: func(self: mouse-event-handle) -> f64;

    /// `x` attribute — getter.
    get-x: func(self: mouse-event-handle) -> f64;

    /// `y` attribute — getter.
    get-y: func(self: mouse-event-handle) -> f64;

    /// `offsetX` attribute — getter.
    get-offset-x: func(self: mouse-event-handle) -> f64;

    /// `offsetY` attribute — getter.
    get-offset-y: func(self: mouse-event-handle) -> f64;

    /// `layerX` attribute — getter.
    get-layer-x: func(self: mouse-event-handle) -> s32;

    /// `layerY` attribute — getter.
    get-layer-y: func(self: mouse-event-handle) -> s32;

    /// `ctrlKey` attribute — getter.
    get-ctrl-key: func(self: mouse-event-handle) -> bool;

    /// `shiftKey` attribute — getter.
    get-shift-key: func(self: mouse-event-handle) -> bool;

    /// `altKey` attribute — getter.
    get-alt-key: func(self: mouse-event-handle) -> bool;

    /// `metaKey` attribute — getter.
    get-meta-key: func(self: mouse-event-handle) -> bool;

    /// `button` attribute — getter.
    get-button: func(self: mouse-event-handle) -> s16;

    /// `buttons` attribute — getter.
    get-buttons: func(self: mouse-event-handle) -> u16;

    /// `relatedTarget` attribute — getter.
    get-related-target: func(self: mouse-event-handle) -> option<u64>;

    /// `getModifierState()` operation.
    get-modifier-state: func(self: mouse-event-handle, key-arg: string) -> bool;

    /// `initMouseEvent()` operation.
    init-mouse-event: func(self: mouse-event-handle, type-arg: string, bubbles-arg: option<bool>, cancelable-arg: option<bool>, view-arg: option<u64>, detail-arg: option<s32>, screen-x-arg: option<s32>, screen-y-arg: option<s32>, client-x-arg: option<s32>, client-y-arg: option<s32>, ctrl-key-arg: option<bool>, alt-key-arg: option<bool>, shift-key-arg: option<bool>, meta-key-arg: option<bool>, button-arg: option<s16>, related-target-arg: option<u64>);
}

interface geometry-utils {
    /// Opaque handle to a host-side `GeometryUtils` instance.

    use types.{dom-rect};

    type geometry-utils-handle = u64;

    /// `getBoxQuads()` operation.
    get-box-quads: func(self: geometry-utils-handle, options: option<u64>) -> list<u64>;

    /// `convertQuadFromNode()` operation.
    convert-quad-from-node: func(self: geometry-utils-handle, quad: u64, %from: u64, options: option<u64>) -> u64;

    /// `convertRectFromNode()` operation.
    convert-rect-from-node: func(self: geometry-utils-handle, rect: dom-rect, %from: u64, options: option<u64>) -> u64;

    /// `convertPointFromNode()` operation.
    convert-point-from-node: func(self: geometry-utils-handle, point: u64, %from: u64, options: option<u64>) -> u64;
}

interface visual-viewport {
    /// Opaque handle to a host-side `VisualViewport` instance.
    type visual-viewport-handle = u64;

    /// `offsetLeft` attribute — getter.
    get-offset-left: func(self: visual-viewport-handle) -> f64;

    /// `offsetTop` attribute — getter.
    get-offset-top: func(self: visual-viewport-handle) -> f64;

    /// `pageLeft` attribute — getter.
    get-page-left: func(self: visual-viewport-handle) -> f64;

    /// `pageTop` attribute — getter.
    get-page-top: func(self: visual-viewport-handle) -> f64;

    /// `width` attribute — getter.
    get-width: func(self: visual-viewport-handle) -> f64;

    /// `height` attribute — getter.
    get-height: func(self: visual-viewport-handle) -> f64;

    /// `scale` attribute — getter.
    get-scale: func(self: visual-viewport-handle) -> f64;

    /// `onresize` attribute — getter.
    get-onresize: func(self: visual-viewport-handle) -> u64;
    /// `onresize` attribute — setter.
    set-onresize: func(self: visual-viewport-handle, value: u64);

    /// `onscroll` attribute — getter.
    get-onscroll: func(self: visual-viewport-handle) -> u64;
    /// `onscroll` attribute — setter.
    set-onscroll: func(self: visual-viewport-handle, value: u64);

    /// `onscrollend` attribute — getter.
    get-onscrollend: func(self: visual-viewport-handle) -> u64;
    /// `onscrollend` attribute — setter.
    set-onscrollend: func(self: visual-viewport-handle, value: u64);
}

interface media-list {
    /// Opaque handle to a host-side `MediaList` instance.
    type media-list-handle = u64;

    /// `mediaText` attribute — getter.
    get-media-text: func(self: media-list-handle) -> string;
    /// `mediaText` attribute — setter.
    set-media-text: func(self: media-list-handle, value: string);

    /// `length` attribute — getter.
    get-length: func(self: media-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: media-list-handle, index: u32) -> option<string>;

    /// `appendMedium()` operation.
    append-medium: func(self: media-list-handle, medium: string);

    /// `deleteMedium()` operation.
    delete-medium: func(self: media-list-handle, medium: string);
}

interface style-sheet {
    /// Opaque handle to a host-side `StyleSheet` instance.
    type style-sheet-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: style-sheet-handle) -> string;

    /// `href` attribute — getter.
    get-href: func(self: style-sheet-handle) -> option<string>;

    /// `ownerNode` attribute — getter.
    get-owner-node: func(self: style-sheet-handle) -> option<u64>;

    /// `parentStyleSheet` attribute — getter.
    get-parent-style-sheet: func(self: style-sheet-handle) -> option<u64>;

    /// `title` attribute — getter.
    get-title: func(self: style-sheet-handle) -> option<string>;

    /// `media` attribute — getter.
    get-media: func(self: style-sheet-handle) -> u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: style-sheet-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: style-sheet-handle, value: bool);
}

interface css-style-sheet {
    /// Opaque handle to a host-side `CSSStyleSheet` instance.
    type css-style-sheet-handle = u64;

    /// `ownerRule` attribute — getter.
    get-owner-rule: func(self: css-style-sheet-handle) -> option<u64>;

    /// `cssRules` attribute — getter.
    get-css-rules: func(self: css-style-sheet-handle) -> u64;

    /// `insertRule()` operation.
    insert-rule: func(self: css-style-sheet-handle, rule: string, index: option<u32>) -> u32;

    /// `deleteRule()` operation.
    delete-rule: func(self: css-style-sheet-handle, index: u32);

    /// `replace()` operation.
    replace: func(self: css-style-sheet-handle, text: string) -> u64;

    /// `replaceSync()` operation.
    replace-sync: func(self: css-style-sheet-handle, text: string);

    /// `rules` attribute — getter.
    get-rules: func(self: css-style-sheet-handle) -> u64;

    /// `addRule()` operation.
    add-rule: func(self: css-style-sheet-handle, selector: option<string>, style: option<string>, index: option<u32>) -> s32;

    /// `removeRule()` operation.
    remove-rule: func(self: css-style-sheet-handle, index: option<u32>);
}

interface style-sheet-list {
    /// Opaque handle to a host-side `StyleSheetList` instance.
    type style-sheet-list-handle = u64;

    /// `item()` operation.
    item: func(self: style-sheet-list-handle, index: u32) -> option<u64>;

    /// `length` attribute — getter.
    get-length: func(self: style-sheet-list-handle) -> u32;
}

interface document-or-shadow-root {
    /// Opaque handle to a host-side `DocumentOrShadowRoot` instance.
    type document-or-shadow-root-handle = u64;

    /// `styleSheets` attribute — getter.
    get-style-sheets: func(self: document-or-shadow-root-handle) -> u64;

    /// `adoptedStyleSheets` attribute — getter.
    get-adopted-style-sheets: func(self: document-or-shadow-root-handle) -> list<u64>;
    /// `adoptedStyleSheets` attribute — setter.
    set-adopted-style-sheets: func(self: document-or-shadow-root-handle, value: list<u64>);

    /// `customElementRegistry` attribute — getter.
    get-custom-element-registry: func(self: document-or-shadow-root-handle) -> option<u64>;

    /// `fullscreenElement` attribute — getter.
    get-fullscreen-element: func(self: document-or-shadow-root-handle) -> option<u64>;

    /// `activeElement` attribute — getter.
    get-active-element: func(self: document-or-shadow-root-handle) -> option<u64>;
}

interface link-style {
    /// Opaque handle to a host-side `LinkStyle` instance.
    type link-style-handle = u64;

    /// `sheet` attribute — getter.
    get-sheet: func(self: link-style-handle) -> option<u64>;
}

interface css-rule-list {
    /// Opaque handle to a host-side `CSSRuleList` instance.
    type css-rule-list-handle = u64;

    /// `item()` operation.
    item: func(self: css-rule-list-handle, index: u32) -> option<u64>;

    /// `length` attribute — getter.
    get-length: func(self: css-rule-list-handle) -> u32;
}

interface css-style-rule {
    /// Opaque handle to a host-side `CSSStyleRule` instance.
    type css-style-rule-handle = u64;

    /// `selectorText` attribute — getter.
    get-selector-text: func(self: css-style-rule-handle) -> string;
    /// `selectorText` attribute — setter.
    set-selector-text: func(self: css-style-rule-handle, value: string);

    /// `style` attribute — getter.
    get-style: func(self: css-style-rule-handle) -> u64;
}

interface css-import-rule {
    /// Opaque handle to a host-side `CSSImportRule` instance.
    type css-import-rule-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: css-import-rule-handle) -> string;

    /// `media` attribute — getter.
    get-media: func(self: css-import-rule-handle) -> u64;

    /// `styleSheet` attribute — getter.
    get-style-sheet: func(self: css-import-rule-handle) -> option<u64>;

    /// `layerName` attribute — getter.
    get-layer-name: func(self: css-import-rule-handle) -> option<string>;

    /// `supportsText` attribute — getter.
    get-supports-text: func(self: css-import-rule-handle) -> option<string>;
}

interface css-grouping-rule {
    /// Opaque handle to a host-side `CSSGroupingRule` instance.
    type css-grouping-rule-handle = u64;

    /// `cssRules` attribute — getter.
    get-css-rules: func(self: css-grouping-rule-handle) -> u64;

    /// `insertRule()` operation.
    insert-rule: func(self: css-grouping-rule-handle, rule: string, index: option<u32>) -> u32;

    /// `deleteRule()` operation.
    delete-rule: func(self: css-grouping-rule-handle, index: u32);
}

interface css-page-descriptors {
    /// Opaque handle to a host-side `CSSPageDescriptors` instance.
    type css-page-descriptors-handle = u64;

    /// `margin` attribute — getter.
    get-margin: func(self: css-page-descriptors-handle) -> string;
    /// `margin` attribute — setter.
    set-margin: func(self: css-page-descriptors-handle, value: string);

    /// `marginTop` attribute — getter.
    get-margin-top: func(self: css-page-descriptors-handle) -> string;
    /// `marginTop` attribute — setter.
    set-margin-top: func(self: css-page-descriptors-handle, value: string);

    /// `marginRight` attribute — getter.
    get-margin-right: func(self: css-page-descriptors-handle) -> string;
    /// `marginRight` attribute — setter.
    set-margin-right: func(self: css-page-descriptors-handle, value: string);

    /// `marginBottom` attribute — getter.
    get-margin-bottom: func(self: css-page-descriptors-handle) -> string;
    /// `marginBottom` attribute — setter.
    set-margin-bottom: func(self: css-page-descriptors-handle, value: string);

    /// `marginLeft` attribute — getter.
    get-margin-left: func(self: css-page-descriptors-handle) -> string;
    /// `marginLeft` attribute — setter.
    set-margin-left: func(self: css-page-descriptors-handle, value: string);

    /// `size` attribute — getter.
    get-size: func(self: css-page-descriptors-handle) -> string;
    /// `size` attribute — setter.
    set-size: func(self: css-page-descriptors-handle, value: string);

    /// `pageOrientation` attribute — getter.
    get-page-orientation: func(self: css-page-descriptors-handle) -> string;
    /// `pageOrientation` attribute — setter.
    set-page-orientation: func(self: css-page-descriptors-handle, value: string);

    /// `marks` attribute — getter.
    get-marks: func(self: css-page-descriptors-handle) -> string;
    /// `marks` attribute — setter.
    set-marks: func(self: css-page-descriptors-handle, value: string);

    /// `bleed` attribute — getter.
    get-bleed: func(self: css-page-descriptors-handle) -> string;
    /// `bleed` attribute — setter.
    set-bleed: func(self: css-page-descriptors-handle, value: string);
}

interface css-page-rule {
    /// Opaque handle to a host-side `CSSPageRule` instance.
    type css-page-rule-handle = u64;

    /// `selectorText` attribute — getter.
    get-selector-text: func(self: css-page-rule-handle) -> string;
    /// `selectorText` attribute — setter.
    set-selector-text: func(self: css-page-rule-handle, value: string);

    /// `style` attribute — getter.
    get-style: func(self: css-page-rule-handle) -> u64;
}

interface css-margin-rule {
    /// Opaque handle to a host-side `CSSMarginRule` instance.
    type css-margin-rule-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: css-margin-rule-handle) -> string;

    /// `style` attribute — getter.
    get-style: func(self: css-margin-rule-handle) -> u64;
}

interface css-namespace-rule {
    /// Opaque handle to a host-side `CSSNamespaceRule` instance.
    type css-namespace-rule-handle = u64;

    /// `namespaceURI` attribute — getter.
    get-namespace-uri: func(self: css-namespace-rule-handle) -> string;

    /// `prefix` attribute — getter.
    get-prefix: func(self: css-namespace-rule-handle) -> string;
}

interface css-style-declaration {
    /// Opaque handle to a host-side `CSSStyleDeclaration` instance.
    type css-style-declaration-handle = u64;

    /// `cssText` attribute — getter.
    get-css-text: func(self: css-style-declaration-handle) -> string;
    /// `cssText` attribute — setter.
    set-css-text: func(self: css-style-declaration-handle, value: string);

    /// `length` attribute — getter.
    get-length: func(self: css-style-declaration-handle) -> u32;

    /// `item()` operation.
    item: func(self: css-style-declaration-handle, index: u32) -> string;

    /// `getPropertyValue()` operation.
    get-property-value: func(self: css-style-declaration-handle, property: string) -> string;

    /// `getPropertyPriority()` operation.
    get-property-priority: func(self: css-style-declaration-handle, property: string) -> string;

    /// `setProperty()` operation.
    set-property: func(self: css-style-declaration-handle, property: string, value: string, priority: option<string>);

    /// `removeProperty()` operation.
    remove-property: func(self: css-style-declaration-handle, property: string) -> string;

    /// `parentRule` attribute — getter.
    get-parent-rule: func(self: css-style-declaration-handle) -> option<u64>;
}

interface css-style-properties {
    /// Opaque handle to a host-side `CSSStyleProperties` instance.
    type css-style-properties-handle = u64;

    /// `cssFloat` attribute — getter.
    get-css-float: func(self: css-style-properties-handle) -> string;
    /// `cssFloat` attribute — setter.
    set-css-float: func(self: css-style-properties-handle, value: string);
}

interface element-css-inline-style {
    /// Opaque handle to a host-side `ElementCSSInlineStyle` instance.
    type element-css-inline-style-handle = u64;

    /// `style` attribute — getter.
    get-style: func(self: element-css-inline-style-handle) -> u64;
}

interface navigator {
    /// Opaque handle to a host-side `Navigator` instance.
    type navigator-handle = u64;

    /// `getBattery()` operation.
    get-battery: func() -> u64;

    /// `clipboard` attribute — getter.
    get-clipboard: func() -> u64;

    /// `credentials` attribute — getter.
    get-credentials: func() -> u64;

    /// `getGamepads()` operation.
    get-gamepads: func() -> list<option<u64>>;

    /// `geolocation` attribute — getter.
    get-geolocation: func() -> u64;

    /// `userActivation` attribute — getter.
    get-user-activation: func() -> u64;

    /// `mediaCapabilities` attribute — getter.
    get-media-capabilities: func() -> u64;

    /// `mediaDevices` attribute — getter.
    get-media-devices: func() -> u64;

    /// `mediaSession` attribute — getter.
    get-media-session: func() -> u64;

    /// `permissions` attribute — getter.
    get-permissions: func() -> u64;

    /// `maxTouchPoints` attribute — getter.
    get-max-touch-points: func() -> s32;

    /// `serviceWorker` attribute — getter.
    get-service-worker: func() -> u64;

    /// `vibrate()` operation.
    vibrate: func(pattern: u64) -> bool;
}

interface battery-manager {
    /// Opaque handle to a host-side `BatteryManager` instance.
    type battery-manager-handle = u64;

    /// `charging` attribute — getter.
    get-charging: func(self: battery-manager-handle) -> bool;

    /// `chargingTime` attribute — getter.
    get-charging-time: func(self: battery-manager-handle) -> f64;

    /// `dischargingTime` attribute — getter.
    get-discharging-time: func(self: battery-manager-handle) -> f64;

    /// `level` attribute — getter.
    get-level: func(self: battery-manager-handle) -> f64;

    /// `onchargingchange` attribute — getter.
    get-onchargingchange: func(self: battery-manager-handle) -> u64;
    /// `onchargingchange` attribute — setter.
    set-onchargingchange: func(self: battery-manager-handle, value: u64);

    /// `onchargingtimechange` attribute — getter.
    get-onchargingtimechange: func(self: battery-manager-handle) -> u64;
    /// `onchargingtimechange` attribute — setter.
    set-onchargingtimechange: func(self: battery-manager-handle, value: u64);

    /// `ondischargingtimechange` attribute — getter.
    get-ondischargingtimechange: func(self: battery-manager-handle) -> u64;
    /// `ondischargingtimechange` attribute — setter.
    set-ondischargingtimechange: func(self: battery-manager-handle, value: u64);

    /// `onlevelchange` attribute — getter.
    get-onlevelchange: func(self: battery-manager-handle) -> u64;
    /// `onlevelchange` attribute — setter.
    set-onlevelchange: func(self: battery-manager-handle, value: u64);
}

interface gamepad {
    /// Opaque handle to a host-side `Gamepad` instance.
    type gamepad-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: gamepad-handle) -> string;

    /// `index` attribute — getter.
    get-index: func(self: gamepad-handle) -> s32;

    /// `connected` attribute — getter.
    get-connected: func(self: gamepad-handle) -> bool;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: gamepad-handle) -> f64;

    /// `mapping` attribute — getter.
    get-mapping: func(self: gamepad-handle) -> u64;

    /// `axes` attribute — getter.
    get-axes: func(self: gamepad-handle) -> list<f64>;

    /// `buttons` attribute — getter.
    get-buttons: func(self: gamepad-handle) -> list<u64>;

    /// `touches` attribute — getter.
    get-touches: func(self: gamepad-handle) -> list<u64>;

    /// `vibrationActuator` attribute — getter.
    get-vibration-actuator: func(self: gamepad-handle) -> u64;
}

interface gamepad-button {
    /// Opaque handle to a host-side `GamepadButton` instance.
    type gamepad-button-handle = u64;

    /// `pressed` attribute — getter.
    get-pressed: func(self: gamepad-button-handle) -> bool;

    /// `touched` attribute — getter.
    get-touched: func(self: gamepad-button-handle) -> bool;

    /// `value` attribute — getter.
    get-value: func(self: gamepad-button-handle) -> f64;
}

interface gamepad-haptic-actuator {
    /// Opaque handle to a host-side `GamepadHapticActuator` instance.
    type gamepad-haptic-actuator-handle = u64;

    /// `effects` attribute — getter.
    get-effects: func(self: gamepad-haptic-actuator-handle) -> list<u64>;

    /// `playEffect()` operation.
    play-effect: func(self: gamepad-haptic-actuator-handle, %type: u64, params: option<u64>) -> u64;

    /// `reset()` operation.
    reset: func(self: gamepad-haptic-actuator-handle) -> u64;
}

interface gamepad-event {
    /// Opaque handle to a host-side `GamepadEvent` instance.
    type gamepad-event-handle = u64;

    /// `gamepad` attribute — getter.
    get-gamepad: func(self: gamepad-event-handle) -> u64;
}

interface window-event-handlers {
    /// Opaque handle to a host-side `WindowEventHandlers` instance.
    type window-event-handlers-handle = u64;

    /// `ongamepadconnected` attribute — getter.
    get-ongamepadconnected: func(self: window-event-handlers-handle) -> u64;
    /// `ongamepadconnected` attribute — setter.
    set-ongamepadconnected: func(self: window-event-handlers-handle, value: u64);

    /// `ongamepaddisconnected` attribute — getter.
    get-ongamepaddisconnected: func(self: window-event-handlers-handle) -> u64;
    /// `ongamepaddisconnected` attribute — setter.
    set-ongamepaddisconnected: func(self: window-event-handlers-handle, value: u64);

    /// `onafterprint` attribute — getter.
    get-onafterprint: func(self: window-event-handlers-handle) -> u64;
    /// `onafterprint` attribute — setter.
    set-onafterprint: func(self: window-event-handlers-handle, value: u64);

    /// `onbeforeprint` attribute — getter.
    get-onbeforeprint: func(self: window-event-handlers-handle) -> u64;
    /// `onbeforeprint` attribute — setter.
    set-onbeforeprint: func(self: window-event-handlers-handle, value: u64);

    /// `onbeforeunload` attribute — getter.
    get-onbeforeunload: func(self: window-event-handlers-handle) -> u64;
    /// `onbeforeunload` attribute — setter.
    set-onbeforeunload: func(self: window-event-handlers-handle, value: u64);

    /// `onhashchange` attribute — getter.
    get-onhashchange: func(self: window-event-handlers-handle) -> u64;
    /// `onhashchange` attribute — setter.
    set-onhashchange: func(self: window-event-handlers-handle, value: u64);

    /// `onlanguagechange` attribute — getter.
    get-onlanguagechange: func(self: window-event-handlers-handle) -> u64;
    /// `onlanguagechange` attribute — setter.
    set-onlanguagechange: func(self: window-event-handlers-handle, value: u64);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: window-event-handlers-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: window-event-handlers-handle, value: u64);

    /// `onmessageerror` attribute — getter.
    get-onmessageerror: func(self: window-event-handlers-handle) -> u64;
    /// `onmessageerror` attribute — setter.
    set-onmessageerror: func(self: window-event-handlers-handle, value: u64);

    /// `onoffline` attribute — getter.
    get-onoffline: func(self: window-event-handlers-handle) -> u64;
    /// `onoffline` attribute — setter.
    set-onoffline: func(self: window-event-handlers-handle, value: u64);

    /// `ononline` attribute — getter.
    get-ononline: func(self: window-event-handlers-handle) -> u64;
    /// `ononline` attribute — setter.
    set-ononline: func(self: window-event-handlers-handle, value: u64);

    /// `onpagehide` attribute — getter.
    get-onpagehide: func(self: window-event-handlers-handle) -> u64;
    /// `onpagehide` attribute — setter.
    set-onpagehide: func(self: window-event-handlers-handle, value: u64);

    /// `onpagereveal` attribute — getter.
    get-onpagereveal: func(self: window-event-handlers-handle) -> u64;
    /// `onpagereveal` attribute — setter.
    set-onpagereveal: func(self: window-event-handlers-handle, value: u64);

    /// `onpageshow` attribute — getter.
    get-onpageshow: func(self: window-event-handlers-handle) -> u64;
    /// `onpageshow` attribute — setter.
    set-onpageshow: func(self: window-event-handlers-handle, value: u64);

    /// `onpageswap` attribute — getter.
    get-onpageswap: func(self: window-event-handlers-handle) -> u64;
    /// `onpageswap` attribute — setter.
    set-onpageswap: func(self: window-event-handlers-handle, value: u64);

    /// `onpopstate` attribute — getter.
    get-onpopstate: func(self: window-event-handlers-handle) -> u64;
    /// `onpopstate` attribute — setter.
    set-onpopstate: func(self: window-event-handlers-handle, value: u64);

    /// `onrejectionhandled` attribute — getter.
    get-onrejectionhandled: func(self: window-event-handlers-handle) -> u64;
    /// `onrejectionhandled` attribute — setter.
    set-onrejectionhandled: func(self: window-event-handlers-handle, value: u64);

    /// `onstorage` attribute — getter.
    get-onstorage: func(self: window-event-handlers-handle) -> u64;
    /// `onstorage` attribute — setter.
    set-onstorage: func(self: window-event-handlers-handle, value: u64);

    /// `onunhandledrejection` attribute — getter.
    get-onunhandledrejection: func(self: window-event-handlers-handle) -> u64;
    /// `onunhandledrejection` attribute — setter.
    set-onunhandledrejection: func(self: window-event-handlers-handle, value: u64);

    /// `onunload` attribute — getter.
    get-onunload: func(self: window-event-handlers-handle) -> u64;
    /// `onunload` attribute — setter.
    set-onunload: func(self: window-event-handlers-handle, value: u64);
}

interface geolocation {
    /// Opaque handle to a host-side `Geolocation` instance.
    type geolocation-handle = u64;

    /// `getCurrentPosition()` operation.
    get-current-position: func(self: geolocation-handle, success-callback: u64, error-callback: option<u64>, options: option<u64>);

    /// `watchPosition()` operation.
    watch-position: func(self: geolocation-handle, success-callback: u64, error-callback: option<u64>, options: option<u64>) -> s32;

    /// `clearWatch()` operation.
    clear-watch: func(self: geolocation-handle, watch-id: s32);
}

interface geolocation-position {
    /// Opaque handle to a host-side `GeolocationPosition` instance.
    type geolocation-position-handle = u64;

    /// `coords` attribute — getter.
    get-coords: func(self: geolocation-position-handle) -> u64;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: geolocation-position-handle) -> u64;

    /// `toJSON()` operation.
    to-json: func(self: geolocation-position-handle) -> u64;
}

interface geolocation-coordinates {
    /// Opaque handle to a host-side `GeolocationCoordinates` instance.
    type geolocation-coordinates-handle = u64;

    /// `accuracy` attribute — getter.
    get-accuracy: func(self: geolocation-coordinates-handle) -> f64;

    /// `latitude` attribute — getter.
    get-latitude: func(self: geolocation-coordinates-handle) -> f64;

    /// `longitude` attribute — getter.
    get-longitude: func(self: geolocation-coordinates-handle) -> f64;

    /// `altitude` attribute — getter.
    get-altitude: func(self: geolocation-coordinates-handle) -> option<f64>;

    /// `altitudeAccuracy` attribute — getter.
    get-altitude-accuracy: func(self: geolocation-coordinates-handle) -> option<f64>;

    /// `heading` attribute — getter.
    get-heading: func(self: geolocation-coordinates-handle) -> option<f64>;

    /// `speed` attribute — getter.
    get-speed: func(self: geolocation-coordinates-handle) -> option<f64>;

    /// `toJSON()` operation.
    to-json: func(self: geolocation-coordinates-handle) -> u64;
}

interface geolocation-position-error {
    /// Opaque handle to a host-side `GeolocationPositionError` instance.
    type geolocation-position-error-handle = u64;

    /// `code` attribute — getter.
    get-code: func(self: geolocation-position-error-handle) -> u16;

    /// `message` attribute — getter.
    get-message: func(self: geolocation-position-error-handle) -> string;
}

interface screen-orientation {
    /// Opaque handle to a host-side `ScreenOrientation` instance.
    type screen-orientation-handle = u64;

    /// `lock()` operation.
    lock: func(self: screen-orientation-handle, orientation: u64) -> u64;

    /// `unlock()` operation.
    unlock: func(self: screen-orientation-handle);

    /// `type` attribute — getter.
    get-type: func(self: screen-orientation-handle) -> u64;

    /// `angle` attribute — getter.
    get-angle: func(self: screen-orientation-handle) -> u16;

    /// `onchange` attribute — getter.
    get-onchange: func(self: screen-orientation-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: screen-orientation-handle, value: u64);
}

interface event {
    /// Opaque handle to a host-side `Event` instance.
    type event-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: event-handle) -> string;

    /// `target` attribute — getter.
    get-target: func(self: event-handle) -> option<u64>;

    /// `srcElement` attribute — getter.
    get-src-element: func(self: event-handle) -> option<u64>;

    /// `currentTarget` attribute — getter.
    get-current-target: func(self: event-handle) -> option<u64>;

    /// `composedPath()` operation.
    composed-path: func(self: event-handle) -> list<u64>;

    /// `eventPhase` attribute — getter.
    get-event-phase: func(self: event-handle) -> u16;

    /// `stopPropagation()` operation.
    stop-propagation: func(self: event-handle);

    /// `cancelBubble` attribute — getter.
    get-cancel-bubble: func(self: event-handle) -> bool;
    /// `cancelBubble` attribute — setter.
    set-cancel-bubble: func(self: event-handle, value: bool);

    /// `stopImmediatePropagation()` operation.
    stop-immediate-propagation: func(self: event-handle);

    /// `bubbles` attribute — getter.
    get-bubbles: func(self: event-handle) -> bool;

    /// `cancelable` attribute — getter.
    get-cancelable: func(self: event-handle) -> bool;

    /// `returnValue` attribute — getter.
    get-return-value: func(self: event-handle) -> bool;
    /// `returnValue` attribute — setter.
    set-return-value: func(self: event-handle, value: bool);

    /// `preventDefault()` operation.
    prevent-default: func(self: event-handle);

    /// `defaultPrevented` attribute — getter.
    get-default-prevented: func(self: event-handle) -> bool;

    /// `composed` attribute — getter.
    get-composed: func(self: event-handle) -> bool;

    /// `isTrusted` attribute — getter.
    get-is-trusted: func(self: event-handle) -> bool;

    /// `timeStamp` attribute — getter.
    get-time-stamp: func(self: event-handle) -> f64;

    /// `initEvent()` operation.
    init-event: func(self: event-handle, %type: string, bubbles: option<bool>, cancelable: option<bool>);
}

interface custom-event {
    /// Opaque handle to a host-side `CustomEvent` instance.
    type custom-event-handle = u64;

    /// `detail` attribute — getter.
    get-detail: func(self: custom-event-handle) -> string;

    /// `initCustomEvent()` operation.
    init-custom-event: func(self: custom-event-handle, %type: string, bubbles: option<bool>, cancelable: option<bool>, detail: option<string>);
}

interface event-listener {
    /// Opaque handle to a host-side `EventListener` instance.
    type event-listener-handle = u64;

    /// `handleEvent()` operation.
    handle-event: func(self: event-listener-handle, event: u64);
}

interface abort-controller {
    /// Opaque handle to a host-side `AbortController` instance.
    type abort-controller-handle = u64;

    /// `signal` attribute — getter.
    get-signal: func(self: abort-controller-handle) -> u64;

    /// `abort()` operation.
    abort: func(self: abort-controller-handle, reason: option<string>);
}

interface abort-signal {
    /// Opaque handle to a host-side `AbortSignal` instance.
    type abort-signal-handle = u64;

    /// `abort()` operation.
    abort: func(reason: option<string>) -> u64;

    /// `timeout()` operation.
    timeout: func(milliseconds: u64) -> u64;

    /// `_any()` operation.
    any: func(signals: list<u64>) -> u64;

    /// `aborted` attribute — getter.
    get-aborted: func(self: abort-signal-handle) -> bool;

    /// `reason` attribute — getter.
    get-reason: func(self: abort-signal-handle) -> string;

    /// `throwIfAborted()` operation.
    throw-if-aborted: func(self: abort-signal-handle);

    /// `onabort` attribute — getter.
    get-onabort: func(self: abort-signal-handle) -> u64;
    /// `onabort` attribute — setter.
    set-onabort: func(self: abort-signal-handle, value: u64);
}

interface non-element-parent-node {
    /// Opaque handle to a host-side `NonElementParentNode` instance.
    type non-element-parent-node-handle = u64;

    /// `getElementById()` operation.
    get-element-by-id: func(self: non-element-parent-node-handle, element-id: string) -> option<u64>;
}

interface parent-node {
    /// Opaque handle to a host-side `ParentNode` instance.
    type parent-node-handle = u64;

    /// `children` attribute — getter.
    get-children: func(self: parent-node-handle) -> u64;

    /// `firstElementChild` attribute — getter.
    get-first-element-child: func(self: parent-node-handle) -> option<u64>;

    /// `lastElementChild` attribute — getter.
    get-last-element-child: func(self: parent-node-handle) -> option<u64>;

    /// `childElementCount` attribute — getter.
    get-child-element-count: func(self: parent-node-handle) -> u32;

    /// `prepend()` operation.
    prepend: func(self: parent-node-handle, nodes: list<string>);

    /// `append()` operation.
    append: func(self: parent-node-handle, nodes: list<string>);

    /// `replaceChildren()` operation.
    replace-children: func(self: parent-node-handle, nodes: list<string>);

    /// `moveBefore()` operation.
    move-before: func(self: parent-node-handle, node: u64, child: option<u64>);

    /// `querySelector()` operation.
    query-selector: func(self: parent-node-handle, selectors: string) -> option<u64>;

    /// `querySelectorAll()` operation.
    query-selector-all: func(self: parent-node-handle, selectors: string) -> u64;
}

interface non-document-type-child-node {
    /// Opaque handle to a host-side `NonDocumentTypeChildNode` instance.
    type non-document-type-child-node-handle = u64;

    /// `previousElementSibling` attribute — getter.
    get-previous-element-sibling: func(self: non-document-type-child-node-handle) -> option<u64>;

    /// `nextElementSibling` attribute — getter.
    get-next-element-sibling: func(self: non-document-type-child-node-handle) -> option<u64>;
}

interface child-node {
    /// Opaque handle to a host-side `ChildNode` instance.
    type child-node-handle = u64;

    /// `before()` operation.
    before: func(self: child-node-handle, nodes: list<string>);

    /// `after()` operation.
    after: func(self: child-node-handle, nodes: list<string>);

    /// `replaceWith()` operation.
    replace-with: func(self: child-node-handle, nodes: list<string>);

    /// `remove()` operation.
    remove: func(self: child-node-handle);
}

interface slottable {
    /// Opaque handle to a host-side `Slottable` instance.
    type slottable-handle = u64;

    /// `assignedSlot` attribute — getter.
    get-assigned-slot: func(self: slottable-handle) -> option<u64>;
}

interface node-list {
    /// Opaque handle to a host-side `NodeList` instance.
    type node-list-handle = u64;

    /// `item()` operation.
    item: func(self: node-list-handle, index: u32) -> option<u64>;

    /// `length` attribute — getter.
    get-length: func(self: node-list-handle) -> u32;
}

interface html-collection {
    /// Opaque handle to a host-side `HTMLCollection` instance.
    type html-collection-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: html-collection-handle) -> u32;

    /// `item()` operation.
    item: func(self: html-collection-handle, index: u32) -> option<u64>;

    /// `namedItem()` operation.
    named-item: func(self: html-collection-handle, name: string) -> option<u64>;
}

interface mutation-observer {
    /// Opaque handle to a host-side `MutationObserver` instance.
    type mutation-observer-handle = u64;

    /// `observe()` operation.
    observe: func(self: mutation-observer-handle, target: u64, options: option<u64>);

    /// `disconnect()` operation.
    disconnect: func(self: mutation-observer-handle);

    /// `takeRecords()` operation.
    take-records: func(self: mutation-observer-handle) -> list<u64>;
}

interface mutation-record {
    /// Opaque handle to a host-side `MutationRecord` instance.
    type mutation-record-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: mutation-record-handle) -> string;

    /// `target` attribute — getter.
    get-target: func(self: mutation-record-handle) -> u64;

    /// `addedNodes` attribute — getter.
    get-added-nodes: func(self: mutation-record-handle) -> u64;

    /// `removedNodes` attribute — getter.
    get-removed-nodes: func(self: mutation-record-handle) -> u64;

    /// `previousSibling` attribute — getter.
    get-previous-sibling: func(self: mutation-record-handle) -> option<u64>;

    /// `nextSibling` attribute — getter.
    get-next-sibling: func(self: mutation-record-handle) -> option<u64>;

    /// `attributeName` attribute — getter.
    get-attribute-name: func(self: mutation-record-handle) -> option<string>;

    /// `attributeNamespace` attribute — getter.
    get-attribute-namespace: func(self: mutation-record-handle) -> option<string>;

    /// `oldValue` attribute — getter.
    get-old-value: func(self: mutation-record-handle) -> option<string>;
}

interface node {
    /// Opaque handle to a host-side `Node` instance.
    type node-handle = u64;

    /// `nodeType` attribute — getter.
    get-node-type: func(self: node-handle) -> u16;

    /// `nodeName` attribute — getter.
    get-node-name: func(self: node-handle) -> string;

    /// `baseURI` attribute — getter.
    get-base-uri: func(self: node-handle) -> string;

    /// `isConnected` attribute — getter.
    get-is-connected: func(self: node-handle) -> bool;

    /// `ownerDocument` attribute — getter.
    get-owner-document: func(self: node-handle) -> option<u64>;

    /// `getRootNode()` operation.
    get-root-node: func(self: node-handle, options: option<u64>) -> u64;

    /// `parentNode` attribute — getter.
    get-parent-node: func(self: node-handle) -> option<u64>;

    /// `parentElement` attribute — getter.
    get-parent-element: func(self: node-handle) -> option<u64>;

    /// `hasChildNodes()` operation.
    has-child-nodes: func(self: node-handle) -> bool;

    /// `childNodes` attribute — getter.
    get-child-nodes: func(self: node-handle) -> u64;

    /// `firstChild` attribute — getter.
    get-first-child: func(self: node-handle) -> option<u64>;

    /// `lastChild` attribute — getter.
    get-last-child: func(self: node-handle) -> option<u64>;

    /// `previousSibling` attribute — getter.
    get-previous-sibling: func(self: node-handle) -> option<u64>;

    /// `nextSibling` attribute — getter.
    get-next-sibling: func(self: node-handle) -> option<u64>;

    /// `nodeValue` attribute — getter.
    get-node-value: func(self: node-handle) -> option<string>;
    /// `nodeValue` attribute — setter.
    set-node-value: func(self: node-handle, value: option<string>);

    /// `textContent` attribute — getter.
    get-text-content: func(self: node-handle) -> option<string>;
    /// `textContent` attribute — setter.
    set-text-content: func(self: node-handle, value: option<string>);

    /// `normalize()` operation.
    normalize: func(self: node-handle);

    /// `cloneNode()` operation.
    clone-node: func(self: node-handle, subtree: option<bool>) -> u64;

    /// `isEqualNode()` operation.
    is-equal-node: func(self: node-handle, other-node: option<u64>) -> bool;

    /// `isSameNode()` operation.
    is-same-node: func(self: node-handle, other-node: option<u64>) -> bool;

    /// `compareDocumentPosition()` operation.
    compare-document-position: func(self: node-handle, other: u64) -> u16;

    /// `contains()` operation.
    contains: func(self: node-handle, other: option<u64>) -> bool;

    /// `lookupPrefix()` operation.
    lookup-prefix: func(self: node-handle, namespace: option<string>) -> option<string>;

    /// `lookupNamespaceURI()` operation.
    lookup-namespace-uri: func(self: node-handle, prefix: option<string>) -> option<string>;

    /// `isDefaultNamespace()` operation.
    is-default-namespace: func(self: node-handle, namespace: option<string>) -> bool;

    /// `insertBefore()` operation.
    insert-before: func(self: node-handle, node: u64, child: option<u64>) -> u64;

    /// `appendChild()` operation.
    append-child: func(self: node-handle, node: u64) -> u64;

    /// `replaceChild()` operation.
    replace-child: func(self: node-handle, node: u64, child: u64) -> u64;

    /// `removeChild()` operation.
    remove-child: func(self: node-handle, child: u64) -> u64;
}

interface dom-implementation {
    /// Opaque handle to a host-side `DOMImplementation` instance.
    type dom-implementation-handle = u64;

    /// `createDocumentType()` operation.
    create-document-type: func(self: dom-implementation-handle, name: string, public-id: string, system-id: string) -> u64;

    /// `createDocument()` operation.
    create-document: func(self: dom-implementation-handle, namespace: option<string>, qualified-name: string, doctype: option<u64>) -> u64;

    /// `createHTMLDocument()` operation.
    create-html-document: func(self: dom-implementation-handle, title: option<string>) -> u64;

    /// `hasFeature()` operation.
    has-feature: func(self: dom-implementation-handle) -> bool;
}

interface document-type {
    /// Opaque handle to a host-side `DocumentType` instance.
    type document-type-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: document-type-handle) -> string;

    /// `publicId` attribute — getter.
    get-public-id: func(self: document-type-handle) -> string;

    /// `systemId` attribute — getter.
    get-system-id: func(self: document-type-handle) -> string;
}

interface shadow-root {
    /// Opaque handle to a host-side `ShadowRoot` instance.
    type shadow-root-handle = u64;

    /// `mode` attribute — getter.
    get-mode: func(self: shadow-root-handle) -> u64;

    /// `delegatesFocus` attribute — getter.
    get-delegates-focus: func(self: shadow-root-handle) -> bool;

    /// `slotAssignment` attribute — getter.
    get-slot-assignment: func(self: shadow-root-handle) -> u64;

    /// `clonable` attribute — getter.
    get-clonable: func(self: shadow-root-handle) -> bool;

    /// `serializable` attribute — getter.
    get-serializable: func(self: shadow-root-handle) -> bool;

    /// `host` attribute — getter.
    get-host: func(self: shadow-root-handle) -> u64;

    /// `onslotchange` attribute — getter.
    get-onslotchange: func(self: shadow-root-handle) -> u64;
    /// `onslotchange` attribute — setter.
    set-onslotchange: func(self: shadow-root-handle, value: u64);

    /// `setHTMLUnsafe()` operation.
    set-html-unsafe: func(self: shadow-root-handle, html: string);

    /// `getHTML()` operation.
    get-html: func(self: shadow-root-handle, options: option<u64>) -> string;

    /// `innerHTML` attribute — getter.
    get-inner-html: func(self: shadow-root-handle) -> string;
    /// `innerHTML` attribute — setter.
    set-inner-html: func(self: shadow-root-handle, value: string);
}

interface named-node-map {
    /// Opaque handle to a host-side `NamedNodeMap` instance.
    type named-node-map-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: named-node-map-handle) -> u32;

    /// `item()` operation.
    item: func(self: named-node-map-handle, index: u32) -> option<u64>;

    /// `getNamedItem()` operation.
    get-named-item: func(self: named-node-map-handle, qualified-name: string) -> option<u64>;

    /// `getNamedItemNS()` operation.
    get-named-item-ns: func(self: named-node-map-handle, namespace: option<string>, local-name: string) -> option<u64>;

    /// `setNamedItem()` operation.
    set-named-item: func(self: named-node-map-handle, attr: u64) -> option<u64>;

    /// `setNamedItemNS()` operation.
    set-named-item-ns: func(self: named-node-map-handle, attr: u64) -> option<u64>;

    /// `removeNamedItem()` operation.
    remove-named-item: func(self: named-node-map-handle, qualified-name: string) -> u64;

    /// `removeNamedItemNS()` operation.
    remove-named-item-ns: func(self: named-node-map-handle, namespace: option<string>, local-name: string) -> u64;
}

interface attr {
    /// Opaque handle to a host-side `Attr` instance.
    type attr-handle = u64;

    /// `namespaceURI` attribute — getter.
    get-namespace-uri: func(self: attr-handle) -> option<string>;

    /// `prefix` attribute — getter.
    get-prefix: func(self: attr-handle) -> option<string>;

    /// `localName` attribute — getter.
    get-local-name: func(self: attr-handle) -> string;

    /// `name` attribute — getter.
    get-name: func(self: attr-handle) -> string;

    /// `value` attribute — getter.
    get-value: func(self: attr-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: attr-handle, value: string);

    /// `ownerElement` attribute — getter.
    get-owner-element: func(self: attr-handle) -> option<u64>;

    /// `specified` attribute — getter.
    get-specified: func(self: attr-handle) -> bool;
}

interface character-data {
    /// Opaque handle to a host-side `CharacterData` instance.
    type character-data-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: character-data-handle) -> string;
    /// `data` attribute — setter.
    set-data: func(self: character-data-handle, value: string);

    /// `length` attribute — getter.
    get-length: func(self: character-data-handle) -> u32;

    /// `substringData()` operation.
    substring-data: func(self: character-data-handle, offset: u32, count: u32) -> string;

    /// `appendData()` operation.
    append-data: func(self: character-data-handle, data: string);

    /// `insertData()` operation.
    insert-data: func(self: character-data-handle, offset: u32, data: string);

    /// `deleteData()` operation.
    delete-data: func(self: character-data-handle, offset: u32, count: u32);

    /// `replaceData()` operation.
    replace-data: func(self: character-data-handle, offset: u32, count: u32, data: string);
}

interface text {
    /// Opaque handle to a host-side `Text` instance.
    type text-handle = u64;

    /// `splitText()` operation.
    split-text: func(self: text-handle, offset: u32) -> u64;

    /// `wholeText` attribute — getter.
    get-whole-text: func(self: text-handle) -> string;
}

interface processing-instruction {
    /// Opaque handle to a host-side `ProcessingInstruction` instance.
    type processing-instruction-handle = u64;

    /// `target` attribute — getter.
    get-target: func(self: processing-instruction-handle) -> string;
}

interface abstract-range {
    /// Opaque handle to a host-side `AbstractRange` instance.
    type abstract-range-handle = u64;

    /// `startContainer` attribute — getter.
    get-start-container: func(self: abstract-range-handle) -> u64;

    /// `startOffset` attribute — getter.
    get-start-offset: func(self: abstract-range-handle) -> u32;

    /// `endContainer` attribute — getter.
    get-end-container: func(self: abstract-range-handle) -> u64;

    /// `endOffset` attribute — getter.
    get-end-offset: func(self: abstract-range-handle) -> u32;

    /// `collapsed` attribute — getter.
    get-collapsed: func(self: abstract-range-handle) -> bool;
}

interface node-iterator {
    /// Opaque handle to a host-side `NodeIterator` instance.
    type node-iterator-handle = u64;

    /// `root` attribute — getter.
    get-root: func(self: node-iterator-handle) -> u64;

    /// `referenceNode` attribute — getter.
    get-reference-node: func(self: node-iterator-handle) -> u64;

    /// `pointerBeforeReferenceNode` attribute — getter.
    get-pointer-before-reference-node: func(self: node-iterator-handle) -> bool;

    /// `whatToShow` attribute — getter.
    get-what-to-show: func(self: node-iterator-handle) -> u32;

    /// `filter` attribute — getter.
    get-filter: func(self: node-iterator-handle) -> option<u64>;

    /// `nextNode()` operation.
    next-node: func(self: node-iterator-handle) -> option<u64>;

    /// `previousNode()` operation.
    previous-node: func(self: node-iterator-handle) -> option<u64>;

    /// `detach()` operation.
    detach: func(self: node-iterator-handle);
}

interface tree-walker {
    /// Opaque handle to a host-side `TreeWalker` instance.
    type tree-walker-handle = u64;

    /// `root` attribute — getter.
    get-root: func(self: tree-walker-handle) -> u64;

    /// `whatToShow` attribute — getter.
    get-what-to-show: func(self: tree-walker-handle) -> u32;

    /// `filter` attribute — getter.
    get-filter: func(self: tree-walker-handle) -> option<u64>;

    /// `currentNode` attribute — getter.
    get-current-node: func(self: tree-walker-handle) -> u64;
    /// `currentNode` attribute — setter.
    set-current-node: func(self: tree-walker-handle, value: u64);

    /// `parentNode()` operation.
    parent-node: func(self: tree-walker-handle) -> option<u64>;

    /// `firstChild()` operation.
    first-child: func(self: tree-walker-handle) -> option<u64>;

    /// `lastChild()` operation.
    last-child: func(self: tree-walker-handle) -> option<u64>;

    /// `previousSibling()` operation.
    previous-sibling: func(self: tree-walker-handle) -> option<u64>;

    /// `nextSibling()` operation.
    next-sibling: func(self: tree-walker-handle) -> option<u64>;

    /// `previousNode()` operation.
    previous-node: func(self: tree-walker-handle) -> option<u64>;

    /// `nextNode()` operation.
    next-node: func(self: tree-walker-handle) -> option<u64>;
}

interface node-filter {
    /// Opaque handle to a host-side `NodeFilter` instance.
    type node-filter-handle = u64;

    /// `acceptNode()` operation.
    accept-node: func(self: node-filter-handle, node: u64) -> u16;
}

interface dom-token-list {
    /// Opaque handle to a host-side `DOMTokenList` instance.
    type dom-token-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: dom-token-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: dom-token-list-handle, index: u32) -> option<string>;

    /// `contains()` operation.
    contains: func(self: dom-token-list-handle, token: string) -> bool;

    /// `add()` operation.
    add: func(self: dom-token-list-handle, tokens: list<string>);

    /// `remove()` operation.
    remove: func(self: dom-token-list-handle, tokens: list<string>);

    /// `toggle()` operation.
    toggle: func(self: dom-token-list-handle, token: string, force: option<bool>) -> bool;

    /// `replace()` operation.
    replace: func(self: dom-token-list-handle, token: string, new-token: string) -> bool;

    /// `supports()` operation.
    supports: func(self: dom-token-list-handle, token: string) -> bool;

    /// `value` attribute — getter.
    get-value: func(self: dom-token-list-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: dom-token-list-handle, value: string);
}

interface x-path-result {
    /// Opaque handle to a host-side `XPathResult` instance.
    type x-path-result-handle = u64;

    /// `resultType` attribute — getter.
    get-result-type: func(self: x-path-result-handle) -> u16;

    /// `numberValue` attribute — getter.
    get-number-value: func(self: x-path-result-handle) -> f64;

    /// `stringValue` attribute — getter.
    get-string-value: func(self: x-path-result-handle) -> string;

    /// `booleanValue` attribute — getter.
    get-boolean-value: func(self: x-path-result-handle) -> bool;

    /// `singleNodeValue` attribute — getter.
    get-single-node-value: func(self: x-path-result-handle) -> option<u64>;

    /// `invalidIteratorState` attribute — getter.
    get-invalid-iterator-state: func(self: x-path-result-handle) -> bool;

    /// `snapshotLength` attribute — getter.
    get-snapshot-length: func(self: x-path-result-handle) -> u32;

    /// `iterateNext()` operation.
    iterate-next: func(self: x-path-result-handle) -> option<u64>;

    /// `snapshotItem()` operation.
    snapshot-item: func(self: x-path-result-handle, index: u32) -> option<u64>;
}

interface x-path-expression {
    /// Opaque handle to a host-side `XPathExpression` instance.
    type x-path-expression-handle = u64;

    /// `evaluate()` operation.
    evaluate: func(self: x-path-expression-handle, context-node: u64, %type: option<u16>, %result: option<u64>) -> u64;
}

interface x-path-ns-resolver {
    /// Opaque handle to a host-side `XPathNSResolver` instance.
    type x-path-ns-resolver-handle = u64;

    /// `lookupNamespaceURI()` operation.
    lookup-namespace-uri: func(self: x-path-ns-resolver-handle, prefix: option<string>) -> option<string>;
}

interface x-path-evaluator-base {
    /// Opaque handle to a host-side `XPathEvaluatorBase` instance.
    type x-path-evaluator-base-handle = u64;

    /// `createExpression()` operation.
    create-expression: func(self: x-path-evaluator-base-handle, expression: string, resolver: option<u64>) -> u64;

    /// `createNSResolver()` operation.
    create-ns-resolver: func(self: x-path-evaluator-base-handle, node-resolver: u64) -> u64;

    /// `evaluate()` operation.
    evaluate: func(self: x-path-evaluator-base-handle, expression: string, context-node: u64, resolver: option<u64>, %type: option<u16>, %result: option<u64>) -> u64;
}

interface xslt-processor {
    /// Opaque handle to a host-side `XSLTProcessor` instance.
    type xslt-processor-handle = u64;

    /// `importStylesheet()` operation.
    import-stylesheet: func(self: xslt-processor-handle, style: u64);

    /// `transformToFragment()` operation.
    transform-to-fragment: func(self: xslt-processor-handle, source: u64, output: u64) -> u64;

    /// `transformToDocument()` operation.
    transform-to-document: func(self: xslt-processor-handle, source: u64) -> u64;

    /// `setParameter()` operation.
    set-parameter: func(self: xslt-processor-handle, namespace-uri: string, local-name: string, value: string);

    /// `getParameter()` operation.
    get-parameter: func(self: xslt-processor-handle, namespace-uri: string, local-name: string) -> string;

    /// `removeParameter()` operation.
    remove-parameter: func(self: xslt-processor-handle, namespace-uri: string, local-name: string);

    /// `clearParameters()` operation.
    clear-parameters: func(self: xslt-processor-handle);

    /// `reset()` operation.
    reset: func(self: xslt-processor-handle);
}

interface selection {
    /// Opaque handle to a host-side `Selection` instance.
    type selection-handle = u64;

    /// `anchorNode` attribute — getter.
    get-anchor-node: func(self: selection-handle) -> option<u64>;

    /// `anchorOffset` attribute — getter.
    get-anchor-offset: func(self: selection-handle) -> u32;

    /// `focusNode` attribute — getter.
    get-focus-node: func(self: selection-handle) -> option<u64>;

    /// `focusOffset` attribute — getter.
    get-focus-offset: func(self: selection-handle) -> u32;

    /// `isCollapsed` attribute — getter.
    get-is-collapsed: func(self: selection-handle) -> bool;

    /// `rangeCount` attribute — getter.
    get-range-count: func(self: selection-handle) -> u32;

    /// `type` attribute — getter.
    get-type: func(self: selection-handle) -> string;

    /// `direction` attribute — getter.
    get-direction: func(self: selection-handle) -> string;

    /// `getRangeAt()` operation.
    get-range-at: func(self: selection-handle, index: u32) -> u64;

    /// `addRange()` operation.
    add-range: func(self: selection-handle, range: u64);

    /// `removeRange()` operation.
    remove-range: func(self: selection-handle, range: u64);

    /// `removeAllRanges()` operation.
    remove-all-ranges: func(self: selection-handle);

    /// `empty()` operation.
    empty: func(self: selection-handle);

    /// `getComposedRanges()` operation.
    get-composed-ranges: func(self: selection-handle, options: option<u64>) -> list<u64>;

    /// `collapse()` operation.
    collapse: func(self: selection-handle, node: option<u64>, offset: option<u32>);

    /// `setPosition()` operation.
    set-position: func(self: selection-handle, node: option<u64>, offset: option<u32>);

    /// `collapseToStart()` operation.
    collapse-to-start: func(self: selection-handle);

    /// `collapseToEnd()` operation.
    collapse-to-end: func(self: selection-handle);

    /// `extend()` operation.
    extend: func(self: selection-handle, node: u64, offset: option<u32>);

    /// `setBaseAndExtent()` operation.
    set-base-and-extent: func(self: selection-handle, anchor-node: u64, anchor-offset: u32, focus-node: u64, focus-offset: u32);

    /// `selectAllChildren()` operation.
    select-all-children: func(self: selection-handle, node: u64);

    /// `modify()` operation.
    modify: func(self: selection-handle, alter: option<string>, direction: option<string>, granularity: option<string>);

    /// `deleteFromDocument()` operation.
    delete-from-document: func(self: selection-handle);

    /// `containsNode()` operation.
    contains-node: func(self: selection-handle, node: u64, allow-partial-containment: option<bool>) -> bool;
}

interface clipboard-event {
    /// Opaque handle to a host-side `ClipboardEvent` instance.
    type clipboard-event-handle = u64;

    /// `clipboardData` attribute — getter.
    get-clipboard-data: func(self: clipboard-event-handle) -> option<u64>;
}

interface clipboard-change-event {
    /// Opaque handle to a host-side `ClipboardChangeEvent` instance.
    type clipboard-change-event-handle = u64;

    /// `types` attribute — getter.
    get-types: func(self: clipboard-change-event-handle) -> list<string>;

    /// `changeId` attribute — getter.
    get-change-id: func(self: clipboard-change-event-handle) -> u64;
}

interface clipboard-item {
    /// Opaque handle to a host-side `ClipboardItem` instance.
    type clipboard-item-handle = u64;

    /// `presentationStyle` attribute — getter.
    get-presentation-style: func(self: clipboard-item-handle) -> u64;

    /// `types` attribute — getter.
    get-types: func(self: clipboard-item-handle) -> list<string>;

    /// `getType()` operation.
    get-type: func(self: clipboard-item-handle, %type: string) -> u64;

    /// `supports()` operation.
    supports: func(%type: string) -> bool;
}

interface clipboard {
    /// Opaque handle to a host-side `Clipboard` instance.
    type clipboard-handle = u64;

    /// `read()` operation.
    read: func(self: clipboard-handle, formats: option<u64>) -> u64;

    /// `readText()` operation.
    read-text: func(self: clipboard-handle) -> u64;

    /// `write()` operation.
    write: func(self: clipboard-handle, data: u64) -> u64;

    /// `writeText()` operation.
    write-text: func(self: clipboard-handle, data: string) -> u64;
}

interface pointer-event {
    /// Opaque handle to a host-side `PointerEvent` instance.
    type pointer-event-handle = u64;

    /// `pointerId` attribute — getter.
    get-pointer-id: func(self: pointer-event-handle) -> s32;

    /// `width` attribute — getter.
    get-width: func(self: pointer-event-handle) -> f64;

    /// `height` attribute — getter.
    get-height: func(self: pointer-event-handle) -> f64;

    /// `pressure` attribute — getter.
    get-pressure: func(self: pointer-event-handle) -> f32;

    /// `tangentialPressure` attribute — getter.
    get-tangential-pressure: func(self: pointer-event-handle) -> f32;

    /// `tiltX` attribute — getter.
    get-tilt-x: func(self: pointer-event-handle) -> s32;

    /// `tiltY` attribute — getter.
    get-tilt-y: func(self: pointer-event-handle) -> s32;

    /// `twist` attribute — getter.
    get-twist: func(self: pointer-event-handle) -> s32;

    /// `altitudeAngle` attribute — getter.
    get-altitude-angle: func(self: pointer-event-handle) -> f64;

    /// `azimuthAngle` attribute — getter.
    get-azimuth-angle: func(self: pointer-event-handle) -> f64;

    /// `pointerType` attribute — getter.
    get-pointer-type: func(self: pointer-event-handle) -> string;

    /// `isPrimary` attribute — getter.
    get-is-primary: func(self: pointer-event-handle) -> bool;

    /// `persistentDeviceId` attribute — getter.
    get-persistent-device-id: func(self: pointer-event-handle) -> s32;

    /// `getCoalescedEvents()` operation.
    get-coalesced-events: func(self: pointer-event-handle) -> list<u64>;

    /// `getPredictedEvents()` operation.
    get-predicted-events: func(self: pointer-event-handle) -> list<u64>;
}

interface wheel-event {
    /// Opaque handle to a host-side `WheelEvent` instance.
    type wheel-event-handle = u64;

    /// `deltaX` attribute — getter.
    get-delta-x: func(self: wheel-event-handle) -> f64;

    /// `deltaY` attribute — getter.
    get-delta-y: func(self: wheel-event-handle) -> f64;

    /// `deltaZ` attribute — getter.
    get-delta-z: func(self: wheel-event-handle) -> f64;

    /// `deltaMode` attribute — getter.
    get-delta-mode: func(self: wheel-event-handle) -> u32;
}

interface touch {
    /// Opaque handle to a host-side `Touch` instance.
    type touch-handle = u64;

    /// `identifier` attribute — getter.
    get-identifier: func(self: touch-handle) -> s32;

    /// `target` attribute — getter.
    get-target: func(self: touch-handle) -> u64;

    /// `screenX` attribute — getter.
    get-screen-x: func(self: touch-handle) -> f64;

    /// `screenY` attribute — getter.
    get-screen-y: func(self: touch-handle) -> f64;

    /// `clientX` attribute — getter.
    get-client-x: func(self: touch-handle) -> f64;

    /// `clientY` attribute — getter.
    get-client-y: func(self: touch-handle) -> f64;

    /// `pageX` attribute — getter.
    get-page-x: func(self: touch-handle) -> f64;

    /// `pageY` attribute — getter.
    get-page-y: func(self: touch-handle) -> f64;

    /// `radiusX` attribute — getter.
    get-radius-x: func(self: touch-handle) -> f32;

    /// `radiusY` attribute — getter.
    get-radius-y: func(self: touch-handle) -> f32;

    /// `rotationAngle` attribute — getter.
    get-rotation-angle: func(self: touch-handle) -> f32;

    /// `force` attribute — getter.
    get-force: func(self: touch-handle) -> f32;

    /// `altitudeAngle` attribute — getter.
    get-altitude-angle: func(self: touch-handle) -> f32;

    /// `azimuthAngle` attribute — getter.
    get-azimuth-angle: func(self: touch-handle) -> f32;

    /// `touchType` attribute — getter.
    get-touch-type: func(self: touch-handle) -> u64;
}

interface touch-list {
    /// Opaque handle to a host-side `TouchList` instance.
    type touch-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: touch-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: touch-list-handle, index: u32) -> option<u64>;
}

interface touch-event {
    /// Opaque handle to a host-side `TouchEvent` instance.
    type touch-event-handle = u64;

    /// `touches` attribute — getter.
    get-touches: func(self: touch-event-handle) -> u64;

    /// `targetTouches` attribute — getter.
    get-target-touches: func(self: touch-event-handle) -> u64;

    /// `changedTouches` attribute — getter.
    get-changed-touches: func(self: touch-event-handle) -> u64;

    /// `altKey` attribute — getter.
    get-alt-key: func(self: touch-event-handle) -> bool;

    /// `metaKey` attribute — getter.
    get-meta-key: func(self: touch-event-handle) -> bool;

    /// `ctrlKey` attribute — getter.
    get-ctrl-key: func(self: touch-event-handle) -> bool;

    /// `shiftKey` attribute — getter.
    get-shift-key: func(self: touch-event-handle) -> bool;

    /// `getModifierState()` operation.
    get-modifier-state: func(self: touch-event-handle, key-arg: string) -> bool;
}

interface ui-event {
    /// Opaque handle to a host-side `UIEvent` instance.
    type ui-event-handle = u64;

    /// `view` attribute — getter.
    get-view: func(self: ui-event-handle) -> option<u64>;

    /// `detail` attribute — getter.
    get-detail: func(self: ui-event-handle) -> s32;

    /// `initUIEvent()` operation.
    init-ui-event: func(self: ui-event-handle, type-arg: string, bubbles-arg: option<bool>, cancelable-arg: option<bool>, view-arg: option<u64>, detail-arg: option<s32>);

    /// `which` attribute — getter.
    get-which: func(self: ui-event-handle) -> u32;
}

interface focus-event {
    /// Opaque handle to a host-side `FocusEvent` instance.
    type focus-event-handle = u64;

    /// `relatedTarget` attribute — getter.
    get-related-target: func(self: focus-event-handle) -> option<u64>;
}

interface input-event {
    /// Opaque handle to a host-side `InputEvent` instance.
    type input-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: input-event-handle) -> option<string>;

    /// `isComposing` attribute — getter.
    get-is-composing: func(self: input-event-handle) -> bool;

    /// `inputType` attribute — getter.
    get-input-type: func(self: input-event-handle) -> string;
}

interface keyboard-event {
    /// Opaque handle to a host-side `KeyboardEvent` instance.
    type keyboard-event-handle = u64;

    /// `key` attribute — getter.
    get-key: func(self: keyboard-event-handle) -> string;

    /// `code` attribute — getter.
    get-code: func(self: keyboard-event-handle) -> string;

    /// `location` attribute — getter.
    get-location: func(self: keyboard-event-handle) -> u32;

    /// `ctrlKey` attribute — getter.
    get-ctrl-key: func(self: keyboard-event-handle) -> bool;

    /// `shiftKey` attribute — getter.
    get-shift-key: func(self: keyboard-event-handle) -> bool;

    /// `altKey` attribute — getter.
    get-alt-key: func(self: keyboard-event-handle) -> bool;

    /// `metaKey` attribute — getter.
    get-meta-key: func(self: keyboard-event-handle) -> bool;

    /// `repeat` attribute — getter.
    get-repeat: func(self: keyboard-event-handle) -> bool;

    /// `isComposing` attribute — getter.
    get-is-composing: func(self: keyboard-event-handle) -> bool;

    /// `getModifierState()` operation.
    get-modifier-state: func(self: keyboard-event-handle, key-arg: string) -> bool;

    /// `initKeyboardEvent()` operation.
    init-keyboard-event: func(self: keyboard-event-handle, type-arg: string, bubbles-arg: option<bool>, cancelable-arg: option<bool>, view-arg: option<u64>, key-arg: option<string>, location-arg: option<u32>, ctrl-key: option<bool>, alt-key: option<bool>, shift-key: option<bool>, meta-key: option<bool>);

    /// `charCode` attribute — getter.
    get-char-code: func(self: keyboard-event-handle) -> u32;

    /// `keyCode` attribute — getter.
    get-key-code: func(self: keyboard-event-handle) -> u32;
}

interface composition-event {
    /// Opaque handle to a host-side `CompositionEvent` instance.
    type composition-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: composition-event-handle) -> string;

    /// `initCompositionEvent()` operation.
    init-composition-event: func(self: composition-event-handle, type-arg: string, bubbles-arg: option<bool>, cancelable-arg: option<bool>, view-arg: option<u64>, data-arg: option<string>);
}

interface text-event {
    /// Opaque handle to a host-side `TextEvent` instance.
    type text-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: text-event-handle) -> string;

    /// `initTextEvent()` operation.
    init-text-event: func(self: text-event-handle, %type: string, bubbles: option<bool>, cancelable: option<bool>, view: option<u64>, data: option<string>);
}

interface blob {
    /// Opaque handle to a host-side `Blob` instance.
    type blob-handle = u64;

    /// `size` attribute — getter.
    get-size: func(self: blob-handle) -> u64;

    /// `type` attribute — getter.
    get-type: func(self: blob-handle) -> string;

    /// `slice()` operation.
    slice: func(self: blob-handle, start: option<s64>, end: option<s64>, content-type: option<string>) -> u64;

    /// `stream()` operation.
    get-stream: func(self: blob-handle) -> u64;

    /// `text()` operation.
    text: func(self: blob-handle) -> u64;

    /// `arrayBuffer()` operation.
    array-buffer: func(self: blob-handle) -> u64;

    /// `bytes()` operation.
    bytes: func(self: blob-handle) -> u64;
}

interface file {
    /// Opaque handle to a host-side `File` instance.
    type file-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: file-handle) -> string;

    /// `lastModified` attribute — getter.
    get-last-modified: func(self: file-handle) -> s64;
}

interface file-list {
    /// Opaque handle to a host-side `FileList` instance.
    type file-list-handle = u64;

    /// `item()` operation.
    item: func(self: file-list-handle, index: u32) -> option<u64>;

    /// `length` attribute — getter.
    get-length: func(self: file-list-handle) -> u32;
}

interface file-reader {
    /// Opaque handle to a host-side `FileReader` instance.
    type file-reader-handle = u64;

    /// `readAsArrayBuffer()` operation.
    read-as-array-buffer: func(self: file-reader-handle, blob: u64);

    /// `readAsBinaryString()` operation.
    read-as-binary-string: func(self: file-reader-handle, blob: u64);

    /// `readAsText()` operation.
    read-as-text: func(self: file-reader-handle, blob: u64, encoding: option<string>);

    /// `readAsDataURL()` operation.
    read-as-data-url: func(self: file-reader-handle, blob: u64);

    /// `abort()` operation.
    abort: func(self: file-reader-handle);

    /// `readyState` attribute — getter.
    get-ready-state: func(self: file-reader-handle) -> u16;

    /// `result` attribute — getter.
    get-result: func(self: file-reader-handle) -> option<string>;

    /// `error` attribute — getter.
    get-error: func(self: file-reader-handle) -> option<u64>;

    /// `onloadstart` attribute — getter.
    get-onloadstart: func(self: file-reader-handle) -> u64;
    /// `onloadstart` attribute — setter.
    set-onloadstart: func(self: file-reader-handle, value: u64);

    /// `onprogress` attribute — getter.
    get-onprogress: func(self: file-reader-handle) -> u64;
    /// `onprogress` attribute — setter.
    set-onprogress: func(self: file-reader-handle, value: u64);

    /// `onload` attribute — getter.
    get-onload: func(self: file-reader-handle) -> u64;
    /// `onload` attribute — setter.
    set-onload: func(self: file-reader-handle, value: u64);

    /// `onabort` attribute — getter.
    get-onabort: func(self: file-reader-handle) -> u64;
    /// `onabort` attribute — setter.
    set-onabort: func(self: file-reader-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: file-reader-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: file-reader-handle, value: u64);

    /// `onloadend` attribute — getter.
    get-onloadend: func(self: file-reader-handle) -> u64;
    /// `onloadend` attribute — setter.
    set-onloadend: func(self: file-reader-handle, value: u64);
}

interface file-reader-sync {
    /// Opaque handle to a host-side `FileReaderSync` instance.
    type file-reader-sync-handle = u64;

    /// `readAsArrayBuffer()` operation.
    read-as-array-buffer: func(self: file-reader-sync-handle, blob: u64) -> list<u8>;

    /// `readAsBinaryString()` operation.
    read-as-binary-string: func(self: file-reader-sync-handle, blob: u64) -> string;

    /// `readAsText()` operation.
    read-as-text: func(self: file-reader-sync-handle, blob: u64, encoding: option<string>) -> string;

    /// `readAsDataURL()` operation.
    read-as-data-url: func(self: file-reader-sync-handle, blob: u64) -> string;
}

interface url {
    /// Opaque handle to a host-side `URL` instance.
    type url-handle = u64;

    /// `createObjectURL()` operation.
    create-object-url: func(obj: u64) -> string;

    /// `revokeObjectURL()` operation.
    revoke-object-url: func(url: string);

    /// `parse()` operation.
    parse: func(url: string, base: option<string>) -> option<u64>;

    /// `canParse()` operation.
    can-parse: func(url: string, base: option<string>) -> bool;

    /// `href` attribute — getter.
    get-href: func(self: url-handle) -> string;
    /// `href` attribute — setter.
    set-href: func(self: url-handle, value: string);

    /// `origin` attribute — getter.
    get-origin: func(self: url-handle) -> string;

    /// `protocol` attribute — getter.
    get-protocol: func(self: url-handle) -> string;
    /// `protocol` attribute — setter.
    set-protocol: func(self: url-handle, value: string);

    /// `username` attribute — getter.
    get-username: func(self: url-handle) -> string;
    /// `username` attribute — setter.
    set-username: func(self: url-handle, value: string);

    /// `password` attribute — getter.
    get-password: func(self: url-handle) -> string;
    /// `password` attribute — setter.
    set-password: func(self: url-handle, value: string);

    /// `host` attribute — getter.
    get-host: func(self: url-handle) -> string;
    /// `host` attribute — setter.
    set-host: func(self: url-handle, value: string);

    /// `hostname` attribute — getter.
    get-hostname: func(self: url-handle) -> string;
    /// `hostname` attribute — setter.
    set-hostname: func(self: url-handle, value: string);

    /// `port` attribute — getter.
    get-port: func(self: url-handle) -> string;
    /// `port` attribute — setter.
    set-port: func(self: url-handle, value: string);

    /// `pathname` attribute — getter.
    get-pathname: func(self: url-handle) -> string;
    /// `pathname` attribute — setter.
    set-pathname: func(self: url-handle, value: string);

    /// `search` attribute — getter.
    get-search: func(self: url-handle) -> string;
    /// `search` attribute — setter.
    set-search: func(self: url-handle, value: string);

    /// `searchParams` attribute — getter.
    get-search-params: func(self: url-handle) -> u64;

    /// `hash` attribute — getter.
    get-hash: func(self: url-handle) -> string;
    /// `hash` attribute — setter.
    set-hash: func(self: url-handle, value: string);

    /// `toJSON()` operation.
    to-json: func(self: url-handle) -> string;
}

interface headers {
    /// Opaque handle to a host-side `Headers` instance.
    type headers-handle = u64;

    /// `append()` operation.
    append: func(self: headers-handle, name: string, value: string);

    /// `delete()` operation.
    delete: func(self: headers-handle, name: string);

    /// `get()` operation.
    get: func(self: headers-handle, name: string) -> option<string>;

    /// `getSetCookie()` operation.
    get-set-cookie: func(self: headers-handle) -> list<string>;

    /// `has()` operation.
    has: func(self: headers-handle, name: string) -> bool;

    /// `set()` operation.
    set: func(self: headers-handle, name: string, value: string);
}

interface body {
    /// Opaque handle to a host-side `Body` instance.
    type body-handle = u64;

    /// `body` attribute — getter.
    get-body: func(self: body-handle) -> option<u64>;

    /// `bodyUsed` attribute — getter.
    get-body-used: func(self: body-handle) -> bool;

    /// `arrayBuffer()` operation.
    array-buffer: func(self: body-handle) -> u64;

    /// `blob()` operation.
    blob: func(self: body-handle) -> u64;

    /// `bytes()` operation.
    bytes: func(self: body-handle) -> u64;

    /// `formData()` operation.
    form-data: func(self: body-handle) -> u64;

    /// `json()` operation.
    json: func(self: body-handle) -> u64;

    /// `text()` operation.
    text: func(self: body-handle) -> u64;
}

interface request {
    /// Opaque handle to a host-side `Request` instance.
    type request-handle = u64;

    /// `method` attribute — getter.
    get-method: func(self: request-handle) -> string;

    /// `url` attribute — getter.
    get-url: func(self: request-handle) -> string;

    /// `headers` attribute — getter.
    get-headers: func(self: request-handle) -> u64;

    /// `destination` attribute — getter.
    get-destination: func(self: request-handle) -> u64;

    /// `referrer` attribute — getter.
    get-referrer: func(self: request-handle) -> string;

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: request-handle) -> u64;

    /// `mode` attribute — getter.
    get-mode: func(self: request-handle) -> u64;

    /// `credentials` attribute — getter.
    get-credentials: func(self: request-handle) -> u64;

    /// `cache` attribute — getter.
    get-cache: func(self: request-handle) -> u64;

    /// `redirect` attribute — getter.
    get-redirect: func(self: request-handle) -> u64;

    /// `integrity` attribute — getter.
    get-integrity: func(self: request-handle) -> string;

    /// `keepalive` attribute — getter.
    get-keepalive: func(self: request-handle) -> bool;

    /// `isReloadNavigation` attribute — getter.
    get-is-reload-navigation: func(self: request-handle) -> bool;

    /// `isHistoryNavigation` attribute — getter.
    get-is-history-navigation: func(self: request-handle) -> bool;

    /// `signal` attribute — getter.
    get-signal: func(self: request-handle) -> u64;

    /// `duplex` attribute — getter.
    get-duplex: func(self: request-handle) -> u64;

    /// `clone()` operation.
    clone: func(self: request-handle) -> u64;
}

interface response {
    /// Opaque handle to a host-side `Response` instance.
    type response-handle = u64;

    /// `error()` operation.
    error: func() -> u64;

    /// `redirect()` operation.
    redirect: func(url: string, status: option<u16>) -> u64;

    /// `json()` operation.
    json: func(data: string, init: option<u64>) -> u64;

    /// `type` attribute — getter.
    get-type: func(self: response-handle) -> u64;

    /// `url` attribute — getter.
    get-url: func(self: response-handle) -> string;

    /// `redirected` attribute — getter.
    get-redirected: func(self: response-handle) -> bool;

    /// `status` attribute — getter.
    get-status: func(self: response-handle) -> u16;

    /// `ok` attribute — getter.
    get-ok: func(self: response-handle) -> bool;

    /// `statusText` attribute — getter.
    get-status-text: func(self: response-handle) -> string;

    /// `headers` attribute — getter.
    get-headers: func(self: response-handle) -> u64;

    /// `clone()` operation.
    clone: func(self: response-handle) -> u64;
}

interface fetch-later-result {
    /// Opaque handle to a host-side `FetchLaterResult` instance.
    type fetch-later-result-handle = u64;

    /// `activated` attribute — getter.
    get-activated: func(self: fetch-later-result-handle) -> bool;
}

interface readable-stream {
    /// Opaque handle to a host-side `ReadableStream` instance.
    type readable-stream-handle = u64;

    /// `from()` operation.
    %from: func(async-iterable: string) -> u64;

    /// `locked` attribute — getter.
    get-locked: func(self: readable-stream-handle) -> bool;

    /// `cancel()` operation.
    cancel: func(self: readable-stream-handle, reason: option<string>) -> u64;

    /// `getReader()` operation.
    get-reader: func(self: readable-stream-handle, options: option<u64>) -> u64;

    /// `pipeThrough()` operation.
    pipe-through: func(self: readable-stream-handle, transform: u64, options: option<u64>) -> u64;

    /// `pipeTo()` operation.
    pipe-to: func(self: readable-stream-handle, destination: u64, options: option<u64>) -> u64;

    /// `tee()` operation.
    tee: func(self: readable-stream-handle) -> list<u64>;
}

interface readable-stream-generic-reader {
    /// Opaque handle to a host-side `ReadableStreamGenericReader` instance.
    type readable-stream-generic-reader-handle = u64;

    /// `closed` attribute — getter.
    get-closed: func(self: readable-stream-generic-reader-handle) -> u64;

    /// `cancel()` operation.
    cancel: func(self: readable-stream-generic-reader-handle, reason: option<string>) -> u64;
}

interface readable-stream-default-reader {
    /// Opaque handle to a host-side `ReadableStreamDefaultReader` instance.
    type readable-stream-default-reader-handle = u64;

    /// `read()` operation.
    read: func(self: readable-stream-default-reader-handle) -> u64;

    /// `releaseLock()` operation.
    release-lock: func(self: readable-stream-default-reader-handle);
}

interface readable-stream-byob-reader {
    /// Opaque handle to a host-side `ReadableStreamBYOBReader` instance.
    type readable-stream-byob-reader-handle = u64;

    /// `read()` operation.
    read: func(self: readable-stream-byob-reader-handle, view: list<u8>, options: option<u64>) -> u64;

    /// `releaseLock()` operation.
    release-lock: func(self: readable-stream-byob-reader-handle);
}

interface readable-stream-default-controller {
    /// Opaque handle to a host-side `ReadableStreamDefaultController` instance.
    type readable-stream-default-controller-handle = u64;

    /// `desiredSize` attribute — getter.
    get-desired-size: func(self: readable-stream-default-controller-handle) -> option<f64>;

    /// `close()` operation.
    close: func(self: readable-stream-default-controller-handle);

    /// `enqueue()` operation.
    enqueue: func(self: readable-stream-default-controller-handle, chunk: option<string>);

    /// `error()` operation.
    error: func(self: readable-stream-default-controller-handle, e: option<string>);
}

interface readable-byte-stream-controller {
    /// Opaque handle to a host-side `ReadableByteStreamController` instance.
    type readable-byte-stream-controller-handle = u64;

    /// `byobRequest` attribute — getter.
    get-byob-request: func(self: readable-byte-stream-controller-handle) -> option<u64>;

    /// `desiredSize` attribute — getter.
    get-desired-size: func(self: readable-byte-stream-controller-handle) -> option<f64>;

    /// `close()` operation.
    close: func(self: readable-byte-stream-controller-handle);

    /// `enqueue()` operation.
    enqueue: func(self: readable-byte-stream-controller-handle, chunk: list<u8>);

    /// `error()` operation.
    error: func(self: readable-byte-stream-controller-handle, e: option<string>);
}

interface readable-stream-byob-request {
    /// Opaque handle to a host-side `ReadableStreamBYOBRequest` instance.
    type readable-stream-byob-request-handle = u64;

    /// `view` attribute — getter.
    get-view: func(self: readable-stream-byob-request-handle) -> option<list<u8>>;

    /// `respond()` operation.
    respond: func(self: readable-stream-byob-request-handle, bytes-written: u64);

    /// `respondWithNewView()` operation.
    respond-with-new-view: func(self: readable-stream-byob-request-handle, view: list<u8>);
}

interface writable-stream {
    /// Opaque handle to a host-side `WritableStream` instance.
    type writable-stream-handle = u64;

    /// `locked` attribute — getter.
    get-locked: func(self: writable-stream-handle) -> bool;

    /// `abort()` operation.
    abort: func(self: writable-stream-handle, reason: option<string>) -> u64;

    /// `close()` operation.
    close: func(self: writable-stream-handle) -> u64;

    /// `getWriter()` operation.
    get-writer: func(self: writable-stream-handle) -> u64;
}

interface writable-stream-default-writer {
    /// Opaque handle to a host-side `WritableStreamDefaultWriter` instance.
    type writable-stream-default-writer-handle = u64;

    /// `closed` attribute — getter.
    get-closed: func(self: writable-stream-default-writer-handle) -> u64;

    /// `desiredSize` attribute — getter.
    get-desired-size: func(self: writable-stream-default-writer-handle) -> option<f64>;

    /// `ready` attribute — getter.
    get-ready: func(self: writable-stream-default-writer-handle) -> u64;

    /// `abort()` operation.
    abort: func(self: writable-stream-default-writer-handle, reason: option<string>) -> u64;

    /// `close()` operation.
    close: func(self: writable-stream-default-writer-handle) -> u64;

    /// `releaseLock()` operation.
    release-lock: func(self: writable-stream-default-writer-handle);

    /// `write()` operation.
    write: func(self: writable-stream-default-writer-handle, chunk: option<string>) -> u64;
}

interface writable-stream-default-controller {
    /// Opaque handle to a host-side `WritableStreamDefaultController` instance.
    type writable-stream-default-controller-handle = u64;

    /// `signal` attribute — getter.
    get-signal: func(self: writable-stream-default-controller-handle) -> u64;

    /// `error()` operation.
    error: func(self: writable-stream-default-controller-handle, e: option<string>);
}

interface transform-stream {
    /// Opaque handle to a host-side `TransformStream` instance.
    type transform-stream-handle = u64;

    /// `readable` attribute — getter.
    get-readable: func(self: transform-stream-handle) -> u64;

    /// `writable` attribute — getter.
    get-writable: func(self: transform-stream-handle) -> u64;
}

interface transform-stream-default-controller {
    /// Opaque handle to a host-side `TransformStreamDefaultController` instance.
    type transform-stream-default-controller-handle = u64;

    /// `desiredSize` attribute — getter.
    get-desired-size: func(self: transform-stream-default-controller-handle) -> option<f64>;

    /// `enqueue()` operation.
    enqueue: func(self: transform-stream-default-controller-handle, chunk: option<string>);

    /// `error()` operation.
    error: func(self: transform-stream-default-controller-handle, reason: option<string>);

    /// `terminate()` operation.
    terminate: func(self: transform-stream-default-controller-handle);
}

interface byte-length-queuing-strategy {
    /// Opaque handle to a host-side `ByteLengthQueuingStrategy` instance.
    type byte-length-queuing-strategy-handle = u64;

    /// `highWaterMark` attribute — getter.
    get-high-water-mark: func(self: byte-length-queuing-strategy-handle) -> f64;

    /// `size` attribute — getter.
    get-size: func(self: byte-length-queuing-strategy-handle) -> u64;
}

interface count-queuing-strategy {
    /// Opaque handle to a host-side `CountQueuingStrategy` instance.
    type count-queuing-strategy-handle = u64;

    /// `highWaterMark` attribute — getter.
    get-high-water-mark: func(self: count-queuing-strategy-handle) -> f64;

    /// `size` attribute — getter.
    get-size: func(self: count-queuing-strategy-handle) -> u64;
}

interface generic-transform-stream {
    /// Opaque handle to a host-side `GenericTransformStream` instance.
    type generic-transform-stream-handle = u64;

    /// `readable` attribute — getter.
    get-readable: func(self: generic-transform-stream-handle) -> u64;

    /// `writable` attribute — getter.
    get-writable: func(self: generic-transform-stream-handle) -> u64;
}

interface xml-http-request-event-target {
    /// Opaque handle to a host-side `XMLHttpRequestEventTarget` instance.
    type xml-http-request-event-target-handle = u64;

    /// `onloadstart` attribute — getter.
    get-onloadstart: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onloadstart` attribute — setter.
    set-onloadstart: func(self: xml-http-request-event-target-handle, value: u64);

    /// `onprogress` attribute — getter.
    get-onprogress: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onprogress` attribute — setter.
    set-onprogress: func(self: xml-http-request-event-target-handle, value: u64);

    /// `onabort` attribute — getter.
    get-onabort: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onabort` attribute — setter.
    set-onabort: func(self: xml-http-request-event-target-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: xml-http-request-event-target-handle, value: u64);

    /// `onload` attribute — getter.
    get-onload: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onload` attribute — setter.
    set-onload: func(self: xml-http-request-event-target-handle, value: u64);

    /// `ontimeout` attribute — getter.
    get-ontimeout: func(self: xml-http-request-event-target-handle) -> u64;
    /// `ontimeout` attribute — setter.
    set-ontimeout: func(self: xml-http-request-event-target-handle, value: u64);

    /// `onloadend` attribute — getter.
    get-onloadend: func(self: xml-http-request-event-target-handle) -> u64;
    /// `onloadend` attribute — setter.
    set-onloadend: func(self: xml-http-request-event-target-handle, value: u64);
}

interface xml-http-request {
    /// Opaque handle to a host-side `XMLHttpRequest` instance.
    type xml-http-request-handle = u64;

    /// `onreadystatechange` attribute — getter.
    get-onreadystatechange: func(self: xml-http-request-handle) -> u64;
    /// `onreadystatechange` attribute — setter.
    set-onreadystatechange: func(self: xml-http-request-handle, value: u64);

    /// `readyState` attribute — getter.
    get-ready-state: func(self: xml-http-request-handle) -> u16;

    /// `open()` operation.
    open: func(self: xml-http-request-handle, method: string, url: string);

    /// `setRequestHeader()` operation.
    set-request-header: func(self: xml-http-request-handle, name: string, value: string);

    /// `timeout` attribute — getter.
    get-timeout: func(self: xml-http-request-handle) -> u32;
    /// `timeout` attribute — setter.
    set-timeout: func(self: xml-http-request-handle, value: u32);

    /// `withCredentials` attribute — getter.
    get-with-credentials: func(self: xml-http-request-handle) -> bool;
    /// `withCredentials` attribute — setter.
    set-with-credentials: func(self: xml-http-request-handle, value: bool);

    /// `upload` attribute — getter.
    get-upload: func(self: xml-http-request-handle) -> u64;

    /// `send()` operation.
    send: func(self: xml-http-request-handle, body: option<u64>);

    /// `abort()` operation.
    abort: func(self: xml-http-request-handle);

    /// `responseURL` attribute — getter.
    get-response-url: func(self: xml-http-request-handle) -> string;

    /// `status` attribute — getter.
    get-status: func(self: xml-http-request-handle) -> u16;

    /// `statusText` attribute — getter.
    get-status-text: func(self: xml-http-request-handle) -> string;

    /// `getResponseHeader()` operation.
    get-response-header: func(self: xml-http-request-handle, name: string) -> option<string>;

    /// `getAllResponseHeaders()` operation.
    get-all-response-headers: func(self: xml-http-request-handle) -> string;

    /// `overrideMimeType()` operation.
    override-mime-type: func(self: xml-http-request-handle, mime: string);

    /// `responseType` attribute — getter.
    get-response-type: func(self: xml-http-request-handle) -> u64;
    /// `responseType` attribute — setter.
    set-response-type: func(self: xml-http-request-handle, value: u64);

    /// `response` attribute — getter.
    get-response: func(self: xml-http-request-handle) -> string;

    /// `responseText` attribute — getter.
    get-response-text: func(self: xml-http-request-handle) -> string;

    /// `responseXML` attribute — getter.
    get-response-xml: func(self: xml-http-request-handle) -> option<u64>;
}

interface form-data {
    /// Opaque handle to a host-side `FormData` instance.
    type form-data-handle = u64;

    /// `append()` operation.
    append: func(self: form-data-handle, name: string, value: string);

    /// `delete()` operation.
    delete: func(self: form-data-handle, name: string);

    /// `get()` operation.
    get: func(self: form-data-handle, name: string) -> option<u64>;

    /// `getAll()` operation.
    get-all: func(self: form-data-handle, name: string) -> list<u64>;

    /// `has()` operation.
    has: func(self: form-data-handle, name: string) -> bool;

    /// `set()` operation.
    set: func(self: form-data-handle, name: string, value: string);
}

interface progress-event {
    /// Opaque handle to a host-side `ProgressEvent` instance.
    type progress-event-handle = u64;

    /// `lengthComputable` attribute — getter.
    get-length-computable: func(self: progress-event-handle) -> bool;

    /// `loaded` attribute — getter.
    get-loaded: func(self: progress-event-handle) -> f64;

    /// `total` attribute — getter.
    get-total: func(self: progress-event-handle) -> f64;
}

interface html-all-collection {
    /// Opaque handle to a host-side `HTMLAllCollection` instance.
    type html-all-collection-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: html-all-collection-handle) -> u32;

    /// `Element()` operation.
    element: func(self: html-all-collection-handle, index: u32);
}

interface radio-node-list {
    /// Opaque handle to a host-side `RadioNodeList` instance.
    type radio-node-list-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: radio-node-list-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: radio-node-list-handle, value: string);
}

interface html-options-collection {
    /// Opaque handle to a host-side `HTMLOptionsCollection` instance.
    type html-options-collection-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: html-options-collection-handle) -> u32;
    /// `length` attribute — setter.
    set-length: func(self: html-options-collection-handle, value: u32);

    /// `undefined()` operation.
    undefined: func(self: html-options-collection-handle, index: u32, %option: option<u64>);

    /// `add()` operation.
    add: func(self: html-options-collection-handle, element: u64, before: option<f64>);

    /// `remove()` operation.
    remove: func(self: html-options-collection-handle, index: s32);

    /// `selectedIndex` attribute — getter.
    get-selected-index: func(self: html-options-collection-handle) -> s32;
    /// `selectedIndex` attribute — setter.
    set-selected-index: func(self: html-options-collection-handle, value: s32);
}

interface dom-string-list {
    /// Opaque handle to a host-side `DOMStringList` instance.
    type dom-string-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: dom-string-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: dom-string-list-handle, index: u32) -> option<string>;

    /// `contains()` operation.
    contains: func(self: dom-string-list-handle, %string: string) -> bool;
}

interface html-or-svg-element {
    /// Opaque handle to a host-side `HTMLOrSVGElement` instance.
    type html-or-svg-element-handle = u64;

    /// `dataset` attribute — getter.
    get-dataset: func(self: html-or-svg-element-handle) -> u64;

    /// `nonce` attribute — getter.
    get-nonce: func(self: html-or-svg-element-handle) -> string;
    /// `nonce` attribute — setter.
    set-nonce: func(self: html-or-svg-element-handle, value: string);

    /// `autofocus` attribute — getter.
    get-autofocus: func(self: html-or-svg-element-handle) -> bool;
    /// `autofocus` attribute — setter.
    set-autofocus: func(self: html-or-svg-element-handle, value: bool);

    /// `tabIndex` attribute — getter.
    get-tab-index: func(self: html-or-svg-element-handle) -> s32;
    /// `tabIndex` attribute — setter.
    set-tab-index: func(self: html-or-svg-element-handle, value: s32);

    /// `focus()` operation.
    focus: func(self: html-or-svg-element-handle, options: option<u64>);

    /// `blur()` operation.
    blur: func(self: html-or-svg-element-handle);
}

interface dom-string-map {
    /// Opaque handle to a host-side `DOMStringMap` instance.
    type dom-string-map-handle = u64;

    /// `DOMString()` operation.
    dom-string: func(self: dom-string-map-handle, name: string);

    /// `undefined()` operation.
    undefined: func(self: dom-string-map-handle, name: string, value: string);
}

interface html-html-element {
    /// Opaque handle to a host-side `HTMLHtmlElement` instance.
    type html-html-element-handle = u64;

    /// `version` attribute — getter.
    get-version: func(self: html-html-element-handle) -> string;
    /// `version` attribute — setter.
    set-version: func(self: html-html-element-handle, value: string);
}

interface html-title-element {
    /// Opaque handle to a host-side `HTMLTitleElement` instance.
    type html-title-element-handle = u64;

    /// `text` attribute — getter.
    get-text: func(self: html-title-element-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: html-title-element-handle, value: string);
}

interface html-base-element {
    /// Opaque handle to a host-side `HTMLBaseElement` instance.
    type html-base-element-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: html-base-element-handle) -> string;
    /// `href` attribute — setter.
    set-href: func(self: html-base-element-handle, value: string);

    /// `target` attribute — getter.
    get-target: func(self: html-base-element-handle) -> string;
    /// `target` attribute — setter.
    set-target: func(self: html-base-element-handle, value: string);
}

interface html-link-element {
    /// Opaque handle to a host-side `HTMLLinkElement` instance.
    type html-link-element-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: html-link-element-handle) -> string;
    /// `href` attribute — setter.
    set-href: func(self: html-link-element-handle, value: string);

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: html-link-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: html-link-element-handle, value: option<string>);

    /// `rel` attribute — getter.
    get-rel: func(self: html-link-element-handle) -> string;
    /// `rel` attribute — setter.
    set-rel: func(self: html-link-element-handle, value: string);

    /// `as` attribute — getter.
    get-as: func(self: html-link-element-handle) -> string;
    /// `as` attribute — setter.
    set-as: func(self: html-link-element-handle, value: string);

    /// `relList` attribute — getter.
    get-rel-list: func(self: html-link-element-handle) -> u64;

    /// `media` attribute — getter.
    get-media: func(self: html-link-element-handle) -> string;
    /// `media` attribute — setter.
    set-media: func(self: html-link-element-handle, value: string);

    /// `integrity` attribute — getter.
    get-integrity: func(self: html-link-element-handle) -> string;
    /// `integrity` attribute — setter.
    set-integrity: func(self: html-link-element-handle, value: string);

    /// `hreflang` attribute — getter.
    get-hreflang: func(self: html-link-element-handle) -> string;
    /// `hreflang` attribute — setter.
    set-hreflang: func(self: html-link-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-link-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-link-element-handle, value: string);

    /// `sizes` attribute — getter.
    get-sizes: func(self: html-link-element-handle) -> u64;

    /// `imageSrcset` attribute — getter.
    get-image-srcset: func(self: html-link-element-handle) -> string;
    /// `imageSrcset` attribute — setter.
    set-image-srcset: func(self: html-link-element-handle, value: string);

    /// `imageSizes` attribute — getter.
    get-image-sizes: func(self: html-link-element-handle) -> string;
    /// `imageSizes` attribute — setter.
    set-image-sizes: func(self: html-link-element-handle, value: string);

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: html-link-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: html-link-element-handle, value: string);

    /// `blocking` attribute — getter.
    get-blocking: func(self: html-link-element-handle) -> u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-link-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-link-element-handle, value: bool);

    /// `fetchPriority` attribute — getter.
    get-fetch-priority: func(self: html-link-element-handle) -> string;
    /// `fetchPriority` attribute — setter.
    set-fetch-priority: func(self: html-link-element-handle, value: string);

    /// `charset` attribute — getter.
    get-charset: func(self: html-link-element-handle) -> string;
    /// `charset` attribute — setter.
    set-charset: func(self: html-link-element-handle, value: string);

    /// `rev` attribute — getter.
    get-rev: func(self: html-link-element-handle) -> string;
    /// `rev` attribute — setter.
    set-rev: func(self: html-link-element-handle, value: string);

    /// `target` attribute — getter.
    get-target: func(self: html-link-element-handle) -> string;
    /// `target` attribute — setter.
    set-target: func(self: html-link-element-handle, value: string);
}

interface html-meta-element {
    /// Opaque handle to a host-side `HTMLMetaElement` instance.
    type html-meta-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-meta-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-meta-element-handle, value: string);

    /// `httpEquiv` attribute — getter.
    get-http-equiv: func(self: html-meta-element-handle) -> string;
    /// `httpEquiv` attribute — setter.
    set-http-equiv: func(self: html-meta-element-handle, value: string);

    /// `content` attribute — getter.
    get-content: func(self: html-meta-element-handle) -> string;
    /// `content` attribute — setter.
    set-content: func(self: html-meta-element-handle, value: string);

    /// `media` attribute — getter.
    get-media: func(self: html-meta-element-handle) -> string;
    /// `media` attribute — setter.
    set-media: func(self: html-meta-element-handle, value: string);

    /// `scheme` attribute — getter.
    get-scheme: func(self: html-meta-element-handle) -> string;
    /// `scheme` attribute — setter.
    set-scheme: func(self: html-meta-element-handle, value: string);
}

interface html-style-element {
    /// Opaque handle to a host-side `HTMLStyleElement` instance.
    type html-style-element-handle = u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-style-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-style-element-handle, value: bool);

    /// `media` attribute — getter.
    get-media: func(self: html-style-element-handle) -> string;
    /// `media` attribute — setter.
    set-media: func(self: html-style-element-handle, value: string);

    /// `blocking` attribute — getter.
    get-blocking: func(self: html-style-element-handle) -> u64;

    /// `type` attribute — getter.
    get-type: func(self: html-style-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-style-element-handle, value: string);
}

interface html-body-element {
    /// Opaque handle to a host-side `HTMLBodyElement` instance.
    type html-body-element-handle = u64;

    /// `text` attribute — getter.
    get-text: func(self: html-body-element-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: html-body-element-handle, value: string);

    /// `link` attribute — getter.
    get-link: func(self: html-body-element-handle) -> string;
    /// `link` attribute — setter.
    set-link: func(self: html-body-element-handle, value: string);

    /// `vLink` attribute — getter.
    get-v-link: func(self: html-body-element-handle) -> string;
    /// `vLink` attribute — setter.
    set-v-link: func(self: html-body-element-handle, value: string);

    /// `aLink` attribute — getter.
    get-a-link: func(self: html-body-element-handle) -> string;
    /// `aLink` attribute — setter.
    set-a-link: func(self: html-body-element-handle, value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func(self: html-body-element-handle) -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(self: html-body-element-handle, value: string);

    /// `background` attribute — getter.
    get-background: func(self: html-body-element-handle) -> string;
    /// `background` attribute — setter.
    set-background: func(self: html-body-element-handle, value: string);
}

interface html-heading-element {
    /// Opaque handle to a host-side `HTMLHeadingElement` instance.
    type html-heading-element-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: html-heading-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-heading-element-handle, value: string);
}

interface html-paragraph-element {
    /// Opaque handle to a host-side `HTMLParagraphElement` instance.
    type html-paragraph-element-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: html-paragraph-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-paragraph-element-handle, value: string);
}

interface htmlhr-element {
    /// Opaque handle to a host-side `HTMLHRElement` instance.
    type htmlhr-element-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: htmlhr-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: htmlhr-element-handle, value: string);

    /// `color` attribute — getter.
    get-color: func(self: htmlhr-element-handle) -> string;
    /// `color` attribute — setter.
    set-color: func(self: htmlhr-element-handle, value: string);

    /// `noShade` attribute — getter.
    get-no-shade: func(self: htmlhr-element-handle) -> bool;
    /// `noShade` attribute — setter.
    set-no-shade: func(self: htmlhr-element-handle, value: bool);

    /// `size` attribute — getter.
    get-size: func(self: htmlhr-element-handle) -> string;
    /// `size` attribute — setter.
    set-size: func(self: htmlhr-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: htmlhr-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: htmlhr-element-handle, value: string);
}

interface html-pre-element {
    /// Opaque handle to a host-side `HTMLPreElement` instance.
    type html-pre-element-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: html-pre-element-handle) -> s32;
    /// `width` attribute — setter.
    set-width: func(self: html-pre-element-handle, value: s32);
}

interface html-quote-element {
    /// Opaque handle to a host-side `HTMLQuoteElement` instance.
    type html-quote-element-handle = u64;

    /// `cite` attribute — getter.
    get-cite: func(self: html-quote-element-handle) -> string;
    /// `cite` attribute — setter.
    set-cite: func(self: html-quote-element-handle, value: string);
}

interface htmlo-list-element {
    /// Opaque handle to a host-side `HTMLOListElement` instance.
    type htmlo-list-element-handle = u64;

    /// `reversed` attribute — getter.
    get-reversed: func(self: htmlo-list-element-handle) -> bool;
    /// `reversed` attribute — setter.
    set-reversed: func(self: htmlo-list-element-handle, value: bool);

    /// `start` attribute — getter.
    get-start: func(self: htmlo-list-element-handle) -> s32;
    /// `start` attribute — setter.
    set-start: func(self: htmlo-list-element-handle, value: s32);

    /// `type` attribute — getter.
    get-type: func(self: htmlo-list-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: htmlo-list-element-handle, value: string);

    /// `compact` attribute — getter.
    get-compact: func(self: htmlo-list-element-handle) -> bool;
    /// `compact` attribute — setter.
    set-compact: func(self: htmlo-list-element-handle, value: bool);
}

interface htmlu-list-element {
    /// Opaque handle to a host-side `HTMLUListElement` instance.
    type htmlu-list-element-handle = u64;

    /// `compact` attribute — getter.
    get-compact: func(self: htmlu-list-element-handle) -> bool;
    /// `compact` attribute — setter.
    set-compact: func(self: htmlu-list-element-handle, value: bool);

    /// `type` attribute — getter.
    get-type: func(self: htmlu-list-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: htmlu-list-element-handle, value: string);
}

interface html-menu-element {
    /// Opaque handle to a host-side `HTMLMenuElement` instance.
    type html-menu-element-handle = u64;

    /// `compact` attribute — getter.
    get-compact: func(self: html-menu-element-handle) -> bool;
    /// `compact` attribute — setter.
    set-compact: func(self: html-menu-element-handle, value: bool);
}

interface htmlli-element {
    /// Opaque handle to a host-side `HTMLLIElement` instance.
    type htmlli-element-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: htmlli-element-handle) -> s32;
    /// `value` attribute — setter.
    set-value: func(self: htmlli-element-handle, value: s32);

    /// `type` attribute — getter.
    get-type: func(self: htmlli-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: htmlli-element-handle, value: string);
}

interface htmld-list-element {
    /// Opaque handle to a host-side `HTMLDListElement` instance.
    type htmld-list-element-handle = u64;

    /// `compact` attribute — getter.
    get-compact: func(self: htmld-list-element-handle) -> bool;
    /// `compact` attribute — setter.
    set-compact: func(self: htmld-list-element-handle, value: bool);
}

interface html-div-element {
    /// Opaque handle to a host-side `HTMLDivElement` instance.
    type html-div-element-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: html-div-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-div-element-handle, value: string);
}

interface html-anchor-element {
    /// Opaque handle to a host-side `HTMLAnchorElement` instance.
    type html-anchor-element-handle = u64;

    /// `target` attribute — getter.
    get-target: func(self: html-anchor-element-handle) -> string;
    /// `target` attribute — setter.
    set-target: func(self: html-anchor-element-handle, value: string);

    /// `download` attribute — getter.
    get-download: func(self: html-anchor-element-handle) -> string;
    /// `download` attribute — setter.
    set-download: func(self: html-anchor-element-handle, value: string);

    /// `ping` attribute — getter.
    get-ping: func(self: html-anchor-element-handle) -> string;
    /// `ping` attribute — setter.
    set-ping: func(self: html-anchor-element-handle, value: string);

    /// `rel` attribute — getter.
    get-rel: func(self: html-anchor-element-handle) -> string;
    /// `rel` attribute — setter.
    set-rel: func(self: html-anchor-element-handle, value: string);

    /// `relList` attribute — getter.
    get-rel-list: func(self: html-anchor-element-handle) -> u64;

    /// `hreflang` attribute — getter.
    get-hreflang: func(self: html-anchor-element-handle) -> string;
    /// `hreflang` attribute — setter.
    set-hreflang: func(self: html-anchor-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-anchor-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-anchor-element-handle, value: string);

    /// `text` attribute — getter.
    get-text: func(self: html-anchor-element-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: html-anchor-element-handle, value: string);

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: html-anchor-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: html-anchor-element-handle, value: string);

    /// `coords` attribute — getter.
    get-coords: func(self: html-anchor-element-handle) -> string;
    /// `coords` attribute — setter.
    set-coords: func(self: html-anchor-element-handle, value: string);

    /// `charset` attribute — getter.
    get-charset: func(self: html-anchor-element-handle) -> string;
    /// `charset` attribute — setter.
    set-charset: func(self: html-anchor-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: html-anchor-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-anchor-element-handle, value: string);

    /// `rev` attribute — getter.
    get-rev: func(self: html-anchor-element-handle) -> string;
    /// `rev` attribute — setter.
    set-rev: func(self: html-anchor-element-handle, value: string);

    /// `shape` attribute — getter.
    get-shape: func(self: html-anchor-element-handle) -> string;
    /// `shape` attribute — setter.
    set-shape: func(self: html-anchor-element-handle, value: string);
}

interface html-data-element {
    /// Opaque handle to a host-side `HTMLDataElement` instance.
    type html-data-element-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: html-data-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-data-element-handle, value: string);
}

interface html-time-element {
    /// Opaque handle to a host-side `HTMLTimeElement` instance.
    type html-time-element-handle = u64;

    /// `dateTime` attribute — getter.
    get-date-time: func(self: html-time-element-handle) -> string;
    /// `dateTime` attribute — setter.
    set-date-time: func(self: html-time-element-handle, value: string);
}

interface htmlbr-element {
    /// Opaque handle to a host-side `HTMLBRElement` instance.
    type htmlbr-element-handle = u64;

    /// `clear` attribute — getter.
    get-clear: func(self: htmlbr-element-handle) -> string;
    /// `clear` attribute — setter.
    set-clear: func(self: htmlbr-element-handle, value: string);
}

interface hyperlink-element-utils {
    /// Opaque handle to a host-side `HyperlinkElementUtils` instance.
    type hyperlink-element-utils-handle = u64;

    /// `origin` attribute — getter.
    get-origin: func(self: hyperlink-element-utils-handle) -> string;

    /// `protocol` attribute — getter.
    get-protocol: func(self: hyperlink-element-utils-handle) -> string;
    /// `protocol` attribute — setter.
    set-protocol: func(self: hyperlink-element-utils-handle, value: string);

    /// `username` attribute — getter.
    get-username: func(self: hyperlink-element-utils-handle) -> string;
    /// `username` attribute — setter.
    set-username: func(self: hyperlink-element-utils-handle, value: string);

    /// `password` attribute — getter.
    get-password: func(self: hyperlink-element-utils-handle) -> string;
    /// `password` attribute — setter.
    set-password: func(self: hyperlink-element-utils-handle, value: string);

    /// `host` attribute — getter.
    get-host: func(self: hyperlink-element-utils-handle) -> string;
    /// `host` attribute — setter.
    set-host: func(self: hyperlink-element-utils-handle, value: string);

    /// `hostname` attribute — getter.
    get-hostname: func(self: hyperlink-element-utils-handle) -> string;
    /// `hostname` attribute — setter.
    set-hostname: func(self: hyperlink-element-utils-handle, value: string);

    /// `port` attribute — getter.
    get-port: func(self: hyperlink-element-utils-handle) -> string;
    /// `port` attribute — setter.
    set-port: func(self: hyperlink-element-utils-handle, value: string);

    /// `pathname` attribute — getter.
    get-pathname: func(self: hyperlink-element-utils-handle) -> string;
    /// `pathname` attribute — setter.
    set-pathname: func(self: hyperlink-element-utils-handle, value: string);

    /// `search` attribute — getter.
    get-search: func(self: hyperlink-element-utils-handle) -> string;
    /// `search` attribute — setter.
    set-search: func(self: hyperlink-element-utils-handle, value: string);

    /// `hash` attribute — getter.
    get-hash: func(self: hyperlink-element-utils-handle) -> string;
    /// `hash` attribute — setter.
    set-hash: func(self: hyperlink-element-utils-handle, value: string);
}

interface html-hyperlink-element-utils {
    /// Opaque handle to a host-side `HTMLHyperlinkElementUtils` instance.
    type html-hyperlink-element-utils-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: html-hyperlink-element-utils-handle) -> string;
    /// `href` attribute — setter.
    set-href: func(self: html-hyperlink-element-utils-handle, value: string);
}

interface html-mod-element {
    /// Opaque handle to a host-side `HTMLModElement` instance.
    type html-mod-element-handle = u64;

    /// `cite` attribute — getter.
    get-cite: func(self: html-mod-element-handle) -> string;
    /// `cite` attribute — setter.
    set-cite: func(self: html-mod-element-handle, value: string);

    /// `dateTime` attribute — getter.
    get-date-time: func(self: html-mod-element-handle) -> string;
    /// `dateTime` attribute — setter.
    set-date-time: func(self: html-mod-element-handle, value: string);
}

interface html-source-element {
    /// Opaque handle to a host-side `HTMLSourceElement` instance.
    type html-source-element-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: html-source-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-source-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-source-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-source-element-handle, value: string);

    /// `srcset` attribute — getter.
    get-srcset: func(self: html-source-element-handle) -> string;
    /// `srcset` attribute — setter.
    set-srcset: func(self: html-source-element-handle, value: string);

    /// `sizes` attribute — getter.
    get-sizes: func(self: html-source-element-handle) -> string;
    /// `sizes` attribute — setter.
    set-sizes: func(self: html-source-element-handle, value: string);

    /// `media` attribute — getter.
    get-media: func(self: html-source-element-handle) -> string;
    /// `media` attribute — setter.
    set-media: func(self: html-source-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: html-source-element-handle) -> u32;
    /// `width` attribute — setter.
    set-width: func(self: html-source-element-handle, value: u32);

    /// `height` attribute — getter.
    get-height: func(self: html-source-element-handle) -> u32;
    /// `height` attribute — setter.
    set-height: func(self: html-source-element-handle, value: u32);
}

interface htmli-frame-element {
    /// Opaque handle to a host-side `HTMLIFrameElement` instance.
    type htmli-frame-element-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: htmli-frame-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: htmli-frame-element-handle, value: string);

    /// `srcdoc` attribute — getter.
    get-srcdoc: func(self: htmli-frame-element-handle) -> string;
    /// `srcdoc` attribute — setter.
    set-srcdoc: func(self: htmli-frame-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: htmli-frame-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: htmli-frame-element-handle, value: string);

    /// `sandbox` attribute — getter.
    get-sandbox: func(self: htmli-frame-element-handle) -> u64;

    /// `allow` attribute — getter.
    get-allow: func(self: htmli-frame-element-handle) -> string;
    /// `allow` attribute — setter.
    set-allow: func(self: htmli-frame-element-handle, value: string);

    /// `allowFullscreen` attribute — getter.
    get-allow-fullscreen: func(self: htmli-frame-element-handle) -> bool;
    /// `allowFullscreen` attribute — setter.
    set-allow-fullscreen: func(self: htmli-frame-element-handle, value: bool);

    /// `width` attribute — getter.
    get-width: func(self: htmli-frame-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: htmli-frame-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: htmli-frame-element-handle) -> string;
    /// `height` attribute — setter.
    set-height: func(self: htmli-frame-element-handle, value: string);

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: htmli-frame-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: htmli-frame-element-handle, value: string);

    /// `loading` attribute — getter.
    get-loading: func(self: htmli-frame-element-handle) -> string;
    /// `loading` attribute — setter.
    set-loading: func(self: htmli-frame-element-handle, value: string);

    /// `contentDocument` attribute — getter.
    get-content-document: func(self: htmli-frame-element-handle) -> option<u64>;

    /// `contentWindow` attribute — getter.
    get-content-window: func(self: htmli-frame-element-handle) -> option<u64>;

    /// `getSVGDocument()` operation.
    get-svg-document: func(self: htmli-frame-element-handle) -> option<u64>;

    /// `align` attribute — getter.
    get-align: func(self: htmli-frame-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: htmli-frame-element-handle, value: string);

    /// `scrolling` attribute — getter.
    get-scrolling: func(self: htmli-frame-element-handle) -> string;
    /// `scrolling` attribute — setter.
    set-scrolling: func(self: htmli-frame-element-handle, value: string);

    /// `frameBorder` attribute — getter.
    get-frame-border: func(self: htmli-frame-element-handle) -> string;
    /// `frameBorder` attribute — setter.
    set-frame-border: func(self: htmli-frame-element-handle, value: string);

    /// `longDesc` attribute — getter.
    get-long-desc: func(self: htmli-frame-element-handle) -> string;
    /// `longDesc` attribute — setter.
    set-long-desc: func(self: htmli-frame-element-handle, value: string);

    /// `marginHeight` attribute — getter.
    get-margin-height: func(self: htmli-frame-element-handle) -> string;
    /// `marginHeight` attribute — setter.
    set-margin-height: func(self: htmli-frame-element-handle, value: string);

    /// `marginWidth` attribute — getter.
    get-margin-width: func(self: htmli-frame-element-handle) -> string;
    /// `marginWidth` attribute — setter.
    set-margin-width: func(self: htmli-frame-element-handle, value: string);
}

interface html-embed-element {
    /// Opaque handle to a host-side `HTMLEmbedElement` instance.
    type html-embed-element-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: html-embed-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-embed-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-embed-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-embed-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: html-embed-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-embed-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: html-embed-element-handle) -> string;
    /// `height` attribute — setter.
    set-height: func(self: html-embed-element-handle, value: string);

    /// `getSVGDocument()` operation.
    get-svg-document: func(self: html-embed-element-handle) -> option<u64>;

    /// `align` attribute — getter.
    get-align: func(self: html-embed-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-embed-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: html-embed-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-embed-element-handle, value: string);
}

interface html-object-element {
    /// Opaque handle to a host-side `HTMLObjectElement` instance.
    type html-object-element-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: html-object-element-handle) -> string;
    /// `data` attribute — setter.
    set-data: func(self: html-object-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-object-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-object-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: html-object-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-object-element-handle, value: string);

    /// `form` attribute — getter.
    get-form: func(self: html-object-element-handle) -> option<u64>;

    /// `width` attribute — getter.
    get-width: func(self: html-object-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-object-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: html-object-element-handle) -> string;
    /// `height` attribute — setter.
    set-height: func(self: html-object-element-handle, value: string);

    /// `contentDocument` attribute — getter.
    get-content-document: func(self: html-object-element-handle) -> option<u64>;

    /// `contentWindow` attribute — getter.
    get-content-window: func(self: html-object-element-handle) -> option<u64>;

    /// `getSVGDocument()` operation.
    get-svg-document: func(self: html-object-element-handle) -> option<u64>;

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-object-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-object-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-object-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-object-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-object-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-object-element-handle, error: string);

    /// `align` attribute — getter.
    get-align: func(self: html-object-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-object-element-handle, value: string);

    /// `archive` attribute — getter.
    get-archive: func(self: html-object-element-handle) -> string;
    /// `archive` attribute — setter.
    set-archive: func(self: html-object-element-handle, value: string);

    /// `code` attribute — getter.
    get-code: func(self: html-object-element-handle) -> string;
    /// `code` attribute — setter.
    set-code: func(self: html-object-element-handle, value: string);

    /// `declare` attribute — getter.
    get-declare: func(self: html-object-element-handle) -> bool;
    /// `declare` attribute — setter.
    set-declare: func(self: html-object-element-handle, value: bool);

    /// `hspace` attribute — getter.
    get-hspace: func(self: html-object-element-handle) -> u32;
    /// `hspace` attribute — setter.
    set-hspace: func(self: html-object-element-handle, value: u32);

    /// `standby` attribute — getter.
    get-standby: func(self: html-object-element-handle) -> string;
    /// `standby` attribute — setter.
    set-standby: func(self: html-object-element-handle, value: string);

    /// `vspace` attribute — getter.
    get-vspace: func(self: html-object-element-handle) -> u32;
    /// `vspace` attribute — setter.
    set-vspace: func(self: html-object-element-handle, value: u32);

    /// `codeBase` attribute — getter.
    get-code-base: func(self: html-object-element-handle) -> string;
    /// `codeBase` attribute — setter.
    set-code-base: func(self: html-object-element-handle, value: string);

    /// `codeType` attribute — getter.
    get-code-type: func(self: html-object-element-handle) -> string;
    /// `codeType` attribute — setter.
    set-code-type: func(self: html-object-element-handle, value: string);

    /// `useMap` attribute — getter.
    get-use-map: func(self: html-object-element-handle) -> string;
    /// `useMap` attribute — setter.
    set-use-map: func(self: html-object-element-handle, value: string);

    /// `border` attribute — getter.
    get-border: func(self: html-object-element-handle) -> string;
    /// `border` attribute — setter.
    set-border: func(self: html-object-element-handle, value: string);
}

interface html-video-element {
    /// Opaque handle to a host-side `HTMLVideoElement` instance.
    type html-video-element-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: html-video-element-handle) -> u32;
    /// `width` attribute — setter.
    set-width: func(self: html-video-element-handle, value: u32);

    /// `height` attribute — getter.
    get-height: func(self: html-video-element-handle) -> u32;
    /// `height` attribute — setter.
    set-height: func(self: html-video-element-handle, value: u32);

    /// `videoWidth` attribute — getter.
    get-video-width: func(self: html-video-element-handle) -> u32;

    /// `videoHeight` attribute — getter.
    get-video-height: func(self: html-video-element-handle) -> u32;

    /// `poster` attribute — getter.
    get-poster: func(self: html-video-element-handle) -> string;
    /// `poster` attribute — setter.
    set-poster: func(self: html-video-element-handle, value: string);

    /// `playsInline` attribute — getter.
    get-plays-inline: func(self: html-video-element-handle) -> bool;
    /// `playsInline` attribute — setter.
    set-plays-inline: func(self: html-video-element-handle, value: bool);
}

interface html-track-element {
    /// Opaque handle to a host-side `HTMLTrackElement` instance.
    type html-track-element-handle = u64;

    /// `kind` attribute — getter.
    get-kind: func(self: html-track-element-handle) -> string;
    /// `kind` attribute — setter.
    set-kind: func(self: html-track-element-handle, value: string);

    /// `src` attribute — getter.
    get-src: func(self: html-track-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-track-element-handle, value: string);

    /// `srclang` attribute — getter.
    get-srclang: func(self: html-track-element-handle) -> string;
    /// `srclang` attribute — setter.
    set-srclang: func(self: html-track-element-handle, value: string);

    /// `label` attribute — getter.
    get-label: func(self: html-track-element-handle) -> string;
    /// `label` attribute — setter.
    set-label: func(self: html-track-element-handle, value: string);

    /// `default` attribute — getter.
    get-default: func(self: html-track-element-handle) -> bool;
    /// `default` attribute — setter.
    set-default: func(self: html-track-element-handle, value: bool);

    /// `readyState` attribute — getter.
    get-ready-state: func(self: html-track-element-handle) -> u16;

    /// `track` attribute — getter.
    get-track: func(self: html-track-element-handle) -> u64;
}

interface html-media-element {
    /// Opaque handle to a host-side `HTMLMediaElement` instance.
    type html-media-element-handle = u64;

    /// `error` attribute — getter.
    get-error: func(self: html-media-element-handle) -> option<u64>;

    /// `src` attribute — getter.
    get-src: func(self: html-media-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-media-element-handle, value: string);

    /// `srcObject` attribute — getter.
    get-src-object: func(self: html-media-element-handle) -> option<u64>;
    /// `srcObject` attribute — setter.
    set-src-object: func(self: html-media-element-handle, value: option<u64>);

    /// `currentSrc` attribute — getter.
    get-current-src: func(self: html-media-element-handle) -> string;

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: html-media-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: html-media-element-handle, value: option<string>);

    /// `networkState` attribute — getter.
    get-network-state: func(self: html-media-element-handle) -> u16;

    /// `preload` attribute — getter.
    get-preload: func(self: html-media-element-handle) -> string;
    /// `preload` attribute — setter.
    set-preload: func(self: html-media-element-handle, value: string);

    /// `buffered` attribute — getter.
    get-buffered: func(self: html-media-element-handle) -> u64;

    /// `load()` operation.
    load: func(self: html-media-element-handle);

    /// `canPlayType()` operation.
    can-play-type: func(self: html-media-element-handle, %type: string) -> u64;

    /// `readyState` attribute — getter.
    get-ready-state: func(self: html-media-element-handle) -> u16;

    /// `seeking` attribute — getter.
    get-seeking: func(self: html-media-element-handle) -> bool;

    /// `currentTime` attribute — getter.
    get-current-time: func(self: html-media-element-handle) -> f64;
    /// `currentTime` attribute — setter.
    set-current-time: func(self: html-media-element-handle, value: f64);

    /// `fastSeek()` operation.
    fast-seek: func(self: html-media-element-handle, time: f64);

    /// `duration` attribute — getter.
    get-duration: func(self: html-media-element-handle) -> f64;

    /// `getStartDate()` operation.
    get-start-date: func(self: html-media-element-handle) -> u64;

    /// `paused` attribute — getter.
    get-paused: func(self: html-media-element-handle) -> bool;

    /// `defaultPlaybackRate` attribute — getter.
    get-default-playback-rate: func(self: html-media-element-handle) -> f64;
    /// `defaultPlaybackRate` attribute — setter.
    set-default-playback-rate: func(self: html-media-element-handle, value: f64);

    /// `playbackRate` attribute — getter.
    get-playback-rate: func(self: html-media-element-handle) -> f64;
    /// `playbackRate` attribute — setter.
    set-playback-rate: func(self: html-media-element-handle, value: f64);

    /// `preservesPitch` attribute — getter.
    get-preserves-pitch: func(self: html-media-element-handle) -> bool;
    /// `preservesPitch` attribute — setter.
    set-preserves-pitch: func(self: html-media-element-handle, value: bool);

    /// `played` attribute — getter.
    get-played: func(self: html-media-element-handle) -> u64;

    /// `seekable` attribute — getter.
    get-seekable: func(self: html-media-element-handle) -> u64;

    /// `ended` attribute — getter.
    get-ended: func(self: html-media-element-handle) -> bool;

    /// `autoplay` attribute — getter.
    get-autoplay: func(self: html-media-element-handle) -> bool;
    /// `autoplay` attribute — setter.
    set-autoplay: func(self: html-media-element-handle, value: bool);

    /// `loop` attribute — getter.
    get-loop: func(self: html-media-element-handle) -> bool;
    /// `loop` attribute — setter.
    set-loop: func(self: html-media-element-handle, value: bool);

    /// `play()` operation.
    play: func(self: html-media-element-handle) -> u64;

    /// `pause()` operation.
    pause: func(self: html-media-element-handle);

    /// `controls` attribute — getter.
    get-controls: func(self: html-media-element-handle) -> bool;
    /// `controls` attribute — setter.
    set-controls: func(self: html-media-element-handle, value: bool);

    /// `volume` attribute — getter.
    get-volume: func(self: html-media-element-handle) -> f64;
    /// `volume` attribute — setter.
    set-volume: func(self: html-media-element-handle, value: f64);

    /// `muted` attribute — getter.
    get-muted: func(self: html-media-element-handle) -> bool;
    /// `muted` attribute — setter.
    set-muted: func(self: html-media-element-handle, value: bool);

    /// `defaultMuted` attribute — getter.
    get-default-muted: func(self: html-media-element-handle) -> bool;
    /// `defaultMuted` attribute — setter.
    set-default-muted: func(self: html-media-element-handle, value: bool);

    /// `loading` attribute — getter.
    get-loading: func(self: html-media-element-handle) -> string;
    /// `loading` attribute — setter.
    set-loading: func(self: html-media-element-handle, value: string);

    /// `audioTracks` attribute — getter.
    get-audio-tracks: func(self: html-media-element-handle) -> u64;

    /// `videoTracks` attribute — getter.
    get-video-tracks: func(self: html-media-element-handle) -> u64;

    /// `textTracks` attribute — getter.
    get-text-tracks: func(self: html-media-element-handle) -> u64;

    /// `addTextTrack()` operation.
    add-text-track: func(self: html-media-element-handle, kind: u64, label: option<string>, language: option<string>) -> u64;
}

interface media-error {
    /// Opaque handle to a host-side `MediaError` instance.
    type media-error-handle = u64;

    /// `code` attribute — getter.
    get-code: func(self: media-error-handle) -> u16;

    /// `message` attribute — getter.
    get-message: func(self: media-error-handle) -> string;
}

interface audio-track-list {
    /// Opaque handle to a host-side `AudioTrackList` instance.
    type audio-track-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: audio-track-list-handle) -> u32;

    /// `AudioTrack()` operation.
    audio-track: func(self: audio-track-list-handle, index: u32);

    /// `getTrackById()` operation.
    get-track-by-id: func(self: audio-track-list-handle, id: string) -> option<u64>;

    /// `onchange` attribute — getter.
    get-onchange: func(self: audio-track-list-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: audio-track-list-handle, value: u64);

    /// `onaddtrack` attribute — getter.
    get-onaddtrack: func(self: audio-track-list-handle) -> u64;
    /// `onaddtrack` attribute — setter.
    set-onaddtrack: func(self: audio-track-list-handle, value: u64);

    /// `onremovetrack` attribute — getter.
    get-onremovetrack: func(self: audio-track-list-handle) -> u64;
    /// `onremovetrack` attribute — setter.
    set-onremovetrack: func(self: audio-track-list-handle, value: u64);
}

interface audio-track {
    /// Opaque handle to a host-side `AudioTrack` instance.
    type audio-track-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: audio-track-handle) -> string;

    /// `kind` attribute — getter.
    get-kind: func(self: audio-track-handle) -> string;

    /// `label` attribute — getter.
    get-label: func(self: audio-track-handle) -> string;

    /// `language` attribute — getter.
    get-language: func(self: audio-track-handle) -> string;

    /// `enabled` attribute — getter.
    get-enabled: func(self: audio-track-handle) -> bool;
    /// `enabled` attribute — setter.
    set-enabled: func(self: audio-track-handle, value: bool);
}

interface video-track-list {
    /// Opaque handle to a host-side `VideoTrackList` instance.
    type video-track-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: video-track-list-handle) -> u32;

    /// `VideoTrack()` operation.
    video-track: func(self: video-track-list-handle, index: u32);

    /// `getTrackById()` operation.
    get-track-by-id: func(self: video-track-list-handle, id: string) -> option<u64>;

    /// `selectedIndex` attribute — getter.
    get-selected-index: func(self: video-track-list-handle) -> s32;

    /// `onchange` attribute — getter.
    get-onchange: func(self: video-track-list-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: video-track-list-handle, value: u64);

    /// `onaddtrack` attribute — getter.
    get-onaddtrack: func(self: video-track-list-handle) -> u64;
    /// `onaddtrack` attribute — setter.
    set-onaddtrack: func(self: video-track-list-handle, value: u64);

    /// `onremovetrack` attribute — getter.
    get-onremovetrack: func(self: video-track-list-handle) -> u64;
    /// `onremovetrack` attribute — setter.
    set-onremovetrack: func(self: video-track-list-handle, value: u64);
}

interface video-track {
    /// Opaque handle to a host-side `VideoTrack` instance.
    type video-track-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: video-track-handle) -> string;

    /// `kind` attribute — getter.
    get-kind: func(self: video-track-handle) -> string;

    /// `label` attribute — getter.
    get-label: func(self: video-track-handle) -> string;

    /// `language` attribute — getter.
    get-language: func(self: video-track-handle) -> string;

    /// `selected` attribute — getter.
    get-selected: func(self: video-track-handle) -> bool;
    /// `selected` attribute — setter.
    set-selected: func(self: video-track-handle, value: bool);
}

interface text-track-list {
    /// Opaque handle to a host-side `TextTrackList` instance.
    type text-track-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: text-track-list-handle) -> u32;

    /// `TextTrack()` operation.
    text-track: func(self: text-track-list-handle, index: u32);

    /// `getTrackById()` operation.
    get-track-by-id: func(self: text-track-list-handle, id: string) -> option<u64>;

    /// `onchange` attribute — getter.
    get-onchange: func(self: text-track-list-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: text-track-list-handle, value: u64);

    /// `onaddtrack` attribute — getter.
    get-onaddtrack: func(self: text-track-list-handle) -> u64;
    /// `onaddtrack` attribute — setter.
    set-onaddtrack: func(self: text-track-list-handle, value: u64);

    /// `onremovetrack` attribute — getter.
    get-onremovetrack: func(self: text-track-list-handle) -> u64;
    /// `onremovetrack` attribute — setter.
    set-onremovetrack: func(self: text-track-list-handle, value: u64);
}

interface text-track {
    /// Opaque handle to a host-side `TextTrack` instance.
    type text-track-handle = u64;

    /// `kind` attribute — getter.
    get-kind: func(self: text-track-handle) -> u64;

    /// `label` attribute — getter.
    get-label: func(self: text-track-handle) -> string;

    /// `language` attribute — getter.
    get-language: func(self: text-track-handle) -> string;

    /// `id` attribute — getter.
    get-id: func(self: text-track-handle) -> string;

    /// `inBandMetadataTrackDispatchType` attribute — getter.
    get-in-band-metadata-track-dispatch-type: func(self: text-track-handle) -> string;

    /// `mode` attribute — getter.
    get-mode: func(self: text-track-handle) -> u64;
    /// `mode` attribute — setter.
    set-mode: func(self: text-track-handle, value: u64);

    /// `cues` attribute — getter.
    get-cues: func(self: text-track-handle) -> option<u64>;

    /// `activeCues` attribute — getter.
    get-active-cues: func(self: text-track-handle) -> option<u64>;

    /// `addCue()` operation.
    add-cue: func(self: text-track-handle, cue: u64);

    /// `removeCue()` operation.
    remove-cue: func(self: text-track-handle, cue: u64);

    /// `oncuechange` attribute — getter.
    get-oncuechange: func(self: text-track-handle) -> u64;
    /// `oncuechange` attribute — setter.
    set-oncuechange: func(self: text-track-handle, value: u64);
}

interface text-track-cue-list {
    /// Opaque handle to a host-side `TextTrackCueList` instance.
    type text-track-cue-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: text-track-cue-list-handle) -> u32;

    /// `TextTrackCue()` operation.
    text-track-cue: func(self: text-track-cue-list-handle, index: u32);

    /// `getCueById()` operation.
    get-cue-by-id: func(self: text-track-cue-list-handle, id: string) -> option<u64>;
}

interface text-track-cue {
    /// Opaque handle to a host-side `TextTrackCue` instance.
    type text-track-cue-handle = u64;

    /// `track` attribute — getter.
    get-track: func(self: text-track-cue-handle) -> option<u64>;

    /// `id` attribute — getter.
    get-id: func(self: text-track-cue-handle) -> string;
    /// `id` attribute — setter.
    set-id: func(self: text-track-cue-handle, value: string);

    /// `startTime` attribute — getter.
    get-start-time: func(self: text-track-cue-handle) -> f64;
    /// `startTime` attribute — setter.
    set-start-time: func(self: text-track-cue-handle, value: f64);

    /// `endTime` attribute — getter.
    get-end-time: func(self: text-track-cue-handle) -> f64;
    /// `endTime` attribute — setter.
    set-end-time: func(self: text-track-cue-handle, value: f64);

    /// `pauseOnExit` attribute — getter.
    get-pause-on-exit: func(self: text-track-cue-handle) -> bool;
    /// `pauseOnExit` attribute — setter.
    set-pause-on-exit: func(self: text-track-cue-handle, value: bool);

    /// `onenter` attribute — getter.
    get-onenter: func(self: text-track-cue-handle) -> u64;
    /// `onenter` attribute — setter.
    set-onenter: func(self: text-track-cue-handle, value: u64);

    /// `onexit` attribute — getter.
    get-onexit: func(self: text-track-cue-handle) -> u64;
    /// `onexit` attribute — setter.
    set-onexit: func(self: text-track-cue-handle, value: u64);
}

interface time-ranges {
    /// Opaque handle to a host-side `TimeRanges` instance.
    type time-ranges-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: time-ranges-handle) -> u32;

    /// `start()` operation.
    start: func(self: time-ranges-handle, index: u32) -> f64;

    /// `end()` operation.
    end: func(self: time-ranges-handle, index: u32) -> f64;
}

interface track-event {
    /// Opaque handle to a host-side `TrackEvent` instance.
    type track-event-handle = u64;

    /// `track` attribute — getter.
    get-track: func(self: track-event-handle) -> option<u64>;
}

interface html-map-element {
    /// Opaque handle to a host-side `HTMLMapElement` instance.
    type html-map-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-map-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-map-element-handle, value: string);

    /// `areas` attribute — getter.
    get-areas: func(self: html-map-element-handle) -> u64;
}

interface html-area-element {
    /// Opaque handle to a host-side `HTMLAreaElement` instance.
    type html-area-element-handle = u64;

    /// `alt` attribute — getter.
    get-alt: func(self: html-area-element-handle) -> string;
    /// `alt` attribute — setter.
    set-alt: func(self: html-area-element-handle, value: string);

    /// `coords` attribute — getter.
    get-coords: func(self: html-area-element-handle) -> string;
    /// `coords` attribute — setter.
    set-coords: func(self: html-area-element-handle, value: string);

    /// `shape` attribute — getter.
    get-shape: func(self: html-area-element-handle) -> string;
    /// `shape` attribute — setter.
    set-shape: func(self: html-area-element-handle, value: string);

    /// `target` attribute — getter.
    get-target: func(self: html-area-element-handle) -> string;
    /// `target` attribute — setter.
    set-target: func(self: html-area-element-handle, value: string);

    /// `download` attribute — getter.
    get-download: func(self: html-area-element-handle) -> string;
    /// `download` attribute — setter.
    set-download: func(self: html-area-element-handle, value: string);

    /// `ping` attribute — getter.
    get-ping: func(self: html-area-element-handle) -> string;
    /// `ping` attribute — setter.
    set-ping: func(self: html-area-element-handle, value: string);

    /// `rel` attribute — getter.
    get-rel: func(self: html-area-element-handle) -> string;
    /// `rel` attribute — setter.
    set-rel: func(self: html-area-element-handle, value: string);

    /// `relList` attribute — getter.
    get-rel-list: func(self: html-area-element-handle) -> u64;

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: html-area-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: html-area-element-handle, value: string);

    /// `noHref` attribute — getter.
    get-no-href: func(self: html-area-element-handle) -> bool;
    /// `noHref` attribute — setter.
    set-no-href: func(self: html-area-element-handle, value: bool);
}

interface html-table-element {
    /// Opaque handle to a host-side `HTMLTableElement` instance.
    type html-table-element-handle = u64;

    /// `caption` attribute — getter.
    get-caption: func(self: html-table-element-handle) -> option<u64>;
    /// `caption` attribute — setter.
    set-caption: func(self: html-table-element-handle, value: option<u64>);

    /// `createCaption()` operation.
    create-caption: func(self: html-table-element-handle) -> u64;

    /// `deleteCaption()` operation.
    delete-caption: func(self: html-table-element-handle);

    /// `tHead` attribute — getter.
    get-t-head: func(self: html-table-element-handle) -> option<u64>;
    /// `tHead` attribute — setter.
    set-t-head: func(self: html-table-element-handle, value: option<u64>);

    /// `createTHead()` operation.
    create-t-head: func(self: html-table-element-handle) -> u64;

    /// `deleteTHead()` operation.
    delete-t-head: func(self: html-table-element-handle);

    /// `tFoot` attribute — getter.
    get-t-foot: func(self: html-table-element-handle) -> option<u64>;
    /// `tFoot` attribute — setter.
    set-t-foot: func(self: html-table-element-handle, value: option<u64>);

    /// `createTFoot()` operation.
    create-t-foot: func(self: html-table-element-handle) -> u64;

    /// `deleteTFoot()` operation.
    delete-t-foot: func(self: html-table-element-handle);

    /// `tBodies` attribute — getter.
    get-t-bodies: func(self: html-table-element-handle) -> u64;

    /// `createTBody()` operation.
    create-t-body: func(self: html-table-element-handle) -> u64;

    /// `rows` attribute — getter.
    get-rows: func(self: html-table-element-handle) -> u64;

    /// `insertRow()` operation.
    insert-row: func(self: html-table-element-handle, index: option<s32>) -> u64;

    /// `deleteRow()` operation.
    delete-row: func(self: html-table-element-handle, index: s32);

    /// `align` attribute — getter.
    get-align: func(self: html-table-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-element-handle, value: string);

    /// `border` attribute — getter.
    get-border: func(self: html-table-element-handle) -> string;
    /// `border` attribute — setter.
    set-border: func(self: html-table-element-handle, value: string);

    /// `frame` attribute — getter.
    get-frame: func(self: html-table-element-handle) -> string;
    /// `frame` attribute — setter.
    set-frame: func(self: html-table-element-handle, value: string);

    /// `rules` attribute — getter.
    get-rules: func(self: html-table-element-handle) -> string;
    /// `rules` attribute — setter.
    set-rules: func(self: html-table-element-handle, value: string);

    /// `summary` attribute — getter.
    get-summary: func(self: html-table-element-handle) -> string;
    /// `summary` attribute — setter.
    set-summary: func(self: html-table-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: html-table-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-table-element-handle, value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func(self: html-table-element-handle) -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(self: html-table-element-handle, value: string);

    /// `cellPadding` attribute — getter.
    get-cell-padding: func(self: html-table-element-handle) -> string;
    /// `cellPadding` attribute — setter.
    set-cell-padding: func(self: html-table-element-handle, value: string);

    /// `cellSpacing` attribute — getter.
    get-cell-spacing: func(self: html-table-element-handle) -> string;
    /// `cellSpacing` attribute — setter.
    set-cell-spacing: func(self: html-table-element-handle, value: string);
}

interface html-table-caption-element {
    /// Opaque handle to a host-side `HTMLTableCaptionElement` instance.
    type html-table-caption-element-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: html-table-caption-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-caption-element-handle, value: string);
}

interface html-table-col-element {
    /// Opaque handle to a host-side `HTMLTableColElement` instance.
    type html-table-col-element-handle = u64;

    /// `span` attribute — getter.
    get-span: func(self: html-table-col-element-handle) -> u32;
    /// `span` attribute — setter.
    set-span: func(self: html-table-col-element-handle, value: u32);

    /// `align` attribute — getter.
    get-align: func(self: html-table-col-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-col-element-handle, value: string);

    /// `ch` attribute — getter.
    get-ch: func(self: html-table-col-element-handle) -> string;
    /// `ch` attribute — setter.
    set-ch: func(self: html-table-col-element-handle, value: string);

    /// `chOff` attribute — getter.
    get-ch-off: func(self: html-table-col-element-handle) -> string;
    /// `chOff` attribute — setter.
    set-ch-off: func(self: html-table-col-element-handle, value: string);

    /// `vAlign` attribute — getter.
    get-v-align: func(self: html-table-col-element-handle) -> string;
    /// `vAlign` attribute — setter.
    set-v-align: func(self: html-table-col-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: html-table-col-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-table-col-element-handle, value: string);
}

interface html-table-section-element {
    /// Opaque handle to a host-side `HTMLTableSectionElement` instance.
    type html-table-section-element-handle = u64;

    /// `rows` attribute — getter.
    get-rows: func(self: html-table-section-element-handle) -> u64;

    /// `insertRow()` operation.
    insert-row: func(self: html-table-section-element-handle, index: option<s32>) -> u64;

    /// `deleteRow()` operation.
    delete-row: func(self: html-table-section-element-handle, index: s32);

    /// `align` attribute — getter.
    get-align: func(self: html-table-section-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-section-element-handle, value: string);

    /// `ch` attribute — getter.
    get-ch: func(self: html-table-section-element-handle) -> string;
    /// `ch` attribute — setter.
    set-ch: func(self: html-table-section-element-handle, value: string);

    /// `chOff` attribute — getter.
    get-ch-off: func(self: html-table-section-element-handle) -> string;
    /// `chOff` attribute — setter.
    set-ch-off: func(self: html-table-section-element-handle, value: string);

    /// `vAlign` attribute — getter.
    get-v-align: func(self: html-table-section-element-handle) -> string;
    /// `vAlign` attribute — setter.
    set-v-align: func(self: html-table-section-element-handle, value: string);
}

interface html-table-row-element {
    /// Opaque handle to a host-side `HTMLTableRowElement` instance.
    type html-table-row-element-handle = u64;

    /// `rowIndex` attribute — getter.
    get-row-index: func(self: html-table-row-element-handle) -> s32;

    /// `sectionRowIndex` attribute — getter.
    get-section-row-index: func(self: html-table-row-element-handle) -> s32;

    /// `cells` attribute — getter.
    get-cells: func(self: html-table-row-element-handle) -> u64;

    /// `insertCell()` operation.
    insert-cell: func(self: html-table-row-element-handle, index: option<s32>) -> u64;

    /// `deleteCell()` operation.
    delete-cell: func(self: html-table-row-element-handle, index: s32);

    /// `align` attribute — getter.
    get-align: func(self: html-table-row-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-row-element-handle, value: string);

    /// `ch` attribute — getter.
    get-ch: func(self: html-table-row-element-handle) -> string;
    /// `ch` attribute — setter.
    set-ch: func(self: html-table-row-element-handle, value: string);

    /// `chOff` attribute — getter.
    get-ch-off: func(self: html-table-row-element-handle) -> string;
    /// `chOff` attribute — setter.
    set-ch-off: func(self: html-table-row-element-handle, value: string);

    /// `vAlign` attribute — getter.
    get-v-align: func(self: html-table-row-element-handle) -> string;
    /// `vAlign` attribute — setter.
    set-v-align: func(self: html-table-row-element-handle, value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func(self: html-table-row-element-handle) -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(self: html-table-row-element-handle, value: string);
}

interface html-table-cell-element {
    /// Opaque handle to a host-side `HTMLTableCellElement` instance.
    type html-table-cell-element-handle = u64;

    /// `colSpan` attribute — getter.
    get-col-span: func(self: html-table-cell-element-handle) -> u32;
    /// `colSpan` attribute — setter.
    set-col-span: func(self: html-table-cell-element-handle, value: u32);

    /// `rowSpan` attribute — getter.
    get-row-span: func(self: html-table-cell-element-handle) -> u32;
    /// `rowSpan` attribute — setter.
    set-row-span: func(self: html-table-cell-element-handle, value: u32);

    /// `headers` attribute — getter.
    get-headers: func(self: html-table-cell-element-handle) -> string;
    /// `headers` attribute — setter.
    set-headers: func(self: html-table-cell-element-handle, value: string);

    /// `cellIndex` attribute — getter.
    get-cell-index: func(self: html-table-cell-element-handle) -> s32;

    /// `scope` attribute — getter.
    get-scope: func(self: html-table-cell-element-handle) -> string;
    /// `scope` attribute — setter.
    set-scope: func(self: html-table-cell-element-handle, value: string);

    /// `abbr` attribute — getter.
    get-abbr: func(self: html-table-cell-element-handle) -> string;
    /// `abbr` attribute — setter.
    set-abbr: func(self: html-table-cell-element-handle, value: string);

    /// `align` attribute — getter.
    get-align: func(self: html-table-cell-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-table-cell-element-handle, value: string);

    /// `axis` attribute — getter.
    get-axis: func(self: html-table-cell-element-handle) -> string;
    /// `axis` attribute — setter.
    set-axis: func(self: html-table-cell-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: html-table-cell-element-handle) -> string;
    /// `height` attribute — setter.
    set-height: func(self: html-table-cell-element-handle, value: string);

    /// `width` attribute — getter.
    get-width: func(self: html-table-cell-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-table-cell-element-handle, value: string);

    /// `ch` attribute — getter.
    get-ch: func(self: html-table-cell-element-handle) -> string;
    /// `ch` attribute — setter.
    set-ch: func(self: html-table-cell-element-handle, value: string);

    /// `chOff` attribute — getter.
    get-ch-off: func(self: html-table-cell-element-handle) -> string;
    /// `chOff` attribute — setter.
    set-ch-off: func(self: html-table-cell-element-handle, value: string);

    /// `noWrap` attribute — getter.
    get-no-wrap: func(self: html-table-cell-element-handle) -> bool;
    /// `noWrap` attribute — setter.
    set-no-wrap: func(self: html-table-cell-element-handle, value: bool);

    /// `vAlign` attribute — getter.
    get-v-align: func(self: html-table-cell-element-handle) -> string;
    /// `vAlign` attribute — setter.
    set-v-align: func(self: html-table-cell-element-handle, value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func(self: html-table-cell-element-handle) -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(self: html-table-cell-element-handle, value: string);
}

interface html-form-element {
    /// Opaque handle to a host-side `HTMLFormElement` instance.
    type html-form-element-handle = u64;

    /// `acceptCharset` attribute — getter.
    get-accept-charset: func(self: html-form-element-handle) -> string;
    /// `acceptCharset` attribute — setter.
    set-accept-charset: func(self: html-form-element-handle, value: string);

    /// `action` attribute — getter.
    get-action: func(self: html-form-element-handle) -> string;
    /// `action` attribute — setter.
    set-action: func(self: html-form-element-handle, value: string);

    /// `autocomplete` attribute — getter.
    get-autocomplete: func(self: html-form-element-handle) -> string;
    /// `autocomplete` attribute — setter.
    set-autocomplete: func(self: html-form-element-handle, value: string);

    /// `enctype` attribute — getter.
    get-enctype: func(self: html-form-element-handle) -> string;
    /// `enctype` attribute — setter.
    set-enctype: func(self: html-form-element-handle, value: string);

    /// `encoding` attribute — getter.
    get-encoding: func(self: html-form-element-handle) -> string;
    /// `encoding` attribute — setter.
    set-encoding: func(self: html-form-element-handle, value: string);

    /// `method` attribute — getter.
    get-method: func(self: html-form-element-handle) -> string;
    /// `method` attribute — setter.
    set-method: func(self: html-form-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: html-form-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-form-element-handle, value: string);

    /// `noValidate` attribute — getter.
    get-no-validate: func(self: html-form-element-handle) -> bool;
    /// `noValidate` attribute — setter.
    set-no-validate: func(self: html-form-element-handle, value: bool);

    /// `target` attribute — getter.
    get-target: func(self: html-form-element-handle) -> string;
    /// `target` attribute — setter.
    set-target: func(self: html-form-element-handle, value: string);

    /// `rel` attribute — getter.
    get-rel: func(self: html-form-element-handle) -> string;
    /// `rel` attribute — setter.
    set-rel: func(self: html-form-element-handle, value: string);

    /// `relList` attribute — getter.
    get-rel-list: func(self: html-form-element-handle) -> u64;

    /// `elements` attribute — getter.
    get-elements: func(self: html-form-element-handle) -> u64;

    /// `length` attribute — getter.
    get-length: func(self: html-form-element-handle) -> u32;

    /// `Element()` operation.
    element: func(self: html-form-element-handle, index: u32);

    /// `submit()` operation.
    submit: func(self: html-form-element-handle);

    /// `requestSubmit()` operation.
    request-submit: func(self: html-form-element-handle, submitter: option<u64>);

    /// `reset()` operation.
    reset: func(self: html-form-element-handle);

    /// `checkValidity()` operation.
    check-validity: func(self: html-form-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-form-element-handle) -> bool;
}

interface html-label-element {
    /// Opaque handle to a host-side `HTMLLabelElement` instance.
    type html-label-element-handle = u64;

    /// `form` attribute — getter.
    get-form: func(self: html-label-element-handle) -> option<u64>;

    /// `htmlFor` attribute — getter.
    get-html-for: func(self: html-label-element-handle) -> string;
    /// `htmlFor` attribute — setter.
    set-html-for: func(self: html-label-element-handle, value: string);

    /// `control` attribute — getter.
    get-control: func(self: html-label-element-handle) -> option<u64>;
}

interface html-input-element {
    /// Opaque handle to a host-side `HTMLInputElement` instance.
    type html-input-element-handle = u64;

    /// `accept` attribute — getter.
    get-accept: func(self: html-input-element-handle) -> string;
    /// `accept` attribute — setter.
    set-accept: func(self: html-input-element-handle, value: string);

    /// `alpha` attribute — getter.
    get-alpha: func(self: html-input-element-handle) -> bool;
    /// `alpha` attribute — setter.
    set-alpha: func(self: html-input-element-handle, value: bool);

    /// `alt` attribute — getter.
    get-alt: func(self: html-input-element-handle) -> string;
    /// `alt` attribute — setter.
    set-alt: func(self: html-input-element-handle, value: string);

    /// `autocomplete` attribute — getter.
    get-autocomplete: func(self: html-input-element-handle) -> string;
    /// `autocomplete` attribute — setter.
    set-autocomplete: func(self: html-input-element-handle, value: string);

    /// `defaultChecked` attribute — getter.
    get-default-checked: func(self: html-input-element-handle) -> bool;
    /// `defaultChecked` attribute — setter.
    set-default-checked: func(self: html-input-element-handle, value: bool);

    /// `checked` attribute — getter.
    get-checked: func(self: html-input-element-handle) -> bool;
    /// `checked` attribute — setter.
    set-checked: func(self: html-input-element-handle, value: bool);

    /// `colorSpace` attribute — getter.
    get-color-space: func(self: html-input-element-handle) -> string;
    /// `colorSpace` attribute — setter.
    set-color-space: func(self: html-input-element-handle, value: string);

    /// `dirName` attribute — getter.
    get-dir-name: func(self: html-input-element-handle) -> string;
    /// `dirName` attribute — setter.
    set-dir-name: func(self: html-input-element-handle, value: string);

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-input-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-input-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-input-element-handle) -> option<u64>;

    /// `files` attribute — getter.
    get-files: func(self: html-input-element-handle) -> option<u64>;
    /// `files` attribute — setter.
    set-files: func(self: html-input-element-handle, value: option<u64>);

    /// `formAction` attribute — getter.
    get-form-action: func(self: html-input-element-handle) -> string;
    /// `formAction` attribute — setter.
    set-form-action: func(self: html-input-element-handle, value: string);

    /// `formEnctype` attribute — getter.
    get-form-enctype: func(self: html-input-element-handle) -> string;
    /// `formEnctype` attribute — setter.
    set-form-enctype: func(self: html-input-element-handle, value: string);

    /// `formMethod` attribute — getter.
    get-form-method: func(self: html-input-element-handle) -> string;
    /// `formMethod` attribute — setter.
    set-form-method: func(self: html-input-element-handle, value: string);

    /// `formNoValidate` attribute — getter.
    get-form-no-validate: func(self: html-input-element-handle) -> bool;
    /// `formNoValidate` attribute — setter.
    set-form-no-validate: func(self: html-input-element-handle, value: bool);

    /// `formTarget` attribute — getter.
    get-form-target: func(self: html-input-element-handle) -> string;
    /// `formTarget` attribute — setter.
    set-form-target: func(self: html-input-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: html-input-element-handle) -> u32;
    /// `height` attribute — setter.
    set-height: func(self: html-input-element-handle, value: u32);

    /// `indeterminate` attribute — getter.
    get-indeterminate: func(self: html-input-element-handle) -> bool;
    /// `indeterminate` attribute — setter.
    set-indeterminate: func(self: html-input-element-handle, value: bool);

    /// `list` attribute — getter.
    get-list: func(self: html-input-element-handle) -> option<u64>;

    /// `max` attribute — getter.
    get-max: func(self: html-input-element-handle) -> string;
    /// `max` attribute — setter.
    set-max: func(self: html-input-element-handle, value: string);

    /// `maxLength` attribute — getter.
    get-max-length: func(self: html-input-element-handle) -> s32;
    /// `maxLength` attribute — setter.
    set-max-length: func(self: html-input-element-handle, value: s32);

    /// `min` attribute — getter.
    get-min: func(self: html-input-element-handle) -> string;
    /// `min` attribute — setter.
    set-min: func(self: html-input-element-handle, value: string);

    /// `minLength` attribute — getter.
    get-min-length: func(self: html-input-element-handle) -> s32;
    /// `minLength` attribute — setter.
    set-min-length: func(self: html-input-element-handle, value: s32);

    /// `multiple` attribute — getter.
    get-multiple: func(self: html-input-element-handle) -> bool;
    /// `multiple` attribute — setter.
    set-multiple: func(self: html-input-element-handle, value: bool);

    /// `name` attribute — getter.
    get-name: func(self: html-input-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-input-element-handle, value: string);

    /// `pattern` attribute — getter.
    get-pattern: func(self: html-input-element-handle) -> string;
    /// `pattern` attribute — setter.
    set-pattern: func(self: html-input-element-handle, value: string);

    /// `placeholder` attribute — getter.
    get-placeholder: func(self: html-input-element-handle) -> string;
    /// `placeholder` attribute — setter.
    set-placeholder: func(self: html-input-element-handle, value: string);

    /// `readOnly` attribute — getter.
    get-read-only: func(self: html-input-element-handle) -> bool;
    /// `readOnly` attribute — setter.
    set-read-only: func(self: html-input-element-handle, value: bool);

    /// `required` attribute — getter.
    get-required: func(self: html-input-element-handle) -> bool;
    /// `required` attribute — setter.
    set-required: func(self: html-input-element-handle, value: bool);

    /// `size` attribute — getter.
    get-size: func(self: html-input-element-handle) -> u32;
    /// `size` attribute — setter.
    set-size: func(self: html-input-element-handle, value: u32);

    /// `src` attribute — getter.
    get-src: func(self: html-input-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-input-element-handle, value: string);

    /// `step` attribute — getter.
    get-step: func(self: html-input-element-handle) -> string;
    /// `step` attribute — setter.
    set-step: func(self: html-input-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-input-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-input-element-handle, value: string);

    /// `defaultValue` attribute — getter.
    get-default-value: func(self: html-input-element-handle) -> string;
    /// `defaultValue` attribute — setter.
    set-default-value: func(self: html-input-element-handle, value: string);

    /// `value` attribute — getter.
    get-value: func(self: html-input-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-input-element-handle, value: string);

    /// `valueAsDate` attribute — getter.
    get-value-as-date: func(self: html-input-element-handle) -> option<u64>;
    /// `valueAsDate` attribute — setter.
    set-value-as-date: func(self: html-input-element-handle, value: option<u64>);

    /// `valueAsNumber` attribute — getter.
    get-value-as-number: func(self: html-input-element-handle) -> f64;
    /// `valueAsNumber` attribute — setter.
    set-value-as-number: func(self: html-input-element-handle, value: f64);

    /// `width` attribute — getter.
    get-width: func(self: html-input-element-handle) -> u32;
    /// `width` attribute — setter.
    set-width: func(self: html-input-element-handle, value: u32);

    /// `stepUp()` operation.
    step-up: func(self: html-input-element-handle, n: option<s32>);

    /// `stepDown()` operation.
    step-down: func(self: html-input-element-handle, n: option<s32>);

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-input-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-input-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-input-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-input-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-input-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-input-element-handle, error: string);

    /// `labels` attribute — getter.
    get-labels: func(self: html-input-element-handle) -> option<u64>;

    /// `select()` operation.
    select: func(self: html-input-element-handle);

    /// `selectionStart` attribute — getter.
    get-selection-start: func(self: html-input-element-handle) -> option<u32>;
    /// `selectionStart` attribute — setter.
    set-selection-start: func(self: html-input-element-handle, value: option<u32>);

    /// `selectionEnd` attribute — getter.
    get-selection-end: func(self: html-input-element-handle) -> option<u32>;
    /// `selectionEnd` attribute — setter.
    set-selection-end: func(self: html-input-element-handle, value: option<u32>);

    /// `selectionDirection` attribute — getter.
    get-selection-direction: func(self: html-input-element-handle) -> option<string>;
    /// `selectionDirection` attribute — setter.
    set-selection-direction: func(self: html-input-element-handle, value: option<string>);

    /// `setRangeText()` operation.
    set-range-text: func(self: html-input-element-handle, replacement: string);

    /// `setSelectionRange()` operation.
    set-selection-range: func(self: html-input-element-handle, start: u32, end: u32, direction: option<string>);

    /// `showPicker()` operation.
    show-picker: func(self: html-input-element-handle);

    /// `align` attribute — getter.
    get-align: func(self: html-input-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-input-element-handle, value: string);

    /// `useMap` attribute — getter.
    get-use-map: func(self: html-input-element-handle) -> string;
    /// `useMap` attribute — setter.
    set-use-map: func(self: html-input-element-handle, value: string);
}

interface html-button-element {
    /// Opaque handle to a host-side `HTMLButtonElement` instance.
    type html-button-element-handle = u64;

    /// `command` attribute — getter.
    get-command: func(self: html-button-element-handle) -> string;
    /// `command` attribute — setter.
    set-command: func(self: html-button-element-handle, value: string);

    /// `commandForElement` attribute — getter.
    get-command-for-element: func(self: html-button-element-handle) -> option<u64>;
    /// `commandForElement` attribute — setter.
    set-command-for-element: func(self: html-button-element-handle, value: option<u64>);

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-button-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-button-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-button-element-handle) -> option<u64>;

    /// `formAction` attribute — getter.
    get-form-action: func(self: html-button-element-handle) -> string;
    /// `formAction` attribute — setter.
    set-form-action: func(self: html-button-element-handle, value: string);

    /// `formEnctype` attribute — getter.
    get-form-enctype: func(self: html-button-element-handle) -> string;
    /// `formEnctype` attribute — setter.
    set-form-enctype: func(self: html-button-element-handle, value: string);

    /// `formMethod` attribute — getter.
    get-form-method: func(self: html-button-element-handle) -> string;
    /// `formMethod` attribute — setter.
    set-form-method: func(self: html-button-element-handle, value: string);

    /// `formNoValidate` attribute — getter.
    get-form-no-validate: func(self: html-button-element-handle) -> bool;
    /// `formNoValidate` attribute — setter.
    set-form-no-validate: func(self: html-button-element-handle, value: bool);

    /// `formTarget` attribute — getter.
    get-form-target: func(self: html-button-element-handle) -> string;
    /// `formTarget` attribute — setter.
    set-form-target: func(self: html-button-element-handle, value: string);

    /// `name` attribute — getter.
    get-name: func(self: html-button-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-button-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-button-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-button-element-handle, value: string);

    /// `value` attribute — getter.
    get-value: func(self: html-button-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-button-element-handle, value: string);

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-button-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-button-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-button-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-button-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-button-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-button-element-handle, error: string);

    /// `labels` attribute — getter.
    get-labels: func(self: html-button-element-handle) -> u64;
}

interface html-select-element {
    /// Opaque handle to a host-side `HTMLSelectElement` instance.
    type html-select-element-handle = u64;

    /// `autocomplete` attribute — getter.
    get-autocomplete: func(self: html-select-element-handle) -> string;
    /// `autocomplete` attribute — setter.
    set-autocomplete: func(self: html-select-element-handle, value: string);

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-select-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-select-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-select-element-handle) -> option<u64>;

    /// `multiple` attribute — getter.
    get-multiple: func(self: html-select-element-handle) -> bool;
    /// `multiple` attribute — setter.
    set-multiple: func(self: html-select-element-handle, value: bool);

    /// `name` attribute — getter.
    get-name: func(self: html-select-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-select-element-handle, value: string);

    /// `required` attribute — getter.
    get-required: func(self: html-select-element-handle) -> bool;
    /// `required` attribute — setter.
    set-required: func(self: html-select-element-handle, value: bool);

    /// `size` attribute — getter.
    get-size: func(self: html-select-element-handle) -> u32;
    /// `size` attribute — setter.
    set-size: func(self: html-select-element-handle, value: u32);

    /// `type` attribute — getter.
    get-type: func(self: html-select-element-handle) -> string;

    /// `options` attribute — getter.
    get-options: func(self: html-select-element-handle) -> u64;

    /// `length` attribute — getter.
    get-length: func(self: html-select-element-handle) -> u32;
    /// `length` attribute — setter.
    set-length: func(self: html-select-element-handle, value: u32);

    /// `item()` operation.
    item: func(self: html-select-element-handle, index: u32) -> option<u64>;

    /// `namedItem()` operation.
    named-item: func(self: html-select-element-handle, name: string) -> option<u64>;

    /// `add()` operation.
    add: func(self: html-select-element-handle, element: u64, before: option<f64>);

    /// `remove()` operation.
    remove: func(self: html-select-element-handle);

    /// `undefined()` operation.
    undefined: func(self: html-select-element-handle, index: u32, %option: option<u64>);

    /// `selectedOptions` attribute — getter.
    get-selected-options: func(self: html-select-element-handle) -> u64;

    /// `selectedIndex` attribute — getter.
    get-selected-index: func(self: html-select-element-handle) -> s32;
    /// `selectedIndex` attribute — setter.
    set-selected-index: func(self: html-select-element-handle, value: s32);

    /// `value` attribute — getter.
    get-value: func(self: html-select-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-select-element-handle, value: string);

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-select-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-select-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-select-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-select-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-select-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-select-element-handle, error: string);

    /// `showPicker()` operation.
    show-picker: func(self: html-select-element-handle);

    /// `labels` attribute — getter.
    get-labels: func(self: html-select-element-handle) -> u64;
}

interface html-data-list-element {
    /// Opaque handle to a host-side `HTMLDataListElement` instance.
    type html-data-list-element-handle = u64;

    /// `options` attribute — getter.
    get-options: func(self: html-data-list-element-handle) -> u64;
}

interface html-opt-group-element {
    /// Opaque handle to a host-side `HTMLOptGroupElement` instance.
    type html-opt-group-element-handle = u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-opt-group-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-opt-group-element-handle, value: bool);

    /// `label` attribute — getter.
    get-label: func(self: html-opt-group-element-handle) -> string;
    /// `label` attribute — setter.
    set-label: func(self: html-opt-group-element-handle, value: string);
}

interface html-option-element {
    /// Opaque handle to a host-side `HTMLOptionElement` instance.
    type html-option-element-handle = u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-option-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-option-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-option-element-handle) -> option<u64>;

    /// `label` attribute — getter.
    get-label: func(self: html-option-element-handle) -> string;
    /// `label` attribute — setter.
    set-label: func(self: html-option-element-handle, value: string);

    /// `defaultSelected` attribute — getter.
    get-default-selected: func(self: html-option-element-handle) -> bool;
    /// `defaultSelected` attribute — setter.
    set-default-selected: func(self: html-option-element-handle, value: bool);

    /// `selected` attribute — getter.
    get-selected: func(self: html-option-element-handle) -> bool;
    /// `selected` attribute — setter.
    set-selected: func(self: html-option-element-handle, value: bool);

    /// `value` attribute — getter.
    get-value: func(self: html-option-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-option-element-handle, value: string);

    /// `text` attribute — getter.
    get-text: func(self: html-option-element-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: html-option-element-handle, value: string);

    /// `index` attribute — getter.
    get-index: func(self: html-option-element-handle) -> s32;
}

interface html-text-area-element {
    /// Opaque handle to a host-side `HTMLTextAreaElement` instance.
    type html-text-area-element-handle = u64;

    /// `autocomplete` attribute — getter.
    get-autocomplete: func(self: html-text-area-element-handle) -> string;
    /// `autocomplete` attribute — setter.
    set-autocomplete: func(self: html-text-area-element-handle, value: string);

    /// `cols` attribute — getter.
    get-cols: func(self: html-text-area-element-handle) -> u32;
    /// `cols` attribute — setter.
    set-cols: func(self: html-text-area-element-handle, value: u32);

    /// `dirName` attribute — getter.
    get-dir-name: func(self: html-text-area-element-handle) -> string;
    /// `dirName` attribute — setter.
    set-dir-name: func(self: html-text-area-element-handle, value: string);

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-text-area-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-text-area-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-text-area-element-handle) -> option<u64>;

    /// `maxLength` attribute — getter.
    get-max-length: func(self: html-text-area-element-handle) -> s32;
    /// `maxLength` attribute — setter.
    set-max-length: func(self: html-text-area-element-handle, value: s32);

    /// `minLength` attribute — getter.
    get-min-length: func(self: html-text-area-element-handle) -> s32;
    /// `minLength` attribute — setter.
    set-min-length: func(self: html-text-area-element-handle, value: s32);

    /// `name` attribute — getter.
    get-name: func(self: html-text-area-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-text-area-element-handle, value: string);

    /// `placeholder` attribute — getter.
    get-placeholder: func(self: html-text-area-element-handle) -> string;
    /// `placeholder` attribute — setter.
    set-placeholder: func(self: html-text-area-element-handle, value: string);

    /// `readOnly` attribute — getter.
    get-read-only: func(self: html-text-area-element-handle) -> bool;
    /// `readOnly` attribute — setter.
    set-read-only: func(self: html-text-area-element-handle, value: bool);

    /// `required` attribute — getter.
    get-required: func(self: html-text-area-element-handle) -> bool;
    /// `required` attribute — setter.
    set-required: func(self: html-text-area-element-handle, value: bool);

    /// `rows` attribute — getter.
    get-rows: func(self: html-text-area-element-handle) -> u32;
    /// `rows` attribute — setter.
    set-rows: func(self: html-text-area-element-handle, value: u32);

    /// `wrap` attribute — getter.
    get-wrap: func(self: html-text-area-element-handle) -> string;
    /// `wrap` attribute — setter.
    set-wrap: func(self: html-text-area-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-text-area-element-handle) -> string;

    /// `defaultValue` attribute — getter.
    get-default-value: func(self: html-text-area-element-handle) -> string;
    /// `defaultValue` attribute — setter.
    set-default-value: func(self: html-text-area-element-handle, value: string);

    /// `value` attribute — getter.
    get-value: func(self: html-text-area-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-text-area-element-handle, value: string);

    /// `textLength` attribute — getter.
    get-text-length: func(self: html-text-area-element-handle) -> u32;

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-text-area-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-text-area-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-text-area-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-text-area-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-text-area-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-text-area-element-handle, error: string);

    /// `labels` attribute — getter.
    get-labels: func(self: html-text-area-element-handle) -> u64;

    /// `select()` operation.
    select: func(self: html-text-area-element-handle);

    /// `selectionStart` attribute — getter.
    get-selection-start: func(self: html-text-area-element-handle) -> u32;
    /// `selectionStart` attribute — setter.
    set-selection-start: func(self: html-text-area-element-handle, value: u32);

    /// `selectionEnd` attribute — getter.
    get-selection-end: func(self: html-text-area-element-handle) -> u32;
    /// `selectionEnd` attribute — setter.
    set-selection-end: func(self: html-text-area-element-handle, value: u32);

    /// `selectionDirection` attribute — getter.
    get-selection-direction: func(self: html-text-area-element-handle) -> string;
    /// `selectionDirection` attribute — setter.
    set-selection-direction: func(self: html-text-area-element-handle, value: string);

    /// `setRangeText()` operation.
    set-range-text: func(self: html-text-area-element-handle, replacement: string);

    /// `setSelectionRange()` operation.
    set-selection-range: func(self: html-text-area-element-handle, start: u32, end: u32, direction: option<string>);
}

interface html-output-element {
    /// Opaque handle to a host-side `HTMLOutputElement` instance.
    type html-output-element-handle = u64;

    /// `htmlFor` attribute — getter.
    get-html-for: func(self: html-output-element-handle) -> u64;

    /// `form` attribute — getter.
    get-form: func(self: html-output-element-handle) -> option<u64>;

    /// `name` attribute — getter.
    get-name: func(self: html-output-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-output-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-output-element-handle) -> string;

    /// `defaultValue` attribute — getter.
    get-default-value: func(self: html-output-element-handle) -> string;
    /// `defaultValue` attribute — setter.
    set-default-value: func(self: html-output-element-handle, value: string);

    /// `value` attribute — getter.
    get-value: func(self: html-output-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-output-element-handle, value: string);

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-output-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-output-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-output-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-output-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-output-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-output-element-handle, error: string);

    /// `labels` attribute — getter.
    get-labels: func(self: html-output-element-handle) -> u64;
}

interface html-progress-element {
    /// Opaque handle to a host-side `HTMLProgressElement` instance.
    type html-progress-element-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: html-progress-element-handle) -> f64;
    /// `value` attribute — setter.
    set-value: func(self: html-progress-element-handle, value: f64);

    /// `max` attribute — getter.
    get-max: func(self: html-progress-element-handle) -> f64;
    /// `max` attribute — setter.
    set-max: func(self: html-progress-element-handle, value: f64);

    /// `position` attribute — getter.
    get-position: func(self: html-progress-element-handle) -> f64;

    /// `labels` attribute — getter.
    get-labels: func(self: html-progress-element-handle) -> u64;
}

interface html-meter-element {
    /// Opaque handle to a host-side `HTMLMeterElement` instance.
    type html-meter-element-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: html-meter-element-handle) -> f64;
    /// `value` attribute — setter.
    set-value: func(self: html-meter-element-handle, value: f64);

    /// `min` attribute — getter.
    get-min: func(self: html-meter-element-handle) -> f64;
    /// `min` attribute — setter.
    set-min: func(self: html-meter-element-handle, value: f64);

    /// `max` attribute — getter.
    get-max: func(self: html-meter-element-handle) -> f64;
    /// `max` attribute — setter.
    set-max: func(self: html-meter-element-handle, value: f64);

    /// `low` attribute — getter.
    get-low: func(self: html-meter-element-handle) -> f64;
    /// `low` attribute — setter.
    set-low: func(self: html-meter-element-handle, value: f64);

    /// `high` attribute — getter.
    get-high: func(self: html-meter-element-handle) -> f64;
    /// `high` attribute — setter.
    set-high: func(self: html-meter-element-handle, value: f64);

    /// `optimum` attribute — getter.
    get-optimum: func(self: html-meter-element-handle) -> f64;
    /// `optimum` attribute — setter.
    set-optimum: func(self: html-meter-element-handle, value: f64);

    /// `labels` attribute — getter.
    get-labels: func(self: html-meter-element-handle) -> u64;
}

interface html-field-set-element {
    /// Opaque handle to a host-side `HTMLFieldSetElement` instance.
    type html-field-set-element-handle = u64;

    /// `disabled` attribute — getter.
    get-disabled: func(self: html-field-set-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: html-field-set-element-handle, value: bool);

    /// `form` attribute — getter.
    get-form: func(self: html-field-set-element-handle) -> option<u64>;

    /// `name` attribute — getter.
    get-name: func(self: html-field-set-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-field-set-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-field-set-element-handle) -> string;

    /// `elements` attribute — getter.
    get-elements: func(self: html-field-set-element-handle) -> u64;

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: html-field-set-element-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: html-field-set-element-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: html-field-set-element-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: html-field-set-element-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: html-field-set-element-handle) -> bool;

    /// `setCustomValidity()` operation.
    set-custom-validity: func(self: html-field-set-element-handle, error: string);
}

interface html-legend-element {
    /// Opaque handle to a host-side `HTMLLegendElement` instance.
    type html-legend-element-handle = u64;

    /// `form` attribute — getter.
    get-form: func(self: html-legend-element-handle) -> option<u64>;

    /// `align` attribute — getter.
    get-align: func(self: html-legend-element-handle) -> string;
    /// `align` attribute — setter.
    set-align: func(self: html-legend-element-handle, value: string);
}

interface validity-state {
    /// Opaque handle to a host-side `ValidityState` instance.
    type validity-state-handle = u64;

    /// `valueMissing` attribute — getter.
    get-value-missing: func(self: validity-state-handle) -> bool;

    /// `typeMismatch` attribute — getter.
    get-type-mismatch: func(self: validity-state-handle) -> bool;

    /// `patternMismatch` attribute — getter.
    get-pattern-mismatch: func(self: validity-state-handle) -> bool;

    /// `tooLong` attribute — getter.
    get-too-long: func(self: validity-state-handle) -> bool;

    /// `tooShort` attribute — getter.
    get-too-short: func(self: validity-state-handle) -> bool;

    /// `rangeUnderflow` attribute — getter.
    get-range-underflow: func(self: validity-state-handle) -> bool;

    /// `rangeOverflow` attribute — getter.
    get-range-overflow: func(self: validity-state-handle) -> bool;

    /// `stepMismatch` attribute — getter.
    get-step-mismatch: func(self: validity-state-handle) -> bool;

    /// `badInput` attribute — getter.
    get-bad-input: func(self: validity-state-handle) -> bool;

    /// `customError` attribute — getter.
    get-custom-error: func(self: validity-state-handle) -> bool;

    /// `valid` attribute — getter.
    get-valid: func(self: validity-state-handle) -> bool;
}

interface submit-event {
    /// Opaque handle to a host-side `SubmitEvent` instance.
    type submit-event-handle = u64;

    /// `submitter` attribute — getter.
    get-submitter: func(self: submit-event-handle) -> option<u64>;
}

interface form-data-event {
    /// Opaque handle to a host-side `FormDataEvent` instance.
    type form-data-event-handle = u64;

    /// `formData` attribute — getter.
    get-form-data: func(self: form-data-event-handle) -> u64;
}

interface html-details-element {
    /// Opaque handle to a host-side `HTMLDetailsElement` instance.
    type html-details-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-details-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-details-element-handle, value: string);

    /// `open` attribute — getter.
    get-open: func(self: html-details-element-handle) -> bool;
    /// `open` attribute — setter.
    set-open: func(self: html-details-element-handle, value: bool);
}

interface html-dialog-element {
    /// Opaque handle to a host-side `HTMLDialogElement` instance.
    type html-dialog-element-handle = u64;

    /// `open` attribute — getter.
    get-open: func(self: html-dialog-element-handle) -> bool;
    /// `open` attribute — setter.
    set-open: func(self: html-dialog-element-handle, value: bool);

    /// `returnValue` attribute — getter.
    get-return-value: func(self: html-dialog-element-handle) -> string;
    /// `returnValue` attribute — setter.
    set-return-value: func(self: html-dialog-element-handle, value: string);

    /// `closedBy` attribute — getter.
    get-closed-by: func(self: html-dialog-element-handle) -> string;
    /// `closedBy` attribute — setter.
    set-closed-by: func(self: html-dialog-element-handle, value: string);

    /// `show()` operation.
    show: func(self: html-dialog-element-handle);

    /// `showModal()` operation.
    show-modal: func(self: html-dialog-element-handle);

    /// `close()` operation.
    close: func(self: html-dialog-element-handle, return-value: option<string>);

    /// `requestClose()` operation.
    request-close: func(self: html-dialog-element-handle, return-value: option<string>);
}

interface html-script-element {
    /// Opaque handle to a host-side `HTMLScriptElement` instance.
    type html-script-element-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: html-script-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-script-element-handle, value: string);

    /// `src` attribute — getter.
    get-src: func(self: html-script-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-script-element-handle, value: string);

    /// `noModule` attribute — getter.
    get-no-module: func(self: html-script-element-handle) -> bool;
    /// `noModule` attribute — setter.
    set-no-module: func(self: html-script-element-handle, value: bool);

    /// `async` attribute — getter.
    get-async: func(self: html-script-element-handle) -> bool;
    /// `async` attribute — setter.
    set-async: func(self: html-script-element-handle, value: bool);

    /// `defer` attribute — getter.
    get-defer: func(self: html-script-element-handle) -> bool;
    /// `defer` attribute — setter.
    set-defer: func(self: html-script-element-handle, value: bool);

    /// `blocking` attribute — getter.
    get-blocking: func(self: html-script-element-handle) -> u64;

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: html-script-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: html-script-element-handle, value: option<string>);

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: html-script-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: html-script-element-handle, value: string);

    /// `integrity` attribute — getter.
    get-integrity: func(self: html-script-element-handle) -> string;
    /// `integrity` attribute — setter.
    set-integrity: func(self: html-script-element-handle, value: string);

    /// `fetchPriority` attribute — getter.
    get-fetch-priority: func(self: html-script-element-handle) -> string;
    /// `fetchPriority` attribute — setter.
    set-fetch-priority: func(self: html-script-element-handle, value: string);

    /// `text` attribute — getter.
    get-text: func(self: html-script-element-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: html-script-element-handle, value: string);

    /// `supports()` operation.
    supports: func(%type: string) -> bool;

    /// `charset` attribute — getter.
    get-charset: func(self: html-script-element-handle) -> string;
    /// `charset` attribute — setter.
    set-charset: func(self: html-script-element-handle, value: string);

    /// `event` attribute — getter.
    get-event: func(self: html-script-element-handle) -> string;
    /// `event` attribute — setter.
    set-event: func(self: html-script-element-handle, value: string);

    /// `htmlFor` attribute — getter.
    get-html-for: func(self: html-script-element-handle) -> string;
    /// `htmlFor` attribute — setter.
    set-html-for: func(self: html-script-element-handle, value: string);
}

interface html-template-element {
    /// Opaque handle to a host-side `HTMLTemplateElement` instance.
    type html-template-element-handle = u64;

    /// `content` attribute — getter.
    get-content: func(self: html-template-element-handle) -> u64;

    /// `shadowRootMode` attribute — getter.
    get-shadow-root-mode: func(self: html-template-element-handle) -> string;
    /// `shadowRootMode` attribute — setter.
    set-shadow-root-mode: func(self: html-template-element-handle, value: string);

    /// `shadowRootDelegatesFocus` attribute — getter.
    get-shadow-root-delegates-focus: func(self: html-template-element-handle) -> bool;
    /// `shadowRootDelegatesFocus` attribute — setter.
    set-shadow-root-delegates-focus: func(self: html-template-element-handle, value: bool);

    /// `shadowRootClonable` attribute — getter.
    get-shadow-root-clonable: func(self: html-template-element-handle) -> bool;
    /// `shadowRootClonable` attribute — setter.
    set-shadow-root-clonable: func(self: html-template-element-handle, value: bool);

    /// `shadowRootSerializable` attribute — getter.
    get-shadow-root-serializable: func(self: html-template-element-handle) -> bool;
    /// `shadowRootSerializable` attribute — setter.
    set-shadow-root-serializable: func(self: html-template-element-handle, value: bool);

    /// `shadowRootCustomElementRegistry` attribute — getter.
    get-shadow-root-custom-element-registry: func(self: html-template-element-handle) -> string;
    /// `shadowRootCustomElementRegistry` attribute — setter.
    set-shadow-root-custom-element-registry: func(self: html-template-element-handle, value: string);
}

interface html-slot-element {
    /// Opaque handle to a host-side `HTMLSlotElement` instance.
    type html-slot-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-slot-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-slot-element-handle, value: string);

    /// `assignedNodes()` operation.
    assigned-nodes: func(self: html-slot-element-handle, options: option<u64>) -> list<u64>;

    /// `assignedElements()` operation.
    assigned-elements: func(self: html-slot-element-handle, options: option<u64>) -> list<u64>;

    /// `assign()` operation.
    assign: func(self: html-slot-element-handle, nodes: list<u64>);
}

interface html-canvas-element {
    /// Opaque handle to a host-side `HTMLCanvasElement` instance.
    type html-canvas-element-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: html-canvas-element-handle) -> u32;
    /// `width` attribute — setter.
    set-width: func(self: html-canvas-element-handle, value: u32);

    /// `height` attribute — getter.
    get-height: func(self: html-canvas-element-handle) -> u32;
    /// `height` attribute — setter.
    set-height: func(self: html-canvas-element-handle, value: u32);

    /// `getContext()` operation.
    get-context: func(self: html-canvas-element-handle, context-id: string, options: option<string>) -> option<u64>;

    /// `toDataURL()` operation.
    to-data-url: func(self: html-canvas-element-handle, %type: option<string>, quality: option<string>) -> string;

    /// `toBlob()` operation.
    to-blob: func(self: html-canvas-element-handle, callback: u64, %type: option<string>, quality: option<string>);

    /// `transferControlToOffscreen()` operation.
    transfer-control-to-offscreen: func(self: html-canvas-element-handle) -> u64;
}

interface canvas-rendering-context2-d {
    /// Opaque handle to a host-side `CanvasRenderingContext2D` instance.
    type canvas-rendering-context2-d-handle = u64;

    /// `canvas` attribute — getter.
    get-canvas: func(self: canvas-rendering-context2-d-handle) -> u64;
}

interface canvas-settings {
    /// Opaque handle to a host-side `CanvasSettings` instance.
    type canvas-settings-handle = u64;

    /// `getContextAttributes()` operation.
    get-context-attributes: func(self: canvas-settings-handle) -> u64;
}

interface canvas-state {
    /// Opaque handle to a host-side `CanvasState` instance.
    type canvas-state-handle = u64;

    /// `save()` operation.
    save: func(self: canvas-state-handle);

    /// `restore()` operation.
    restore: func(self: canvas-state-handle);

    /// `reset()` operation.
    reset: func(self: canvas-state-handle);

    /// `isContextLost()` operation.
    is-context-lost: func(self: canvas-state-handle) -> bool;
}

interface canvas-transform {
    /// Opaque handle to a host-side `CanvasTransform` instance.
    type canvas-transform-handle = u64;

    /// `scale()` operation.
    scale: func(self: canvas-transform-handle, x: f64, y: f64);

    /// `rotate()` operation.
    rotate: func(self: canvas-transform-handle, angle: f64);

    /// `translate()` operation.
    translate: func(self: canvas-transform-handle, x: f64, y: f64);

    /// `transform()` operation.
    transform: func(self: canvas-transform-handle, a: f64, b: f64, c: f64, d: f64, e: f64, f: f64);

    /// `getTransform()` operation.
    get-transform: func(self: canvas-transform-handle) -> u64;

    /// `setTransform()` operation.
    set-transform: func(self: canvas-transform-handle, a: f64, b: f64, c: f64, d: f64, e: f64, f: f64);

    /// `resetTransform()` operation.
    reset-transform: func(self: canvas-transform-handle);
}

interface canvas-compositing {
    /// Opaque handle to a host-side `CanvasCompositing` instance.
    type canvas-compositing-handle = u64;

    /// `globalAlpha` attribute — getter.
    get-global-alpha: func(self: canvas-compositing-handle) -> f64;
    /// `globalAlpha` attribute — setter.
    set-global-alpha: func(self: canvas-compositing-handle, value: f64);

    /// `globalCompositeOperation` attribute — getter.
    get-global-composite-operation: func(self: canvas-compositing-handle) -> string;
    /// `globalCompositeOperation` attribute — setter.
    set-global-composite-operation: func(self: canvas-compositing-handle, value: string);
}

interface canvas-image-smoothing {
    /// Opaque handle to a host-side `CanvasImageSmoothing` instance.
    type canvas-image-smoothing-handle = u64;

    /// `imageSmoothingEnabled` attribute — getter.
    get-image-smoothing-enabled: func(self: canvas-image-smoothing-handle) -> bool;
    /// `imageSmoothingEnabled` attribute — setter.
    set-image-smoothing-enabled: func(self: canvas-image-smoothing-handle, value: bool);

    /// `imageSmoothingQuality` attribute — getter.
    get-image-smoothing-quality: func(self: canvas-image-smoothing-handle) -> u64;
    /// `imageSmoothingQuality` attribute — setter.
    set-image-smoothing-quality: func(self: canvas-image-smoothing-handle, value: u64);
}

interface canvas-fill-stroke-styles {
    /// Opaque handle to a host-side `CanvasFillStrokeStyles` instance.
    type canvas-fill-stroke-styles-handle = u64;

    /// `strokeStyle` attribute — getter.
    get-stroke-style: func(self: canvas-fill-stroke-styles-handle) -> string;
    /// `strokeStyle` attribute — setter.
    set-stroke-style: func(self: canvas-fill-stroke-styles-handle, value: string);

    /// `fillStyle` attribute — getter.
    get-fill-style: func(self: canvas-fill-stroke-styles-handle) -> string;
    /// `fillStyle` attribute — setter.
    set-fill-style: func(self: canvas-fill-stroke-styles-handle, value: string);

    /// `createLinearGradient()` operation.
    create-linear-gradient: func(self: canvas-fill-stroke-styles-handle, x0: f64, y0: f64, x1: f64, y1: f64) -> u64;

    /// `createRadialGradient()` operation.
    create-radial-gradient: func(self: canvas-fill-stroke-styles-handle, x0: f64, y0: f64, r0: f64, x1: f64, y1: f64, r1: f64) -> u64;

    /// `createConicGradient()` operation.
    create-conic-gradient: func(self: canvas-fill-stroke-styles-handle, start-angle: f64, x: f64, y: f64) -> u64;

    /// `createPattern()` operation.
    create-pattern: func(self: canvas-fill-stroke-styles-handle, image: u64, repetition: string) -> option<u64>;
}

interface canvas-shadow-styles {
    /// Opaque handle to a host-side `CanvasShadowStyles` instance.
    type canvas-shadow-styles-handle = u64;

    /// `shadowOffsetX` attribute — getter.
    get-shadow-offset-x: func(self: canvas-shadow-styles-handle) -> f64;
    /// `shadowOffsetX` attribute — setter.
    set-shadow-offset-x: func(self: canvas-shadow-styles-handle, value: f64);

    /// `shadowOffsetY` attribute — getter.
    get-shadow-offset-y: func(self: canvas-shadow-styles-handle) -> f64;
    /// `shadowOffsetY` attribute — setter.
    set-shadow-offset-y: func(self: canvas-shadow-styles-handle, value: f64);

    /// `shadowBlur` attribute — getter.
    get-shadow-blur: func(self: canvas-shadow-styles-handle) -> f64;
    /// `shadowBlur` attribute — setter.
    set-shadow-blur: func(self: canvas-shadow-styles-handle, value: f64);

    /// `shadowColor` attribute — getter.
    get-shadow-color: func(self: canvas-shadow-styles-handle) -> string;
    /// `shadowColor` attribute — setter.
    set-shadow-color: func(self: canvas-shadow-styles-handle, value: string);
}

interface canvas-filters {
    /// Opaque handle to a host-side `CanvasFilters` instance.
    type canvas-filters-handle = u64;

    /// `filter` attribute — getter.
    get-filter: func(self: canvas-filters-handle) -> string;
    /// `filter` attribute — setter.
    set-filter: func(self: canvas-filters-handle, value: string);
}

interface canvas-rect {
    /// Opaque handle to a host-side `CanvasRect` instance.
    type canvas-rect-handle = u64;

    /// `clearRect()` operation.
    clear-rect: func(self: canvas-rect-handle, x: f64, y: f64, w: f64, h: f64);

    /// `fillRect()` operation.
    fill-rect: func(self: canvas-rect-handle, x: f64, y: f64, w: f64, h: f64);

    /// `strokeRect()` operation.
    stroke-rect: func(self: canvas-rect-handle, x: f64, y: f64, w: f64, h: f64);
}

interface canvas-draw-path {
    /// Opaque handle to a host-side `CanvasDrawPath` instance.
    type canvas-draw-path-handle = u64;

    /// `beginPath()` operation.
    begin-path: func(self: canvas-draw-path-handle);

    /// `fill()` operation.
    fill: func(self: canvas-draw-path-handle, fill-rule: option<u64>);

    /// `stroke()` operation.
    stroke: func(self: canvas-draw-path-handle);

    /// `clip()` operation.
    clip: func(self: canvas-draw-path-handle, fill-rule: option<u64>);

    /// `isPointInPath()` operation.
    is-point-in-path: func(self: canvas-draw-path-handle, x: f64, y: f64, fill-rule: option<u64>) -> bool;

    /// `isPointInStroke()` operation.
    is-point-in-stroke: func(self: canvas-draw-path-handle, x: f64, y: f64) -> bool;
}

interface canvas-user-interface {
    /// Opaque handle to a host-side `CanvasUserInterface` instance.
    type canvas-user-interface-handle = u64;

    /// `drawFocusIfNeeded()` operation.
    draw-focus-if-needed: func(self: canvas-user-interface-handle, element: u64);
}

interface canvas-text {
    /// Opaque handle to a host-side `CanvasText` instance.
    type canvas-text-handle = u64;

    /// `fillText()` operation.
    fill-text: func(self: canvas-text-handle, text: string, x: f64, y: f64, max-width: option<f64>);

    /// `strokeText()` operation.
    stroke-text: func(self: canvas-text-handle, text: string, x: f64, y: f64, max-width: option<f64>);

    /// `measureText()` operation.
    measure-text: func(self: canvas-text-handle, text: string) -> u64;
}

interface canvas-draw-image {
    /// Opaque handle to a host-side `CanvasDrawImage` instance.
    type canvas-draw-image-handle = u64;

    /// `drawImage()` operation.
    draw-image: func(self: canvas-draw-image-handle, image: u64, dx: f64, dy: f64);
}

interface canvas-image-data {
    /// Opaque handle to a host-side `CanvasImageData` instance.
    type canvas-image-data-handle = u64;

    /// `createImageData()` operation.
    create-image-data: func(self: canvas-image-data-handle, sw: s32, sh: s32, settings: option<u64>) -> u64;

    /// `getImageData()` operation.
    get-image-data: func(self: canvas-image-data-handle, sx: s32, sy: s32, sw: s32, sh: s32, settings: option<u64>) -> u64;

    /// `putImageData()` operation.
    put-image-data: func(self: canvas-image-data-handle, image-data: u64, dx: s32, dy: s32);
}

interface canvas-path-drawing-styles {
    /// Opaque handle to a host-side `CanvasPathDrawingStyles` instance.
    type canvas-path-drawing-styles-handle = u64;

    /// `lineWidth` attribute — getter.
    get-line-width: func(self: canvas-path-drawing-styles-handle) -> f64;
    /// `lineWidth` attribute — setter.
    set-line-width: func(self: canvas-path-drawing-styles-handle, value: f64);

    /// `lineCap` attribute — getter.
    get-line-cap: func(self: canvas-path-drawing-styles-handle) -> u64;
    /// `lineCap` attribute — setter.
    set-line-cap: func(self: canvas-path-drawing-styles-handle, value: u64);

    /// `lineJoin` attribute — getter.
    get-line-join: func(self: canvas-path-drawing-styles-handle) -> u64;
    /// `lineJoin` attribute — setter.
    set-line-join: func(self: canvas-path-drawing-styles-handle, value: u64);

    /// `miterLimit` attribute — getter.
    get-miter-limit: func(self: canvas-path-drawing-styles-handle) -> f64;
    /// `miterLimit` attribute — setter.
    set-miter-limit: func(self: canvas-path-drawing-styles-handle, value: f64);

    /// `setLineDash()` operation.
    set-line-dash: func(self: canvas-path-drawing-styles-handle, segments: list<f64>);

    /// `getLineDash()` operation.
    get-line-dash: func(self: canvas-path-drawing-styles-handle) -> list<f64>;

    /// `lineDashOffset` attribute — getter.
    get-line-dash-offset: func(self: canvas-path-drawing-styles-handle) -> f64;
    /// `lineDashOffset` attribute — setter.
    set-line-dash-offset: func(self: canvas-path-drawing-styles-handle, value: f64);
}

interface canvas-text-drawing-styles {
    /// Opaque handle to a host-side `CanvasTextDrawingStyles` instance.
    type canvas-text-drawing-styles-handle = u64;

    /// `lang` attribute — getter.
    get-lang: func(self: canvas-text-drawing-styles-handle) -> string;
    /// `lang` attribute — setter.
    set-lang: func(self: canvas-text-drawing-styles-handle, value: string);

    /// `font` attribute — getter.
    get-font: func(self: canvas-text-drawing-styles-handle) -> string;
    /// `font` attribute — setter.
    set-font: func(self: canvas-text-drawing-styles-handle, value: string);

    /// `textAlign` attribute — getter.
    get-text-align: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `textAlign` attribute — setter.
    set-text-align: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `textBaseline` attribute — getter.
    get-text-baseline: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `textBaseline` attribute — setter.
    set-text-baseline: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `direction` attribute — getter.
    get-direction: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `direction` attribute — setter.
    set-direction: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `letterSpacing` attribute — getter.
    get-letter-spacing: func(self: canvas-text-drawing-styles-handle) -> string;
    /// `letterSpacing` attribute — setter.
    set-letter-spacing: func(self: canvas-text-drawing-styles-handle, value: string);

    /// `fontKerning` attribute — getter.
    get-font-kerning: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `fontKerning` attribute — setter.
    set-font-kerning: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `fontStretch` attribute — getter.
    get-font-stretch: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `fontStretch` attribute — setter.
    set-font-stretch: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `fontVariantCaps` attribute — getter.
    get-font-variant-caps: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `fontVariantCaps` attribute — setter.
    set-font-variant-caps: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `textRendering` attribute — getter.
    get-text-rendering: func(self: canvas-text-drawing-styles-handle) -> u64;
    /// `textRendering` attribute — setter.
    set-text-rendering: func(self: canvas-text-drawing-styles-handle, value: u64);

    /// `wordSpacing` attribute — getter.
    get-word-spacing: func(self: canvas-text-drawing-styles-handle) -> string;
    /// `wordSpacing` attribute — setter.
    set-word-spacing: func(self: canvas-text-drawing-styles-handle, value: string);
}

interface canvas-path {
    /// Opaque handle to a host-side `CanvasPath` instance.
    type canvas-path-handle = u64;

    /// `closePath()` operation.
    close-path: func(self: canvas-path-handle);

    /// `moveTo()` operation.
    move-to: func(self: canvas-path-handle, x: f64, y: f64);

    /// `lineTo()` operation.
    line-to: func(self: canvas-path-handle, x: f64, y: f64);

    /// `quadraticCurveTo()` operation.
    quadratic-curve-to: func(self: canvas-path-handle, cpx: f64, cpy: f64, x: f64, y: f64);

    /// `bezierCurveTo()` operation.
    bezier-curve-to: func(self: canvas-path-handle, cp1x: f64, cp1y: f64, cp2x: f64, cp2y: f64, x: f64, y: f64);

    /// `arcTo()` operation.
    arc-to: func(self: canvas-path-handle, x1: f64, y1: f64, x2: f64, y2: f64, radius: f64);

    /// `rect()` operation.
    rect: func(self: canvas-path-handle, x: f64, y: f64, w: f64, h: f64);

    /// `roundRect()` operation.
    round-rect: func(self: canvas-path-handle, x: f64, y: f64, w: f64, h: f64, radii: option<f64>);

    /// `arc()` operation.
    arc: func(self: canvas-path-handle, x: f64, y: f64, radius: f64, start-angle: f64, end-angle: f64, counterclockwise: option<bool>);

    /// `ellipse()` operation.
    ellipse: func(self: canvas-path-handle, x: f64, y: f64, radius-x: f64, radius-y: f64, rotation: f64, start-angle: f64, end-angle: f64, counterclockwise: option<bool>);
}

interface canvas-gradient {
    /// Opaque handle to a host-side `CanvasGradient` instance.
    type canvas-gradient-handle = u64;

    /// `addColorStop()` operation.
    add-color-stop: func(self: canvas-gradient-handle, offset: f64, color: string);
}

interface canvas-pattern {
    /// Opaque handle to a host-side `CanvasPattern` instance.
    type canvas-pattern-handle = u64;

    /// `setTransform()` operation.
    set-transform: func(self: canvas-pattern-handle, transform: option<u64>);
}

interface text-metrics {
    /// Opaque handle to a host-side `TextMetrics` instance.
    type text-metrics-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: text-metrics-handle) -> f64;

    /// `actualBoundingBoxLeft` attribute — getter.
    get-actual-bounding-box-left: func(self: text-metrics-handle) -> f64;

    /// `actualBoundingBoxRight` attribute — getter.
    get-actual-bounding-box-right: func(self: text-metrics-handle) -> f64;

    /// `fontBoundingBoxAscent` attribute — getter.
    get-font-bounding-box-ascent: func(self: text-metrics-handle) -> f64;

    /// `fontBoundingBoxDescent` attribute — getter.
    get-font-bounding-box-descent: func(self: text-metrics-handle) -> f64;

    /// `actualBoundingBoxAscent` attribute — getter.
    get-actual-bounding-box-ascent: func(self: text-metrics-handle) -> f64;

    /// `actualBoundingBoxDescent` attribute — getter.
    get-actual-bounding-box-descent: func(self: text-metrics-handle) -> f64;

    /// `emHeightAscent` attribute — getter.
    get-em-height-ascent: func(self: text-metrics-handle) -> f64;

    /// `emHeightDescent` attribute — getter.
    get-em-height-descent: func(self: text-metrics-handle) -> f64;

    /// `hangingBaseline` attribute — getter.
    get-hanging-baseline: func(self: text-metrics-handle) -> f64;

    /// `alphabeticBaseline` attribute — getter.
    get-alphabetic-baseline: func(self: text-metrics-handle) -> f64;

    /// `ideographicBaseline` attribute — getter.
    get-ideographic-baseline: func(self: text-metrics-handle) -> f64;
}

interface path2-d {
    /// Opaque handle to a host-side `Path2D` instance.
    type path2-d-handle = u64;

    /// `addPath()` operation.
    add-path: func(self: path2-d-handle, path: u64, transform: option<u64>);
}

interface image-bitmap-rendering-context {
    /// Opaque handle to a host-side `ImageBitmapRenderingContext` instance.
    type image-bitmap-rendering-context-handle = u64;

    /// `canvas` attribute — getter.
    get-canvas: func(self: image-bitmap-rendering-context-handle) -> u64;

    /// `transferFromImageBitmap()` operation.
    transfer-from-image-bitmap: func(self: image-bitmap-rendering-context-handle, bitmap: option<u64>);
}

interface offscreen-canvas {
    /// Opaque handle to a host-side `OffscreenCanvas` instance.
    type offscreen-canvas-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: offscreen-canvas-handle) -> u64;
    /// `width` attribute — setter.
    set-width: func(self: offscreen-canvas-handle, value: u64);

    /// `height` attribute — getter.
    get-height: func(self: offscreen-canvas-handle) -> u64;
    /// `height` attribute — setter.
    set-height: func(self: offscreen-canvas-handle, value: u64);

    /// `getContext()` operation.
    get-context: func(self: offscreen-canvas-handle, context-id: u64, options: option<string>) -> option<u64>;

    /// `transferToImageBitmap()` operation.
    transfer-to-image-bitmap: func(self: offscreen-canvas-handle) -> u64;

    /// `convertToBlob()` operation.
    convert-to-blob: func(self: offscreen-canvas-handle, options: option<u64>) -> u64;

    /// `oncontextlost` attribute — getter.
    get-oncontextlost: func(self: offscreen-canvas-handle) -> u64;
    /// `oncontextlost` attribute — setter.
    set-oncontextlost: func(self: offscreen-canvas-handle, value: u64);

    /// `oncontextrestored` attribute — getter.
    get-oncontextrestored: func(self: offscreen-canvas-handle) -> u64;
    /// `oncontextrestored` attribute — setter.
    set-oncontextrestored: func(self: offscreen-canvas-handle, value: u64);
}

interface offscreen-canvas-rendering-context2-d {
    /// Opaque handle to a host-side `OffscreenCanvasRenderingContext2D` instance.
    type offscreen-canvas-rendering-context2-d-handle = u64;

    /// `canvas` attribute — getter.
    get-canvas: func(self: offscreen-canvas-rendering-context2-d-handle) -> u64;
}

interface custom-element-registry {
    /// Opaque handle to a host-side `CustomElementRegistry` instance.
    type custom-element-registry-handle = u64;

    /// `define()` operation.
    define: func(self: custom-element-registry-handle, name: string, %constructor: u64, options: option<u64>);

    /// `getName()` operation.
    get-name: func(self: custom-element-registry-handle, %constructor: u64) -> option<string>;

    /// `whenDefined()` operation.
    when-defined: func(self: custom-element-registry-handle, name: string) -> u64;

    /// `upgrade()` operation.
    upgrade: func(self: custom-element-registry-handle, root: u64);

    /// `initialize()` operation.
    initialize: func(self: custom-element-registry-handle, root: u64);
}

interface element-internals {
    /// Opaque handle to a host-side `ElementInternals` instance.
    type element-internals-handle = u64;

    /// `shadowRoot` attribute — getter.
    get-shadow-root: func(self: element-internals-handle) -> option<u64>;

    /// `setFormValue()` operation.
    set-form-value: func(self: element-internals-handle, value: option<string>, state: option<string>);

    /// `form` attribute — getter.
    get-form: func(self: element-internals-handle) -> option<u64>;

    /// `setValidity()` operation.
    set-validity: func(self: element-internals-handle, %flags: option<u64>, message: option<string>, anchor: option<u64>);

    /// `willValidate` attribute — getter.
    get-will-validate: func(self: element-internals-handle) -> bool;

    /// `validity` attribute — getter.
    get-validity: func(self: element-internals-handle) -> u64;

    /// `validationMessage` attribute — getter.
    get-validation-message: func(self: element-internals-handle) -> string;

    /// `checkValidity()` operation.
    check-validity: func(self: element-internals-handle) -> bool;

    /// `reportValidity()` operation.
    report-validity: func(self: element-internals-handle) -> bool;

    /// `labels` attribute — getter.
    get-labels: func(self: element-internals-handle) -> u64;

    /// `states` attribute — getter.
    get-states: func(self: element-internals-handle) -> u64;
}

interface visibility-state-entry {
    /// Opaque handle to a host-side `VisibilityStateEntry` instance.
    type visibility-state-entry-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: visibility-state-entry-handle) -> string;

    /// `entryType` attribute — getter.
    get-entry-type: func(self: visibility-state-entry-handle) -> string;

    /// `startTime` attribute — getter.
    get-start-time: func(self: visibility-state-entry-handle) -> f64;

    /// `duration` attribute — getter.
    get-duration: func(self: visibility-state-entry-handle) -> u32;
}

interface user-activation {
    /// Opaque handle to a host-side `UserActivation` instance.
    type user-activation-handle = u64;

    /// `hasBeenActive` attribute — getter.
    get-has-been-active: func(self: user-activation-handle) -> bool;

    /// `isActive` attribute — getter.
    get-is-active: func(self: user-activation-handle) -> bool;
}

interface toggle-event {
    /// Opaque handle to a host-side `ToggleEvent` instance.
    type toggle-event-handle = u64;

    /// `oldState` attribute — getter.
    get-old-state: func(self: toggle-event-handle) -> string;

    /// `newState` attribute — getter.
    get-new-state: func(self: toggle-event-handle) -> string;

    /// `source` attribute — getter.
    get-source: func(self: toggle-event-handle) -> option<u64>;
}

interface command-event {
    /// Opaque handle to a host-side `CommandEvent` instance.
    type command-event-handle = u64;

    /// `source` attribute — getter.
    get-source: func(self: command-event-handle) -> option<u64>;

    /// `command` attribute — getter.
    get-command: func(self: command-event-handle) -> string;
}

interface element-content-editable {
    /// Opaque handle to a host-side `ElementContentEditable` instance.
    type element-content-editable-handle = u64;

    /// `contentEditable` attribute — getter.
    get-content-editable: func(self: element-content-editable-handle) -> string;
    /// `contentEditable` attribute — setter.
    set-content-editable: func(self: element-content-editable-handle, value: string);

    /// `enterKeyHint` attribute — getter.
    get-enter-key-hint: func(self: element-content-editable-handle) -> string;
    /// `enterKeyHint` attribute — setter.
    set-enter-key-hint: func(self: element-content-editable-handle, value: string);

    /// `isContentEditable` attribute — getter.
    get-is-content-editable: func(self: element-content-editable-handle) -> bool;

    /// `inputMode` attribute — getter.
    get-input-mode: func(self: element-content-editable-handle) -> string;
    /// `inputMode` attribute — setter.
    set-input-mode: func(self: element-content-editable-handle, value: string);
}

interface close-watcher {
    /// Opaque handle to a host-side `CloseWatcher` instance.
    type close-watcher-handle = u64;

    /// `requestClose()` operation.
    request-close: func(self: close-watcher-handle);

    /// `close()` operation.
    close: func(self: close-watcher-handle);

    /// `destroy()` operation.
    destroy: func(self: close-watcher-handle);

    /// `oncancel` attribute — getter.
    get-oncancel: func(self: close-watcher-handle) -> u64;
    /// `oncancel` attribute — setter.
    set-oncancel: func(self: close-watcher-handle, value: u64);

    /// `onclose` attribute — getter.
    get-onclose: func(self: close-watcher-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: close-watcher-handle, value: u64);
}

interface data-transfer {
    /// Opaque handle to a host-side `DataTransfer` instance.
    type data-transfer-handle = u64;

    /// `dropEffect` attribute — getter.
    get-drop-effect: func(self: data-transfer-handle) -> string;
    /// `dropEffect` attribute — setter.
    set-drop-effect: func(self: data-transfer-handle, value: string);

    /// `effectAllowed` attribute — getter.
    get-effect-allowed: func(self: data-transfer-handle) -> string;
    /// `effectAllowed` attribute — setter.
    set-effect-allowed: func(self: data-transfer-handle, value: string);

    /// `items` attribute — getter.
    get-items: func(self: data-transfer-handle) -> u64;

    /// `setDragImage()` operation.
    set-drag-image: func(self: data-transfer-handle, image: u64, x: s32, y: s32);

    /// `types` attribute — getter.
    get-types: func(self: data-transfer-handle) -> list<string>;

    /// `getData()` operation.
    get-data: func(self: data-transfer-handle, format: string) -> string;

    /// `setData()` operation.
    set-data: func(self: data-transfer-handle, format: string, data: string);

    /// `clearData()` operation.
    clear-data: func(self: data-transfer-handle, format: option<string>);

    /// `files` attribute — getter.
    get-files: func(self: data-transfer-handle) -> u64;
}

interface data-transfer-item-list {
    /// Opaque handle to a host-side `DataTransferItemList` instance.
    type data-transfer-item-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: data-transfer-item-list-handle) -> u32;

    /// `DataTransferItem()` operation.
    data-transfer-item: func(self: data-transfer-item-list-handle, index: u32);

    /// `add()` operation.
    add: func(self: data-transfer-item-list-handle, data: string, %type: string) -> option<u64>;

    /// `remove()` operation.
    remove: func(self: data-transfer-item-list-handle, index: u32);

    /// `clear()` operation.
    clear: func(self: data-transfer-item-list-handle);
}

interface data-transfer-item {
    /// Opaque handle to a host-side `DataTransferItem` instance.
    type data-transfer-item-handle = u64;

    /// `kind` attribute — getter.
    get-kind: func(self: data-transfer-item-handle) -> string;

    /// `type` attribute — getter.
    get-type: func(self: data-transfer-item-handle) -> string;

    /// `getAsString()` operation.
    get-as-string: func(self: data-transfer-item-handle, callback: option<u64>);

    /// `getAsFile()` operation.
    get-as-file: func(self: data-transfer-item-handle) -> option<u64>;
}

interface drag-event {
    /// Opaque handle to a host-side `DragEvent` instance.
    type drag-event-handle = u64;

    /// `dataTransfer` attribute — getter.
    get-data-transfer: func(self: drag-event-handle) -> option<u64>;
}

interface popover-target-attributes {
    /// Opaque handle to a host-side `PopoverTargetAttributes` instance.
    type popover-target-attributes-handle = u64;

    /// `popoverTargetElement` attribute — getter.
    get-popover-target-element: func(self: popover-target-attributes-handle) -> option<u64>;
    /// `popoverTargetElement` attribute — setter.
    set-popover-target-element: func(self: popover-target-attributes-handle, value: option<u64>);

    /// `popoverTargetAction` attribute — getter.
    get-popover-target-action: func(self: popover-target-attributes-handle) -> string;
    /// `popoverTargetAction` attribute — setter.
    set-popover-target-action: func(self: popover-target-attributes-handle, value: string);
}

interface origin {
    /// Opaque handle to a host-side `Origin` instance.
    type origin-handle = u64;

    /// `from()` operation.
    %from: func(value: string) -> u64;

    /// `opaque` attribute — getter.
    get-opaque: func(self: origin-handle) -> bool;

    /// `isSameOrigin()` operation.
    is-same-origin: func(self: origin-handle, other: u64) -> bool;

    /// `isSameSite()` operation.
    is-same-site: func(self: origin-handle, other: u64) -> bool;
}

interface bar-prop {
    /// Opaque handle to a host-side `BarProp` instance.
    type bar-prop-handle = u64;

    /// `visible` attribute — getter.
    get-visible: func(self: bar-prop-handle) -> bool;
}

interface location {
    /// Opaque handle to a host-side `Location` instance.
    type location-handle = u64;

    /// `href` attribute — getter.
    get-href: func() -> string;
    /// `href` attribute — setter.
    set-href: func(value: string);

    /// `origin` attribute — getter.
    get-origin: func() -> string;

    /// `protocol` attribute — getter.
    get-protocol: func() -> string;
    /// `protocol` attribute — setter.
    set-protocol: func(value: string);

    /// `host` attribute — getter.
    get-host: func() -> string;
    /// `host` attribute — setter.
    set-host: func(value: string);

    /// `hostname` attribute — getter.
    get-hostname: func() -> string;
    /// `hostname` attribute — setter.
    set-hostname: func(value: string);

    /// `port` attribute — getter.
    get-port: func() -> string;
    /// `port` attribute — setter.
    set-port: func(value: string);

    /// `pathname` attribute — getter.
    get-pathname: func() -> string;
    /// `pathname` attribute — setter.
    set-pathname: func(value: string);

    /// `search` attribute — getter.
    get-search: func() -> string;
    /// `search` attribute — setter.
    set-search: func(value: string);

    /// `hash` attribute — getter.
    get-hash: func() -> string;
    /// `hash` attribute — setter.
    set-hash: func(value: string);

    /// `assign()` operation.
    assign: func(url: string);

    /// `replace()` operation.
    replace: func(url: string);

    /// `reload()` operation.
    reload: func();

    /// `ancestorOrigins` attribute — getter.
    get-ancestor-origins: func() -> list<string>;
}

interface history {
    /// Opaque handle to a host-side `History` instance.
    type history-handle = u64;

    /// `length` attribute — getter.
    get-length: func() -> u32;

    /// `scrollRestoration` attribute — getter.
    get-scroll-restoration: func() -> u64;
    /// `scrollRestoration` attribute — setter.
    set-scroll-restoration: func(value: u64);

    /// `state` attribute — getter.
    get-state: func() -> string;

    /// `go()` operation.
    go: func(delta: option<s32>);

    /// `back()` operation.
    back: func();

    /// `forward()` operation.
    forward: func();

    /// `pushState()` operation.
    push-state: func(data: string, unused: string, url: option<string>);

    /// `replaceState()` operation.
    replace-state: func(data: string, unused: string, url: option<string>);
}

interface navigation {
    /// Opaque handle to a host-side `Navigation` instance.
    type navigation-handle = u64;

    /// `entries()` operation.
    entries: func(self: navigation-handle) -> list<u64>;

    /// `currentEntry` attribute — getter.
    get-current-entry: func(self: navigation-handle) -> option<u64>;

    /// `updateCurrentEntry()` operation.
    update-current-entry: func(self: navigation-handle, options: u64);

    /// `transition` attribute — getter.
    get-transition: func(self: navigation-handle) -> option<u64>;

    /// `activation` attribute — getter.
    get-activation: func(self: navigation-handle) -> option<u64>;

    /// `canGoBack` attribute — getter.
    get-can-go-back: func(self: navigation-handle) -> bool;

    /// `canGoForward` attribute — getter.
    get-can-go-forward: func(self: navigation-handle) -> bool;

    /// `navigate()` operation.
    navigate: func(self: navigation-handle, url: string, options: option<u64>) -> u64;

    /// `reload()` operation.
    reload: func(self: navigation-handle, options: option<u64>) -> u64;

    /// `traverseTo()` operation.
    traverse-to: func(self: navigation-handle, key: string, options: option<u64>) -> u64;

    /// `back()` operation.
    back: func(self: navigation-handle, options: option<u64>) -> u64;

    /// `forward()` operation.
    forward: func(self: navigation-handle, options: option<u64>) -> u64;

    /// `onnavigate` attribute — getter.
    get-onnavigate: func(self: navigation-handle) -> u64;
    /// `onnavigate` attribute — setter.
    set-onnavigate: func(self: navigation-handle, value: u64);

    /// `onnavigatesuccess` attribute — getter.
    get-onnavigatesuccess: func(self: navigation-handle) -> u64;
    /// `onnavigatesuccess` attribute — setter.
    set-onnavigatesuccess: func(self: navigation-handle, value: u64);

    /// `onnavigateerror` attribute — getter.
    get-onnavigateerror: func(self: navigation-handle) -> u64;
    /// `onnavigateerror` attribute — setter.
    set-onnavigateerror: func(self: navigation-handle, value: u64);

    /// `oncurrententrychange` attribute — getter.
    get-oncurrententrychange: func(self: navigation-handle) -> u64;
    /// `oncurrententrychange` attribute — setter.
    set-oncurrententrychange: func(self: navigation-handle, value: u64);
}

interface navigation-history-entry {
    /// Opaque handle to a host-side `NavigationHistoryEntry` instance.
    type navigation-history-entry-handle = u64;

    /// `url` attribute — getter.
    get-url: func(self: navigation-history-entry-handle) -> option<string>;

    /// `key` attribute — getter.
    get-key: func(self: navigation-history-entry-handle) -> string;

    /// `id` attribute — getter.
    get-id: func(self: navigation-history-entry-handle) -> string;

    /// `index` attribute — getter.
    get-index: func(self: navigation-history-entry-handle) -> s64;

    /// `sameDocument` attribute — getter.
    get-same-document: func(self: navigation-history-entry-handle) -> bool;

    /// `getState()` operation.
    get-state: func(self: navigation-history-entry-handle) -> string;

    /// `ondispose` attribute — getter.
    get-ondispose: func(self: navigation-history-entry-handle) -> u64;
    /// `ondispose` attribute — setter.
    set-ondispose: func(self: navigation-history-entry-handle, value: u64);
}

interface navigation-transition {
    /// Opaque handle to a host-side `NavigationTransition` instance.
    type navigation-transition-handle = u64;

    /// `navigationType` attribute — getter.
    get-navigation-type: func(self: navigation-transition-handle) -> u64;

    /// `from` attribute — getter.
    get-from: func(self: navigation-transition-handle) -> u64;

    /// `to` attribute — getter.
    get-to: func(self: navigation-transition-handle) -> u64;

    /// `committed` attribute — getter.
    get-committed: func(self: navigation-transition-handle) -> u64;

    /// `finished` attribute — getter.
    get-finished: func(self: navigation-transition-handle) -> u64;
}

interface navigation-activation {
    /// Opaque handle to a host-side `NavigationActivation` instance.
    type navigation-activation-handle = u64;

    /// `from` attribute — getter.
    get-from: func(self: navigation-activation-handle) -> option<u64>;

    /// `entry` attribute — getter.
    get-entry: func(self: navigation-activation-handle) -> u64;

    /// `navigationType` attribute — getter.
    get-navigation-type: func(self: navigation-activation-handle) -> u64;
}

interface navigate-event {
    /// Opaque handle to a host-side `NavigateEvent` instance.
    type navigate-event-handle = u64;

    /// `navigationType` attribute — getter.
    get-navigation-type: func(self: navigate-event-handle) -> u64;

    /// `destination` attribute — getter.
    get-destination: func(self: navigate-event-handle) -> u64;

    /// `canIntercept` attribute — getter.
    get-can-intercept: func(self: navigate-event-handle) -> bool;

    /// `userInitiated` attribute — getter.
    get-user-initiated: func(self: navigate-event-handle) -> bool;

    /// `hashChange` attribute — getter.
    get-hash-change: func(self: navigate-event-handle) -> bool;

    /// `signal` attribute — getter.
    get-signal: func(self: navigate-event-handle) -> u64;

    /// `formData` attribute — getter.
    get-form-data: func(self: navigate-event-handle) -> option<u64>;

    /// `downloadRequest` attribute — getter.
    get-download-request: func(self: navigate-event-handle) -> option<string>;

    /// `info` attribute — getter.
    get-info: func(self: navigate-event-handle) -> string;

    /// `hasUAVisualTransition` attribute — getter.
    get-has-ua-visual-transition: func(self: navigate-event-handle) -> bool;

    /// `sourceElement` attribute — getter.
    get-source-element: func(self: navigate-event-handle) -> option<u64>;

    /// `intercept()` operation.
    intercept: func(self: navigate-event-handle, options: option<u64>);

    /// `scroll()` operation.
    scroll: func(self: navigate-event-handle);
}

interface navigation-precommit-controller {
    /// Opaque handle to a host-side `NavigationPrecommitController` instance.
    type navigation-precommit-controller-handle = u64;

    /// `redirect()` operation.
    redirect: func(self: navigation-precommit-controller-handle, url: string, options: option<u64>);

    /// `addHandler()` operation.
    add-handler: func(self: navigation-precommit-controller-handle, handler: u64);
}

interface navigation-destination {
    /// Opaque handle to a host-side `NavigationDestination` instance.
    type navigation-destination-handle = u64;

    /// `url` attribute — getter.
    get-url: func(self: navigation-destination-handle) -> string;

    /// `key` attribute — getter.
    get-key: func(self: navigation-destination-handle) -> string;

    /// `id` attribute — getter.
    get-id: func(self: navigation-destination-handle) -> string;

    /// `index` attribute — getter.
    get-index: func(self: navigation-destination-handle) -> s64;

    /// `sameDocument` attribute — getter.
    get-same-document: func(self: navigation-destination-handle) -> bool;

    /// `getState()` operation.
    get-state: func(self: navigation-destination-handle) -> string;
}

interface navigation-current-entry-change-event {
    /// Opaque handle to a host-side `NavigationCurrentEntryChangeEvent` instance.
    type navigation-current-entry-change-event-handle = u64;

    /// `navigationType` attribute — getter.
    get-navigation-type: func(self: navigation-current-entry-change-event-handle) -> option<u64>;

    /// `from` attribute — getter.
    get-from: func(self: navigation-current-entry-change-event-handle) -> u64;
}

interface pop-state-event {
    /// Opaque handle to a host-side `PopStateEvent` instance.
    type pop-state-event-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: pop-state-event-handle) -> string;

    /// `hasUAVisualTransition` attribute — getter.
    get-has-ua-visual-transition: func(self: pop-state-event-handle) -> bool;
}

interface hash-change-event {
    /// Opaque handle to a host-side `HashChangeEvent` instance.
    type hash-change-event-handle = u64;

    /// `oldURL` attribute — getter.
    get-old-url: func(self: hash-change-event-handle) -> string;

    /// `newURL` attribute — getter.
    get-new-url: func(self: hash-change-event-handle) -> string;
}

interface page-swap-event {
    /// Opaque handle to a host-side `PageSwapEvent` instance.
    type page-swap-event-handle = u64;

    /// `activation` attribute — getter.
    get-activation: func(self: page-swap-event-handle) -> option<u64>;

    /// `viewTransition` attribute — getter.
    get-view-transition: func(self: page-swap-event-handle) -> option<u64>;
}

interface page-reveal-event {
    /// Opaque handle to a host-side `PageRevealEvent` instance.
    type page-reveal-event-handle = u64;

    /// `viewTransition` attribute — getter.
    get-view-transition: func(self: page-reveal-event-handle) -> option<u64>;
}

interface page-transition-event {
    /// Opaque handle to a host-side `PageTransitionEvent` instance.
    type page-transition-event-handle = u64;

    /// `persisted` attribute — getter.
    get-persisted: func(self: page-transition-event-handle) -> bool;
}

interface before-unload-event {
    /// Opaque handle to a host-side `BeforeUnloadEvent` instance.
    type before-unload-event-handle = u64;

    /// `returnValue` attribute — getter.
    get-return-value: func(self: before-unload-event-handle) -> string;
    /// `returnValue` attribute — setter.
    set-return-value: func(self: before-unload-event-handle, value: string);
}

interface not-restored-reason-details {
    /// Opaque handle to a host-side `NotRestoredReasonDetails` instance.
    type not-restored-reason-details-handle = u64;

    /// `reason` attribute — getter.
    get-reason: func(self: not-restored-reason-details-handle) -> string;

    /// `toJSON()` operation.
    to-json: func(self: not-restored-reason-details-handle) -> u64;
}

interface not-restored-reasons {
    /// Opaque handle to a host-side `NotRestoredReasons` instance.
    type not-restored-reasons-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: not-restored-reasons-handle) -> option<string>;

    /// `id` attribute — getter.
    get-id: func(self: not-restored-reasons-handle) -> option<string>;

    /// `name` attribute — getter.
    get-name: func(self: not-restored-reasons-handle) -> option<string>;

    /// `url` attribute — getter.
    get-url: func(self: not-restored-reasons-handle) -> option<string>;

    /// `reasons` attribute — getter.
    get-reasons: func(self: not-restored-reasons-handle) -> option<list<u64>>;

    /// `children` attribute — getter.
    get-children: func(self: not-restored-reasons-handle) -> option<list<u64>>;

    /// `toJSON()` operation.
    to-json: func(self: not-restored-reasons-handle) -> u64;
}

interface error-event {
    /// Opaque handle to a host-side `ErrorEvent` instance.
    type error-event-handle = u64;

    /// `message` attribute — getter.
    get-message: func(self: error-event-handle) -> string;

    /// `filename` attribute — getter.
    get-filename: func(self: error-event-handle) -> string;

    /// `lineno` attribute — getter.
    get-lineno: func(self: error-event-handle) -> u32;

    /// `colno` attribute — getter.
    get-colno: func(self: error-event-handle) -> u32;

    /// `error` attribute — getter.
    get-error: func(self: error-event-handle) -> string;
}

interface promise-rejection-event {
    /// Opaque handle to a host-side `PromiseRejectionEvent` instance.
    type promise-rejection-event-handle = u64;

    /// `promise` attribute — getter.
    get-promise: func(self: promise-rejection-event-handle) -> u64;

    /// `reason` attribute — getter.
    get-reason: func(self: promise-rejection-event-handle) -> string;
}

interface dom-parser {
    /// Opaque handle to a host-side `DOMParser` instance.
    type dom-parser-handle = u64;

    /// `parseFromString()` operation.
    parse-from-string: func(self: dom-parser-handle, %string: string, %type: u64) -> u64;
}

interface xml-serializer {
    /// Opaque handle to a host-side `XMLSerializer` instance.
    type xml-serializer-handle = u64;

    /// `serializeToString()` operation.
    serialize-to-string: func(self: xml-serializer-handle, root: u64) -> string;
}

interface navigator-id {
    /// Opaque handle to a host-side `NavigatorID` instance.
    type navigator-id-handle = u64;

    /// `appCodeName` attribute — getter.
    get-app-code-name: func(self: navigator-id-handle) -> string;

    /// `appName` attribute — getter.
    get-app-name: func(self: navigator-id-handle) -> string;

    /// `appVersion` attribute — getter.
    get-app-version: func(self: navigator-id-handle) -> string;

    /// `platform` attribute — getter.
    get-platform: func(self: navigator-id-handle) -> string;

    /// `product` attribute — getter.
    get-product: func(self: navigator-id-handle) -> string;

    /// `productSub` attribute — getter.
    get-product-sub: func(self: navigator-id-handle) -> string;

    /// `userAgent` attribute — getter.
    get-user-agent: func(self: navigator-id-handle) -> string;

    /// `vendor` attribute — getter.
    get-vendor: func(self: navigator-id-handle) -> string;

    /// `vendorSub` attribute — getter.
    get-vendor-sub: func(self: navigator-id-handle) -> string;

    /// `taintEnabled()` operation.
    taint-enabled: func(self: navigator-id-handle) -> bool;

    /// `oscpu` attribute — getter.
    get-oscpu: func(self: navigator-id-handle) -> string;
}

interface navigator-language {
    /// Opaque handle to a host-side `NavigatorLanguage` instance.
    type navigator-language-handle = u64;

    /// `language` attribute — getter.
    get-language: func(self: navigator-language-handle) -> string;

    /// `languages` attribute — getter.
    get-languages: func(self: navigator-language-handle) -> list<string>;
}

interface navigator-on-line {
    /// Opaque handle to a host-side `NavigatorOnLine` instance.
    type navigator-on-line-handle = u64;

    /// `onLine` attribute — getter.
    get-on-line: func(self: navigator-on-line-handle) -> bool;
}

interface navigator-content-utils {
    /// Opaque handle to a host-side `NavigatorContentUtils` instance.
    type navigator-content-utils-handle = u64;

    /// `registerProtocolHandler()` operation.
    register-protocol-handler: func(self: navigator-content-utils-handle, scheme: string, url: string);

    /// `unregisterProtocolHandler()` operation.
    unregister-protocol-handler: func(self: navigator-content-utils-handle, scheme: string, url: string);
}

interface navigator-cookies {
    /// Opaque handle to a host-side `NavigatorCookies` instance.
    type navigator-cookies-handle = u64;

    /// `cookieEnabled` attribute — getter.
    get-cookie-enabled: func(self: navigator-cookies-handle) -> bool;
}

interface navigator-plugins {
    /// Opaque handle to a host-side `NavigatorPlugins` instance.
    type navigator-plugins-handle = u64;

    /// `plugins` attribute — getter.
    get-plugins: func(self: navigator-plugins-handle) -> u64;

    /// `mimeTypes` attribute — getter.
    get-mime-types: func(self: navigator-plugins-handle) -> u64;

    /// `javaEnabled()` operation.
    java-enabled: func(self: navigator-plugins-handle) -> bool;

    /// `pdfViewerEnabled` attribute — getter.
    get-pdf-viewer-enabled: func(self: navigator-plugins-handle) -> bool;
}

interface plugin-array {
    /// Opaque handle to a host-side `PluginArray` instance.
    type plugin-array-handle = u64;

    /// `refresh()` operation.
    refresh: func(self: plugin-array-handle);

    /// `length` attribute — getter.
    get-length: func(self: plugin-array-handle) -> u32;

    /// `item()` operation.
    item: func(self: plugin-array-handle, index: u32) -> option<u64>;

    /// `namedItem()` operation.
    named-item: func(self: plugin-array-handle, name: string) -> option<u64>;
}

interface mime-type-array {
    /// Opaque handle to a host-side `MimeTypeArray` instance.
    type mime-type-array-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: mime-type-array-handle) -> u32;

    /// `item()` operation.
    item: func(self: mime-type-array-handle, index: u32) -> option<u64>;

    /// `namedItem()` operation.
    named-item: func(self: mime-type-array-handle, name: string) -> option<u64>;
}

interface plugin {
    /// Opaque handle to a host-side `Plugin` instance.
    type plugin-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: plugin-handle) -> string;

    /// `description` attribute — getter.
    get-description: func(self: plugin-handle) -> string;

    /// `filename` attribute — getter.
    get-filename: func(self: plugin-handle) -> string;

    /// `length` attribute — getter.
    get-length: func(self: plugin-handle) -> u32;

    /// `item()` operation.
    item: func(self: plugin-handle, index: u32) -> option<u64>;

    /// `namedItem()` operation.
    named-item: func(self: plugin-handle, name: string) -> option<u64>;
}

interface mime-type {
    /// Opaque handle to a host-side `MimeType` instance.
    type mime-type-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: mime-type-handle) -> string;

    /// `description` attribute — getter.
    get-description: func(self: mime-type-handle) -> string;

    /// `suffixes` attribute — getter.
    get-suffixes: func(self: mime-type-handle) -> string;

    /// `enabledPlugin` attribute — getter.
    get-enabled-plugin: func(self: mime-type-handle) -> u64;
}

interface image-data {
    /// Opaque handle to a host-side `ImageData` instance.
    type image-data-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: image-data-handle) -> u32;

    /// `height` attribute — getter.
    get-height: func(self: image-data-handle) -> u32;

    /// `data` attribute — getter.
    get-data: func(self: image-data-handle) -> u64;

    /// `pixelFormat` attribute — getter.
    get-pixel-format: func(self: image-data-handle) -> u64;

    /// `colorSpace` attribute — getter.
    get-color-space: func(self: image-data-handle) -> u64;
}

interface image-bitmap {
    /// Opaque handle to a host-side `ImageBitmap` instance.
    type image-bitmap-handle = u64;

    /// `width` attribute — getter.
    get-width: func(self: image-bitmap-handle) -> u32;

    /// `height` attribute — getter.
    get-height: func(self: image-bitmap-handle) -> u32;

    /// `close()` operation.
    close: func(self: image-bitmap-handle);
}

interface animation-frame-provider {
    /// Opaque handle to a host-side `AnimationFrameProvider` instance.
    type animation-frame-provider-handle = u64;

    /// `requestAnimationFrame()` operation.
    request-animation-frame: func(self: animation-frame-provider-handle, callback: u64) -> u32;

    /// `cancelAnimationFrame()` operation.
    cancel-animation-frame: func(self: animation-frame-provider-handle, handle: u32);
}

interface message-event {
    /// Opaque handle to a host-side `MessageEvent` instance.
    type message-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: message-event-handle) -> string;

    /// `origin` attribute — getter.
    get-origin: func(self: message-event-handle) -> string;

    /// `lastEventId` attribute — getter.
    get-last-event-id: func(self: message-event-handle) -> string;

    /// `source` attribute — getter.
    get-source: func(self: message-event-handle) -> option<u64>;

    /// `ports` attribute — getter.
    get-ports: func(self: message-event-handle) -> list<u64>;

    /// `initMessageEvent()` operation.
    init-message-event: func(self: message-event-handle, %type: string, bubbles: option<bool>, cancelable: option<bool>, data: option<string>, origin: option<string>, last-event-id: option<string>, source: option<u64>, ports: option<list<u64>>);
}

interface event-source {
    /// Opaque handle to a host-side `EventSource` instance.
    type event-source-handle = u64;

    /// `url` attribute — getter.
    get-url: func(self: event-source-handle) -> string;

    /// `withCredentials` attribute — getter.
    get-with-credentials: func(self: event-source-handle) -> bool;

    /// `readyState` attribute — getter.
    get-ready-state: func(self: event-source-handle) -> u16;

    /// `onopen` attribute — getter.
    get-onopen: func(self: event-source-handle) -> u64;
    /// `onopen` attribute — setter.
    set-onopen: func(self: event-source-handle, value: u64);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: event-source-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: event-source-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: event-source-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: event-source-handle, value: u64);

    /// `close()` operation.
    close: func(self: event-source-handle);
}

interface message-channel {
    /// Opaque handle to a host-side `MessageChannel` instance.
    type message-channel-handle = u64;

    /// `port1` attribute — getter.
    get-port1: func(self: message-channel-handle) -> u64;

    /// `port2` attribute — getter.
    get-port2: func(self: message-channel-handle) -> u64;
}

interface message-event-target {
    /// Opaque handle to a host-side `MessageEventTarget` instance.
    type message-event-target-handle = u64;

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: message-event-target-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: message-event-target-handle, value: u64);

    /// `onmessageerror` attribute — getter.
    get-onmessageerror: func(self: message-event-target-handle) -> u64;
    /// `onmessageerror` attribute — setter.
    set-onmessageerror: func(self: message-event-target-handle, value: u64);
}

interface message-port {
    /// Opaque handle to a host-side `MessagePort` instance.
    type message-port-handle = u64;

    /// `postMessage()` operation.
    post-message: func(self: message-port-handle, message: string, transfer: list<u64>);

    /// `start()` operation.
    start: func(self: message-port-handle);

    /// `close()` operation.
    close: func(self: message-port-handle);

    /// `onclose` attribute — getter.
    get-onclose: func(self: message-port-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: message-port-handle, value: u64);
}

interface broadcast-channel {
    /// Opaque handle to a host-side `BroadcastChannel` instance.
    type broadcast-channel-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: broadcast-channel-handle) -> string;

    /// `postMessage()` operation.
    post-message: func(self: broadcast-channel-handle, message: string);

    /// `close()` operation.
    close: func(self: broadcast-channel-handle);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: broadcast-channel-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: broadcast-channel-handle, value: u64);

    /// `onmessageerror` attribute — getter.
    get-onmessageerror: func(self: broadcast-channel-handle) -> u64;
    /// `onmessageerror` attribute — setter.
    set-onmessageerror: func(self: broadcast-channel-handle, value: u64);
}

interface worker-global-scope {
    /// Opaque handle to a host-side `WorkerGlobalScope` instance.
    type worker-global-scope-handle = u64;

    /// `self` attribute — getter.
    get-self: func(self: worker-global-scope-handle) -> u64;

    /// `location` attribute — getter.
    get-location: func(self: worker-global-scope-handle) -> u64;

    /// `navigator` attribute — getter.
    get-navigator: func(self: worker-global-scope-handle) -> u64;

    /// `importScripts()` operation.
    import-scripts: func(self: worker-global-scope-handle, urls: list<string>);

    /// `onerror` attribute — getter.
    get-onerror: func(self: worker-global-scope-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: worker-global-scope-handle, value: u64);

    /// `onlanguagechange` attribute — getter.
    get-onlanguagechange: func(self: worker-global-scope-handle) -> u64;
    /// `onlanguagechange` attribute — setter.
    set-onlanguagechange: func(self: worker-global-scope-handle, value: u64);

    /// `onoffline` attribute — getter.
    get-onoffline: func(self: worker-global-scope-handle) -> u64;
    /// `onoffline` attribute — setter.
    set-onoffline: func(self: worker-global-scope-handle, value: u64);

    /// `ononline` attribute — getter.
    get-ononline: func(self: worker-global-scope-handle) -> u64;
    /// `ononline` attribute — setter.
    set-ononline: func(self: worker-global-scope-handle, value: u64);

    /// `onrejectionhandled` attribute — getter.
    get-onrejectionhandled: func(self: worker-global-scope-handle) -> u64;
    /// `onrejectionhandled` attribute — setter.
    set-onrejectionhandled: func(self: worker-global-scope-handle, value: u64);

    /// `onunhandledrejection` attribute — getter.
    get-onunhandledrejection: func(self: worker-global-scope-handle) -> u64;
    /// `onunhandledrejection` attribute — setter.
    set-onunhandledrejection: func(self: worker-global-scope-handle, value: u64);
}

interface dedicated-worker-global-scope {
    /// Opaque handle to a host-side `DedicatedWorkerGlobalScope` instance.
    type dedicated-worker-global-scope-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: dedicated-worker-global-scope-handle) -> string;

    /// `postMessage()` operation.
    post-message: func(self: dedicated-worker-global-scope-handle, message: string, transfer: list<u64>);

    /// `close()` operation.
    close: func(self: dedicated-worker-global-scope-handle);
}

interface shared-worker-global-scope {
    /// Opaque handle to a host-side `SharedWorkerGlobalScope` instance.
    type shared-worker-global-scope-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: shared-worker-global-scope-handle) -> string;

    /// `close()` operation.
    close: func(self: shared-worker-global-scope-handle);

    /// `onconnect` attribute — getter.
    get-onconnect: func(self: shared-worker-global-scope-handle) -> u64;
    /// `onconnect` attribute — setter.
    set-onconnect: func(self: shared-worker-global-scope-handle, value: u64);
}

interface abstract-worker {
    /// Opaque handle to a host-side `AbstractWorker` instance.
    type abstract-worker-handle = u64;

    /// `onerror` attribute — getter.
    get-onerror: func(self: abstract-worker-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: abstract-worker-handle, value: u64);
}

interface worker {
    /// Opaque handle to a host-side `Worker` instance.
    type worker-handle = u64;

    /// `terminate()` operation.
    terminate: func(self: worker-handle);

    /// `postMessage()` operation.
    post-message: func(self: worker-handle, message: string, transfer: list<u64>);
}

interface shared-worker {
    /// Opaque handle to a host-side `SharedWorker` instance.
    type shared-worker-handle = u64;

    /// `port` attribute — getter.
    get-port: func(self: shared-worker-handle) -> u64;
}

interface navigator-concurrent-hardware {
    /// Opaque handle to a host-side `NavigatorConcurrentHardware` instance.
    type navigator-concurrent-hardware-handle = u64;

    /// `hardwareConcurrency` attribute — getter.
    get-hardware-concurrency: func(self: navigator-concurrent-hardware-handle) -> u64;
}

interface worker-navigator {
    /// Opaque handle to a host-side `WorkerNavigator` instance.
    type worker-navigator-handle = u64;

    /// `mediaCapabilities` attribute — getter.
    get-media-capabilities: func(self: worker-navigator-handle) -> u64;

    /// `permissions` attribute — getter.
    get-permissions: func(self: worker-navigator-handle) -> u64;

    /// `serviceWorker` attribute — getter.
    get-service-worker: func(self: worker-navigator-handle) -> u64;
}

interface worker-location {
    /// Opaque handle to a host-side `WorkerLocation` instance.
    type worker-location-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: worker-location-handle) -> u64;
    /// `href` attribute — setter.
    set-href: func(self: worker-location-handle, value: u64);

    /// `origin` attribute — getter.
    get-origin: func(self: worker-location-handle) -> string;

    /// `protocol` attribute — getter.
    get-protocol: func(self: worker-location-handle) -> string;

    /// `host` attribute — getter.
    get-host: func(self: worker-location-handle) -> string;

    /// `hostname` attribute — getter.
    get-hostname: func(self: worker-location-handle) -> string;

    /// `port` attribute — getter.
    get-port: func(self: worker-location-handle) -> string;

    /// `pathname` attribute — getter.
    get-pathname: func(self: worker-location-handle) -> string;

    /// `search` attribute — getter.
    get-search: func(self: worker-location-handle) -> string;

    /// `hash` attribute — getter.
    get-hash: func(self: worker-location-handle) -> string;
}

interface worklet {
    /// Opaque handle to a host-side `Worklet` instance.
    type worklet-handle = u64;

    /// `addModule()` operation.
    add-module: func(self: worklet-handle, module-url: string, options: option<u64>) -> u64;
}

interface storage {
    /// Opaque handle to a host-side `Storage` instance.
    type storage-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: storage-handle) -> u32;

    /// `key()` operation.
    key: func(self: storage-handle, index: u32) -> option<string>;

    /// `getItem()` operation.
    get-item: func(self: storage-handle, key: string) -> option<string>;

    /// `setItem()` operation.
    set-item: func(self: storage-handle, key: string, value: string);

    /// `removeItem()` operation.
    remove-item: func(self: storage-handle, key: string);

    /// `clear()` operation.
    clear: func(self: storage-handle);
}

interface window-session-storage {
    /// Opaque handle to a host-side `WindowSessionStorage` instance.
    type window-session-storage-handle = u64;

    /// `sessionStorage` attribute — getter.
    get-session-storage: func(self: window-session-storage-handle) -> u64;
}

interface window-local-storage {
    /// Opaque handle to a host-side `WindowLocalStorage` instance.
    type window-local-storage-handle = u64;

    /// `localStorage` attribute — getter.
    get-local-storage: func(self: window-local-storage-handle) -> u64;
}

interface storage-event {
    /// Opaque handle to a host-side `StorageEvent` instance.
    type storage-event-handle = u64;

    /// `key` attribute — getter.
    get-key: func(self: storage-event-handle) -> option<string>;

    /// `oldValue` attribute — getter.
    get-old-value: func(self: storage-event-handle) -> option<string>;

    /// `newValue` attribute — getter.
    get-new-value: func(self: storage-event-handle) -> option<string>;

    /// `url` attribute — getter.
    get-url: func(self: storage-event-handle) -> string;

    /// `storageArea` attribute — getter.
    get-storage-area: func(self: storage-event-handle) -> option<u64>;

    /// `initStorageEvent()` operation.
    init-storage-event: func(self: storage-event-handle, %type: string, bubbles: option<bool>, cancelable: option<bool>, key: option<string>, old-value: option<string>, new-value: option<string>, url: option<string>, storage-area: option<u64>);
}

interface html-marquee-element {
    /// Opaque handle to a host-side `HTMLMarqueeElement` instance.
    type html-marquee-element-handle = u64;

    /// `behavior` attribute — getter.
    get-behavior: func(self: html-marquee-element-handle) -> string;
    /// `behavior` attribute — setter.
    set-behavior: func(self: html-marquee-element-handle, value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func(self: html-marquee-element-handle) -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(self: html-marquee-element-handle, value: string);

    /// `direction` attribute — getter.
    get-direction: func(self: html-marquee-element-handle) -> string;
    /// `direction` attribute — setter.
    set-direction: func(self: html-marquee-element-handle, value: string);

    /// `height` attribute — getter.
    get-height: func(self: html-marquee-element-handle) -> string;
    /// `height` attribute — setter.
    set-height: func(self: html-marquee-element-handle, value: string);

    /// `hspace` attribute — getter.
    get-hspace: func(self: html-marquee-element-handle) -> u32;
    /// `hspace` attribute — setter.
    set-hspace: func(self: html-marquee-element-handle, value: u32);

    /// `loop` attribute — getter.
    get-loop: func(self: html-marquee-element-handle) -> s32;
    /// `loop` attribute — setter.
    set-loop: func(self: html-marquee-element-handle, value: s32);

    /// `scrollAmount` attribute — getter.
    get-scroll-amount: func(self: html-marquee-element-handle) -> u32;
    /// `scrollAmount` attribute — setter.
    set-scroll-amount: func(self: html-marquee-element-handle, value: u32);

    /// `scrollDelay` attribute — getter.
    get-scroll-delay: func(self: html-marquee-element-handle) -> u32;
    /// `scrollDelay` attribute — setter.
    set-scroll-delay: func(self: html-marquee-element-handle, value: u32);

    /// `trueSpeed` attribute — getter.
    get-true-speed: func(self: html-marquee-element-handle) -> bool;
    /// `trueSpeed` attribute — setter.
    set-true-speed: func(self: html-marquee-element-handle, value: bool);

    /// `vspace` attribute — getter.
    get-vspace: func(self: html-marquee-element-handle) -> u32;
    /// `vspace` attribute — setter.
    set-vspace: func(self: html-marquee-element-handle, value: u32);

    /// `width` attribute — getter.
    get-width: func(self: html-marquee-element-handle) -> string;
    /// `width` attribute — setter.
    set-width: func(self: html-marquee-element-handle, value: string);

    /// `start()` operation.
    start: func(self: html-marquee-element-handle);

    /// `stop()` operation.
    stop: func(self: html-marquee-element-handle);
}

interface html-frame-set-element {
    /// Opaque handle to a host-side `HTMLFrameSetElement` instance.
    type html-frame-set-element-handle = u64;

    /// `cols` attribute — getter.
    get-cols: func(self: html-frame-set-element-handle) -> string;
    /// `cols` attribute — setter.
    set-cols: func(self: html-frame-set-element-handle, value: string);

    /// `rows` attribute — getter.
    get-rows: func(self: html-frame-set-element-handle) -> string;
    /// `rows` attribute — setter.
    set-rows: func(self: html-frame-set-element-handle, value: string);
}

interface html-frame-element {
    /// Opaque handle to a host-side `HTMLFrameElement` instance.
    type html-frame-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-frame-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-frame-element-handle, value: string);

    /// `scrolling` attribute — getter.
    get-scrolling: func(self: html-frame-element-handle) -> string;
    /// `scrolling` attribute — setter.
    set-scrolling: func(self: html-frame-element-handle, value: string);

    /// `src` attribute — getter.
    get-src: func(self: html-frame-element-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: html-frame-element-handle, value: string);

    /// `frameBorder` attribute — getter.
    get-frame-border: func(self: html-frame-element-handle) -> string;
    /// `frameBorder` attribute — setter.
    set-frame-border: func(self: html-frame-element-handle, value: string);

    /// `longDesc` attribute — getter.
    get-long-desc: func(self: html-frame-element-handle) -> string;
    /// `longDesc` attribute — setter.
    set-long-desc: func(self: html-frame-element-handle, value: string);

    /// `noResize` attribute — getter.
    get-no-resize: func(self: html-frame-element-handle) -> bool;
    /// `noResize` attribute — setter.
    set-no-resize: func(self: html-frame-element-handle, value: bool);

    /// `contentDocument` attribute — getter.
    get-content-document: func(self: html-frame-element-handle) -> option<u64>;

    /// `contentWindow` attribute — getter.
    get-content-window: func(self: html-frame-element-handle) -> option<u64>;

    /// `marginHeight` attribute — getter.
    get-margin-height: func(self: html-frame-element-handle) -> string;
    /// `marginHeight` attribute — setter.
    set-margin-height: func(self: html-frame-element-handle, value: string);

    /// `marginWidth` attribute — getter.
    get-margin-width: func(self: html-frame-element-handle) -> string;
    /// `marginWidth` attribute — setter.
    set-margin-width: func(self: html-frame-element-handle, value: string);
}

interface html-directory-element {
    /// Opaque handle to a host-side `HTMLDirectoryElement` instance.
    type html-directory-element-handle = u64;

    /// `compact` attribute — getter.
    get-compact: func(self: html-directory-element-handle) -> bool;
    /// `compact` attribute — setter.
    set-compact: func(self: html-directory-element-handle, value: bool);
}

interface html-font-element {
    /// Opaque handle to a host-side `HTMLFontElement` instance.
    type html-font-element-handle = u64;

    /// `color` attribute — getter.
    get-color: func(self: html-font-element-handle) -> string;
    /// `color` attribute — setter.
    set-color: func(self: html-font-element-handle, value: string);

    /// `face` attribute — getter.
    get-face: func(self: html-font-element-handle) -> string;
    /// `face` attribute — setter.
    set-face: func(self: html-font-element-handle, value: string);

    /// `size` attribute — getter.
    get-size: func(self: html-font-element-handle) -> string;
    /// `size` attribute — setter.
    set-size: func(self: html-font-element-handle, value: string);
}

interface html-param-element {
    /// Opaque handle to a host-side `HTMLParamElement` instance.
    type html-param-element-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: html-param-element-handle) -> string;
    /// `name` attribute — setter.
    set-name: func(self: html-param-element-handle, value: string);

    /// `value` attribute — getter.
    get-value: func(self: html-param-element-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: html-param-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: html-param-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: html-param-element-handle, value: string);

    /// `valueType` attribute — getter.
    get-value-type: func(self: html-param-element-handle) -> string;
    /// `valueType` attribute — setter.
    set-value-type: func(self: html-param-element-handle, value: string);
}

interface external {
    /// Opaque handle to a host-side `External` instance.
    type external-handle = u64;

    /// `AddSearchProvider()` operation.
    add-search-provider: func(self: external-handle);

    /// `IsSearchProviderInstalled()` operation.
    is-search-provider-installed: func(self: external-handle);
}

interface idb-factory {
/// Opaque handle to a IDBFactory instance.
    type idb-factory-handle = u64;

    // open: skipped (unmappable return type)
    // delete-database: skipped (unmappable return type)
    // databases: skipped (unmappable return type)
    cmp: func(handle: idb-factory-handle, first: string, second: string) -> s16;
}

interface idb-database {
/// Opaque handle to a IDBDatabase instance.
    type idb-db-handle = u64;

    name: func(handle: idb-db-handle) -> string;
    version: func(handle: idb-db-handle) -> u64;
    // transaction: skipped (unmappable return type)
}

interface idb-transaction {
/// Opaque handle to a IDBTransaction instance.
    type idb-tx-handle = u64;

    // object-store: skipped (unmappable return type)
    commit: func(handle: idb-tx-handle);
    abort: func(handle: idb-tx-handle);
}

interface idb-object-store {
/// Opaque handle to a IDBObjectStore instance.
    type idb-store-handle = u64;

    get-name: func(handle: idb-store-handle) -> string;
    set-name: func(handle: idb-store-handle, value: string);
    key-path: func(handle: idb-store-handle) -> string;
    auto-increment: func(handle: idb-store-handle) -> bool;
    // put: skipped (unmappable return type)
    // add: skipped (unmappable return type)
    // delete: skipped (unmappable return type)
    // clear: skipped (unmappable return type)
    // get: skipped (unmappable return type)
    // get-key: skipped (unmappable return type)
    // get-all: skipped (unmappable return type)
    // get-all-keys: skipped (unmappable return type)
    // get-all-records: skipped (unmappable return type)
}

interface idb-index {
/// Opaque handle to a IDBIndex instance.
    type idb-index-handle = u64;

    get-name: func(handle: idb-index-handle) -> string;
    set-name: func(handle: idb-index-handle, value: string);
    key-path: func(handle: idb-index-handle) -> string;
    multi-entry: func(handle: idb-index-handle) -> bool;
    unique: func(handle: idb-index-handle) -> bool;
    // get: skipped (unmappable return type)
    // get-key: skipped (unmappable return type)
    // get-all: skipped (unmappable return type)
    // get-all-keys: skipped (unmappable return type)
    // get-all-records: skipped (unmappable return type)
}

interface idb-cursor {
/// Opaque handle to a IDBCursor instance.
    type idb-cursor-handle = u64;

    source: func(handle: idb-cursor-handle) -> string;
    key: func(handle: idb-cursor-handle) -> string;
    primary-key: func(handle: idb-cursor-handle) -> string;
    advance: func(handle: idb-cursor-handle, count: u32);
    continue: func(handle: idb-cursor-handle, key: string);
    continue-primary-key: func(handle: idb-cursor-handle, key: string, primary-key: string);
    // update: skipped (unmappable return type)
    // delete: skipped (unmappable return type)
}

interface idb-request {
/// Opaque handle to a IDBRequest instance.
    type idb-request-handle = u64;

    result-val: func(handle: idb-request-handle) -> string;
    source: func(handle: idb-request-handle) -> option<string>;
}

interface intersection-observer {
/// Opaque handle to a IntersectionObserver instance.
    type io-handle = u64;

}

interface media-capabilities {
    /// Opaque handle to a host-side `MediaCapabilities` instance.
    type media-capabilities-handle = u64;

    /// `decodingInfo()` operation.
    decoding-info: func(self: media-capabilities-handle, configuration: u64) -> u64;

    /// `encodingInfo()` operation.
    encoding-info: func(self: media-capabilities-handle, configuration: u64) -> u64;
}

interface media-stream {
    /// Opaque handle to a host-side `MediaStream` instance.
    type media-stream-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: media-stream-handle) -> string;

    /// `getAudioTracks()` operation.
    get-audio-tracks: func(self: media-stream-handle) -> list<u64>;

    /// `getVideoTracks()` operation.
    get-video-tracks: func(self: media-stream-handle) -> list<u64>;

    /// `getTracks()` operation.
    get-tracks: func(self: media-stream-handle) -> list<u64>;

    /// `getTrackById()` operation.
    get-track-by-id: func(self: media-stream-handle, track-id: string) -> option<u64>;

    /// `addTrack()` operation.
    add-track: func(self: media-stream-handle, track: u64);

    /// `removeTrack()` operation.
    remove-track: func(self: media-stream-handle, track: u64);

    /// `clone()` operation.
    clone: func(self: media-stream-handle) -> u64;

    /// `active` attribute — getter.
    get-active: func(self: media-stream-handle) -> bool;

    /// `onaddtrack` attribute — getter.
    get-onaddtrack: func(self: media-stream-handle) -> u64;
    /// `onaddtrack` attribute — setter.
    set-onaddtrack: func(self: media-stream-handle, value: u64);

    /// `onremovetrack` attribute — getter.
    get-onremovetrack: func(self: media-stream-handle) -> u64;
    /// `onremovetrack` attribute — setter.
    set-onremovetrack: func(self: media-stream-handle, value: u64);
}

interface media-stream-track {
    /// Opaque handle to a host-side `MediaStreamTrack` instance.
    type media-stream-track-handle = u64;

    /// `kind` attribute — getter.
    get-kind: func(self: media-stream-track-handle) -> string;

    /// `id` attribute — getter.
    get-id: func(self: media-stream-track-handle) -> string;

    /// `label` attribute — getter.
    get-label: func(self: media-stream-track-handle) -> string;

    /// `enabled` attribute — getter.
    get-enabled: func(self: media-stream-track-handle) -> bool;
    /// `enabled` attribute — setter.
    set-enabled: func(self: media-stream-track-handle, value: bool);

    /// `muted` attribute — getter.
    get-muted: func(self: media-stream-track-handle) -> bool;

    /// `onmute` attribute — getter.
    get-onmute: func(self: media-stream-track-handle) -> u64;
    /// `onmute` attribute — setter.
    set-onmute: func(self: media-stream-track-handle, value: u64);

    /// `onunmute` attribute — getter.
    get-onunmute: func(self: media-stream-track-handle) -> u64;
    /// `onunmute` attribute — setter.
    set-onunmute: func(self: media-stream-track-handle, value: u64);

    /// `readyState` attribute — getter.
    get-ready-state: func(self: media-stream-track-handle) -> u64;

    /// `onended` attribute — getter.
    get-onended: func(self: media-stream-track-handle) -> u64;
    /// `onended` attribute — setter.
    set-onended: func(self: media-stream-track-handle, value: u64);

    /// `clone()` operation.
    clone: func(self: media-stream-track-handle) -> u64;

    /// `stop()` operation.
    stop: func(self: media-stream-track-handle);

    /// `getCapabilities()` operation.
    get-capabilities: func(self: media-stream-track-handle) -> u64;

    /// `getConstraints()` operation.
    get-constraints: func(self: media-stream-track-handle) -> u64;

    /// `getSettings()` operation.
    get-settings: func(self: media-stream-track-handle) -> u64;

    /// `applyConstraints()` operation.
    apply-constraints: func(self: media-stream-track-handle, constraints: option<u64>) -> u64;
}

interface media-stream-track-event {
    /// Opaque handle to a host-side `MediaStreamTrackEvent` instance.
    type media-stream-track-event-handle = u64;

    /// `track` attribute — getter.
    get-track: func(self: media-stream-track-event-handle) -> u64;
}

interface overconstrained-error {
    /// Opaque handle to a host-side `OverconstrainedError` instance.
    type overconstrained-error-handle = u64;

    /// `constraint` attribute — getter.
    get-constraint: func(self: overconstrained-error-handle) -> string;
}

interface media-devices {
    /// Opaque handle to a host-side `MediaDevices` instance.
    type media-devices-handle = u64;

    /// `ondevicechange` attribute — getter.
    get-ondevicechange: func(self: media-devices-handle) -> u64;
    /// `ondevicechange` attribute — setter.
    set-ondevicechange: func(self: media-devices-handle, value: u64);

    /// `enumerateDevices()` operation.
    enumerate-devices: func(self: media-devices-handle) -> u64;

    /// `getSupportedConstraints()` operation.
    get-supported-constraints: func(self: media-devices-handle) -> u64;

    /// `getUserMedia()` operation.
    get-user-media: func(self: media-devices-handle, constraints: option<u64>) -> u64;

    /// `getDisplayMedia()` operation.
    get-display-media: func(self: media-devices-handle, options: option<u64>) -> u64;
}

interface media-device-info {
    /// Opaque handle to a host-side `MediaDeviceInfo` instance.
    type media-device-info-handle = u64;

    /// `deviceId` attribute — getter.
    get-device-id: func(self: media-device-info-handle) -> string;

    /// `kind` attribute — getter.
    get-kind: func(self: media-device-info-handle) -> u64;

    /// `label` attribute — getter.
    get-label: func(self: media-device-info-handle) -> string;

    /// `groupId` attribute — getter.
    get-group-id: func(self: media-device-info-handle) -> string;

    /// `toJSON()` operation.
    to-json: func(self: media-device-info-handle) -> u64;
}

interface input-device-info {
    /// Opaque handle to a host-side `InputDeviceInfo` instance.
    type input-device-info-handle = u64;

    /// `getCapabilities()` operation.
    get-capabilities: func(self: input-device-info-handle) -> u64;
}

interface device-change-event {
    /// Opaque handle to a host-side `DeviceChangeEvent` instance.
    type device-change-event-handle = u64;

    /// `devices` attribute — getter.
    get-devices: func(self: device-change-event-handle) -> list<u64>;

    /// `userInsertedDevices` attribute — getter.
    get-user-inserted-devices: func(self: device-change-event-handle) -> list<u64>;
}

interface media-session {
    /// Opaque handle to a host-side `MediaSession` instance.
    type media-session-handle = u64;

    /// `metadata` attribute — getter.
    get-metadata: func(self: media-session-handle) -> option<u64>;
    /// `metadata` attribute — setter.
    set-metadata: func(self: media-session-handle, value: option<u64>);

    /// `playbackState` attribute — getter.
    get-playback-state: func(self: media-session-handle) -> u64;
    /// `playbackState` attribute — setter.
    set-playback-state: func(self: media-session-handle, value: u64);

    /// `setActionHandler()` operation.
    set-action-handler: func(self: media-session-handle, action: u64, handler: option<u64>);

    /// `setPositionState()` operation.
    set-position-state: func(self: media-session-handle, state: option<u64>);

    /// `setMicrophoneActive()` operation.
    set-microphone-active: func(self: media-session-handle, active: bool) -> u64;

    /// `setCameraActive()` operation.
    set-camera-active: func(self: media-session-handle, active: bool) -> u64;

    /// `setScreenshareActive()` operation.
    set-screenshare-active: func(self: media-session-handle, active: bool) -> u64;
}

interface media-metadata {
    /// Opaque handle to a host-side `MediaMetadata` instance.
    type media-metadata-handle = u64;

    /// `title` attribute — getter.
    get-title: func(self: media-metadata-handle) -> string;
    /// `title` attribute — setter.
    set-title: func(self: media-metadata-handle, value: string);

    /// `artist` attribute — getter.
    get-artist: func(self: media-metadata-handle) -> string;
    /// `artist` attribute — setter.
    set-artist: func(self: media-metadata-handle, value: string);

    /// `album` attribute — getter.
    get-album: func(self: media-metadata-handle) -> string;
    /// `album` attribute — setter.
    set-album: func(self: media-metadata-handle, value: string);

    /// `artwork` attribute — getter.
    get-artwork: func(self: media-metadata-handle) -> list<u64>;
    /// `artwork` attribute — setter.
    set-artwork: func(self: media-metadata-handle, value: list<u64>);

    /// `chapterInfo` attribute — getter.
    get-chapter-info: func(self: media-metadata-handle) -> list<u64>;
}

interface chapter-information {
    /// Opaque handle to a host-side `ChapterInformation` instance.
    type chapter-information-handle = u64;

    /// `title` attribute — getter.
    get-title: func(self: chapter-information-handle) -> string;

    /// `startTime` attribute — getter.
    get-start-time: func(self: chapter-information-handle) -> f64;

    /// `artwork` attribute — getter.
    get-artwork: func(self: chapter-information-handle) -> list<u64>;
}

interface media-recorder {
    /// Opaque handle to a host-side `MediaRecorder` instance.
    type media-recorder-handle = u64;

    /// `stream` attribute — getter.
    get-stream: func(self: media-recorder-handle) -> u64;

    /// `mimeType` attribute — getter.
    get-mime-type: func(self: media-recorder-handle) -> string;

    /// `state` attribute — getter.
    get-state: func(self: media-recorder-handle) -> u64;

    /// `onstart` attribute — getter.
    get-onstart: func(self: media-recorder-handle) -> u64;
    /// `onstart` attribute — setter.
    set-onstart: func(self: media-recorder-handle, value: u64);

    /// `onstop` attribute — getter.
    get-onstop: func(self: media-recorder-handle) -> u64;
    /// `onstop` attribute — setter.
    set-onstop: func(self: media-recorder-handle, value: u64);

    /// `ondataavailable` attribute — getter.
    get-ondataavailable: func(self: media-recorder-handle) -> u64;
    /// `ondataavailable` attribute — setter.
    set-ondataavailable: func(self: media-recorder-handle, value: u64);

    /// `onpause` attribute — getter.
    get-onpause: func(self: media-recorder-handle) -> u64;
    /// `onpause` attribute — setter.
    set-onpause: func(self: media-recorder-handle, value: u64);

    /// `onresume` attribute — getter.
    get-onresume: func(self: media-recorder-handle) -> u64;
    /// `onresume` attribute — setter.
    set-onresume: func(self: media-recorder-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: media-recorder-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: media-recorder-handle, value: u64);

    /// `videoBitsPerSecond` attribute — getter.
    get-video-bits-per-second: func(self: media-recorder-handle) -> u32;

    /// `audioBitsPerSecond` attribute — getter.
    get-audio-bits-per-second: func(self: media-recorder-handle) -> u32;

    /// `audioBitrateMode` attribute — getter.
    get-audio-bitrate-mode: func(self: media-recorder-handle) -> u64;

    /// `start()` operation.
    start: func(self: media-recorder-handle, timeslice: option<u32>);

    /// `stop()` operation.
    stop: func(self: media-recorder-handle);

    /// `pause()` operation.
    pause: func(self: media-recorder-handle);

    /// `resume()` operation.
    resume: func(self: media-recorder-handle);

    /// `requestData()` operation.
    request-data: func(self: media-recorder-handle);

    /// `isTypeSupported()` operation.
    is-type-supported: func(%type: string) -> bool;
}

interface blob-event {
    /// Opaque handle to a host-side `BlobEvent` instance.
    type blob-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: blob-event-handle) -> u64;

    /// `timecode` attribute — getter.
    get-timecode: func(self: blob-event-handle) -> f64;
}

interface capture-controller {
    /// Opaque handle to a host-side `CaptureController` instance.
    type capture-controller-handle = u64;

    /// `setFocusBehavior()` operation.
    set-focus-behavior: func(self: capture-controller-handle, focus-behavior: u64);
}

interface speech-recognition {
    /// Opaque handle to a host-side `SpeechRecognition` instance.
    type speech-recognition-handle = u64;

    /// `grammars` attribute — getter.
    get-grammars: func(self: speech-recognition-handle) -> u64;
    /// `grammars` attribute — setter.
    set-grammars: func(self: speech-recognition-handle, value: u64);

    /// `lang` attribute — getter.
    get-lang: func(self: speech-recognition-handle) -> string;
    /// `lang` attribute — setter.
    set-lang: func(self: speech-recognition-handle, value: string);

    /// `continuous` attribute — getter.
    get-continuous: func(self: speech-recognition-handle) -> bool;
    /// `continuous` attribute — setter.
    set-continuous: func(self: speech-recognition-handle, value: bool);

    /// `interimResults` attribute — getter.
    get-interim-results: func(self: speech-recognition-handle) -> bool;
    /// `interimResults` attribute — setter.
    set-interim-results: func(self: speech-recognition-handle, value: bool);

    /// `maxAlternatives` attribute — getter.
    get-max-alternatives: func(self: speech-recognition-handle) -> u32;
    /// `maxAlternatives` attribute — setter.
    set-max-alternatives: func(self: speech-recognition-handle, value: u32);

    /// `processLocally` attribute — getter.
    get-process-locally: func(self: speech-recognition-handle) -> bool;
    /// `processLocally` attribute — setter.
    set-process-locally: func(self: speech-recognition-handle, value: bool);

    /// `phrases` attribute — getter.
    get-phrases: func(self: speech-recognition-handle) -> list<u64>;
    /// `phrases` attribute — setter.
    set-phrases: func(self: speech-recognition-handle, value: list<u64>);

    /// `start()` operation.
    start: func(self: speech-recognition-handle);

    /// `stop()` operation.
    stop: func(self: speech-recognition-handle);

    /// `abort()` operation.
    abort: func(self: speech-recognition-handle);

    /// `available()` operation.
    available: func(options: u64) -> u64;

    /// `install()` operation.
    install: func(options: u64) -> u64;

    /// `onaudiostart` attribute — getter.
    get-onaudiostart: func(self: speech-recognition-handle) -> u64;
    /// `onaudiostart` attribute — setter.
    set-onaudiostart: func(self: speech-recognition-handle, value: u64);

    /// `onsoundstart` attribute — getter.
    get-onsoundstart: func(self: speech-recognition-handle) -> u64;
    /// `onsoundstart` attribute — setter.
    set-onsoundstart: func(self: speech-recognition-handle, value: u64);

    /// `onspeechstart` attribute — getter.
    get-onspeechstart: func(self: speech-recognition-handle) -> u64;
    /// `onspeechstart` attribute — setter.
    set-onspeechstart: func(self: speech-recognition-handle, value: u64);

    /// `onspeechend` attribute — getter.
    get-onspeechend: func(self: speech-recognition-handle) -> u64;
    /// `onspeechend` attribute — setter.
    set-onspeechend: func(self: speech-recognition-handle, value: u64);

    /// `onsoundend` attribute — getter.
    get-onsoundend: func(self: speech-recognition-handle) -> u64;
    /// `onsoundend` attribute — setter.
    set-onsoundend: func(self: speech-recognition-handle, value: u64);

    /// `onaudioend` attribute — getter.
    get-onaudioend: func(self: speech-recognition-handle) -> u64;
    /// `onaudioend` attribute — setter.
    set-onaudioend: func(self: speech-recognition-handle, value: u64);

    /// `onresult` attribute — getter.
    get-onresult: func(self: speech-recognition-handle) -> u64;
    /// `onresult` attribute — setter.
    set-onresult: func(self: speech-recognition-handle, value: u64);

    /// `onnomatch` attribute — getter.
    get-onnomatch: func(self: speech-recognition-handle) -> u64;
    /// `onnomatch` attribute — setter.
    set-onnomatch: func(self: speech-recognition-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: speech-recognition-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: speech-recognition-handle, value: u64);

    /// `onstart` attribute — getter.
    get-onstart: func(self: speech-recognition-handle) -> u64;
    /// `onstart` attribute — setter.
    set-onstart: func(self: speech-recognition-handle, value: u64);

    /// `onend` attribute — getter.
    get-onend: func(self: speech-recognition-handle) -> u64;
    /// `onend` attribute — setter.
    set-onend: func(self: speech-recognition-handle, value: u64);
}

interface speech-recognition-error-event {
    /// Opaque handle to a host-side `SpeechRecognitionErrorEvent` instance.
    type speech-recognition-error-event-handle = u64;

    /// `error` attribute — getter.
    get-error: func(self: speech-recognition-error-event-handle) -> u64;

    /// `message` attribute — getter.
    get-message: func(self: speech-recognition-error-event-handle) -> string;
}

interface speech-recognition-alternative {
    /// Opaque handle to a host-side `SpeechRecognitionAlternative` instance.
    type speech-recognition-alternative-handle = u64;

    /// `transcript` attribute — getter.
    get-transcript: func(self: speech-recognition-alternative-handle) -> string;

    /// `confidence` attribute — getter.
    get-confidence: func(self: speech-recognition-alternative-handle) -> f32;
}

interface speech-recognition-result {
    /// Opaque handle to a host-side `SpeechRecognitionResult` instance.
    type speech-recognition-result-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: speech-recognition-result-handle) -> u32;

    /// `item()` operation.
    item: func(self: speech-recognition-result-handle, index: u32) -> u64;

    /// `isFinal` attribute — getter.
    get-is-final: func(self: speech-recognition-result-handle) -> bool;
}

interface speech-recognition-result-list {
    /// Opaque handle to a host-side `SpeechRecognitionResultList` instance.
    type speech-recognition-result-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: speech-recognition-result-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: speech-recognition-result-list-handle, index: u32) -> u64;
}

interface speech-recognition-event {
    /// Opaque handle to a host-side `SpeechRecognitionEvent` instance.
    type speech-recognition-event-handle = u64;

    /// `resultIndex` attribute — getter.
    get-result-index: func(self: speech-recognition-event-handle) -> u32;

    /// `results` attribute — getter.
    get-results: func(self: speech-recognition-event-handle) -> u64;
}

interface speech-grammar {
    /// Opaque handle to a host-side `SpeechGrammar` instance.
    type speech-grammar-handle = u64;

    /// `src` attribute — getter.
    get-src: func(self: speech-grammar-handle) -> string;
    /// `src` attribute — setter.
    set-src: func(self: speech-grammar-handle, value: string);

    /// `weight` attribute — getter.
    get-weight: func(self: speech-grammar-handle) -> f32;
    /// `weight` attribute — setter.
    set-weight: func(self: speech-grammar-handle, value: f32);
}

interface speech-grammar-list {
    /// Opaque handle to a host-side `SpeechGrammarList` instance.
    type speech-grammar-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: speech-grammar-list-handle) -> u32;

    /// `item()` operation.
    item: func(self: speech-grammar-list-handle, index: u32) -> u64;

    /// `addFromURI()` operation.
    add-from-uri: func(self: speech-grammar-list-handle, src: string, weight: option<f32>);

    /// `addFromString()` operation.
    add-from-string: func(self: speech-grammar-list-handle, %string: string, weight: option<f32>);
}

interface speech-recognition-phrase {
    /// Opaque handle to a host-side `SpeechRecognitionPhrase` instance.
    type speech-recognition-phrase-handle = u64;

    /// `phrase` attribute — getter.
    get-phrase: func(self: speech-recognition-phrase-handle) -> string;

    /// `boost` attribute — getter.
    get-boost: func(self: speech-recognition-phrase-handle) -> f32;
}

interface speech-synthesis {
    /// Opaque handle to a host-side `SpeechSynthesis` instance.
    type speech-synthesis-handle = u64;

    /// `pending` attribute — getter.
    get-pending: func(self: speech-synthesis-handle) -> bool;

    /// `speaking` attribute — getter.
    get-speaking: func(self: speech-synthesis-handle) -> bool;

    /// `paused` attribute — getter.
    get-paused: func(self: speech-synthesis-handle) -> bool;

    /// `onvoiceschanged` attribute — getter.
    get-onvoiceschanged: func(self: speech-synthesis-handle) -> u64;
    /// `onvoiceschanged` attribute — setter.
    set-onvoiceschanged: func(self: speech-synthesis-handle, value: u64);

    /// `speak()` operation.
    speak: func(self: speech-synthesis-handle, utterance: u64);

    /// `cancel()` operation.
    cancel: func(self: speech-synthesis-handle);

    /// `pause()` operation.
    pause: func(self: speech-synthesis-handle);

    /// `resume()` operation.
    resume: func(self: speech-synthesis-handle);

    /// `getVoices()` operation.
    get-voices: func(self: speech-synthesis-handle) -> list<u64>;
}

interface speech-synthesis-utterance {
    /// Opaque handle to a host-side `SpeechSynthesisUtterance` instance.
    type speech-synthesis-utterance-handle = u64;

    /// `text` attribute — getter.
    get-text: func(self: speech-synthesis-utterance-handle) -> string;
    /// `text` attribute — setter.
    set-text: func(self: speech-synthesis-utterance-handle, value: string);

    /// `lang` attribute — getter.
    get-lang: func(self: speech-synthesis-utterance-handle) -> string;
    /// `lang` attribute — setter.
    set-lang: func(self: speech-synthesis-utterance-handle, value: string);

    /// `voice` attribute — getter.
    get-voice: func(self: speech-synthesis-utterance-handle) -> option<u64>;
    /// `voice` attribute — setter.
    set-voice: func(self: speech-synthesis-utterance-handle, value: option<u64>);

    /// `volume` attribute — getter.
    get-volume: func(self: speech-synthesis-utterance-handle) -> f32;
    /// `volume` attribute — setter.
    set-volume: func(self: speech-synthesis-utterance-handle, value: f32);

    /// `rate` attribute — getter.
    get-rate: func(self: speech-synthesis-utterance-handle) -> f32;
    /// `rate` attribute — setter.
    set-rate: func(self: speech-synthesis-utterance-handle, value: f32);

    /// `pitch` attribute — getter.
    get-pitch: func(self: speech-synthesis-utterance-handle) -> f32;
    /// `pitch` attribute — setter.
    set-pitch: func(self: speech-synthesis-utterance-handle, value: f32);

    /// `onstart` attribute — getter.
    get-onstart: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onstart` attribute — setter.
    set-onstart: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onend` attribute — getter.
    get-onend: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onend` attribute — setter.
    set-onend: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onpause` attribute — getter.
    get-onpause: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onpause` attribute — setter.
    set-onpause: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onresume` attribute — getter.
    get-onresume: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onresume` attribute — setter.
    set-onresume: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onmark` attribute — getter.
    get-onmark: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onmark` attribute — setter.
    set-onmark: func(self: speech-synthesis-utterance-handle, value: u64);

    /// `onboundary` attribute — getter.
    get-onboundary: func(self: speech-synthesis-utterance-handle) -> u64;
    /// `onboundary` attribute — setter.
    set-onboundary: func(self: speech-synthesis-utterance-handle, value: u64);
}

interface speech-synthesis-event {
    /// Opaque handle to a host-side `SpeechSynthesisEvent` instance.
    type speech-synthesis-event-handle = u64;

    /// `utterance` attribute — getter.
    get-utterance: func(self: speech-synthesis-event-handle) -> u64;

    /// `charIndex` attribute — getter.
    get-char-index: func(self: speech-synthesis-event-handle) -> u32;

    /// `charLength` attribute — getter.
    get-char-length: func(self: speech-synthesis-event-handle) -> u32;

    /// `elapsedTime` attribute — getter.
    get-elapsed-time: func(self: speech-synthesis-event-handle) -> f32;

    /// `name` attribute — getter.
    get-name: func(self: speech-synthesis-event-handle) -> string;
}

interface speech-synthesis-error-event {
    /// Opaque handle to a host-side `SpeechSynthesisErrorEvent` instance.
    type speech-synthesis-error-event-handle = u64;

    /// `error` attribute — getter.
    get-error: func(self: speech-synthesis-error-event-handle) -> u64;
}

interface speech-synthesis-voice {
    /// Opaque handle to a host-side `SpeechSynthesisVoice` instance.
    type speech-synthesis-voice-handle = u64;

    /// `voiceURI` attribute — getter.
    get-voice-uri: func(self: speech-synthesis-voice-handle) -> string;

    /// `name` attribute — getter.
    get-name: func(self: speech-synthesis-voice-handle) -> string;

    /// `lang` attribute — getter.
    get-lang: func(self: speech-synthesis-voice-handle) -> string;

    /// `localService` attribute — getter.
    get-local-service: func(self: speech-synthesis-voice-handle) -> bool;

    /// `default` attribute — getter.
    get-default: func(self: speech-synthesis-voice-handle) -> bool;
}

interface base-audio-context {
    /// Opaque handle to a host-side `BaseAudioContext` instance.
    type base-audio-context-handle = u64;

    /// `destination` attribute — getter.
    get-destination: func(self: base-audio-context-handle) -> u64;

    /// `sampleRate` attribute — getter.
    get-sample-rate: func(self: base-audio-context-handle) -> f32;

    /// `currentTime` attribute — getter.
    get-current-time: func(self: base-audio-context-handle) -> f64;

    /// `listener` attribute — getter.
    get-listener: func(self: base-audio-context-handle) -> u64;

    /// `state` attribute — getter.
    get-state: func(self: base-audio-context-handle) -> u64;

    /// `renderQuantumSize` attribute — getter.
    get-render-quantum-size: func(self: base-audio-context-handle) -> u32;

    /// `audioWorklet` attribute — getter.
    get-audio-worklet: func(self: base-audio-context-handle) -> u64;

    /// `onstatechange` attribute — getter.
    get-onstatechange: func(self: base-audio-context-handle) -> u64;
    /// `onstatechange` attribute — setter.
    set-onstatechange: func(self: base-audio-context-handle, value: u64);

    /// `createAnalyser()` operation.
    create-analyser: func(self: base-audio-context-handle) -> u64;

    /// `createBiquadFilter()` operation.
    create-biquad-filter: func(self: base-audio-context-handle) -> u64;

    /// `createBuffer()` operation.
    create-buffer: func(self: base-audio-context-handle, number-of-channels: u32, length: u32, sample-rate: f32) -> u64;

    /// `createBufferSource()` operation.
    create-buffer-source: func(self: base-audio-context-handle) -> u64;

    /// `createChannelMerger()` operation.
    create-channel-merger: func(self: base-audio-context-handle, number-of-inputs: option<u32>) -> u64;

    /// `createChannelSplitter()` operation.
    create-channel-splitter: func(self: base-audio-context-handle, number-of-outputs: option<u32>) -> u64;

    /// `createConstantSource()` operation.
    create-constant-source: func(self: base-audio-context-handle) -> u64;

    /// `createConvolver()` operation.
    create-convolver: func(self: base-audio-context-handle) -> u64;

    /// `createDelay()` operation.
    create-delay: func(self: base-audio-context-handle, max-delay-time: option<f64>) -> u64;

    /// `createDynamicsCompressor()` operation.
    create-dynamics-compressor: func(self: base-audio-context-handle) -> u64;

    /// `createGain()` operation.
    create-gain: func(self: base-audio-context-handle) -> u64;

    /// `createIIRFilter()` operation.
    create-iir-filter: func(self: base-audio-context-handle, feedforward: list<f64>, feedback: list<f64>) -> u64;

    /// `createOscillator()` operation.
    create-oscillator: func(self: base-audio-context-handle) -> u64;

    /// `createPanner()` operation.
    create-panner: func(self: base-audio-context-handle) -> u64;

    /// `createPeriodicWave()` operation.
    create-periodic-wave: func(self: base-audio-context-handle, real: list<f32>, imag: list<f32>, constraints: option<u64>) -> u64;

    /// `createScriptProcessor()` operation.
    create-script-processor: func(self: base-audio-context-handle, buffer-size: option<u32>, number-of-input-channels: option<u32>, number-of-output-channels: option<u32>) -> u64;

    /// `createStereoPanner()` operation.
    create-stereo-panner: func(self: base-audio-context-handle) -> u64;

    /// `createWaveShaper()` operation.
    create-wave-shaper: func(self: base-audio-context-handle) -> u64;

    /// `decodeAudioData()` operation.
    decode-audio-data: func(self: base-audio-context-handle, audio-data: list<u8>, success-callback: option<u64>, error-callback: option<u64>) -> u64;
}

interface audio-context {
    /// Opaque handle to a host-side `AudioContext` instance.
    type audio-context-handle = u64;

    /// `baseLatency` attribute — getter.
    get-base-latency: func(self: audio-context-handle) -> f64;

    /// `outputLatency` attribute — getter.
    get-output-latency: func(self: audio-context-handle) -> f64;

    /// `sinkId` attribute — getter.
    get-sink-id: func(self: audio-context-handle) -> string;

    /// `onsinkchange` attribute — getter.
    get-onsinkchange: func(self: audio-context-handle) -> u64;
    /// `onsinkchange` attribute — setter.
    set-onsinkchange: func(self: audio-context-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: audio-context-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: audio-context-handle, value: u64);

    /// `playbackStats` attribute — getter.
    get-playback-stats: func(self: audio-context-handle) -> u64;

    /// `getOutputTimestamp()` operation.
    get-output-timestamp: func(self: audio-context-handle) -> u64;

    /// `resume()` operation.
    resume: func(self: audio-context-handle) -> u64;

    /// `suspend()` operation.
    suspend: func(self: audio-context-handle) -> u64;

    /// `close()` operation.
    close: func(self: audio-context-handle) -> u64;

    /// `setSinkId()` operation.
    set-sink-id: func(self: audio-context-handle, sink-id: string) -> u64;

    /// `createMediaElementSource()` operation.
    create-media-element-source: func(self: audio-context-handle, media-element: u64) -> u64;

    /// `createMediaStreamSource()` operation.
    create-media-stream-source: func(self: audio-context-handle, media-stream: u64) -> u64;

    /// `createMediaStreamTrackSource()` operation.
    create-media-stream-track-source: func(self: audio-context-handle, media-stream-track: u64) -> u64;

    /// `createMediaStreamDestination()` operation.
    create-media-stream-destination: func(self: audio-context-handle) -> u64;
}

interface audio-sink-info {
    /// Opaque handle to a host-side `AudioSinkInfo` instance.
    type audio-sink-info-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: audio-sink-info-handle) -> u64;
}

interface offline-audio-context {
    /// Opaque handle to a host-side `OfflineAudioContext` instance.
    type offline-audio-context-handle = u64;

    /// `startRendering()` operation.
    start-rendering: func(self: offline-audio-context-handle) -> u64;

    /// `resume()` operation.
    resume: func(self: offline-audio-context-handle) -> u64;

    /// `suspend()` operation.
    suspend: func(self: offline-audio-context-handle, suspend-time: f64) -> u64;

    /// `length` attribute — getter.
    get-length: func(self: offline-audio-context-handle) -> u32;

    /// `oncomplete` attribute — getter.
    get-oncomplete: func(self: offline-audio-context-handle) -> u64;
    /// `oncomplete` attribute — setter.
    set-oncomplete: func(self: offline-audio-context-handle, value: u64);
}

interface offline-audio-completion-event {
    /// Opaque handle to a host-side `OfflineAudioCompletionEvent` instance.
    type offline-audio-completion-event-handle = u64;

    /// `renderedBuffer` attribute — getter.
    get-rendered-buffer: func(self: offline-audio-completion-event-handle) -> u64;
}

interface audio-buffer {
    /// Opaque handle to a host-side `AudioBuffer` instance.
    type audio-buffer-handle = u64;

    /// `sampleRate` attribute — getter.
    get-sample-rate: func(self: audio-buffer-handle) -> f32;

    /// `length` attribute — getter.
    get-length: func(self: audio-buffer-handle) -> u32;

    /// `duration` attribute — getter.
    get-duration: func(self: audio-buffer-handle) -> f64;

    /// `numberOfChannels` attribute — getter.
    get-number-of-channels: func(self: audio-buffer-handle) -> u32;

    /// `getChannelData()` operation.
    get-channel-data: func(self: audio-buffer-handle, channel: u32) -> list<f32>;

    /// `copyFromChannel()` operation.
    copy-from-channel: func(self: audio-buffer-handle, destination: list<f32>, channel-number: u32, buffer-offset: option<u32>);

    /// `copyToChannel()` operation.
    copy-to-channel: func(self: audio-buffer-handle, source: list<f32>, channel-number: u32, buffer-offset: option<u32>);
}

interface audio-node {
    /// Opaque handle to a host-side `AudioNode` instance.
    type audio-node-handle = u64;

    /// `connect()` operation.
    connect: func(self: audio-node-handle, destination-node: u64, output: option<u32>, input: option<u32>) -> u64;

    /// `disconnect()` operation.
    disconnect: func(self: audio-node-handle);

    /// `context` attribute — getter.
    get-context: func(self: audio-node-handle) -> u64;

    /// `numberOfInputs` attribute — getter.
    get-number-of-inputs: func(self: audio-node-handle) -> u32;

    /// `numberOfOutputs` attribute — getter.
    get-number-of-outputs: func(self: audio-node-handle) -> u32;

    /// `channelCount` attribute — getter.
    get-channel-count: func(self: audio-node-handle) -> u32;
    /// `channelCount` attribute — setter.
    set-channel-count: func(self: audio-node-handle, value: u32);

    /// `channelCountMode` attribute — getter.
    get-channel-count-mode: func(self: audio-node-handle) -> u64;
    /// `channelCountMode` attribute — setter.
    set-channel-count-mode: func(self: audio-node-handle, value: u64);

    /// `channelInterpretation` attribute — getter.
    get-channel-interpretation: func(self: audio-node-handle) -> u64;
    /// `channelInterpretation` attribute — setter.
    set-channel-interpretation: func(self: audio-node-handle, value: u64);
}

interface audio-param {
    /// Opaque handle to a host-side `AudioParam` instance.
    type audio-param-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: audio-param-handle) -> f32;
    /// `value` attribute — setter.
    set-value: func(self: audio-param-handle, value: f32);

    /// `automationRate` attribute — getter.
    get-automation-rate: func(self: audio-param-handle) -> u64;
    /// `automationRate` attribute — setter.
    set-automation-rate: func(self: audio-param-handle, value: u64);

    /// `defaultValue` attribute — getter.
    get-default-value: func(self: audio-param-handle) -> f32;

    /// `minValue` attribute — getter.
    get-min-value: func(self: audio-param-handle) -> f32;

    /// `maxValue` attribute — getter.
    get-max-value: func(self: audio-param-handle) -> f32;

    /// `setValueAtTime()` operation.
    set-value-at-time: func(self: audio-param-handle, value: f32, start-time: f64) -> u64;

    /// `linearRampToValueAtTime()` operation.
    linear-ramp-to-value-at-time: func(self: audio-param-handle, value: f32, end-time: f64) -> u64;

    /// `exponentialRampToValueAtTime()` operation.
    exponential-ramp-to-value-at-time: func(self: audio-param-handle, value: f32, end-time: f64) -> u64;

    /// `setTargetAtTime()` operation.
    set-target-at-time: func(self: audio-param-handle, target: f32, start-time: f64, time-constant: f32) -> u64;

    /// `setValueCurveAtTime()` operation.
    set-value-curve-at-time: func(self: audio-param-handle, values: list<f32>, start-time: f64, duration: f64) -> u64;

    /// `cancelScheduledValues()` operation.
    cancel-scheduled-values: func(self: audio-param-handle, cancel-time: f64) -> u64;

    /// `cancelAndHoldAtTime()` operation.
    cancel-and-hold-at-time: func(self: audio-param-handle, cancel-time: f64) -> u64;
}

interface audio-scheduled-source-node {
    /// Opaque handle to a host-side `AudioScheduledSourceNode` instance.
    type audio-scheduled-source-node-handle = u64;

    /// `onended` attribute — getter.
    get-onended: func(self: audio-scheduled-source-node-handle) -> u64;
    /// `onended` attribute — setter.
    set-onended: func(self: audio-scheduled-source-node-handle, value: u64);

    /// `start()` operation.
    start: func(self: audio-scheduled-source-node-handle, when: option<f64>);

    /// `stop()` operation.
    stop: func(self: audio-scheduled-source-node-handle, when: option<f64>);
}

interface analyser-node {
    /// Opaque handle to a host-side `AnalyserNode` instance.
    type analyser-node-handle = u64;

    /// `getFloatFrequencyData()` operation.
    get-float-frequency-data: func(self: analyser-node-handle, array: list<f32>);

    /// `getByteFrequencyData()` operation.
    get-byte-frequency-data: func(self: analyser-node-handle, array: list<u8>);

    /// `getFloatTimeDomainData()` operation.
    get-float-time-domain-data: func(self: analyser-node-handle, array: list<f32>);

    /// `getByteTimeDomainData()` operation.
    get-byte-time-domain-data: func(self: analyser-node-handle, array: list<u8>);

    /// `fftSize` attribute — getter.
    get-fft-size: func(self: analyser-node-handle) -> u32;
    /// `fftSize` attribute — setter.
    set-fft-size: func(self: analyser-node-handle, value: u32);

    /// `frequencyBinCount` attribute — getter.
    get-frequency-bin-count: func(self: analyser-node-handle) -> u32;

    /// `minDecibels` attribute — getter.
    get-min-decibels: func(self: analyser-node-handle) -> f64;
    /// `minDecibels` attribute — setter.
    set-min-decibels: func(self: analyser-node-handle, value: f64);

    /// `maxDecibels` attribute — getter.
    get-max-decibels: func(self: analyser-node-handle) -> f64;
    /// `maxDecibels` attribute — setter.
    set-max-decibels: func(self: analyser-node-handle, value: f64);

    /// `smoothingTimeConstant` attribute — getter.
    get-smoothing-time-constant: func(self: analyser-node-handle) -> f64;
    /// `smoothingTimeConstant` attribute — setter.
    set-smoothing-time-constant: func(self: analyser-node-handle, value: f64);
}

interface audio-buffer-source-node {
    /// Opaque handle to a host-side `AudioBufferSourceNode` instance.
    type audio-buffer-source-node-handle = u64;

    /// `buffer` attribute — getter.
    get-buffer: func(self: audio-buffer-source-node-handle) -> option<u64>;
    /// `buffer` attribute — setter.
    set-buffer: func(self: audio-buffer-source-node-handle, value: option<u64>);

    /// `playbackRate` attribute — getter.
    get-playback-rate: func(self: audio-buffer-source-node-handle) -> u64;

    /// `detune` attribute — getter.
    get-detune: func(self: audio-buffer-source-node-handle) -> u64;

    /// `loop` attribute — getter.
    get-loop: func(self: audio-buffer-source-node-handle) -> bool;
    /// `loop` attribute — setter.
    set-loop: func(self: audio-buffer-source-node-handle, value: bool);

    /// `loopStart` attribute — getter.
    get-loop-start: func(self: audio-buffer-source-node-handle) -> f64;
    /// `loopStart` attribute — setter.
    set-loop-start: func(self: audio-buffer-source-node-handle, value: f64);

    /// `loopEnd` attribute — getter.
    get-loop-end: func(self: audio-buffer-source-node-handle) -> f64;
    /// `loopEnd` attribute — setter.
    set-loop-end: func(self: audio-buffer-source-node-handle, value: f64);

    /// `start()` operation.
    start: func(self: audio-buffer-source-node-handle, when: option<f64>, offset: option<f64>, duration: option<f64>);
}

interface audio-destination-node {
    /// Opaque handle to a host-side `AudioDestinationNode` instance.
    type audio-destination-node-handle = u64;

    /// `maxChannelCount` attribute — getter.
    get-max-channel-count: func(self: audio-destination-node-handle) -> u32;
}

interface audio-listener {
    /// Opaque handle to a host-side `AudioListener` instance.
    type audio-listener-handle = u64;

    /// `positionX` attribute — getter.
    get-position-x: func(self: audio-listener-handle) -> u64;

    /// `positionY` attribute — getter.
    get-position-y: func(self: audio-listener-handle) -> u64;

    /// `positionZ` attribute — getter.
    get-position-z: func(self: audio-listener-handle) -> u64;

    /// `forwardX` attribute — getter.
    get-forward-x: func(self: audio-listener-handle) -> u64;

    /// `forwardY` attribute — getter.
    get-forward-y: func(self: audio-listener-handle) -> u64;

    /// `forwardZ` attribute — getter.
    get-forward-z: func(self: audio-listener-handle) -> u64;

    /// `upX` attribute — getter.
    get-up-x: func(self: audio-listener-handle) -> u64;

    /// `upY` attribute — getter.
    get-up-y: func(self: audio-listener-handle) -> u64;

    /// `upZ` attribute — getter.
    get-up-z: func(self: audio-listener-handle) -> u64;

    /// `setPosition()` operation.
    set-position: func(self: audio-listener-handle, x: f32, y: f32, z: f32);

    /// `setOrientation()` operation.
    set-orientation: func(self: audio-listener-handle, x: f32, y: f32, z: f32, x-up: f32, y-up: f32, z-up: f32);
}

interface audio-processing-event {
    /// Opaque handle to a host-side `AudioProcessingEvent` instance.
    type audio-processing-event-handle = u64;

    /// `playbackTime` attribute — getter.
    get-playback-time: func(self: audio-processing-event-handle) -> f64;

    /// `inputBuffer` attribute — getter.
    get-input-buffer: func(self: audio-processing-event-handle) -> u64;

    /// `outputBuffer` attribute — getter.
    get-output-buffer: func(self: audio-processing-event-handle) -> u64;
}

interface biquad-filter-node {
    /// Opaque handle to a host-side `BiquadFilterNode` instance.
    type biquad-filter-node-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: biquad-filter-node-handle) -> u64;
    /// `type` attribute — setter.
    set-type: func(self: biquad-filter-node-handle, value: u64);

    /// `frequency` attribute — getter.
    get-frequency: func(self: biquad-filter-node-handle) -> u64;

    /// `detune` attribute — getter.
    get-detune: func(self: biquad-filter-node-handle) -> u64;

    /// `Q` attribute — getter.
    get-q: func(self: biquad-filter-node-handle) -> u64;

    /// `gain` attribute — getter.
    get-gain: func(self: biquad-filter-node-handle) -> u64;

    /// `getFrequencyResponse()` operation.
    get-frequency-response: func(self: biquad-filter-node-handle, frequency-hz: list<f32>, mag-response: list<f32>, phase-response: list<f32>);
}

interface constant-source-node {
    /// Opaque handle to a host-side `ConstantSourceNode` instance.
    type constant-source-node-handle = u64;

    /// `offset` attribute — getter.
    get-offset: func(self: constant-source-node-handle) -> u64;
}

interface convolver-node {
    /// Opaque handle to a host-side `ConvolverNode` instance.
    type convolver-node-handle = u64;

    /// `buffer` attribute — getter.
    get-buffer: func(self: convolver-node-handle) -> option<u64>;
    /// `buffer` attribute — setter.
    set-buffer: func(self: convolver-node-handle, value: option<u64>);

    /// `normalize` attribute — getter.
    get-normalize: func(self: convolver-node-handle) -> bool;
    /// `normalize` attribute — setter.
    set-normalize: func(self: convolver-node-handle, value: bool);
}

interface delay-node {
    /// Opaque handle to a host-side `DelayNode` instance.
    type delay-node-handle = u64;

    /// `delayTime` attribute — getter.
    get-delay-time: func(self: delay-node-handle) -> u64;
}

interface dynamics-compressor-node {
    /// Opaque handle to a host-side `DynamicsCompressorNode` instance.
    type dynamics-compressor-node-handle = u64;

    /// `threshold` attribute — getter.
    get-threshold: func(self: dynamics-compressor-node-handle) -> u64;

    /// `knee` attribute — getter.
    get-knee: func(self: dynamics-compressor-node-handle) -> u64;

    /// `ratio` attribute — getter.
    get-ratio: func(self: dynamics-compressor-node-handle) -> u64;

    /// `reduction` attribute — getter.
    get-reduction: func(self: dynamics-compressor-node-handle) -> f32;

    /// `attack` attribute — getter.
    get-attack: func(self: dynamics-compressor-node-handle) -> u64;

    /// `release` attribute — getter.
    get-release: func(self: dynamics-compressor-node-handle) -> u64;
}

interface gain-node {
    /// Opaque handle to a host-side `GainNode` instance.
    type gain-node-handle = u64;

    /// `gain` attribute — getter.
    get-gain: func(self: gain-node-handle) -> u64;
}

interface iir-filter-node {
    /// Opaque handle to a host-side `IIRFilterNode` instance.
    type iir-filter-node-handle = u64;

    /// `getFrequencyResponse()` operation.
    get-frequency-response: func(self: iir-filter-node-handle, frequency-hz: list<f32>, mag-response: list<f32>, phase-response: list<f32>);
}

interface media-element-audio-source-node {
    /// Opaque handle to a host-side `MediaElementAudioSourceNode` instance.
    type media-element-audio-source-node-handle = u64;

    /// `mediaElement` attribute — getter.
    get-media-element: func(self: media-element-audio-source-node-handle) -> u64;
}

interface media-stream-audio-destination-node {
    /// Opaque handle to a host-side `MediaStreamAudioDestinationNode` instance.
    type media-stream-audio-destination-node-handle = u64;

    /// `stream` attribute — getter.
    get-stream: func(self: media-stream-audio-destination-node-handle) -> u64;
}

interface media-stream-audio-source-node {
    /// Opaque handle to a host-side `MediaStreamAudioSourceNode` instance.
    type media-stream-audio-source-node-handle = u64;

    /// `mediaStream` attribute — getter.
    get-media-stream: func(self: media-stream-audio-source-node-handle) -> u64;
}

interface oscillator-node {
    /// Opaque handle to a host-side `OscillatorNode` instance.
    type oscillator-node-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: oscillator-node-handle) -> u64;
    /// `type` attribute — setter.
    set-type: func(self: oscillator-node-handle, value: u64);

    /// `frequency` attribute — getter.
    get-frequency: func(self: oscillator-node-handle) -> u64;

    /// `detune` attribute — getter.
    get-detune: func(self: oscillator-node-handle) -> u64;

    /// `setPeriodicWave()` operation.
    set-periodic-wave: func(self: oscillator-node-handle, periodic-wave: u64);
}

interface panner-node {
    /// Opaque handle to a host-side `PannerNode` instance.
    type panner-node-handle = u64;

    /// `panningModel` attribute — getter.
    get-panning-model: func(self: panner-node-handle) -> u64;
    /// `panningModel` attribute — setter.
    set-panning-model: func(self: panner-node-handle, value: u64);

    /// `positionX` attribute — getter.
    get-position-x: func(self: panner-node-handle) -> u64;

    /// `positionY` attribute — getter.
    get-position-y: func(self: panner-node-handle) -> u64;

    /// `positionZ` attribute — getter.
    get-position-z: func(self: panner-node-handle) -> u64;

    /// `orientationX` attribute — getter.
    get-orientation-x: func(self: panner-node-handle) -> u64;

    /// `orientationY` attribute — getter.
    get-orientation-y: func(self: panner-node-handle) -> u64;

    /// `orientationZ` attribute — getter.
    get-orientation-z: func(self: panner-node-handle) -> u64;

    /// `distanceModel` attribute — getter.
    get-distance-model: func(self: panner-node-handle) -> u64;
    /// `distanceModel` attribute — setter.
    set-distance-model: func(self: panner-node-handle, value: u64);

    /// `refDistance` attribute — getter.
    get-ref-distance: func(self: panner-node-handle) -> f64;
    /// `refDistance` attribute — setter.
    set-ref-distance: func(self: panner-node-handle, value: f64);

    /// `maxDistance` attribute — getter.
    get-max-distance: func(self: panner-node-handle) -> f64;
    /// `maxDistance` attribute — setter.
    set-max-distance: func(self: panner-node-handle, value: f64);

    /// `rolloffFactor` attribute — getter.
    get-rolloff-factor: func(self: panner-node-handle) -> f64;
    /// `rolloffFactor` attribute — setter.
    set-rolloff-factor: func(self: panner-node-handle, value: f64);

    /// `coneInnerAngle` attribute — getter.
    get-cone-inner-angle: func(self: panner-node-handle) -> f64;
    /// `coneInnerAngle` attribute — setter.
    set-cone-inner-angle: func(self: panner-node-handle, value: f64);

    /// `coneOuterAngle` attribute — getter.
    get-cone-outer-angle: func(self: panner-node-handle) -> f64;
    /// `coneOuterAngle` attribute — setter.
    set-cone-outer-angle: func(self: panner-node-handle, value: f64);

    /// `coneOuterGain` attribute — getter.
    get-cone-outer-gain: func(self: panner-node-handle) -> f64;
    /// `coneOuterGain` attribute — setter.
    set-cone-outer-gain: func(self: panner-node-handle, value: f64);

    /// `setPosition()` operation.
    set-position: func(self: panner-node-handle, x: f32, y: f32, z: f32);

    /// `setOrientation()` operation.
    set-orientation: func(self: panner-node-handle, x: f32, y: f32, z: f32);
}

interface script-processor-node {
    /// Opaque handle to a host-side `ScriptProcessorNode` instance.
    type script-processor-node-handle = u64;

    /// `onaudioprocess` attribute — getter.
    get-onaudioprocess: func(self: script-processor-node-handle) -> u64;
    /// `onaudioprocess` attribute — setter.
    set-onaudioprocess: func(self: script-processor-node-handle, value: u64);

    /// `bufferSize` attribute — getter.
    get-buffer-size: func(self: script-processor-node-handle) -> s32;
}

interface stereo-panner-node {
    /// Opaque handle to a host-side `StereoPannerNode` instance.
    type stereo-panner-node-handle = u64;

    /// `pan` attribute — getter.
    get-pan: func(self: stereo-panner-node-handle) -> u64;
}

interface wave-shaper-node {
    /// Opaque handle to a host-side `WaveShaperNode` instance.
    type wave-shaper-node-handle = u64;

    /// `curve` attribute — getter.
    get-curve: func(self: wave-shaper-node-handle) -> option<list<f32>>;
    /// `curve` attribute — setter.
    set-curve: func(self: wave-shaper-node-handle, value: option<list<f32>>);

    /// `oversample` attribute — getter.
    get-oversample: func(self: wave-shaper-node-handle) -> u64;
    /// `oversample` attribute — setter.
    set-oversample: func(self: wave-shaper-node-handle, value: u64);
}

interface audio-worklet {
    /// Opaque handle to a host-side `AudioWorklet` instance.
    type audio-worklet-handle = u64;

    /// `port` attribute — getter.
    get-port: func(self: audio-worklet-handle) -> u64;
}

interface audio-worklet-global-scope {
    /// Opaque handle to a host-side `AudioWorkletGlobalScope` instance.
    type audio-worklet-global-scope-handle = u64;

    /// `registerProcessor()` operation.
    register-processor: func(self: audio-worklet-global-scope-handle, name: string, processor-ctor: u64);

    /// `currentFrame` attribute — getter.
    get-current-frame: func(self: audio-worklet-global-scope-handle) -> u64;

    /// `currentTime` attribute — getter.
    get-current-time: func(self: audio-worklet-global-scope-handle) -> f64;

    /// `sampleRate` attribute — getter.
    get-sample-rate: func(self: audio-worklet-global-scope-handle) -> f32;

    /// `renderQuantumSize` attribute — getter.
    get-render-quantum-size: func(self: audio-worklet-global-scope-handle) -> u32;

    /// `port` attribute — getter.
    get-port: func(self: audio-worklet-global-scope-handle) -> u64;
}

interface audio-worklet-node {
    /// Opaque handle to a host-side `AudioWorkletNode` instance.
    type audio-worklet-node-handle = u64;

    /// `parameters` attribute — getter.
    get-parameters: func(self: audio-worklet-node-handle) -> u64;

    /// `port` attribute — getter.
    get-port: func(self: audio-worklet-node-handle) -> u64;

    /// `onprocessorerror` attribute — getter.
    get-onprocessorerror: func(self: audio-worklet-node-handle) -> u64;
    /// `onprocessorerror` attribute — setter.
    set-onprocessorerror: func(self: audio-worklet-node-handle, value: u64);
}

interface audio-worklet-processor {
    /// Opaque handle to a host-side `AudioWorkletProcessor` instance.
    type audio-worklet-processor-handle = u64;

    /// `port` attribute — getter.
    get-port: func(self: audio-worklet-processor-handle) -> u64;
}

interface audio-playback-stats {
    /// Opaque handle to a host-side `AudioPlaybackStats` instance.
    type audio-playback-stats-handle = u64;

    /// `underrunDuration` attribute — getter.
    get-underrun-duration: func(self: audio-playback-stats-handle) -> f64;

    /// `underrunEvents` attribute — getter.
    get-underrun-events: func(self: audio-playback-stats-handle) -> u32;

    /// `totalDuration` attribute — getter.
    get-total-duration: func(self: audio-playback-stats-handle) -> f64;

    /// `averageLatency` attribute — getter.
    get-average-latency: func(self: audio-playback-stats-handle) -> f64;

    /// `minimumLatency` attribute — getter.
    get-minimum-latency: func(self: audio-playback-stats-handle) -> f64;

    /// `maximumLatency` attribute — getter.
    get-maximum-latency: func(self: audio-playback-stats-handle) -> f64;

    /// `resetLatency()` operation.
    reset-latency: func(self: audio-playback-stats-handle);

    /// `toJSON()` operation.
    to-json: func(self: audio-playback-stats-handle) -> u64;
}

interface text-decoder-common {
    /// Opaque handle to a host-side `TextDecoderCommon` instance.
    type text-decoder-common-handle = u64;

    /// `encoding` attribute — getter.
    get-encoding: func(self: text-decoder-common-handle) -> string;

    /// `fatal` attribute — getter.
    get-fatal: func(self: text-decoder-common-handle) -> bool;

    /// `ignoreBOM` attribute — getter.
    get-ignore-bom: func(self: text-decoder-common-handle) -> bool;
}

interface text-decoder {
    /// Opaque handle to a host-side `TextDecoder` instance.
    type text-decoder-handle = u64;

    /// `decode()` operation.
    decode: func(self: text-decoder-handle, input: option<u64>, options: option<u64>) -> string;
}

interface text-encoder-common {
    /// Opaque handle to a host-side `TextEncoderCommon` instance.
    type text-encoder-common-handle = u64;

    /// `encoding` attribute — getter.
    get-encoding: func(self: text-encoder-common-handle) -> string;
}

interface text-encoder {
    /// Opaque handle to a host-side `TextEncoder` instance.
    type text-encoder-handle = u64;

    /// `encode()` operation.
    encode: func(self: text-encoder-handle, input: option<string>) -> list<u8>;

    /// `encodeInto()` operation.
    encode-into: func(self: text-encoder-handle, source: string, destination: list<u8>) -> u64;
}

interface notification {
    /// Opaque handle to a host-side `Notification` instance.
    type notification-handle = u64;

    /// `permission` attribute — getter.
    get-permission: func() -> u64;

    /// `requestPermission()` operation.
    request-permission: func(deprecated-callback: option<u64>) -> u64;

    /// `maxActions` attribute — getter.
    get-max-actions: func() -> u32;

    /// `onclick` attribute — getter.
    get-onclick: func(self: notification-handle) -> u64;
    /// `onclick` attribute — setter.
    set-onclick: func(self: notification-handle, value: u64);

    /// `onshow` attribute — getter.
    get-onshow: func(self: notification-handle) -> u64;
    /// `onshow` attribute — setter.
    set-onshow: func(self: notification-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: notification-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: notification-handle, value: u64);

    /// `onclose` attribute — getter.
    get-onclose: func(self: notification-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: notification-handle, value: u64);

    /// `title` attribute — getter.
    get-title: func(self: notification-handle) -> string;

    /// `dir` attribute — getter.
    get-dir: func(self: notification-handle) -> u64;

    /// `lang` attribute — getter.
    get-lang: func(self: notification-handle) -> string;

    /// `body` attribute — getter.
    get-body: func(self: notification-handle) -> string;

    /// `navigate` attribute — getter.
    get-navigate: func(self: notification-handle) -> string;

    /// `tag` attribute — getter.
    get-tag: func(self: notification-handle) -> string;

    /// `image` attribute — getter.
    get-image: func(self: notification-handle) -> string;

    /// `icon` attribute — getter.
    get-icon: func(self: notification-handle) -> string;

    /// `badge` attribute — getter.
    get-badge: func(self: notification-handle) -> string;

    /// `vibrate` attribute — getter.
    get-vibrate: func(self: notification-handle) -> list<u32>;

    /// `timestamp` attribute — getter.
    get-timestamp: func(self: notification-handle) -> u64;

    /// `renotify` attribute — getter.
    get-renotify: func(self: notification-handle) -> bool;

    /// `silent` attribute — getter.
    get-silent: func(self: notification-handle) -> option<bool>;

    /// `requireInteraction` attribute — getter.
    get-require-interaction: func(self: notification-handle) -> bool;

    /// `data` attribute — getter.
    get-data: func(self: notification-handle) -> string;

    /// `actions` attribute — getter.
    get-actions: func(self: notification-handle) -> list<u64>;

    /// `close()` operation.
    close: func(self: notification-handle);
}

interface service-worker-registration {
    /// Opaque handle to a host-side `ServiceWorkerRegistration` instance.
    type service-worker-registration-handle = u64;

    /// `showNotification()` operation.
    show-notification: func(self: service-worker-registration-handle, title: string, options: option<u64>) -> u64;

    /// `getNotifications()` operation.
    get-notifications: func(self: service-worker-registration-handle, filter: option<u64>) -> u64;

    /// `installing` attribute — getter.
    get-installing: func(self: service-worker-registration-handle) -> option<u64>;

    /// `waiting` attribute — getter.
    get-waiting: func(self: service-worker-registration-handle) -> option<u64>;

    /// `active` attribute — getter.
    get-active: func(self: service-worker-registration-handle) -> option<u64>;

    /// `navigationPreload` attribute — getter.
    get-navigation-preload: func(self: service-worker-registration-handle) -> u64;

    /// `scope` attribute — getter.
    get-scope: func(self: service-worker-registration-handle) -> string;

    /// `updateViaCache` attribute — getter.
    get-update-via-cache: func(self: service-worker-registration-handle) -> u64;

    /// `update()` operation.
    update: func(self: service-worker-registration-handle) -> u64;

    /// `unregister()` operation.
    unregister: func(self: service-worker-registration-handle) -> u64;

    /// `onupdatefound` attribute — getter.
    get-onupdatefound: func(self: service-worker-registration-handle) -> u64;
    /// `onupdatefound` attribute — setter.
    set-onupdatefound: func(self: service-worker-registration-handle, value: u64);
}

interface notification-event {
    /// Opaque handle to a host-side `NotificationEvent` instance.
    type notification-event-handle = u64;

    /// `notification` attribute — getter.
    get-notification: func(self: notification-event-handle) -> u64;

    /// `action` attribute — getter.
    get-action: func(self: notification-event-handle) -> string;
}

interface service-worker-global-scope {
    /// Opaque handle to a host-side `ServiceWorkerGlobalScope` instance.
    type service-worker-global-scope-handle = u64;

    /// `onnotificationclick` attribute — getter.
    get-onnotificationclick: func(self: service-worker-global-scope-handle) -> u64;
    /// `onnotificationclick` attribute — setter.
    set-onnotificationclick: func(self: service-worker-global-scope-handle, value: u64);

    /// `onnotificationclose` attribute — getter.
    get-onnotificationclose: func(self: service-worker-global-scope-handle) -> u64;
    /// `onnotificationclose` attribute — setter.
    set-onnotificationclose: func(self: service-worker-global-scope-handle, value: u64);

    /// `clients` attribute — getter.
    get-clients: func(self: service-worker-global-scope-handle) -> u64;

    /// `registration` attribute — getter.
    get-registration: func(self: service-worker-global-scope-handle) -> u64;

    /// `serviceWorker` attribute — getter.
    get-service-worker: func(self: service-worker-global-scope-handle) -> u64;

    /// `skipWaiting()` operation.
    skip-waiting: func(self: service-worker-global-scope-handle) -> u64;

    /// `oninstall` attribute — getter.
    get-oninstall: func(self: service-worker-global-scope-handle) -> u64;
    /// `oninstall` attribute — setter.
    set-oninstall: func(self: service-worker-global-scope-handle, value: u64);

    /// `onactivate` attribute — getter.
    get-onactivate: func(self: service-worker-global-scope-handle) -> u64;
    /// `onactivate` attribute — setter.
    set-onactivate: func(self: service-worker-global-scope-handle, value: u64);

    /// `onfetch` attribute — getter.
    get-onfetch: func(self: service-worker-global-scope-handle) -> u64;
    /// `onfetch` attribute — setter.
    set-onfetch: func(self: service-worker-global-scope-handle, value: u64);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: service-worker-global-scope-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: service-worker-global-scope-handle, value: u64);

    /// `onmessageerror` attribute — getter.
    get-onmessageerror: func(self: service-worker-global-scope-handle) -> u64;
    /// `onmessageerror` attribute — setter.
    set-onmessageerror: func(self: service-worker-global-scope-handle, value: u64);
}

interface intersection-observer-entry {
    /// Opaque handle to a host-side `IntersectionObserverEntry` instance.

    use types.{dom-rect};

    type intersection-observer-entry-handle = u64;

    /// `time` attribute — getter.
    get-time: func(self: intersection-observer-entry-handle) -> f64;

    /// `rootBounds` attribute — getter.
    get-root-bounds: func(self: intersection-observer-entry-handle) -> option<dom-rect>;

    /// `boundingClientRect` attribute — getter.
    get-bounding-client-rect: func(self: intersection-observer-entry-handle) -> dom-rect;

    /// `intersectionRect` attribute — getter.
    get-intersection-rect: func(self: intersection-observer-entry-handle) -> dom-rect;

    /// `isIntersecting` attribute — getter.
    get-is-intersecting: func(self: intersection-observer-entry-handle) -> bool;

    /// `isVisible` attribute — getter.
    get-is-visible: func(self: intersection-observer-entry-handle) -> bool;

    /// `intersectionRatio` attribute — getter.
    get-intersection-ratio: func(self: intersection-observer-entry-handle) -> f64;

    /// `target` attribute — getter.
    get-target: func(self: intersection-observer-entry-handle) -> u64;
}

interface resize-observer {
    /// Opaque handle to a host-side `ResizeObserver` instance.
    type resize-observer-handle = u64;

    /// `observe()` operation.
    observe: func(self: resize-observer-handle, target: u64, options: option<u64>);

    /// `unobserve()` operation.
    unobserve: func(self: resize-observer-handle, target: u64);

    /// `disconnect()` operation.
    disconnect: func(self: resize-observer-handle);
}

interface resize-observer-entry {
    /// Opaque handle to a host-side `ResizeObserverEntry` instance.

    use types.{dom-rect};

    type resize-observer-entry-handle = u64;

    /// `target` attribute — getter.
    get-target: func(self: resize-observer-entry-handle) -> u64;

    /// `contentRect` attribute — getter.
    get-content-rect: func(self: resize-observer-entry-handle) -> dom-rect;

    /// `borderBoxSize` attribute — getter.
    get-border-box-size: func(self: resize-observer-entry-handle) -> list<u64>;

    /// `contentBoxSize` attribute — getter.
    get-content-box-size: func(self: resize-observer-entry-handle) -> list<u64>;

    /// `devicePixelContentBoxSize` attribute — getter.
    get-device-pixel-content-box-size: func(self: resize-observer-entry-handle) -> list<u64>;
}

interface resize-observer-size {
    /// Opaque handle to a host-side `ResizeObserverSize` instance.
    type resize-observer-size-handle = u64;

    /// `inlineSize` attribute — getter.
    get-inline-size: func(self: resize-observer-size-handle) -> f64;

    /// `blockSize` attribute — getter.
    get-block-size: func(self: resize-observer-size-handle) -> f64;
}

interface payment-request {
    /// Opaque handle to a host-side `PaymentRequest` instance.
    type payment-request-handle = u64;

    /// `show()` operation.
    show: func(self: payment-request-handle, details-promise: option<u64>) -> u64;

    /// `abort()` operation.
    abort: func(self: payment-request-handle) -> u64;

    /// `canMakePayment()` operation.
    can-make-payment: func(self: payment-request-handle) -> u64;

    /// `id` attribute — getter.
    get-id: func(self: payment-request-handle) -> string;

    /// `shippingAddress` attribute — getter.
    get-shipping-address: func(self: payment-request-handle) -> option<u64>;

    /// `shippingOption` attribute — getter.
    get-shipping-option: func(self: payment-request-handle) -> option<string>;

    /// `shippingType` attribute — getter.
    get-shipping-type: func(self: payment-request-handle) -> option<u64>;

    /// `onshippingaddresschange` attribute — getter.
    get-onshippingaddresschange: func(self: payment-request-handle) -> u64;
    /// `onshippingaddresschange` attribute — setter.
    set-onshippingaddresschange: func(self: payment-request-handle, value: u64);

    /// `onshippingoptionchange` attribute — getter.
    get-onshippingoptionchange: func(self: payment-request-handle) -> u64;
    /// `onshippingoptionchange` attribute — setter.
    set-onshippingoptionchange: func(self: payment-request-handle, value: u64);

    /// `onpaymentmethodchange` attribute — getter.
    get-onpaymentmethodchange: func(self: payment-request-handle) -> u64;
    /// `onpaymentmethodchange` attribute — setter.
    set-onpaymentmethodchange: func(self: payment-request-handle, value: u64);
}

interface payment-response {
    /// Opaque handle to a host-side `PaymentResponse` instance.
    type payment-response-handle = u64;

    /// `toJSON()` operation.
    to-json: func(self: payment-response-handle) -> u64;

    /// `requestId` attribute — getter.
    get-request-id: func(self: payment-response-handle) -> string;

    /// `methodName` attribute — getter.
    get-method-name: func(self: payment-response-handle) -> string;

    /// `details` attribute — getter.
    get-details: func(self: payment-response-handle) -> u64;

    /// `shippingAddress` attribute — getter.
    get-shipping-address: func(self: payment-response-handle) -> option<u64>;

    /// `shippingOption` attribute — getter.
    get-shipping-option: func(self: payment-response-handle) -> option<string>;

    /// `payerName` attribute — getter.
    get-payer-name: func(self: payment-response-handle) -> option<string>;

    /// `payerEmail` attribute — getter.
    get-payer-email: func(self: payment-response-handle) -> option<string>;

    /// `payerPhone` attribute — getter.
    get-payer-phone: func(self: payment-response-handle) -> option<string>;

    /// `complete()` operation.
    complete: func(self: payment-response-handle, %result: option<u64>, details: option<u64>) -> u64;

    /// `retry()` operation.
    retry: func(self: payment-response-handle, error-fields: option<u64>) -> u64;

    /// `onpayerdetailchange` attribute — getter.
    get-onpayerdetailchange: func(self: payment-response-handle) -> u64;
    /// `onpayerdetailchange` attribute — setter.
    set-onpayerdetailchange: func(self: payment-response-handle, value: u64);
}

interface payment-method-change-event {
    /// Opaque handle to a host-side `PaymentMethodChangeEvent` instance.
    type payment-method-change-event-handle = u64;

    /// `methodName` attribute — getter.
    get-method-name: func(self: payment-method-change-event-handle) -> string;

    /// `methodDetails` attribute — getter.
    get-method-details: func(self: payment-method-change-event-handle) -> option<u64>;
}

interface payment-request-update-event {
    /// Opaque handle to a host-side `PaymentRequestUpdateEvent` instance.
    type payment-request-update-event-handle = u64;

    /// `updateWith()` operation.
    update-with: func(self: payment-request-update-event-handle, details-promise: u64);
}

interface performance {
    /// Opaque handle to a host-side `Performance` instance.
    type performance-handle = u64;

    /// `now()` operation.
    now: func() -> f64;

    /// `timeOrigin` attribute — getter.
    get-time-origin: func() -> f64;

    /// `toJSON()` operation.
    to-json: func() -> u64;

    /// `timing` attribute — getter.
    get-timing: func() -> u64;

    /// `navigation` attribute — getter.
    get-navigation: func() -> u64;

    /// `getEntries()` operation.
    get-entries: func() -> u64;

    /// `getEntriesByType()` operation.
    get-entries-by-type: func(%type: string) -> u64;

    /// `getEntriesByName()` operation.
    get-entries-by-name: func(name: string, %type: option<string>) -> u64;

    /// `clearResourceTimings()` operation.
    clear-resource-timings: func();

    /// `setResourceTimingBufferSize()` operation.
    set-resource-timing-buffer-size: func(max-size: u32);

    /// `onresourcetimingbufferfull` attribute — getter.
    get-onresourcetimingbufferfull: func() -> u64;
    /// `onresourcetimingbufferfull` attribute — setter.
    set-onresourcetimingbufferfull: func(value: u64);

    /// `mark()` operation.
    mark: func(mark-name: string, mark-options: option<u64>) -> u64;

    /// `clearMarks()` operation.
    clear-marks: func(mark-name: option<string>);

    /// `measure()` operation.
    measure: func(measure-name: string, start-or-measure-options: option<string>, end-mark: option<string>) -> u64;

    /// `clearMeasures()` operation.
    clear-measures: func(measure-name: option<string>);
}

interface performance-navigation-timing {
    /// Opaque handle to a host-side `PerformanceNavigationTiming` instance.
    type performance-navigation-timing-handle = u64;

    /// `unloadEventStart` attribute — getter.
    get-unload-event-start: func(self: performance-navigation-timing-handle) -> f64;

    /// `unloadEventEnd` attribute — getter.
    get-unload-event-end: func(self: performance-navigation-timing-handle) -> f64;

    /// `domInteractive` attribute — getter.
    get-dom-interactive: func(self: performance-navigation-timing-handle) -> f64;

    /// `domContentLoadedEventStart` attribute — getter.
    get-dom-content-loaded-event-start: func(self: performance-navigation-timing-handle) -> f64;

    /// `domContentLoadedEventEnd` attribute — getter.
    get-dom-content-loaded-event-end: func(self: performance-navigation-timing-handle) -> f64;

    /// `domComplete` attribute — getter.
    get-dom-complete: func(self: performance-navigation-timing-handle) -> f64;

    /// `loadEventStart` attribute — getter.
    get-load-event-start: func(self: performance-navigation-timing-handle) -> f64;

    /// `loadEventEnd` attribute — getter.
    get-load-event-end: func(self: performance-navigation-timing-handle) -> f64;

    /// `type` attribute — getter.
    get-type: func(self: performance-navigation-timing-handle) -> u64;

    /// `redirectCount` attribute — getter.
    get-redirect-count: func(self: performance-navigation-timing-handle) -> u16;

    /// `criticalCHRestart` attribute — getter.
    get-critical-ch-restart: func(self: performance-navigation-timing-handle) -> f64;

    /// `notRestoredReasons` attribute — getter.
    get-not-restored-reasons: func(self: performance-navigation-timing-handle) -> option<u64>;

    /// `confidence` attribute — getter.
    get-confidence: func(self: performance-navigation-timing-handle) -> u64;

    /// `toJSON()` operation.
    to-json: func(self: performance-navigation-timing-handle) -> u64;
}

interface performance-timing-confidence {
    /// Opaque handle to a host-side `PerformanceTimingConfidence` instance.
    type performance-timing-confidence-handle = u64;

    /// `randomizedTriggerRate` attribute — getter.
    get-randomized-trigger-rate: func(self: performance-timing-confidence-handle) -> f64;

    /// `value` attribute — getter.
    get-value: func(self: performance-timing-confidence-handle) -> u64;

    /// `toJSON()` operation.
    to-json: func(self: performance-timing-confidence-handle) -> u64;
}

interface performance-timing {
    /// Opaque handle to a host-side `PerformanceTiming` instance.
    type performance-timing-handle = u64;

    /// `navigationStart` attribute — getter.
    get-navigation-start: func(self: performance-timing-handle) -> u64;

    /// `unloadEventStart` attribute — getter.
    get-unload-event-start: func(self: performance-timing-handle) -> u64;

    /// `unloadEventEnd` attribute — getter.
    get-unload-event-end: func(self: performance-timing-handle) -> u64;

    /// `redirectStart` attribute — getter.
    get-redirect-start: func(self: performance-timing-handle) -> u64;

    /// `redirectEnd` attribute — getter.
    get-redirect-end: func(self: performance-timing-handle) -> u64;

    /// `fetchStart` attribute — getter.
    get-fetch-start: func(self: performance-timing-handle) -> u64;

    /// `domainLookupStart` attribute — getter.
    get-domain-lookup-start: func(self: performance-timing-handle) -> u64;

    /// `domainLookupEnd` attribute — getter.
    get-domain-lookup-end: func(self: performance-timing-handle) -> u64;

    /// `connectStart` attribute — getter.
    get-connect-start: func(self: performance-timing-handle) -> u64;

    /// `connectEnd` attribute — getter.
    get-connect-end: func(self: performance-timing-handle) -> u64;

    /// `secureConnectionStart` attribute — getter.
    get-secure-connection-start: func(self: performance-timing-handle) -> u64;

    /// `requestStart` attribute — getter.
    get-request-start: func(self: performance-timing-handle) -> u64;

    /// `responseStart` attribute — getter.
    get-response-start: func(self: performance-timing-handle) -> u64;

    /// `responseEnd` attribute — getter.
    get-response-end: func(self: performance-timing-handle) -> u64;

    /// `domLoading` attribute — getter.
    get-dom-loading: func(self: performance-timing-handle) -> u64;

    /// `domInteractive` attribute — getter.
    get-dom-interactive: func(self: performance-timing-handle) -> u64;

    /// `domContentLoadedEventStart` attribute — getter.
    get-dom-content-loaded-event-start: func(self: performance-timing-handle) -> u64;

    /// `domContentLoadedEventEnd` attribute — getter.
    get-dom-content-loaded-event-end: func(self: performance-timing-handle) -> u64;

    /// `domComplete` attribute — getter.
    get-dom-complete: func(self: performance-timing-handle) -> u64;

    /// `loadEventStart` attribute — getter.
    get-load-event-start: func(self: performance-timing-handle) -> u64;

    /// `loadEventEnd` attribute — getter.
    get-load-event-end: func(self: performance-timing-handle) -> u64;

    /// `toJSON()` operation.
    to-json: func(self: performance-timing-handle) -> u64;
}

interface performance-navigation {
    /// Opaque handle to a host-side `PerformanceNavigation` instance.
    type performance-navigation-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: performance-navigation-handle) -> u16;

    /// `redirectCount` attribute — getter.
    get-redirect-count: func(self: performance-navigation-handle) -> u16;

    /// `toJSON()` operation.
    to-json: func(self: performance-navigation-handle) -> u64;
}

interface performance-entry {
    /// Opaque handle to a host-side `PerformanceEntry` instance.
    type performance-entry-handle = u64;

    /// `id` attribute — getter.
    get-id: func(self: performance-entry-handle) -> u64;

    /// `name` attribute — getter.
    get-name: func(self: performance-entry-handle) -> string;

    /// `entryType` attribute — getter.
    get-entry-type: func(self: performance-entry-handle) -> string;

    /// `startTime` attribute — getter.
    get-start-time: func(self: performance-entry-handle) -> f64;

    /// `duration` attribute — getter.
    get-duration: func(self: performance-entry-handle) -> f64;

    /// `navigationId` attribute — getter.
    get-navigation-id: func(self: performance-entry-handle) -> u64;

    /// `toJSON()` operation.
    to-json: func(self: performance-entry-handle) -> u64;
}

interface performance-observer {
    /// Opaque handle to a host-side `PerformanceObserver` instance.
    type performance-observer-handle = u64;

    /// `observe()` operation.
    observe: func(self: performance-observer-handle, options: option<u64>);

    /// `disconnect()` operation.
    disconnect: func(self: performance-observer-handle);

    /// `takeRecords()` operation.
    take-records: func(self: performance-observer-handle) -> u64;

    /// `supportedEntryTypes` attribute — getter.
    get-supported-entry-types: func() -> list<string>;
}

interface performance-observer-entry-list {
    /// Opaque handle to a host-side `PerformanceObserverEntryList` instance.
    type performance-observer-entry-list-handle = u64;

    /// `getEntries()` operation.
    get-entries: func(self: performance-observer-entry-list-handle) -> u64;

    /// `getEntriesByType()` operation.
    get-entries-by-type: func(self: performance-observer-entry-list-handle, %type: string) -> u64;

    /// `getEntriesByName()` operation.
    get-entries-by-name: func(self: performance-observer-entry-list-handle, name: string, %type: option<string>) -> u64;
}

interface performance-resource-timing {
    /// Opaque handle to a host-side `PerformanceResourceTiming` instance.
    type performance-resource-timing-handle = u64;

    /// `initiatorType` attribute — getter.
    get-initiator-type: func(self: performance-resource-timing-handle) -> string;

    /// `deliveryType` attribute — getter.
    get-delivery-type: func(self: performance-resource-timing-handle) -> string;

    /// `nextHopProtocol` attribute — getter.
    get-next-hop-protocol: func(self: performance-resource-timing-handle) -> string;

    /// `workerStart` attribute — getter.
    get-worker-start: func(self: performance-resource-timing-handle) -> f64;

    /// `redirectStart` attribute — getter.
    get-redirect-start: func(self: performance-resource-timing-handle) -> f64;

    /// `redirectEnd` attribute — getter.
    get-redirect-end: func(self: performance-resource-timing-handle) -> f64;

    /// `fetchStart` attribute — getter.
    get-fetch-start: func(self: performance-resource-timing-handle) -> f64;

    /// `domainLookupStart` attribute — getter.
    get-domain-lookup-start: func(self: performance-resource-timing-handle) -> f64;

    /// `domainLookupEnd` attribute — getter.
    get-domain-lookup-end: func(self: performance-resource-timing-handle) -> f64;

    /// `connectStart` attribute — getter.
    get-connect-start: func(self: performance-resource-timing-handle) -> f64;

    /// `connectEnd` attribute — getter.
    get-connect-end: func(self: performance-resource-timing-handle) -> f64;

    /// `secureConnectionStart` attribute — getter.
    get-secure-connection-start: func(self: performance-resource-timing-handle) -> f64;

    /// `requestStart` attribute — getter.
    get-request-start: func(self: performance-resource-timing-handle) -> f64;

    /// `finalResponseHeadersStart` attribute — getter.
    get-final-response-headers-start: func(self: performance-resource-timing-handle) -> f64;

    /// `firstInterimResponseStart` attribute — getter.
    get-first-interim-response-start: func(self: performance-resource-timing-handle) -> f64;

    /// `responseStart` attribute — getter.
    get-response-start: func(self: performance-resource-timing-handle) -> f64;

    /// `responseEnd` attribute — getter.
    get-response-end: func(self: performance-resource-timing-handle) -> f64;

    /// `workerRouterEvaluationStart` attribute — getter.
    get-worker-router-evaluation-start: func(self: performance-resource-timing-handle) -> f64;

    /// `workerCacheLookupStart` attribute — getter.
    get-worker-cache-lookup-start: func(self: performance-resource-timing-handle) -> f64;

    /// `workerMatchedRouterSource` attribute — getter.
    get-worker-matched-router-source: func(self: performance-resource-timing-handle) -> string;

    /// `workerFinalRouterSource` attribute — getter.
    get-worker-final-router-source: func(self: performance-resource-timing-handle) -> string;

    /// `transferSize` attribute — getter.
    get-transfer-size: func(self: performance-resource-timing-handle) -> u64;

    /// `encodedBodySize` attribute — getter.
    get-encoded-body-size: func(self: performance-resource-timing-handle) -> u64;

    /// `decodedBodySize` attribute — getter.
    get-decoded-body-size: func(self: performance-resource-timing-handle) -> u64;

    /// `responseStatus` attribute — getter.
    get-response-status: func(self: performance-resource-timing-handle) -> u16;

    /// `renderBlockingStatus` attribute — getter.
    get-render-blocking-status: func(self: performance-resource-timing-handle) -> u64;

    /// `contentType` attribute — getter.
    get-content-type: func(self: performance-resource-timing-handle) -> string;

    /// `contentEncoding` attribute — getter.
    get-content-encoding: func(self: performance-resource-timing-handle) -> string;

    /// `toJSON()` operation.
    to-json: func(self: performance-resource-timing-handle) -> u64;
}

interface performance-mark {
    /// Opaque handle to a host-side `PerformanceMark` instance.
    type performance-mark-handle = u64;

    /// `detail` attribute — getter.
    get-detail: func(self: performance-mark-handle) -> string;
}

interface performance-measure {
    /// Opaque handle to a host-side `PerformanceMeasure` instance.
    type performance-measure-handle = u64;

    /// `detail` attribute — getter.
    get-detail: func(self: performance-measure-handle) -> string;
}

interface permissions {
    /// Opaque handle to a host-side `Permissions` instance.
    type permissions-handle = u64;

    /// `query()` operation.
    query: func(self: permissions-handle, permission-desc: u64) -> u64;
}

interface permission-status {
    /// Opaque handle to a host-side `PermissionStatus` instance.
    type permission-status-handle = u64;

    /// `state` attribute — getter.
    get-state: func(self: permission-status-handle) -> u64;

    /// `name` attribute — getter.
    get-name: func(self: permission-status-handle) -> string;

    /// `onchange` attribute — getter.
    get-onchange: func(self: permission-status-handle) -> u64;
    /// `onchange` attribute — setter.
    set-onchange: func(self: permission-status-handle, value: u64);
}

interface service-worker {
/// Opaque handle to a ServiceWorker instance.
    type sw-handle = u64;

    script-url: func(handle: sw-handle) -> string;
    post-message: func(handle: sw-handle, message: string, transfer: list<list<u8>>);
}

interface idb-open-db-request {
    /// Opaque handle to a host-side `IDBOpenDBRequest` instance.
    type idb-open-db-request-handle = u64;

    /// `onblocked` attribute — getter.
    get-onblocked: func(self: idb-open-db-request-handle) -> u64;
    /// `onblocked` attribute — setter.
    set-onblocked: func(self: idb-open-db-request-handle, value: u64);

    /// `onupgradeneeded` attribute — getter.
    get-onupgradeneeded: func(self: idb-open-db-request-handle) -> u64;
    /// `onupgradeneeded` attribute — setter.
    set-onupgradeneeded: func(self: idb-open-db-request-handle, value: u64);
}

interface idb-version-change-event {
    /// Opaque handle to a host-side `IDBVersionChangeEvent` instance.
    type idb-version-change-event-handle = u64;

    /// `oldVersion` attribute — getter.
    get-old-version: func(self: idb-version-change-event-handle) -> u64;

    /// `newVersion` attribute — getter.
    get-new-version: func(self: idb-version-change-event-handle) -> option<u64>;
}

interface window-or-worker-global-scope {
    /// Opaque handle to a host-side `WindowOrWorkerGlobalScope` instance.
    type window-or-worker-global-scope-handle = u64;

    /// `indexedDB` attribute — getter.
    get-indexed-db: func(self: window-or-worker-global-scope-handle) -> u64;

    /// `fetch()` operation.
    fetch: func(self: window-or-worker-global-scope-handle, input: u64, init: option<u64>) -> u64;

    /// `performance` attribute — getter.
    get-performance: func(self: window-or-worker-global-scope-handle) -> u64;

    /// `origin` attribute — getter.
    get-origin: func(self: window-or-worker-global-scope-handle) -> string;

    /// `isSecureContext` attribute — getter.
    get-is-secure-context: func(self: window-or-worker-global-scope-handle) -> bool;

    /// `crossOriginIsolated` attribute — getter.
    get-cross-origin-isolated: func(self: window-or-worker-global-scope-handle) -> bool;

    /// `reportError()` operation.
    report-error: func(self: window-or-worker-global-scope-handle, e: string);

    /// `btoa()` operation.
    btoa: func(self: window-or-worker-global-scope-handle, data: string) -> string;

    /// `atob()` operation.
    atob: func(self: window-or-worker-global-scope-handle, data: string) -> string;

    /// `setTimeout()` operation.
    set-timeout: func(self: window-or-worker-global-scope-handle, handler: u64, timeout: option<s32>, arguments: list<string>) -> s32;

    /// `clearTimeout()` operation.
    clear-timeout: func(self: window-or-worker-global-scope-handle, id: option<s32>);

    /// `setInterval()` operation.
    set-interval: func(self: window-or-worker-global-scope-handle, handler: u64, timeout: option<s32>, arguments: list<string>) -> s32;

    /// `clearInterval()` operation.
    clear-interval: func(self: window-or-worker-global-scope-handle, id: option<s32>);

    /// `queueMicrotask()` operation.
    queue-microtask: func(self: window-or-worker-global-scope-handle, callback: u64);

    /// `createImageBitmap()` operation.
    create-image-bitmap: func(self: window-or-worker-global-scope-handle, image: u64, options: option<u64>) -> u64;

    /// `structuredClone()` operation.
    structured-clone: func(self: window-or-worker-global-scope-handle, value: string, options: option<u64>) -> string;

    /// `caches` attribute — getter.
    get-caches: func(self: window-or-worker-global-scope-handle) -> u64;

    /// `crypto` attribute — getter.
    get-crypto: func(self: window-or-worker-global-scope-handle) -> u64;
}

interface idb-key-range {
    /// Opaque handle to a host-side `IDBKeyRange` instance.
    type idb-key-range-handle = u64;

    /// `lower` attribute — getter.
    get-lower: func(self: idb-key-range-handle) -> string;

    /// `upper` attribute — getter.
    get-upper: func(self: idb-key-range-handle) -> string;

    /// `lowerOpen` attribute — getter.
    get-lower-open: func(self: idb-key-range-handle) -> bool;

    /// `upperOpen` attribute — getter.
    get-upper-open: func(self: idb-key-range-handle) -> bool;

    /// `only()` operation.
    only: func(value: string) -> u64;

    /// `lowerBound()` operation.
    lower-bound: func(lower: string, open: option<bool>) -> u64;

    /// `upperBound()` operation.
    upper-bound: func(upper: string, open: option<bool>) -> u64;

    /// `bound()` operation.
    bound: func(lower: string, upper: string, lower-open: option<bool>, upper-open: option<bool>) -> u64;

    /// `includes()` operation.
    includes: func(self: idb-key-range-handle, key: string) -> bool;
}

interface idb-record {
    /// Opaque handle to a host-side `IDBRecord` instance.
    type idb-record-handle = u64;

    /// `key` attribute — getter.
    get-key: func(self: idb-record-handle) -> string;

    /// `primaryKey` attribute — getter.
    get-primary-key: func(self: idb-record-handle) -> string;

    /// `value` attribute — getter.
    get-value: func(self: idb-record-handle) -> string;
}

interface idb-cursor-with-value {
    /// Opaque handle to a host-side `IDBCursorWithValue` instance.
    type idb-cursor-with-value-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: idb-cursor-with-value-handle) -> string;
}

interface navigator-storage {
    /// Opaque handle to a host-side `NavigatorStorage` instance.
    type navigator-storage-handle = u64;

    /// `storage` attribute — getter.
    get-storage: func(self: navigator-storage-handle) -> u64;
}

interface storage-manager {
    /// Opaque handle to a host-side `StorageManager` instance.
    type storage-manager-handle = u64;

    /// `persisted()` operation.
    persisted: func(self: storage-manager-handle) -> u64;

    /// `persist()` operation.
    persist: func(self: storage-manager-handle) -> u64;

    /// `estimate()` operation.
    estimate: func(self: storage-manager-handle) -> u64;
}

interface navigator-locks {
    /// Opaque handle to a host-side `NavigatorLocks` instance.
    type navigator-locks-handle = u64;

    /// `locks` attribute — getter.
    get-locks: func(self: navigator-locks-handle) -> u64;
}

interface lock-manager {
    /// Opaque handle to a host-side `LockManager` instance.
    type lock-manager-handle = u64;

    /// `request()` operation.
    request: func(self: lock-manager-handle, name: string, callback: u64) -> u64;

    /// `query()` operation.
    query: func(self: lock-manager-handle) -> u64;
}

interface lock {
    /// Opaque handle to a host-side `Lock` instance.
    type lock-handle = u64;

    /// `name` attribute — getter.
    get-name: func(self: lock-handle) -> string;

    /// `mode` attribute — getter.
    get-mode: func(self: lock-handle) -> u64;
}

interface svg-element {
    /// Opaque handle to a host-side `SVGElement` instance.
    type svg-element-handle = u64;

    /// `className` attribute — getter.
    get-class-name: func(self: svg-element-handle) -> u64;

    /// `ownerSVGElement` attribute — getter.
    get-owner-svg-element: func(self: svg-element-handle) -> option<u64>;

    /// `viewportElement` attribute — getter.
    get-viewport-element: func(self: svg-element-handle) -> option<u64>;
}

interface svg-graphics-element {
    /// Opaque handle to a host-side `SVGGraphicsElement` instance.

    use types.{dom-rect};

    type svg-graphics-element-handle = u64;

    /// `transform` attribute — getter.
    get-transform: func(self: svg-graphics-element-handle) -> u64;

    /// `getBBox()` operation.
    get-b-box: func(self: svg-graphics-element-handle, options: option<u64>) -> dom-rect;

    /// `getCTM()` operation.
    get-ctm: func(self: svg-graphics-element-handle) -> option<u64>;

    /// `getScreenCTM()` operation.
    get-screen-ctm: func(self: svg-graphics-element-handle) -> option<u64>;
}

interface svg-geometry-element {
    /// Opaque handle to a host-side `SVGGeometryElement` instance.
    type svg-geometry-element-handle = u64;

    /// `pathLength` attribute — getter.
    get-path-length: func(self: svg-geometry-element-handle) -> u64;

    /// `isPointInFill()` operation.
    is-point-in-fill: func(self: svg-geometry-element-handle, point: option<u64>) -> bool;

    /// `isPointInStroke()` operation.
    is-point-in-stroke: func(self: svg-geometry-element-handle, point: option<u64>) -> bool;

    /// `getTotalLength()` operation.
    get-total-length: func(self: svg-geometry-element-handle) -> f32;

    /// `getPointAtLength()` operation.
    get-point-at-length: func(self: svg-geometry-element-handle, distance: f32) -> u64;
}

interface svg-number {
    /// Opaque handle to a host-side `SVGNumber` instance.
    type svg-number-handle = u64;

    /// `value` attribute — getter.
    get-value: func(self: svg-number-handle) -> f32;
    /// `value` attribute — setter.
    set-value: func(self: svg-number-handle, value: f32);
}

interface svg-length {
    /// Opaque handle to a host-side `SVGLength` instance.
    type svg-length-handle = u64;

    /// `unitType` attribute — getter.
    get-unit-type: func(self: svg-length-handle) -> u16;

    /// `value` attribute — getter.
    get-value: func(self: svg-length-handle) -> f32;
    /// `value` attribute — setter.
    set-value: func(self: svg-length-handle, value: f32);

    /// `valueInSpecifiedUnits` attribute — getter.
    get-value-in-specified-units: func(self: svg-length-handle) -> f32;
    /// `valueInSpecifiedUnits` attribute — setter.
    set-value-in-specified-units: func(self: svg-length-handle, value: f32);

    /// `valueAsString` attribute — getter.
    get-value-as-string: func(self: svg-length-handle) -> string;
    /// `valueAsString` attribute — setter.
    set-value-as-string: func(self: svg-length-handle, value: string);

    /// `newValueSpecifiedUnits()` operation.
    new-value-specified-units: func(self: svg-length-handle, unit-type: u16, value-in-specified-units: f32);

    /// `convertToSpecifiedUnits()` operation.
    convert-to-specified-units: func(self: svg-length-handle, unit-type: u16);
}

interface svg-angle {
    /// Opaque handle to a host-side `SVGAngle` instance.
    type svg-angle-handle = u64;

    /// `unitType` attribute — getter.
    get-unit-type: func(self: svg-angle-handle) -> u16;

    /// `value` attribute — getter.
    get-value: func(self: svg-angle-handle) -> f32;
    /// `value` attribute — setter.
    set-value: func(self: svg-angle-handle, value: f32);

    /// `valueInSpecifiedUnits` attribute — getter.
    get-value-in-specified-units: func(self: svg-angle-handle) -> f32;
    /// `valueInSpecifiedUnits` attribute — setter.
    set-value-in-specified-units: func(self: svg-angle-handle, value: f32);

    /// `valueAsString` attribute — getter.
    get-value-as-string: func(self: svg-angle-handle) -> string;
    /// `valueAsString` attribute — setter.
    set-value-as-string: func(self: svg-angle-handle, value: string);

    /// `newValueSpecifiedUnits()` operation.
    new-value-specified-units: func(self: svg-angle-handle, unit-type: u16, value-in-specified-units: f32);

    /// `convertToSpecifiedUnits()` operation.
    convert-to-specified-units: func(self: svg-angle-handle, unit-type: u16);
}

interface svg-number-list {
    /// Opaque handle to a host-side `SVGNumberList` instance.
    type svg-number-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: svg-number-list-handle) -> u32;

    /// `numberOfItems` attribute — getter.
    get-number-of-items: func(self: svg-number-list-handle) -> u32;

    /// `clear()` operation.
    clear: func(self: svg-number-list-handle);

    /// `initialize()` operation.
    initialize: func(self: svg-number-list-handle, new-item: u64) -> u64;

    /// `getItem()` operation.
    get-item: func(self: svg-number-list-handle, index: u32) -> u64;

    /// `insertItemBefore()` operation.
    insert-item-before: func(self: svg-number-list-handle, new-item: u64, index: u32) -> u64;

    /// `replaceItem()` operation.
    replace-item: func(self: svg-number-list-handle, new-item: u64, index: u32) -> u64;

    /// `removeItem()` operation.
    remove-item: func(self: svg-number-list-handle, index: u32) -> u64;

    /// `appendItem()` operation.
    append-item: func(self: svg-number-list-handle, new-item: u64) -> u64;

    /// `undefined()` operation.
    undefined: func(self: svg-number-list-handle, index: u32, new-item: u64);
}

interface svg-length-list {
    /// Opaque handle to a host-side `SVGLengthList` instance.
    type svg-length-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: svg-length-list-handle) -> u32;

    /// `numberOfItems` attribute — getter.
    get-number-of-items: func(self: svg-length-list-handle) -> u32;

    /// `clear()` operation.
    clear: func(self: svg-length-list-handle);

    /// `initialize()` operation.
    initialize: func(self: svg-length-list-handle, new-item: u64) -> u64;

    /// `getItem()` operation.
    get-item: func(self: svg-length-list-handle, index: u32) -> u64;

    /// `insertItemBefore()` operation.
    insert-item-before: func(self: svg-length-list-handle, new-item: u64, index: u32) -> u64;

    /// `replaceItem()` operation.
    replace-item: func(self: svg-length-list-handle, new-item: u64, index: u32) -> u64;

    /// `removeItem()` operation.
    remove-item: func(self: svg-length-list-handle, index: u32) -> u64;

    /// `appendItem()` operation.
    append-item: func(self: svg-length-list-handle, new-item: u64) -> u64;

    /// `undefined()` operation.
    undefined: func(self: svg-length-list-handle, index: u32, new-item: u64);
}

interface svg-string-list {
    /// Opaque handle to a host-side `SVGStringList` instance.
    type svg-string-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: svg-string-list-handle) -> u32;

    /// `numberOfItems` attribute — getter.
    get-number-of-items: func(self: svg-string-list-handle) -> u32;

    /// `clear()` operation.
    clear: func(self: svg-string-list-handle);

    /// `initialize()` operation.
    initialize: func(self: svg-string-list-handle, new-item: string) -> string;

    /// `getItem()` operation.
    get-item: func(self: svg-string-list-handle, index: u32) -> string;

    /// `insertItemBefore()` operation.
    insert-item-before: func(self: svg-string-list-handle, new-item: string, index: u32) -> string;

    /// `replaceItem()` operation.
    replace-item: func(self: svg-string-list-handle, new-item: string, index: u32) -> string;

    /// `removeItem()` operation.
    remove-item: func(self: svg-string-list-handle, index: u32) -> string;

    /// `appendItem()` operation.
    append-item: func(self: svg-string-list-handle, new-item: string) -> string;

    /// `undefined()` operation.
    undefined: func(self: svg-string-list-handle, index: u32, new-item: string);
}

interface svg-animated-boolean {
    /// Opaque handle to a host-side `SVGAnimatedBoolean` instance.
    type svg-animated-boolean-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-boolean-handle) -> bool;
    /// `baseVal` attribute — setter.
    set-base-val: func(self: svg-animated-boolean-handle, value: bool);

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-boolean-handle) -> bool;
}

interface svg-animated-enumeration {
    /// Opaque handle to a host-side `SVGAnimatedEnumeration` instance.
    type svg-animated-enumeration-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-enumeration-handle) -> u16;
    /// `baseVal` attribute — setter.
    set-base-val: func(self: svg-animated-enumeration-handle, value: u16);

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-enumeration-handle) -> u16;
}

interface svg-animated-integer {
    /// Opaque handle to a host-side `SVGAnimatedInteger` instance.
    type svg-animated-integer-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-integer-handle) -> s32;
    /// `baseVal` attribute — setter.
    set-base-val: func(self: svg-animated-integer-handle, value: s32);

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-integer-handle) -> s32;
}

interface svg-animated-number {
    /// Opaque handle to a host-side `SVGAnimatedNumber` instance.
    type svg-animated-number-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-number-handle) -> f32;
    /// `baseVal` attribute — setter.
    set-base-val: func(self: svg-animated-number-handle, value: f32);

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-number-handle) -> f32;
}

interface svg-animated-length {
    /// Opaque handle to a host-side `SVGAnimatedLength` instance.
    type svg-animated-length-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-length-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-length-handle) -> u64;
}

interface svg-animated-angle {
    /// Opaque handle to a host-side `SVGAnimatedAngle` instance.
    type svg-animated-angle-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-angle-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-angle-handle) -> u64;
}

interface svg-animated-string {
    /// Opaque handle to a host-side `SVGAnimatedString` instance.
    type svg-animated-string-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-string-handle) -> string;
    /// `baseVal` attribute — setter.
    set-base-val: func(self: svg-animated-string-handle, value: string);

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-string-handle) -> string;
}

interface svg-animated-rect {
    /// Opaque handle to a host-side `SVGAnimatedRect` instance.

    use types.{dom-rect};

    type svg-animated-rect-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-rect-handle) -> dom-rect;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-rect-handle) -> dom-rect;
}

interface svg-animated-number-list {
    /// Opaque handle to a host-side `SVGAnimatedNumberList` instance.
    type svg-animated-number-list-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-number-list-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-number-list-handle) -> u64;
}

interface svg-animated-length-list {
    /// Opaque handle to a host-side `SVGAnimatedLengthList` instance.
    type svg-animated-length-list-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-length-list-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-length-list-handle) -> u64;
}

interface svg-tests {
    /// Opaque handle to a host-side `SVGTests` instance.
    type svg-tests-handle = u64;

    /// `requiredExtensions` attribute — getter.
    get-required-extensions: func(self: svg-tests-handle) -> u64;

    /// `systemLanguage` attribute — getter.
    get-system-language: func(self: svg-tests-handle) -> u64;
}

interface svg-fit-to-view-box {
    /// Opaque handle to a host-side `SVGFitToViewBox` instance.
    type svg-fit-to-view-box-handle = u64;

    /// `viewBox` attribute — getter.
    get-view-box: func(self: svg-fit-to-view-box-handle) -> u64;

    /// `preserveAspectRatio` attribute — getter.
    get-preserve-aspect-ratio: func(self: svg-fit-to-view-box-handle) -> u64;
}

interface svguri-reference {
    /// Opaque handle to a host-side `SVGURIReference` instance.
    type svguri-reference-handle = u64;

    /// `href` attribute — getter.
    get-href: func(self: svguri-reference-handle) -> u64;
}

interface document {
    /// Opaque handle to a host-side `Document` instance.
    type document-handle = u64;

    /// `rootElement` attribute — getter.
    get-root-element: func() -> option<u64>;

    /// `elementFromPoint()` operation.
    element-from-point: func(x: f64, y: f64) -> option<u64>;

    /// `elementsFromPoint()` operation.
    elements-from-point: func(x: f64, y: f64) -> list<u64>;

    /// `caretPositionFromPoint()` operation.
    caret-position-from-point: func(x: f64, y: f64, options: option<u64>) -> option<u64>;

    /// `scrollingElement` attribute — getter.
    get-scrolling-element: func() -> option<u64>;

    /// `implementation` attribute — getter.
    get-implementation: func() -> u64;

    /// `URL` attribute — getter.
    get-url: func() -> string;

    /// `documentURI` attribute — getter.
    get-document-uri: func() -> string;

    /// `compatMode` attribute — getter.
    get-compat-mode: func() -> string;

    /// `characterSet` attribute — getter.
    get-character-set: func() -> string;

    /// `charset` attribute — getter.
    get-charset: func() -> string;

    /// `inputEncoding` attribute — getter.
    get-input-encoding: func() -> string;

    /// `contentType` attribute — getter.
    get-content-type: func() -> string;

    /// `doctype` attribute — getter.
    get-doctype: func() -> option<u64>;

    /// `documentElement` attribute — getter.
    get-document-element: func() -> option<u64>;

    /// `getElementsByTagName()` operation.
    get-elements-by-tag-name: func(qualified-name: string) -> u64;

    /// `getElementsByTagNameNS()` operation.
    get-elements-by-tag-name-ns: func(namespace: option<string>, local-name: string) -> u64;

    /// `getElementsByClassName()` operation.
    get-elements-by-class-name: func(class-names: string) -> u64;

    /// `createElement()` operation.
    create-element: func(local-name: string, options: option<string>) -> u64;

    /// `createElementNS()` operation.
    create-element-ns: func(namespace: option<string>, qualified-name: string, options: option<string>) -> u64;

    /// `createDocumentFragment()` operation.
    create-document-fragment: func() -> u64;

    /// `createTextNode()` operation.
    create-text-node: func(data: string) -> u64;

    /// `createCDATASection()` operation.
    create-cdata-section: func(data: string) -> u64;

    /// `createComment()` operation.
    create-comment: func(data: string) -> u64;

    /// `createProcessingInstruction()` operation.
    create-processing-instruction: func(target: string, data: string) -> u64;

    /// `importNode()` operation.
    import-node: func(node: u64, options: option<bool>) -> u64;

    /// `adoptNode()` operation.
    adopt-node: func(node: u64) -> u64;

    /// `createAttribute()` operation.
    create-attribute: func(local-name: string) -> u64;

    /// `createAttributeNS()` operation.
    create-attribute-ns: func(namespace: option<string>, qualified-name: string) -> u64;

    /// `createEvent()` operation.
    create-event: func(%interface: string) -> u64;

    /// `createRange()` operation.
    create-range: func() -> u64;

    /// `createNodeIterator()` operation.
    create-node-iterator: func(root: u64, what-to-show: option<u32>, filter: option<u64>) -> u64;

    /// `createTreeWalker()` operation.
    create-tree-walker: func(root: u64, what-to-show: option<u32>, filter: option<u64>) -> u64;

    /// `fullscreenEnabled` attribute — getter.
    get-fullscreen-enabled: func() -> bool;

    /// `fullscreen` attribute — getter.
    get-fullscreen: func() -> bool;

    /// `exitFullscreen()` operation.
    exit-fullscreen: func() -> u64;

    /// `onfullscreenchange` attribute — getter.
    get-onfullscreenchange: func() -> u64;
    /// `onfullscreenchange` attribute — setter.
    set-onfullscreenchange: func(value: u64);

    /// `onfullscreenerror` attribute — getter.
    get-onfullscreenerror: func() -> u64;
    /// `onfullscreenerror` attribute — setter.
    set-onfullscreenerror: func(value: u64);

    /// `parseHTMLUnsafe()` operation.
    parse-html-unsafe: func(html: string) -> u64;

    /// `location` attribute — getter.
    get-location: func() -> option<u64>;

    /// `domain` attribute — getter.
    get-domain: func() -> string;
    /// `domain` attribute — setter.
    set-domain: func(value: string);

    /// `referrer` attribute — getter.
    get-referrer: func() -> string;

    /// `cookie` attribute — getter.
    get-cookie: func() -> string;
    /// `cookie` attribute — setter.
    set-cookie: func(value: string);

    /// `lastModified` attribute — getter.
    get-last-modified: func() -> string;

    /// `readyState` attribute — getter.
    get-ready-state: func() -> u64;

    /// `object()` operation.
    object: func(name: string);

    /// `title` attribute — getter.
    get-title: func() -> string;
    /// `title` attribute — setter.
    set-title: func(value: string);

    /// `dir` attribute — getter.
    get-dir: func() -> string;
    /// `dir` attribute — setter.
    set-dir: func(value: string);

    /// `body` attribute — getter.
    get-body: func() -> option<u64>;
    /// `body` attribute — setter.
    set-body: func(value: option<u64>);

    /// `head` attribute — getter.
    get-head: func() -> option<u64>;

    /// `images` attribute — getter.
    get-images: func() -> u64;

    /// `embeds` attribute — getter.
    get-embeds: func() -> u64;

    /// `plugins` attribute — getter.
    get-plugins: func() -> u64;

    /// `links` attribute — getter.
    get-links: func() -> u64;

    /// `forms` attribute — getter.
    get-forms: func() -> u64;

    /// `scripts` attribute — getter.
    get-scripts: func() -> u64;

    /// `getElementsByName()` operation.
    get-elements-by-name: func(element-name: string) -> u64;

    /// `currentScript` attribute — getter.
    get-current-script: func() -> option<u64>;

    /// `open()` operation.
    open: func(unused1: option<string>, unused2: option<string>) -> u64;

    /// `close()` operation.
    close: func();

    /// `write()` operation.
    write: func(text: list<string>);

    /// `writeln()` operation.
    writeln: func(text: list<string>);

    /// `defaultView` attribute — getter.
    get-default-view: func() -> option<u64>;

    /// `hasFocus()` operation.
    has-focus: func() -> bool;

    /// `designMode` attribute — getter.
    get-design-mode: func() -> string;
    /// `designMode` attribute — setter.
    set-design-mode: func(value: string);

    /// `execCommand()` operation.
    exec-command: func(command-id: string, show-ui: option<bool>, value: option<string>) -> bool;

    /// `queryCommandEnabled()` operation.
    query-command-enabled: func(command-id: string) -> bool;

    /// `queryCommandIndeterm()` operation.
    query-command-indeterm: func(command-id: string) -> bool;

    /// `queryCommandState()` operation.
    query-command-state: func(command-id: string) -> bool;

    /// `queryCommandSupported()` operation.
    query-command-supported: func(command-id: string) -> bool;

    /// `queryCommandValue()` operation.
    query-command-value: func(command-id: string) -> string;

    /// `hidden` attribute — getter.
    get-hidden: func() -> bool;

    /// `visibilityState` attribute — getter.
    get-visibility-state: func() -> u64;

    /// `onreadystatechange` attribute — getter.
    get-onreadystatechange: func() -> u64;
    /// `onreadystatechange` attribute — setter.
    set-onreadystatechange: func(value: u64);

    /// `onvisibilitychange` attribute — getter.
    get-onvisibilitychange: func() -> u64;
    /// `onvisibilitychange` attribute — setter.
    set-onvisibilitychange: func(value: u64);

    /// `fgColor` attribute — getter.
    get-fg-color: func() -> string;
    /// `fgColor` attribute — setter.
    set-fg-color: func(value: string);

    /// `linkColor` attribute — getter.
    get-link-color: func() -> string;
    /// `linkColor` attribute — setter.
    set-link-color: func(value: string);

    /// `vlinkColor` attribute — getter.
    get-vlink-color: func() -> string;
    /// `vlinkColor` attribute — setter.
    set-vlink-color: func(value: string);

    /// `alinkColor` attribute — getter.
    get-alink-color: func() -> string;
    /// `alinkColor` attribute — setter.
    set-alink-color: func(value: string);

    /// `bgColor` attribute — getter.
    get-bg-color: func() -> string;
    /// `bgColor` attribute — setter.
    set-bg-color: func(value: string);

    /// `anchors` attribute — getter.
    get-anchors: func() -> u64;

    /// `applets` attribute — getter.
    get-applets: func() -> u64;

    /// `clear()` operation.
    clear: func();

    /// `captureEvents()` operation.
    capture-events: func();

    /// `releaseEvents()` operation.
    release-events: func();

    /// `all` attribute — getter.
    get-all: func() -> u64;

    /// `getSelection()` operation.
    get-selection: func() -> option<u64>;
}

interface svgsvg-element {
    /// Opaque handle to a host-side `SVGSVGElement` instance.

    use types.{dom-rect};

    type svgsvg-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svgsvg-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svgsvg-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svgsvg-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svgsvg-element-handle) -> u64;

    /// `currentScale` attribute — getter.
    get-current-scale: func(self: svgsvg-element-handle) -> f32;
    /// `currentScale` attribute — setter.
    set-current-scale: func(self: svgsvg-element-handle, value: f32);

    /// `currentTranslate` attribute — getter.
    get-current-translate: func(self: svgsvg-element-handle) -> u64;

    /// `getIntersectionList()` operation.
    get-intersection-list: func(self: svgsvg-element-handle, rect: dom-rect, reference-element: option<u64>) -> u64;

    /// `getEnclosureList()` operation.
    get-enclosure-list: func(self: svgsvg-element-handle, rect: dom-rect, reference-element: option<u64>) -> u64;

    /// `checkIntersection()` operation.
    check-intersection: func(self: svgsvg-element-handle, element: u64, rect: dom-rect) -> bool;

    /// `checkEnclosure()` operation.
    check-enclosure: func(self: svgsvg-element-handle, element: u64, rect: dom-rect) -> bool;

    /// `deselectAll()` operation.
    deselect-all: func(self: svgsvg-element-handle);

    /// `createSVGNumber()` operation.
    create-svg-number: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGLength()` operation.
    create-svg-length: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGAngle()` operation.
    create-svg-angle: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGPoint()` operation.
    create-svg-point: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGMatrix()` operation.
    create-svg-matrix: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGRect()` operation.
    create-svg-rect: func(self: svgsvg-element-handle) -> dom-rect;

    /// `createSVGTransform()` operation.
    create-svg-transform: func(self: svgsvg-element-handle) -> u64;

    /// `createSVGTransformFromMatrix()` operation.
    create-svg-transform-from-matrix: func(self: svgsvg-element-handle, matrix: option<u64>) -> u64;

    /// `getElementById()` operation.
    get-element-by-id: func(self: svgsvg-element-handle, element-id: string) -> option<u64>;

    /// `suspendRedraw()` operation.
    suspend-redraw: func(self: svgsvg-element-handle, max-wait-milliseconds: u32) -> u32;

    /// `unsuspendRedraw()` operation.
    unsuspend-redraw: func(self: svgsvg-element-handle, suspend-handle-id: u32);

    /// `unsuspendRedrawAll()` operation.
    unsuspend-redraw-all: func(self: svgsvg-element-handle);

    /// `forceRedraw()` operation.
    force-redraw: func(self: svgsvg-element-handle);
}

interface svg-use-element {
    /// Opaque handle to a host-side `SVGUseElement` instance.
    type svg-use-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-use-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-use-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svg-use-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svg-use-element-handle) -> u64;

    /// `instanceRoot` attribute — getter.
    get-instance-root: func(self: svg-use-element-handle) -> option<u64>;

    /// `animatedInstanceRoot` attribute — getter.
    get-animated-instance-root: func(self: svg-use-element-handle) -> option<u64>;
}

interface svg-element-instance {
    /// Opaque handle to a host-side `SVGElementInstance` instance.
    type svg-element-instance-handle = u64;

    /// `correspondingElement` attribute — getter.
    get-corresponding-element: func(self: svg-element-instance-handle) -> option<u64>;

    /// `correspondingUseElement` attribute — getter.
    get-corresponding-use-element: func(self: svg-element-instance-handle) -> option<u64>;
}

interface shadow-animation {
    /// Opaque handle to a host-side `ShadowAnimation` instance.
    type shadow-animation-handle = u64;

    /// `sourceAnimation` attribute — getter.
    get-source-animation: func(self: shadow-animation-handle) -> u64;
}

interface get-svg-document {
    /// Opaque handle to a host-side `GetSVGDocument` instance.
    type get-svg-document-handle = u64;

    /// `getSVGDocument()` operation.
    get-svg-document: func(self: get-svg-document-handle) -> u64;
}

interface svg-style-element {
    /// Opaque handle to a host-side `SVGStyleElement` instance.
    type svg-style-element-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: svg-style-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: svg-style-element-handle, value: string);

    /// `media` attribute — getter.
    get-media: func(self: svg-style-element-handle) -> string;
    /// `media` attribute — setter.
    set-media: func(self: svg-style-element-handle, value: string);

    /// `title` attribute — getter.
    get-title: func(self: svg-style-element-handle) -> string;
    /// `title` attribute — setter.
    set-title: func(self: svg-style-element-handle, value: string);

    /// `disabled` attribute — getter.
    get-disabled: func(self: svg-style-element-handle) -> bool;
    /// `disabled` attribute — setter.
    set-disabled: func(self: svg-style-element-handle, value: bool);
}

interface svg-transform {
    /// Opaque handle to a host-side `SVGTransform` instance.
    type svg-transform-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: svg-transform-handle) -> u16;

    /// `matrix` attribute — getter.
    get-matrix: func(self: svg-transform-handle) -> u64;

    /// `angle` attribute — getter.
    get-angle: func(self: svg-transform-handle) -> f32;

    /// `setMatrix()` operation.
    set-matrix: func(self: svg-transform-handle, matrix: option<u64>);

    /// `setTranslate()` operation.
    set-translate: func(self: svg-transform-handle, tx: f32, ty: f32);

    /// `setScale()` operation.
    set-scale: func(self: svg-transform-handle, sx: f32, sy: f32);

    /// `setRotate()` operation.
    set-rotate: func(self: svg-transform-handle, angle: f32, cx: f32, cy: f32);

    /// `setSkewX()` operation.
    set-skew-x: func(self: svg-transform-handle, angle: f32);

    /// `setSkewY()` operation.
    set-skew-y: func(self: svg-transform-handle, angle: f32);
}

interface svg-transform-list {
    /// Opaque handle to a host-side `SVGTransformList` instance.
    type svg-transform-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: svg-transform-list-handle) -> u32;

    /// `numberOfItems` attribute — getter.
    get-number-of-items: func(self: svg-transform-list-handle) -> u32;

    /// `clear()` operation.
    clear: func(self: svg-transform-list-handle);

    /// `initialize()` operation.
    initialize: func(self: svg-transform-list-handle, new-item: u64) -> u64;

    /// `getItem()` operation.
    get-item: func(self: svg-transform-list-handle, index: u32) -> u64;

    /// `insertItemBefore()` operation.
    insert-item-before: func(self: svg-transform-list-handle, new-item: u64, index: u32) -> u64;

    /// `replaceItem()` operation.
    replace-item: func(self: svg-transform-list-handle, new-item: u64, index: u32) -> u64;

    /// `removeItem()` operation.
    remove-item: func(self: svg-transform-list-handle, index: u32) -> u64;

    /// `appendItem()` operation.
    append-item: func(self: svg-transform-list-handle, new-item: u64) -> u64;

    /// `undefined()` operation.
    undefined: func(self: svg-transform-list-handle, index: u32, new-item: u64);

    /// `createSVGTransformFromMatrix()` operation.
    create-svg-transform-from-matrix: func(self: svg-transform-list-handle, matrix: option<u64>) -> u64;

    /// `consolidate()` operation.
    consolidate: func(self: svg-transform-list-handle) -> option<u64>;
}

interface svg-animated-transform-list {
    /// Opaque handle to a host-side `SVGAnimatedTransformList` instance.
    type svg-animated-transform-list-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-transform-list-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-transform-list-handle) -> u64;
}

interface svg-preserve-aspect-ratio {
    /// Opaque handle to a host-side `SVGPreserveAspectRatio` instance.
    type svg-preserve-aspect-ratio-handle = u64;

    /// `align` attribute — getter.
    get-align: func(self: svg-preserve-aspect-ratio-handle) -> u16;
    /// `align` attribute — setter.
    set-align: func(self: svg-preserve-aspect-ratio-handle, value: u16);

    /// `meetOrSlice` attribute — getter.
    get-meet-or-slice: func(self: svg-preserve-aspect-ratio-handle) -> u16;
    /// `meetOrSlice` attribute — setter.
    set-meet-or-slice: func(self: svg-preserve-aspect-ratio-handle, value: u16);
}

interface svg-animated-preserve-aspect-ratio {
    /// Opaque handle to a host-side `SVGAnimatedPreserveAspectRatio` instance.
    type svg-animated-preserve-aspect-ratio-handle = u64;

    /// `baseVal` attribute — getter.
    get-base-val: func(self: svg-animated-preserve-aspect-ratio-handle) -> u64;

    /// `animVal` attribute — getter.
    get-anim-val: func(self: svg-animated-preserve-aspect-ratio-handle) -> u64;
}

interface svg-rect-element {
    /// Opaque handle to a host-side `SVGRectElement` instance.
    type svg-rect-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-rect-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-rect-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svg-rect-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svg-rect-element-handle) -> u64;

    /// `rx` attribute — getter.
    get-rx: func(self: svg-rect-element-handle) -> u64;

    /// `ry` attribute — getter.
    get-ry: func(self: svg-rect-element-handle) -> u64;
}

interface svg-circle-element {
    /// Opaque handle to a host-side `SVGCircleElement` instance.
    type svg-circle-element-handle = u64;

    /// `cx` attribute — getter.
    get-cx: func(self: svg-circle-element-handle) -> u64;

    /// `cy` attribute — getter.
    get-cy: func(self: svg-circle-element-handle) -> u64;

    /// `r` attribute — getter.
    get-r: func(self: svg-circle-element-handle) -> u64;
}

interface svg-ellipse-element {
    /// Opaque handle to a host-side `SVGEllipseElement` instance.
    type svg-ellipse-element-handle = u64;

    /// `cx` attribute — getter.
    get-cx: func(self: svg-ellipse-element-handle) -> u64;

    /// `cy` attribute — getter.
    get-cy: func(self: svg-ellipse-element-handle) -> u64;

    /// `rx` attribute — getter.
    get-rx: func(self: svg-ellipse-element-handle) -> u64;

    /// `ry` attribute — getter.
    get-ry: func(self: svg-ellipse-element-handle) -> u64;
}

interface svg-line-element {
    /// Opaque handle to a host-side `SVGLineElement` instance.
    type svg-line-element-handle = u64;

    /// `x1` attribute — getter.
    get-x1: func(self: svg-line-element-handle) -> u64;

    /// `y1` attribute — getter.
    get-y1: func(self: svg-line-element-handle) -> u64;

    /// `x2` attribute — getter.
    get-x2: func(self: svg-line-element-handle) -> u64;

    /// `y2` attribute — getter.
    get-y2: func(self: svg-line-element-handle) -> u64;
}

interface svg-animated-points {
    /// Opaque handle to a host-side `SVGAnimatedPoints` instance.
    type svg-animated-points-handle = u64;

    /// `points` attribute — getter.
    get-points: func(self: svg-animated-points-handle) -> u64;

    /// `animatedPoints` attribute — getter.
    get-animated-points: func(self: svg-animated-points-handle) -> u64;
}

interface svg-point-list {
    /// Opaque handle to a host-side `SVGPointList` instance.
    type svg-point-list-handle = u64;

    /// `length` attribute — getter.
    get-length: func(self: svg-point-list-handle) -> u32;

    /// `numberOfItems` attribute — getter.
    get-number-of-items: func(self: svg-point-list-handle) -> u32;

    /// `clear()` operation.
    clear: func(self: svg-point-list-handle);

    /// `initialize()` operation.
    initialize: func(self: svg-point-list-handle, new-item: u64) -> u64;

    /// `getItem()` operation.
    get-item: func(self: svg-point-list-handle, index: u32) -> u64;

    /// `insertItemBefore()` operation.
    insert-item-before: func(self: svg-point-list-handle, new-item: u64, index: u32) -> u64;

    /// `replaceItem()` operation.
    replace-item: func(self: svg-point-list-handle, new-item: u64, index: u32) -> u64;

    /// `removeItem()` operation.
    remove-item: func(self: svg-point-list-handle, index: u32) -> u64;

    /// `appendItem()` operation.
    append-item: func(self: svg-point-list-handle, new-item: u64) -> u64;

    /// `undefined()` operation.
    undefined: func(self: svg-point-list-handle, index: u32, new-item: u64);
}

interface svg-text-content-element {
    /// Opaque handle to a host-side `SVGTextContentElement` instance.

    use types.{dom-rect};

    type svg-text-content-element-handle = u64;

    /// `textLength` attribute — getter.
    get-text-length: func(self: svg-text-content-element-handle) -> u64;

    /// `lengthAdjust` attribute — getter.
    get-length-adjust: func(self: svg-text-content-element-handle) -> u64;

    /// `getNumberOfChars()` operation.
    get-number-of-chars: func(self: svg-text-content-element-handle) -> s32;

    /// `getComputedTextLength()` operation.
    get-computed-text-length: func(self: svg-text-content-element-handle) -> f32;

    /// `getSubStringLength()` operation.
    get-sub-string-length: func(self: svg-text-content-element-handle, charnum: u32, nchars: u32) -> f32;

    /// `getStartPositionOfChar()` operation.
    get-start-position-of-char: func(self: svg-text-content-element-handle, charnum: u32) -> u64;

    /// `getEndPositionOfChar()` operation.
    get-end-position-of-char: func(self: svg-text-content-element-handle, charnum: u32) -> u64;

    /// `getExtentOfChar()` operation.
    get-extent-of-char: func(self: svg-text-content-element-handle, charnum: u32) -> dom-rect;

    /// `getRotationOfChar()` operation.
    get-rotation-of-char: func(self: svg-text-content-element-handle, charnum: u32) -> f32;

    /// `getCharNumAtPosition()` operation.
    get-char-num-at-position: func(self: svg-text-content-element-handle, point: option<u64>) -> s32;

    /// `selectSubString()` operation.
    select-sub-string: func(self: svg-text-content-element-handle, charnum: u32, nchars: u32);
}

interface svg-text-positioning-element {
    /// Opaque handle to a host-side `SVGTextPositioningElement` instance.
    type svg-text-positioning-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-text-positioning-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-text-positioning-element-handle) -> u64;

    /// `dx` attribute — getter.
    get-dx: func(self: svg-text-positioning-element-handle) -> u64;

    /// `dy` attribute — getter.
    get-dy: func(self: svg-text-positioning-element-handle) -> u64;

    /// `rotate` attribute — getter.
    get-rotate: func(self: svg-text-positioning-element-handle) -> u64;
}

interface svg-text-path-element {
    /// Opaque handle to a host-side `SVGTextPathElement` instance.
    type svg-text-path-element-handle = u64;

    /// `startOffset` attribute — getter.
    get-start-offset: func(self: svg-text-path-element-handle) -> u64;

    /// `method` attribute — getter.
    get-method: func(self: svg-text-path-element-handle) -> u64;

    /// `spacing` attribute — getter.
    get-spacing: func(self: svg-text-path-element-handle) -> u64;
}

interface svg-image-element {
    /// Opaque handle to a host-side `SVGImageElement` instance.
    type svg-image-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-image-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-image-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svg-image-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svg-image-element-handle) -> u64;

    /// `preserveAspectRatio` attribute — getter.
    get-preserve-aspect-ratio: func(self: svg-image-element-handle) -> u64;

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: svg-image-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: svg-image-element-handle, value: option<string>);
}

interface svg-foreign-object-element {
    /// Opaque handle to a host-side `SVGForeignObjectElement` instance.
    type svg-foreign-object-element-handle = u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-foreign-object-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-foreign-object-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svg-foreign-object-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svg-foreign-object-element-handle) -> u64;
}

interface svg-marker-element {
    /// Opaque handle to a host-side `SVGMarkerElement` instance.
    type svg-marker-element-handle = u64;

    /// `refX` attribute — getter.
    get-ref-x: func(self: svg-marker-element-handle) -> u64;

    /// `refY` attribute — getter.
    get-ref-y: func(self: svg-marker-element-handle) -> u64;

    /// `markerUnits` attribute — getter.
    get-marker-units: func(self: svg-marker-element-handle) -> u64;

    /// `markerWidth` attribute — getter.
    get-marker-width: func(self: svg-marker-element-handle) -> u64;

    /// `markerHeight` attribute — getter.
    get-marker-height: func(self: svg-marker-element-handle) -> u64;

    /// `orientType` attribute — getter.
    get-orient-type: func(self: svg-marker-element-handle) -> u64;

    /// `orientAngle` attribute — getter.
    get-orient-angle: func(self: svg-marker-element-handle) -> u64;

    /// `orient` attribute — getter.
    get-orient: func(self: svg-marker-element-handle) -> string;
    /// `orient` attribute — setter.
    set-orient: func(self: svg-marker-element-handle, value: string);

    /// `setOrientToAuto()` operation.
    set-orient-to-auto: func(self: svg-marker-element-handle);

    /// `setOrientToAngle()` operation.
    set-orient-to-angle: func(self: svg-marker-element-handle, angle: u64);
}

interface svg-gradient-element {
    /// Opaque handle to a host-side `SVGGradientElement` instance.
    type svg-gradient-element-handle = u64;

    /// `gradientUnits` attribute — getter.
    get-gradient-units: func(self: svg-gradient-element-handle) -> u64;

    /// `gradientTransform` attribute — getter.
    get-gradient-transform: func(self: svg-gradient-element-handle) -> u64;

    /// `spreadMethod` attribute — getter.
    get-spread-method: func(self: svg-gradient-element-handle) -> u64;
}

interface svg-linear-gradient-element {
    /// Opaque handle to a host-side `SVGLinearGradientElement` instance.
    type svg-linear-gradient-element-handle = u64;

    /// `x1` attribute — getter.
    get-x1: func(self: svg-linear-gradient-element-handle) -> u64;

    /// `y1` attribute — getter.
    get-y1: func(self: svg-linear-gradient-element-handle) -> u64;

    /// `x2` attribute — getter.
    get-x2: func(self: svg-linear-gradient-element-handle) -> u64;

    /// `y2` attribute — getter.
    get-y2: func(self: svg-linear-gradient-element-handle) -> u64;
}

interface svg-radial-gradient-element {
    /// Opaque handle to a host-side `SVGRadialGradientElement` instance.
    type svg-radial-gradient-element-handle = u64;

    /// `cx` attribute — getter.
    get-cx: func(self: svg-radial-gradient-element-handle) -> u64;

    /// `cy` attribute — getter.
    get-cy: func(self: svg-radial-gradient-element-handle) -> u64;

    /// `r` attribute — getter.
    get-r: func(self: svg-radial-gradient-element-handle) -> u64;

    /// `fx` attribute — getter.
    get-fx: func(self: svg-radial-gradient-element-handle) -> u64;

    /// `fy` attribute — getter.
    get-fy: func(self: svg-radial-gradient-element-handle) -> u64;

    /// `fr` attribute — getter.
    get-fr: func(self: svg-radial-gradient-element-handle) -> u64;
}

interface svg-stop-element {
    /// Opaque handle to a host-side `SVGStopElement` instance.
    type svg-stop-element-handle = u64;

    /// `offset` attribute — getter.
    get-offset: func(self: svg-stop-element-handle) -> u64;
}

interface svg-pattern-element {
    /// Opaque handle to a host-side `SVGPatternElement` instance.
    type svg-pattern-element-handle = u64;

    /// `patternUnits` attribute — getter.
    get-pattern-units: func(self: svg-pattern-element-handle) -> u64;

    /// `patternContentUnits` attribute — getter.
    get-pattern-content-units: func(self: svg-pattern-element-handle) -> u64;

    /// `patternTransform` attribute — getter.
    get-pattern-transform: func(self: svg-pattern-element-handle) -> u64;

    /// `x` attribute — getter.
    get-x: func(self: svg-pattern-element-handle) -> u64;

    /// `y` attribute — getter.
    get-y: func(self: svg-pattern-element-handle) -> u64;

    /// `width` attribute — getter.
    get-width: func(self: svg-pattern-element-handle) -> u64;

    /// `height` attribute — getter.
    get-height: func(self: svg-pattern-element-handle) -> u64;
}

interface svg-script-element {
    /// Opaque handle to a host-side `SVGScriptElement` instance.
    type svg-script-element-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: svg-script-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: svg-script-element-handle, value: string);

    /// `crossOrigin` attribute — getter.
    get-cross-origin: func(self: svg-script-element-handle) -> option<string>;
    /// `crossOrigin` attribute — setter.
    set-cross-origin: func(self: svg-script-element-handle, value: option<string>);
}

interface svga-element {
    /// Opaque handle to a host-side `SVGAElement` instance.
    type svga-element-handle = u64;

    /// `target` attribute — getter.
    get-target: func(self: svga-element-handle) -> u64;

    /// `download` attribute — getter.
    get-download: func(self: svga-element-handle) -> string;
    /// `download` attribute — setter.
    set-download: func(self: svga-element-handle, value: string);

    /// `ping` attribute — getter.
    get-ping: func(self: svga-element-handle) -> string;
    /// `ping` attribute — setter.
    set-ping: func(self: svga-element-handle, value: string);

    /// `rel` attribute — getter.
    get-rel: func(self: svga-element-handle) -> string;
    /// `rel` attribute — setter.
    set-rel: func(self: svga-element-handle, value: string);

    /// `relList` attribute — getter.
    get-rel-list: func(self: svga-element-handle) -> u64;

    /// `hreflang` attribute — getter.
    get-hreflang: func(self: svga-element-handle) -> string;
    /// `hreflang` attribute — setter.
    set-hreflang: func(self: svga-element-handle, value: string);

    /// `type` attribute — getter.
    get-type: func(self: svga-element-handle) -> string;
    /// `type` attribute — setter.
    set-type: func(self: svga-element-handle, value: string);

    /// `referrerPolicy` attribute — getter.
    get-referrer-policy: func(self: svga-element-handle) -> string;
    /// `referrerPolicy` attribute — setter.
    set-referrer-policy: func(self: svga-element-handle, value: string);
}

interface url-search-params {
    /// Opaque handle to a host-side `URLSearchParams` instance.
    type url-search-params-handle = u64;

    /// `size` attribute — getter.
    get-size: func(self: url-search-params-handle) -> u32;

    /// `append()` operation.
    append: func(self: url-search-params-handle, name: string, value: string);

    /// `delete()` operation.
    delete: func(self: url-search-params-handle, name: string, value: option<string>);

    /// `get()` operation.
    get: func(self: url-search-params-handle, name: string) -> option<string>;

    /// `getAll()` operation.
    get-all: func(self: url-search-params-handle, name: string) -> list<string>;

    /// `has()` operation.
    has: func(self: url-search-params-handle, name: string, value: option<string>) -> bool;

    /// `set()` operation.
    set: func(self: url-search-params-handle, name: string, value: string);

    /// `sort()` operation.
    sort: func(self: url-search-params-handle);
}

interface module {
    /// Opaque handle to a host-side `Module` instance.
    type module-handle = u64;

    /// `exports()` operation.
    exports: func(module-object: u64) -> list<u64>;

    /// `imports()` operation.
    imports: func(module-object: u64) -> list<u64>;

    /// `customSections()` operation.
    custom-sections: func(module-object: u64, section-name: string) -> list<list<u8>>;
}

interface instance {
    /// Opaque handle to a host-side `Instance` instance.
    type instance-handle = u64;

    /// `exports` attribute — getter.
    get-exports: func(self: instance-handle) -> u64;
}

interface memory {
    /// Opaque handle to a host-side `Memory` instance.
    type memory-handle = u64;

    /// `grow()` operation.
    grow: func(self: memory-handle, delta: u64) -> u64;

    /// `toFixedLengthBuffer()` operation.
    to-fixed-length-buffer: func(self: memory-handle) -> list<u8>;

    /// `toResizableBuffer()` operation.
    to-resizable-buffer: func(self: memory-handle) -> list<u8>;

    /// `buffer` attribute — getter.
    get-buffer: func(self: memory-handle) -> list<u8>;
}

interface table {
    /// Opaque handle to a host-side `Table` instance.
    type table-handle = u64;

    /// `grow()` operation.
    grow: func(self: table-handle, delta: u64, value: option<string>) -> u64;

    /// `get()` operation.
    get: func(self: table-handle, index: u64) -> string;

    /// `set()` operation.
    set: func(self: table-handle, index: u64, value: option<string>);

    /// `length` attribute — getter.
    get-length: func(self: table-handle) -> u64;
}

interface global {
    /// Opaque handle to a host-side `Global` instance.
    type global-handle = u64;

    /// `valueOf()` operation.
    value-of: func(self: global-handle) -> string;

    /// `value` attribute — getter.
    get-value: func(self: global-handle) -> string;
    /// `value` attribute — setter.
    set-value: func(self: global-handle, value: string);
}

interface exception {
    /// Opaque handle to a host-side `Exception` instance.
    type exception-handle = u64;

    /// `getArg()` operation.
    get-arg: func(self: exception-handle, exception-tag: u64, index: u32) -> string;

    /// `is()` operation.
    is: func(self: exception-handle, exception-tag: u64) -> bool;

    /// `stack` attribute — getter.
    get-stack: func(self: exception-handle) -> string;
}

interface animation {
/// Opaque handle to a Animation instance.
    type animation-handle = u64;

    // new-animation: skipped (unmappable parameter type)
    get-id: func(handle: animation-handle) -> string;
    set-id: func(handle: animation-handle, value: string);
    get-playback-rate: func(handle: animation-handle) -> f64;
    set-playback-rate: func(handle: animation-handle, value: f64);
    pending: func(handle: animation-handle) -> bool;
    cancel: func(handle: animation-handle);
    finish: func(handle: animation-handle);
    play: func(handle: animation-handle);
    pause: func(handle: animation-handle);
    update-playback-rate: func(handle: animation-handle, playback-rate: f64);
    reverse: func(handle: animation-handle);
    persist: func(handle: animation-handle);
    commit-styles: func(handle: animation-handle);
}

interface keyframe-effect {
/// Opaque handle to a KeyframeEffect instance.
    type keyframe-handle = u64;

}

interface animation-timeline {
/// Opaque handle to a AnimationTimeline instance.
    type timeline-handle = u64;

}

interface rtc-peer-connection {
    /// Opaque handle to a host-side `RTCPeerConnection` instance.
    type rtc-peer-connection-handle = u64;

    /// `createOffer()` operation.
    create-offer: func(self: rtc-peer-connection-handle, options: option<u64>) -> u64;

    /// `createAnswer()` operation.
    create-answer: func(self: rtc-peer-connection-handle, options: option<u64>) -> u64;

    /// `setLocalDescription()` operation.
    set-local-description: func(self: rtc-peer-connection-handle, description: option<u64>) -> u64;

    /// `localDescription` attribute — getter.
    get-local-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `currentLocalDescription` attribute — getter.
    get-current-local-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `pendingLocalDescription` attribute — getter.
    get-pending-local-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `setRemoteDescription()` operation.
    set-remote-description: func(self: rtc-peer-connection-handle, description: u64) -> u64;

    /// `remoteDescription` attribute — getter.
    get-remote-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `currentRemoteDescription` attribute — getter.
    get-current-remote-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `pendingRemoteDescription` attribute — getter.
    get-pending-remote-description: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `addIceCandidate()` operation.
    add-ice-candidate: func(self: rtc-peer-connection-handle, candidate: option<u64>) -> u64;

    /// `signalingState` attribute — getter.
    get-signaling-state: func(self: rtc-peer-connection-handle) -> u64;

    /// `iceGatheringState` attribute — getter.
    get-ice-gathering-state: func(self: rtc-peer-connection-handle) -> u64;

    /// `iceConnectionState` attribute — getter.
    get-ice-connection-state: func(self: rtc-peer-connection-handle) -> u64;

    /// `connectionState` attribute — getter.
    get-connection-state: func(self: rtc-peer-connection-handle) -> u64;

    /// `canTrickleIceCandidates` attribute — getter.
    get-can-trickle-ice-candidates: func(self: rtc-peer-connection-handle) -> option<bool>;

    /// `restartIce()` operation.
    restart-ice: func(self: rtc-peer-connection-handle);

    /// `getConfiguration()` operation.
    get-configuration: func(self: rtc-peer-connection-handle) -> u64;

    /// `setConfiguration()` operation.
    set-configuration: func(self: rtc-peer-connection-handle, configuration: option<u64>);

    /// `close()` operation.
    close: func(self: rtc-peer-connection-handle);

    /// `onnegotiationneeded` attribute — getter.
    get-onnegotiationneeded: func(self: rtc-peer-connection-handle) -> u64;
    /// `onnegotiationneeded` attribute — setter.
    set-onnegotiationneeded: func(self: rtc-peer-connection-handle, value: u64);

    /// `onicecandidate` attribute — getter.
    get-onicecandidate: func(self: rtc-peer-connection-handle) -> u64;
    /// `onicecandidate` attribute — setter.
    set-onicecandidate: func(self: rtc-peer-connection-handle, value: u64);

    /// `onicecandidateerror` attribute — getter.
    get-onicecandidateerror: func(self: rtc-peer-connection-handle) -> u64;
    /// `onicecandidateerror` attribute — setter.
    set-onicecandidateerror: func(self: rtc-peer-connection-handle, value: u64);

    /// `onsignalingstatechange` attribute — getter.
    get-onsignalingstatechange: func(self: rtc-peer-connection-handle) -> u64;
    /// `onsignalingstatechange` attribute — setter.
    set-onsignalingstatechange: func(self: rtc-peer-connection-handle, value: u64);

    /// `oniceconnectionstatechange` attribute — getter.
    get-oniceconnectionstatechange: func(self: rtc-peer-connection-handle) -> u64;
    /// `oniceconnectionstatechange` attribute — setter.
    set-oniceconnectionstatechange: func(self: rtc-peer-connection-handle, value: u64);

    /// `onicegatheringstatechange` attribute — getter.
    get-onicegatheringstatechange: func(self: rtc-peer-connection-handle) -> u64;
    /// `onicegatheringstatechange` attribute — setter.
    set-onicegatheringstatechange: func(self: rtc-peer-connection-handle, value: u64);

    /// `onconnectionstatechange` attribute — getter.
    get-onconnectionstatechange: func(self: rtc-peer-connection-handle) -> u64;
    /// `onconnectionstatechange` attribute — setter.
    set-onconnectionstatechange: func(self: rtc-peer-connection-handle, value: u64);

    /// `generateCertificate()` operation.
    generate-certificate: func(keygen-algorithm: u64) -> u64;

    /// `getSenders()` operation.
    get-senders: func(self: rtc-peer-connection-handle) -> list<u64>;

    /// `getReceivers()` operation.
    get-receivers: func(self: rtc-peer-connection-handle) -> list<u64>;

    /// `getTransceivers()` operation.
    get-transceivers: func(self: rtc-peer-connection-handle) -> list<u64>;

    /// `addTrack()` operation.
    add-track: func(self: rtc-peer-connection-handle, track: u64, streams: list<u64>) -> u64;

    /// `removeTrack()` operation.
    remove-track: func(self: rtc-peer-connection-handle, sender: u64);

    /// `addTransceiver()` operation.
    add-transceiver: func(self: rtc-peer-connection-handle, track-or-kind: string, init: option<u64>) -> u64;

    /// `ontrack` attribute — getter.
    get-ontrack: func(self: rtc-peer-connection-handle) -> u64;
    /// `ontrack` attribute — setter.
    set-ontrack: func(self: rtc-peer-connection-handle, value: u64);

    /// `sctp` attribute — getter.
    get-sctp: func(self: rtc-peer-connection-handle) -> option<u64>;

    /// `createDataChannel()` operation.
    create-data-channel: func(self: rtc-peer-connection-handle, label: string, data-channel-dict: option<u64>) -> u64;

    /// `ondatachannel` attribute — getter.
    get-ondatachannel: func(self: rtc-peer-connection-handle) -> u64;
    /// `ondatachannel` attribute — setter.
    set-ondatachannel: func(self: rtc-peer-connection-handle, value: u64);

    /// `getStats()` operation.
    get-stats: func(self: rtc-peer-connection-handle, selector: option<u64>) -> u64;
}

interface rtc-session-description {
    /// Opaque handle to a host-side `RTCSessionDescription` instance.
    type rtc-session-description-handle = u64;

    /// `type` attribute — getter.
    get-type: func(self: rtc-session-description-handle) -> u64;

    /// `sdp` attribute — getter.
    get-sdp: func(self: rtc-session-description-handle) -> string;

    /// `toJSON()` operation.
    to-json: func(self: rtc-session-description-handle) -> u64;
}

interface rtc-ice-candidate {
    /// Opaque handle to a host-side `RTCIceCandidate` instance.
    type rtc-ice-candidate-handle = u64;

    /// `candidate` attribute — getter.
    get-candidate: func(self: rtc-ice-candidate-handle) -> string;

    /// `sdpMid` attribute — getter.
    get-sdp-mid: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `sdpMLineIndex` attribute — getter.
    get-sdp-m-line-index: func(self: rtc-ice-candidate-handle) -> option<u16>;

    /// `foundation` attribute — getter.
    get-foundation: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `component` attribute — getter.
    get-component: func(self: rtc-ice-candidate-handle) -> option<u64>;

    /// `priority` attribute — getter.
    get-priority: func(self: rtc-ice-candidate-handle) -> option<u32>;

    /// `address` attribute — getter.
    get-address: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `protocol` attribute — getter.
    get-protocol: func(self: rtc-ice-candidate-handle) -> option<u64>;

    /// `port` attribute — getter.
    get-port: func(self: rtc-ice-candidate-handle) -> option<u16>;

    /// `type` attribute — getter.
    get-type: func(self: rtc-ice-candidate-handle) -> option<u64>;

    /// `tcpType` attribute — getter.
    get-tcp-type: func(self: rtc-ice-candidate-handle) -> option<u64>;

    /// `relatedAddress` attribute — getter.
    get-related-address: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `relatedPort` attribute — getter.
    get-related-port: func(self: rtc-ice-candidate-handle) -> option<u16>;

    /// `usernameFragment` attribute — getter.
    get-username-fragment: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `relayProtocol` attribute — getter.
    get-relay-protocol: func(self: rtc-ice-candidate-handle) -> option<u64>;

    /// `url` attribute — getter.
    get-url: func(self: rtc-ice-candidate-handle) -> option<string>;

    /// `toJSON()` operation.
    to-json: func(self: rtc-ice-candidate-handle) -> u64;
}

interface rtc-peer-connection-ice-event {
    /// Opaque handle to a host-side `RTCPeerConnectionIceEvent` instance.
    type rtc-peer-connection-ice-event-handle = u64;

    /// `candidate` attribute — getter.
    get-candidate: func(self: rtc-peer-connection-ice-event-handle) -> option<u64>;

    /// `url` attribute — getter.
    get-url: func(self: rtc-peer-connection-ice-event-handle) -> option<string>;
}

interface rtc-peer-connection-ice-error-event {
    /// Opaque handle to a host-side `RTCPeerConnectionIceErrorEvent` instance.
    type rtc-peer-connection-ice-error-event-handle = u64;

    /// `address` attribute — getter.
    get-address: func(self: rtc-peer-connection-ice-error-event-handle) -> option<string>;

    /// `port` attribute — getter.
    get-port: func(self: rtc-peer-connection-ice-error-event-handle) -> option<u16>;

    /// `url` attribute — getter.
    get-url: func(self: rtc-peer-connection-ice-error-event-handle) -> string;

    /// `errorCode` attribute — getter.
    get-error-code: func(self: rtc-peer-connection-ice-error-event-handle) -> u16;

    /// `errorText` attribute — getter.
    get-error-text: func(self: rtc-peer-connection-ice-error-event-handle) -> string;
}

interface rtc-certificate {
    /// Opaque handle to a host-side `RTCCertificate` instance.
    type rtc-certificate-handle = u64;

    /// `expires` attribute — getter.
    get-expires: func(self: rtc-certificate-handle) -> u64;

    /// `getFingerprints()` operation.
    get-fingerprints: func(self: rtc-certificate-handle) -> list<u64>;
}

interface rtc-rtp-sender {
    /// Opaque handle to a host-side `RTCRtpSender` instance.
    type rtc-rtp-sender-handle = u64;

    /// `track` attribute — getter.
    get-track: func(self: rtc-rtp-sender-handle) -> option<u64>;

    /// `transport` attribute — getter.
    get-transport: func(self: rtc-rtp-sender-handle) -> option<u64>;

    /// `getCapabilities()` operation.
    get-capabilities: func(kind: string) -> option<u64>;

    /// `setParameters()` operation.
    set-parameters: func(self: rtc-rtp-sender-handle, parameters: u64, set-parameter-options: option<u64>) -> u64;

    /// `getParameters()` operation.
    get-parameters: func(self: rtc-rtp-sender-handle) -> u64;

    /// `replaceTrack()` operation.
    replace-track: func(self: rtc-rtp-sender-handle, with-track: option<u64>) -> u64;

    /// `setStreams()` operation.
    set-streams: func(self: rtc-rtp-sender-handle, streams: list<u64>);

    /// `getStats()` operation.
    get-stats: func(self: rtc-rtp-sender-handle) -> u64;

    /// `dtmf` attribute — getter.
    get-dtmf: func(self: rtc-rtp-sender-handle) -> option<u64>;
}

interface rtc-rtp-receiver {
    /// Opaque handle to a host-side `RTCRtpReceiver` instance.
    type rtc-rtp-receiver-handle = u64;

    /// `track` attribute — getter.
    get-track: func(self: rtc-rtp-receiver-handle) -> u64;

    /// `transport` attribute — getter.
    get-transport: func(self: rtc-rtp-receiver-handle) -> option<u64>;

    /// `getCapabilities()` operation.
    get-capabilities: func(kind: string) -> option<u64>;

    /// `getParameters()` operation.
    get-parameters: func(self: rtc-rtp-receiver-handle) -> u64;

    /// `getContributingSources()` operation.
    get-contributing-sources: func(self: rtc-rtp-receiver-handle) -> list<u64>;

    /// `getSynchronizationSources()` operation.
    get-synchronization-sources: func(self: rtc-rtp-receiver-handle) -> list<u64>;

    /// `getStats()` operation.
    get-stats: func(self: rtc-rtp-receiver-handle) -> u64;

    /// `jitterBufferTarget` attribute — getter.
    get-jitter-buffer-target: func(self: rtc-rtp-receiver-handle) -> option<f64>;
    /// `jitterBufferTarget` attribute — setter.
    set-jitter-buffer-target: func(self: rtc-rtp-receiver-handle, value: option<f64>);
}

interface rtc-rtp-transceiver {
    /// Opaque handle to a host-side `RTCRtpTransceiver` instance.
    type rtc-rtp-transceiver-handle = u64;

    /// `mid` attribute — getter.
    get-mid: func(self: rtc-rtp-transceiver-handle) -> option<string>;

    /// `sender` attribute — getter.
    get-sender: func(self: rtc-rtp-transceiver-handle) -> u64;

    /// `receiver` attribute — getter.
    get-receiver: func(self: rtc-rtp-transceiver-handle) -> u64;

    /// `direction` attribute — getter.
    get-direction: func(self: rtc-rtp-transceiver-handle) -> u64;
    /// `direction` attribute — setter.
    set-direction: func(self: rtc-rtp-transceiver-handle, value: u64);

    /// `currentDirection` attribute — getter.
    get-current-direction: func(self: rtc-rtp-transceiver-handle) -> option<u64>;

    /// `stop()` operation.
    stop: func(self: rtc-rtp-transceiver-handle);

    /// `setCodecPreferences()` operation.
    set-codec-preferences: func(self: rtc-rtp-transceiver-handle, codecs: list<u64>);
}

interface rtc-dtls-transport {
    /// Opaque handle to a host-side `RTCDtlsTransport` instance.
    type rtc-dtls-transport-handle = u64;

    /// `iceTransport` attribute — getter.
    get-ice-transport: func(self: rtc-dtls-transport-handle) -> u64;

    /// `state` attribute — getter.
    get-state: func(self: rtc-dtls-transport-handle) -> u64;

    /// `getRemoteCertificates()` operation.
    get-remote-certificates: func(self: rtc-dtls-transport-handle) -> list<list<u8>>;

    /// `onstatechange` attribute — getter.
    get-onstatechange: func(self: rtc-dtls-transport-handle) -> u64;
    /// `onstatechange` attribute — setter.
    set-onstatechange: func(self: rtc-dtls-transport-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: rtc-dtls-transport-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: rtc-dtls-transport-handle, value: u64);
}

interface rtc-ice-transport {
    /// Opaque handle to a host-side `RTCIceTransport` instance.
    type rtc-ice-transport-handle = u64;

    /// `role` attribute — getter.
    get-role: func(self: rtc-ice-transport-handle) -> u64;

    /// `component` attribute — getter.
    get-component: func(self: rtc-ice-transport-handle) -> u64;

    /// `state` attribute — getter.
    get-state: func(self: rtc-ice-transport-handle) -> u64;

    /// `gatheringState` attribute — getter.
    get-gathering-state: func(self: rtc-ice-transport-handle) -> u64;

    /// `getLocalCandidates()` operation.
    get-local-candidates: func(self: rtc-ice-transport-handle) -> list<u64>;

    /// `getRemoteCandidates()` operation.
    get-remote-candidates: func(self: rtc-ice-transport-handle) -> list<u64>;

    /// `getSelectedCandidatePair()` operation.
    get-selected-candidate-pair: func(self: rtc-ice-transport-handle) -> option<u64>;

    /// `getLocalParameters()` operation.
    get-local-parameters: func(self: rtc-ice-transport-handle) -> option<u64>;

    /// `getRemoteParameters()` operation.
    get-remote-parameters: func(self: rtc-ice-transport-handle) -> option<u64>;

    /// `onstatechange` attribute — getter.
    get-onstatechange: func(self: rtc-ice-transport-handle) -> u64;
    /// `onstatechange` attribute — setter.
    set-onstatechange: func(self: rtc-ice-transport-handle, value: u64);

    /// `ongatheringstatechange` attribute — getter.
    get-ongatheringstatechange: func(self: rtc-ice-transport-handle) -> u64;
    /// `ongatheringstatechange` attribute — setter.
    set-ongatheringstatechange: func(self: rtc-ice-transport-handle, value: u64);

    /// `onselectedcandidatepairchange` attribute — getter.
    get-onselectedcandidatepairchange: func(self: rtc-ice-transport-handle) -> u64;
    /// `onselectedcandidatepairchange` attribute — setter.
    set-onselectedcandidatepairchange: func(self: rtc-ice-transport-handle, value: u64);
}

interface rtc-ice-candidate-pair {
    /// Opaque handle to a host-side `RTCIceCandidatePair` instance.
    type rtc-ice-candidate-pair-handle = u64;

    /// `local` attribute — getter.
    get-local: func(self: rtc-ice-candidate-pair-handle) -> u64;

    /// `remote` attribute — getter.
    get-remote: func(self: rtc-ice-candidate-pair-handle) -> u64;
}

interface rtc-track-event {
    /// Opaque handle to a host-side `RTCTrackEvent` instance.
    type rtc-track-event-handle = u64;

    /// `receiver` attribute — getter.
    get-receiver: func(self: rtc-track-event-handle) -> u64;

    /// `track` attribute — getter.
    get-track: func(self: rtc-track-event-handle) -> u64;

    /// `streams` attribute — getter.
    get-streams: func(self: rtc-track-event-handle) -> list<u64>;

    /// `transceiver` attribute — getter.
    get-transceiver: func(self: rtc-track-event-handle) -> u64;
}

interface rtc-sctp-transport {
    /// Opaque handle to a host-side `RTCSctpTransport` instance.
    type rtc-sctp-transport-handle = u64;

    /// `transport` attribute — getter.
    get-transport: func(self: rtc-sctp-transport-handle) -> u64;

    /// `state` attribute — getter.
    get-state: func(self: rtc-sctp-transport-handle) -> u64;

    /// `maxMessageSize` attribute — getter.
    get-max-message-size: func(self: rtc-sctp-transport-handle) -> option<f64>;

    /// `maxChannels` attribute — getter.
    get-max-channels: func(self: rtc-sctp-transport-handle) -> option<u16>;

    /// `onstatechange` attribute — getter.
    get-onstatechange: func(self: rtc-sctp-transport-handle) -> u64;
    /// `onstatechange` attribute — setter.
    set-onstatechange: func(self: rtc-sctp-transport-handle, value: u64);
}

interface rtc-data-channel {
    /// Opaque handle to a host-side `RTCDataChannel` instance.
    type rtc-data-channel-handle = u64;

    /// `label` attribute — getter.
    get-label: func(self: rtc-data-channel-handle) -> string;

    /// `ordered` attribute — getter.
    get-ordered: func(self: rtc-data-channel-handle) -> bool;

    /// `maxPacketLifeTime` attribute — getter.
    get-max-packet-life-time: func(self: rtc-data-channel-handle) -> option<u16>;

    /// `maxRetransmits` attribute — getter.
    get-max-retransmits: func(self: rtc-data-channel-handle) -> option<u16>;

    /// `protocol` attribute — getter.
    get-protocol: func(self: rtc-data-channel-handle) -> string;

    /// `negotiated` attribute — getter.
    get-negotiated: func(self: rtc-data-channel-handle) -> bool;

    /// `id` attribute — getter.
    get-id: func(self: rtc-data-channel-handle) -> option<u16>;

    /// `readyState` attribute — getter.
    get-ready-state: func(self: rtc-data-channel-handle) -> u64;

    /// `bufferedAmount` attribute — getter.
    get-buffered-amount: func(self: rtc-data-channel-handle) -> u32;

    /// `bufferedAmountLowThreshold` attribute — getter.
    get-buffered-amount-low-threshold: func(self: rtc-data-channel-handle) -> u32;
    /// `bufferedAmountLowThreshold` attribute — setter.
    set-buffered-amount-low-threshold: func(self: rtc-data-channel-handle, value: u32);

    /// `onopen` attribute — getter.
    get-onopen: func(self: rtc-data-channel-handle) -> u64;
    /// `onopen` attribute — setter.
    set-onopen: func(self: rtc-data-channel-handle, value: u64);

    /// `onbufferedamountlow` attribute — getter.
    get-onbufferedamountlow: func(self: rtc-data-channel-handle) -> u64;
    /// `onbufferedamountlow` attribute — setter.
    set-onbufferedamountlow: func(self: rtc-data-channel-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: rtc-data-channel-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: rtc-data-channel-handle, value: u64);

    /// `onclosing` attribute — getter.
    get-onclosing: func(self: rtc-data-channel-handle) -> u64;
    /// `onclosing` attribute — setter.
    set-onclosing: func(self: rtc-data-channel-handle, value: u64);

    /// `onclose` attribute — getter.
    get-onclose: func(self: rtc-data-channel-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: rtc-data-channel-handle, value: u64);

    /// `close()` operation.
    close: func(self: rtc-data-channel-handle);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: rtc-data-channel-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: rtc-data-channel-handle, value: u64);

    /// `binaryType` attribute — getter.
    get-binary-type: func(self: rtc-data-channel-handle) -> u64;
    /// `binaryType` attribute — setter.
    set-binary-type: func(self: rtc-data-channel-handle, value: u64);

    /// `send()` operation.
    send: func(self: rtc-data-channel-handle, data: string);
}

interface rtc-data-channel-event {
    /// Opaque handle to a host-side `RTCDataChannelEvent` instance.
    type rtc-data-channel-event-handle = u64;

    /// `channel` attribute — getter.
    get-channel: func(self: rtc-data-channel-event-handle) -> u64;
}

interface rtcdtmf-sender {
    /// Opaque handle to a host-side `RTCDTMFSender` instance.
    type rtcdtmf-sender-handle = u64;

    /// `insertDTMF()` operation.
    insert-dtmf: func(self: rtcdtmf-sender-handle, tones: string, duration: option<u32>, inter-tone-gap: option<u32>);

    /// `ontonechange` attribute — getter.
    get-ontonechange: func(self: rtcdtmf-sender-handle) -> u64;
    /// `ontonechange` attribute — setter.
    set-ontonechange: func(self: rtcdtmf-sender-handle, value: u64);

    /// `canInsertDTMF` attribute — getter.
    get-can-insert-dtmf: func(self: rtcdtmf-sender-handle) -> bool;

    /// `toneBuffer` attribute — getter.
    get-tone-buffer: func(self: rtcdtmf-sender-handle) -> string;
}

interface rtcdtmf-tone-change-event {
    /// Opaque handle to a host-side `RTCDTMFToneChangeEvent` instance.
    type rtcdtmf-tone-change-event-handle = u64;

    /// `tone` attribute — getter.
    get-tone: func(self: rtcdtmf-tone-change-event-handle) -> string;
}

interface rtc-error {
    /// Opaque handle to a host-side `RTCError` instance.
    type rtc-error-handle = u64;

    /// `errorDetail` attribute — getter.
    get-error-detail: func(self: rtc-error-handle) -> u64;

    /// `sdpLineNumber` attribute — getter.
    get-sdp-line-number: func(self: rtc-error-handle) -> option<s32>;

    /// `sctpCauseCode` attribute — getter.
    get-sctp-cause-code: func(self: rtc-error-handle) -> option<s32>;

    /// `receivedAlert` attribute — getter.
    get-received-alert: func(self: rtc-error-handle) -> option<u32>;

    /// `sentAlert` attribute — getter.
    get-sent-alert: func(self: rtc-error-handle) -> option<u32>;
}

interface rtc-error-event {
    /// Opaque handle to a host-side `RTCErrorEvent` instance.
    type rtc-error-event-handle = u64;

    /// `error` attribute — getter.
    get-error: func(self: rtc-error-event-handle) -> u64;
}

interface web-socket {
    /// Opaque handle to a host-side `WebSocket` instance.
    type web-socket-handle = u64;

    /// `url` attribute — getter.
    get-url: func(self: web-socket-handle) -> string;

    /// `readyState` attribute — getter.
    get-ready-state: func(self: web-socket-handle) -> u16;

    /// `bufferedAmount` attribute — getter.
    get-buffered-amount: func(self: web-socket-handle) -> u64;

    /// `onopen` attribute — getter.
    get-onopen: func(self: web-socket-handle) -> u64;
    /// `onopen` attribute — setter.
    set-onopen: func(self: web-socket-handle, value: u64);

    /// `onerror` attribute — getter.
    get-onerror: func(self: web-socket-handle) -> u64;
    /// `onerror` attribute — setter.
    set-onerror: func(self: web-socket-handle, value: u64);

    /// `onclose` attribute — getter.
    get-onclose: func(self: web-socket-handle) -> u64;
    /// `onclose` attribute — setter.
    set-onclose: func(self: web-socket-handle, value: u64);

    /// `extensions` attribute — getter.
    get-extensions: func(self: web-socket-handle) -> string;

    /// `protocol` attribute — getter.
    get-protocol: func(self: web-socket-handle) -> string;

    /// `close()` operation.
    close: func(self: web-socket-handle, code: option<u16>, reason: option<string>);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: web-socket-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: web-socket-handle, value: u64);

    /// `binaryType` attribute — getter.
    get-binary-type: func(self: web-socket-handle) -> u64;
    /// `binaryType` attribute — setter.
    set-binary-type: func(self: web-socket-handle, value: u64);

    /// `send()` operation.
    send: func(self: web-socket-handle, data: string);
}

interface close-event {
    /// Opaque handle to a host-side `CloseEvent` instance.
    type close-event-handle = u64;

    /// `wasClean` attribute — getter.
    get-was-clean: func(self: close-event-handle) -> bool;

    /// `code` attribute — getter.
    get-code: func(self: close-event-handle) -> u16;

    /// `reason` attribute — getter.
    get-reason: func(self: close-event-handle) -> string;
}

interface service-worker-container {
    /// Opaque handle to a host-side `ServiceWorkerContainer` instance.
    type service-worker-container-handle = u64;

    /// `controller` attribute — getter.
    get-controller: func(self: service-worker-container-handle) -> option<u64>;

    /// `ready` attribute — getter.
    get-ready: func(self: service-worker-container-handle) -> u64;

    /// `register()` operation.
    register: func(self: service-worker-container-handle, script-url: string, options: option<u64>) -> u64;

    /// `getRegistrations()` operation.
    get-registrations: func(self: service-worker-container-handle) -> u64;

    /// `startMessages()` operation.
    start-messages: func(self: service-worker-container-handle);

    /// `oncontrollerchange` attribute — getter.
    get-oncontrollerchange: func(self: service-worker-container-handle) -> u64;
    /// `oncontrollerchange` attribute — setter.
    set-oncontrollerchange: func(self: service-worker-container-handle, value: u64);

    /// `onmessage` attribute — getter.
    get-onmessage: func(self: service-worker-container-handle) -> u64;
    /// `onmessage` attribute — setter.
    set-onmessage: func(self: service-worker-container-handle, value: u64);

    /// `onmessageerror` attribute — getter.
    get-onmessageerror: func(self: service-worker-container-handle) -> u64;
    /// `onmessageerror` attribute — setter.
    set-onmessageerror: func(self: service-worker-container-handle, value: u64);
}

interface navigation-preload-manager {
    /// Opaque handle to a host-side `NavigationPreloadManager` instance.
    type navigation-preload-manager-handle = u64;

    /// `enable()` operation.
    enable: func(self: navigation-preload-manager-handle) -> u64;

    /// `disable()` operation.
    disable: func(self: navigation-preload-manager-handle) -> u64;

    /// `setHeaderValue()` operation.
    set-header-value: func(self: navigation-preload-manager-handle, value: string) -> u64;

    /// `getState()` operation.
    get-state: func(self: navigation-preload-manager-handle) -> u64;
}

interface client {
    /// Opaque handle to a host-side `Client` instance.
    type client-handle = u64;

    /// `url` attribute — getter.
    get-url: func(self: client-handle) -> string;

    /// `frameType` attribute — getter.
    get-frame-type: func(self: client-handle) -> u64;

    /// `id` attribute — getter.
    get-id: func(self: client-handle) -> string;

    /// `type` attribute — getter.
    get-type: func(self: client-handle) -> u64;

    /// `postMessage()` operation.
    post-message: func(self: client-handle, message: string, transfer: list<u64>);
}

interface window-client {
    /// Opaque handle to a host-side `WindowClient` instance.
    type window-client-handle = u64;

    /// `visibilityState` attribute — getter.
    get-visibility-state: func(self: window-client-handle) -> u64;

    /// `focused` attribute — getter.
    get-focused: func(self: window-client-handle) -> bool;

    /// `ancestorOrigins` attribute — getter.
    get-ancestor-origins: func(self: window-client-handle) -> list<string>;

    /// `focus()` operation.
    focus: func(self: window-client-handle) -> u64;

    /// `navigate()` operation.
    navigate: func(self: window-client-handle, url: string) -> u64;
}

interface clients {
    /// Opaque handle to a host-side `Clients` instance.
    type clients-handle = u64;

    /// `matchAll()` operation.
    match-all: func(self: clients-handle, options: option<u64>) -> u64;

    /// `openWindow()` operation.
    open-window: func(self: clients-handle, url: string) -> u64;

    /// `claim()` operation.
    claim: func(self: clients-handle) -> u64;
}

interface extendable-event {
    /// Opaque handle to a host-side `ExtendableEvent` instance.
    type extendable-event-handle = u64;

    /// `waitUntil()` operation.
    wait-until: func(self: extendable-event-handle, f: u64);
}

interface install-event {
    /// Opaque handle to a host-side `InstallEvent` instance.
    type install-event-handle = u64;

    /// `addRoutes()` operation.
    add-routes: func(self: install-event-handle, rules: u64) -> u64;
}

interface fetch-event {
    /// Opaque handle to a host-side `FetchEvent` instance.
    type fetch-event-handle = u64;

    /// `request` attribute — getter.
    get-request: func(self: fetch-event-handle) -> u64;

    /// `preloadResponse` attribute — getter.
    get-preload-response: func(self: fetch-event-handle) -> u64;

    /// `clientId` attribute — getter.
    get-client-id: func(self: fetch-event-handle) -> string;

    /// `resultingClientId` attribute — getter.
    get-resulting-client-id: func(self: fetch-event-handle) -> string;

    /// `replacesClientId` attribute — getter.
    get-replaces-client-id: func(self: fetch-event-handle) -> string;

    /// `handled` attribute — getter.
    get-handled: func(self: fetch-event-handle) -> u64;

    /// `respondWith()` operation.
    respond-with: func(self: fetch-event-handle, r: u64);
}

interface extendable-message-event {
    /// Opaque handle to a host-side `ExtendableMessageEvent` instance.
    type extendable-message-event-handle = u64;

    /// `data` attribute — getter.
    get-data: func(self: extendable-message-event-handle) -> string;

    /// `origin` attribute — getter.
    get-origin: func(self: extendable-message-event-handle) -> string;

    /// `lastEventId` attribute — getter.
    get-last-event-id: func(self: extendable-message-event-handle) -> string;

    /// `source` attribute — getter.
    get-source: func(self: extendable-message-event-handle) -> option<u64>;

    /// `ports` attribute — getter.
    get-ports: func(self: extendable-message-event-handle) -> list<u64>;
}

interface cache {
    /// Opaque handle to a host-side `Cache` instance.
    type cache-handle = u64;

    /// `matchAll()` operation.
    match-all: func(self: cache-handle, request: option<u64>, options: option<u64>) -> u64;

    /// `add()` operation.
    add: func(self: cache-handle, request: u64) -> u64;

    /// `addAll()` operation.
    add-all: func(self: cache-handle, requests: list<u64>) -> u64;

    /// `put()` operation.
    put: func(self: cache-handle, request: u64, response: u64) -> u64;

    /// `delete()` operation.
    delete: func(self: cache-handle, request: u64, options: option<u64>) -> u64;

    /// `keys()` operation.
    keys: func(self: cache-handle, request: option<u64>, options: option<u64>) -> u64;
}

interface cache-storage {
    /// Opaque handle to a host-side `CacheStorage` instance.
    type cache-storage-handle = u64;

    /// `has()` operation.
    has: func(self: cache-storage-handle, cache-name: string) -> u64;

    /// `open()` operation.
    open: func(self: cache-storage-handle, cache-name: string) -> u64;

    /// `delete()` operation.
    delete: func(self: cache-storage-handle, cache-name: string) -> u64;

    /// `keys()` operation.
    keys: func(self: cache-storage-handle) -> u64;
}

/// Full browser world — all 568 auto-generated interfaces.
world browser-full {
    import abort-controller;
    import abort-signal;
    import abstract-range;
    import abstract-worker;
    import analyser-node;
    import animation;
    import animation-event;
    import animation-frame-provider;
    import animation-timeline;
    import attr;
    import audio-buffer;
    import audio-buffer-source-node;
    import audio-context;
    import audio-data;
    import audio-decoder;
    import audio-destination-node;
    import audio-encoder;
    import audio-listener;
    import audio-node;
    import audio-param;
    import audio-playback-stats;
    import audio-processing-event;
    import audio-scheduled-source-node;
    import audio-sink-info;
    import audio-track;
    import audio-track-list;
    import audio-worklet;
    import audio-worklet-global-scope;
    import audio-worklet-node;
    import audio-worklet-processor;
    import authenticator-assertion-response;
    import authenticator-attestation-response;
    import authenticator-response;
    import bar-prop;
    import base-audio-context;
    import battery-manager;
    import before-unload-event;
    import biquad-filter-node;
    import blob;
    import blob-event;
    import body;
    import broadcast-channel;
    import byte-length-queuing-strategy;
    import cache;
    import cache-storage;
    import canvas-compositing;
    import canvas-draw-image;
    import canvas-draw-path;
    import canvas-fill-stroke-styles;
    import canvas-filters;
    import canvas-gradient;
    import canvas-image-data;
    import canvas-image-smoothing;
    import canvas-path;
    import canvas-path-drawing-styles;
    import canvas-pattern;
    import canvas-rect;
    import canvas-rendering-context2-d;
    import canvas-settings;
    import canvas-shadow-styles;
    import canvas-state;
    import canvas-text;
    import canvas-text-drawing-styles;
    import canvas-transform;
    import canvas-user-interface;
    import capture-controller;
    import caret-position;
    import chapter-information;
    import character-data;
    import child-node;
    import client;
    import clients;
    import clipboard;
    import clipboard-change-event;
    import clipboard-event;
    import clipboard-item;
    import close-event;
    import close-watcher;
    import command-event;
    import composition-event;
    import constant-source-node;
    import convolver-node;
    import count-queuing-strategy;
    import credential;
    import credential-user-data;
    import credentials-container;
    import crypto;
    import crypto-key;
    import css-condition-rule;
    import css-font-face-descriptors;
    import css-font-face-rule;
    import css-font-feature-values-map;
    import css-font-feature-values-rule;
    import css-font-palette-values-rule;
    import css-grouping-rule;
    import css-import-rule;
    import css-keyframe-rule;
    import css-keyframes-rule;
    import css-margin-rule;
    import css-media-rule;
    import css-namespace-rule;
    import css-page-descriptors;
    import css-page-rule;
    import css-rule;
    import css-rule-list;
    import css-style-declaration;
    import css-style-properties;
    import css-style-rule;
    import css-style-sheet;
    import css-supports-rule;
    import custom-element-registry;
    import custom-event;
    import data-transfer;
    import data-transfer-item;
    import data-transfer-item-list;
    import dedicated-worker-global-scope;
    import delay-node;
    import device-change-event;
    import document;
    import document-or-shadow-root;
    import document-type;
    import dom-implementation;
    import dom-parser;
    import dom-string-list;
    import dom-string-map;
    import dom-token-list;
    import drag-event;
    import dynamics-compressor-node;
    import element;
    import element-content-editable;
    import element-css-inline-style;
    import element-internals;
    import encoded-audio-chunk;
    import encoded-video-chunk;
    import error-event;
    import event;
    import event-listener;
    import event-source;
    import exception;
    import extendable-event;
    import extendable-message-event;
    import external;
    import federated-credential;
    import fetch-event;
    import fetch-later-result;
    import file;
    import file-list;
    import file-reader;
    import file-reader-sync;
    import focus-event;
    import form-data;
    import form-data-event;
    import gain-node;
    import gamepad;
    import gamepad-button;
    import gamepad-event;
    import gamepad-haptic-actuator;
    import generic-transform-stream;
    import geolocation;
    import geolocation-coordinates;
    import geolocation-position;
    import geolocation-position-error;
    import geometry-utils;
    import get-svg-document;
    import global;
    import global-event-handlers;
    import hash-change-event;
    import headers;
    import history;
    import html-all-collection;
    import html-anchor-element;
    import html-area-element;
    import html-base-element;
    import html-body-element;
    import html-button-element;
    import html-canvas-element;
    import html-collection;
    import html-data-element;
    import html-data-list-element;
    import html-details-element;
    import html-dialog-element;
    import html-directory-element;
    import html-div-element;
    import html-element;
    import html-embed-element;
    import html-field-set-element;
    import html-font-element;
    import html-form-element;
    import html-frame-element;
    import html-frame-set-element;
    import html-heading-element;
    import html-html-element;
    import html-hyperlink-element-utils;
    import html-image-element;
    import html-input-element;
    import html-label-element;
    import html-legend-element;
    import html-link-element;
    import html-map-element;
    import html-marquee-element;
    import html-media-element;
    import html-menu-element;
    import html-meta-element;
    import html-meter-element;
    import html-mod-element;
    import html-object-element;
    import html-opt-group-element;
    import html-option-element;
    import html-options-collection;
    import html-or-svg-element;
    import html-output-element;
    import html-paragraph-element;
    import html-param-element;
    import html-pre-element;
    import html-progress-element;
    import html-quote-element;
    import html-script-element;
    import html-select-element;
    import html-slot-element;
    import html-source-element;
    import html-style-element;
    import html-table-caption-element;
    import html-table-cell-element;
    import html-table-col-element;
    import html-table-element;
    import html-table-row-element;
    import html-table-section-element;
    import html-template-element;
    import html-text-area-element;
    import html-time-element;
    import html-title-element;
    import html-track-element;
    import html-video-element;
    import htmlbr-element;
    import htmld-list-element;
    import htmlhr-element;
    import htmli-frame-element;
    import htmlli-element;
    import htmlo-list-element;
    import htmlu-list-element;
    import hyperlink-element-utils;
    import idb-cursor;
    import idb-cursor-with-value;
    import idb-database;
    import idb-factory;
    import idb-index;
    import idb-key-range;
    import idb-object-store;
    import idb-open-db-request;
    import idb-record;
    import idb-request;
    import idb-transaction;
    import idb-version-change-event;
    import iir-filter-node;
    import image-bitmap;
    import image-bitmap-rendering-context;
    import image-data;
    import image-decoder;
    import image-track;
    import image-track-list;
    import input-device-info;
    import input-event;
    import install-event;
    import instance;
    import intersection-observer;
    import intersection-observer-entry;
    import keyboard-event;
    import keyframe-effect;
    import link-style;
    import location;
    import lock;
    import lock-manager;
    import media-capabilities;
    import media-device-info;
    import media-devices;
    import media-element-audio-source-node;
    import media-error;
    import media-list;
    import media-metadata;
    import media-query-list;
    import media-query-list-event;
    import media-recorder;
    import media-session;
    import media-stream;
    import media-stream-audio-destination-node;
    import media-stream-audio-source-node;
    import media-stream-track;
    import media-stream-track-event;
    import memory;
    import message-channel;
    import message-event;
    import message-event-target;
    import message-port;
    import mime-type;
    import mime-type-array;
    import module;
    import mouse-event;
    import mutation-observer;
    import mutation-record;
    import named-node-map;
    import navigate-event;
    import navigation;
    import navigation-activation;
    import navigation-current-entry-change-event;
    import navigation-destination;
    import navigation-history-entry;
    import navigation-precommit-controller;
    import navigation-preload-manager;
    import navigation-transition;
    import navigator;
    import navigator-concurrent-hardware;
    import navigator-content-utils;
    import navigator-cookies;
    import navigator-id;
    import navigator-language;
    import navigator-locks;
    import navigator-on-line;
    import navigator-plugins;
    import navigator-storage;
    import node;
    import node-filter;
    import node-iterator;
    import node-list;
    import non-document-type-child-node;
    import non-element-parent-node;
    import not-restored-reason-details;
    import not-restored-reasons;
    import notification;
    import notification-event;
    import offline-audio-completion-event;
    import offline-audio-context;
    import offscreen-canvas;
    import offscreen-canvas-rendering-context2-d;
    import origin;
    import oscillator-node;
    import overconstrained-error;
    import page-reveal-event;
    import page-swap-event;
    import page-transition-event;
    import panner-node;
    import parent-node;
    import password-credential;
    import path2-d;
    import payment-method-change-event;
    import payment-request;
    import payment-request-update-event;
    import payment-response;
    import performance;
    import performance-entry;
    import performance-mark;
    import performance-measure;
    import performance-navigation;
    import performance-navigation-timing;
    import performance-observer;
    import performance-observer-entry-list;
    import performance-resource-timing;
    import performance-timing;
    import performance-timing-confidence;
    import permission-status;
    import permissions;
    import plugin;
    import plugin-array;
    import pointer-event;
    import pop-state-event;
    import popover-target-attributes;
    import processing-instruction;
    import progress-event;
    import promise-rejection-event;
    import public-key-credential;
    import radio-node-list;
    import range;
    import readable-byte-stream-controller;
    import readable-stream;
    import readable-stream-byob-reader;
    import readable-stream-byob-request;
    import readable-stream-default-controller;
    import readable-stream-default-reader;
    import readable-stream-generic-reader;
    import request;
    import resize-observer;
    import resize-observer-entry;
    import resize-observer-size;
    import response;
    import rtc-certificate;
    import rtc-data-channel;
    import rtc-data-channel-event;
    import rtc-dtls-transport;
    import rtc-error;
    import rtc-error-event;
    import rtc-ice-candidate;
    import rtc-ice-candidate-pair;
    import rtc-ice-transport;
    import rtc-peer-connection;
    import rtc-peer-connection-ice-error-event;
    import rtc-peer-connection-ice-event;
    import rtc-rtp-receiver;
    import rtc-rtp-sender;
    import rtc-rtp-transceiver;
    import rtc-sctp-transport;
    import rtc-session-description;
    import rtc-track-event;
    import rtcdtmf-sender;
    import rtcdtmf-tone-change-event;
    import screen;
    import screen-orientation;
    import script-processor-node;
    import selection;
    import service-worker;
    import service-worker-container;
    import service-worker-global-scope;
    import service-worker-registration;
    import shadow-animation;
    import shadow-root;
    import shared-worker;
    import shared-worker-global-scope;
    import slottable;
    import speech-grammar;
    import speech-grammar-list;
    import speech-recognition;
    import speech-recognition-alternative;
    import speech-recognition-error-event;
    import speech-recognition-event;
    import speech-recognition-phrase;
    import speech-recognition-result;
    import speech-recognition-result-list;
    import speech-synthesis;
    import speech-synthesis-error-event;
    import speech-synthesis-event;
    import speech-synthesis-utterance;
    import speech-synthesis-voice;
    import stereo-panner-node;
    import storage;
    import storage-event;
    import storage-manager;
    import style-sheet;
    import style-sheet-list;
    import submit-event;
    import subtle-crypto;
    import svg-angle;
    import svg-animated-angle;
    import svg-animated-boolean;
    import svg-animated-enumeration;
    import svg-animated-integer;
    import svg-animated-length;
    import svg-animated-length-list;
    import svg-animated-number;
    import svg-animated-number-list;
    import svg-animated-points;
    import svg-animated-preserve-aspect-ratio;
    import svg-animated-rect;
    import svg-animated-string;
    import svg-animated-transform-list;
    import svg-circle-element;
    import svg-element;
    import svg-element-instance;
    import svg-ellipse-element;
    import svg-fit-to-view-box;
    import svg-foreign-object-element;
    import svg-geometry-element;
    import svg-gradient-element;
    import svg-graphics-element;
    import svg-image-element;
    import svg-length;
    import svg-length-list;
    import svg-line-element;
    import svg-linear-gradient-element;
    import svg-marker-element;
    import svg-number;
    import svg-number-list;
    import svg-pattern-element;
    import svg-point-list;
    import svg-preserve-aspect-ratio;
    import svg-radial-gradient-element;
    import svg-rect-element;
    import svg-script-element;
    import svg-stop-element;
    import svg-string-list;
    import svg-style-element;
    import svg-tests;
    import svg-text-content-element;
    import svg-text-path-element;
    import svg-text-positioning-element;
    import svg-transform;
    import svg-transform-list;
    import svg-use-element;
    import svga-element;
    import svgsvg-element;
    import svguri-reference;
    import table;
    import text;
    import text-decoder;
    import text-decoder-common;
    import text-encoder;
    import text-encoder-common;
    import text-event;
    import text-metrics;
    import text-track;
    import text-track-cue;
    import text-track-cue-list;
    import text-track-list;
    import time-ranges;
    import toggle-event;
    import touch;
    import touch-event;
    import touch-list;
    import track-event;
    import transform-stream;
    import transform-stream-default-controller;
    import transition-event;
    import tree-walker;
    import types;
    import ui-event;
    import url;
    import url-search-params;
    import user-activation;
    import validity-state;
    import video-color-space;
    import video-decoder;
    import video-encoder;
    import video-frame;
    import video-track;
    import video-track-list;
    import visibility-state-entry;
    import visual-viewport;
    import wave-shaper-node;
    import web-gl-active-info;
    import web-gl-context-event;
    import web-gl-object;
    import web-gl-rendering-context-base;
    import web-gl-rendering-context-overloads;
    import web-gl-shader-precision-format;
    import web-gl2-rendering-context-base;
    import web-gl2-rendering-context-overloads;
    import web-socket;
    import wheel-event;
    import window;
    import window-client;
    import window-event-handlers;
    import window-local-storage;
    import window-or-worker-global-scope;
    import window-session-storage;
    import worker;
    import worker-global-scope;
    import worker-location;
    import worker-navigator;
    import worklet;
    import writable-stream;
    import writable-stream-default-controller;
    import writable-stream-default-writer;
    import x-path-evaluator-base;
    import x-path-expression;
    import x-path-ns-resolver;
    import x-path-result;
    import xml-http-request;
    import xml-http-request-event-target;
    import xml-serializer;
    import xslt-processor;
    import event-target;
    import style;
    import console;
    import platform-helpers;
    export event-callbacks;
    export lifecycle;
    export timer-callbacks;
    export animation-callbacks;
    export resize-observer-callbacks;
    export mutation-observer-callbacks;
    export media-query-list-callbacks;
    export scroll-callbacks;
    export window-resize-callbacks;
}

