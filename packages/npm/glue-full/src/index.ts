// @ts-nocheck
// Auto-generated aggregation package

import { INTERFACES as dom } from "@celestia/tairitsu-glue-dom";
import { INTERFACES as events } from "@celestia/tairitsu-glue-events";
import { INTERFACES as css } from "@celestia/tairitsu-glue-css";
import { INTERFACES as html } from "@celestia/tairitsu-glue-html";
import { INTERFACES as observers } from "@celestia/tairitsu-glue-observers";
import { INTERFACES as resize_observer } from "@celestia/tairitsu-glue-resize-observer";
import { INTERFACES as platform } from "@celestia/tairitsu-glue-platform";
import { INTERFACES as auth } from "@celestia/tairitsu-glue-auth";
import { INTERFACES as canvas } from "@celestia/tairitsu-glue-canvas";
import { INTERFACES as crypto } from "@celestia/tairitsu-glue-crypto";
import { INTERFACES as device } from "@celestia/tairitsu-glue-device";
import { INTERFACES as fetch } from "@celestia/tairitsu-glue-fetch";
import { INTERFACES as file_api } from "@celestia/tairitsu-glue-file-api";
import { INTERFACES as geolocation } from "@celestia/tairitsu-glue-geolocation";
import { INTERFACES as indexed_db } from "@celestia/tairitsu-glue-indexed-db";
import { INTERFACES as media } from "@celestia/tairitsu-glue-media";
import { INTERFACES as misc } from "@celestia/tairitsu-glue-misc";
import { INTERFACES as notifications } from "@celestia/tairitsu-glue-notifications";
import { INTERFACES as payments } from "@celestia/tairitsu-glue-payments";
import { INTERFACES as performance } from "@celestia/tairitsu-glue-performance";
import { INTERFACES as permissions } from "@celestia/tairitsu-glue-permissions";
import { INTERFACES as service_workers } from "@celestia/tairitsu-glue-service-workers";
import { INTERFACES as storage } from "@celestia/tairitsu-glue-storage";
import { INTERFACES as streams } from "@celestia/tairitsu-glue-streams";
import { INTERFACES as svg } from "@celestia/tairitsu-glue-svg";
import { INTERFACES as url } from "@celestia/tairitsu-glue-url";
import { INTERFACES as wasm } from "@celestia/tairitsu-glue-wasm";
import { INTERFACES as web_animations } from "@celestia/tairitsu-glue-web-animations";
import { INTERFACES as webrtc } from "@celestia/tairitsu-glue-webrtc";
import { INTERFACES as websocket } from "@celestia/tairitsu-glue-websocket";
import { INTERFACES as websockets } from "@celestia/tairitsu-glue-websockets";
import { INTERFACES as workers } from "@celestia/tairitsu-glue-workers";

export const INTERFACES = {
  ...dom,
  ...events,
  ...css,
  ...html,
  ...observers,
  ...resize_observer,
  ...platform,
  ...auth,
  ...canvas,
  ...crypto,
  ...device,
  ...fetch,
  ...file_api,
  ...geolocation,
  ...indexed_db,
  ...media,
  ...misc,
  ...notifications,
  ...payments,
  ...performance,
  ...permissions,
  ...service_workers,
  ...storage,
  ...streams,
  ...svg,
  ...url,
  ...wasm,
  ...web_animations,
  ...webrtc,
  ...websocket,
  ...websockets,
  ...workers,
};
