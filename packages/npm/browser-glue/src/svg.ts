// @ts-nocheck
// Auto-generated stub — delegates to browser-glue glue module

/**
 * svg glue — implements the `tairitsu-browser:svg` WIT import interfaces.
 *
 * Auto-generated from: /mnt/sdb1/tairitsu/packages/browser-glue/../../packages/browser-worlds/wit/generated/svg.wit
 * Generated by: scripts/generate_browser_glue.py
 *
 * DO NOT EDIT MANUALLY - regenerate with: just glue-gen
 *
 * All browser objects are represented as opaque bigint handles.
 */

// ---------------------------------------------------------------------------
// Custom type definitions
// ---------------------------------------------------------------------------

/** Type definition for DOMTokenListValue */
export type DOMTokenListValue = string;

/** Type definition for EventHandlerRecord */
export type EventHandlerRecord = any;

/** Type definition for WebGLObject */
export type WebGLObject = any;

/** Type definition for u64 */
export type u64 = bigint;

/** Type definition for RTCDataChannelBinaryType */
export type RTCDataChannelBinaryType = "blob" | "arraybuffer";

/** Type definition for CSSFontFaceDescriptors */
export type CSSFontFaceDescriptors = any;

/** Type definition for CSSFontFeatureValuesMap */
export type CSSFontFeatureValuesMap = any;

/** Type definition for OnBeforeUnloadEventHandlerRecord */
export type OnBeforeUnloadEventHandlerRecord = OnBeforeUnloadEventHandlerNonNull | null;

/** Type definition for OnErrorEventHandlerRecord */
export type OnErrorEventHandlerRecord = OnErrorEventHandlerNonNull | null;

/** Type definition for VoidFunctionRecord */
export type VoidFunctionRecord = VoidFunction;

/** Type definition for EventHandler */
export type EventHandler = (this: any, ev: any) => any;

/** Type definition for GeometryUtils */
export type GeometryUtils = any;

/** Type definition for HyperlinkElementUtils */
export type HyperlinkElementUtils = any;

/** Type definition for PopoverTargetAttributes */
export type PopoverTargetAttributes = any;

/** Type definition for CSSPageDescriptors */
export type CSSPageDescriptors = any;

/** Type definition for CSSMarginRule */
export type CSSMarginRule = CSSRule;

/** Type definition for CSSStyleProperties */
export type CSSStyleProperties = Record<string, string>;

/** Type definition for Origin */
export type Origin = string;

/** Type definition for FetchLaterResult */
export type FetchLaterResult = any;

/** Type definition for NotRestoredReasonDetails */
export type NotRestoredReasonDetails = any;

/** Type definition for NotRestoredReasons */
export type NotRestoredReasons = any;

/** Type definition for DeviceChangeEvent */
export type DeviceChangeEvent = Event;

/** Type definition for ChapterInformation */
export type ChapterInformation = any;

/** Type definition for PerformanceTimingConfidence */
export type PerformanceTimingConfidence = any;

/** Type definition for NotificationEvent */
export type NotificationEvent = Event;

/** Type definition for VisibilityStateEntry */
export type VisibilityStateEntry = PerformanceEntry;

/** Type definition for CommandEvent */
export type CommandEvent = Event;

/** Type definition for CloseWatcher */
export type CloseWatcher = EventTarget;

/** Type definition for CaptureController */
export type CaptureController = any;

/** Type definition for Navigation */
export type Navigation = any;

/** Type definition for NavigationTransition */
export type NavigationTransition = any;

/** Type definition for NavigateEvent */
export type NavigateEvent = Event;

/** Type definition for NavigationPrecommitController */
export type NavigationPrecommitController = any;

/** Type definition for NavigationDestination */
export type NavigationDestination = any;

/** Type definition for NavigationCurrentEntryChangeEvent */
export type NavigationCurrentEntryChangeEvent = Event;

/** Type definition for BatteryManager */
export type BatteryManager = any;

/** Type definition for AudioTrackList */
export type AudioTrackList = any;

/** Type definition for AudioTrack */
export type AudioTrack = any;

/** Type definition for VideoTrackList */
export type VideoTrackList = any;

/** Type definition for VideoTrack */
export type VideoTrack = any;

/** Type definition for WorkerGlobalScope */
export type WorkerGlobalScope = any;

/** Type definition for DedicatedWorkerGlobalScope */
export type DedicatedWorkerGlobalScope = any;

/** Type definition for SharedWorkerGlobalScope */
export type SharedWorkerGlobalScope = any;

/** Type definition for WorkerNavigator */
export type WorkerNavigator = any;

/** Type definition for WorkerLocation */
export type WorkerLocation = any;

/** Type definition for ServiceWorkerGlobalScope */
export type ServiceWorkerGlobalScope = any;

/** Type definition for Client */
export type Client = any;

/** Type definition for WindowClient */
export type WindowClient = any;

/** Type definition for Clients */
export type Clients = any;

/** Type definition for ExtendableEvent */
export type ExtendableEvent = Event;

/** Type definition for InstallEvent */
export type InstallEvent = Event;

/** Type definition for FetchEvent */
export type FetchEvent = Event;

/** Type definition for ExtendableMessageEvent */
export type ExtendableMessageEvent = MessageEvent;

/** Type definition for SpeechRecognition */
export type SpeechRecognition = any;

/** Type definition for SpeechRecognitionErrorEvent */
export type SpeechRecognitionErrorEvent = Event;

/** Type definition for SpeechRecognitionEvent */
export type SpeechRecognitionEvent = Event;

/** Type definition for SpeechGrammar */
export type SpeechGrammar = any;

/** Type definition for SpeechGrammarList */
export type SpeechGrammarList = any;

/** Type definition for SpeechRecognitionPhrase */
export type SpeechRecognitionPhrase = any;

/** Type definition for Module */
export type Module = typeof WebAssembly.Module;

/** Type definition for Instance */
export type Instance = typeof WebAssembly.Instance;

/** Type definition for Memory */
export type Memory = typeof WebAssembly.Memory;

/** Type definition for Table */
export type Table = typeof WebAssembly.Table;

/** Type definition for Global */
export type Global = typeof WebAssembly.Global;

/** Type definition for Exception */
export type Exception = any;

/** Type definition for HTMLString */
export type HTMLString = string;

/** Type definition for MediaText */
export type MediaText = string;

/** Type definition for HTMLHyperlinkHref */
export type HTMLHyperlinkHref = string;

/** Type definition for LocationHref */
export type LocationHref = string;

/** Type definition for URLHref */
export type URLHref = string;


// ---------------------------------------------------------------------------
// Async handle table for Promise-based operations
// ---------------------------------------------------------------------------

let _nextAsyncHandle = 1n;

interface AsyncHandle<T> {
  promise: Promise<T>;
  result: { ok: true; value: T } | { ok: false; error: string } | null;
}

const _asyncHandles = new Map<bigint, AsyncHandle<unknown>>();

// ---------------------------------------------------------------------------
// Synthetic handle tables for primitive/utility types
// ---------------------------------------------------------------------------

/** Handle table for attr values */
const _attrHandles = new Map<bigint, Attr>();
let _nextAttr = 1n;

/** Handle table for boolean values */
const _booleanHandles = new Map<bigint, boolean>();
let _nextBoolean = 1n;

/** Handle table for c-data-section values */
const _cdataSectionHandles = new Map<bigint, CDATASection>();
let _nextCdataSection = 1n;

/** Handle table for comment values */
const _commentHandles = new Map<bigint, Comment>();
let _nextComment = 1n;

/** Handle table for document values */
const _documentHandles = new Map<bigint, Document>();
let _nextDocument = 1n;

/** Handle table for document-fragment values */
const _documentFragmentHandles = new Map<bigint, DocumentFragment>();
let _nextDocumentFragment = 1n;

/** Handle table for document-type values */
const _documentTypeHandles = new Map<bigint, DocumentType>();
let _nextDocumentType = 1n;

/** Handle table for dom-implementation values */
const _domImplementationHandles = new Map<bigint, DOMImplementation>();
let _nextDomImplementation = 1n;

/** Handle table for element values */
const _elementHandles = new Map<bigint, Element>();
let _nextElement = 1n;

/** Handle table for element-list values */
const _elementListHandles = new Map<bigint, Element[]>();
let _nextElementList = 1n;

/** Handle table for event values */
const _eventHandles = new Map<bigint, Event>();
let _nextEvent = 1n;

/** Handle table for event-handler values */
const _eventHandlerHandles = new Map<bigint, EventHandler>();
let _nextEventHandler = 1n;

/** Handle table for event-target values */
const _eventTargetHandles = new Map<bigint, EventTarget>();
let _nextEventTarget = 1n;

/** Handle table for html-all-collection values */
const _htmlAllCollectionHandles = new Map<bigint, HTMLAllCollection>();
let _nextHtmlAllCollection = 1n;

/** Handle table for html-collection values */
const _htmlCollectionHandles = new Map<bigint, HTMLCollection>();
let _nextHtmlCollection = 1n;

/** Handle table for html-element values */
const _htmlElementHandles = new Map<bigint, HTMLElement>();
let _nextHtmlElement = 1n;

/** Handle table for location values */
const _locationHandles = new Map<bigint, Location>();
let _nextLocation = 1n;

/** Handle table for node values */
const _nodeHandles = new Map<bigint, Node>();
let _nextNode = 1n;

/** Handle table for node-filter values */
const _nodeFilterHandles = new Map<bigint, NodeFilter>();
let _nextNodeFilter = 1n;

/** Handle table for node-iterator values */
const _nodeIteratorHandles = new Map<bigint, NodeIterator>();
let _nextNodeIterator = 1n;

/** Handle table for node-list values */
const _nodeListHandles = new Map<bigint, NodeList>();
let _nextNodeList = 1n;

/** Handle table for processing-instruction values */
const _processingInstructionHandles = new Map<bigint, ProcessingInstruction>();
let _nextProcessingInstruction = 1n;

/** Handle table for range values */
const _rangeHandles = new Map<bigint, Range>();
let _nextRange = 1n;

/** Handle table for selection values */
const _selectionHandles = new Map<bigint, Selection>();
let _nextSelection = 1n;

/** Handle table for string values */
const _stringHandles = new Map<bigint, string>();
let _nextString = 1n;

/** Handle table for text values */
const _textHandles = new Map<bigint, Text>();
let _nextText = 1n;

/** Handle table for tree-walker values */
const _treeWalkerHandles = new Map<bigint, TreeWalker>();
let _nextTreeWalker = 1n;

/** Handle table for void values */
const _voidHandles = new Map<bigint, void>();
let _nextVoid = 1n;

/** Handle table for window values */
const _windowHandles = new Map<bigint, Window>();
let _nextWindow = 1n;

// ---------------------------------------------------------------------------
// Helper functions for handle lookups
// ---------------------------------------------------------------------------

/** Lookup a attr value by handle. */
function lookupAttr(handle: bigint): Attr {
  const obj = _attrHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`attr handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional attr value by handle. */
function lookupOptionAttr(handle: bigint | undefined): Attr | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _attrHandles.get(handle) ?? null;
}

/** Lookup a boolean value by handle. */
function lookupBoolean(handle: bigint): boolean {
  const obj = _booleanHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`boolean handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional boolean value by handle. */
function lookupOptionBoolean(handle: bigint | undefined): boolean | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _booleanHandles.get(handle) ?? null;
}

/** Lookup a c-data-section value by handle. */
function lookupCdataSection(handle: bigint): CDATASection {
  const obj = _cdataSectionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`c-data-section handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional c-data-section value by handle. */
function lookupOptionCdataSection(handle: bigint | undefined): CDATASection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _cdataSectionHandles.get(handle) ?? null;
}

/** Lookup a comment value by handle. */
function lookupComment(handle: bigint): Comment {
  const obj = _commentHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`comment handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional comment value by handle. */
function lookupOptionComment(handle: bigint | undefined): Comment | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _commentHandles.get(handle) ?? null;
}

/** Lookup a document value by handle. */
function lookupDocument(handle: bigint): Document {
  const obj = _documentHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document value by handle. */
function lookupOptionDocument(handle: bigint | undefined): Document | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentHandles.get(handle) ?? null;
}

/** Lookup a document-fragment value by handle. */
function lookupDocumentFragment(handle: bigint): DocumentFragment {
  const obj = _documentFragmentHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document-fragment handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document-fragment value by handle. */
function lookupOptionDocumentFragment(handle: bigint | undefined): DocumentFragment | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentFragmentHandles.get(handle) ?? null;
}

/** Lookup a document-type value by handle. */
function lookupDocumentType(handle: bigint): DocumentType {
  const obj = _documentTypeHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`document-type handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional document-type value by handle. */
function lookupOptionDocumentType(handle: bigint | undefined): DocumentType | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _documentTypeHandles.get(handle) ?? null;
}

/** Lookup a dom-implementation value by handle. */
function lookupDomImplementation(handle: bigint): DOMImplementation {
  const obj = _domImplementationHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`dom-implementation handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional dom-implementation value by handle. */
function lookupOptionDomImplementation(handle: bigint | undefined): DOMImplementation | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _domImplementationHandles.get(handle) ?? null;
}

/** Lookup a element value by handle. */
function lookupElement(handle: bigint): Element {
  const obj = _elementHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`element handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional element value by handle. */
function lookupOptionElement(handle: bigint | undefined): Element | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _elementHandles.get(handle) ?? null;
}

/** Lookup a element-list value by handle. */
function lookupElementList(handle: bigint): Element[] {
  const obj = _elementListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`element-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional element-list value by handle. */
function lookupOptionElementList(handle: bigint | undefined): Element[] | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _elementListHandles.get(handle) ?? null;
}

/** Lookup a event value by handle. */
function lookupEvent(handle: bigint): Event {
  const obj = _eventHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event value by handle. */
function lookupOptionEvent(handle: bigint | undefined): Event | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventHandles.get(handle) ?? null;
}

/** Lookup a event-handler value by handle. */
function lookupEventHandler(handle: bigint): EventHandler {
  const obj = _eventHandlerHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-handler handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-handler value by handle. */
function lookupOptionEventHandler(handle: bigint | undefined): EventHandler | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventHandlerHandles.get(handle) ?? null;
}

/** Lookup a event-target value by handle. */
function lookupEventTarget(handle: bigint): EventTarget {
  const obj = _eventTargetHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`event-target handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional event-target value by handle. */
function lookupOptionEventTarget(handle: bigint | undefined): EventTarget | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _eventTargetHandles.get(handle) ?? null;
}

/** Lookup a html-all-collection value by handle. */
function lookupHtmlAllCollection(handle: bigint): HTMLAllCollection {
  const obj = _htmlAllCollectionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`html-all-collection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional html-all-collection value by handle. */
function lookupOptionHtmlAllCollection(handle: bigint | undefined): HTMLAllCollection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlAllCollectionHandles.get(handle) ?? null;
}

/** Lookup a html-collection value by handle. */
function lookupHtmlCollection(handle: bigint): HTMLCollection {
  const obj = _htmlCollectionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`html-collection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional html-collection value by handle. */
function lookupOptionHtmlCollection(handle: bigint | undefined): HTMLCollection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlCollectionHandles.get(handle) ?? null;
}

/** Lookup a html-element value by handle. */
function lookupHtmlElement(handle: bigint): HTMLElement {
  const obj = _htmlElementHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`html-element handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional html-element value by handle. */
function lookupOptionHtmlElement(handle: bigint | undefined): HTMLElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _htmlElementHandles.get(handle) ?? null;
}

/** Lookup a location value by handle. */
function lookupLocation(handle: bigint): Location {
  const obj = _locationHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`location handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional location value by handle. */
function lookupOptionLocation(handle: bigint | undefined): Location | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _locationHandles.get(handle) ?? null;
}

/** Lookup a node value by handle. */
function lookupNode(handle: bigint): Node {
  const obj = _nodeHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node value by handle. */
function lookupOptionNode(handle: bigint | undefined): Node | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeHandles.get(handle) ?? null;
}

/** Lookup a node-filter value by handle. */
function lookupNodeFilter(handle: bigint): NodeFilter {
  const obj = _nodeFilterHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node-filter handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node-filter value by handle. */
function lookupOptionNodeFilter(handle: bigint | undefined): NodeFilter | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeFilterHandles.get(handle) ?? null;
}

/** Lookup a node-iterator value by handle. */
function lookupNodeIterator(handle: bigint): NodeIterator {
  const obj = _nodeIteratorHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node-iterator handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node-iterator value by handle. */
function lookupOptionNodeIterator(handle: bigint | undefined): NodeIterator | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeIteratorHandles.get(handle) ?? null;
}

/** Lookup a node-list value by handle. */
function lookupNodeList(handle: bigint): NodeList {
  const obj = _nodeListHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`node-list handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional node-list value by handle. */
function lookupOptionNodeList(handle: bigint | undefined): NodeList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _nodeListHandles.get(handle) ?? null;
}

/** Lookup a processing-instruction value by handle. */
function lookupProcessingInstruction(handle: bigint): ProcessingInstruction {
  const obj = _processingInstructionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`processing-instruction handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional processing-instruction value by handle. */
function lookupOptionProcessingInstruction(handle: bigint | undefined): ProcessingInstruction | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _processingInstructionHandles.get(handle) ?? null;
}

/** Lookup a range value by handle. */
function lookupRange(handle: bigint): Range {
  const obj = _rangeHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`range handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional range value by handle. */
function lookupOptionRange(handle: bigint | undefined): Range | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _rangeHandles.get(handle) ?? null;
}

/** Lookup a selection value by handle. */
function lookupSelection(handle: bigint): Selection {
  const obj = _selectionHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`selection handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional selection value by handle. */
function lookupOptionSelection(handle: bigint | undefined): Selection | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _selectionHandles.get(handle) ?? null;
}

/** Lookup a string value by handle. */
function lookupString(handle: bigint): string {
  const obj = _stringHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`string handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional string value by handle. */
function lookupOptionString(handle: bigint | undefined): string | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _stringHandles.get(handle) ?? null;
}

/** Lookup a text value by handle. */
function lookupText(handle: bigint): Text {
  const obj = _textHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`text handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional text value by handle. */
function lookupOptionText(handle: bigint | undefined): Text | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _textHandles.get(handle) ?? null;
}

/** Lookup a tree-walker value by handle. */
function lookupTreeWalker(handle: bigint): TreeWalker {
  const obj = _treeWalkerHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`tree-walker handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional tree-walker value by handle. */
function lookupOptionTreeWalker(handle: bigint | undefined): TreeWalker | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _treeWalkerHandles.get(handle) ?? null;
}

/** Lookup a void value by handle. */
function lookupVoid(handle: bigint): void {
  const obj = _voidHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`void handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional void value by handle. */
function lookupOptionVoid(handle: bigint | undefined): void | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _voidHandles.get(handle) ?? null;
}

/** Lookup a window value by handle. */
function lookupWindow(handle: bigint): Window {
  const obj = _windowHandles.get(handle);
  if (obj === undefined) {
    throw new Error(`window handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional window value by handle. */
function lookupOptionWindow(handle: bigint | undefined): Window | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _windowHandles.get(handle) ?? null;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgElementHandle = bigint;

/** Handle table for SvgElement instances */
const _svgElementhandles = new Map<bigint, SvgElement>();
let _nextSvgElement = 1n;

/** Lookup a SvgElement by handle, throwing if not found. */
function lookupSvgElement(handle: bigint): SvgElement {
  const obj = _svgElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgElement by handle. */
function lookupOptionSvgElement(handle: bigint | undefined): SvgElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgElementhandles.get(handle) ?? null;
}
/**
 * `get-class-name()` operation.
 */
export function getClassName(self: bigint): bigint {
  const obj = lookupSvgElement(self);
  return obj.className;
}

/**
 * `get-owner-svg-element()` operation.
 */
export function getOwnerSvgElement(self: bigint): bigint | undefined {
  const obj = lookupSvgElement(self);
  return obj.ownerSvgElement ?? undefined;
}

/**
 * `get-viewport-element()` operation.
 */
export function getViewportElement(self: bigint): bigint | undefined {
  const obj = lookupSvgElement(self);
  return obj.viewportElement ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-graphics-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgGraphicsElementHandle = bigint;

/** Handle table for SvgGraphicsElement instances */
const _svgGraphicsElementhandles = new Map<bigint, SvgGraphicsElement>();
let _nextSvgGraphicsElement = 1n;

/** Lookup a SvgGraphicsElement by handle, throwing if not found. */
function lookupSvgGraphicsElement(handle: bigint): SvgGraphicsElement {
  const obj = _svgGraphicsElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgGraphicsElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgGraphicsElement by handle. */
function lookupOptionSvgGraphicsElement(handle: bigint | undefined): SvgGraphicsElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgGraphicsElementhandles.get(handle) ?? null;
}
/**
 * `get-transform()` operation.
 */
export function getTransform(self: bigint): bigint {
  const obj = lookupSvgGraphicsElement(self);
  return obj.getTransform();
}

/**
 * `get-b-box()` operation.
 */
export function getBBox(self: bigint, options: bigint | undefined): DomRect {
  const obj = lookupSvgGraphicsElement(self);
  return obj.bBox;
}

/**
 * `get-ctm()` operation.
 */
export function getCtm(self: bigint): bigint | undefined {
  const obj = lookupSvgGraphicsElement(self);
  return obj.ctm ?? undefined;
}

/**
 * `get-screen-ctm()` operation.
 */
export function getScreenCtm(self: bigint): bigint | undefined {
  const obj = lookupSvgGraphicsElement(self);
  return obj.screenCtm ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-geometry-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgGeometryElementHandle = bigint;

/** Handle table for SvgGeometryElement instances */
const _svgGeometryElementhandles = new Map<bigint, SvgGeometryElement>();
let _nextSvgGeometryElement = 1n;

/** Lookup a SvgGeometryElement by handle, throwing if not found. */
function lookupSvgGeometryElement(handle: bigint): SvgGeometryElement {
  const obj = _svgGeometryElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgGeometryElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgGeometryElement by handle. */
function lookupOptionSvgGeometryElement(handle: bigint | undefined): SvgGeometryElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgGeometryElementhandles.get(handle) ?? null;
}
/**
 * `get-path-length()` operation.
 */
export function getPathLength(self: bigint): bigint {
  const obj = lookupSvgGeometryElement(self);
  return obj.pathLength;
}

/**
 * `is-point-in-fill()` operation.
 */
export function isPointInFill(self: bigint, point: bigint | undefined): boolean {
  const obj = lookupSvgGeometryElement(self);
  return obj.isPointInFill(point);
}

/**
 * `is-point-in-stroke()` operation.
 */
export function isPointInStroke(self: bigint, point: bigint | undefined): boolean {
  const obj = lookupSvgGeometryElement(self);
  return obj.isPointInStroke(point);
}

/**
 * `get-total-length()` operation.
 */
export function getTotalLength(self: bigint): number {
  const obj = lookupSvgGeometryElement(self);
  return obj.totalLength;
}

/**
 * `get-point-at-length()` operation.
 */
export function getPointAtLength(self: bigint, distance: number): bigint {
  const obj = lookupSvgGeometryElement(self);
  return obj.pointAtLength;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-number
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgNumberHandle = bigint;

/** Handle table for SvgNumber instances */
const _svgNumberhandles = new Map<bigint, SvgNumber>();
let _nextSvgNumber = 1n;

/** Lookup a SvgNumber by handle, throwing if not found. */
function lookupSvgNumber(handle: bigint): SvgNumber {
  const obj = _svgNumberhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgNumber handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgNumber by handle. */
function lookupOptionSvgNumber(handle: bigint | undefined): SvgNumber | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgNumberhandles.get(handle) ?? null;
}
/**
 * `get-value()` operation.
 */
export function SvgNumberGetValue(self: bigint): number {
  const obj = lookupSvgNumber(self);
  return obj.value;
}

/**
 * `set-value()` operation.
 */
export function SvgNumberSetValue(self: bigint, value: number): void {
  const obj = lookupSvgNumber(self);
  obj.value = value;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-length
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgLengthHandle = bigint;

/** Handle table for SvgLength instances */
const _svgLengthhandles = new Map<bigint, SvgLength>();
let _nextSvgLength = 1n;

/** Lookup a SvgLength by handle, throwing if not found. */
function lookupSvgLength(handle: bigint): SvgLength {
  const obj = _svgLengthhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgLength handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgLength by handle. */
function lookupOptionSvgLength(handle: bigint | undefined): SvgLength | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgLengthhandles.get(handle) ?? null;
}
/**
 * `get-unit-type()` operation.
 */
export function SvgLengthGetUnitType(self: bigint): number {
  const obj = lookupSvgLength(self);
  return obj.unitType;
}

/**
 * `get-value()` operation.
 */
export function SvgLengthGetValue(self: bigint): number {
  const obj = lookupSvgLength(self);
  return obj.value;
}

/**
 * `set-value()` operation.
 */
export function SvgLengthSetValue(self: bigint, value: number): void {
  const obj = lookupSvgLength(self);
  obj.value = value;
}

/**
 * `get-value-in-specified-units()` operation.
 */
export function SvgLengthGetValueInSpecifiedUnits(self: bigint): number {
  const obj = lookupSvgLength(self);
  return obj.valueInSpecifiedUnits;
}

/**
 * `set-value-in-specified-units()` operation.
 */
export function SvgLengthSetValueInSpecifiedUnits(self: bigint, value: number): void {
  const obj = lookupSvgLength(self);
  obj.valueInSpecifiedUnits = value;
}

/**
 * `get-value-as-string()` operation.
 */
export function SvgLengthGetValueAsString(self: bigint): string {
  const obj = lookupSvgLength(self);
  return obj.valueAsString;
}

/**
 * `set-value-as-string()` operation.
 */
export function SvgLengthSetValueAsString(self: bigint, value: string): void {
  const obj = lookupSvgLength(self);
  obj.valueAsString = value;
}

/**
 * `new-value-specified-units()` operation.
 */
export function SvgLengthNewValueSpecifiedUnits(self: bigint, unitType: number, valueInSpecifiedUnits: number): void {
  const obj = lookupSvgLength(self);
  obj.newValueSpecifiedUnits(unitType, valueInSpecifiedUnits);
}

/**
 * `convert-to-specified-units()` operation.
 */
export function SvgLengthConvertToSpecifiedUnits(self: bigint, unitType: number): void {
  const obj = lookupSvgLength(self);
  obj.convertToSpecifiedUnits(unitType);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-angle
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAngleHandle = bigint;

/** Handle table for SvgAngle instances */
const _svgAnglehandles = new Map<bigint, SvgAngle>();
let _nextSvgAngle = 1n;

/** Lookup a SvgAngle by handle, throwing if not found. */
function lookupSvgAngle(handle: bigint): SvgAngle {
  const obj = _svgAnglehandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAngle handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAngle by handle. */
function lookupOptionSvgAngle(handle: bigint | undefined): SvgAngle | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnglehandles.get(handle) ?? null;
}
/**
 * `get-unit-type()` operation.
 */
export function SvgAngleGetUnitType(self: bigint): number {
  const obj = lookupSvgAngle(self);
  return obj.unitType;
}

/**
 * `get-value()` operation.
 */
export function SvgAngleGetValue(self: bigint): number {
  const obj = lookupSvgAngle(self);
  return obj.value;
}

/**
 * `set-value()` operation.
 */
export function SvgAngleSetValue(self: bigint, value: number): void {
  const obj = lookupSvgAngle(self);
  obj.value = value;
}

/**
 * `get-value-in-specified-units()` operation.
 */
export function SvgAngleGetValueInSpecifiedUnits(self: bigint): number {
  const obj = lookupSvgAngle(self);
  return obj.valueInSpecifiedUnits;
}

/**
 * `set-value-in-specified-units()` operation.
 */
export function SvgAngleSetValueInSpecifiedUnits(self: bigint, value: number): void {
  const obj = lookupSvgAngle(self);
  obj.valueInSpecifiedUnits = value;
}

/**
 * `get-value-as-string()` operation.
 */
export function SvgAngleGetValueAsString(self: bigint): string {
  const obj = lookupSvgAngle(self);
  return obj.valueAsString;
}

/**
 * `set-value-as-string()` operation.
 */
export function SvgAngleSetValueAsString(self: bigint, value: string): void {
  const obj = lookupSvgAngle(self);
  obj.valueAsString = value;
}

/**
 * `new-value-specified-units()` operation.
 */
export function SvgAngleNewValueSpecifiedUnits(self: bigint, unitType: number, valueInSpecifiedUnits: number): void {
  const obj = lookupSvgAngle(self);
  obj.newValueSpecifiedUnits(unitType, valueInSpecifiedUnits);
}

/**
 * `convert-to-specified-units()` operation.
 */
export function SvgAngleConvertToSpecifiedUnits(self: bigint, unitType: number): void {
  const obj = lookupSvgAngle(self);
  obj.convertToSpecifiedUnits(unitType);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-number-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgNumberListHandle = bigint;

/** Handle table for SvgNumberList instances */
const _svgNumberListhandles = new Map<bigint, SvgNumberList>();
let _nextSvgNumberList = 1n;

/** Lookup a SvgNumberList by handle, throwing if not found. */
function lookupSvgNumberList(handle: bigint): SvgNumberList {
  const obj = _svgNumberListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgNumberList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgNumberList by handle. */
function lookupOptionSvgNumberList(handle: bigint | undefined): SvgNumberList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgNumberListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SvgNumberListGetLength(self: bigint): number {
  const obj = lookupSvgNumberList(self);
  return obj.length;
}

/**
 * `get-number-of-items()` operation.
 */
export function SvgNumberListGetNumberOfItems(self: bigint): number {
  const obj = lookupSvgNumberList(self);
  return obj.numberOfItems;
}

/**
 * `clear()` operation.
 */
export function SvgNumberListClear(self: bigint): void {
  const obj = lookupSvgNumberList(self);
  obj.clear();
}

/**
 * `initialize()` operation.
 */
export function SvgNumberListInitialize(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.initialize(newItem);
}

/**
 * `get-item()` operation.
 */
export function SvgNumberListGetItem(self: bigint, index: number): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.getItem(index);
}

/**
 * `insert-item-before()` operation.
 */
export function SvgNumberListInsertItemBefore(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.insertItemBefore(newItem, index);
}

/**
 * `replace-item()` operation.
 */
export function SvgNumberListReplaceItem(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.replaceItem(newItem, index);
}

/**
 * `remove-item()` operation.
 */
export function SvgNumberListRemoveItem(self: bigint, index: number): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.removeItem(index);
}

/**
 * `append-item()` operation.
 */
export function SvgNumberListAppendItem(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgNumberList(self);
  return obj.appendItem(newItem);
}

/**
 * `undefined()` operation.
 */
export function SvgNumberListUndefined(self: bigint, index: number, newItem: bigint): void {
  const obj = lookupSvgNumberList(self);
  obj.undefined(index, newItem);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-length-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgLengthListHandle = bigint;

/** Handle table for SvgLengthList instances */
const _svgLengthListhandles = new Map<bigint, SvgLengthList>();
let _nextSvgLengthList = 1n;

/** Lookup a SvgLengthList by handle, throwing if not found. */
function lookupSvgLengthList(handle: bigint): SvgLengthList {
  const obj = _svgLengthListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgLengthList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgLengthList by handle. */
function lookupOptionSvgLengthList(handle: bigint | undefined): SvgLengthList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgLengthListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SvgLengthListGetLength(self: bigint): number {
  const obj = lookupSvgLengthList(self);
  return obj.length;
}

/**
 * `get-number-of-items()` operation.
 */
export function SvgLengthListGetNumberOfItems(self: bigint): number {
  const obj = lookupSvgLengthList(self);
  return obj.numberOfItems;
}

/**
 * `clear()` operation.
 */
export function SvgLengthListClear(self: bigint): void {
  const obj = lookupSvgLengthList(self);
  obj.clear();
}

/**
 * `initialize()` operation.
 */
export function SvgLengthListInitialize(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.initialize(newItem);
}

/**
 * `get-item()` operation.
 */
export function SvgLengthListGetItem(self: bigint, index: number): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.getItem(index);
}

/**
 * `insert-item-before()` operation.
 */
export function SvgLengthListInsertItemBefore(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.insertItemBefore(newItem, index);
}

/**
 * `replace-item()` operation.
 */
export function SvgLengthListReplaceItem(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.replaceItem(newItem, index);
}

/**
 * `remove-item()` operation.
 */
export function SvgLengthListRemoveItem(self: bigint, index: number): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.removeItem(index);
}

/**
 * `append-item()` operation.
 */
export function SvgLengthListAppendItem(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgLengthList(self);
  return obj.appendItem(newItem);
}

/**
 * `undefined()` operation.
 */
export function SvgLengthListUndefined(self: bigint, index: number, newItem: bigint): void {
  const obj = lookupSvgLengthList(self);
  obj.undefined(index, newItem);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-string-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgStringListHandle = bigint;

/** Handle table for SvgStringList instances */
const _svgStringListhandles = new Map<bigint, SvgStringList>();
let _nextSvgStringList = 1n;

/** Lookup a SvgStringList by handle, throwing if not found. */
function lookupSvgStringList(handle: bigint): SvgStringList {
  const obj = _svgStringListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgStringList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgStringList by handle. */
function lookupOptionSvgStringList(handle: bigint | undefined): SvgStringList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgStringListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SvgStringListGetLength(self: bigint): number {
  const obj = lookupSvgStringList(self);
  return obj.length;
}

/**
 * `get-number-of-items()` operation.
 */
export function SvgStringListGetNumberOfItems(self: bigint): number {
  const obj = lookupSvgStringList(self);
  return obj.numberOfItems;
}

/**
 * `clear()` operation.
 */
export function SvgStringListClear(self: bigint): void {
  const obj = lookupSvgStringList(self);
  obj.clear();
}

/**
 * `initialize()` operation.
 */
export function SvgStringListInitialize(self: bigint, newItem: string): string {
  const obj = lookupSvgStringList(self);
  return obj.initialize(newItem);
}

/**
 * `get-item()` operation.
 */
export function SvgStringListGetItem(self: bigint, index: number): string {
  const obj = lookupSvgStringList(self);
  return obj.getItem(index);
}

/**
 * `insert-item-before()` operation.
 */
export function SvgStringListInsertItemBefore(self: bigint, newItem: string, index: number): string {
  const obj = lookupSvgStringList(self);
  return obj.insertItemBefore(newItem, index);
}

/**
 * `replace-item()` operation.
 */
export function SvgStringListReplaceItem(self: bigint, newItem: string, index: number): string {
  const obj = lookupSvgStringList(self);
  return obj.replaceItem(newItem, index);
}

/**
 * `remove-item()` operation.
 */
export function SvgStringListRemoveItem(self: bigint, index: number): string {
  const obj = lookupSvgStringList(self);
  return obj.removeItem(index);
}

/**
 * `append-item()` operation.
 */
export function SvgStringListAppendItem(self: bigint, newItem: string): string {
  const obj = lookupSvgStringList(self);
  return obj.appendItem(newItem);
}

/**
 * `undefined()` operation.
 */
export function SvgStringListUndefined(self: bigint, index: number, newItem: string): void {
  const obj = lookupSvgStringList(self);
  obj.undefined(index, newItem);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-boolean
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedBooleanHandle = bigint;

/** Handle table for SvgAnimatedBoolean instances */
const _svgAnimatedBooleanhandles = new Map<bigint, SvgAnimatedBoolean>();
let _nextSvgAnimatedBoolean = 1n;

/** Lookup a SvgAnimatedBoolean by handle, throwing if not found. */
function lookupSvgAnimatedBoolean(handle: bigint): SvgAnimatedBoolean {
  const obj = _svgAnimatedBooleanhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedBoolean handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedBoolean by handle. */
function lookupOptionSvgAnimatedBoolean(handle: bigint | undefined): SvgAnimatedBoolean | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedBooleanhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedBooleanGetBaseVal(self: bigint): boolean {
  const obj = lookupSvgAnimatedBoolean(self);
  return obj.baseVal;
}

/**
 * `set-base-val()` operation.
 */
export function SvgAnimatedBooleanSetBaseVal(self: bigint, value: boolean): void {
  const obj = lookupSvgAnimatedBoolean(self);
  obj.baseVal = value;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedBooleanGetAnimVal(self: bigint): boolean {
  const obj = lookupSvgAnimatedBoolean(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-enumeration
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedEnumerationHandle = bigint;

/** Handle table for SvgAnimatedEnumeration instances */
const _svgAnimatedEnumerationhandles = new Map<bigint, SvgAnimatedEnumeration>();
let _nextSvgAnimatedEnumeration = 1n;

/** Lookup a SvgAnimatedEnumeration by handle, throwing if not found. */
function lookupSvgAnimatedEnumeration(handle: bigint): SvgAnimatedEnumeration {
  const obj = _svgAnimatedEnumerationhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedEnumeration handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedEnumeration by handle. */
function lookupOptionSvgAnimatedEnumeration(handle: bigint | undefined): SvgAnimatedEnumeration | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedEnumerationhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedEnumerationGetBaseVal(self: bigint): number {
  const obj = lookupSvgAnimatedEnumeration(self);
  return obj.baseVal;
}

/**
 * `set-base-val()` operation.
 */
export function SvgAnimatedEnumerationSetBaseVal(self: bigint, value: number): void {
  const obj = lookupSvgAnimatedEnumeration(self);
  obj.baseVal = value;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedEnumerationGetAnimVal(self: bigint): number {
  const obj = lookupSvgAnimatedEnumeration(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-integer
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedIntegerHandle = bigint;

/** Handle table for SvgAnimatedInteger instances */
const _svgAnimatedIntegerhandles = new Map<bigint, SvgAnimatedInteger>();
let _nextSvgAnimatedInteger = 1n;

/** Lookup a SvgAnimatedInteger by handle, throwing if not found. */
function lookupSvgAnimatedInteger(handle: bigint): SvgAnimatedInteger {
  const obj = _svgAnimatedIntegerhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedInteger handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedInteger by handle. */
function lookupOptionSvgAnimatedInteger(handle: bigint | undefined): SvgAnimatedInteger | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedIntegerhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedIntegerGetBaseVal(self: bigint): number {
  const obj = lookupSvgAnimatedInteger(self);
  return obj.baseVal;
}

/**
 * `set-base-val()` operation.
 */
export function SvgAnimatedIntegerSetBaseVal(self: bigint, value: number): void {
  const obj = lookupSvgAnimatedInteger(self);
  obj.baseVal = value;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedIntegerGetAnimVal(self: bigint): number {
  const obj = lookupSvgAnimatedInteger(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-number
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedNumberHandle = bigint;

/** Handle table for SvgAnimatedNumber instances */
const _svgAnimatedNumberhandles = new Map<bigint, SvgAnimatedNumber>();
let _nextSvgAnimatedNumber = 1n;

/** Lookup a SvgAnimatedNumber by handle, throwing if not found. */
function lookupSvgAnimatedNumber(handle: bigint): SvgAnimatedNumber {
  const obj = _svgAnimatedNumberhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedNumber handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedNumber by handle. */
function lookupOptionSvgAnimatedNumber(handle: bigint | undefined): SvgAnimatedNumber | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedNumberhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedNumberGetBaseVal(self: bigint): number {
  const obj = lookupSvgAnimatedNumber(self);
  return obj.baseVal;
}

/**
 * `set-base-val()` operation.
 */
export function SvgAnimatedNumberSetBaseVal(self: bigint, value: number): void {
  const obj = lookupSvgAnimatedNumber(self);
  obj.baseVal = value;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedNumberGetAnimVal(self: bigint): number {
  const obj = lookupSvgAnimatedNumber(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-length
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedLengthHandle = bigint;

/** Handle table for SvgAnimatedLength instances */
const _svgAnimatedLengthhandles = new Map<bigint, SvgAnimatedLength>();
let _nextSvgAnimatedLength = 1n;

/** Lookup a SvgAnimatedLength by handle, throwing if not found. */
function lookupSvgAnimatedLength(handle: bigint): SvgAnimatedLength {
  const obj = _svgAnimatedLengthhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedLength handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedLength by handle. */
function lookupOptionSvgAnimatedLength(handle: bigint | undefined): SvgAnimatedLength | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedLengthhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedLengthGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedLength(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedLengthGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedLength(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-angle
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedAngleHandle = bigint;

/** Handle table for SvgAnimatedAngle instances */
const _svgAnimatedAnglehandles = new Map<bigint, SvgAnimatedAngle>();
let _nextSvgAnimatedAngle = 1n;

/** Lookup a SvgAnimatedAngle by handle, throwing if not found. */
function lookupSvgAnimatedAngle(handle: bigint): SvgAnimatedAngle {
  const obj = _svgAnimatedAnglehandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedAngle handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedAngle by handle. */
function lookupOptionSvgAnimatedAngle(handle: bigint | undefined): SvgAnimatedAngle | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedAnglehandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedAngleGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedAngle(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedAngleGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedAngle(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-string
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedStringHandle = bigint;

/** Handle table for SvgAnimatedString instances */
const _svgAnimatedStringhandles = new Map<bigint, SvgAnimatedString>();
let _nextSvgAnimatedString = 1n;

/** Lookup a SvgAnimatedString by handle, throwing if not found. */
function lookupSvgAnimatedString(handle: bigint): SvgAnimatedString {
  const obj = _svgAnimatedStringhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedString handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedString by handle. */
function lookupOptionSvgAnimatedString(handle: bigint | undefined): SvgAnimatedString | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedStringhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedStringGetBaseVal(self: bigint): string {
  const obj = lookupSvgAnimatedString(self);
  return obj.baseVal;
}

/**
 * `set-base-val()` operation.
 */
export function SvgAnimatedStringSetBaseVal(self: bigint, value: string): void {
  const obj = lookupSvgAnimatedString(self);
  obj.baseVal = value;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedStringGetAnimVal(self: bigint): string {
  const obj = lookupSvgAnimatedString(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-rect
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedRectHandle = bigint;

/** Handle table for SvgAnimatedRect instances */
const _svgAnimatedRecthandles = new Map<bigint, SvgAnimatedRect>();
let _nextSvgAnimatedRect = 1n;

/** Lookup a SvgAnimatedRect by handle, throwing if not found. */
function lookupSvgAnimatedRect(handle: bigint): SvgAnimatedRect {
  const obj = _svgAnimatedRecthandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedRect handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedRect by handle. */
function lookupOptionSvgAnimatedRect(handle: bigint | undefined): SvgAnimatedRect | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedRecthandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedRectGetBaseVal(self: bigint): DomRect {
  const obj = lookupSvgAnimatedRect(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedRectGetAnimVal(self: bigint): DomRect {
  const obj = lookupSvgAnimatedRect(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-number-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedNumberListHandle = bigint;

/** Handle table for SvgAnimatedNumberList instances */
const _svgAnimatedNumberListhandles = new Map<bigint, SvgAnimatedNumberList>();
let _nextSvgAnimatedNumberList = 1n;

/** Lookup a SvgAnimatedNumberList by handle, throwing if not found. */
function lookupSvgAnimatedNumberList(handle: bigint): SvgAnimatedNumberList {
  const obj = _svgAnimatedNumberListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedNumberList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedNumberList by handle. */
function lookupOptionSvgAnimatedNumberList(handle: bigint | undefined): SvgAnimatedNumberList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedNumberListhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedNumberListGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedNumberList(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedNumberListGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedNumberList(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-length-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedLengthListHandle = bigint;

/** Handle table for SvgAnimatedLengthList instances */
const _svgAnimatedLengthListhandles = new Map<bigint, SvgAnimatedLengthList>();
let _nextSvgAnimatedLengthList = 1n;

/** Lookup a SvgAnimatedLengthList by handle, throwing if not found. */
function lookupSvgAnimatedLengthList(handle: bigint): SvgAnimatedLengthList {
  const obj = _svgAnimatedLengthListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedLengthList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedLengthList by handle. */
function lookupOptionSvgAnimatedLengthList(handle: bigint | undefined): SvgAnimatedLengthList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedLengthListhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedLengthListGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedLengthList(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedLengthListGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedLengthList(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-tests
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTestsHandle = bigint;

/** Handle table for SvgTests instances */
const _svgTestshandles = new Map<bigint, SvgTests>();
let _nextSvgTests = 1n;

/** Lookup a SvgTests by handle, throwing if not found. */
function lookupSvgTests(handle: bigint): SvgTests {
  const obj = _svgTestshandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTests handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTests by handle. */
function lookupOptionSvgTests(handle: bigint | undefined): SvgTests | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTestshandles.get(handle) ?? null;
}
/**
 * `get-required-extensions()` operation.
 */
export function getRequiredExtensions(self: bigint): bigint {
  const obj = lookupSvgTests(self);
  return obj.requiredExtensions;
}

/**
 * `get-system-language()` operation.
 */
export function getSystemLanguage(self: bigint): bigint {
  const obj = lookupSvgTests(self);
  return obj.systemLanguage;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-fit-to-view-box
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgFitToViewBoxHandle = bigint;

/** Handle table for SvgFitToViewBox instances */
const _svgFitToViewBoxhandles = new Map<bigint, SvgFitToViewBox>();
let _nextSvgFitToViewBox = 1n;

/** Lookup a SvgFitToViewBox by handle, throwing if not found. */
function lookupSvgFitToViewBox(handle: bigint): SvgFitToViewBox {
  const obj = _svgFitToViewBoxhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgFitToViewBox handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgFitToViewBox by handle. */
function lookupOptionSvgFitToViewBox(handle: bigint | undefined): SvgFitToViewBox | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgFitToViewBoxhandles.get(handle) ?? null;
}
/**
 * `get-view-box()` operation.
 */
export function getViewBox(self: bigint): bigint {
  const obj = lookupSvgFitToViewBox(self);
  return obj.viewBox;
}

/**
 * `get-preserve-aspect-ratio()` operation.
 */
export function SvgFitToViewBoxGetPreserveAspectRatio(self: bigint): bigint {
  const obj = lookupSvgFitToViewBox(self);
  return obj.preserveAspectRatio;
}

// ---------------------------------------------------------------------------
// WIT interface: svguri-reference
// ---------------------------------------------------------------------------

/** Type alias */
export type SvguriReferenceHandle = bigint;

/** Handle table for SvguriReference instances */
const _svguriReferencehandles = new Map<bigint, SvguriReference>();
let _nextSvguriReference = 1n;

/** Lookup a SvguriReference by handle, throwing if not found. */
function lookupSvguriReference(handle: bigint): SvguriReference {
  const obj = _svguriReferencehandles.get(handle);
  if (!obj) {
    throw new Error(`SvguriReference handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvguriReference by handle. */
function lookupOptionSvguriReference(handle: bigint | undefined): SvguriReference | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svguriReferencehandles.get(handle) ?? null;
}
/**
 * `get-href()` operation.
 */
export function getHref(self: bigint): bigint {
  const obj = lookupSvguriReference(self);
  return obj.href;
}

// ---------------------------------------------------------------------------
// WIT interface: document
// ---------------------------------------------------------------------------

/** Type alias */
export type DocumentHandle = bigint;

/** Handle for global singleton Document (fixed to 0n). */
const _Document_HANDLE = 0n;

/** Get the global Document object. */
function getGlobalDocument(): Document {
  return document;
}

/**
 * `get-root-element()` operation.
 */
export function getRootElement(): bigint | undefined {
  return document.rootElement ?? undefined;
}

/**
 * `element-from-point()` operation.
 */
export function elementFromPoint(x: number, y: number): bigint | undefined {
  const _callResult = document.elementFromPoint(Number(x), Number(y));
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `elements-from-point()` operation.
 */
export function elementsFromPoint(x: number, y: number): bigint {
  const _callResult = document.elementsFromPoint(x, y);
  const handle = _nextElementList++;
  _elementListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `caret-position-from-point()` operation.
 */
export function caretPositionFromPoint(x: number, y: number, options: bigint | undefined): bigint | undefined {
  const _callResult = document.caretPositionFromPoint(x, y, options as any);
  if (_callResult === null) return undefined;
  const handle = _nextCaretPosition++;
  _caretPositionhandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-scrolling-element()` operation.
 */
export function getScrollingElement(): bigint | undefined {
  const _callResult = document.scrollingElement;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-implementation()` operation.
 */
export function getImplementation(): bigint {
  const _callResult = document.implementation;
  const handle = _nextDomImplementation++;
  _domImplementationHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-url()` operation.
 */
export function getUrl(): string {
  return document.URL;
}

/**
 * `get-document-uri()` operation.
 */
export function getDocumentUri(): string {
  return document.documentURI;
}

/**
 * `get-compat-mode()` operation.
 */
export function getCompatMode(): string {
  return document.compatMode;
}

/**
 * `get-character-set()` operation.
 */
export function getCharacterSet(): string {
  return document.characterSet;
}

/**
 * `get-charset()` operation.
 */
export function getCharset(): string {
  return document.charset;
}

/**
 * `get-input-encoding()` operation.
 */
export function getInputEncoding(): string {
  return document.inputEncoding;
}

/**
 * `get-content-type()` operation.
 */
export function getContentType(): string {
  return document.contentType;
}

/**
 * `get-doctype()` operation.
 */
export function getDoctype(): bigint | undefined {
  const _callResult = document.doctype;
  if (_callResult === null) return undefined;
  const handle = _nextDocumentType++;
  _documentTypeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-document-element()` operation.
 */
export function getDocumentElement(): bigint | undefined {
  const _callResult = document.documentElement;
  if (_callResult === null) return undefined;
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-elements-by-tag-name()` operation.
 */
export function getElementsByTagName(qualifiedName: string): bigint {
  const _callResult = document.getElementsByTagName(qualifiedName);
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-elements-by-tag-name-ns()` operation.
 */
export function getElementsByTagNameNs(namespace: string | undefined, localName: string): bigint {
  const _callResult = (document as any).getElementsByTagNameNs(namespace, localName as any);
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-elements-by-class-name()` operation.
 */
export function getElementsByClassName(classNames: string): bigint {
  const _callResult = document.getElementsByClassName(classNames);
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-element()` operation.
 */
export function createElement(localName: string, options: string | undefined): bigint {
  const _callResult = (document as any).createElement(localName, options);
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-element-ns()` operation.
 */
export function createElementNs(namespace: string | undefined, qualifiedName: string, options: string | undefined): bigint {
  const _callResult = (document as any).createElementNS(namespace as any, qualifiedName as any, options);
  const handle = _nextElement++;
  _elementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-document-fragment()` operation.
 */
export function createDocumentFragment(): bigint {
  const _callResult = document.createDocumentFragment();
  const handle = _nextDocumentFragment++;
  _documentFragmentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-text-node()` operation.
 */
export function createTextNode(data: string): bigint {
  const _callResult = document.createTextNode(data as any);
  const handle = _nextText++;
  _textHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-cdata-section()` operation.
 */
export function createCdataSection(data: string): bigint {
  const _callResult = document.createCDATASection(data as any);
  const handle = _nextCdataSection++;
  _cdataSectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-comment()` operation.
 */
export function createComment(data: string): bigint {
  const _callResult = document.createComment(data as any);
  const handle = _nextComment++;
  _commentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-processing-instruction()` operation.
 */
export function createProcessingInstruction(target: string, data: string): bigint {
  const _callResult = document.createProcessingInstruction(target as any, data as any);
  const handle = _nextProcessingInstruction++;
  _processingInstructionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `import-node()` operation.
 */
export function importNode(node: bigint, options: boolean | undefined): bigint {
  const _callResult = document.importNode(lookupNode(node), options);
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `adopt-node()` operation.
 */
export function adoptNode(node: bigint): bigint {
  const _callResult = document.adoptNode(lookupNode(node));
  const handle = _nextNode++;
  _nodeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-attribute()` operation.
 */
export function createAttribute(localName: string): bigint {
  const _callResult = document.createAttribute(localName);
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-attribute-ns()` operation.
 */
export function createAttributeNs(namespace: string | undefined, qualifiedName: string): bigint {
  const _callResult = document.createAttributeNS(namespace as any, qualifiedName as any);
  const handle = _nextAttr++;
  _attrHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-event()` operation.
 */
export function createEvent(_interface: string): bigint {
  const _callResult = document.createEvent(_interface);
  const handle = _nextEvent++;
  _eventHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-range()` operation.
 */
export function createRange(): bigint {
  const _callResult = document.createRange();
  const handle = _nextRange++;
  _rangeHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-node-iterator()` operation.
 */
export function createNodeIterator(root: bigint, whatToShow: number | undefined, filter: bigint | undefined): bigint {
  const _callResult = document.createNodeIterator(lookupNode(root), whatToShow, lookupOptionNodeFilter(filter));
  const handle = _nextNodeIterator++;
  _nodeIteratorHandles.set(handle, _callResult);
  return handle;
}

/**
 * `create-tree-walker()` operation.
 */
export function createTreeWalker(root: bigint, whatToShow: number | undefined, filter: bigint | undefined): bigint {
  const _callResult = document.createTreeWalker(lookupNode(root), whatToShow, lookupOptionNodeFilter(filter));
  const handle = _nextTreeWalker++;
  _treeWalkerHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-fullscreen-enabled()` operation.
 */
export function getFullscreenEnabled(): boolean {
  return document.fullscreenEnabled;
}

/**
 * `get-fullscreen()` operation.
 */
export function getFullscreen(): boolean {
  return document.fullscreen;
}

/**
 * `exit-fullscreen()` operation.
 *
 * Async operation: returns request ID, poll with `pollExitFullscreen()`
 */
export function exitFullscreen(): bigint {
  const requestId = _nextAsyncHandle++;
  const promise = document.exitFullscreen()
    .then((result: unknown) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: true, value: result };
      }
    })
    .catch((err: Error) => {
      const entry = _asyncHandles.get(requestId);
      if (entry) {
        entry.result = { ok: false, error: err.message };
      }
    });

  _asyncHandles.set(requestId, { promise, result: null });
  return requestId;
}

/**
 * Poll an async `exitFullscreen()` operation.
 * Returns undefined if still pending, or the result if complete.
 */
export function pollExitFullscreen(requestId: bigint): { ok: true; value: bigint } | { ok: false; error: string } | undefined {
  const entry = _asyncHandles.get(requestId);
  if (!entry) {
    return { ok: false, error: `Unknown request ID ${requestId}` };
  }
  return entry.result as { ok: true; value: bigint } | { ok: false; error: string } | null ?? undefined;
}

/**
 * `get-onfullscreenchange()` operation.
 */
export function getOnfullscreenchange(): bigint {
  const handler = document.onfullscreenchange;
  if (handler == null) return 0n;
  const handle = _nextEventHandler++;
  _eventHandlerHandles.set(handle, handler);
  return handle;
}

/**
 * `set-onfullscreenchange()` operation.
 */
export function setOnfullscreenchange(value: bigint): void {
  document.onfullscreenchange = value as any;
}

/**
 * `get-onfullscreenerror()` operation.
 */
export function getOnfullscreenerror(): bigint {
  const handler = document.onfullscreenerror;
  if (handler == null) return 0n;
  const handle = _nextEventHandler++;
  _eventHandlerHandles.set(handle, handler);
  return handle;
}

/**
 * `set-onfullscreenerror()` operation.
 */
export function setOnfullscreenerror(value: bigint): void {
  document.onfullscreenerror = value as any;
}

/**
 * `parse-html-unsafe()` operation.
 */
export function parseHtmlUnsafe(html: string): bigint {
  return (document as any).parseHtmlUnsafe(html);
}

/**
 * `get-location()` operation.
 */
export function getLocation(): bigint | undefined {
  const _callResult = document.location;
  if (_callResult === null) return undefined;
  const handle = _nextLocation++;
  _locationHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-domain()` operation.
 */
export function getDomain(): bigint {
  const _callResult = document.domain;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-domain()` operation.
 */
export function setDomain(value: string): void {
  document.domain = value;
}

/**
 * `get-referrer()` operation.
 */
export function getReferrer(): bigint {
  const _callResult = document.referrer;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-cookie()` operation.
 */
export function getCookie(): bigint {
  const _callResult = document.cookie;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-cookie()` operation.
 */
export function setCookie(value: string): void {
  (document as any).getSetCookie = value;
}

/**
 * `get-last-modified()` operation.
 */
export function getLastModified(): bigint {
  const _callResult = document.lastModified;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-ready-state()` operation.
 */
export function getReadyState(): bigint {
  const _callResult = document.readyState;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `object()` operation.
 */
export function object(name: string): void {
  (document as any).object(name);
}

/**
 * `get-title()` operation.
 */
export function DocumentGetTitle(): bigint {
  const _callResult = document.title;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-title()` operation.
 */
export function DocumentSetTitle(value: string): void {
  document.title = value;
}

/**
 * `get-dir()` operation.
 */
export function getDir(): bigint {
  const _callResult = document.dir;
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-dir()` operation.
 */
export function setDir(value: string): void {
  document.dir = value;
}

/**
 * `get-body()` operation.
 */
export function getBody(): bigint | undefined {
  const _callResult = document.body;
  if (_callResult === null) return undefined;
  const handle = _nextHtmlElement++;
  _htmlElementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `set-body()` operation.
 */
export function setBody(value: bigint | undefined): void {
  document.body = lookupOptionHtmlElement(value) as any;
}

/**
 * `get-head()` operation.
 */
export function getHead(): bigint | undefined {
  const _callResult = document.head;
  if (_callResult === null) return undefined;
  const handle = _nextHtmlElement++;
  _htmlElementHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-images()` operation.
 */
export function getImages(): bigint {
  const _callResult = document.images;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-embeds()` operation.
 */
export function getEmbeds(): bigint {
  const _callResult = document.embeds;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-plugins()` operation.
 */
export function getPlugins(): bigint {
  const _callResult = document.plugins;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-links()` operation.
 */
export function getLinks(): bigint {
  const _callResult = document.links;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-forms()` operation.
 */
export function getForms(): bigint {
  const _callResult = document.forms;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-scripts()` operation.
 */
export function getScripts(): bigint {
  const _callResult = document.scripts;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-elements-by-name()` operation.
 */
export function getElementsByName(elementName: string): bigint {
  const _callResult = document.getElementsByName(elementName);
  const handle = _nextNodeList++;
  _nodeListHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-current-script()` operation.
 */
export function getCurrentScript(): bigint | undefined {
  const _callResult = document.currentScript;
  if (_callResult === null) return undefined;
  const handle = _nextEventTarget++;
  _eventTargetHandles.set(handle, _callResult);
  return handle;
}

/**
 * `open()` operation.
 */
export function open(unused1: string | undefined, unused2: string | undefined): bigint {
  const _callResult = document.open(unused1, unused2);
  const handle = _nextDocument++;
  _documentHandles.set(handle, _callResult);
  return handle;
}

/**
 * `close()` operation.
 */
export function close(): void {
  document.close();
}

/**
 * `write()` operation.
 */
export function write(text: (string)[]): void {
  document.write(text as any);
}

/**
 * `writeln()` operation.
 */
export function writeln(text: (string)[]): void {
  document.writeln(text as any);
}

/**
 * `get-default-view()` operation.
 */
export function getDefaultView(): bigint | undefined {
  const _callResult = document.defaultView;
  if (_callResult === null) return undefined;
  const handle = _nextWindow++;
  _windowHandles.set(handle, _callResult);
  return handle;
}

/**
 * `has-focus()` operation.
 */
export function hasFocus(): bigint {
  const _callResult = document.hasFocus();
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-design-mode()` operation.
 */
export function getDesignMode(): string {
  return document.designMode;
}

/**
 * `set-design-mode()` operation.
 */
export function setDesignMode(value: string): void {
  document.designMode = value;
}

/**
 * `exec-command()` operation.
 */
export function execCommand(commandId: string, showUi: boolean | undefined, value: string | undefined): bigint {
  const _callResult = document.execCommand(commandId, showUi, value);
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-command-enabled()` operation.
 */
export function queryCommandEnabled(commandId: string): bigint {
  const _callResult = document.queryCommandEnabled(commandId);
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-command-indeterm()` operation.
 */
export function queryCommandIndeterm(commandId: string): bigint {
  const _callResult = document.queryCommandIndeterm(commandId);
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-command-state()` operation.
 */
export function queryCommandState(commandId: string): bigint {
  const _callResult = document.queryCommandState(commandId);
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-command-supported()` operation.
 */
export function queryCommandSupported(commandId: string): bigint {
  const _callResult = document.queryCommandSupported(commandId);
  const handle = _nextBoolean++;
  _booleanHandles.set(handle, _callResult);
  return handle;
}

/**
 * `query-command-value()` operation.
 */
export function queryCommandValue(commandId: string): bigint {
  const _callResult = document.queryCommandValue(commandId);
  const handle = _nextString++;
  _stringHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-hidden()` operation.
 */
export function getHidden(): boolean {
  return document.hidden;
}

/**
 * `get-visibility-state()` operation.
 */
export function getVisibilityState(): bigint {
  const value = document.visibilityState;
  switch (value) {
    case 'visible': return 0n;
    case 'hidden': return 1n;
    default: return 0n;
  }
}

/**
 * `get-onreadystatechange()` operation.
 */
export function getOnreadystatechange(): bigint {
  const handler = document.onreadystatechange;
  if (handler == null) return 0n;
  const handle = _nextEventHandler++;
  _eventHandlerHandles.set(handle, handler);
  return handle;
}

/**
 * `set-onreadystatechange()` operation.
 */
export function setOnreadystatechange(value: bigint): void {
  document.onreadystatechange = value as any;
}

/**
 * `get-onvisibilitychange()` operation.
 */
export function getOnvisibilitychange(): bigint {
  const handler = document.onvisibilitychange;
  if (handler == null) return 0n;
  const handle = _nextEventHandler++;
  _eventHandlerHandles.set(handle, handler);
  return handle;
}

/**
 * `set-onvisibilitychange()` operation.
 */
export function setOnvisibilitychange(value: bigint): void {
  document.onvisibilitychange = value as any;
}

/**
 * `get-fg-color()` operation.
 */
export function getFgColor(): string {
  return document.fgColor;
}

/**
 * `set-fg-color()` operation.
 */
export function setFgColor(value: string): void {
  document.fgColor = value;
}

/**
 * `get-link-color()` operation.
 */
export function getLinkColor(): string {
  return document.linkColor;
}

/**
 * `set-link-color()` operation.
 */
export function setLinkColor(value: string): void {
  document.linkColor = value;
}

/**
 * `get-vlink-color()` operation.
 */
export function getVlinkColor(): string {
  return document.vlinkColor;
}

/**
 * `set-vlink-color()` operation.
 */
export function setVlinkColor(value: string): void {
  document.vlinkColor = value;
}

/**
 * `get-alink-color()` operation.
 */
export function getAlinkColor(): string {
  return document.alinkColor;
}

/**
 * `set-alink-color()` operation.
 */
export function setAlinkColor(value: string): void {
  document.alinkColor = value;
}

/**
 * `get-bg-color()` operation.
 */
export function getBgColor(): string {
  return document.bgColor;
}

/**
 * `set-bg-color()` operation.
 */
export function setBgColor(value: string): void {
  document.bgColor = value;
}

/**
 * `get-anchors()` operation.
 */
export function getAnchors(): bigint {
  const _callResult = document.anchors;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-applets()` operation.
 */
export function getApplets(): bigint {
  const _callResult = document.applets;
  const handle = _nextHtmlCollection++;
  _htmlCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `clear()` operation.
 */
export function DocumentClear(): void {
  document.clear();
}

/**
 * `capture-events()` operation.
 */
export function captureEvents(): void {
  document.captureEvents();
}

/**
 * `release-events()` operation.
 */
export function releaseEvents(): void {
  document.releaseEvents();
}

/**
 * `get-all()` operation.
 */
export function getAll(): bigint {
  const _callResult = document.all;
  const handle = _nextHtmlAllCollection++;
  _htmlAllCollectionHandles.set(handle, _callResult);
  return handle;
}

/**
 * `get-selection()` operation.
 */
export function getSelection(): bigint | undefined {
  const _callResult = document.getSelection();
  if (_callResult === null) return undefined;
  const handle = _nextSelection++;
  _selectionHandles.set(handle, _callResult);
  return handle;
}

// ---------------------------------------------------------------------------
// WIT interface: svgsvg-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgsvgElementHandle = bigint;

/** Handle table for SvgsvgElement instances */
const _svgsvgElementhandles = new Map<bigint, SvgsvgElement>();
let _nextSvgsvgElement = 1n;

/** Register a new SvgsvgElement and return its handle. */
function registerSvgsvgElement(obj: SvgsvgElement): bigint {
  const handle = _nextSvgsvgElement++;
  _svgsvgElementhandles.set(handle, obj);
  return handle;
}
/** Lookup a SvgsvgElement by handle, throwing if not found. */
function lookupSvgsvgElement(handle: bigint): SvgsvgElement {
  const obj = _svgsvgElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgsvgElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgsvgElement by handle. */
function lookupOptionSvgsvgElement(handle: bigint | undefined): SvgsvgElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgsvgElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgsvgElementGetX(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgsvgElementGetY(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgsvgElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgsvgElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.height;
}

/**
 * `get-current-scale()` operation.
 */
export function getCurrentScale(self: bigint): number {
  const obj = lookupSvgsvgElement(self);
  return obj.currentScale;
}

/**
 * `set-current-scale()` operation.
 */
export function setCurrentScale(self: bigint, value: number): void {
  const obj = lookupSvgsvgElement(self);
  obj.currentScale = value;
}

/**
 * `get-current-translate()` operation.
 */
export function getCurrentTranslate(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.currentTranslate;
}

/**
 * `get-intersection-list()` operation.
 */
export function getIntersectionList(self: bigint, rect: DomRect, referenceElement: bigint | undefined): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.intersectionList;
}

/**
 * `get-enclosure-list()` operation.
 */
export function getEnclosureList(self: bigint, rect: DomRect, referenceElement: bigint | undefined): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.enclosureList;
}

/**
 * `check-intersection()` operation.
 */
export function checkIntersection(self: bigint, element: bigint, rect: DomRect): boolean {
  const obj = lookupSvgsvgElement(self);
  return obj.checkIntersection(element, rect);
}

/**
 * `check-enclosure()` operation.
 */
export function checkEnclosure(self: bigint, element: bigint, rect: DomRect): boolean {
  const obj = lookupSvgsvgElement(self);
  return obj.checkEnclosure(element, rect);
}

/**
 * `deselect-all()` operation.
 */
export function deselectAll(self: bigint): void {
  const obj = lookupSvgsvgElement(self);
  obj.deselectAll();
}

/**
 * `create-svg-number()` operation.
 */
export function createSvgNumber(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgNumber();
}

/**
 * `create-svg-length()` operation.
 */
export function createSvgLength(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgLength();
}

/**
 * `create-svg-angle()` operation.
 */
export function createSvgAngle(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgAngle();
}

/**
 * `create-svg-point()` operation.
 */
export function createSvgPoint(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgPoint();
}

/**
 * `create-svg-matrix()` operation.
 */
export function createSvgMatrix(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgMatrix();
}

/**
 * `create-svg-rect()` operation.
 */
export function createSvgRect(self: bigint): DomRect {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgRect();
}

/**
 * `create-svg-transform()` operation.
 */
export function createSvgTransform(self: bigint): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgTransform();
}

/**
 * `create-svg-transform-from-matrix()` operation.
 */
export function SvgsvgElementCreateSvgTransformFromMatrix(self: bigint, matrix: bigint | undefined): bigint {
  const obj = lookupSvgsvgElement(self);
  return obj.createSvgTransformFromMatrix(matrix);
}

/**
 * `get-element-by-id()` operation.
 */
export function getElementById(self: bigint, elementId: string): bigint | undefined {
  const obj = lookupSvgsvgElement(self);
  return obj.getElementById(elementId) ?? undefined;
}

/**
 * `suspend-redraw()` operation.
 */
export function suspendRedraw(self: bigint, maxWaitMilliseconds: number): number {
  const obj = lookupSvgsvgElement(self);
  return obj.suspendRedraw(maxWaitMilliseconds);
}

/**
 * `unsuspend-redraw()` operation.
 */
export function unsuspendRedraw(self: bigint, suspendHandleId: number): void {
  const obj = lookupSvgsvgElement(self);
  obj.unsuspendRedraw(suspendHandleId);
}

/**
 * `unsuspend-redraw-all()` operation.
 */
export function unsuspendRedrawAll(self: bigint): void {
  const obj = lookupSvgsvgElement(self);
  obj.unsuspendRedrawAll();
}

/**
 * `force-redraw()` operation.
 */
export function forceRedraw(self: bigint): void {
  const obj = lookupSvgsvgElement(self);
  obj.forceRedraw();
}

// ---------------------------------------------------------------------------
// WIT interface: svg-use-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgUseElementHandle = bigint;

/** Handle table for SvgUseElement instances */
const _svgUseElementhandles = new Map<bigint, SvgUseElement>();
let _nextSvgUseElement = 1n;

/** Lookup a SvgUseElement by handle, throwing if not found. */
function lookupSvgUseElement(handle: bigint): SvgUseElement {
  const obj = _svgUseElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgUseElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgUseElement by handle. */
function lookupOptionSvgUseElement(handle: bigint | undefined): SvgUseElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgUseElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgUseElementGetX(self: bigint): bigint {
  const obj = lookupSvgUseElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgUseElementGetY(self: bigint): bigint {
  const obj = lookupSvgUseElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgUseElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgUseElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgUseElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgUseElement(self);
  return obj.height;
}

/**
 * `get-instance-root()` operation.
 */
export function getInstanceRoot(self: bigint): bigint | undefined {
  const obj = lookupSvgUseElement(self);
  return obj.instanceRoot ?? undefined;
}

/**
 * `get-animated-instance-root()` operation.
 */
export function getAnimatedInstanceRoot(self: bigint): bigint | undefined {
  const obj = lookupSvgUseElement(self);
  return obj.animatedInstanceRoot ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-element-instance
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgElementInstanceHandle = bigint;

/** Handle table for SvgElementInstance instances */
const _svgElementInstancehandles = new Map<bigint, SvgElementInstance>();
let _nextSvgElementInstance = 1n;

/** Lookup a SvgElementInstance by handle, throwing if not found. */
function lookupSvgElementInstance(handle: bigint): SvgElementInstance {
  const obj = _svgElementInstancehandles.get(handle);
  if (!obj) {
    throw new Error(`SvgElementInstance handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgElementInstance by handle. */
function lookupOptionSvgElementInstance(handle: bigint | undefined): SvgElementInstance | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgElementInstancehandles.get(handle) ?? null;
}
/**
 * `get-corresponding-element()` operation.
 */
export function getCorrespondingElement(self: bigint): bigint | undefined {
  const obj = lookupSvgElementInstance(self);
  return obj.correspondingElement ?? undefined;
}

/**
 * `get-corresponding-use-element()` operation.
 */
export function getCorrespondingUseElement(self: bigint): bigint | undefined {
  const obj = lookupSvgElementInstance(self);
  return obj.correspondingUseElement ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: shadow-animation
// ---------------------------------------------------------------------------

/** Type alias */
export type ShadowAnimationHandle = bigint;

/** Handle table for ShadowAnimation instances */
const _shadowAnimationhandles = new Map<bigint, ShadowAnimation>();
let _nextShadowAnimation = 1n;

/** Lookup a ShadowAnimation by handle, throwing if not found. */
function lookupShadowAnimation(handle: bigint): ShadowAnimation {
  const obj = _shadowAnimationhandles.get(handle);
  if (!obj) {
    throw new Error(`ShadowAnimation handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional ShadowAnimation by handle. */
function lookupOptionShadowAnimation(handle: bigint | undefined): ShadowAnimation | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _shadowAnimationhandles.get(handle) ?? null;
}
/**
 * `get-source-animation()` operation.
 */
export function getSourceAnimation(self: bigint): bigint {
  const obj = lookupShadowAnimation(self);
  return obj.sourceAnimation;
}

// ---------------------------------------------------------------------------
// WIT interface: get-svg-document
// ---------------------------------------------------------------------------

/** Type alias */
export type GetSvgDocumentHandle = bigint;

/** Handle table for GetSvgDocument instances */
const _getSvgDocumenthandles = new Map<bigint, GetSvgDocument>();
let _nextGetSvgDocument = 1n;

/** Lookup a GetSvgDocument by handle, throwing if not found. */
function lookupGetSvgDocument(handle: bigint): GetSvgDocument {
  const obj = _getSvgDocumenthandles.get(handle);
  if (!obj) {
    throw new Error(`GetSvgDocument handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional GetSvgDocument by handle. */
function lookupOptionGetSvgDocument(handle: bigint | undefined): GetSvgDocument | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _getSvgDocumenthandles.get(handle) ?? null;
}
/**
 * `get-svg-document()` operation.
 */
export function getSvgDocument(self: bigint): bigint {
  const obj = lookupGetSvgDocument(self);
  return obj.getSVGDocument();
}

// ---------------------------------------------------------------------------
// WIT interface: svg-style-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgStyleElementHandle = bigint;

/** Handle table for SvgStyleElement instances */
const _svgStyleElementhandles = new Map<bigint, SvgStyleElement>();
let _nextSvgStyleElement = 1n;

/** Lookup a SvgStyleElement by handle, throwing if not found. */
function lookupSvgStyleElement(handle: bigint): SvgStyleElement {
  const obj = _svgStyleElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgStyleElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgStyleElement by handle. */
function lookupOptionSvgStyleElement(handle: bigint | undefined): SvgStyleElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgStyleElementhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function SvgStyleElementGetType(self: bigint): string {
  const obj = lookupSvgStyleElement(self);
  return obj.getType();
}

/**
 * `set-type()` operation.
 */
export function SvgStyleElementSetType(self: bigint, value: string): void {
  const obj = lookupSvgStyleElement(self);
  obj.type = value;
}

/**
 * `get-media()` operation.
 */
export function getMedia(self: bigint): string {
  const obj = lookupSvgStyleElement(self);
  return obj.media;
}

/**
 * `set-media()` operation.
 */
export function setMedia(self: bigint, value: string): void {
  const obj = lookupSvgStyleElement(self);
  obj.media = value;
}

/**
 * `get-title()` operation.
 */
export function SvgStyleElementGetTitle(self: bigint): string {
  const obj = lookupSvgStyleElement(self);
  return obj.title;
}

/**
 * `set-title()` operation.
 */
export function SvgStyleElementSetTitle(self: bigint, value: string): void {
  const obj = lookupSvgStyleElement(self);
  obj.title = value;
}

/**
 * `get-disabled()` operation.
 */
export function getDisabled(self: bigint): boolean {
  const obj = lookupSvgStyleElement(self);
  return obj.disabled;
}

/**
 * `set-disabled()` operation.
 */
export function setDisabled(self: bigint, value: boolean): void {
  const obj = lookupSvgStyleElement(self);
  obj.disabled = value;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-transform
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTransformHandle = bigint;

/** Handle table for SvgTransform instances */
const _svgTransformhandles = new Map<bigint, SvgTransform>();
let _nextSvgTransform = 1n;

/** Lookup a SvgTransform by handle, throwing if not found. */
function lookupSvgTransform(handle: bigint): SvgTransform {
  const obj = _svgTransformhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTransform handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTransform by handle. */
function lookupOptionSvgTransform(handle: bigint | undefined): SvgTransform | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTransformhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function SvgTransformGetType(self: bigint): number {
  const obj = lookupSvgTransform(self);
  return obj.getType();
}

/**
 * `get-matrix()` operation.
 */
export function getMatrix(self: bigint): bigint {
  const obj = lookupSvgTransform(self);
  return obj.matrix;
}

/**
 * `get-angle()` operation.
 */
export function getAngle(self: bigint): number {
  const obj = lookupSvgTransform(self);
  return obj.angle;
}

/**
 * `set-matrix()` operation.
 */
export function setMatrix(self: bigint, matrix: bigint | undefined): void {
  const obj = lookupSvgTransform(self);
  obj.matrix = matrix;
}

/**
 * `set-translate()` operation.
 */
export function setTranslate(self: bigint, tx: number, ty: number): void {
  const obj = lookupSvgTransform(self);
  obj.translate = ty;
}

/**
 * `set-scale()` operation.
 */
export function setScale(self: bigint, sx: number, sy: number): void {
  const obj = lookupSvgTransform(self);
  obj.scale = sy;
}

/**
 * `set-rotate()` operation.
 */
export function setRotate(self: bigint, angle: number, cx: number, cy: number): void {
  const obj = lookupSvgTransform(self);
  obj.rotate = cy;
}

/**
 * `set-skew-x()` operation.
 */
export function setSkewX(self: bigint, angle: number): void {
  const obj = lookupSvgTransform(self);
  obj.skewX = angle;
}

/**
 * `set-skew-y()` operation.
 */
export function setSkewY(self: bigint, angle: number): void {
  const obj = lookupSvgTransform(self);
  obj.skewY = angle;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-transform-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTransformListHandle = bigint;

/** Handle table for SvgTransformList instances */
const _svgTransformListhandles = new Map<bigint, SvgTransformList>();
let _nextSvgTransformList = 1n;

/** Register a new SvgTransformList and return its handle. */
function registerSvgTransformList(obj: SvgTransformList): bigint {
  const handle = _nextSvgTransformList++;
  _svgTransformListhandles.set(handle, obj);
  return handle;
}
/** Lookup a SvgTransformList by handle, throwing if not found. */
function lookupSvgTransformList(handle: bigint): SvgTransformList {
  const obj = _svgTransformListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTransformList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTransformList by handle. */
function lookupOptionSvgTransformList(handle: bigint | undefined): SvgTransformList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTransformListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SvgTransformListGetLength(self: bigint): number {
  const obj = lookupSvgTransformList(self);
  return obj.length;
}

/**
 * `get-number-of-items()` operation.
 */
export function SvgTransformListGetNumberOfItems(self: bigint): number {
  const obj = lookupSvgTransformList(self);
  return obj.numberOfItems;
}

/**
 * `clear()` operation.
 */
export function SvgTransformListClear(self: bigint): void {
  const obj = lookupSvgTransformList(self);
  obj.clear();
}

/**
 * `initialize()` operation.
 */
export function SvgTransformListInitialize(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.initialize(newItem);
}

/**
 * `get-item()` operation.
 */
export function SvgTransformListGetItem(self: bigint, index: number): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.getItem(index);
}

/**
 * `insert-item-before()` operation.
 */
export function SvgTransformListInsertItemBefore(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.insertItemBefore(newItem, index);
}

/**
 * `replace-item()` operation.
 */
export function SvgTransformListReplaceItem(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.replaceItem(newItem, index);
}

/**
 * `remove-item()` operation.
 */
export function SvgTransformListRemoveItem(self: bigint, index: number): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.removeItem(index);
}

/**
 * `append-item()` operation.
 */
export function SvgTransformListAppendItem(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.appendItem(newItem);
}

/**
 * `undefined()` operation.
 */
export function SvgTransformListUndefined(self: bigint, index: number, newItem: bigint): void {
  const obj = lookupSvgTransformList(self);
  obj.undefined(index, newItem);
}

/**
 * `create-svg-transform-from-matrix()` operation.
 */
export function SvgTransformListCreateSvgTransformFromMatrix(self: bigint, matrix: bigint | undefined): bigint {
  const obj = lookupSvgTransformList(self);
  return obj.createSvgTransformFromMatrix(matrix);
}

/**
 * `consolidate()` operation.
 */
export function consolidate(self: bigint): bigint | undefined {
  const obj = lookupSvgTransformList(self);
  return obj.consolidate() ?? undefined;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-transform-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedTransformListHandle = bigint;

/** Handle table for SvgAnimatedTransformList instances */
const _svgAnimatedTransformListhandles = new Map<bigint, SvgAnimatedTransformList>();
let _nextSvgAnimatedTransformList = 1n;

/** Lookup a SvgAnimatedTransformList by handle, throwing if not found. */
function lookupSvgAnimatedTransformList(handle: bigint): SvgAnimatedTransformList {
  const obj = _svgAnimatedTransformListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedTransformList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedTransformList by handle. */
function lookupOptionSvgAnimatedTransformList(handle: bigint | undefined): SvgAnimatedTransformList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedTransformListhandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedTransformListGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedTransformList(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedTransformListGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedTransformList(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-preserve-aspect-ratio
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgPreserveAspectRatioHandle = bigint;

/** Handle table for SvgPreserveAspectRatio instances */
const _svgPreserveAspectRatiohandles = new Map<bigint, SvgPreserveAspectRatio>();
let _nextSvgPreserveAspectRatio = 1n;

/** Lookup a SvgPreserveAspectRatio by handle, throwing if not found. */
function lookupSvgPreserveAspectRatio(handle: bigint): SvgPreserveAspectRatio {
  const obj = _svgPreserveAspectRatiohandles.get(handle);
  if (!obj) {
    throw new Error(`SvgPreserveAspectRatio handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgPreserveAspectRatio by handle. */
function lookupOptionSvgPreserveAspectRatio(handle: bigint | undefined): SvgPreserveAspectRatio | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgPreserveAspectRatiohandles.get(handle) ?? null;
}
/**
 * `get-align()` operation.
 */
export function getAlign(self: bigint): number {
  const obj = lookupSvgPreserveAspectRatio(self);
  return obj.align;
}

/**
 * `set-align()` operation.
 */
export function setAlign(self: bigint, value: number): void {
  const obj = lookupSvgPreserveAspectRatio(self);
  obj.align = value;
}

/**
 * `get-meet-or-slice()` operation.
 */
export function getMeetOrSlice(self: bigint): number {
  const obj = lookupSvgPreserveAspectRatio(self);
  return obj.meetOrSlice;
}

/**
 * `set-meet-or-slice()` operation.
 */
export function setMeetOrSlice(self: bigint, value: number): void {
  const obj = lookupSvgPreserveAspectRatio(self);
  obj.meetOrSlice = value;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-preserve-aspect-ratio
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedPreserveAspectRatioHandle = bigint;

/** Handle table for SvgAnimatedPreserveAspectRatio instances */
const _svgAnimatedPreserveAspectRatiohandles = new Map<bigint, SvgAnimatedPreserveAspectRatio>();
let _nextSvgAnimatedPreserveAspectRatio = 1n;

/** Lookup a SvgAnimatedPreserveAspectRatio by handle, throwing if not found. */
function lookupSvgAnimatedPreserveAspectRatio(handle: bigint): SvgAnimatedPreserveAspectRatio {
  const obj = _svgAnimatedPreserveAspectRatiohandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedPreserveAspectRatio handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedPreserveAspectRatio by handle. */
function lookupOptionSvgAnimatedPreserveAspectRatio(handle: bigint | undefined): SvgAnimatedPreserveAspectRatio | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedPreserveAspectRatiohandles.get(handle) ?? null;
}
/**
 * `get-base-val()` operation.
 */
export function SvgAnimatedPreserveAspectRatioGetBaseVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedPreserveAspectRatio(self);
  return obj.baseVal;
}

/**
 * `get-anim-val()` operation.
 */
export function SvgAnimatedPreserveAspectRatioGetAnimVal(self: bigint): bigint {
  const obj = lookupSvgAnimatedPreserveAspectRatio(self);
  return obj.animVal;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-rect-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgRectElementHandle = bigint;

/** Handle table for SvgRectElement instances */
const _svgRectElementhandles = new Map<bigint, SvgRectElement>();
let _nextSvgRectElement = 1n;

/** Lookup a SvgRectElement by handle, throwing if not found. */
function lookupSvgRectElement(handle: bigint): SvgRectElement {
  const obj = _svgRectElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgRectElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgRectElement by handle. */
function lookupOptionSvgRectElement(handle: bigint | undefined): SvgRectElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgRectElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgRectElementGetX(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgRectElementGetY(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgRectElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgRectElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.height;
}

/**
 * `get-rx()` operation.
 */
export function SvgRectElementGetRx(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.rx;
}

/**
 * `get-ry()` operation.
 */
export function SvgRectElementGetRy(self: bigint): bigint {
  const obj = lookupSvgRectElement(self);
  return obj.ry;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-circle-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgCircleElementHandle = bigint;

/** Handle table for SvgCircleElement instances */
const _svgCircleElementhandles = new Map<bigint, SvgCircleElement>();
let _nextSvgCircleElement = 1n;

/** Lookup a SvgCircleElement by handle, throwing if not found. */
function lookupSvgCircleElement(handle: bigint): SvgCircleElement {
  const obj = _svgCircleElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgCircleElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgCircleElement by handle. */
function lookupOptionSvgCircleElement(handle: bigint | undefined): SvgCircleElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgCircleElementhandles.get(handle) ?? null;
}
/**
 * `get-cx()` operation.
 */
export function SvgCircleElementGetCx(self: bigint): bigint {
  const obj = lookupSvgCircleElement(self);
  return obj.cx;
}

/**
 * `get-cy()` operation.
 */
export function SvgCircleElementGetCy(self: bigint): bigint {
  const obj = lookupSvgCircleElement(self);
  return obj.cy;
}

/**
 * `get-r()` operation.
 */
export function SvgCircleElementGetR(self: bigint): bigint {
  const obj = lookupSvgCircleElement(self);
  return obj.r;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-ellipse-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgEllipseElementHandle = bigint;

/** Handle table for SvgEllipseElement instances */
const _svgEllipseElementhandles = new Map<bigint, SvgEllipseElement>();
let _nextSvgEllipseElement = 1n;

/** Lookup a SvgEllipseElement by handle, throwing if not found. */
function lookupSvgEllipseElement(handle: bigint): SvgEllipseElement {
  const obj = _svgEllipseElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgEllipseElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgEllipseElement by handle. */
function lookupOptionSvgEllipseElement(handle: bigint | undefined): SvgEllipseElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgEllipseElementhandles.get(handle) ?? null;
}
/**
 * `get-cx()` operation.
 */
export function SvgEllipseElementGetCx(self: bigint): bigint {
  const obj = lookupSvgEllipseElement(self);
  return obj.cx;
}

/**
 * `get-cy()` operation.
 */
export function SvgEllipseElementGetCy(self: bigint): bigint {
  const obj = lookupSvgEllipseElement(self);
  return obj.cy;
}

/**
 * `get-rx()` operation.
 */
export function SvgEllipseElementGetRx(self: bigint): bigint {
  const obj = lookupSvgEllipseElement(self);
  return obj.rx;
}

/**
 * `get-ry()` operation.
 */
export function SvgEllipseElementGetRy(self: bigint): bigint {
  const obj = lookupSvgEllipseElement(self);
  return obj.ry;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-line-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgLineElementHandle = bigint;

/** Handle table for SvgLineElement instances */
const _svgLineElementhandles = new Map<bigint, SvgLineElement>();
let _nextSvgLineElement = 1n;

/** Lookup a SvgLineElement by handle, throwing if not found. */
function lookupSvgLineElement(handle: bigint): SvgLineElement {
  const obj = _svgLineElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgLineElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgLineElement by handle. */
function lookupOptionSvgLineElement(handle: bigint | undefined): SvgLineElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgLineElementhandles.get(handle) ?? null;
}
/**
 * `get-x1()` operation.
 */
export function SvgLineElementGetX1(self: bigint): bigint {
  const obj = lookupSvgLineElement(self);
  return obj.x1;
}

/**
 * `get-y1()` operation.
 */
export function SvgLineElementGetY1(self: bigint): bigint {
  const obj = lookupSvgLineElement(self);
  return obj.y1;
}

/**
 * `get-x2()` operation.
 */
export function SvgLineElementGetX2(self: bigint): bigint {
  const obj = lookupSvgLineElement(self);
  return obj.x2;
}

/**
 * `get-y2()` operation.
 */
export function SvgLineElementGetY2(self: bigint): bigint {
  const obj = lookupSvgLineElement(self);
  return obj.y2;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-animated-points
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgAnimatedPointsHandle = bigint;

/** Handle table for SvgAnimatedPoints instances */
const _svgAnimatedPointshandles = new Map<bigint, SvgAnimatedPoints>();
let _nextSvgAnimatedPoints = 1n;

/** Lookup a SvgAnimatedPoints by handle, throwing if not found. */
function lookupSvgAnimatedPoints(handle: bigint): SvgAnimatedPoints {
  const obj = _svgAnimatedPointshandles.get(handle);
  if (!obj) {
    throw new Error(`SvgAnimatedPoints handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgAnimatedPoints by handle. */
function lookupOptionSvgAnimatedPoints(handle: bigint | undefined): SvgAnimatedPoints | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgAnimatedPointshandles.get(handle) ?? null;
}
/**
 * `get-points()` operation.
 */
export function getPoints(self: bigint): bigint {
  const obj = lookupSvgAnimatedPoints(self);
  return obj.points;
}

/**
 * `get-animated-points()` operation.
 */
export function getAnimatedPoints(self: bigint): bigint {
  const obj = lookupSvgAnimatedPoints(self);
  return obj.animatedPoints;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-point-list
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgPointListHandle = bigint;

/** Handle table for SvgPointList instances */
const _svgPointListhandles = new Map<bigint, SvgPointList>();
let _nextSvgPointList = 1n;

/** Lookup a SvgPointList by handle, throwing if not found. */
function lookupSvgPointList(handle: bigint): SvgPointList {
  const obj = _svgPointListhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgPointList handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgPointList by handle. */
function lookupOptionSvgPointList(handle: bigint | undefined): SvgPointList | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgPointListhandles.get(handle) ?? null;
}
/**
 * `get-length()` operation.
 */
export function SvgPointListGetLength(self: bigint): number {
  const obj = lookupSvgPointList(self);
  return obj.length;
}

/**
 * `get-number-of-items()` operation.
 */
export function SvgPointListGetNumberOfItems(self: bigint): number {
  const obj = lookupSvgPointList(self);
  return obj.numberOfItems;
}

/**
 * `clear()` operation.
 */
export function SvgPointListClear(self: bigint): void {
  const obj = lookupSvgPointList(self);
  obj.clear();
}

/**
 * `initialize()` operation.
 */
export function SvgPointListInitialize(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgPointList(self);
  return obj.initialize(newItem);
}

/**
 * `get-item()` operation.
 */
export function SvgPointListGetItem(self: bigint, index: number): bigint {
  const obj = lookupSvgPointList(self);
  return obj.getItem(index);
}

/**
 * `insert-item-before()` operation.
 */
export function SvgPointListInsertItemBefore(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgPointList(self);
  return obj.insertItemBefore(newItem, index);
}

/**
 * `replace-item()` operation.
 */
export function SvgPointListReplaceItem(self: bigint, newItem: bigint, index: number): bigint {
  const obj = lookupSvgPointList(self);
  return obj.replaceItem(newItem, index);
}

/**
 * `remove-item()` operation.
 */
export function SvgPointListRemoveItem(self: bigint, index: number): bigint {
  const obj = lookupSvgPointList(self);
  return obj.removeItem(index);
}

/**
 * `append-item()` operation.
 */
export function SvgPointListAppendItem(self: bigint, newItem: bigint): bigint {
  const obj = lookupSvgPointList(self);
  return obj.appendItem(newItem);
}

/**
 * `undefined()` operation.
 */
export function SvgPointListUndefined(self: bigint, index: number, newItem: bigint): void {
  const obj = lookupSvgPointList(self);
  obj.undefined(index, newItem);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-text-content-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTextContentElementHandle = bigint;

/** Handle table for SvgTextContentElement instances */
const _svgTextContentElementhandles = new Map<bigint, SvgTextContentElement>();
let _nextSvgTextContentElement = 1n;

/** Lookup a SvgTextContentElement by handle, throwing if not found. */
function lookupSvgTextContentElement(handle: bigint): SvgTextContentElement {
  const obj = _svgTextContentElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTextContentElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTextContentElement by handle. */
function lookupOptionSvgTextContentElement(handle: bigint | undefined): SvgTextContentElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTextContentElementhandles.get(handle) ?? null;
}
/**
 * `get-text-length()` operation.
 */
export function getTextLength(self: bigint): bigint {
  const obj = lookupSvgTextContentElement(self);
  return obj.textLength;
}

/**
 * `get-length-adjust()` operation.
 */
export function getLengthAdjust(self: bigint): bigint {
  const obj = lookupSvgTextContentElement(self);
  return obj.lengthAdjust;
}

/**
 * `get-number-of-chars()` operation.
 */
export function getNumberOfChars(self: bigint): number {
  const obj = lookupSvgTextContentElement(self);
  return obj.numberOfChars;
}

/**
 * `get-computed-text-length()` operation.
 */
export function getComputedTextLength(self: bigint): number {
  const obj = lookupSvgTextContentElement(self);
  return obj.computedTextLength;
}

/**
 * `get-sub-string-length()` operation.
 */
export function getSubStringLength(self: bigint, charnum: number, nchars: number): number {
  const obj = lookupSvgTextContentElement(self);
  return obj.subStringLength;
}

/**
 * `get-start-position-of-char()` operation.
 */
export function getStartPositionOfChar(self: bigint, charnum: number): bigint {
  const obj = lookupSvgTextContentElement(self);
  return obj.startPositionOfChar;
}

/**
 * `get-end-position-of-char()` operation.
 */
export function getEndPositionOfChar(self: bigint, charnum: number): bigint {
  const obj = lookupSvgTextContentElement(self);
  return obj.endPositionOfChar;
}

/**
 * `get-extent-of-char()` operation.
 */
export function getExtentOfChar(self: bigint, charnum: number): DomRect {
  const obj = lookupSvgTextContentElement(self);
  return obj.extentOfChar;
}

/**
 * `get-rotation-of-char()` operation.
 */
export function getRotationOfChar(self: bigint, charnum: number): number {
  const obj = lookupSvgTextContentElement(self);
  return obj.rotationOfChar;
}

/**
 * `get-char-num-at-position()` operation.
 */
export function getCharNumAtPosition(self: bigint, point: bigint | undefined): number {
  const obj = lookupSvgTextContentElement(self);
  return obj.charNumAtPosition;
}

/**
 * `select-sub-string()` operation.
 */
export function selectSubString(self: bigint, charnum: number, nchars: number): void {
  const obj = lookupSvgTextContentElement(self);
  obj.selectSubString(charnum, nchars);
}

// ---------------------------------------------------------------------------
// WIT interface: svg-text-positioning-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTextPositioningElementHandle = bigint;

/** Handle table for SvgTextPositioningElement instances */
const _svgTextPositioningElementhandles = new Map<bigint, SvgTextPositioningElement>();
let _nextSvgTextPositioningElement = 1n;

/** Lookup a SvgTextPositioningElement by handle, throwing if not found. */
function lookupSvgTextPositioningElement(handle: bigint): SvgTextPositioningElement {
  const obj = _svgTextPositioningElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTextPositioningElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTextPositioningElement by handle. */
function lookupOptionSvgTextPositioningElement(handle: bigint | undefined): SvgTextPositioningElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTextPositioningElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgTextPositioningElementGetX(self: bigint): bigint {
  const obj = lookupSvgTextPositioningElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgTextPositioningElementGetY(self: bigint): bigint {
  const obj = lookupSvgTextPositioningElement(self);
  return obj.y;
}

/**
 * `get-dx()` operation.
 */
export function getDx(self: bigint): bigint {
  const obj = lookupSvgTextPositioningElement(self);
  return obj.dx;
}

/**
 * `get-dy()` operation.
 */
export function getDy(self: bigint): bigint {
  const obj = lookupSvgTextPositioningElement(self);
  return obj.dy;
}

/**
 * `get-rotate()` operation.
 */
export function getRotate(self: bigint): bigint {
  const obj = lookupSvgTextPositioningElement(self);
  return obj.rotate;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-text-path-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgTextPathElementHandle = bigint;

/** Handle table for SvgTextPathElement instances */
const _svgTextPathElementhandles = new Map<bigint, SvgTextPathElement>();
let _nextSvgTextPathElement = 1n;

/** Lookup a SvgTextPathElement by handle, throwing if not found. */
function lookupSvgTextPathElement(handle: bigint): SvgTextPathElement {
  const obj = _svgTextPathElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgTextPathElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgTextPathElement by handle. */
function lookupOptionSvgTextPathElement(handle: bigint | undefined): SvgTextPathElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgTextPathElementhandles.get(handle) ?? null;
}
/**
 * `get-start-offset()` operation.
 */
export function getStartOffset(self: bigint): bigint {
  const obj = lookupSvgTextPathElement(self);
  return obj.startOffset;
}

/**
 * `get-method()` operation.
 */
export function getMethod(self: bigint): bigint {
  const obj = lookupSvgTextPathElement(self);
  return obj.method;
}

/**
 * `get-spacing()` operation.
 */
export function getSpacing(self: bigint): bigint {
  const obj = lookupSvgTextPathElement(self);
  return obj.spacing;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-image-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgImageElementHandle = bigint;

/** Handle table for SvgImageElement instances */
const _svgImageElementhandles = new Map<bigint, SvgImageElement>();
let _nextSvgImageElement = 1n;

/** Lookup a SvgImageElement by handle, throwing if not found. */
function lookupSvgImageElement(handle: bigint): SvgImageElement {
  const obj = _svgImageElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgImageElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgImageElement by handle. */
function lookupOptionSvgImageElement(handle: bigint | undefined): SvgImageElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgImageElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgImageElementGetX(self: bigint): bigint {
  const obj = lookupSvgImageElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgImageElementGetY(self: bigint): bigint {
  const obj = lookupSvgImageElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgImageElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgImageElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgImageElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgImageElement(self);
  return obj.height;
}

/**
 * `get-preserve-aspect-ratio()` operation.
 */
export function SvgImageElementGetPreserveAspectRatio(self: bigint): bigint {
  const obj = lookupSvgImageElement(self);
  return obj.preserveAspectRatio;
}

/**
 * `get-cross-origin()` operation.
 */
export function SvgImageElementGetCrossOrigin(self: bigint): string | undefined {
  const obj = lookupSvgImageElement(self);
  return obj.crossOrigin ?? undefined;
}

/**
 * `set-cross-origin()` operation.
 */
export function SvgImageElementSetCrossOrigin(self: bigint, value: string | undefined): void {
  const obj = lookupSvgImageElement(self);
  obj.crossOrigin = value;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-foreign-object-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgForeignObjectElementHandle = bigint;

/** Handle table for SvgForeignObjectElement instances */
const _svgForeignObjectElementhandles = new Map<bigint, SvgForeignObjectElement>();
let _nextSvgForeignObjectElement = 1n;

/** Lookup a SvgForeignObjectElement by handle, throwing if not found. */
function lookupSvgForeignObjectElement(handle: bigint): SvgForeignObjectElement {
  const obj = _svgForeignObjectElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgForeignObjectElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgForeignObjectElement by handle. */
function lookupOptionSvgForeignObjectElement(handle: bigint | undefined): SvgForeignObjectElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgForeignObjectElementhandles.get(handle) ?? null;
}
/**
 * `get-x()` operation.
 */
export function SvgForeignObjectElementGetX(self: bigint): bigint {
  const obj = lookupSvgForeignObjectElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgForeignObjectElementGetY(self: bigint): bigint {
  const obj = lookupSvgForeignObjectElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgForeignObjectElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgForeignObjectElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgForeignObjectElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgForeignObjectElement(self);
  return obj.height;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-marker-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgMarkerElementHandle = bigint;

/** Handle table for SvgMarkerElement instances */
const _svgMarkerElementhandles = new Map<bigint, SvgMarkerElement>();
let _nextSvgMarkerElement = 1n;

/** Lookup a SvgMarkerElement by handle, throwing if not found. */
function lookupSvgMarkerElement(handle: bigint): SvgMarkerElement {
  const obj = _svgMarkerElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgMarkerElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgMarkerElement by handle. */
function lookupOptionSvgMarkerElement(handle: bigint | undefined): SvgMarkerElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgMarkerElementhandles.get(handle) ?? null;
}
/**
 * `get-ref-x()` operation.
 */
export function getRefX(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.refX;
}

/**
 * `get-ref-y()` operation.
 */
export function getRefY(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.refY;
}

/**
 * `get-marker-units()` operation.
 */
export function getMarkerUnits(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.markerUnits;
}

/**
 * `get-marker-width()` operation.
 */
export function getMarkerWidth(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.markerWidth;
}

/**
 * `get-marker-height()` operation.
 */
export function getMarkerHeight(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.markerHeight;
}

/**
 * `get-orient-type()` operation.
 */
export function getOrientType(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.orientType;
}

/**
 * `get-orient-angle()` operation.
 */
export function getOrientAngle(self: bigint): bigint {
  const obj = lookupSvgMarkerElement(self);
  return obj.orientAngle;
}

/**
 * `get-orient()` operation.
 */
export function getOrient(self: bigint): string {
  const obj = lookupSvgMarkerElement(self);
  return obj.orient;
}

/**
 * `set-orient()` operation.
 */
export function setOrient(self: bigint, value: string): void {
  const obj = lookupSvgMarkerElement(self);
  obj.orient = value;
}

/**
 * `set-orient-to-auto()` operation.
 */
export function setOrientToAuto(self: bigint): void {
  const obj = lookupSvgMarkerElement(self);
  obj.orientToAuto = self;
}

/**
 * `set-orient-to-angle()` operation.
 */
export function setOrientToAngle(self: bigint, angle: bigint): void {
  const obj = lookupSvgMarkerElement(self);
  obj.orientToAngle = angle;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-gradient-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgGradientElementHandle = bigint;

/** Handle table for SvgGradientElement instances */
const _svgGradientElementhandles = new Map<bigint, SvgGradientElement>();
let _nextSvgGradientElement = 1n;

/** Lookup a SvgGradientElement by handle, throwing if not found. */
function lookupSvgGradientElement(handle: bigint): SvgGradientElement {
  const obj = _svgGradientElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgGradientElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgGradientElement by handle. */
function lookupOptionSvgGradientElement(handle: bigint | undefined): SvgGradientElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgGradientElementhandles.get(handle) ?? null;
}
/**
 * `get-gradient-units()` operation.
 */
export function getGradientUnits(self: bigint): bigint {
  const obj = lookupSvgGradientElement(self);
  return obj.gradientUnits;
}

/**
 * `get-gradient-transform()` operation.
 */
export function getGradientTransform(self: bigint): bigint {
  const obj = lookupSvgGradientElement(self);
  return obj.gradientTransform;
}

/**
 * `get-spread-method()` operation.
 */
export function getSpreadMethod(self: bigint): bigint {
  const obj = lookupSvgGradientElement(self);
  return obj.spreadMethod;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-linear-gradient-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgLinearGradientElementHandle = bigint;

/** Handle table for SvgLinearGradientElement instances */
const _svgLinearGradientElementhandles = new Map<bigint, SvgLinearGradientElement>();
let _nextSvgLinearGradientElement = 1n;

/** Lookup a SvgLinearGradientElement by handle, throwing if not found. */
function lookupSvgLinearGradientElement(handle: bigint): SvgLinearGradientElement {
  const obj = _svgLinearGradientElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgLinearGradientElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgLinearGradientElement by handle. */
function lookupOptionSvgLinearGradientElement(handle: bigint | undefined): SvgLinearGradientElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgLinearGradientElementhandles.get(handle) ?? null;
}
/**
 * `get-x1()` operation.
 */
export function SvgLinearGradientElementGetX1(self: bigint): bigint {
  const obj = lookupSvgLinearGradientElement(self);
  return obj.x1;
}

/**
 * `get-y1()` operation.
 */
export function SvgLinearGradientElementGetY1(self: bigint): bigint {
  const obj = lookupSvgLinearGradientElement(self);
  return obj.y1;
}

/**
 * `get-x2()` operation.
 */
export function SvgLinearGradientElementGetX2(self: bigint): bigint {
  const obj = lookupSvgLinearGradientElement(self);
  return obj.x2;
}

/**
 * `get-y2()` operation.
 */
export function SvgLinearGradientElementGetY2(self: bigint): bigint {
  const obj = lookupSvgLinearGradientElement(self);
  return obj.y2;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-radial-gradient-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgRadialGradientElementHandle = bigint;

/** Handle table for SvgRadialGradientElement instances */
const _svgRadialGradientElementhandles = new Map<bigint, SvgRadialGradientElement>();
let _nextSvgRadialGradientElement = 1n;

/** Lookup a SvgRadialGradientElement by handle, throwing if not found. */
function lookupSvgRadialGradientElement(handle: bigint): SvgRadialGradientElement {
  const obj = _svgRadialGradientElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgRadialGradientElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgRadialGradientElement by handle. */
function lookupOptionSvgRadialGradientElement(handle: bigint | undefined): SvgRadialGradientElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgRadialGradientElementhandles.get(handle) ?? null;
}
/**
 * `get-cx()` operation.
 */
export function SvgRadialGradientElementGetCx(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.cx;
}

/**
 * `get-cy()` operation.
 */
export function SvgRadialGradientElementGetCy(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.cy;
}

/**
 * `get-r()` operation.
 */
export function SvgRadialGradientElementGetR(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.r;
}

/**
 * `get-fx()` operation.
 */
export function getFx(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.fx;
}

/**
 * `get-fy()` operation.
 */
export function getFy(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.fy;
}

/**
 * `get-fr()` operation.
 */
export function getFr(self: bigint): bigint {
  const obj = lookupSvgRadialGradientElement(self);
  return obj.fr;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-stop-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgStopElementHandle = bigint;

/** Handle table for SvgStopElement instances */
const _svgStopElementhandles = new Map<bigint, SvgStopElement>();
let _nextSvgStopElement = 1n;

/** Lookup a SvgStopElement by handle, throwing if not found. */
function lookupSvgStopElement(handle: bigint): SvgStopElement {
  const obj = _svgStopElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgStopElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgStopElement by handle. */
function lookupOptionSvgStopElement(handle: bigint | undefined): SvgStopElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgStopElementhandles.get(handle) ?? null;
}
/**
 * `get-offset()` operation.
 */
export function getOffset(self: bigint): bigint {
  const obj = lookupSvgStopElement(self);
  return obj.offset;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-pattern-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgPatternElementHandle = bigint;

/** Handle table for SvgPatternElement instances */
const _svgPatternElementhandles = new Map<bigint, SvgPatternElement>();
let _nextSvgPatternElement = 1n;

/** Lookup a SvgPatternElement by handle, throwing if not found. */
function lookupSvgPatternElement(handle: bigint): SvgPatternElement {
  const obj = _svgPatternElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgPatternElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgPatternElement by handle. */
function lookupOptionSvgPatternElement(handle: bigint | undefined): SvgPatternElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgPatternElementhandles.get(handle) ?? null;
}
/**
 * `get-pattern-units()` operation.
 */
export function getPatternUnits(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.patternUnits;
}

/**
 * `get-pattern-content-units()` operation.
 */
export function getPatternContentUnits(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.patternContentUnits;
}

/**
 * `get-pattern-transform()` operation.
 */
export function getPatternTransform(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.patternTransform;
}

/**
 * `get-x()` operation.
 */
export function SvgPatternElementGetX(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.x;
}

/**
 * `get-y()` operation.
 */
export function SvgPatternElementGetY(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.y;
}

/**
 * `get-width()` operation.
 */
export function SvgPatternElementGetWidth(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.width;
}

/**
 * `get-height()` operation.
 */
export function SvgPatternElementGetHeight(self: bigint): bigint {
  const obj = lookupSvgPatternElement(self);
  return obj.height;
}

// ---------------------------------------------------------------------------
// WIT interface: svg-script-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgScriptElementHandle = bigint;

/** Handle table for SvgScriptElement instances */
const _svgScriptElementhandles = new Map<bigint, SvgScriptElement>();
let _nextSvgScriptElement = 1n;

/** Lookup a SvgScriptElement by handle, throwing if not found. */
function lookupSvgScriptElement(handle: bigint): SvgScriptElement {
  const obj = _svgScriptElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgScriptElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgScriptElement by handle. */
function lookupOptionSvgScriptElement(handle: bigint | undefined): SvgScriptElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgScriptElementhandles.get(handle) ?? null;
}
/**
 * `get-type()` operation.
 */
export function SvgScriptElementGetType(self: bigint): string {
  const obj = lookupSvgScriptElement(self);
  return obj.getType();
}

/**
 * `set-type()` operation.
 */
export function SvgScriptElementSetType(self: bigint, value: string): void {
  const obj = lookupSvgScriptElement(self);
  obj.type = value;
}

/**
 * `get-cross-origin()` operation.
 */
export function SvgScriptElementGetCrossOrigin(self: bigint): string | undefined {
  const obj = lookupSvgScriptElement(self);
  return obj.crossOrigin ?? undefined;
}

/**
 * `set-cross-origin()` operation.
 */
export function SvgScriptElementSetCrossOrigin(self: bigint, value: string | undefined): void {
  const obj = lookupSvgScriptElement(self);
  obj.crossOrigin = value;
}

// ---------------------------------------------------------------------------
// WIT interface: svga-element
// ---------------------------------------------------------------------------

/** Type alias */
export type SvgaElementHandle = bigint;

/** Handle table for SvgaElement instances */
const _svgaElementhandles = new Map<bigint, SvgaElement>();
let _nextSvgaElement = 1n;

/** Lookup a SvgaElement by handle, throwing if not found. */
function lookupSvgaElement(handle: bigint): SvgaElement {
  const obj = _svgaElementhandles.get(handle);
  if (!obj) {
    throw new Error(`SvgaElement handle ${handle} not found`);
  }
  return obj!;
}

/** Lookup an optional SvgaElement by handle. */
function lookupOptionSvgaElement(handle: bigint | undefined): SvgaElement | null {
  if (handle === undefined || handle === 0n) {
    return null;
  }
  return _svgaElementhandles.get(handle) ?? null;
}
/**
 * `get-target()` operation.
 */
export function getTarget(self: bigint): bigint {
  const obj = lookupSvgaElement(self);
  return obj.target;
}

/**
 * `get-download()` operation.
 */
export function getDownload(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.download;
}

/**
 * `set-download()` operation.
 */
export function setDownload(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.download = value;
}

/**
 * `get-ping()` operation.
 */
export function getPing(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.ping;
}

/**
 * `set-ping()` operation.
 */
export function setPing(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.ping = value;
}

/**
 * `get-rel()` operation.
 */
export function getRel(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.rel;
}

/**
 * `set-rel()` operation.
 */
export function setRel(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.rel = value;
}

/**
 * `get-rel-list()` operation.
 */
export function getRelList(self: bigint): bigint {
  const obj = lookupSvgaElement(self);
  return obj.relList;
}

/**
 * `get-hreflang()` operation.
 */
export function getHreflang(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.hreflang;
}

/**
 * `set-hreflang()` operation.
 */
export function setHreflang(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.hreflang = value;
}

/**
 * `get-type()` operation.
 */
export function SvgaElementGetType(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.getType();
}

/**
 * `set-type()` operation.
 */
export function SvgaElementSetType(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.type = value;
}

/**
 * `get-referrer-policy()` operation.
 */
export function getReferrerPolicy(self: bigint): string {
  const obj = lookupSvgaElement(self);
  return obj.referrerPolicy;
}

/**
 * `set-referrer-policy()` operation.
 */
export function setReferrerPolicy(self: bigint, value: string): void {
  const obj = lookupSvgaElement(self);
  obj.referrerPolicy = value;
}

// ---------------------------------------------------------------------------
// Exports
// ---------------------------------------------------------------------------

export default {
  getClassName,
  getOwnerSvgElement,
  getViewportElement,
  getTransform,
  getBBox,
  getCtm,
  getScreenCtm,
  getPathLength,
  isPointInFill,
  isPointInStroke,
  getTotalLength,
  getPointAtLength,
  SvgNumberGetValue,
  SvgNumberSetValue,
  SvgLengthGetUnitType,
  SvgLengthGetValue,
  SvgLengthSetValue,
  SvgLengthGetValueInSpecifiedUnits,
  SvgLengthSetValueInSpecifiedUnits,
  SvgLengthGetValueAsString,
  SvgLengthSetValueAsString,
  SvgLengthNewValueSpecifiedUnits,
  SvgLengthConvertToSpecifiedUnits,
  SvgAngleGetUnitType,
  SvgAngleGetValue,
  SvgAngleSetValue,
  SvgAngleGetValueInSpecifiedUnits,
  SvgAngleSetValueInSpecifiedUnits,
  SvgAngleGetValueAsString,
  SvgAngleSetValueAsString,
  SvgAngleNewValueSpecifiedUnits,
  SvgAngleConvertToSpecifiedUnits,
  SvgNumberListGetLength,
  SvgNumberListGetNumberOfItems,
  SvgNumberListClear,
  SvgNumberListInitialize,
  SvgNumberListGetItem,
  SvgNumberListInsertItemBefore,
  SvgNumberListReplaceItem,
  SvgNumberListRemoveItem,
  SvgNumberListAppendItem,
  SvgNumberListUndefined,
  SvgLengthListGetLength,
  SvgLengthListGetNumberOfItems,
  SvgLengthListClear,
  SvgLengthListInitialize,
  SvgLengthListGetItem,
  SvgLengthListInsertItemBefore,
  SvgLengthListReplaceItem,
  SvgLengthListRemoveItem,
  SvgLengthListAppendItem,
  SvgLengthListUndefined,
  SvgStringListGetLength,
  SvgStringListGetNumberOfItems,
  SvgStringListClear,
  SvgStringListInitialize,
  SvgStringListGetItem,
  SvgStringListInsertItemBefore,
  SvgStringListReplaceItem,
  SvgStringListRemoveItem,
  SvgStringListAppendItem,
  SvgStringListUndefined,
  SvgAnimatedBooleanGetBaseVal,
  SvgAnimatedBooleanSetBaseVal,
  SvgAnimatedBooleanGetAnimVal,
  SvgAnimatedEnumerationGetBaseVal,
  SvgAnimatedEnumerationSetBaseVal,
  SvgAnimatedEnumerationGetAnimVal,
  SvgAnimatedIntegerGetBaseVal,
  SvgAnimatedIntegerSetBaseVal,
  SvgAnimatedIntegerGetAnimVal,
  SvgAnimatedNumberGetBaseVal,
  SvgAnimatedNumberSetBaseVal,
  SvgAnimatedNumberGetAnimVal,
  SvgAnimatedLengthGetBaseVal,
  SvgAnimatedLengthGetAnimVal,
  SvgAnimatedAngleGetBaseVal,
  SvgAnimatedAngleGetAnimVal,
  SvgAnimatedStringGetBaseVal,
  SvgAnimatedStringSetBaseVal,
  SvgAnimatedStringGetAnimVal,
  SvgAnimatedRectGetBaseVal,
  SvgAnimatedRectGetAnimVal,
  SvgAnimatedNumberListGetBaseVal,
  SvgAnimatedNumberListGetAnimVal,
  SvgAnimatedLengthListGetBaseVal,
  SvgAnimatedLengthListGetAnimVal,
  getRequiredExtensions,
  getSystemLanguage,
  getViewBox,
  SvgFitToViewBoxGetPreserveAspectRatio,
  getHref,
  getRootElement,
  elementFromPoint,
  elementsFromPoint,
  caretPositionFromPoint,
  getScrollingElement,
  getImplementation,
  getUrl,
  getDocumentUri,
  getCompatMode,
  getCharacterSet,
  getCharset,
  getInputEncoding,
  getContentType,
  getDoctype,
  getDocumentElement,
  getElementsByTagName,
  getElementsByTagNameNs,
  getElementsByClassName,
  createElement,
  createElementNs,
  createDocumentFragment,
  createTextNode,
  createCdataSection,
  createComment,
  createProcessingInstruction,
  importNode,
  adoptNode,
  createAttribute,
  createAttributeNs,
  createEvent,
  createRange,
  createNodeIterator,
  createTreeWalker,
  getFullscreenEnabled,
  getFullscreen,
  exitFullscreen,
  pollExitFullscreen,
  getOnfullscreenchange,
  setOnfullscreenchange,
  getOnfullscreenerror,
  setOnfullscreenerror,
  parseHtmlUnsafe,
  getLocation,
  getDomain,
  setDomain,
  getReferrer,
  getCookie,
  setCookie,
  getLastModified,
  getReadyState,
  object,
  DocumentGetTitle,
  DocumentSetTitle,
  getDir,
  setDir,
  getBody,
  setBody,
  getHead,
  getImages,
  getEmbeds,
  getPlugins,
  getLinks,
  getForms,
  getScripts,
  getElementsByName,
  getCurrentScript,
  open,
  close,
  write,
  writeln,
  getDefaultView,
  hasFocus,
  getDesignMode,
  setDesignMode,
  execCommand,
  queryCommandEnabled,
  queryCommandIndeterm,
  queryCommandState,
  queryCommandSupported,
  queryCommandValue,
  getHidden,
  getVisibilityState,
  getOnreadystatechange,
  setOnreadystatechange,
  getOnvisibilitychange,
  setOnvisibilitychange,
  getFgColor,
  setFgColor,
  getLinkColor,
  setLinkColor,
  getVlinkColor,
  setVlinkColor,
  getAlinkColor,
  setAlinkColor,
  getBgColor,
  setBgColor,
  getAnchors,
  getApplets,
  DocumentClear,
  captureEvents,
  releaseEvents,
  getAll,
  getSelection,
  SvgsvgElementGetX,
  SvgsvgElementGetY,
  SvgsvgElementGetWidth,
  SvgsvgElementGetHeight,
  getCurrentScale,
  setCurrentScale,
  getCurrentTranslate,
  getIntersectionList,
  getEnclosureList,
  checkIntersection,
  checkEnclosure,
  deselectAll,
  createSvgNumber,
  createSvgLength,
  createSvgAngle,
  createSvgPoint,
  createSvgMatrix,
  createSvgRect,
  createSvgTransform,
  SvgsvgElementCreateSvgTransformFromMatrix,
  getElementById,
  suspendRedraw,
  unsuspendRedraw,
  unsuspendRedrawAll,
  forceRedraw,
  SvgUseElementGetX,
  SvgUseElementGetY,
  SvgUseElementGetWidth,
  SvgUseElementGetHeight,
  getInstanceRoot,
  getAnimatedInstanceRoot,
  getCorrespondingElement,
  getCorrespondingUseElement,
  getSourceAnimation,
  getSvgDocument,
  SvgStyleElementGetType,
  SvgStyleElementSetType,
  getMedia,
  setMedia,
  SvgStyleElementGetTitle,
  SvgStyleElementSetTitle,
  getDisabled,
  setDisabled,
  SvgTransformGetType,
  getMatrix,
  getAngle,
  setMatrix,
  setTranslate,
  setScale,
  setRotate,
  setSkewX,
  setSkewY,
  SvgTransformListGetLength,
  SvgTransformListGetNumberOfItems,
  SvgTransformListClear,
  SvgTransformListInitialize,
  SvgTransformListGetItem,
  SvgTransformListInsertItemBefore,
  SvgTransformListReplaceItem,
  SvgTransformListRemoveItem,
  SvgTransformListAppendItem,
  SvgTransformListUndefined,
  SvgTransformListCreateSvgTransformFromMatrix,
  consolidate,
  SvgAnimatedTransformListGetBaseVal,
  SvgAnimatedTransformListGetAnimVal,
  getAlign,
  setAlign,
  getMeetOrSlice,
  setMeetOrSlice,
  SvgAnimatedPreserveAspectRatioGetBaseVal,
  SvgAnimatedPreserveAspectRatioGetAnimVal,
  SvgRectElementGetX,
  SvgRectElementGetY,
  SvgRectElementGetWidth,
  SvgRectElementGetHeight,
  SvgRectElementGetRx,
  SvgRectElementGetRy,
  SvgCircleElementGetCx,
  SvgCircleElementGetCy,
  SvgCircleElementGetR,
  SvgEllipseElementGetCx,
  SvgEllipseElementGetCy,
  SvgEllipseElementGetRx,
  SvgEllipseElementGetRy,
  SvgLineElementGetX1,
  SvgLineElementGetY1,
  SvgLineElementGetX2,
  SvgLineElementGetY2,
  getPoints,
  getAnimatedPoints,
  SvgPointListGetLength,
  SvgPointListGetNumberOfItems,
  SvgPointListClear,
  SvgPointListInitialize,
  SvgPointListGetItem,
  SvgPointListInsertItemBefore,
  SvgPointListReplaceItem,
  SvgPointListRemoveItem,
  SvgPointListAppendItem,
  SvgPointListUndefined,
  getTextLength,
  getLengthAdjust,
  getNumberOfChars,
  getComputedTextLength,
  getSubStringLength,
  getStartPositionOfChar,
  getEndPositionOfChar,
  getExtentOfChar,
  getRotationOfChar,
  getCharNumAtPosition,
  selectSubString,
  SvgTextPositioningElementGetX,
  SvgTextPositioningElementGetY,
  getDx,
  getDy,
  getRotate,
  getStartOffset,
  getMethod,
  getSpacing,
  SvgImageElementGetX,
  SvgImageElementGetY,
  SvgImageElementGetWidth,
  SvgImageElementGetHeight,
  SvgImageElementGetPreserveAspectRatio,
  SvgImageElementGetCrossOrigin,
  SvgImageElementSetCrossOrigin,
  SvgForeignObjectElementGetX,
  SvgForeignObjectElementGetY,
  SvgForeignObjectElementGetWidth,
  SvgForeignObjectElementGetHeight,
  getRefX,
  getRefY,
  getMarkerUnits,
  getMarkerWidth,
  getMarkerHeight,
  getOrientType,
  getOrientAngle,
  getOrient,
  setOrient,
  setOrientToAuto,
  setOrientToAngle,
  getGradientUnits,
  getGradientTransform,
  getSpreadMethod,
  SvgLinearGradientElementGetX1,
  SvgLinearGradientElementGetY1,
  SvgLinearGradientElementGetX2,
  SvgLinearGradientElementGetY2,
  SvgRadialGradientElementGetCx,
  SvgRadialGradientElementGetCy,
  SvgRadialGradientElementGetR,
  getFx,
  getFy,
  getFr,
  getOffset,
  getPatternUnits,
  getPatternContentUnits,
  getPatternTransform,
  SvgPatternElementGetX,
  SvgPatternElementGetY,
  SvgPatternElementGetWidth,
  SvgPatternElementGetHeight,
  SvgScriptElementGetType,
  SvgScriptElementSetType,
  SvgScriptElementGetCrossOrigin,
  SvgScriptElementSetCrossOrigin,
  getTarget,
  getDownload,
  setDownload,
  getPing,
  setPing,
  getRel,
  setRel,
  getRelList,
  getHreflang,
  setHreflang,
  SvgaElementGetType,
  SvgaElementSetType,
  getReferrerPolicy,
  setReferrerPolicy
};
